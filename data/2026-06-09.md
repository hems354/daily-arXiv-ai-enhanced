<div id=toc></div>

# Table of Contents

- [cs.CV](#cs.CV) [Total: 189]
- [eess.SP](#eess.SP) [Total: 21]
- [q-bio.NC](#q-bio.NC) [Total: 2]
- [eess.IV](#eess.IV) [Total: 7]
- [cs.LG](#cs.LG) [Total: 188]


<div id='cs.CV'></div>

# cs.CV [[Back]](#toc)

### [1] [Page image classifier fine-tuned on century-spanning archives of scanned documents for further content-specific processing](https://arxiv.org/abs/2606.07558)
*Kateryna Lutsai,Pavel Straňák,David Novák,Dana Křivánková*

Main category: cs.CV

TL;DR: 本文针对人文学科数字化项目中历史文档图像的大规模自动分类需求，构建了包含48,000余张标注图像的捷克考古档案数据集，比较了传统特征工程、CNN、Transformer及多模态CLIP模型。RegNetY-16GF以99.16%的Top-1准确率实现近乎完美的分类，并在64.9万张未标注页面上展现出超过90%的模型间一致性，最终模型与数据集均以开源形式发布。

<details>
  <summary>Details</summary>

Motivation: 人文学科数字化项目产生海量异构历史档案，人工按视觉内容类型（文本、表格、图形）进行分类在大规模场景下不可行，亟需自动化分类系统以支持后续内容特定的下游处理（如OCR或结构化数据提取）。

Method: 构建了包含48,000余张标注历史页面图像的数据集，经四轮领域专家审核标注。以随机森林分类器（基于手工图像特征）作为基准，随后微调并比较了多种深度学习架构：CNN（EfficientNetV2, RegNetY）、视觉与文档图像Transformer（ViT, DiT）以及多模态CLIP模型。采用与领域专家共同设计的11类别标签方案，通过五折交叉验证进行评估。

Result: 基于特征的基线方法准确率约75%。微调后的CNN和Transformer显著超越基线：RegNetY-16GF达99.16%，ViT-large达99.12% Top-1准确率。CLIP ViT-B/16在优化文本描述后达99.14%。在649,508张未标注档案页面上，纯图像模型间一致性超过90%；但微调CLIP与纯图像模型的一致性低于65%，不适合部署。

Conclusion: 纯图像模型（尤其是RegNetY-16GF）能够实现近乎完美的分类准确率，并在大规模未标注数据上保持高度一致。微调CLIP尽管在测试集上表现优异，但在未标注数据上与纯图像模型的一致性差，不适宜实际部署。最终模型、标注数据集及软件均以开源形式发布。

Original Abstract: Purpose: Digitization projects in the humanities produce vast, heterogeneous archives of historical documents, making manual sorting impractical at scale. This work addresses the need for an automated system to classify scanned page images based on visual content type - text, tables, and graphics - enabling content-specific downstream processing such as Optical Character Recognition (OCR) or structured data extraction. Methods: An image classification system was developed and evaluated on a dataset of over 48,000 annotated historical page images from century-old Czech archaeological archives, refined through four successive annotation stages with domain-expert review. A Random Forest Classifier baseline was established using hand-crafted image features. Subsequently, deep learning architectures were fine-tuned and compared: Convolutional Neural Networks (EfficientNetV2, RegNetY), Vision and Document Image Transformers (ViT, DiT), and multimodal CLIP models. An 11-category label scheme was designed collaboratively with domain experts and evaluated via five-fold cross-validation. Results: The feature-based baseline achieved approximately 75% accuracy. Fine-tuned CNNs and Transformers substantially outperformed it, with RegNetY-16GF achieving 99.16% and ViT-large 99.12% Top-1 accuracy on the held-out test set. CLIP ViT-B/16 reached 99.14% with optimized text descriptions. Conclusion: Image-only models, particularly RegNetY-16GF, deliver near-perfect classification accuracy and produce consistent labels across 649,508 unlabeled archival pages with over 90% inter-model agreement. Fine-tuned CLIP, despite competitive test-set accuracy, showed under 65% agreement with image-only models on unlabeled data, making it less suitable for deployment. The final models, annotated dataset, and software are publicly available under open-source licenses.

中文摘要翻译: 目的：人文学科的数字化项目产生了海量异构的历史档案文献，使得人工分类在大规模场景下不可行。本研究旨在解决基于视觉内容类型（文本、表格和图形）对扫描页面图像进行自动分类的需求，从而支持内容特定的下游处理，如光学字符识别（OCR）或结构化数据提取。方法：我们开发并评估了一个图像分类系统，数据集包含超过48,000张来自百年历史的捷克考古档案的标注历史页面图像，经过四轮连续的标注阶段并由领域专家审核。建立了基于手工图像特征的随机森林分类器作为基线。随后，微调并比较了多种深度学习架构：卷积神经网络（EfficientNetV2, RegNetY）、视觉和文档图像Transformer（ViT, DiT）以及多模态CLIP模型。与领域专家协作设计了11类别标签方案，并通过五折交叉验证进行评估。结果：基于特征的基线达到了约75%的准确率。微调后的CNN和Transformer显著优于基线，其中RegNetY-16GF在保留测试集上达到99.16%的Top-1准确率，ViT-large达到99.12%。CLIP ViT-B/16在优化文本描述后达到了99.14%的Top-1准确率。结论：纯图像模型，尤其是RegNetY-16GF，能够实现近乎完美的分类准确率，并在649,508张未标注档案页面上产生一致的标签，模型间一致性超过90%。微调后的CLIP尽管在测试集上表现具有竞争力，但在未标注数据上与纯图像模型的一致性低于65%，因此不适合部署。最终模型、标注数据集和软件均以开源许可协议公开发布。

</details>


### [2] [Multimodal Group Emotion Recognition In-the-Wild Towards a Privacy-Safe Non-Individual Approach](https://arxiv.org/abs/2606.07585)
*Anderson Augusma*

Main category: cs.CV

TL;DR: 本文针对野外群体情绪识别（GER）中的隐私保护问题，提出了两种互补框架：跨注意力多模态音频-视频融合架构（结合帧注意力池化）和变分编码器多解码器（VE-MD）架构。实验表明，在不使用个体特征作为输入的情况下，仍能达到有竞争力的性能，同时有效降低个体监控风险。

<details>
  <summary>Details</summary>

Motivation: 传统的情绪识别方法依赖个体级线索（如面部、视线或语音分析），存在个体监控和监视的风险。本文旨在通过利用集体音频-视频信号在群体层面推断情绪，从而在保护隐私的前提下实现有效的群体情绪识别。

Method: 提出两种互补框架：(1) 跨注意力多模态音频-视频融合架构，结合帧注意力池化（FAP）进行时间聚合，并采用合成数据增强；(2) 变分编码器多解码器（VE-MD）架构，学习用于情绪分类和结构表征预测（包括身体和面部线索）的共享潜在空间，探索了基于DETR和基于热图的两种解码策略。

Result: 通过消融研究验证了方法的鲁棒性；两种解码策略分别分析了结构表征在群体和个体设置中的作用；实验表明在不使用个体特征作为输入数据的情况下仍能达到有竞争力的性能。

Conclusion: 三个主要贡献：(1) 阐明了多模态和结构线索在群体级情感计算中的作用；(2) 提出了两种用于隐私保护的多模态群体情绪识别架构；(3) 证明了无需个体特征作为输入即可实现有竞争力的性能。

Original Abstract: This thesis addresses group emotion recognition (GER) in-the-wild with a focus on privacy preservation. Unlike traditional emotion recognition methods that rely on individual-level cues such as face, gaze, or voice analysis, this work uses collective audio-video signals to infer emotions at the group level, reducing risks of individual monitoring and surveillance. Two complementary frameworks are proposed. The first is a cross-attention multimodal architecture for audio-video fusion, combined with Frames Attention Pooling (FAP) for temporal aggregation. It is supported by synthetic data augmentation and validated through ablation studies, demonstrating robustness in real-world GER conditions. The second framework, Variational Encoder Multi-Decoder (VE-MD), learns a shared latent space for emotion classification and structural representation prediction, including body and face cues. Two decoding strategies, DETR-based and heatmap-based, are explored to analyze the role of structural representations in group and individual settings. The thesis makes three main contributions: it clarifies the role of multimodality and structural cues in group-level affective computing; introduces two architectures for privacy-preserving multimodal GER; and shows that competitive performance can be achieved without using individual features as input data.

中文摘要翻译: 本论文针对野外场景下的群体情绪识别（GER）问题，重点关注隐私保护。与依赖个体级线索（如面部、视线或语音分析）的传统情绪识别方法不同，本文利用集体音频-视频信号在群体层面推断情绪，从而降低个体监控和监视的风险。本文提出了两种互补框架。第一种是用于音频-视频融合的跨注意力多模态架构，结合帧注意力池化（FAP）进行时间聚合。该架构得到合成数据增强的支持，并通过消融研究验证，在真实世界群体情绪识别条件下展现出鲁棒性。第二种框架——变分编码器多解码器（VE-MD）——学习一个共享潜在空间，用于情绪分类和结构表征预测（包括身体和面部线索）。本文探索了基于DETR和基于热图的两种解码策略，以分析结构表征在群体和个体设置中的作用。本论文的三大贡献包括：阐明了多模态和结构线索在群体级情感计算中的作用；提出了两种用于隐私保护的多模态群体情绪识别架构；证明了在不使用个体特征作为输入数据的情况下，仍能达到有竞争力的性能。

</details>


### [3] [SlideCheck: Guiding Self-Supervised Pretraining of Pathology Foundation Models via Dataset Distributions](https://arxiv.org/abs/2606.07590)
*Mingyi He,Xinyi Guo,Xitong Ling,Weiming Chen,Jiawen Li,Lianghui Zhu,Minxi Ouyang,Mingxi Fu,Yizhi Wang,Tian Guan*

Main category: cs.CV

TL;DR: SlideCheck是一种轻量级预训练数据指导工具，基于冻结的病理基础模型patch特征，通过双头MLP分别建模异常形态和恶性证据，利用评分-注意力一致性和正则化特征空间评分器生成patch级异常/恶性分数，用于组织、过滤和审计病理预训练数据。实验表明，SlideCheck定义的数据分布能影响自监督ViT预训练的下游行为，筛选子集可接近全数据性能。

<details>
  <summary>Details</summary>

Motivation: 病理基础模型通常在大量WSI patch上预训练，但数据构建时的监督信号往往是切片级别的、稀疏的或异质的，导致难以理解和控制哪些生物模式进入预训练数据。现有方法缺少一个轻量级的工具来对预训练数据进行组织和审计。

Method: SlideCheck使用双头MLP分别建模广泛的异常形态和恶性证据：(1) 正则化特征空间评分器为patch级证据估计提供监督锚点；(2) 评分-注意力一致性机制将patch分数与WSI级MIL注意力结合，挖掘高置信度伪标签。随后利用这些分数构建广泛正样本的ViT预训练子集，当异常或恶性证据超过阈值时选择该patch。

Result: 实验表明，SlideCheck定义的数据分布能够影响自监督ViT预训练的下游行为，说明生物组成是病理基础模型开发中重要的可控因素。筛选后的子集可以接近全数据训练的性能，表明显式评分的patch池可以支持更高效、可审计的预训练数据构建。

Conclusion: SlideCheck定位为一种数据指导和审计层，能够将大量无差别的patch池转化为可控且可复用的预训练数据集，为病理基础模型的数据构建提供了更高效、可审计的解决方案。

Original Abstract: Pathology foundation models are pretrained on large streams of WSI-derived patches, while supervision during data construction is often slide-level, sparse, or heterogeneous. This mismatch makes it difficult to understand and control which biological patterns enter the pretraining data. We propose SlideCheck, a lightweight pretraining data guidance tool built on frozen pathology foundation model patch features. Rather than serving as a standalone patch diagnostic model, SlideCheck provides explicit abnormality and malignancy scores for organizing, filtering, and auditing pathology pretraining data. SlideCheck uses a dual-head MLP to separately model broad abnormal morphology and malignant evidence. A regularized feature-space scorer provides a supervised anchor for patch-level evidence estimation, while score-attention agreement combines patch scores with WSI-level MIL attention to mine high-confidence pseudo labels. The same scores are then used to construct broad-positive ViT pretraining subsets, where a patch is selected if either abnormality or malignancy evidence exceeds a threshold. Experiments show that SlideCheck-defined data distributions influence the downstream behavior of self-supervised ViT pretraining, indicating that biological composition is an important controllable factor in pathology foundation model development. Curated subsets can approach full-data performance, suggesting that explicitly scored patch pools may support more efficient and auditable pretraining data construction. These findings position SlideCheck as a data guidance and auditing layer for transforming large, undifferentiated patch pools into controllable and reusable pretraining datasets.

中文摘要翻译: 病理基础模型在大量来自全切片图像（WSI）的补丁（patch）流上进行预训练，而数据构建过程中的监督信号通常是切片级别的、稀疏的或异质的。这种不匹配使得理解和控制哪些生物模式进入预训练数据变得困难。我们提出SlideCheck，一种轻量级的预训练数据指导工具，它建立在冻结的病理基础模型补丁特征之上。SlideCheck并非作为一个独立的补丁诊断模型，而是提供明确的异常评分和恶性评分，用于组织、筛选和审计病理预训练数据。SlideCheck使用双头多层感知机（MLP）分别建模广泛的异常形态和恶性证据。一个正则化的特征空间评分器为补丁级别的证据估计提供监督锚点，同时评分-注意力一致性机制将补丁分数与全切片图像级别的多实例学习（MIL）注意力相结合，以挖掘高置信度的伪标签。随后，相同的分数被用于构建广泛正样本的ViT预训练子集，其中当异常或恶性证据超过阈值时，选择该补丁。实验表明，SlideCheck定义的数据分布会影响自监督ViT预训练的下游行为，表明生物组成是病理基础模型开发中一个重要的可控因素。筛选后的子集可以接近全数据训练的性能，这表明显式评分的补丁池可以支持更高效、可审计的预训练数据构建。这些发现将SlideCheck定位为一种数据指导和审计层，用于将大量无差别的补丁池转化为可控且可复用的预训练数据集。

</details>


### [4] [Simultaneous hyperkinetic movement disorders phenotyping: a cross-cohort pediatric transfer study using routine videos, markerless pose estimation and a tabular foundation model](https://arxiv.org/abs/2606.07674)
*Laura Cif,Diane Demailly,Zohra Souei,Muhammad Mushhood Ur Rehman,Juan Dario Ortigoza Escobar,Mayté Castro Jiménez,Cécile A. Hubsch,Sophie Huby,Morgan Dornadic,Gun-Marie Hariz,Eduardo M. Moraud,Jocelyne Bloch,Gabriella A. Horvath,Xavier Vasques*

Main category: cs.CV

TL;DR: 本文提出并外部验证了一个基于视频的框架，利用常规临床记录同时检测多种过度运动障碍（如肌张力障碍、震颤、肌阵挛、舞蹈症、手足徐动症、投掷症、刻板行为和抽动症）。该框架结合无标记姿态估计、运动学描述符和预训练基础模型，在成人队列上训练后，通过轻量级校准迁移至儿科真实世界样本，显著提升了分类性能（Jaccard指数从0.548升至0.633）。

<details>
  <summary>Details</summary>

Motivation: 过度运动障碍（HDs）的多种现象学（如肌张力障碍、震颤等）常同时出现，临床鉴别困难。现有方法多针对单一运动障碍，缺乏能同时检测多种现象学且可跨人群（成人到儿童）迁移的视频框架。

Method: 1) 使用无标记姿态估计提取患者运动关键点；2) 计算运动学描述符；3) 结合预训练基础模型构建共享预测主干；4) 在21名成人患者和4名健康对照上训练；5) 外部验证于12名儿科患者，不重新训练主干，仅通过临床医生选择的小样本进行轻量级校准（仅调整最终受试者级决策步骤）。

Result: 在保留的儿科患者（n=7）上：Hamming准确率从0.804提升至0.839，Jaccard指数从0.548提升至0.633。在临床医生诊断一致性更高的现象学子集上，Hamming准确率达0.9，Jaccard指数达0.786，表明性能提升不依赖于最不可靠的标签。

Conclusion: 该视频框架能够利用常规临床记录同时检测多种过度运动障碍现象学，并成功实现从成人队列到儿科人群的跨队列迁移，通过轻量级校准即可获得显著性能提升，具有良好的临床实用性和泛化能力。

Original Abstract: Objective: To develop and externally test a video-based framework for simultaneous detection of hyperkinetic MDs phenomenologies: dystonia, tremor, myoclonus, chorea, athetosis, ballismus, stereotypies, and tics using routine clinical recordings, with explicit testing of external, cross-cohort transfer from adult to pediatric populations. Methods: In this proof-of-concept study, the framework combines markerless pose estimation, kinematic descriptors, and a pretrained fondation model. A shared predictive backbone was developed on 21 adults with confirmed hyperkinetic MDs and 4 healthy controls assessed under a standardized protocol. External validation was performed on an independent external cohort: a real-world pediatric sample (n=12, monogenic combined MDs). For the external dataset, the backbone was deployed without retraining; lightweight calibration adjusted only the final subject-level decision step using a small labeled subset of patients selected by clinicians as representative of the cohort's phenotypic range. Results: After local calibration of the decision layer on the clinician-selected subset, performance improved consistently on the held-out pediatric patients (n=7): Hamming accuracy rose from 0.804 to 0.839 and the Jaccard index from 0.548 to 0.633. This calibrated performance was preserved, and the Jaccard index further improved, when the evaluation was restricted to the phenomenologies with more definite clinician agreement (Hamming accuracy 0.9, Jaccard index 0.786), indicating that the gains did not rest on the least-reliable labels.

中文摘要翻译: 目的：开发并外部测试一个基于视频的框架，用于利用常规临床记录同时检测过度运动障碍（HDs）的多种现象学：肌张力障碍、震颤、肌阵挛、舞蹈症、手足徐动症、投掷症、刻板行为和抽动症，并明确测试从成人到儿科人群的外部跨队列迁移能力。方法：在这项概念验证研究中，该框架结合了无标记姿态估计、运动学描述符和预训练基础模型。在21名确诊的过度运动障碍成人患者和4名健康对照者的标准化评估方案数据上开发了共享预测主干。外部验证在一个独立的真实世界儿科队列（n=12，单基因复合型运动障碍）上进行。对于外部数据集，主干未经重新训练直接部署；轻量级校准仅调整了最终的受试者级决策步骤，使用临床医生选择的小标记子集（代表队列表型范围）。结果：在临床医生选择的子集上进行决策层局部校准后，保留的儿科患者（n=7）性能持续改善：Hamming准确率从0.804提升至0.839，Jaccard指数从0.548提升至0.633。当评估限定于临床医生诊断一致性更高的现象学时，校准后的性能得以保持且Jaccard指数进一步改善（Hamming准确率0.9，Jaccard指数0.786），表明性能提升不依赖于最不可靠的标签。

</details>


### [5] [A Mechanistic Analysis of Adversarial Fine-tuning of Vision Transformers](https://arxiv.org/abs/2606.07593)
*Hannah Gao,Isha Agarwal,Dylan Hadfield-Menell,Rachel Ma*

Main category: cs.CV

TL;DR: 本文通过机制分析视角，研究了对抗性微调对ViT在受扰动图像上性能的影响，发现虽然针对特定扰动的微调能提升模型在该类扰动上的表现，但这种提升无法泛化到未见过的扰动类别，且对抗训练并未改变ViT学习到的稀疏表示的本质。

<details>
  <summary>Details</summary>

Motivation: 图像分类模型在高风险现实场景中需要抵御输入图像的扰动（如模糊、锐化），但视觉Transformer（ViT）在鲁棒性方面的研究尚不充分，尤其缺乏对其内部机制的理解。

Method: 对ViT进行针对低频率和高频率图像损坏的对抗性微调，然后通过分析模型的注意力机制、内部表示和知识演化，从机制层面解释下游模型性能的变化。

Result: 1）对常见损坏输入进行微调能提升模型在新损坏数据上的性能和置信度，但这种提升无法迁移到训练中未见的其他损坏类别；2）尽管观察到各层的视觉注意力和知识演化发生了变化，但对抗训练并未导致ViT学习的稀疏表示发生根本性改变。

Conclusion: 对抗性微调对ViT鲁棒性的改善是有限的、任务特定的，无法泛化到未知扰动类型，且模型底层的稀疏表示机制并未因此发生本质变化。

Original Abstract: The widespread use of image classification models in high-risk, real-world situations necessitates making these models robust to slight disturbances or perturbations, such as blurring or sharpening, in the input images. While vision transformers (ViTs) play an integral role in many modern-day multi-modal models like Vision-Language-Models (VLMs) and Vision-Language-Action (VLA) models, they have received a lack of attention in the setting of robustness. In this work, we analyze the effects of adversarial fine-tuning, a popular method for improving model robustness to image perturbations, on a ViT's performance on perturbed and regular images through a mechanistic lens. We adversarially train a ViT on low-frequency and high-frequency image corruptions, and attempt to explain changes in downstream model performance through an examination of the model's attention mechanisms, internal representations, and knowledge evolution. Overall, our results suggest that, while fine-tuning on inputs with common corruptions improves model performance and certainty on new instances of corrupted data, these improvements do not transfer to other classes of corruptions not seen in the training. Additionally, despite observing changes in visual attention and knowledge evolution across layers, we found that adversarial training did not lead to fundamental changes in the sparse representations learned by ViTs.

中文摘要翻译: 图像分类模型在高风险现实场景中的广泛应用，要求这些模型能够抵御输入图像中轻微扰动（如模糊或锐化）的影响。尽管视觉Transformer（ViT）在现代多模态模型（如视觉-语言模型和视觉-语言-动作模型）中扮演着不可或缺的角色，但它们在鲁棒性方面受到的关注仍然不足。本文从机制分析的视角，研究了对抗性微调（一种提升模型对图像扰动鲁棒性的常用方法）对ViT在受扰动图像和常规图像上性能的影响。我们针对低频率和高频率图像损坏对ViT进行对抗性训练，并通过分析模型的注意力机制、内部表示和知识演化，尝试解释下游模型性能变化的原因。总体而言，我们的结果表明：虽然对包含常见损坏的输入进行微调能够提升模型在新损坏数据上的性能和置信度，但这些改进无法迁移到训练中未见的其他损坏类别。此外，尽管我们观察到各层的视觉注意力和知识演化发生了变化，但对抗训练并未导致ViT学习的稀疏表示发生根本性改变。

</details>


### [6] [VisualLeakBench: Reproducible Action-Boundary Propagation Failures in Vision-Language Agents](https://arxiv.org/abs/2606.07595)
*Youting Wang,Yuan Tang,Yitian Qian,Chen Zhao*

Main category: cs.CV

TL;DR: 本文研究了视觉-语言智能体在读取图像后，将其中敏感或危险文本复制到下游工具参数中的“动作边界传播”失败模式，并提出了VisualLeakBench基准来量化该问题。

<details>
  <summary>Details</summary>

Motivation: 现有视觉-语言智能体在执行截图、文档等图像理解任务时，会将图像中的敏感文本传递到工具调用参数中，导致隐私泄露或安全风险，但目前缺乏系统性的评估基准来分析这一失败模式。

Method: 构建了包含500张图像（涵盖UI、聊天、文档、表单、仪表盘五种场景）的VisualLeakBench基准，选取100张图像的子集，在两种工作流（笔记捕获和外部交接）下，使用四种生产级VLM系统进行测试。通过防御性系统提示和标注目标的oracle上界诊断来分析视觉到工具的传播行为。

Result: 基线设置下，PII目标字符串在78.8%的案例中被传播到工具参数，渲染危险文本为85.5%。防御性提示使PII传播降至2.0%（主要通过抑制工具使用），但渲染危险文本传播仍高达52.6%。工具表面依赖性明显：搜索类工具抑制PII传播，但渲染危险文本仍跨越工具边界。

Conclusion: 视觉-语言智能体存在严重的动作边界传播风险，防御性提示对PII有效但对渲染危险文本效果有限，且以牺牲工具可用性为代价。主要失败发生在工具边界，响应侧泄漏是残余风险。

Original Abstract: Vision-language agents increasingly consume screenshots, documents, and user interfaces before writing to memory, sending messages, or invoking external tools. We study a concrete failure mode in this setting: action-boundary propagation, where sensitive or unsafe visible text is copied from an image into downstream tool arguments. We present VisualLeakBench, a diversified 500-image benchmark spanning UI, chat, document, form, and dashboard scenes, and evaluate a stratified 100-image agent subset with four production VLM systems under two workflows: note capture and external handoff. At baseline, target strings are propagated into tool arguments in 78.8% of PII cases and 85.5% of rendered unsafe-text cases. Under a defensive system prompt, rendered unsafe-text propagation remains high at 52.6%, while PII tool propagation falls to 2.0%, largely by suppressing tool use rather than preserving utility. Rates are tool-surface dependent: search-like tools suppress PII propagation, but rendered unsafe text still crosses tool boundaries. We measure visual-to-tool propagation rather than downstream instruction execution. We additionally provide a labeled-target oracle upper-bound diagnostic that localizes most failures at the tool boundary while leaving response-side leakage as residual risk.

中文摘要翻译: 视觉-语言智能体越来越多地消费截图、文档和用户界面，然后写入内存、发送消息或调用外部工具。我们研究了这一设置中的一种具体失败模式：动作边界传播（action-boundary propagation），即敏感或危险的可见文本从图像中被复制到下游工具参数中。我们提出了VisualLeakBench，一个涵盖UI、聊天、文档、表单和仪表盘场景的多样化500图像基准，并在两个工作流（笔记捕获和外部交接）下，使用四种生产级VLM系统对分层抽样的100图像智能体子集进行评估。在基线设置下，目标字符串在78.8%的PII案例和85.5%的渲染危险文本案例中被传播到工具参数中。在使用防御性系统提示时，渲染危险文本的传播率仍高达52.6%，而PII的工具传播率降至2.0%，这主要通过抑制工具使用而非保持实用性来实现。传播率依赖于工具表面：类似搜索的工具抑制了PII传播，但渲染危险文本仍然跨越工具边界。我们衡量的是视觉到工具的传播，而非下游指令执行。我们还提供了一个带标注目标的oracle上界诊断，将大多数失败定位在工具边界，而将响应侧泄漏作为残余风险。

</details>


### [7] [Can You Trust What You See? Human and AI Detection of Synthetic Legal Evidence](https://arxiv.org/abs/2606.07613)
*Jinzhe Tan,Ali Ekber Cinar,Karim Benyekhlef*

Main category: cs.CV

TL;DR: 本文研究了人类和前沿多模态大语言模型（MLLM）在民事纠纷常见的以物体为中心的视觉证据中，区分真实照片与AI生成图像的能力。实验发现人类准确率仅64.8%，MLLM虽能100%识别真实图像但漏检大量AI生成图像，两者均不可作为独立认证工具，需结合人工审查、MLLM筛查和溯源基础设施。

<details>
  <summary>Details</summary>

Motivation: 长期以来视觉证据被视为可靠的法庭证明形式，但AI生成技术的进步正在动摇这一假设。本文旨在检验人类和前沿多模态大语言模型能否有效区分真实证据照片与AI生成的伪造图像。

Method: 构建了SLED-1400数据集（200张真实证据图像+1200张由6种文本到图像生成器合成的图像，涵盖10个证据类别）；通过受控网络实验（136名非专业参与者）和标准化评估（4种MLLM：GPT-5.1、Gemini-3-Pro、Gemini-3-Flash、Qwen3-VL-235B）进行对比测试。

Result: 人类总体准确率64.8%，在最强生成器（Gemini-3-Pro-Image和Flux-2-Max）上准确率仅48.5%和51.0%，与随机猜测无异；MLLM对真实图像100%特异度，但对较难生成器合成的图像平均检测率仅5.9%；人类与MLLM的错误模式基本不相关，而各MLLM之间高度相关。

Conclusion: 人类和MLLM均不可作为可靠的独立认证工具。视觉证据在法律程序中应被视为内在可质疑的，可行的程序性应对措施需结合训练有素的人工审查、MLLM筛查以及C2PA Content Credentials等溯源基础设施。

Original Abstract: Visual evidence has long been treated as a reliable form of legal proof, but advances in artificial intelligence (AI) are undermining that assumption. This article asks how well humans and frontier multimodal large language models (MLLMs) can distinguish authentic evidentiary photographs from AI-generated counterparts in the object-centric scenarios typical of civil disputes. We built Synthetic Legal Evidence Detection (SLED-1400), a dataset of 200 authentic evidence images paired with 1,200 synthetic counterparts produced by six contemporary text-to-image generators across ten evidence categories. The same stimuli and response format were used in a controlled web experiment with 136 lay participants and in a standardized evaluation of four MLLMs (GPT-5.1, Gemini-3-Pro, Gemini-3-Flash, Qwen3-VL-235B). Human accuracy was 64.8% overall, and 48.5% and 51.0% on the two strongest generators (Gemini-3-Pro-Image and Flux-2-Max), indistinguishable from chance. MLLMs never misclassified an authentic image (100% specificity), but missed most synthetic outputs from the harder generators, with average MLLM detection at 5.9% on Gemini-3-Pro-Image outputs. Human and MLLM errors were largely uncorrelated, while the four MLLMs were strongly correlated with each other. Neither group is a reliable standalone authenticator. We argue that visual evidence in legal proceedings should be treated as inherently contestable, and that a workable procedural response must combine trained human review, MLLM screening, and provenance infrastructure such as C2PA Content Credentials.

中文摘要翻译: 视觉证据长期以来被视为一种可靠的法庭证明形式，但人工智能（AI）的进步正在削弱这一假设。本文探究了在民事纠纷中常见的以物体为中心的场景下，人类和前沿多模态大语言模型（MLLMs）区分真实证据照片与AI生成对应图像的能力。我们构建了合成法律证据检测数据集（SLED-1400），该数据集包含200张真实证据图像及由六种当代文本到图像生成器生成的1200张合成对应图像，涵盖十个证据类别。我们在受控网络实验中以相同的刺激和响应格式对136名非专业参与者进行了测试，并对四种MLLM（GPT-5.1、Gemini-3-Pro、Gemini-3-Flash、Qwen3-VL-235B）进行了标准化评估。人类总体准确率为64.8%，在两个最强生成器（Gemini-3-Pro-Image和Flux-2-Max）上的准确率分别为48.5%和51.0%，与随机猜测无异。MLLM从未错误分类过真实图像（100%特异度），但漏检了大部分来自较难生成器的合成输出，MLLM对Gemini-3-Pro-Image输出的平均检测率仅为5.9%。人类和MLLM的错误模式基本不相关，而四种MLLM之间则高度相关。两个群体均不能作为可靠的独立认证工具。我们认为，法律程序中的视觉证据应被视为内在可质疑的，而可行的程序性应对措施必须结合训练有素的人工审查、MLLM筛查以及C2PA Content Credentials等溯源基础设施。

</details>


### [8] [SENTRY: Statistical Reliability Analysis of Vision Transformers Under Soft Errors](https://arxiv.org/abs/2606.07620)
*Pramit Kumar Bhaduri,Mahdi Taheri,Samira Nazari,Maksim Jenihhin,Christian Herglotz,Michael Hubner*

Main category: cs.CV

TL;DR: 本文提出了一种基于有限总体抽样理论的统计故障注入框架，用于在安全关键领域（如自动驾驶和医学成像）中高效评估Vision Transformers（ViT）的软错误可靠性。该方法仅需数千个样本即可在99%置信度下将故障率误差控制在1%以内，实验成本降低高达10,700倍。研究发现ViT存在高度非均匀的可靠性分布：仅3%的FP32比特翻转导致故障，但这些故障绝大多数引发灾难性精度崩溃；脆弱性主要集中在归一化层和IEEE-754格式的关键指数位上。

<details>
  <summary>Details</summary>

Motivation: ViT在安全关键领域（如自动驾驶和医学成像）中的可靠性至关重要，但其庞大的参数量使得穷举故障注入实验不可行，迫切需要一种高效的可靠性评估方法。

Method: 提出了一种基于有限总体抽样理论的统计故障注入框架，利用统计学方法在少量样本下提供形式化的可靠性保证，从而替代不可行的穷举故障注入。

Result: 该方法仅需数千个样本即可在99%置信度下将故障率误差控制在1%以内，实验成本降低高达10,700倍。实验发现ViT存在高度非均匀的可靠性分布：仅3%的FP32比特翻转导致故障，但这些故障绝大多数引发灾难性精度崩溃；脆弱性主要集中在归一化层和IEEE-754格式的关键指数位上。

Conclusion: 本文为边缘部署的ViT架构提供了数学基础和可行的加固设计指导，揭示了ViT在软错误下的脆弱性分布规律，并为安全关键应用中的ViT可靠性评估提供了高效实用的方法论。

Original Abstract: With the growth of Vision Transformers in safety-critical domains like autonomous systems and medical imaging, ensuring their reliability against soft errors is paramount. While ViTs offer state-of-the-art accuracy, their massive parameter counts render exhaustive fault injection campaigns infeasible. To bridge this gap, a statistical fault injection framework is presented, leveraging finite-population sampling theory to provide formal reliability guarantees. It is demonstrated that failure rates are bounded within a 1% margin at 99\% confidence using only a few thousand samples, regardless of model scale. This methodology achieves up to a 10,700 times reduction in experimental cost compared to exhaustive approaches, while preserving the ability to localize vulnerabilities across architectural components. Through extensive evaluation of different architectures like ViT-Tiny and ViT-Small, a highly non-uniform reliability landscape is uncovered. It is shown that while only 3% of FP32 bit-flips result in failure, the vast majority of these events lead to catastrophic accuracy collapse. Specific vulnerabilities are localized to normalization layers and critical exponent bits within the IEEE-754 format, providing a mathematical foundation and actionable insights for the design of hardened, edge-deployed ViT architectures.

中文摘要翻译: 随着Vision Transformers在自动驾驶系统和医学成像等安全关键领域的广泛应用，确保其抵御软错误的可靠性变得至关重要。尽管ViT具有最先进的精度，但其庞大的参数量使得穷举故障注入实验不可行。为弥补这一差距，本文提出了一种统计故障注入框架，利用有限总体抽样理论提供形式化的可靠性保证。实验证明，无论模型规模如何，仅需数千个样本即可在99%置信度下将故障率误差控制在1%以内。与穷举方法相比，该方法实现了高达10,700倍的实验成本降低，同时保留了对各架构组件进行脆弱性定位的能力。通过对ViT-Tiny和ViT-Small等不同架构的广泛评估，揭示了一个高度非均匀的可靠性分布。研究发现，虽然仅3%的FP32比特翻转会导致故障，但这些事件中的绝大多数引发灾难性的精度崩溃。具体的脆弱性被定位到归一化层和IEEE-754格式中的关键指数位，为加固型边缘部署ViT架构的设计提供了数学基础和可行的指导。

</details>


### [9] [Real-Time Industrial Defect Detection on Edge Hardware Using Fine-Tuned YOLOv8: A Systematic Benchmark on the NEU Surface Defect Database and MVTec AD with Automotive & Battery Manufacturing Extensions](https://arxiv.org/abs/2606.07659)
*Emmanuel Ezeji Somtochukwu,Nitesh Rijal*

Main category: cs.CV

TL;DR: 本文提出Industrial-YOLO，一种基于YOLOv8架构的边缘优化框架，通过TensorRT和OpenVINO加速，在NVIDIA Jetson Orin平台上实现超过120 FPS的推理速度和98.5% mAP，用于高速制造环境中的实时表面缺陷检测。

<details>
  <summary>Details</summary>

Motivation: 高速制造环境中需要严格的表面缺陷检测，但深度学习模型在资源受限的边缘硬件上部署时面临延迟挑战，现有方法难以兼顾高精度和实时性。

Method: 基于YOLOv8架构进行微调，引入TensorRT和OpenVINO加速引擎进行目标特定的边缘优化，在NEU钢材表面缺陷数据集、MVTec AD数据集以及自建汽车制造异常数据集上进行系统评估。

Result: 在NVIDIA Jetson Orin平台上推理速度超过120 FPS，mAP达到98.5%；在真实汽车装配线上实现零延迟部署，展现出强鲁棒性。

Conclusion: Industrial-YOLO为下一代自动光学检测系统提供了可扩展的蓝图，能够在边缘硬件上同时实现高精度和实时性能。

Original Abstract: Automated surface defect detection is critical for ensuring rigorous quality control in high-speed manufacturing environments. While deep learning models offer remarkable accuracy, deploying them on resource-constrained edge hardware without introducing significant latency remains a persistent challenge. This paper presents Industrial-YOLO, an edge-optimized framework built upon a fine-tuned YOLOv8 architecture specifically engineered for real-time industrial defect detection. We conduct a systematic benchmark utilizing the NEU surface defect database for steel sheets and the MVTec AD dataset, supplemented with custom automotive manufacturing extensions representing real-world structural anomalies (scratches, pits, and inclusions). To bridge the gap between algorithmic complexity and edge hardware constraints, target-specific optimizations are introduced via TensorRT and OpenVINO acceleration engines. Experimental results demonstrate that Industrial-YOLO achieves a high-velocity inference speed exceeding 120 FPS on the NVIDIA Jetson Orin platform while maintaining an exceptional mean Average Precision (mAP) of 98.5%. The proposed framework showcases highly robust, zero-latency performance when deployed directly onto an active automotive assembly line, offering a scalable blueprint for next-generation automated optical inspection (AOI) systems.

中文摘要翻译: 自动化表面缺陷检测对于确保高速制造环境中的严格质量控制至关重要。虽然深度学习模型具有卓越的准确性，但在资源受限的边缘硬件上部署且不引入显著延迟仍是一个持续的挑战。本文提出Industrial-YOLO，一种基于微调YOLOv8架构的边缘优化框架，专为实时工业缺陷检测而设计。我们利用NEU钢材表面缺陷数据集和MVTec AD数据集进行系统性基准测试，并辅以代表真实世界结构异常（划痕、凹坑和夹杂物）的自定义汽车制造扩展数据集。为弥合算法复杂度与边缘硬件约束之间的差距，通过TensorRT和OpenVINO加速引擎引入了目标特定的优化。实验结果表明，Industrial-YOLO在NVIDIA Jetson Orin平台上实现了超过120 FPS的高速推理速度，同时保持了98.5%的卓越平均精度均值（mAP）。该框架在直接部署到活跃的汽车装配线上时展现出高度鲁棒、零延迟的性能，为下一代自动光学检测（AOI）系统提供了可扩展的蓝图。

</details>


### [10] [Eyes All Around: Design and Analysis of 360-Degree LiDAR Perception Using Equivariant Feature Learning in Unstructured Traffic](https://arxiv.org/abs/2606.07626)
*Pranav Darshan,Raghuveer Narayanan Rajesh,M Uttara Kumari*

Main category: cs.CV

TL;DR: 本文研究面向自动驾驶的360度LiDAR感知管线，提出结合扇区全景处理与旋转等变稀疏卷积的框架，并在印度城市交通数据集上验证，结果显示对车辆等大目标检测效果良好，但对行人和两轮车等小目标检测仍具挑战。

<details>
  <summary>Details</summary>

Motivation: 现有基于LiDAR的3D目标检测器多针对有限视场场景开发和评估，在全周360度感知下的行为尚不明确；同时密集无结构的城市交通场景中存在多种道路使用者、频繁遮挡、不规则运动模式等问题，亟需研究适用于全景感知的检测方法。

Method: 提出一种360度感知框架，结合扇区式全景处理（方位角扇区空间处理）与旋转等变稀疏卷积，实现全景感知和变换等变特征提取。

Result: 在自定义Ouster OS0 LiDAR数据集上，汽车检测效果最佳（92.02/90.51），公交车（80.53/76.34）和卡车（78.59/74.16）表现良好；行人（67.45/61.02）、自行车（73.21/69.54）和摩托车（71.20/68.13）检测得分较低，反映了小目标和可变性大的道路使用者在密集城市场景中的检测难度更大。

Conclusion: 所提出的360度感知框架在多个目标类别上表现稳定，其中大目标检测效果最好，而小目标和可变性大的目标检测仍是挑战。

Original Abstract: Perception in dense, unstructured urban traffic remains a major challenge for autonomous driving because of the wide variety of road users, frequent occlusions, irregular motion patterns, and the lack of standardized road layouts. Although recent LiDAR based 3D object detectors have shown strong performance in structured driving scenarios, most are developed and evaluated for limited field of view settings, and their behavior under full surround 360-degree sensing is still not well understood. This paper studies a 360-degree LiDAR perception pipeline for autonomous driving, with particular attention to panoramic sensing, azimuthal sector wise spatial processing, and transformation equivariant feature extraction in complex urban scenes. The paper presents a practical 360-degree perception framework that combines sector wise panoramic processing with rotation equivariant sparse convolutions and evaluates its behavior on a custom Ouster OS0 LiDAR dataset collected across diverse Indian urban traffic conditions. The results show generally stable detection across several object classes, with the strongest performance for cars at 92.02/90.51, buses at 80.53/76.34, and trucks at 78.59/74.16, while lower scores for pedestrians at 67.45/61.02, cyclists at 73.21/69.54, and motorcyclists at 71.20/68.13 reflect the greater difficulty of detecting smaller and more variable road users in dense urban scenes.

中文摘要翻译: 在密集无结构的城市交通中，由于道路使用者的多样性、频繁的遮挡、不规则的运动模式以及缺乏标准化的道路布局，感知仍然是自动驾驶面临的主要挑战。尽管近年来基于LiDAR的3D目标检测器在结构化驾驶场景中表现出色，但大多数都是在有限视场设置下开发和评估的，它们在全周360度感知下的行为尚不明确。本文研究了用于自动驾驶的360度LiDAR感知管线，特别关注复杂城市场景中的全景感知、方位角扇区空间处理和变换等变特征提取。本文提出了一种实用的360度感知框架，将扇区式全景处理与旋转等变稀疏卷积相结合，并在涵盖多种印度城市交通条件的自定义Ouster OS0 LiDAR数据集上评估其行为。结果表明，多个目标类别的检测总体稳定，其中汽车表现最佳（92.02/90.51），公交车（80.53/76.34）和卡车（78.59/74.16）表现良好，而行人的较低得分（67.45/61.02）、自行车（73.21/69.54）和摩托车（71.20/68.13）反映了在密集城市场景中检测更小、更多变的道路使用者的更大难度。

</details>


### [11] [LEGS: Laplacian-Enhanced Gaussian Splatting with a Nonlinear Weighted Loss](https://arxiv.org/abs/2606.07932)
*Yongfei Guo,Qizhou Huo,Xuan Sun,Yuanhao Gong*

Main category: cs.CV

TL;DR: 提出LEGS方法，用二阶拉普拉斯引导和非线性加权损失替代一阶梯度引导，在保持3DGS渲染管线不变的前提下提升结构感知能力，在多个数据集上PSNR提升最高达1.69 dB。

<details>
  <summary>Details</summary>

Motivation: 标准3DGS的光度损失对平坦区域和结构丰富区域同等处理，限制了锐利轮廓和精细细节的恢复；EGGS虽引入边缘引导但仅依赖一阶梯度响应和线性加权，仍存在不足。

Method: 用二阶拉普拉斯结构引导替代一阶梯度引导，将归一化的拉普拉斯响应通过非线性响应-权重函数映射为像素级权重，构建非线性加权损失函数，保持原始3DGS渲染管线不变。

Result: 在Tanks&Temples和Mip-NeRF360数据集上，相比3DGS提升PSNR最高1.68 dB，相比EGGS提升最高0.52 dB；集成到FastGS和FasterGS中进一步提升PSNR最高1.69 dB。

Conclusion: LEGS作为一种通用的损失级扩展方法，能有效提升各类高斯溅射管线的结构感知能力，在AR/VR、沉浸式可视化和实时3D内容生成等领域具有应用潜力。

Original Abstract: 3D Gaussian Splatting (3DGS) has become an efficient explicit representation for radiance field reconstruction and real-time novel view synthesis. However, its standard photometric loss treats flat and structure-rich regions similarly, which may limit the recovery of sharp contours and fine details. Edge-Guided Gaussian Splatting (EGGS) improves structure awareness through edge-guided weighting, but mainly relies on first-order gradient responses and linear weighting. In this paper, we propose LEGS, a Laplacian-Enhanced Gaussian Splatting method with a nonlinearly weighted loss. LEGS replaces first-order gradient guidance with second-order Laplacian structural guidance and maps the normalized Laplacian response into pixel-wise weights through nonlinear response-to-weight functions. The proposed loss improves structure-aware Gaussian optimization while keeping the original 3DGS rendering pipeline unchanged. Experiments on the full Tanks\&Temples and Mip-NeRF360 datasets show that LEGS improves peak signal-to-noise ratio (PSNR) by up to 1.68 dB over 3DGS and up to 0.52 dB over EGGS. Incorporating the proposed second-order nonlinear weighting strategy into FastGS and FasterGS further improves PSNR by up to 1.69 dB, demonstrating its effectiveness as a general loss-level extension for Gaussian Splatting pipelines with potential applications in AR/VR, immersive visualization, and real-time 3D content generation.

中文摘要翻译: 3D高斯溅射（3DGS）已成为辐射场重建和实时新颖视图合成的一种高效显式表示方法。然而，其标准光度损失对平坦区域和结构丰富区域同等处理，这可能限制锐利轮廓和精细细节的恢复。边缘引导高斯溅射（EGGS）通过边缘引导加权提升了结构感知能力，但主要依赖一阶梯度响应和线性加权。本文提出LEGS，一种带非线性加权损失的拉普拉斯增强高斯溅射方法。LEGS用二阶拉普拉斯结构引导替代一阶梯度引导，并通过非线性响应-权重函数将归一化的拉普拉斯响应映射为像素级权重。所提出的损失在保持原始3DGS渲染管线不变的前提下改进了结构感知的高斯优化。在完整Tanks&Temples和Mip-NeRF360数据集上的实验表明，LEGS相比3DGS峰值信噪比（PSNR）提升最高达1.68 dB，相比EGGS提升最高达0.52 dB。将该二阶非线性加权策略集成到FastGS和FasterGS中，PSNR进一步提升最高达1.69 dB，证明了其作为高斯溅射管线通用损失级扩展的有效性，在AR/VR、沉浸式可视化和实时3D内容生成等领域具有潜在应用价值。

</details>


### [12] [AMN: An Adaptive Multi-Scale Fusion Network with Boundary and Uncertainty Modeling for Nuclei Segmentation](https://arxiv.org/abs/2606.07633)
*Spoorthi M,Suja Palaniswamy*

Main category: cs.CV

TL;DR: 提出AMN（自适应多尺度细胞核网络），一种双编码器分割框架，融合Swin Transformer和ResNet-50特征金字塔，通过学习的逐通道门控机制动态加权，在多类细胞核分割任务上优于八种基线模型。

<details>
  <summary>Details</summary>

Motivation: 现有方法仅单独使用卷积或Transformer编码器，无法同时捕获细粒度局部纹理和长程空间上下文，限制了细胞核亚型分类的准确性。

Method: AMN采用双编码器结构（Swin Transformer + ResNet-50特征金字塔），通过可学习的逐通道门控机制在每个尺度动态融合两种编码器的贡献；训练使用多目标损失函数，包括类别加权焦点损失、边界感知损失和不确定性调制分类项。

Result: 在CoNIC基准上，AMN对七类细胞核达到平均Dice 0.82、平均F1 0.68（淋巴细胞类F1 0.67）；在MoNuSeg跨数据集评估中无需重新训练即展现强泛化能力，优于U-Net、ResU-Net、DeepLabV3+、SegNet、ViT-Small、HmsU-Net、ConvFormer-UNet和BEFUnet八种基线模型。

Conclusion: AMN通过自适应多尺度双编码器融合策略有效结合了CNN和Transformer的优势，在细胞核分割任务上取得最优性能，且学到的表示具有良好的跨数据集泛化性和领域鲁棒性。

Original Abstract: Accurate classification of nuclei subtypes in histopathology images is critical for downstream tasks including tumor grading, immune infiltrate quantification, and prognosis prediction. Existing approaches rely on either convolutional or transformer-based encoders in isolation, limiting their ability to simultaneously capture fine-grained local texture and long-range spatial context. We present AMN (Adaptive Multi-Scale Nuclei Network), a dual-encoder segmentation framework that jointly leverages a Swin Transformer and a ResNet-50 feature pyramid, fused via a learned per-channel gating mechanism that dynamically weighs each encoder's contribution at every scale. AMN is trained with a multi-objective loss combining class-weighted focal loss, boundary-aware loss with positive-pixel emphasis, and a novel uncertainty-modulated classification term that suppresses overconfident erroneous predictions. Evaluated on the CoNIC benchmark across seven nuclei classes, AMN achieves a mean Dice of 0.82 and mean F1 of 0.68, with an F1 of 0.67 on the diagnostically challenging lymphocyte class. AMN outperforms eight baseline models spanning pure-CNN, pure-transformer, and recent hybrid architectures: U-Net, ResU-Net, DeepLabV3+, SegNet, ViT-Small, HmsU-Net, ConvFormer-UNet, and BEFUnet. Cross-dataset evaluation on MoNuSeg demonstrates strong generalization without retraining and validating the domain robustness of the learned representations.

中文摘要翻译: 在组织病理学图像中准确分类细胞核亚型对于肿瘤分级、免疫浸润定量和预后预测等下游任务至关重要。现有方法依赖于单独使用卷积或基于Transformer的编码器，限制了它们同时捕获细粒度局部纹理和长程空间上下文的能力。我们提出了AMN（自适应多尺度细胞核网络），一种双编码器分割框架，它联合利用Swin Transformer和ResNet-50特征金字塔，通过一个可学习的逐通道门控机制在每个尺度上动态权衡每个编码器的贡献。AMN采用多目标损失函数进行训练，该损失函数结合了类别加权焦点损失、强调阳性像素的边界感知损失以及一个新颖的不确定性调制分类项，用于抑制过度自信的错误预测。在涵盖七类细胞核的CoNIC基准上评估，AMN实现了平均Dice 0.82和平均F1 0.68，其中在诊断上具有挑战性的淋巴细胞类别上F1达到0.67。AMN优于八种基线模型，涵盖纯CNN、纯Transformer以及最近的混合架构：U-Net、ResU-Net、DeepLabV3+、SegNet、ViT-Small、HmsU-Net、ConvFormer-UNet和BEFUnet。在MoNuSeg上的跨数据集评估表明，无需重新训练即可实现强大的泛化能力，验证了所学表示的领域鲁棒性。

</details>


### [13] [DAL-PCQA: Enabling Distortion-Level and Language-Driven Reasoning for Point Cloud Quality Assessment](https://arxiv.org/abs/2606.07938)
*Swarna Chakraborty,Gabriel De Castro Araújo,Syeda Tasmi Faria,Marcelo M. Carvalho,Mylene C. Q. Farias*

Main category: cs.CV

TL;DR: 本文提出DAL-PCQA数据集，通过多层级失真标签、质量类别和结构化自然语言描述，增强点云质量评估的可解释性，使模型能够像人类一样识别具体失真类型（如模糊、颜色偏移等），实验表明失真感知监督显著提升了多模态模型描述生成的质量。

<details>
  <summary>Details</summary>

Motivation: 现有PCQA方法仅预测平均意见得分（MOS），无法揭示感知质量下降的具体原因；而人类观察者会自然地以特定失真类型（如模糊、颜色偏移、点密度变化、缺失区域、几何变形等）进行推理。为弥合这一差距，需要构建包含结构化失真标签和自然语言描述的标记数据集。

Method: 构建DAL-PCQA数据集：在基准点云上增加多层级失真严重程度标签、离散质量类别，以及符合人类感知的结构化自然语言描述；定义涵盖光度与几何伪影的点云特定失真分类体系；采用零样本和微调的多模态模型生成感知质量描述。

Result: 统计分析揭示了不同失真类型和质量水平下的特征退化模式；实验表明，基于失真感知监督的多模态模型在词汇和语义对齐上显著优于未使用此类监督的模型，生成的描述与真实标注更一致。

Conclusion: DAL-PCQA通过支持可解释的、失真级别的推理，推动了语言驱动的、可解释的点云质量评估发展，数据集已公开提供。

Original Abstract: Point Cloud Quality Assessment (PCQA) methods typically predict scalar Mean Opinion Scores (MOS), which quantify overall perceptual degradation but do not reveal its causes. In contrast, human observers naturally reason in terms of specific distortions such as blur, color shifts, point density changes, missing regions, and geometric deformations. To close this gap, we introduce DAL-PCQA, a distortion-aware, language-annotated dataset for PCQA. DAL-PCQA augments benchmark point clouds with multi-level distortion severity labels, discrete quality categories, and structured natural language descriptions aligned with human perception. We define a point-cloud-specific distortion taxonomy that covers both photometric and geometric artifacts. Statistical analysis reveals characteristic degradation patterns across distortion types and quality levels. To assess the utility of these annotations, we compare zero-shot and fine-tuned multimodal models for generating perceptual quality descriptions. Experiments show that distortion-aware supervision substantially improves lexical and semantic alignment with ground-truth descriptions. By enabling interpretable, distortion-level reasoning, DAL-PCQA facilitates language-driven, explainable point cloud quality assessment. The dataset is publicly available at https://github.com/swarna96/DAL-PCQA.

中文摘要翻译: 点云质量评估（PCQA）方法通常预测平均意见得分（MOS），这类标量值量化了整体感知退化，但无法揭示其成因。相比之下，人类观察者会自然地以特定失真类型进行推理，例如模糊、颜色偏移、点密度变化、缺失区域和几何变形。为弥合这一差距，我们提出了DAL-PCQA，一个面向PCQA的失真感知、语言标注的数据集。DAL-PCQA为基准点云增加了多层级失真严重程度标签、离散质量类别，以及符合人类感知的结构化自然语言描述。我们定义了一个点云特定的失真分类体系，涵盖光度伪影和几何伪影。统计分析揭示了不同失真类型和质量水平下的特征退化模式。为评估这些标注的实用性，我们比较了零样本和微调后的多模态模型在生成感知质量描述方面的表现。实验表明，失真感知监督显著提高了生成描述与真实标注在词汇和语义上的对齐程度。通过支持可解释的、失真级别的推理，DAL-PCQA推动了语言驱动的、可解释的点云质量评估方法发展。该数据集已在https://github.com/swarna96/DAL-PCQA公开提供。

</details>


### [14] [NeuroAlign: Hierarchical Multimodal Fusion of Dynamic and Structural Neuroimaging for MCI Analysis](https://arxiv.org/abs/2606.07635)
*Xiongri Shen,Zhenxi Song,Jiaqi wang,Yi Zhong,Leilei Zhao,Chenqi Xu,Linling Li,Yichen Wei,Lingyan Liang,Demao Deng,Luping Song,Ping Luan,Ahmed M. Anter,Shuqiang Wang,Baiying Lei,Zhiguo Zhang*

Main category: cs.CV

TL;DR: NeuroAlign是一个用于多模态神经影像融合的结构化层次框架，通过双模态层次对齐和双域层次交互机制实现fMRI与DTI的有效融合，并在MCI/SCD检测中取得竞争性结果，同时提供了无梯度的归因分析方法以解释脑区模式。

<details>
  <summary>Details</summary>

Motivation: fMRI与DTI的多模态融合虽能提供互补信息用于认知障碍分析，但面临异质特征空间和表示不对齐的挑战，现有方法难以有效融合这两种模态的互补信息。

Method: 提出NeuroAlign层次化框架，包含：(1) 双模态层次对齐（DMHA），建模多尺度动态连接并对齐动态-静态与功能-结构嵌入；(2) 双域层次交互（DDHI），实现连接级与区域级特征的细粒度调制与全局交互；(3) 协同激活映射（SAM），一种无梯度、面向标记的归因方法，用于解释DFC、SFC、ALFF和FA特征。

Result: 在GUTCM、ADNI和OASIS三个数据集上采用五折验证，NeuroAlign在MCI/SCD检测中取得了竞争性性能，并展现出初步的跨数据集迁移能力。归因分析揭示了模态特异性和部分一致的脑区模式。

Conclusion: NeuroAlign有效解决了多模态神经影像融合中的特征空间异质性和表示不对齐问题，为多模态表征分析提供了模型层面的可解释性证据。

Original Abstract: Multimodal neuroimaging fusion of functional MRI (fMRI) and diffusion tensor imaging (DTI) provides complementary information for cognitive impairment analysis, but remains challenged by heterogeneous feature spaces and misaligned representations. We propose \textit{NeuroAlign}, a hierarchical framework for structured multimodal fusion. It introduces (1) \textit{Dual-Modal Hierarchical Alignment} (DMHA), which models multi-scale dynamic connectivity and aligns dynamic-static and functional-structural embeddings; and (2) \textit{Dual-Domain Hierarchical Interaction} (DDHI), which enables fine-grained modulation and global interaction between connectivity- and region-level features. To support feature-level inspection, we design \textit{Synergistic Activation Mapping} (SAM), a gradient-free, marker-oriented attribution method for DFC, SFC, ALFF, and FA. Evaluated on GUTCM, ADNI, and OASIS under five-fold validation, NeuroAlign achieves competitive MCI/SCD detection and preliminary cross-dataset transferability. Attribution analyses reveal modality-specific and partially consistent brain patterns, providing model-derived evidence for multimodal representation analysis.

中文摘要翻译: 功能磁共振成像（fMRI）与扩散张量成像（DTI）的多模态神经影像融合为认知障碍分析提供了互补信息，但一直受限于异质特征空间和不对齐的表征。本文提出NeuroAlign，一个用于结构化多模态融合的层次化框架。该框架引入：(1) 双模态层次对齐（DMHA），建模多尺度动态连接并对齐动态-静态与功能-结构嵌入；(2) 双域层次交互（DDHI），实现连接级与区域级特征之间的细粒度调制与全局交互。为了支持特征层面的可解释性分析，我们设计了协同激活映射（SAM），一种面向标记的无梯度归因方法，适用于DFC、SFC、ALFF和FA特征。在GUTCM、ADNI和OASIS数据集上的五折验证评估中，NeuroAlign在MCI/SCD检测任务上取得了竞争性性能，并展现出初步的跨数据集迁移能力。归因分析揭示了模态特异性以及部分一致的脑区模式，为多模态表征分析提供了模型驱动的证据。

</details>


### [15] [Crayotter: Traceable Multi-Agent Workflows for Long-Form Video Editing](https://arxiv.org/abs/2606.07636)
*Lecheng Yan,Yichong Zhang,Ben Pan,Xiaoyu Zheng,Jiawei Qian,Anqi Wu,Wenxi Li,Chenyang Lyu*

Main category: cs.CV

TL;DR: 本文提出Crayotter，一个开源的多模态多智能体系统，用于基于提示的长视频编辑。它将制作流程分为三个阶段：覆盖感知素材准备、基于伪影的编辑研究和工具驱动的时间线执行。每个阶段生成可检查的伪影（覆盖报告、多模态分析、编辑蓝图、工具调用和中间渲染），使编辑过程可追溯，允许对失败片段进行诊断和选择性修正。在23个编辑主题上的人工评估中，Crayotter平均得分为3.40/5，显著优于两个基线系统（2.44和1.70）。

<details>
  <summary>Details</summary>

Motivation: 现有长视频编辑系统在处理异构素材时缺乏对叙事意图的保持能力，失败时难以诊断和定位问题，通常需要完全重新开始编辑流程。

Method: Crayotter采用多智能体多模态架构，将视频编辑组织为三个阶段：(1) 覆盖感知素材准备——分析素材覆盖范围；(2) 基于伪影的编辑研究——生成多模态分析和编辑蓝图；(3) 工具驱动的时间线执行——实际执行编辑操作。每个阶段产出可检查的伪影（artifact），实现编辑过程的可追溯和选择性修正。

Result: 在23个编辑主题上的人工评估中，Crayotter平均得分为3.40/5，显著高于CapCut-Mate的2.44和CutClaw的1.70，在主题对齐、叙事连贯性和编辑平滑性等方面均有一致提升。

Conclusion: Crayotter通过引入可检查伪影的多阶段多智能体框架，有效提升了基于提示的长视频编辑的可追溯性和可控性，并为进一步的策略优化（如强化学习）提供了可回放轨迹模式和可验证奖励设计的基础。

Original Abstract: Editing a long-form video from heterogeneous footage requires more than selecting clips: an agent must preserve narrative intent across material preparation, timeline construction, post-production, and revision while leaving enough evidence to diagnose failures. We present \textbf{Crayotter}, an open-source multimodal multi-agent system for prompt-driven video editing. Crayotter organizes production into three phases: coverage-aware material preparation, artifact-based editing research, and tool-grounded timeline execution. Each phase externalizes inspectable artifacts, including coverage reports, multimodal analyses, editing blueprints, tool calls, and intermediate renders. These artifacts make an editing run traceable and allow failed segments to be diagnosed and selectively revised instead of requiring a full restart. We evaluate Crayotter on 23 editing themes against CapCut-Mate and CutClaw. Under human evaluation, Crayotter achieves an average score of 3.40/5, compared with 2.44 and 1.70 for the two baselines, with consistent gains in theme alignment, narrative coherence, and editing smoothness. We additionally describe a replayable trajectory schema and verifiable reward design that prepare these workflows for future policy optimization. Code, traces, and examples are publicly available at https://github.com/idwts/Crayotter.

中文摘要翻译: 从异构素材中编辑长视频不仅仅需要选择片段：一个智能体必须在素材准备、时间线构建、后期制作和修订过程中保持叙事意图，同时留下足够的证据用以诊断失败。我们提出\textbf{Crayotter}，一个开源的多模态多智能体系统，用于基于提示的视频编辑。Crayotter将制作过程组织为三个阶段：覆盖感知素材准备、基于伪影的编辑研究和工具驱动的时间线执行。每个阶段都会外化可检查的伪影，包括覆盖报告、多模态分析、编辑蓝图、工具调用和中间渲染。这些伪影使编辑过程可追溯，并允许对失败片段进行诊断和选择性修订，而无需完全重新开始。我们在23个编辑主题上对Crayotter进行了评估，并与CapCut-Mate和CutClaw进行了比较。在人工评估下，Crayotter的平均得分为3.40/5，而两个基线系统分别为2.44和1.70，在主题对齐、叙事连贯性和编辑平滑性方面均有一致提升。我们还描述了一种可回放轨迹模式和可验证奖励设计，为这些工作流未来的策略优化做好准备。代码、轨迹和示例可在https://github.com/idwts/Crayotter公开获取。

</details>


### [16] [Anchor-Conditioned Compositional Control for Landscape Image Generation](https://arxiv.org/abs/2606.07638)
*Gadha Lekshmi P,Govind Arun,Rohith Syam,Ahmed Elgammal*

Main category: cs.CV

TL;DR: 本文提出了一种锚点条件微调框架，通过四维组合锚向量注入扩散模型，实现了对景观图像生成中构图控制的精度提升，定量实验表明该方法在地平线检测率和三分法对齐度上均达到最优，且构图控制精度具有类别依赖性。

<details>
  <summary>Details</summary>

Motivation: 现有的图像生成模型虽然被广泛用作创意工具，但对摄影师和视觉艺术家日常使用的构图控制（如三分法、地平线位置等）支持有限，缺乏精细的构图控制能力。

Method: 提出了一种锚点条件微调框架，从训练图像中提取四维组合锚向量，通过解耦交叉注意力机制结合傅里叶编码和三路分类器自由引导丢弃（dropout），将该向量注入扩散模型。

Result: 该架构在基线及三种消融变体对比中取得最高地平线检测率（0.850）和最高三分法对齐度（0.817）；类别特异性消融实验表明，在构图同质的场景子集上训练可将地平线偏差最多降低40%。

Conclusion: 构图控制精度具有类别依赖性，即针对特定构图类型进行专门训练比混合训练效果更优。

Original Abstract: Image generative models, though widely used as creative tools, offer limited support for the kind of compositional control that photographers and visual artists routinely exercise. This paper presents early results on an anchor conditioned finetuning framework for landscape image generation, in which a four dimensional compositional anchor vector is extracted from training images and injected into a diffusion model via a decoupled cross attention mechanism with Fourier encoding and three way classifier free guidance dropout. Quantitative evaluation against a baseline and three ablation variants shows that the proposed architecture achieves the highest horizon detection rate of 0.850 and the highest rule of thirds alignment of 0.817. A category specific ablation further demonstrates that training on compositionally homogeneous scene subsets reduces horizon deviation by up to 40 percent compared to mixed training. This establishes that compositional control precision is category dependent.

中文摘要翻译: 图像生成模型尽管被广泛用作创意工具，但对摄影师和视觉艺术家日常使用的构图控制能力支持有限。本文提出了一个用于景观图像生成的锚点条件微调框架的初步结果，其中从训练图像中提取一个四维组合锚向量，并通过带有傅里叶编码和三路分类器自由引导丢弃（dropout）的解耦交叉注意力机制将其注入扩散模型。与基线及三种消融变体的定量评估表明，所提出的架构实现了最高的地平线检测率（0.850）和最高的三分法对齐度（0.817）。进一步的类别特异性消融实验表明，在构图同质的场景子集上训练可将地平线偏差比混合训练最多降低40%。这表明构图控制精度是类别依赖的。

</details>


### [17] [MOSS-Video-Preview: Toward Real-Time Video Understanding via Cross-Attention](https://arxiv.org/abs/2606.07639)
*Pengyu Wang,Chenkun Tan,Shaojun Zhou,Wei Huang,Qirui Zhou,Zhan Huang,Zhen Ye,Jijun Cheng,Xiaomeng Qian,Yanxin Chen,Xingyang He,Huazheng Zeng,Chenghao Wang,Pengfei Wang,Hongkai Wang,Shanqing Gao,Yixian Tian,Chenghao Liu,Xinghao Wang,Botian Jiang,Xipeng Qiu*

Main category: cs.CV

TL;DR: 本文提出MOSS-Video-Preview，采用双通道交叉注意力架构，将视觉特征通过侧通道而非自回归序列输入，实现感知与生成分离，从而支持实时视频理解中的连续感知、答案修订和适时沉默。在单个H200上实现首令牌延迟降低约5倍，解码吞吐量提升2.7倍。

<details>
  <summary>Details</summary>

Motivation: 视频理解正从离线范式（完整视频输入后输出单一答案）转向实时交互范式（模型在回复的同时感知新帧、根据新证据修订答案、无话可说时保持沉默）。现有decoder-only架构中感知被生成阻塞，无法满足实时需求，因此需要一种感知不与生成相互阻塞的双通道架构。

Method: (1) 双通道架构：采用交叉注意力骨干网络，视觉特征通过侧通道输入而非加入自回归序列，使感知与生成在分离的非阻塞路径上运行，降低视觉处理频率并暴露独立的通道级压缩接口；(2) 数据合成流水线：将密集字幕转换为实时理解问答数据，答案根据模型当前感知到的内容进行修订；(3) 离线模型特化：利用合成数据对离线模型进行微调，诱导出实时行为。

Result: (1) 在单个H200上处理每视频256帧，首令牌延迟加速约5倍，解码吞吐量提升2.7倍；(2) 离线能力几乎无损；(3) 在空间和细粒度时序推理上保持鲁棒；(4) 获得了离线模型不具备的连续感知、答案修订和适时沉默行为；(5) 整体性能落后于Qwen2.5-VL-7B基线，差距主要归因于数据和规模而非架构。

Conclusion: 本文对范式、架构和数据的系统研究表明，双通道交叉注意力架构是实现实时视频理解的可行路径，为未来实时视频理解研究提供了新方向。

Original Abstract: Video understanding is shifting from the offline paradigm -- taking a fully recorded video as input and producing a single answer after it ends -- toward real-time interaction, in which the model perceives new frames while still replying, revises its answer as new evidence appears, and remains silent when there is nothing to say. We present MOSS-Video-Preview to validate this paradigm. Our central claim is that perception must not be blocked by generation; its natural realization is a two-channel architecture. We argue that a cross-attention backbone is better suited to real-time vision-language fusion than the prevailing decoder-only design: visual features enter through a side channel rather than joining the autoregressive sequence, so perception and generation run on separate, non-blocking pathways -- reducing the frequency of visual processing and exposing a clean channel-wise interface for independent compression. We complement this with a data synthesis pipeline that converts dense captions into real-time understanding QA whose answers are revised to match what the model has perceived so far, and we specialize an offline model on these data to elicit real-time behavior. Our model trails the strong Qwen2.5-VL-7B baseline overall -- a gap we attribute primarily to data and scale rather than the architecture -- yet attains competitive offline video and multimodal understanding, remains robust on the spatial and fine-grained temporal reasoning central to real-time use, and acquires behaviors that offline models lack: continuous perception, answer revision, and timely silence. On a single H200 with 256 frames per video, it achieves about a 5x speedup in time to first token and 2.7x higher decoding throughput, with negligible degradation in offline ability. Our study of paradigm, architecture, and data outlines a viable path toward real-time video understanding.

中文摘要翻译: 视频理解正从离线范式——即输入完整录制视频并在结束后产生单一答案——转向实时交互范式，模型在回复的同时感知新帧、根据新出现的证据修订答案、并在无话可说时保持沉默。我们提出MOSS-Video-Preview来验证这一范式。我们的核心主张是：感知不能被生成所阻塞；其自然实现方式是双通道架构。我们认为，交叉注意力骨干网络比当前主流的仅解码器设计更适合实时视觉-语言融合：视觉特征通过侧通道进入而非加入自回归序列，因此感知和生成在分离的、非阻塞的路径上运行——降低了视觉处理的频率，并暴露出一个用于独立压缩的干净通道级接口。我们辅之以一个数据合成流水线，将密集字幕转换为实时理解问答数据，其答案经过修订以匹配模型当前已感知到的内容，并在此类数据上特化一个离线模型以诱导实时行为。我们的模型在整体上落后于强大的Qwen2.5-VL-7B基线——这一差距我们主要归因于数据和规模而非架构——但在离线视频和多模态理解上达到了有竞争力的水平，在对实时应用至关重要的空间和细粒度时序推理上保持鲁棒，并获得了离线模型所缺乏的行为：连续感知、答案修订和适时沉默。在单个H200上处理每视频256帧时，它在首令牌延迟上实现了约5倍的加速，解码吞吐量提升2.7倍，且离线能力几乎没有下降。我们对范式、架构和数据的研究勾勒出一条通往实时视频理解的可行路径。

</details>


### [18] [No Free Lunch for Synthetic Images under Data Scarcity Conditions](https://arxiv.org/abs/2606.07640)
*Borja Arroyo Galende,Alejandro Almodóvar,Patricia A. Apellániz,Juan Parras,Silvia Uribe,Santiago Zazo*

Main category: cs.CV

TL;DR: 本文研究了数据稀缺和隐私敏感条件下，合成数据生成中保真度、隐私和效用之间的权衡，提出了一个联合评估框架，并对比了VAE、GAN和DDPM三种生成模型在加入差分隐私机制后的表现差异。

<details>
  <summary>Details</summary>

Motivation: 现有研究缺乏在数据稀缺和隐私敏感环境下对生成模型的保真度、隐私和效用三个维度的联合评估，不同模型在引入隐私机制后的行为差异尚未被系统研究。

Method: 提出了一个联合评估保真度、隐私和效用的评估框架，应用于VAE、GAN和DDPM三种生成模型，在MNIST、OCTMNIST和OrganAMNIST三个数据集上进行评估，并在训练中引入差分隐私机制。

Result: GAN和DDPM在引入差分隐私后表现出更强的鲁棒性，在多种噪声水平下仍能保持较高的保真度和下游效用；VAE在隐私约束增强时性能下降更快。

Conclusion: 对深度生成模型进行多维评估至关重要，不同模型在应用隐私技术时行为存在显著差异。

Original Abstract: This study investigates the trade-offs between fidelity, privacy, and utility in synthetic data generation under conditions of data scarcity and privacy sensitivity. We propose an evaluation framework that jointly assesses these three dimensions and apply it to three widely used generative models, VAE, GAN, and DDPM. The evaluation spans three image datasets, MNIST, OCTMNIST, and OrganAMNIST, encompassing both general-purpose and medical imaging domains. Notable differences arise between the three models in their behaviour when differential privacy mechanisms are introduced during training. GAN and DDPM demonstrate greater robustness, maintaining higher fidelity and downstream utility across a range of noise levels, while VAE degrades more rapidly as privacy constraints increase. This study highlights the importance of a multidimensional evaluation of deep generative models, also noting that their behaviour significantly differs when privacy techniques are applied.

中文摘要翻译: 本研究探讨了在数据稀缺和隐私敏感条件下，合成数据生成中保真度、隐私和效用之间的权衡。我们提出了一个联合评估这三个维度的评估框架，并将其应用于三种广泛使用的生成模型：VAE、GAN和DDPM。评估覆盖了三个图像数据集——MNIST、OCTMNIST和OrganAMNIST，涵盖通用和医学影像领域。在训练过程中引入差分隐私机制后，三种模型的行为出现了显著差异。GAN和DDPM表现出更强的鲁棒性，能在多种噪声水平下维持较高的保真度和下游效用，而VAE随着隐私约束增强性能下降更快。本研究强调了深度生成模型多维评估的重要性，并指出在应用隐私技术时不同模型的行为存在显著差异。

</details>


### [19] [Readable Yet Unpredictable: Rotated-Outcome Prediction in Vision-Language Models](https://arxiv.org/abs/2606.07641)
*Lexin Wang,Shenghua Liu,Yiwei Wang,Jiafeng Guo,Xueqi Cheng*

Main category: cs.CV

TL;DR: 本文研究视觉-语言模型（VLM）能否从原始图像直接预测180°旋转后的内容，发现当前VLM在直接观察旋转图像时能识别内容，但无法从原始视图推理出旋转后的结果，旋转预测准确率接近零。

<details>
  <summary>Details</summary>

Motivation: 探究VLM是否具备从原始图像中推断出未直接观察的变换后视觉状态的能力，尤其是180°平面旋转后的内容预测能力，以揭示当前模型在视觉推理方面的根本性局限。

Method: 提出RotOutBench配对诊断基准，包含开放视觉案例和受控文本-图像旋转两种任务，通过对比模型在直接观察旋转图像与从原始图像预测旋转结果的表现，评估VLM的旋转结果预测能力。

Result: 许多VLM在直接观察原始或旋转图像时能识别相关内容，但无法从原始图像单独推断旋转结果；在受控文本-图像旋转任务中，预测旋转准确率几乎为零，即使模型具有高直接读取准确率；模型内部状态可接近旋转图像读取状态，但最终输出仍偏向原始字符串。

Conclusion: 当前VLM能够识别被展示的变换后视觉状态，但常常无法从原始视角预测该状态，揭示了模型在视觉推理任务中的根本性缺陷。

Original Abstract: Can vision-language models predict what a 180° rotation would reveal from the original image alone? We study this ability through Rotated-Outcome Prediction: given an original image, a model must answer what would be seen or read after a 180° in-plane rotation, without directly observing the rotated target. To isolate this gap, we introduce RotOutBench, a paired diagnostic benchmark spanning open visual cases and controlled text-image rotations. A sharp pattern emerges: many VLMs can recognize the relevant content when directly given either the original or rotated image, yet fail to infer the rotated result from the original image alone. On controlled text-image rotations, predicted-rotation accuracy collapses to near zero even for models with high direct-reading accuracy. A model-level case study further shows that the prediction state can approach a rotated-image reading state, while the final readout still shifts toward the original string. Current VLMs can recognize a transformed visual state when it is shown, but often fail to predict that state from the original view.

中文摘要翻译: 视觉-语言模型能否仅从原始图像预测180°旋转后所呈现的内容？我们通过旋转结果预测来研究这一能力：给定一张原始图像，模型必须回答在180°平面旋转后会看到或读取到什么，而无需直接观察旋转后的目标。为孤立这一差距，我们引入了RotOutBench，一个涵盖开放视觉案例和受控文本-图像旋转的配对诊断基准。一个显著的模式浮现出来：许多视觉-语言模型在直接给定原始图像或旋转图像时能够识别相关内容，但仅凭原始图像却无法推断旋转后的结果。在受控文本-图像旋转任务上，即使对于具有高直接读取准确率的模型，预测旋转准确率也降至接近零。进一步的模型级案例研究表明，预测状态可以接近旋转图像读取状态，但最终读出仍然向原始字符串偏移。当前视觉-语言模型能够识别被展示的变换后视觉状态，但常常无法从原始视角预测该状态。

</details>


### [20] [Do VLMs See What Sensors Feel? A Scalable Expert-Guided Design for Wheelchair Accessibility Assessment from Street View](https://arxiv.org/abs/2606.07642)
*Dongdong Wang,Alina Hagen,Isabelle Gatmaitan,Hao Zhou,Yiwen Dong,Shabboo Valipoor,Vivian W. H. Wong,Lingyao Li*

Main category: cs.CV

TL;DR: 本文探讨了视觉语言模型（VLMs）能否从谷歌街景图像中识别无障碍障碍，提出了一种专家引导的检索增强框架，并在佛罗里达大学校园数据集上验证了该方法与真实轮椅导航行为（GPS停留时间）的部分一致性。

<details>
  <summary>Details</summary>

Motivation: 评估建成环境交互（如轮椅无障碍性）非常困难，因为现实世界中的移动性受到分布式、上下文依赖和临时性障碍的影响，难以大规模捕获。因此需要一种可扩展的评估方法。

Method: 提出专家引导的检索增强框架（expert-guided retrieval-augmented framework），结合谷歌街景图像、ADA无障碍指南和专家评分标准来评估无障碍维度。在佛罗里达大学校园收集包含407个GSV位置的数据集，并以GPS导出的轮椅停留行为作为移动摩擦信号进行验证。

Result: VLM评分与停留时间呈负相关且分布相似，表明与移动摩擦的行为代理指标存在部分但一致的对齐。视觉线索分析显示，路边坡道和人行横道等环境对象与较高VLM无障碍评分相关，但对细微地面条件、临时障碍物和视角依赖障碍的对齐仍有限。

Conclusion: 专家引导的VLM在大规模无障碍评估中具有潜力，能够与真实世界轮椅导航的传感器衍生指标对齐。

Original Abstract: Assessing built-environment interaction, such as wheelchair accessibility, is difficult because real-world mobility is shaped by distributed, context-dependent, and temporary barriers that are hard to capture at scale. To support scalable assessment, this paper examines whether vision-language models (VLMs) can identify accessibility barriers from Google Street View (GSV) imagery. We propose an expert-guided retrieval-augmented framework that combines GSV images, ADA-informed guidance, and expert-derived rubrics to evaluate accessibility dimensions. We collect a campus-scale dataset at the University of Florida, linking 407 unique GSV locations with GPS-derived wheelchair dwell behavior as a mobility-friction signal. Results show that VLM ratings are both negatively correlated and distributionally similar with dwell time, indicating partial but consistent alignment with a behavioral proxy for mobility friction. Visual cue analysis shows that certain environmental objects, such as curb ramps and crosswalks, are associated with higher VLM accessibility scores, while alignment remains limited for subtle surface conditions, transient obstructions, and viewpoint-dependent barriers. Overall, our findings show the potential of expert-guided VLMs for scalable accessibility assessment aligning with sensor-derived indicators of real-world wheelchair navigation.

中文摘要翻译: 评估建成环境交互（例如轮椅无障碍性）十分困难，因为现实世界中的移动性受到分布式、上下文依赖且难以大规模捕获的临时性障碍的影响。为支持可扩展的评估，本文探究了视觉语言模型（VLM）能否从谷歌街景（GSV）图像中识别无障碍障碍。我们提出了一种专家引导的检索增强框架，该框架结合GSV图像、基于《美国残疾人法案》（ADA）的指导以及专家制定的评分标准来评估无障碍维度。我们在佛罗里达大学校园收集了一个校园规模的数据集，将407个独特的GSV位置与GPS导出的轮椅停留行为（作为移动摩擦信号）关联起来。结果表明，VLM评分与停留时间既呈负相关，又在分布上相似，表明与移动摩擦的行为代理指标存在部分但一致的对齐。视觉线索分析显示，某些环境对象（如路边坡道和人行横道）与较高的VLM无障碍评分相关，但针对细微地面条件、临时障碍物和视角依赖障碍的对齐仍然有限。总体而言，我们的研究结果表明，专家引导的VLM在大规模无障碍评估中具有潜力，能够与真实世界轮椅导航的传感器衍生指标对齐。

</details>


### [21] [AVI-Bench: Toward Human-like Audio-Visual Intelligence of Omni-MLLMs](https://arxiv.org/abs/2606.07643)
*Yaoting Wang,Ziyi Zhang,Wenming Tu,Shaoxuan Xu,Wenjie Du,Cheng Liang,Weijun Wang,Yuanchao Li,Guangyao Li,Hao Fei,Yuanchun Li,Henghui Ding,Yunxin Liu*

Main category: cs.CV

TL;DR: 本文提出AVI-Bench，一个受认知科学启发的基准测试，用于系统评估全模态大语言模型（Omni-MLLMs）的音频-视觉智能（AVI），包含感知、理解、推理三个阶段的跨模态任务，以及用于测试原始音频-视觉感觉的扩展集AVI-Bench-PriSe。实验发现当前模型存在显著局限性，并据此提出了四层AVI分类法。

<details>
  <summary>Details</summary>

Motivation: 现有全模态大语言模型虽已实现视觉、音频和语言的强集成，但其音频-视觉智能缺乏系统性和全面的评估基准，因此需要建立一个能够细粒度诊断模型能力和失败模式的评估框架。

Method: 提出了AVI-Bench基准，包含三个认知阶段（感知、理解、推理）的跨模态任务；以及AVI-Bench-PriSe扩展集，使用陌生且低语义刺激来测试模型在常见训练分布之外的原始音频-视觉感觉能力。基于实验结果，进一步提出了四层AVI分类法。

Result: 对开源和闭源模型的广泛实验揭示了当前全模态大语言模型在音频-视觉智能方面存在显著局限性，并据此归纳出四层AVI分类法。

Conclusion: AVI-Bench为开发更鲁棒、更具泛化能力的音频-视觉智能提供了原则性的评估框架，有助于指导未来全模态大语言模型的发展。

Original Abstract: Recent advances in Omni-Multimodal Large Language Models (Omni-MLLMs) have enabled strong integration of vision, audio, and language. However, their audio-visual intelligence (AVI) remains insufficiently evaluated due to the lack of systematic and comprehensive benchmarks. We introduce AVI-Bench, a cognitively inspired benchmark that evaluates Omni-MLLMs across three stages, perception, understanding, and reasoning, through cross-modal tasks requiring joint audio-visual interpretation. This design enables fine-grained diagnosis of model capabilities and failure modes. To further assess robustness beyond familiar domains, we propose AVI-Bench-PriSe, an extension that probes models' primitive audio-visual sensation using unfamiliar, low-semantic stimuli, testing generalization beyond common training distributions. Extensive experiments on both open-source and closed-source models reveal substantial limitations in current Omni-MLLMs. Based on these findings, we present a four-level AVI taxonomy. Overall, AVI-Bench provides a principled evaluation framework to guide the development of more robust and generalizable AVI. Project website: https://fudancvl.github.io/AVI-Bench/

中文摘要翻译: 全模态大语言模型（Omni-MLLMs）的最新进展使得视觉、音频和语言的强集成成为可能。然而，由于缺乏系统且全面的基准测试，其音频-视觉智能（AVI）尚未得到充分评估。本文提出AVI-Bench，一个受认知科学启发的基准测试，通过需要联合音频-视觉解释的跨模态任务，在感知、理解和推理三个阶段评估全模态大语言模型。该设计能够对模型能力和失败模式进行细粒度诊断。为了进一步评估模型在熟悉领域之外的鲁棒性，我们提出AVI-Bench-PriSe扩展集，该扩展集使用陌生且低语义的刺激来探测模型的原始音频-视觉感觉能力，测试其超出常见训练分布的泛化能力。对开源和闭源模型的广泛实验揭示了当前全模态大语言模型的显著局限性。基于这些发现，我们提出了一个四层AVI分类法。总体而言，AVI-Bench提供了一个原则性的评估框架，以指导更鲁棒、更具泛化能力的音频-视觉智能的发展。项目网站：https://fudancvl.github.io/AVI-Bench/

</details>


### [22] [FineGen: A VLM-based Multi-Agent Framework for Fine-Grained Image-Text Dataset Construction](https://arxiv.org/abs/2606.07645)
*Chang Kong,Yuebing Li,Peng Mo,Haigang Zhang,Qiuming Luo*

Main category: cs.CV

TL;DR: 提出FineGen，一种基于VLM的多智能体框架，通过生成-验证-纠正流水线自动构建高质量硬负样本数据集，在FG-OVD基准上实现+14.4%的硬样本精度提升。

<details>
  <summary>Details</summary>

Motivation: 当前视觉-语言数据集中硬负样本稀缺，严重制约了模型的细粒度感知能力。

Method: 采用基于VLM的多智能体框架，通过协作的生成-验证-纠正流水线及闭环反馈机制，确保合成的硬负样本在语义上有效但与视觉内容严格矛盾。

Result: 构建了FineGen-100K数据集（147,000+属性特定硬负样本，1:10正负比），属性有效率达96.7%；在下游FG-OVD基准上微调后，硬样本精度提升+14.4%，显著优于现有方法。

Conclusion: 所提出的自动化硬负样本生成方法能有效提升细粒度视觉-语言感知能力，验证了合成数据对模型性能的显著促进作用。

Original Abstract: The scarcity of hard negative samples in current vision-language datasets significantly hinders fine-grained perception. To address this, we propose FineGen, a VLM-based Multi-Agent framework for automated dataset construction. By employing a collaborative Generation-Verification-Correction pipeline with a closed-loop feedback mechanism, FineGen ensures synthesized hard negatives are semantically valid yet strictly contradictory to visual content. Applying this to ImageNet, we construct FineGen-100K, a hierarchical dataset containing over 147,000 attribute-specific hard negatives with a rigorous 1:10 positive-to-negative ratio. Extensive evaluations confirm a 96.7% attribute validity rate. Crucially, downstream validation on the FG-OVD benchmark shows that fine-tuning on FineGen-100K yields a substantial +14.4% accuracy improvement on hard samples, significantly outperforming state-of-the-art methods.

中文摘要翻译: 当前视觉-语言数据集中硬负样本的稀缺严重阻碍了细粒度感知。为解决这一问题，我们提出FineGen——一种基于VLM的多智能体框架，用于自动化数据集构建。通过采用协作的生成-验证-纠正流水线及闭环反馈机制，FineGen确保合成的硬负样本在语义上有效但与视觉内容严格矛盾。将该方法应用于ImageNet，我们构建了FineGen-100K，一个包含超过147,000个属性特定硬负样本的分层数据集，具有严格的正负样本1:10比例。广泛评估证实了96.7%的属性有效率。关键的是，在FG-OVD基准上的下游验证表明，在FineGen-100K上微调后，硬样本的准确率提升了+14.4%，显著优于现有最先进方法。

</details>


### [23] [DOME: Learning Transferable Domain Variables from Sparse Supervision for Test-Time Adaptation](https://arxiv.org/abs/2606.07646)
*Xiaoran Xu,Yifan Xu,Yupeng Wu,Xiaoshan Yang,Changsheng Xu*

Main category: cs.CV

TL;DR: 提出DOME域编码器，通过视觉-语言预训练显式建模样本级别的域分布，用于测试时自适应，在多个基准上超越复杂方法。

<details>
  <summary>Details</summary>

Motivation: 现有TTA方法隐含地推断单一全局域分布，忽略了真实场景中域偏移的多维性和样本特异性，导致自适应效果脆弱。

Method: 利用视觉-语言预训练提取密集连续表示，将域参数化为分布变量，引入动量更新的稀疏域库进行解耦监督，从而以零样本方式显式建模每个样本的域信息。

Result: 在ImageNet-C、ImageNet-R和ImageNet-Sketch上，将显式域线索注入基础熵最小化TTA策略即可达到超越复杂TTA方法的最优性能。

Conclusion: 鲁棒的自适应能力并不源于复杂的自适应算法，而在于显式、结构化的域表示。

Original Abstract: Test-time adaptation (TTA) aims to align a model to shifting test domains using only unlabeled streaming data. Most existing methods implicitly infer a single global domain distribution, ignoring the multidimensional and sample-specific nature of real-world domain shifts, leading to fragile adaptation. We propose DOME, an effective domain encoder that explicitly models each sample's domain in a zero-shot manner. DOME leverages vision-language pretraining to extract dense, continuous representations, parameterizes domains as distributional variables, and introduces a momentum-updated sparse domain bank for disentangled supervision. By injecting these explicit domain cues into downstream models, even a basic entropy-minimization TTA strategy achieves state-of-the-art performance across ImageNet-C, ImageNet-R, and ImageNet-Sketch, outperforming complex TTA approaches. Our results demonstrate that robust adaptation stems not from intricate adaptation algorithms, but from explicit, structured domain representation.

中文摘要翻译: 测试时自适应（TTA）旨在仅使用无标签的流式数据将模型对齐到变化的测试域。现有方法大多隐含地推断单一的全局域分布，忽略了真实世界域偏移的多维性和样本特异性，导致自适应效果脆弱。我们提出DOME，一种高效的域编码器，以零样本方式显式建模每个样本的域。DOME利用视觉-语言预训练提取密集连续表示，将域参数化为分布变量，并引入动量更新的稀疏域库进行解耦监督。通过将这些显式域线索注入下游模型，即使是最基础的熵最小化TTA策略也能在ImageNet-C、ImageNet-R和ImageNet-Sketch上达到超越复杂TTA方法的最优性能。我们的结果表明，鲁棒的自适应能力并非源于复杂的自适应算法，而在于显式、结构化的域表示。

</details>


### [24] [Steer Where It Matters: Token-Level Visual-Sensitivity Steering for LVLMs Hallucination Mitigation](https://arxiv.org/abs/2606.07647)
*Ruipeng Zhang,Zhihao Li,C. L. Philip Chen,Tong Zhang*

Main category: cs.CV

TL;DR: 本文提出TLVS方法，通过提取token级别的视觉敏感性引导向量，并在解码时自适应调整干预强度，仅在关键位置抑制幻觉，从而提升多模态大模型的生成可靠性。

<details>
  <summary>Details</summary>

Motivation: 现有激活引导方法存在两个主要问题：(1) 在整个序列上平均图像与无图像的差异，稀释了视觉条件对token预测的稀疏局部信号，导致引导方向信噪比低；(2) 使用固定的引导强度，对非关键token过度扰动，造成干预预算分配不当和不稳定性。

Method: TLVS方法首先提取token级别的引导向量并进行精炼，然后在每个解码步骤根据视觉敏感性自适应调整引导强度，仅在易产生幻觉的位置施加干预。该方法轻量级、即插即用，仅需最小训练进行校准，可应用于多种视觉-语言模型。

Result: 在POPE、AMBER、CHAIR (COCO)、MMHal和HallusionBench等多个基准上，TLVS相比之前的引导方法取得了一致的性能提升。

Conclusion: TLVS通过细粒度、视觉敏感性自适应的激活引导，有效抑制了多模态大语言模型的幻觉问题，同时保留了基于证据的内容，是一种轻量且通用的解决方案。

Original Abstract: Large vision language models (LVLMs) have made rapid advancements and are deployed across various applications, yet hallucinations remain a major challenge. Activation steering is appealing due to its minimal training overhead and controllability at inference time. However, we found that during autoregressive decoding, visual conditioning affects token prediction sparsely and locally across decoding steps, and many existing methods that average image-versus-no-image differences over the entire sequence dilute these critical signals, yielding low signal-to-noise ratio steering directions. Additionally, many existing methods apply a fixed steering strength, which misallocates the intervention budget, over-perturbs non-critical tokens, and can cause instability. To address these limitations, we propose Token-Level Visual-Sensitivity Steering (TLVS) for hallucination mitigation. Our approach first extracts token-level steering vectors and refines them, and then applies fine-grained, visual-sensitivity-adaptive steering only where it matters. This lightweight, plug-and-play mechanism requires only minimal training for calibration and can be applied across diverse vision-language models. It modulates the steering strength at each decoding step, selectively suppressing hallucination-prone spans while preserving evidence-grounded content. We evaluate TLVS on several benchmarks, including POPE, AMBER, CHAIR (COCO), MMHal, and HallusionBench, demonstrating consistent improvements over previous steering methods.

中文摘要翻译: 大型视觉语言模型（LVLMs）已取得快速进展并部署于各类应用中，但幻觉仍然是一个主要挑战。激活引导方法因其训练开销小和推理时可控制性而具有吸引力。然而，我们发现在自回归解码过程中，视觉条件对token预测的影响是稀疏且局部的，许多现有方法对整个序列上图像与无图像差异进行平均，稀释了这些关键信号，产生了低信噪比的引导方向。此外，许多现有方法使用固定的引导强度，导致干预预算分配不当，过度扰动非关键token，并可能引发不稳定性。为解决这些局限，我们提出用于幻觉缓解的Token级视觉敏感性引导方法（TLVS）。该方法首先提取token级别的引导向量并对其进行精炼，然后仅在关键位置应用细粒度、视觉敏感性自适应的引导。这种轻量级、即插即用的机制仅需最小训练进行校准，可应用于多种视觉-语言模型。它在每个解码步骤调节引导强度，选择性地抑制易产生幻觉的片段，同时保留基于证据的内容。我们在POPE、AMBER、CHAIR (COCO)、MMHal和HallusionBench等多个基准上评估了TLVS，结果表明其相较于之前的引导方法取得了一致的改进。

</details>


### [25] [AQIFormer: A Transformer-Based Multi-View Architecture for Cross-City Air Quality Classification](https://arxiv.org/abs/2606.07648)
*Om Kathalkar,Nitin Nilesh,Sachin Chaudhari,Anoop Namboodiri*

Main category: cs.CV

TL;DR: 提出AQIFormer，一种基于Transformer的集成架构，通过前后视图融合、天气感知注意力机制和多任务学习实现跨城市空气污染分类，在26778对图像上达到89.96%准确率，优于现有方法14.96%，且跨城市泛化仅下降8.29%。

<details>
  <summary>Details</summary>

Motivation: 传统传感器监测空气污染存在可扩展性和经济性瓶颈，基于图像的空气质量估计虽成为替代方案，但现有方法跨城市泛化能力有限，且未能充分利用多视角信息。

Method: 提出AQIFormer架构，核心包括：(1) 双视图集成——同时利用前后交通场景图像；(2) 天气感知注意力机制——融合气象参数；(3) 多任务学习——综合优化空气质量分类目标。

Result: 在26778对前后图像数据集上达89.96%准确率，较现有最优方法提升14.96%。跨城市泛化实验中，在印度那格浦尔独立数据集上达81.67%准确率，经少样本微调后性能仅下降8.29%。

Conclusion: AQIFormer通过双视图融合和天气感知机制有效解决了跨城市空气污染估计算法的泛化瓶颈，验证了多视角整合与气象信息对提升模型鲁棒性的关键作用。

Original Abstract: Air pollution represents one of the most critical environmental and public health challenges globally, with traditional sensor-based monitoring systems facing significant scalability and economic constraints. Image-based air quality estimation has emerged as a promising alternative, leveraging the visual characteristics of atmospheric pollutants in traffic scenes. However, existing methods suffer from limited cross-city generalization and inadequate exploitation of multi-view perspectives. We present AQIFormer, a novel transformer-based ensemble architecture that addresses these fundamental limitations through innovative dual-view integration, weather-aware attention mechanisms, and comprehensive multi-task learning. Our approach uniquely combines front and rear traffic imagery with meteorological parameters to achieve robust air quality classification across diverse urban environments. Extensive evaluation on a comprehensive dataset of 26,678 synchronized front-rear image pairs demonstrates good performance with 89.96% accuracy, representing a 14.96% improvement over state-of-the-art methods. Most importantly, our model maintains exceptional cross-city generalization capabilities, achieving 81.67% accuracy on an independent dataset collected in Nagpur, India with only 8.29% performance degradation using few-shot adaptation with minimal training samples.

中文摘要翻译: 空气污染是全球面临的最关键的环境与公共卫生挑战之一，传统的基于传感器的监测系统面临显著的可扩展性和经济性约束。基于图像的空气质量估计作为一种有前景的替代方案，利用交通场景中大气污染物的视觉特征进行估计。然而，现有方法存在跨城市泛化能力有限以及对多视角信息利用不充分的问题。我们提出AQIFormer，一种新颖的基于Transformer的集成架构，通过创新的双视图融合、天气感知注意力机制和全面的多任务学习来解决这些根本性局限。该方法独特地将前后交通场景图像与气象参数相结合，实现在不同城市环境下的鲁棒空气质量分类。在包含26,678对同步前后图像的综合数据集上进行的广泛评估表明，该方法达到了89.96%的准确率，相比现有最优方法提升了14.96%。最重要的是，我们的模型保持了优异的跨城市泛化能力，在印度那格浦尔收集的独立数据集上达到了81.67%的准确率，通过少样本自适应仅需极少量训练样本，性能下降仅为8.29%。

</details>


### [26] [ViMax: Agentic Video Generation](https://arxiv.org/abs/2606.07649)
*Lingxuan Huang,Sizhe He,Hengji Zhou,Liqiang Nie,Lianghao Xia,Chao Huang*

Main category: cs.CV

TL;DR: 提出ViMax框架，通过多智能体协作实现长视频生成，解决现有方法缺乏叙事结构和跨场景一致性的问题。

<details>
  <summary>Details</summary>

Motivation: 现有短视频生成方法无法提供系统化的叙事规划和视觉一致性，生成的是孤立序列，缺乏跨场景的角色和环境一致性维持机制。

Method: 采用分层叙事引擎结合检索增强生成（RAG）实现全局故事连贯性，设计依赖感知的视觉一致性机制跟踪跨时间边界的角色和场景状态，并通过VLM引导的智能体持续监控和优化叙事连贯性与视觉保真度。

Result: 框架通过多智能体协作生成扩展的叙事内容，在多场景时间线上同时保持了叙事完整性和视觉连贯性。

Conclusion: ViMax通过多智能体协同架构有效解决了长视频生成中的叙事规划和视觉一致性问题。

Original Abstract: Long-form video generation requires systematic narrative planning and visual consistency that current short-clip methods cannot provide. Existing methods generate isolated sequences without narrative structure and lack mechanisms for maintaining character and environmental consistency across scenes. We present ViMax, an agentic video generation framework that addresses video creation through coordinated multi-agent collaboration where specialized components negotiate narrative decisions, visual continuity, and production quality. Our framework employs a hierarchical narrative engine with retrieval-augmented generation for global story coherence and a dependency-aware visual consistency mechanism that tracks character and environmental states across temporal boundaries, while VLM-guided agents continuously monitor and refine both narrative coherence and visual fidelity. The framework enables coordinated agent collaboration to generate extended narrative content. This maintains both storytelling integrity and visual coherence across multi-scene timelines.

中文摘要翻译: 长视频生成需要系统化的叙事规划和视觉一致性，而当前的短视频方法无法满足这些需求。现有方法生成的是孤立的序列，缺乏叙事结构，也没有维持跨场景角色和环境一致性的机制。我们提出了ViMax，一个智能体视频生成框架，通过协调的多智能体协作来解决视频创作问题，其中专门的组件协商叙事决策、视觉连续性和制作质量。我们的框架采用分层叙事引擎结合检索增强生成来实现全局故事连贯性，并设计了一个依赖感知的视觉一致性机制，用于跨时间边界跟踪角色和环境状态，同时由VLM引导的智能体持续监控和优化叙事连贯性与视觉保真度。该框架通过协调智能体协作来生成扩展的叙事内容，从而在多场景时间线上同时保持叙事完整性和视觉连贯性。

</details>


### [27] [A Dataset for Dynamic Human Preferences for Vision Language Models](https://arxiv.org/abs/2606.07653)
*Hannah Gao,Dylan Hadfield-Menell,Rachel Ma*

Main category: cs.CV

TL;DR: 本文提出一个用于评估视觉语言模型（VLM）理解动态人类偏好能力的新基准，该基准通过自动化流程生成，包含图像依赖变化、多模态偏好数据集，并对现有SOTA模型进行了评估。

<details>
  <summary>Details</summary>

Motivation: 现有视觉语言基准主要评估静态能力和一般性偏好，忽略了VLM在人机交互场景中需根据用户实时偏好进行动态调整的需求。

Method: 提出自动化管道生成基准，包含图像依赖变化、动态多模态人类偏好数据集，并在该基准上对SOTA视觉语言模型进行评估。

Result: 构建了新基准数据集，并在其上对多个SOTA视觉语言模型进行了评估。

Conclusion: 现有VLM在理解动态人类偏好方面仍有不足，本文提出的基准为该方向的研究提供了评估工具。

Original Abstract: Given the increased adoption of Vision Language Models (VLMs) in human-interactive settings, it is important that we evaluate how well these models can adapt to real-time preferences for different users. While an increasing number of vision-language benchmarks have recently been introduced, they focus largely on evaluating static capabilities and generally-held preferences learned from extensive training data. This work introduces a new benchmark for evaluating the ability of VLMs to understand dynamic human-preferences, i.e. preferences that are passed in-context at inference time. We provide an automated pipeline for generating this benchmark with variations on image dependence, a dynamic multi-modal human-preference dataset, and evaluations of state-of-the-art models on the novel benchmark.

中文摘要翻译: 鉴于视觉语言模型（VLM）在人机交互场景中的应用日益广泛，评估这些模型能否适应用户的实时偏好变得尤为重要。尽管近期涌现出大量视觉语言基准，但它们主要侧重于评估静态能力以及从大量训练数据中学到的一般性偏好。本文引入了一个新基准，用于评估VLM理解动态人类偏好的能力，即在推理时通过上下文传递的偏好。我们提供了一个自动化管道来生成该基准，包含不同图像依赖度的变体、一个动态多模态人类偏好数据集，以及在该新基准上对当前最先进模型的评估结果。

</details>


### [28] [MM-Matryoshka: Towards Budget-Elastic Visual Document Retrieval via a 2D Multimodal Matryoshka Training Framework](https://arxiv.org/abs/2606.07654)
*Haowen Xiang,Yibo Yan,Jiahao Huo,Yu Huang,Yi Cao,Mingdong Ou,Xuming Hu*

Main category: cs.CV

TL;DR: 提出MM-Matryoshka，一个二维Matryoshka训练框架，使多向量视觉文档检索（ColPali风格）在向量维度和编码器层数两个维度上均具备预算弹性，无需训练多个模型即可在推理时权衡精度与存储/计算开销。

<details>
  <summary>Details</summary>

Motivation: 现有的多向量视觉文档检索器（如ColPali）虽能实现细粒度匹配，但存储和计算开销巨大；已有的效率优化技术通常只优化单一部分（如只压缩维度或只减少层数），缺乏统一的、可在向量宽度和编码器深度两个维度同时弹性调节精度与开销的方法。

Method: 提出MM-Matryoshka二维Matryoshka训练框架，在训练时同时对多向量视觉文档检索器的向量嵌入维度（dimension）和Transformer编码器层数（layer）施加嵌套的渐进式截断约束，使得单个模型能够学习在不同预算下保持良好性能，推理时无需额外训练即可在2D预算空间中灵活选择。

Result: 在多个代表性骨干网络上进行综合实验，结果表明MM-Matryoshka相比直接截断基线方法能够显著保留更高的检索质量，同时大幅降低存储和计算开销，实现了鲁棒的预算弹性。

Conclusion: MM-Matryoshka为高效的视觉文档检索提供了一种统一的、鲁棒的预算弹性解决方案，能够在精度与效率之间灵活权衡。

Original Abstract: Multi-vector visual document retrievers achieve strong fine-grained matching by representing each page with multiple vectors from deep Vision-Language Models (VLMs), but this design makes deployment expensive in both storage and computational overhead. Existing efficiency techniques usually optimize only part of this budget, leaving multimodal retrievers without a unified way to trade accuracy for both vector width and encoder depth. Therefore, we propose MM-Matryoshka, a 2D Matryoshka training framework for budget-elastic Visual Document Retrieval (VDR), enabling ColPali-style multi-vector retrieval elastic along both dimension and layer. At inference time, a single retriever can select a 2D selectable budget without training separate models for different budgets. Through comprehensive experiments across multiple representative backbones, we demonstrate that by retaining significantly higher quality than direct truncation baselines while substantially reducing storage and computational overhead, MM-Matryoshka can offer robust budget elasticity for efficient VDR.

中文摘要翻译: 多向量视觉文档检索器通过将每个页面表示为来自深度视觉-语言模型（VLM）的多个向量，实现了强大的细粒度匹配能力，但这种设计使得部署在存储和计算开销上都十分昂贵。现有的效率优化技术通常只优化该预算中的单一部分，使得多模态检索器缺乏一种统一的方式来权衡精度与向量宽度和编码器深度。因此，我们提出了MM-Matryoshka，一个用于预算弹性视觉文档检索（VDR）的二维Matryoshka训练框架，使得ColPali风格的多向量检索在维度（dimension）和层数（layer）两个维度上均具备弹性。在推理时，单个检索器可以自主选择二维可调预算，而无需针对不同预算分别训练独立模型。通过在多个代表性骨干网络上进行综合实验，我们证明，MM-Matryoshka相比直接截断基线方法能够保留显著更高的检索质量，同时大幅降低存储和计算开销，从而为高效的视觉文档检索提供了鲁棒的预算弹性。

</details>


### [29] [What neurosurgeons need to see: synthetic intra-operative MRI from ultrasound for brain-shift compensation in brain tumour surgery](https://arxiv.org/abs/2606.07658)
*Santiago Cepeda,Olga Esteban-Sinovas,Ignacio Arrese,Rosario Sarabia*

Main category: cs.CV

TL;DR: 提出一个端到端流水线，通过融合术前MRI、术中超声(ioUS)生成的合成MRI及基于该合成图像的变形配准，在术前影像空间中生成新的全脑MRI体积，从而弥补脑移位造成的神经导航退化，为外科医生提供类似于术中MRI的手术视野更新。

<details>
  <summary>Details</summary>

Motivation: 神经导航因开颅后脑移位而逐渐退化；术中MRI虽能补偿但成本高昂且很少可用，而术中超声(ioUS)虽廉价便捷，但现有结合ioUS与术前MRI的导航系统多依赖刚性配准，变形多模态配准受限于超声散斑对比度、窄视野及无法表示术前扫描中不存在的结构（尤其是切除腔和残余肿瘤）。

Method: 提出端到端流水线：采用2.5D残差-Transformer合成主干(ResViT-2.5D)从ioUS生成合成MRI，再通过两阶段配准（NiftyReg耦合基于合成图像锚定的SynthMorph阶段）将该合成MRI与术前MRI融合，生成术前影像空间中的全脑MRI体积。

Result: 在ReMIND术后队列中，ResViT-2.5D生成的合成图像在结构、强度和感知指标上紧密匹配术中T2。在14名受试者215个专家标记点上，基于合成锚定的配准将平均目标配准误差从6.27mm降至5.86mm，与经典NiftyReg基线(5.85mm)相当，且每例均产生微分同胚变形场。

Conclusion: 该方法的贡献不在于配准精度的提升，而在于生成的集成体积本身——在超声视野内反映了术中切除后的状态，为外科医生提供类似MRI的手术视野更新，有望整合到手术导航流程中。

Original Abstract: Maximal safe resection is the primary objective in glioma surgery. Neuronavigation guidance is progressively degraded by brain shift after dural opening. Intraoperative MRI can compensate but needs dedicated infrastructure and is rarely available, whereas intraoperative ultrasound (ioUS) is inexpensive, repeatable, and compatible with routine workflows. Navigation systems combining ioUS with preoperative MRI usually rely on rigid registration; even deformable multimodal registration is limited by ultrasound speckle contrast, a narrow field of view, and the inability to represent structures absent from the preoperative scan, most critically the resection cavity and residual tumor. We propose an end-to-end pipeline that generates a new whole-brain MRI volume in the preoperative imaging space by merging the preoperative MRI, a synthetic MRI generated from the ioUS, and a deformable registration anchored on that synthetic image. It integrates a 2.5D residual-transformer synthesis backbone (ResViT-2.5D) and a two-stage registration coupling NiftyReg with a synthesis-anchored SynthMorph stage, operating directly on raw scanner inputs. On a post-resection ReMIND cohort, ResViT-2.5D produced synthetic images closely matching the intraoperative T2 across structural, intensity, and perceptual metrics. In 14 subjects with 215 expert landmarks, the synthesis-anchored registration reduced the mean target registration error from 6.27 to 5.86 mm, matching a strong classical NiftyReg baseline (5.85 mm) while yielding a diffeomorphic deformation field in every subject. The contribution is not a gain in registration accuracy but the integrated volume itself, which inside the ultrasound field of view it reflects the intraoperative post-resection state. This provides the surgeon with an MRI-like update of the operative field with potential for integration into surgical-navigation workflows.

中文摘要翻译: 最大安全切除是胶质瘤手术的首要目标。开颅后脑移位会逐渐降低神经导航的引导精度。术中MRI可以补偿这一缺陷，但需要专用基础设施且很少可用；而术中超声(ioUS)成本低廉、可重复且与常规工作流程兼容。将ioUS与术前MRI结合的导航系统通常依赖刚性配准；即使是可变形多模态配准也受限于超声散斑对比度、狭窄视野以及无法表示术前扫描中不存在的结构，最关键的是切除腔和残余肿瘤。我们提出了一个端到端流水线，通过融合术前MRI、由ioUS生成的合成MRI以及基于该合成图像的变形配准，在术前影像空间中生成一个新的全脑MRI体积。该流水线集成了一个2.5D残差-Transformer合成主干(ResViT-2.5D)和一个两阶段配准（将NiftyReg与基于合成锚定的SynthMorph阶段耦合），直接对原始扫描仪输入进行操作。在术后ReMIND队列上，ResViT-2.5D生成的合成图像在结构、强度和感知指标上紧密匹配术中T2。在14名受试者的215个专家标记点上，基于合成锚定的配准将平均目标配准误差从6.27毫米降至5.86毫米，匹配了强大的经典NiftyReg基线（5.85毫米），同时每例均产生微分同胚变形场。该贡献不在于配准精度的提升，而在于集成体积本身——在超声视野内，它反映了术中切除后的状态。这为外科医生提供了手术视野的类似MRI的更新，具有整合到手术导航流程中的潜力。

</details>


### [30] [Need We Teach Foundation Models What is a Generative Image? Gradient-Free Generative Artifact Detection via Analytic Spectral Adaptation](https://arxiv.org/abs/2606.07660)
*Qiaoyu Chen,Bing Zhang*

Main category: cs.CV

TL;DR: 提出一种无梯度方法，将生成式伪造检测重新定义为OOD异常度量问题，在冻结的 foundation model 上建立绝对自然锚点，显著优于梯度优化范式，并支持在线学习和联邦协作。

<details>
  <summary>Details</summary>

Motivation: 基于梯度更新的微调方法会破坏 foundation model 的内在表征，在小样本下过拟合局部捷径，且大规模调参引入错误归纳偏置导致"锚点漂移"，损害跨域零样本伪造检测性能。

Method: 将检测从二分类重构为OOD异常度量问题；冻结 foundation model 作为稳定坐标系，通过分析解耦统计与语义偏差、注意力加权空间矩及感知不一致性的正交投影，在真实视觉流形上建立绝对自然锚点；使用Sherman-Morrison公式实现瞬时在线学习。

Result: 在极端零样本设定下（以人脸伪造训练，在通用T2I生成上测试），该方法显著优于基于梯度优化的范式；无反向传播的前向传播和线性求解器支持低延迟边缘部署。

Conclusion: 无梯度的OOD异常度量方法有效避免了锚点漂移，在跨域零样本检测中表现优越，且具备在线学习和隐私保护联邦协作的能力。

Original Abstract: Adapting foundation models to detect generative artifacts via gradient-based updates compromises their intrinsic representations. Under optimization on limited samples, models overfit to local domain shortcuts. Fine-tuning massive weights on specialized data introduces erroneous inductive biases, inducing a measurable $\mathcal{L}_2$ norm perturbation in the high-dimensional feature space -- a phenomenon we formalize as anchor drift. Amplified by nonlinear activations, this drift impairs zero-shot forgery detection across unseen domains.We propose a gradient-free methodology reframing detection from binary classification to an out-of-distribution (OOD) anomaly measurement problem. Treating a frozen foundation model as a stable coordinate system, we establish an absolute natural anchor on the real visual manifold by analytically decoupling statistical and semantic deviations, derived from attention-weighted spatial moments and orthogonal projection of perceptual inconsistencies. Evaluated in an extreme zero-shot setting (trained on face forgeries, tested on universal Text-to-Image generations), our method significantly outperforms gradient-optimized paradigms. Backpropagation-free forward passes and linear solvers enable hardware-agnostic, edge-deployable calibration with minimal latency. Furthermore, the Sherman-Morrison formula unlocks instantaneous online learning against novel attacks and enables privacy-preserving federated collaboration via covariance delta transmission.

中文摘要翻译: 通过基于梯度的更新来适配基础模型以检测生成伪影会破坏其内在表征。在有限样本上的优化过程中，模型会过拟合局部领域捷径。对专门数据进行大规模权重的微调会引入错误的归纳偏置，在高维特征空间中诱发可测量的$\mathcal{L}_2$范数扰动——我们将这一现象形式化为锚点漂移。该漂移经非线性激活函数放大后，损害了跨未见领域的零样本伪造检测能力。我们提出一种无梯度方法，将检测从二分类重构为分布外（OOD）异常度量问题。将冻结的基础模型视为稳定坐标系，通过分析解耦统计偏差与语义偏差——基于注意力加权的空间矩和感知不一致性的正交投影——在真实视觉流形上建立一个绝对自然锚点。在极端零样本设定下（以人脸伪造训练，在通用文本到图像生成上测试），我们的方法显著优于基于梯度优化的范式。无需反向传播的前向传播和线性求解器使得该方法能够实现硬件无关、可边缘部署的校准，且延迟极低。此外，Sherman-Morrison公式支持针对新型攻击的瞬时在线学习，并通过协方差增量传输实现隐私保护的联邦协作。

</details>


### [31] [PereStruct: Multimodal Semantic Assembly for Robust Historical Document Parsing](https://arxiv.org/abs/2606.07661)
*Maksim Shandybo,Ivan Bespalov,Daniil Yefimov,Marina Kosheleva,Alexander Loukianov*

Main category: cs.CV

TL;DR: 本文提出PereStruct，一个结合微调YOLO与语义组装模块的自动化管线，用于解析具有复杂多栏布局的历史报纸，在块到文章映射任务上达到F1=0.904，端到端忠实度显著优于通用视觉语言模型（BLEU约0.96 vs 0.34）。

<details>
  <summary>Details</summary>

Motivation: 历史文档（尤其是报纸）存在严重物理退化、不规则页面结构及多栏布局，现有最先进的视觉语言模型在此类分布外数据上表现不佳，成为大规模档案数字化的核心瓶颈。

Method: 提出PereStruct管线：(1) 微调YOLO架构进行版面分析与块检测，在1,426张人工标注扫描页上训练；(2) 新颖的语义组装模块，联合建模TF-IDF词汇语义相似度、YOLO视觉嵌入以及几何布局约束，实现文章重建。

Result: (1) 块到文章映射F1=0.904；(2) 端到端忠实度BLEU≈0.96，远超Qwen3.6-35B-A3B和Qwen3.6-Plus的0.34；(3) 发布599页训练语料与93页基准测试集。

Conclusion: 模块化架构在复杂历史版面任务上远超通用视觉语言模型，PereStruct为复杂档案材料的高保真数字化与语义重建建立了稳健基础。

Original Abstract: Parsing historical documents with complex, non-standard layouts remains a fundamental bottleneck in large-scale archival digitization. Unlike modern typography, historical newspapers exhibit severe physical degradation and highly irregular page structures that confound even state-of-the-art vision-language models, presenting severe out-of-distribution challenges. We address this gap with an automated pipeline specifically designed for parsing historical newspapers, documents characterized by particularly intricate multi-column layouts. Our approach combines a fine-tuned YOLO architecture for layout analysis and block detection, trained on 1,426 fully human-annotated scanned pages, with a novel semantic assembly module that reconstructs articles by jointly modeling lexical-semantic similarity via TF-IDF, visual embeddings from our fine-tuned YOLO, and geometric layout constraints. This multi-modal integration yields state-of-the-art performance, achieving an F1 score of 0.904 on block-to-article mapping. Notably, end-to-end evaluation against vision-language models (Qwen3.6-35B-A3B and Qwen3.6-Plus) demonstrates that PereStruct achieves substantially higher fidelity (BLEU approximately 0.96 vs 0.34), validating that modular architectures excel where generic VLMs fail on complex historical layouts. To support reproducibility and advance research in this domain, we release both the training corpus of 599 annotated pages and a curated PereStruct benchmark of 93 pages with expert-verified ground-truth block-to-article mappings. This framework establishes a robust foundation for high-fidelity digitization and semantic reconstruction of complex archival materials.

中文摘要翻译: 解析具有复杂非标准版面的历史文档，仍是大规模档案数字化的根本瓶颈。与现代排版不同，历史报纸存在严重的物理退化和高度不规则的页面结构，即使最先进的视觉语言模型也难以应对，呈现出严峻的分布外挑战。我们针对上述空白，提出了一种专为解析历史报纸（具有特别复杂的多栏布局特征的文档）设计的自动化管线。我们的方法结合了微调的YOLO架构进行版面分析和块检测（在1,426张完全人工标注的扫描页上训练），以及一个新颖的语义组装模块——该模块通过联合建模基于TF-IDF的词汇语义相似度、来自微调YOLO的视觉嵌入以及几何布局约束，来重建文章。这种多模态集成取得了当前最优性能，在块到文章映射任务上达到了0.904的F1分数。值得注意的是，针对视觉语言模型（Qwen3.6-35B-A3B和Qwen3.6-Plus）的端到端评估表明，PereStruct实现了显著更高的忠实度（BLEU约0.96对比0.34），验证了在通用VLM难以应对的复杂历史版面上，模块化架构具有显著优势。为支持可复现性并推动该领域研究，我们发布了包含599页标注页面的训练语料库，以及包含93页、带有专家验证的块到文章映射地面真值的PereStruct基准测试集。该框架为复杂档案材料的高保真数字化与语义重建奠定了坚实基础。

</details>


### [32] [MemoVAD: Resource-Efficient Video Anomaly Detection via Dynamic Semantic Memory in Edge Computing Scenarios](https://arxiv.org/abs/2606.07669)
*Guo Li,Jiandian Zeng,Yang Li,Zihao Peng,Ke Chen,Tian Wang*

Main category: cs.CV

TL;DR: MemoVAD提出了一种边缘-云端协作的视频异常检测框架，通过不确定性感知门控策略选择性调用云端的视觉语言模型，并结合动态语义记忆缓存，在边缘设备上实现了高效的VAD，显著降低通信开销的同时达到SOTA性能。

<details>
  <summary>Details</summary>

Motivation: 真实监控场景中，视频异常检测需要在边缘设备有限的计算资源下实现高级语义理解。视觉语言模型虽能提供丰富的开放词汇语义，但其延迟和计算成本过高，无法部署在边缘设备上。现有方法难以兼顾效率与语义丰富性。

Method: 提出MemoVAD边缘-云端协作框架：（1）边缘端使用轻量级检测器和因果时序上下文编码器（TCE）建模时序依赖；（2）基于主观逻辑的不确定性感知门控（UAG）策略，仅对高不确定性和语义新颖的片段查询云端VLM；（3）动态语义记忆（DSM）缓存VLM验证的原型，通过语义适配器逐步将VLM级语义融入边缘模型。

Result: 在UCF-Crime和XD-Violence数据集上通过真实边缘设备实验表明，MemoVAD大幅降低了通信开销，同时超越了现有最先进方法的性能。

Conclusion: 通过边缘-云端协同架构，选择性融入VLM语义，能够在实际边缘设备上实现高效且高性能的视频异常检测，有效解决了语义丰富性与计算资源受限之间的矛盾。

Original Abstract: Deploying Video Anomaly Detection (VAD) in real-world surveillance faces a fundamental tension between the demand for high-level semantics to ensure effectiveness and the limited computational resources of edge devices. Vision-Language Models (VLMs) provide rich open-vocabulary semantics, but their latency and computational cost preclude on-device deployment. To address the challenge, we propose MemoVAD, an edge-cloud collaborative framework that selectively incorporates VLM semantics into streaming VAD. MemoVAD runs most inference on the edge with a lightweight detector and a causal Temporal Context Encoder (TCE) to model temporal dependencies. Specifically, we introduce an Uncertainty-Aware Gating (UAG) policy grounded in Subjective Logic to model perceived uncertainty and query the cloud-based VLM only for high-uncertainty and semantically novel clips. Besides, a Dynamic Semantic Memory (DSM) is designed to cache VLM-verified prototypes for efficient retrieval, enabling the edge model to progressively incorporate VLM-level semantics via a semantic adapter. Experiments on UCF-Crime and XD-Violence datasets via a real edge device show that MemoVAD substantially reduces communication overhead while surpassing state-of-the-art performance.

中文摘要翻译: 在真实监控场景中部署视频异常检测（VAD）面临一个根本性矛盾：一方面需要高级语义来确保有效性，另一方面边缘设备的计算资源又十分有限。视觉语言模型（VLM）提供了丰富的开放词汇语义，但其延迟和计算成本阻碍了设备端部署。为解决这一挑战，我们提出MemoVAD，一种边缘-云端协作框架，能够选择性地将VLM语义融入流式VAD中。MemoVAD在边缘端运行大部分推理，使用轻量级检测器和因果时序上下文编码器（TCE）建模时序依赖。具体而言，我们引入了一种基于主观逻辑的不确定性感知门控（UAG）策略，对感知不确定性进行建模，仅对高不确定性和语义新颖的片段查询云端VLM。此外，我们设计了动态语义记忆（DSM），用于缓存经过VLM验证的原型以实现高效检索，使边缘模型能够通过语义适配器逐步融入VLM级别的语义。在UCF-Crime和XD-Violence数据集上通过真实边缘设备进行的实验表明，MemoVAD大幅降低了通信开销，同时超越了现有的最先进性能。

</details>


### [33] [Liquid Neural Networks as a Drop-in Continuous-Time Deformation Field for Dynamic 3D Gaussian Splatting](https://arxiv.org/abs/2606.07670)
*Mingzhao Li,Arghya Pal,Guan Yuan Tan*

Main category: cs.CV

TL;DR: 本文提出用液态神经网络（LNN）中的闭式连续时间（CfC）单元替代D-3DGS中的MLP变形场，将离散的逐帧偏移预测变为显式的连续时间函数，在提升高频运动重建质量的同时保持管道其他部分不变。

<details>
  <summary>Details</summary>

Motivation: D-3DGS使用位置编码MLP根据帧时间t变形3D高斯，但MLP架构本质上耦合不了不同t值，只能预测离散的逐帧偏移，时间平滑性仅作为优化的副产品出现，缺乏显式的连续时间建模。

Method: 将变形场重新设计为闭式连续时间（CfC）单元的堆叠——CfC是液态时间常数ODE的闭式解，每个单元通过sigmoid时间门在两个候选隐藏状态间插值，从而将学习到的平滑时间响应直接嵌入损失景观，无需数值求解器。

Result: 在8个D-NeRF和7个NeRF-DS场景上，液态变形场在整体上匹配或超过MLP基线，最大的性能增益集中在高频关节运动最剧烈的场景上。

Conclusion: 这是一种近乎零摩擦的架构设计，将离散的MLP变形场转变为t的显式连续时间函数，有效提升了动态场景重建中高频运动的表现。

Original Abstract: Deformable 3D Gaussian Splatting (D-3DGS) re-constructs dynamic scenes from monocular video by deforming a canonical set of 3D Gaussians through a positional-encoded MLP of frame time t. Although fitted to a continuous variable, the MLP couples no two values of t in its architecture and effectively predicts discrete per-frame offsets, leaving temporal smoothness to emerge only as a byproduct of optimisation. We redesign the deformation field as a stack of Closed-form Continuous-time (CfC) cells, a Liquid Neural Network (LNN), that is the closed-form solution of the Liquid Time-constant ODE while preserving every other part of the D-3DGS pipeline. Each cell exposes a sigmoidal time gate that interpolates between two candidate hidden states, baking a learned smooth response to t into the loss landscape without invoking any numerical solver. On the eight D-NeRF and seven NeRF-DS scenes the liquid field matches or exceeds the MLP baseline in aggregate, with its largest gains concentrated on the scenes with the most high-frequency articulated motion. The result is a near-zero-friction architectural design that turns the discrete MLP deformation field into an explicit continuous-time function of t.

中文摘要翻译: 可变形3D高斯泼溅（D-3DGS）通过一个以帧时间t为输入的位置编码MLP对一组规范3D高斯进行变形，从而从单目视频重建动态场景。尽管拟合的是连续变量，但该MLP在架构上并未耦合t的任意两个取值，实际上预测的是离散的逐帧偏移，时间平滑性仅仅是优化的副产品。我们将变形场重新设计为一组闭式连续时间（CfC）单元的堆叠——这是一种液态神经网络（LNN），是液态时间常数ODE的闭式解，同时保留D-3DGS管道的所有其他部分。每个单元暴露一个sigmoid时间门，在两个候选隐藏状态之间进行插值，从而在不调用任何数值求解器的情况下，将学习到的对t的平滑响应直接嵌入损失景观。在8个D-NeRF和7个NeRF-DS场景上，液态变形场在整体上匹配或超过MLP基线，其最大的性能增益集中在高频关节运动最剧烈的场景上。结果是一种近乎零摩擦的架构设计，将离散的MLP变形场转变为t的显式连续时间函数。

</details>


### [34] [What Makes Video World Model Latents Action-Relevant: Prediction over Reconstruction](https://arxiv.org/abs/2606.07687)
*Jewon Yeom,Hanseul Kim,Jeongjae Park,Sungmok Jung,Jaejin Lee,Taesup Kim*

Main category: cs.CV

TL;DR: 本文通过统一探针评估发现，视频世界模型中的动作相关结构主要由时间视频预训练驱动，而非像素重建保真度；特征级潜在预测提供额外收益但并非主要因素。

<details>
  <summary>Details</summary>

Motivation: 探究视频世界模型中哪些预训练信号能诱导出潜在空间中与动作相关的结构，即区分不同预训练范式（图像自监督、视频预训练、重构自编码器、扩散模型等）对动作可恢复性的贡献。

Method: 采用统一的反向动力学探针评估框架，在多种编码器族（图像自监督、视频预训练、重构自编码器、扩散模型、捷径强制动力学模型）上进行评估，通过共同的反向动力学探针目标来衡量动作相关结构的质量。

Result: (1) 时间视频预训练是驱动动作相关结构的主要因素，而非像素重建质量；(2) 视频预训练的自监督编码器在视觉保真度和动作预测之间取得最佳帕累托权衡；(3) 自然视频时间上下文带来主要收益，特征级潜在预测仅提供额外小幅增益；(4) 在CALVIN基准中，静态环境任务可通过强图像先验部分掩盖时间结构的重要性；(5) 反向动力学监督显著提高了对视觉损坏的鲁棒性。

Conclusion: 时间预测结构（而非重建保真度）是产生动作相关视频表示的主要成分，动作感知目标能够正则化潜在空间的几何结构，超越干净环境下的性能表现。

Original Abstract: Video world models are increasingly used to provide predictive visual representations, yet it remains unclear which pretraining signals induce action-relevant structure in their latent spaces. We study this question through a unified probe-based evaluation across diverse encoder families, including image-only self-supervision, video pretraining with and without latent prediction, reconstruction-based autoencoders, diffusion models, and shortcut-forcing dynamics models. Using a common inverse-dynamics probing objective, we find that action-relevant structure is driven primarily by temporal video pretraining rather than pixel reconstruction fidelity: models with strong pixel decoding quality can exhibit near-zero action recoverability, while video-pretrained self-supervised encoders consistently achieve the best Pareto trade-off between visual fidelity and action prediction. Comparing V-JEPA and VideoMAE further shows that most gains arise from natural-video temporal context, with feature-level latent prediction providing a smaller additional benefit. These trends transfer across robotic benchmarks, though CALVIN reveals that static-environment tasks can partially mask the importance of temporal structure by allowing strong image priors to suffice. Finally, inverse-dynamics supervision substantially improves robustness to visual corruption, suggesting that action-aware objectives regularize latent geometry beyond clean-setting performance. Our results identify temporal predictive structure -- not reconstruction fidelity -- as the primary ingredient underlying action-relevant video representations.

中文摘要翻译: 视频世界模型越来越多地被用于提供预测性视觉表示，但尚不清楚哪些预训练信号能在其潜在空间中诱导出与动作相关的结构。我们通过一个统一的基于探针的评估框架，在多种编码器族上研究这一问题，包括纯图像自监督、带/不带潜在预测的视频预训练、基于重构的自编码器、扩散模型以及捷径强制动力学模型。使用共同的反向动力学探针目标，我们发现动作相关结构主要由时间视频预训练驱动，而非像素重建保真度：具有强像素解码质量的模型可能表现出近乎为零的动作可恢复性，而视频预训练的自监督编码器在视觉保真度和动作预测之间始终实现最佳帕累托权衡。比较V-JEPA和VideoMAE进一步表明，大多数增益来自自然视频时间上下文，特征级潜在预测仅提供额外的小幅收益。这些趋势可迁移至机器人基准测试，尽管CALVIN表明，静态环境任务通过允许强图像先验足够使用，可能部分掩盖时间结构的重要性。最后，反向动力学监督显著提高了对视觉损坏的鲁棒性，这表明动作感知目标在干净环境性能之外正则化了潜在空间的几何结构。我们的结果指出，时间预测结构——而非重建保真度——是产生动作相关视频表示的主要成分。

</details>


### [35] [Struct-Searcher: Agentic Structural Thinking Advances Multimodal Deep Information Seeking](https://arxiv.org/abs/2606.07689)
*Fan Zhang,Vireo Zhang,Shengju Qian,Haoxuan Li,Zheng Lian,Hao Wu,Yuan Gao,Xinyu Geng,Xin Wang,Pheng-Ann Heng*

Main category: cs.CV

TL;DR: 本文提出Struct-Searcher，一种基于信念修正理论的结构化智能体工作流，通过维护动态演化的多模态结构化图来实现冲突感知的深度信息搜索，在多个基准测试上取得最优性能。

<details>
  <summary>Details</summary>

Motivation: 现有深度研究智能体主要基于证据累积模型，线性聚合证据，缺乏处理跨异构模态间矛盾信息的机制，无法有效应对多模态环境中的冲突信息。

Method: 基于信念修正理论，设计结构化的智能体工作流，在整个推理过程中显式维护一个动态演化的多模态结构化图，实现冲突感知的多模态深度信息搜索。

Result: Struct-Searcher即插即用且模型无关，在BrowseComp-VL上基于五种不同骨干模型平均相对准确率提升17.2%；在MM-BrowseComp、HLE-VL和BrowseComp-VL上分别超越次优方法3.7%、1.5%和0.7%。

Conclusion: 基于信念修正理论的结构化工作流能够有效处理多模态矛盾信息，显著提升深度研究智能体的性能，且具有良好的通用性和可迁移性。

Original Abstract: Deep research agents have attracted increasing attention for their ability to collect large-scale online information to acquire target knowledge, with recent efforts shifting from purely text-based information seeking to multimodal settings. However, existing agentic workflows are largely aligned with evidence accumulation models, which linearly aggregate evidence and lack principled mechanisms for handling contradictory information across heterogeneous modalities. Towards this end, we propose Struct-Searcher, a structural agentic workflow grounded in belief revision theory that explicitly maintains an evolving multimodal structural graph throughout the reasoning process, enabling effective conflict-aware multimodal deep information seeking. Extensive experiments across multiple benchmark datasets and backbone models demonstrate that Struct-Searcher is (1) plug-and-play and model-agnostic, yielding an average relative accuracy improvement of 17.2% on BrowseComp-VL across five different backbones. (2) top-performing, consistently outperforming state-of-the-art vision-language models (VLMs) and deep research agents, with relative accuracy improvements of 3.7% on MM-BrowseComp, 1.5% on HLE-VL, and 0.7% on BrowseComp-VL over the second-best competing approach.

中文摘要翻译: 深度研究智能体因其能够收集大规模在线信息以获取目标知识的能力而日益受到关注，近期的研究正从纯文本信息搜索转向多模态场景。然而，现有的智能体工作流大多与证据累积模型对齐，线性地聚合证据，缺乏处理跨异构模态间矛盾信息的原理性机制。为此，我们提出Struct-Searcher，一种基于信念修正理论的结构化智能体工作流，在整个推理过程中显式维护一个动态演化的多模态结构化图，从而支持有效的冲突感知多模态深度信息搜索。在多个基准数据集和骨干模型上的大量实验表明，Struct-Searcher是（1）即插即用且模型无关的，在BrowseComp-VL上基于五种不同骨干模型平均相对准确率提升17.2%；（2）性能最优的，持续超越最先进的视觉语言模型（VLM）和深度研究智能体，在MM-BrowseComp、HLE-VL和BrowseComp-VL上分别比次优方法相对准确率提升3.7%、1.5%和0.7%。

</details>


### [36] [Cross-View Urban Traffic Dataset: Drone-Supervised Ground Truth for Monocular Bird's-Eye View Localization](https://arxiv.org/abs/2606.07708)
*Prakhar Bhardwaj,Simone Weikl,Kilian Mang,Elia Jonas Sandtner*

Main category: cs.CV

TL;DR: 本文提出了一个跨视角城市交通感知数据集和基准，包含同步的行人视角自行车视频和无人机航拍视频，支持跨视角身份匹配和自车视角到鸟瞰图预测两个关联任务，并提供了标准化评估、标注工具和基线方法。

<details>
  <summary>Details</summary>

Motivation: 现有城市驾驶和V2X数据集缺乏跨极端视角的身份级别对齐，无法支持交叉路口场景中需要身份保持、局部交互和全局空间结构联合推理的交通分析任务。

Method: 构建了同步的ego-centric自行车视频和无人机航拍视频数据集，定义了跨视角身份匹配（街景与无人机视角目标轨迹匹配）和ego-to-BEV预测（利用航拍监督）两个任务，提供了基于楔形的跨视角匹配方法和三种BEV预测基线（逆透视映射、MonoLayout风格基线、回归基线）。

Result: 跨视角匹配召回率较高但受限于过度分配和时间不一致性；ego-to-BEV预测受益于航拍监督但在轻量级单目感知下远未饱和。

Conclusion: 该基准可行但具有挑战性，有望支持跨视角感知、城市场景对齐和自车到全局交通理解等未来研究。

Original Abstract: We introduce a dataset and benchmark for cross-view urban traffic perception built from synchronized ego-centric bicycle videos and aerial drone videos recorded at real urban intersections. The benchmark targets two linked tasks: cross-view identity matching between street-view and drone-view object tracks, and ego-to-bird's-eye-view prediction using aerial supervision. In contrast to prior urban driving and V2X datasets, our benchmark provides identity-level alignment across radically different viewpoints together with standardized evaluation, annotation tooling, and baseline implementations. This setting is motivated by intersection-centric traffic analysis, where identity preservation, local interactions, and global spatial structure must be reasoned about jointly across views. We evaluate methods at both the track and frame levels, including cross-view ID precision/recall/IDF1, near--far breakdowns, temporal stability, and consistency metrics. We also provide baseline results for wedge-based cross-view matching and for three BEV prediction baselines: inverse perspective mapping, a MonoLayout-style learned baseline, and a regression baseline. The results show that the benchmark is feasible but challenging: cross-view matching achieves strong recall yet remains limited by over-assignment and temporal inconsistency, while ego-to-BEV prediction benefits from aerial supervision but remains far from saturated under lightweight monocular sensing. We hope that this benchmark will support future research on cross-view perception, urban scene alignment, and ego-to-global traffic understanding.

中文摘要翻译: 我们介绍了一个用于跨视角城市交通感知的数据集和基准，该数据集由在真实城市交叉路口录制的同步行人视角自行车视频和无人机航拍视频构建而成。该基准针对两个关联任务：街景视角与无人机视角目标轨迹之间的跨视角身份匹配，以及利用航拍监督的自车到鸟瞰图预测。与先前的城市驾驶和V2X数据集不同，我们的基准提供了跨极端视角的身份级别对齐，以及标准化的评估、标注工具和基线实现。该设定源于交叉路口中心的交通分析需求，其中身份保持、局部交互和全局空间结构需要跨视角联合推理。我们在轨迹级别和帧级别评估方法，包括跨视角身份精度/召回率/IDF1、近-远分解、时间稳定性和一致性指标。我们还提供了基于楔形的跨视角匹配基线以及三种BEV预测基线（逆透视映射、MonoLayout风格学习基线和回归基线）的基准结果。结果表明，该基准可行但具有挑战性：跨视角匹配实现了较高的召回率，但仍然受限于过度分配和时间不一致性；而自车到BEV预测受益于航拍监督，但在轻量级单目感知下远未饱和。我们希望该基准能够支持未来在跨视角感知、城市场景对齐和自车到全局交通理解方面的研究。

</details>


### [37] [DroneDAR: Long-Range Drone Distance Estimation Using Monocular Vision and Bounding-Box Features](https://arxiv.org/abs/2606.07756)
*Knut Peterson,Zaid Mayers,David Han*

Main category: cs.CV

TL;DR: 本文研究利用图像裁剪块和边界框几何信息对远程小型无人机进行单目距离估计，提出了引入门控机制的DroneDAR模型，分析了骨干网络容量、裁剪分辨率和损失函数对不同距离范围性能的影响，并探讨了长距离下的常见失败模式。

<details>
  <summary>Details</summary>

Motivation: 长距离图像中小型无人机的准确距离估计对跟踪和态势感知至关重要，但由于目标尺度剧烈变化、背景杂波和视觉噪声，该任务极具挑战性。现有方法在真实长距离条件下的鲁棒性不足。

Method: 提出DroneDAR模型，采用卷积骨干网络并引入轻量级门控机制显式融合边界框线索（边界框几何特征）与外观特征，基于图像裁剪块进行单目距离回归。

Result: 实验分析了骨干网络容量、裁剪分辨率和回归损失函数对不同距离范围性能的影响，揭示了长距离条件下对边界框噪声敏感以及裁剪块纹理细节减少等常见失败模式。

Conclusion: 为设计和训练在真实长距离条件下保持鲁棒性的距离估计器提供了指导，并指出了当无人机仅占几个像素时提升可靠性的改进方向。

Original Abstract: Accurate distance estimation for small drones in long-range imagery is important for tracking and situational awareness, yet remains challenging due to extreme target scale variation, background clutter, and noisy visual cues. This paper studies monocular drone distance estimation using image crops together with bounding-box geometry, a practical setting in which a detector provides a candidate drone region and the model predicts range from appearance and box-derived features. We evaluate a Droneranger-style baseline, and introduce a new DroneDAR (Drone Detection And Ranging) model that combines a convolutional backbone with explicit bounding-box cues through a lightweight gating mechanism. Experiments analyze how backbone capacity, crop resolution, and regression loss functions affect performance across distance regimes. We further examine common failure modes at long distances, including sensitivity to bounding-box noise and reduced texture detail in the crop. The results provide guidance for designing and training range estimators that remain robust under real-world long-range conditions and highlight directions for improving reliability when drones occupy only a few pixels.

中文摘要翻译: 远程图像中小型无人机的准确距离估计对于跟踪和态势感知至关重要，但由于极端的目标尺度变化、背景杂波和噪声视觉线索，这一任务仍然具有挑战性。本文研究使用图像裁剪块和边界框几何信息进行单目无人机距离估计，这是一种实用设置，其中检测器提供候选无人机区域，模型从外观和框衍生特征预测距离。我们评估了一个Droneranger风格的基线，并引入了一种新的DroneDAR（无人机检测与测距）模型，该模型通过轻量级门控机制将卷积骨干网络与显式的边界框线索相结合。实验分析了骨干网络容量、裁剪分辨率和回归损失函数在不同距离范围内对性能的影响。我们进一步研究了长距离下的常见失败模式，包括对边界框噪声的敏感性和裁剪块中纹理细节的减少。研究结果为设计和训练在真实长距离条件下保持鲁棒性的距离估计器提供了指导，并指出了当无人机仅占几个像素时提高可靠性的改进方向。

</details>


### [38] [Quantum-Enhanced Similarity Measures for Polarimetric Materials Classification](https://arxiv.org/abs/2606.07766)
*Sara Shojaei,Seyed Mohamad Ali Tousi,Emma Bennett,Param Sangani,Ali Shiri Sichani,Ilker Ersoy,Hadi Ali-Akbarpour,Filiz Bunyak,G. N. DeSouza*

Main category: cs.CV

TL;DR: 提出了一种量子-经典混合流水线，将偏振材料分类建模为点匹配问题。利用SWAP测试电路计算量子态编码后的嵌入向量保真度，实现材料分类，在23种材料数据集上取得了有竞争力的精度，并具备开放集判别潜力。

<details>
  <summary>Details</summary>

Motivation: 探索NISQ时代的量子计算在材料识别任务中的应用潜力，构建一个可行的量子-经典混合材料分类框架。

Method: 使用偏振光反射的体素立方体训练编码器生成32维嵌入；推理时丢弃编码器头，将嵌入编码为量子态概率幅；通过SWAP测试电路估计查询立方体与锚点数据集之间的嵌入保真度，聚合保真度作为材料相似度分数，取最高聚合保真度的锚点类别作为查询材料类别。

Result: 在23种材料（每种约800个样本）的Mueller矩阵数据集上评估，将提出的量子SWAP测试方法与基于最优传输的经典分类器进行了比较，结果表明该方法具有竞争力的分类精度和开放集判别潜力。

Conclusion: 该方法作为一种面向NISQ设备的材料识别路径是可行的，兼具分类准确性与开放集判别能力。

Original Abstract: We present a quantum--classical hybrid pipeline for polarimetric material classification that casts this as a point-matching problem. Voxel cubes, containing polarized light reflections, are used to train an encoder to produce 32-dimensional embeddings for the voxels of the cubes. At inference, the encoder head is discarded and the embeddings are encoded as probability amplitudes of quantum states. Next, a SWAP-test circuit estimates the fidelity between each of the 32D embeddings from the query cube and a dataset of anchor cubes. The aggregated fidelity serves as materials similarity scores, and the class of the anchor with highest aggregated fidelity is deemed as the class of the queried material. We evaluate our approach on a dataset of 23 materials ($\approx$800 samples each) derived from their Mueller matrices. The point-matching approaches from the proposed quantum SWAP-test and a classical classifier using Optimal Transport are compared. Our results demonstrate the competitive classification accuracy alongside open-set discrimination potential, establishing it as a viable path toward NISQ-based material recognition.

中文摘要翻译: 我们提出了一种用于偏振材料分类的量子-经典混合流水线，将该问题建模为点匹配问题。包含偏振光反射的体素立方体被用于训练编码器，以生成这些立方体的32维嵌入。在推理时，丢弃编码器头，并将嵌入编码为量子态的概率幅。然后，SWAP测试电路估算查询立方体的每个32维嵌入与锚点立方体数据集之间的保真度。聚合后的保真度作为材料相似度分数，具有最高聚合保真度的锚点类别被判定为查询材料的类别。我们在一个由23种材料（每种约800个样本）的Mueller矩阵构成的数据集上评估了所提方法。比较了所提出的量子SWAP测试点匹配方法与使用最优传输的经典分类器。实验结果表明，该方法具有有竞争力的分类精度以及开放集判别潜力，从而确立其作为一条面向NISQ材料识别的可行路径。

</details>


### [39] [DALE-CT: Depth-Aware Foundation Models for Computed Tomography](https://arxiv.org/abs/2606.07775)
*Evan W. Damron,Mahmut S. Gokmen,Mitchell A. Klusty,Caroline N. Leach,Emily B. Collier,V. K. Cody Bumgardner*

Main category: cs.CV

TL;DR: 本文提出DALE-CT，一种基于2D切片架构的CT图像自监督学习模型，通过3D深度感知预训练和双重监督（解剖掩膜+异常标注）策略，在无需文本监督且数据量远小于3D模型的情况下，达到了与最先进3D视觉-语言模型接近的性能（Macro AUROC 0.833）。

<details>
  <summary>Details</summary>

Motivation: 自监督学习和视觉-语言模型的突破推动了对高容量CT视觉编码器的需求，但原生3D模型计算开销大、数据需求高，亟需一种灵活、可扩展的2D替代方案，同时要求2D模型能有效感知3D深度信息以提升表征质量。

Method: 基于LeJEPA框架从零训练2D模型族DALE-CT；提出3D深度感知预训练策略，利用自动解剖掩膜和人工标注异常的密集辅助监督信号增强表征；采用线性探针结合多实例学习（MIL）进行多异常检测评估。

Result: 双监督模型DALE-CT-2S在冻结主干上进行线性探针评估，多异常检测的Macro AUROC达到0.833，与最先进的3D视觉-语言模型性能接近，且完全从零训练，数据量显著更少，无需文本监督。

Conclusion: 基于2D切片架构的深度感知自监督学习能以更低的计算和数据成本取得与3D模型相当的性能，为CT影像分析提供了高效的替代方案；代码和模型已开源以促进可重复性研究。

Original Abstract: Recent breakthroughs in self-supervised learning (SSL), such as the Latent-Euclidean Joint-Embedding Predictive Architecture (LeJEPA), alongside successes in integrating visual encoders with language models, have driven the demand for adaptable, high-capacity vision encoders in Computed Tomography (CT). In this work, we explore 2D slice-based architectures as a flexible alternative to native 3D models for processing volumetric CT data. Using the CT-RATE dataset, we trained DALE-CT (Depth-Aware Latent-Euclidean Computed Tomography), a 2D model family built entirely from scratch using LeJEPA, and compared its performance against a continually pre-trained DINOv2 baseline. To enhance representation quality, we developed a novel 3D depth-aware pre-training strategy anchored by dense auxiliary supervision from both automated anatomical masks and human-annotated abnormalities. Under linear probe evaluation with Multiple Instance Learning (MIL) for multi-abnormality detection, the frozen backbone of this dual-supervised model (DALE-CT-2S) achieves a Macro AUROC of 0.833. This performance demonstrates near-parity with state-of-the-art 3D vision-language models, achieved entirely from scratch with significantly less data and no textual supervision. To ensure reproducibility, all training code, evaluation scripts, and model weights have been made publicly available.

中文摘要翻译: 近期自监督学习（SSL）的突破性进展——例如潜在-欧几里得联合嵌入预测架构（LeJEPA），以及视觉编码器与语言模型成功整合的成果——推动了对计算机断层扫描（CT）领域适应性高容量视觉编码器的需求。在本工作中，我们探索了基于2D切片架构作为原生3D模型处理体素CT数据的灵活替代方案。利用CT-RATE数据集，我们训练了DALE-CT（深度感知潜在-欧几里得计算机断层扫描），这是一个完全基于LeJEPA从零构建的2D模型族，并将其性能与持续预训练的DINOv2基线进行了比较。为提升表征质量，我们开发了一种新颖的3D深度感知预训练策略，该策略通过来自自动解剖掩膜和人工标注异常的双重密集辅助监督信号进行锚定。在使用多实例学习（MIL）进行多异常检测的线性探针评估下，该双监督模型（DALE-CT-2S）的冻结主干达到了0.833的Macro AUROC。这一性能证明了其与最先进的3D视觉-语言模型近乎持平，且完全从零训练，数据量显著更少，无需任何文本监督。为确保可重复性，所有训练代码、评估脚本和模型权重均已公开。

</details>


### [40] [The Last Visible Pixel: Probing Fine-Scale Perception in Vision-Language Models](https://arxiv.org/abs/2606.07861)
*Lujun Li,Lama Sleem,Niccolo Gentile,Yangjie Xu,Yewei Song,Wenbo Wu,Radu State*

Main category: cs.CV

TL;DR: 本文提出FineSightBench基准，系统评估视觉语言模型在像素级感知（4-48px）上的能力，发现感知在12px左右饱和，而推理即使在更大尺度也表现有限，暴露了模型在细粒度视觉推理上的根本缺陷。

<details>
  <summary>Details</summary>

Motivation: 现有视觉语言模型在多模态理解和推理上表现出色，但它们在细粒度视觉感知（即能够可靠感知的最小视觉模式尺寸）方面的能力尚未被充分探索。

Method: 构建FineSightBench基准，将感知任务（字母、形状、物体的像素级识别）与推理任务（空间推理、计数、排序）分离，在4-48px的受控尺度范围内进行系统评估。

Result: 感知能力在约12px时达到饱和，而推理能力即使在更大尺度上仍然受限，存在持续的计数和序列错误。

Conclusion: 当前视觉语言模型在细粒度视觉推理上存在根本性缺陷，亟需更严格的评估和改进。

Original Abstract: Recent vision-language models (VLMs) excel at multimodal understanding and reasoning, yet their fine-grained visual perception remains underexplored. A natural extension of ``How many r are there in Strawberry?'' asks: how small a visual pattern can a VLM reliably perceive? As such, we introduce FineSightBench, a new benchmark that systematically probes this limit by separating perception tasks (pixel-level recognition of letters, shapes, objects) from reasoning tasks (spatial reasoning, counting, ordering over small targets) across controlled scales of 4--48px. Through comprehensive experiments and detailed failure mode analysis on state-of-the-art models, we reveal a sharp dissociation: perception saturates around 12px, while reasoning remains limited even at larger scales, with persistent numeracy and sequence errors. These findings expose fundamental deficiencies in VLMs' fine-scale visual reasoning that demand more rigorous evaluation.

中文摘要翻译: 最近的视觉语言模型在多模态理解和推理方面表现出色，但其细粒度视觉感知能力仍未得到充分探索。"草莓（Strawberry）中有多少个字母r？"这一问题的自然延伸是：视觉语言模型能够可靠感知的最小视觉模式是多小？为此，我们引入了FineSightBench，这是一个全新的基准，通过将感知任务（字母、形状、物体的像素级识别）与推理任务（空间推理、计数、小目标排序）在4-48px的受控尺度范围内进行分离，系统地探索了这一极限。通过对最先进模型进行全面的实验和详细的故障模式分析，我们揭示了一个显著的分离现象：感知能力在约12px时达到饱和，而推理能力即使在更大尺度上仍然受限，存在持续的计数和序列错误。这些发现暴露了视觉语言模型在细粒度视觉推理方面的根本缺陷，亟需更严格的评估。

</details>


### [41] [VisualFLIP: Do Predictions Depend on Task-Critical Visual Evidence in Multimodal Reasoning?](https://arxiv.org/abs/2606.07872)
*Didi Zhu,Changrui Chen,Stefanos Zafeiriou,Jiankang Deng*

Main category: cs.CV

TL;DR: 提出了VisualFLIP基准测试，通过配对图像扰动来评估多模态大语言模型的视觉接地能力，发现准确率不足以衡量推理是否正确基于视觉证据。

<details>
  <summary>Details</summary>

Motivation: 现有评估仅依赖准确率，但模型可能答案正确而推理有缺陷，无法判断预测是否真正由关键视觉证据支持。

Method: 构建了包含1,374张图像的配对基准，在保持问题不变的前提下对图像进行最小扰动，使正确答案确定性翻转；提出配对准确率和崩溃率两个指标。

Result: 评估24个MLLM发现：配对正确性与证据依赖性既有关系又有区别；能力强的模型在关键视觉变化后仍可能未更新答案；部分模型在序列设置中崩溃更严重。

Conclusion: 准确率不足以评估视觉接地，配对指标能更有效揭示模型推理是否真正依赖视觉证据。

Original Abstract: When a multimodal large language model answers a visual reasoning question correctly, is the prediction actually supported by the task-critical visual evidence? Correct answers can coexist with flawed reasoning, making accuracy alone an incomplete test of grounding. We introduce VisualFLIP, a paired benchmark with 1,374 images arranged as same-question perturbation pairs across cardinality, attribute, spatial, and logic tasks. Each pair keeps the question fixed but minimally changes the evidence so the gold answer deterministically flips. We evaluate 24 MLLMs with pair accuracy, which requires solving both sides of a pair, and Collapse Rate (CR), which measures how often a model that solves at least one side repeats the same non-empty answer for both images. Together, these metrics show that paired correctness and evidence dependence are related but distinct: capable models can still fail to update after task-critical visual changes, and collapse becomes more severe for some models when the edited image follows an earlier answer in a sequential setting. Further details are available on our project page: https://didizhu-judy.github.io/VisualFLIP/

中文摘要翻译: 当一个多模态大语言模型正确回答了一个视觉推理问题时，该预测是否真正得到了任务关键视觉证据的支持？正确的答案可能与有缺陷的推理并存，这使得准确率本身成为不完整的接地测试。我们提出了VisualFLIP，一个配对基准测试，包含1,374张图像，按相同问题扰动配对方式排列，涵盖基数、属性、空间和逻辑任务。每对保持问题不变，但最小程度地改变证据，使黄金答案确定性翻转。我们使用配对准确率（要求同时解出配对两侧）和崩溃率（衡量至少解出一侧的模型对两张图像重复相同非空答案的频率）评估了24个MLLM。这两个指标共同表明，配对正确性与证据依赖性既相关又不同：有能力的模型仍然可能在任务关键视觉变化后未能更新答案，并且在某些模型中，当编辑后的图像在序列设置中跟随先前的答案时，崩溃变得更加严重。更多详情请参见我们的项目页面：https://didizhu-judy.github.io/VisualFLIP/

</details>


### [42] [The Cross-Architecture Substrate: A Domain-Transcendent, Calibration-Surviving Geometric Invariant of Modern Vision Encoders](https://arxiv.org/abs/2606.07882)
*Yousef Radwan*

Main category: cs.CV

TL;DR: 研究发现不同架构的视觉编码器（分类、对比、重建、图文匹配）在训练后，其前16个主成分方向收敛到同一个16维几何结构（称为跨架构基底）。该基底在多种视觉领域间高度一致，但无法跨模态迁移，可用于标签无关的可迁移性过滤、域检测、低样本探测和无教师蒸馏辅助。

<details>
  <summary>Details</summary>

Motivation: 探究不同视觉神经网络（分类、对比、重建、图文匹配）训练后的内部表示是否因训练目标不同而不同。直觉上应不同，但研究发现它们实际上收敛到一个共同的几何结构。

Method: 对13种现代视觉编码器进行PCA、中心核对齐（CKA）和Pang 2026校准分析，提取前16个主成分方向构成的几何结构（跨架构基底），并在多种视觉领域（自然照片、医疗CT、卫星、显微镜、素描、深度图、热红外、天文）中验证其普适性。

Result: 跨架构基底在四个视觉域的中位Procrustes-CKA为0.679，八个域为0.604；Pang校准下全局分离比7.4倍、局部4.82-5.30（p<10^{-44}）；非像素统计、非Gabor特征、非随机投影；在训练前10%即出现。应用：标签无关可迁移性过滤（比LogME快3倍，+0.15 Kendall-tau）、四域检测器（99.6%）、16维低样本探测（比768维DINOv2高3.78pp）、无教师蒸馏辅助（33对中最高提升7.56pp）。基底无法跨模态、无法帮助跨范式蒸馏、无法预测迁移质量（rho=0.08）。

Conclusion: 不同视觉编码器在训练后收敛到同一个低维几何结构（跨架构基底），该基底具有跨域普适性但无法跨模态，揭示了视觉表示学习中的一种内在共性，并提供了多种实用应用。

Original Abstract: Different vision neural networks -- trained to classify, contrast, reconstruct, or match images to text -- should have correspondingly different internal representations. We report that they do not. After training, the top sixteen principal directions of variation inside thirteen modern vision encoders converge to the same sixteen-dimensional geometric object. We call this the cross-architecture substrate and study it with PCA, centred kernel alignment (CKA), and Pang 2026 calibration. The substrate transports across four visual domains (natural photographs, medical CT, satellite, microscopy) at median Procrustes-CKA 0.679, and across eight domains (adding sketches, depth, thermal infrared, astronomy) at 0.604, every pair >0.40. It survives Pang calibration globally (7.4x disc-vs-MAE separation, n=13,394) and locally (4.82-5.30, p<10^{-44}). It is not pixel statistics (0.263), not Gabor features (0.31), not a random projection (0.041), and emerges in the first 10% of training while accuracy keeps climbing. We deliver four applications: a label-free transferability filter beating LogME (3x faster, +0.15 Kendall-tau); a four-way domain detector (99.6% accuracy); a frozen low-shot probe (16 dims beat 768-dim DINOv2 by 3.78pp at N=50 labels per class); and a teacher-free distillation auxiliary matching trained-teacher KD on 33 pairs (7.56pp peak gain at 10% label fraction). The substrate does not cross modalities, does not help cross-paradigm distillation, and does not predict transfer quality (rho=0.08 against transfer accuracy).

中文摘要翻译: 不同的视觉神经网络——经过训练以进行分类、对比、重建或将图像与文本匹配——本应具有相应不同的内部表示。但我们报告它们并非如此。训练后，十三个现代视觉编码器内部变异的前十六个主成分方向收敛到同一个十六维几何对象。我们将其称为跨架构基底，并使用PCA、中心核对齐（CKA）和Pang 2026校准对其进行研究。该基底在四个视觉领域（自然照片、医学CT、卫星图像、显微镜）中以中位Procrustes-CKA 0.679进行传输，在八个领域（增加素描、深度图、热红外、天文图像）中为0.604，每对都大于0.40。它在全局范围（7.4倍对比判别器与MAE分离，n=13,394）和局部范围（4.82-5.30，p<10^{-44}）均能通过Pang校准。它不是像素统计（0.263），不是Gabor特征（0.31），不是随机投影（0.041），并在训练的前10%中出现，而准确率仍在持续上升。我们提供了四个应用：一个标签无关的可迁移性过滤器，优于LogME（快3倍，+0.15 Kendall-tau）；一个四域检测器（99.6%准确率）；一个冻结低样本探测（16维在每类N=50标签下比768维DINOv2高3.78个百分点）；以及一个无教师蒸馏辅助，在33对上匹配经过训练的教师知识蒸馏（在10%标签比例下峰值增益7.56个百分点）。该基底无法跨模态，无法帮助跨范式蒸馏，也无法预测迁移质量（相对于迁移准确率的rho=0.08）。

</details>


### [43] [C3VD-DEFCOL: A Deformable Colonoscopy Dataset with Time-Resolved 3D Ground Truth and Realistic Appearance](https://arxiv.org/abs/2606.07891)
*Ethan Luk,Mayank V. Golhar,Anthony Song,Raúl Iranzo,Víctor M. Batlle,Lalithkumar Seenivasan,José M. M. Montiel,Nicholas J. Durr*

Main category: cs.CV

TL;DR: C3VD-DEFCOL是一个用于评估可变形结肠镜三维重建的数据集和框架，它通过合成变形和模拟真实纹理生成带几何与外观真值的视频数据，为可变形重建算法提供定量评估平台。

<details>
  <summary>Details</summary>

Motivation: 现有数据集缺乏同时具备真实体内外观和密集时间分辨3D真值的数据，特别是在非刚性变形条件下，限制了可变形结肠镜三维重建算法的开发与评估。

Method: 基于C3VD/C3VDv2的结肠网格和相机轨迹，生成可控的结肠表面变形（包括蠕动波和中心线运动），渲染每帧深度、法线、光流、相机位姿和3D网格；然后利用渲染的几何信息（主要是深度）条件化LTX-2.3模拟到真实转换模型，生成具有体内样黏膜外观的RGB视频，同时保留底层3D场景结构。

Result: 生成110个视频，覆盖11种独特的结肠网格几何、多种相机轨迹和参数化变形模式（含3种蠕动严重程度）；通过外观逼真度、几何一致性和时序一致性指标评估视频质量；在下游位姿估计任务上验证了位姿误差随变形程度增加而增长，提供了现有体内数据集无法实现的受控压力测试。

Conclusion: C3VD-DEFCOL作为可重复的定量评估平台，能够测试可变形三维重建算法，有助于缩小合成数据集与真实结肠镜数据之间的领域差距。

Original Abstract: 3D reconstruction could improve colonoscopy by estimating mucosal coverage and alerting clinicians to missed regions during screening. However, algorithm development is limited as no current datasets provide both a realistic in vivo appearance and dense, time-resolved 3D ground truth, especially under non-rigid deformation. We present C3VD-DEFCOL, a framework and dataset for evaluating deformable colonoscopy reconstruction with paired geometry and realistic texture. Starting from C3VD/C3VDv2 colon meshes and camera trajectories, we generate controlled deformations of the colon surface, including peristaltic waves and centerline motion, and render per-frame depth, surface normals, optical flow, camera poses, and time-stamped 3D meshes. We then use the rendered geometry, primarily depth, to condition an LTX-2.3-based sim-to-real translation model that produces RGB clips with in vivo-like mucosal color, texture, vasculature, and specular appearance while preserving the underlying 3D scene structure. The resulting dataset contains 110 videos from 11 unique colon mesh geometries, with varying camera trajectories, appearances, and parameterized deformation regimes, including three peristaltic severity levels that serve as controlled evaluation axes. We evaluate the generated videos using appearance realism, geometric consistency, and temporal consistency metrics, and use the paired ground truth to benchmark the downstream task of pose estimation in deformable 3D reconstruction. Our experiments show how pose estimation error increases with increasing deformation severity, providing a controlled stress test that is not possible with existing in vivo datasets. Overall, C3VD-DEFCOL is designed as a reproducible, quantitative evaluation platform for testing deformable 3D reconstruction algorithms, with the goal of reducing the domain gap between synthetic datasets and in vivo colonoscopy.

中文摘要翻译: 三维重建可以通过估计黏膜覆盖情况并在筛查期间提醒临床医生遗漏区域来改进结肠镜检查。然而，算法开发受到限制，因为目前没有数据集同时提供逼真的体内外观和密集的时间分辨三维真值，特别是在非刚性变形条件下。我们提出了C3VD-DEFCOL，这是一个用于评估可变形结肠镜重建的数据集和框架，配备成对的几何信息和逼真的纹理。从C3VD/C3VDv2结肠网格和相机轨迹出发，我们生成结肠表面的可控变形，包括蠕动波和中心线运动，并渲染每帧深度、表面法线、光流、相机位姿和带时间戳的三维网格。然后，我们利用渲染的几何信息（主要是深度）来条件化一个基于LTX-2.3的模拟到真实转换模型，该模型生成具有体内样黏膜颜色、纹理、血管和镜面反射外观的RGB视频片段，同时保留底层三维场景结构。生成的数据集包含来自11种独特结肠网格几何形状的110个视频，具有变化的相机轨迹、外观和参数化变形模式，包括三种蠕动严重级别，作为受控评估轴。我们使用外观逼真度、几何一致性和时序一致性指标评估生成的视频，并利用成对真值来基准测试可变形三维重建中的位姿估计下游任务。实验表明，位姿估计误差随变形严重程度增加而增大，提供了现有体内数据集无法实现的受控压力测试。总体而言，C3VD-DEFCOL被设计为一个可重复的定量评估平台，用于测试可变形三维重建算法，目标是缩小合成数据集与真实结肠镜检查之间的领域差距。

</details>


### [44] [TBD-VLA: Temporal Block Diffusion Vision Language Action Model](https://arxiv.org/abs/2606.07895)
*Sung-Wook Lee,Xuhui Kang,Yen-Ling Kuo*

Main category: cs.CV

TL;DR: TBD-VLA 提出了一种基于块扩散的离散令牌VLA框架，通过时间块内掩码离散扩散与块间自回归生成相结合，实现了时间动作生成，兼顾时间连贯性与推理速度提升。

<details>
  <summary>Details</summary>

Motivation: 现有离散VLA模型将动作生成视为离散动作空间上的自回归下一个令牌预测，推理延迟高且忽略了动作轨迹中的时间结构；而并行解码虽提升了效率，但缺乏对令牌依赖关系的显式建模。

Method: 将动作序列划分为时间块，在每个块内执行掩码离散扩散实现并行动作解码，同时在块间保持自回归生成，统一了时间自回归与并行动作解码。

Result: 在仿真和真实世界操作任务中显著优于先前的VLA方法，并且显式时间建模支持通过时间修补实现动作块的异步执行（如实时分块）。

Conclusion: TBD-VLA 提供了一条可扩展的路径，用于构建快速、具备时间感知能力的离散VLA模型。

Original Abstract: Discrete Vision-Language-Action (VLA) models typically formulate action generation as next-token prediction over discretized action spaces, conditioning each token autoregressively on prior context. While effective, this paradigm incurs high inference latency and largely ignores the temporal structure inherent in action trajectories. Recent efforts introduce parallel decoding to improve efficiency, enabling faster inference, but lack explicit mechanisms for modeling token dependencies. We introduce TBD-VLA, a discrete token-based VLA framework that incorporates block diffusion to enable temporal action generation. We partition action sequences into temporal blocks and perform masked discrete diffusion within each block, while maintaining autoregressive generation across blocks. This design unifies temporal autoregression and parallel action decoding, achieving both strong temporal coherence and improved inference speed. In addition, the explicit temporal modeling enables asynchronous execution of action chunks (e.g., Real-Time Chunking) via temporal in-painting. TBD-VLA significantly outperforms prior VLA approaches in both simulation and real-world manipulation tasks, offering a scalable path toward fast, temporally aware, discrete VLA models. Project webpage: https://tbd-vla.github.io/

中文摘要翻译: 离散视觉-语言-动作（VLA）模型通常将动作生成建模为离散动作空间上的下一个令牌预测，每个令牌以自回归方式依赖于先前的上下文。尽管这种范式行之有效，但它带来了较高的推理延迟，并且在很大程度上忽略了动作轨迹中固有的时间结构。最近的并行解码方法提高了效率，实现了更快的推理，但缺乏显式建模令牌依赖关系的机制。我们提出了TBD-VLA，一种基于离散令牌的VLA框架，引入了块扩散以实现时间动作生成。我们将动作序列划分为时间块，在每个块内执行掩码离散扩散，同时在块间保持自回归生成。这种设计统一了时间自回归与并行动作解码，同时实现了强时间连贯性和推理速度提升。此外，显式的时间建模使得能够通过时间修补实现动作块的异步执行（例如实时分块）。TBD-VLA在仿真和真实世界操作任务中均显著优于先前的VLA方法，为构建快速、具备时间感知能力的离散VLA模型提供了一条可扩展的路径。项目网页：https://tbd-vla.github.io/

</details>


### [45] [3D Oral Modelling with Improved Vertex Distribution Using Matching-Based Learning](https://arxiv.org/abs/2606.07907)
*Jihun Cho,Soo-Yeon Jeong,Eun-Jeong Bae,Sun-Young Ihm*

Main category: cs.CV

TL;DR: 本文针对先前基于深度学习的3D口内重建模型中预测顶点集中于高密度区域的问题，提出了改进的损失函数，引入匈牙利匹配过滤与排斥损失，使顶点分布更均匀，尽管准确率从77.49%下降至68.02%，但有效缓解了顶点聚类问题。

<details>
  <summary>Details</summary>

Motivation: 先前提出的基于深度学习的3D口内重建模型虽然达到了77.49%的准确率，但预测顶点倾向于集中在真实点云的高密度区域，而其他区域覆盖不足，导致重建模型分布不均匀。本文旨在解决这一顶点聚类问题。

Method: 提出改进的损失函数：引入带过滤机制的匈牙利匹配（Hungarian matching with filtering）和排斥损失（Repulsion Loss），以强制重建模型中顶点分布更加均匀。

Result: 改进后模型准确率为68.02%，数值上低于原模型的77.49%。但先前工作中存在的顶点聚类问题得到显著缓解，预测顶点在整个重建表面上分布更加均匀。

Conclusion: 通过在损失函数中引入匈牙利匹配过滤与排斥损失，虽然准确率有所下降，但有效改善了预测顶点的分布均匀性，克服了原模型顶点集中于高密度区域的局限性。

Original Abstract: In our previous work, a deep learning-based framework for 3D intraoral reconstruction was proposed. The model directly predicts explicit 3D point cloud coordinates from ten fixed-angle intraoral images, employing MobileNetV2 and Multi-head Attention for multi-view feature fusion, with a combined L1 Loss and Chamfer Distance as the loss function. Although the model achieved an accuracy of 77.49%, predicted vertices tended to concentrate in high-density regions of the ground truth, leaving other regions largely uncovered.
  In this paper, an improved loss function is proposed to address this limitation. Hungarian matching with filtering and Repulsion Loss are introduced to enforce more uniform vertex distribution across the reconstructed model. The proposed model achieves an accuracy of 68.02%, which is numerically lower than the previous model. However, the vertex clustering issue observed in the prior work is substantially alleviated, with predicted vertices distributed more evenly across the entire reconstructed surface.

中文摘要翻译: 在我们先前的工作中，提出了一种基于深度学习的3D口内重建框架。该模型从十张固定角度的口内图像直接预测显式3D点云坐标，采用MobileNetV2与多头注意力机制进行多视图特征融合，并组合使用L1损失和倒角距离作为损失函数。尽管该模型达到了77.49%的准确率，但预测顶点倾向于集中在真实点云的高密度区域，导致其他区域覆盖不足。本文提出了一种改进的损失函数来解决这一问题。引入带过滤的匈牙利匹配与排斥损失，以强制重建模型中顶点分布更加均匀。所提模型准确率为68.02%，数值上低于先前模型。然而，先前工作中观察到的顶点聚类问题得到了显著缓解，预测顶点在整个重建表面上分布更加均匀。

</details>


### [46] [Decoupling Semantics and Logic: A Training-Free Coarse-to-Fine Pipeline for Video Retrieval-Augmented Generation](https://arxiv.org/abs/2606.07924)
*Jiaxin Dai,Zehang Wei,Jiamin Yan,Xiang Xiang*

Main category: cs.CV

TL;DR: 本文提出了一种全无训练、两阶段级联的视频RAG流水线，通过解耦语义检索与认知推理、隔离噪声模态、并利用A.I.R.过滤智能体与Prompt Sculpting机制，实现跨语言长视频理解、严格人格遵从和零幻觉时间 grounding。

<details>
  <summary>Details</summary>

Motivation: 针对跨语言长视频理解、严格人格遵从和零幻觉时间 grounding 三个关键挑战，现有方法在资源受限场景下难以兼顾高精度检索与逻辑一致生成。

Method: 提出全无训练的两阶段级联Video RAG流水线：第一阶段使用高保真视觉摘要和全局文本描述进行密集检索（隔离OCR/ASR等噪声模态）；第二阶段由基于商用LLM的A.I.R.过滤智能体进行细粒度认知重排序，重新引入完整多模态上下文以对齐用户人格；最后通过Prompt Sculpting机制约束生成器输出严格格式的JSON结果并附带精确块级引用。

Result: 在MAGMaR RAG赛道上的评估表明，该方法在信息检索和个人条件生成方面表现出卓越的精度。

Conclusion: 所提出的资源感知型流水线在无需训练的条件下，有效解决了跨语言长视频理解中检索精度与逻辑一致性之间的权衡问题。

Original Abstract: This paper presents our system description for the 2nd Workshop on Multimodal Augmented Generation via MultimodAl Retrieval (MAGMaR). Addressing the critical challenges of cross-lingual long-video comprehension, strict persona adherence, and zero-hallucination temporal grounding, we propose a fully training-free, two-stage cascaded Video RAG pipeline. Our architecture strategically decouples semantic retrieval from cognitive logical reasoning through a modality-aware division of labor. In the first stage, a high-recall semantic pre-fetching module employs dense retrieval using only high-fidelity visual summaries and global text descriptions, explicitly isolating noisy modalities (e.g., OCR and ASR) to maintain a pristine vector space. In the second stage, an Adaptive, Iterative, and Reasoning-based (A.I.R.) filtering agent, powered by a commercial Large Language Model (LLM), performs fine-grained cognitive reranking. The agent re-incorporates full multimodal contexts to enforce strict logical alignment with user personas, effectively pruning semantically similar but logically irrelevant candidates. Finally, a Prompt Sculpting mechanism constrains the generator to synthesize the distilled subset into strictly formatted JSON responses with exact chunk-level citations. Evaluated on the RAG track, our resource-aware approach shows exceptional precision in both information retrieval and persona-conditioned generation.

中文摘要翻译: 本文介绍了我们在第二届多模态增强生成与多模态检索研讨会（MAGMaR）上的系统描述。针对跨语言长视频理解、严格人格遵从以及零幻觉时间定位等关键挑战，我们提出了一种完全无需训练的两阶段级联视频RAG流水线。我们的架构通过模态感知的职责分工，策略性地将语义检索与认知逻辑推理解耦。在第一阶段，一个高召回率的语义预取模块使用仅包含高保真视觉摘要和全局文本描述的密集检索，明确隔离噪声模态（如OCR和ASR），以维持纯净的向量空间。在第二阶段，一个由商用大语言模型驱动的自适应、迭代与推理（A.I.R.）过滤智能体执行细粒度认知重排序。该智能体重新引入完整的多模态上下文，以强制执行与用户人格的严格逻辑对齐，有效剪除语义相似但逻辑无关的候选内容。最后，Prompt Sculpting机制约束生成器将蒸馏后的子集合成为严格格式化的JSON响应，并附带精确的块级引用。在RAG赛道上的评估表明，我们的资源感知型方法在信息检索和人格条件生成方面均表现出卓越的精度。

</details>


### [47] [REACT 2026: The Fourth Multiple Appropriate Facial Reaction Generation Challenge: Personalised MAFRG and Appropriate EEG Reaction Prediction](https://arxiv.org/abs/2606.07935)
*Siyang Song,Micol Spitale,Zijian Wu,Xiangyu Kong,Cheng Luo,Cristina Palmero,German Barquero,Sergio Escalera,Michel Valstar,Mohamed Daoudi,Fabien Ringeval,Andrew Howes,Elisabeth Andre,Hatice Gunes*

Main category: cs.CV

TL;DR: REACT 2026挑战赛提出多模态适切面部反应生成（MAFRG）问题，新增个性化Big-Five人格标签和EEG记录，设置离线/在线、通用/个性化四个子挑战，并发布基线模型与数据集。

<details>
  <summary>Details</summary>

Motivation: 现有面部反应生成模型多针对单次交互，未充分考虑不同听众的个性化反应差异，且缺乏多模态（行为、情感、神经生理）信号的融合探索。

Method: 基于MARS数据集扩展个体层面的Big-Five人格标签与EEG记录，定义四个子挑战（离线通用、离线个性化、在线通用、在线个性化），提供公开基线模型供参赛者改进与基准测试。

Result: 在REACT系列挑战赛持续推动下，已发展出一系列生成式深度学习模型；2026挑战赛进一步引入个性化标签与神经生理信号，拓宽了二元交互建模的边界。

Conclusion: REACT 2026挑战赛通过引入个性与神经生理信号，建立了新的一对多个性化面部反应生成范式，为二元交互的多模态建模提供了标准化基准平台。

Original Abstract: In dyadic interactions, various human facial reactions could be appropriate for responding to each human speaker behaviour. Following the successful organisation of the REACT 2023, 2024 and 2025 challenge series, a body of generative deep learning (DL) models have been developed for the problem of multiple appropriate facial reaction generation (MAFRG). This year, we propose the REACT 2026 challenge encouraging the development and benchmarking of Machine Learning (ML) models that can generate multiple personalised, appropriate, diverse, realistic and synchronised human-style facial reactions expressed by a specific human listener for responding to each given speaker behaviour. As a key of the challenge, we continuously provide challenge participants with MARS dataset introduced by REACT 2025 but additionally provide individual-level Big-Five personality labels and EEG recordings. This introduces a new one-to-many personalised facial reaction generation setting combining human expressive behavioural, affective and neurophysiological signals, which remains largely unexplored in current dyadic interaction modelling. This paper also presents the challenge guidelines and new baselines on the four proposed sub-challenges: Offline generic and personalised MAFRG as well as Online generic and personalised MAFRG, respectively, which are publicly available at https://github.com/reactmultimodalchallenge/baseline_react2026.

中文摘要翻译: 在二元交互中，人类听众可能表现出多种合适的面部反应来回应说话者的每种行为。继REACT 2023、2024和2025挑战系列成功举办之后，一系列生成式深度学习模型已被开发用于解决多合适面部反应生成问题。今年，我们提出REACT 2026挑战赛，鼓励开发和基准测试机器学习模型，这些模型能够生成由特定人类听众表达的多种个性化、合适、多样、真实且同步的类人面部反应，以回应每个给定的说话者行为。作为挑战的关键，我们持续为参与者提供REACT 2025引入的MARS数据集，并额外提供个体层面的大五人格标签和EEG记录。这引入了一种新的“一对多”个性化面部反应生成设定，融合了人类表达性行为、情感和神经生理信号，这在当前的二元交互建模中仍 largely 未被探索。本文还介绍了挑战指南以及针对四个子挑战的新基线：分别为离线通用与个性化MAFRG以及在线通用与个性化MAFRG，这些基线代码已公开在https://github.com/reactmultimodalchallenge/baseline_react2026。

</details>


### [48] [ChronoPhyBench: Do MLLMs Truly Understand the World or Merely Exploit Language Priors?](https://arxiv.org/abs/2606.07962)
*Bin Zhu,Yanhao Jia,Kexin Zhao,Jie Wang,Munan Ning,Hao Li,Yuwei Niu,Tanqing Sun,Huangchong Yan,Mingjun Pan,Xinyi Wu,Qishen Yin,Yunyang Ge,Shuai Zhao,Li Yuan*

Main category: cs.CV

TL;DR: 提出ChronoPhyBench基准，通过时序物理动力学推理任务评估多模态大模型是否真正具备跨模态推理能力，而非仅依赖语言先验。实验表明现有开源模型的多模态物理推理能力仍处于初级阶段。

<details>
  <summary>Details</summary>

Motivation: 当前多模态大模型在开放世界推理中表现优异，但存在关键歧义：模型是真正融合跨模态信息构建基于物理的推理链，还是仅利用强语言先验掩盖单模态依赖、幻觉出多模态能力。为严格消除语言模态偏差和捷径，亟需设计更严谨的评估基准。

Method: 提出ChronoPhyBench基准，将下一状态预测与VQA范式统一，通过历史视频上下文和文本描述条件化，强制模型通过单图像选择和更复杂的多帧时间排序来推断后续物理状态。同时构建超过10,000个长视频、500万token的大规模多模态推理数据集。

Result: 实验评估显示，与以往基准结论形成鲜明对比：当前开源模型执行基于物理的多模态推理能力仍处于初级阶段。

Conclusion: 本工作旨在系统压力测试多模态模型的推理能力、量化幻觉率、推动物理AI发展，为社区提供迈向通用人工智能的鲁棒且透明的评估框架。

Original Abstract: Recent advancements in Multimodal Large Language Models (MLLMs) have demonstrated remarkable proficiency in open-world reasoning and understanding. However, a critical ambiguity persists: it remains unclear whether these models genuinely synthesize cross-modal information to construct physically grounded reasoning chains, or if they merely exploit strong language priors to mask single-modality reliance, thereby hallucinating advanced multimodal capabilities. Motivated by this, and to rigorously mitigate language modality bias and shortcuts, we propose a novel multimodal Chrono}logical Physical Dynamics Reasoning Benchmark ChronoPhyBench, which unifies next state prediction with Visual Question Answering (VQA) paradigms by conditioning on historical video context and textual captions to enforce models to deduce subsequent physical states through both single image selection and the inherently more complex task of multiple frame chronological sorting. Concurrently, we construct a large-scale multimodal reasoning dataset curated using the ChronoPhyBench criteria, comprising over 10,000 long-form videos paired with meticulously annotated captions, totaling 5M tokens. Our experimental evaluations reveal a stark contrast to conclusions drawn by previous benchmarks. The capacity of current open-source models to perform physically grounded multimodal reasoning remains in its infancy. Ultimately, this work seeks to systematically stress-test the reasoning capabilities of multimodal models, quantify hallucination rates, and advance the development of Physical AI, thereby providing the community with a robust and transparent evaluation framework toward Artificial General Intelligence (AGI).

中文摘要翻译: 多模态大语言模型的最新进展已在开放世界推理与理解方面展现出卓越能力。然而，一个关键歧义依然存在：这些模型究竟是真正融合跨模态信息以构建基于物理的推理链，还是仅仅利用强语言先验来掩盖单模态依赖，从而幻觉出先进的多模态能力？受此启发，为严格缓解语言模态偏差与捷径问题，我们提出了一种新颖的多模态时序物理动力学推理基准——ChronoPhyBench。该基准通过将下一状态预测与视觉问答范式相统一，基于历史视频上下文和文本描述进行条件化，强制模型通过单图像选择以及更具挑战性的多帧时间排序任务来推断后续物理状态。同时，我们依据ChronoPhyBench标准构建了一个大规模多模态推理数据集，包含超过10,000个长视频及其精细标注的描述，总计500万token。实验评估结果显示，与以往基准的结论形成鲜明对比：当前开源模型执行基于物理的多模态推理能力仍处于初级阶段。最终，本工作旨在系统性地压力测试多模态模型的推理能力、量化幻觉率，并推动物理AI的发展，从而为社区提供一个鲁棒且透明的评估框架，助力迈向通用人工智能。

</details>


### [49] [DisCo: World Models with Discrete Camera Motion Control](https://arxiv.org/abs/2606.07967)
*Hongrui Huang,Junke Wang,Quanhao Li,Yu-Gang Jiang,Zuxuan Wu*

Main category: cs.CV

TL;DR: 本文提出DisCo，一种基于离散动作原语的可控视频世界模型，通过解耦动作表示来提高动作跟随的可靠性，并构建了DisCoBench基准来评估短程、长程和高动态探索场景下的模型性能。

<details>
  <summary>Details</summary>

Motivation: 现有可控视频生成模型依赖连续相机轨迹作为动作条件，导致动作表示纠缠，不同运动模式下的特征相似度高，从而降低了动作可控性，尤其面对复杂运动序列时不可靠。

Method: 提出DisCo模型，将生成过程条件化在一组紧凑的离散动作原语（discrete action primitives）上，以提升不同动作之间的可分离性，从而改善动作跟随的可靠性。

Result: 实验表明，DisCo在短程、长程和高动态探索场景下均实现了显著更可靠的动作跟随，同时保持了视觉质量。

Conclusion: 离散动作原语能够有效解耦动作表示、提升动作可控性，是构建更可靠可控视频世界模型的有效路径。

Original Abstract: Controllable video world models target interactive world exploration, where models must faithfully execute explicit action commands while preserving visual quality and temporal coherence. However, most existing approaches rely on continuous camera trajectories as action conditions, which often lead to unreliable action following, especially under complex motion sequences. In this work, we identify action representation entanglement as a key bottleneck in controllable video generation, and show that continuous camera representations lead to high feature similarity across distinct motion patterns, degrading action controllability. Based on this insight, we propose DisCo, a controllable video world model that conditions generation on a compact set of discrete action primitives to improve action separability. We further introduce DisCoBench, a comprehensive benchmark for evaluating the ability of models in short-term, long-horizon, and highly dynamic exploration scenarios. Extensive experiments demonstrate that DisCo achieves significantly more reliable action following while preserving visual quality.

中文摘要翻译: 可控视频世界模型旨在实现交互式世界探索，要求模型在保持视觉质量和时间一致性的同时，忠实地执行显式的动作指令。然而，现有大多数方法依赖连续相机轨迹作为动作条件，这往往导致动作跟随不可靠，尤其在面对复杂运动序列时。在本工作中，我们指出动作表示纠缠是可控制视频生成的一个关键瓶颈，并证明连续相机表示会导致不同运动模式下的特征高度相似，从而削弱动作可控性。基于这一发现，我们提出DisCo——一种可控视频世界模型，它将生成过程条件化在一组紧凑的离散动作原语上，以提升动作的可分离性。我们还引入了DisCoBench，一个综合基准，用于评估模型在短程、长程和高动态探索场景下的能力。大量实验表明，DisCo在实现显著更可靠的动作跟随的同时，保持了视觉质量。

</details>


### [50] [FMRFusion: Frequency-Aware Multi-View Representation Learning for Heterogeneous Image Fusion](https://arxiv.org/abs/2606.07985)
*Tao Zhoua,Yunlong Liu,Qinghui Chen,Zekai Zhang,Minlong Sun,Changlin Biana,Dagang Li,Wenmin Wang,Jinglin Zhang*

Main category: cs.CV

TL;DR: 本文提出FMRFusion，一种频率感知的多视图表示学习网络，用于红外与可见光图像融合。该方法通过多尺度结构感知模块、双线性频率分解机制、跨视图互补交互以及流匹配逐步优化，在多个基准数据集上取得了优越的融合性能，尤其在夜间场景下表现突出。

<details>
  <summary>Details</summary>

Motivation: 现有图像融合方法通常采用单模块堆叠的方式提取两种模态的特征，未能充分学习各自模态的独特特性，导致融合效果受限，且在真实异构数据场景下的鲁棒性不足。

Method: 提出FMRFusion网络，包含：(1) 多尺度结构感知模块，用于捕获判别性结构，提取细粒度局部结构和关键上下文信息；(2) 双线性频率分解机制，将特征分离为高频和低频分量，实现跨频域的局部细节与全局表示联合建模；(3) 跨视图互补交互模块，显式建模和融合反射光信息与辐射强度响应之间的互补特性；(4) 流匹配方法，通过学习从粗数据到高质量表示的变换，逐步优化融合特征。

Result: 在多个基准数据集上的大量实验表明，FMRFusion在多种融合任务中均取得了优越且一致的性能，尤其在夜间场景下表现优异。

Conclusion: FMRFusion通过频率感知的多视图表示学习，有效解决了异构图像融合中特征学习不完整的问题，在多种融合场景（尤其夜间场景）中实现了卓越的融合效果。

Original Abstract: Infrared and visible image fusion aims to generate a composite image that retains significant target information and preserves detailed textures, integrating two heterogeneous modalities. Previous image fusion methods typically adopt a single-module stacking approach to extract features from the two modalities. However, these approaches may result in incomplete learning of their distinct characteristics, thereby limiting the fusion effectiveness and constrain ing robustness in real-world heterogeneous data scenarios. To address these challenges, we propose FMRFusion, a frequency-aware multi-view representation learning network for Heterogeneous Image Fusion. A Multi-Scale Struc tural Perception Module is introduced to effectively capture discriminative structures, extracting fine-grained local structures and essential contextual information. A bilinear frequency decomposition mechanism is employed to sepa rate features into high-frequency and low-frequency components, enabling joint modeling of local details and global representations across different frequency domains. Moreover, a Cross-View Complementary Interaction is incorpo rated to explicitly model and fuse the complementary characteristics between reflected light information and radiative intensity responses, facilitating effective cross-view interaction. We further improve the Performance of the fused results by flow matching, which progressively refines the fused features by learning the transformation from coarse data to high-quality representations. Extensive experiments conducted on multiple benchmark datasets demonstrate that FMRFusion achieves superior and consistent performance across a range of fusion tasks, especially in nighttime scenarios

中文摘要翻译: 红外与可见光图像融合旨在生成一幅保留显著目标信息并保持细节纹理的复合图像，整合两种异质模态。以往的图像融合方法通常采用单模块堆叠的方式从两种模态中提取特征。然而，这些方法可能导致对各自模态独特特征的学习不完整，从而限制融合效果，并在真实异构数据场景中制约其鲁棒性。为解决上述挑战，我们提出FMRFusion——一种面向异构图像融合的频率感知多视图表示学习网络。引入多尺度结构感知模块，有效捕获判别性结构，提取细粒度局部结构和关键上下文信息。采用双线性频率分解机制，将特征分离为高频和低频分量，实现跨不同频域的局部细节与全局表示的联合建模。此外，引入跨视图互补交互模块，显式建模并融合反射光信息与辐射强度响应之间的互补特性，促进有效的跨视图交互。我们进一步通过流匹配来提升融合结果的性能，该方法通过学习从粗数据到高质量表示的变换，逐步优化融合特征。在多个基准数据集上进行的大量实验表明，FMRFusion在多种融合任务中，尤其在夜间场景下，均取得了优越且一致的性能。

</details>


### [51] [Learning a Semantic Calibration Network for Open-Vocabulary Semantic Segmentation](https://arxiv.org/abs/2606.08001)
*Yang Sun,Tao Wang,Anastasia Ioannou,Ge Xu*

Main category: cs.CV

TL;DR: 本文提出语义校准网络(SCN)，通过类消歧模块和logits融合模块显式建模类别间语义相关性，在不牺牲CLIP预训练模型泛化能力的前提下提升开放词汇语义分割的判别性能，在主流基准上取得SOTA结果。

<details>
  <summary>Details</summary>

Motivation: 现有开放词汇语义分割方法要么聚焦于特征聚合，要么对预训练模型进行简单微调，未能有效建模类别间的语义相关性，导致模型判别能力不足，且容易损害CLIP预训练模型的泛化能力。

Method: SCN包含两个核心模块：(1) 类消歧模块(CD)：通过残差架构捕获隐式类间依赖关系，解决语义歧义问题；(2) Logits融合模块(LF)：动态整合多方面语义证据，在保持CLIP泛化能力的同时实现鲁棒的语义一致性。此外，采用交叉注意力机制将文本嵌入转换为视觉感知的伪文本嵌入，补充原始掩码-文本相似度分数。

Result: 在主流基准数据集上的综合实验表明，所提方法相比现有最优算法取得了显著的性能提升。

Conclusion: 通过显式建模类别间语义相关性并动态融合语义证据，SCN有效增强了开放词汇语义分割的判别能力，同时保持了CLIP模型的泛化特性，为开放词汇分割任务提供了新的解决思路。

Original Abstract: Semantic image segmentation assigns a predefined category label to each pixel, has achieved significant progress lately. Open-Vocabulary Segmentation (OVS) extends the segmentation task from a fixed set to an open set, enabling the identification and segmentation of novel concepts based on arbitrary text inputs, such as category names or descriptions. In this paper, we propose a novel Semantic Calibration Network (SCN) for open-vocabulary semantic segmentation. Different from prior approaches that focus on feature aggregation or simple fine-tuning of pre-trained models, SCN refines the mask classification process by explicitly modeling the semantic correlations between classes, aiming to enhance the model's discriminative power while effectively preserving the generalization abilities of the pre-trained CLIP model. Specifically, SCN comprises two core components: Class Disambiguation (CD) and Logits Fusion (LF). First, a cross-attention mechanism is utilized to transform the text embeddings into visually aware pseudo-text embeddings, in order to derive an enhanced similarity score that complements the original mask-text similarity score. Subsequently, the Class Disambiguation module captures implicit inter-class dependencies through a residual architecture to effectively resolve semantic ambiguities. Finally, the Logits Fusion module dynamically integrates multifaceted semantic evidence to ensure that the model achieves a robust semantic consensus while maintaining CLIP's inherent generalization capability. Comprehensive experimental results on mainstream benchmarks demonstrate that the proposed method achieves significant performance improvements compared to state-of-the-art algorithms.

中文摘要翻译: 语义图像分割为每个像素分配预定义的类别标签，近年来取得了显著进展。开放词汇分割(OVS)将分割任务从固定类别集扩展到开放集，使得能够基于任意文本输入（如类别名称或描述）识别和分割新概念。本文提出一种新颖的语义校准网络(SCN)用于开放词汇语义分割。与以往关注特征聚合或对预训练模型进行简单微调的方法不同，SCN通过显式建模类别间的语义相关性来细化掩码分类过程，旨在增强模型的判别能力，同时有效保持预训练CLIP模型的泛化能力。具体而言，SCN包含两个核心组件：类消歧模块(CD)和Logits融合模块(LF)。首先，利用交叉注意力机制将文本嵌入转换为视觉感知的伪文本嵌入，以推导出增强的相似度分数来补充原始掩码-文本相似度分数。随后，类消歧模块通过残差架构捕获隐式类间依赖关系，有效解决语义歧义问题。最后，Logits融合模块动态整合多方面的语义证据，确保模型在保持CLIP固有泛化能力的同时达成鲁棒的语义共识。在主流基准上的综合实验结果表明，所提方法相比现有最优算法取得了显著的性能提升。

</details>


### [52] [Aqua Boundary-Saliency Attention Module for Lightweight Underwater Salient Instance Segmentation Detection Transformer](https://arxiv.org/abs/2606.08002)
*M. Fazri Nizar,Julian Supardi,Muhammad Naufal Rachmatullah*

Main category: cs.CV

TL;DR: 本文提出了一种轻量级水下显著实例分割检测变换器（LUSIS-DETR），其核心是水边界-显著性注意力模块（AquaBSAM），通过嵌入水下边界、对比度、衰减、色度、暗通道和中心先验等多重线索，在不依赖大型基础模型或额外模态估计的情况下，实现了高效且领先的水下实例分割性能，并在NVIDIA T4 GPU上达到4.31-6.34毫秒的实时推理延迟。

<details>
  <summary>Details</summary>

Motivation: 现有的基于提示和辅助模态的方法虽然提升了掩码质量，但过度依赖大型基础模型、提示生成或额外模态估计，导致部署复杂且效率低下。本文旨在设计一种轻量级、无需额外模态或提示的紧凑型框架，实现高效的水下实例分割。

Method: 提出LUSIS-DETR框架，其核心是AquaBSAM模块。该模块将水下边界、对比度、衰减、色度、暗通道和中心先验线索通过有界残差调制嵌入到DINOv2初始化的多尺度特征中。此外，辅助掩码监督和小目标复制粘贴仅在训练阶段使用，不增加推理负担。

Result: 在UIIS、UIIS10K、USIS10K和USIS16K四个水下实例分割数据集上，在类别感知和显著实例协议下均取得了与最先进方法相比具有竞争力的领先性能。在NVIDIA T4 GPU上采用TensorRT半精度（FP16）推理，延迟仅为4.31-6.34毫秒，支持实时推理。

Conclusion: LUSIS-DETR通过精心设计的水下先验注意力模块，在保持轻量化和高效推理的同时，达到了领先的水下实例分割性能，为水下机器人感知等实时应用提供了一种可行的解决方案。

Original Abstract: Underwater instance segmentation integrates pixel-level mask prediction and instance-level discrimination for marine resource exploration, ecological monitoring, and underwater robotic perception. Recent prompt-based and auxiliary-modality methods improve mask quality, but their reliance on large foundation models, prompt generation, or extra modality estimation complicates efficient deployment. This work introduces Lightweight Underwater Salient Instance Segmentation Detection Transformer (LUSIS-DETR), a compact detection-transformer framework built around the Aqua Boundary-Saliency Attention Module (AquaBSAM). AquaBSAM embeds underwater boundary, contrast, attenuation, chroma, dark-channel, and center-prior cues into DINOv2-initialized multi-scale features through bounded residual modulation, while auxiliary mask supervision and small-object copy-paste are training-only. Extensive evaluation on four recent underwater instance segmentation datasets, UIIS, UIIS10K, USIS10K, and USIS16K, shows competitively leading performance against previous state-of-the-art works across category-aware and salient-instance protocols. TensorRT half-precision (FP16) benchmarking on an NVIDIA T4 graphics processing unit (GPU) achieves 4.31-6.34 milliseconds (ms) latency, supporting real-time inference under an accessible reproduction setting.

中文摘要翻译: 水下实例分割将像素级掩码预测与实例级判别相结合，用于海洋资源勘探、生态监测和水下机器人感知。最近的基于提示和辅助模态的方法提升了掩码质量，但它们对大型基础模型、提示生成或额外模态估计的依赖使得高效部署变得复杂。本文介绍了轻量级水下显著实例分割检测变换器（LUSIS-DETR），这是一种围绕水边界-显著性注意力模块（AquaBSAM）构建的紧凑型检测变换器框架。AquaBSAM通过有界残差调制将水下边界、对比度、衰减、色度、暗通道和中心先验线索嵌入到DINOv2初始化的多尺度特征中，而辅助掩码监督和小目标复制粘贴仅在训练阶段使用。在四个近期水下实例分割数据集UIIS、UIIS10K、USIS10K和USIS16K上的广泛评估表明，该方法在类别感知和显著实例协议下均取得了与先前最先进方法相比具有竞争力的领先性能。在NVIDIA T4图形处理器（GPU）上进行的TensorRT半精度（FP16）基准测试实现了4.31-6.34毫秒（ms）的延迟，在可复现的部署条件下支持实时推理。

</details>


### [53] [GVC-Seg: Training-Free 3D Instance Segmentation via Geometric Visual Correspondence](https://arxiv.org/abs/2606.08014)
*Liang Xu,Fangjing Wang,Jinyu Yang,Feng Zheng*

Main category: cs.CV

TL;DR: 提出一种无需训练的3D实例分割方法GVC-Seg，通过几何视觉对应（Geometric Visual Correspondence）来消除不同分割模型间的置信度偏差，实现无偏集成学习，在多个基准上达到SOTA性能。

<details>
  <summary>Details</summary>

Motivation: 现有基于多模型集成的3D实例分割方法受限于不同分割模型间的置信度差异，导致结果偏向高置信度模型，这种偏差是模型依赖的，受数据预处理和训练策略影响。作者旨在消除这种置信度偏差，实现无偏的集成学习。

Method: 提出GVC-Seg方法，核心是利用3D几何线索与2D视觉线索之间的几何视觉对应（Geometric Visual Correspondence）来缓解置信度偏差。具体包括：3D提议生成模块、掩码感知的CLIP特征提取模块，分别用于实例掩码生成和实例语义推理，从而提升提议质量评估，实现不同模型间的无偏集成学习。

Result: 在多个具有挑战性的基准数据集上达到最先进（state-of-the-art）性能，同时在开放词汇语义分割场景中也展现出强大潜力。

Conclusion: GVC-Seg通过无需训练的几何视觉对应机制有效解决了多模型集成中的置信度偏差问题，为3D实例分割提供了一种高性能且具有开放词汇泛化能力的新范式。

Original Abstract: Accurate 3D instance segmentation in point cloud data is critical for machine vision applications. Recent advancements leverage multiple pre-trained foundation models to generate 3D proposals, followed by the application of proposal aggregation methods, which significantly enhance performance. However, they often produce sub-optimal results due to inherent variations in confidence levels across different segmentation models, resulting in a bias toward the model with higher confidence. This bias is inherently model-dependent and is influenced by factors such as data preprocessing techniques and training strategies. To address this bias, we propose a novel, training-free 3D instance segmentation approach via Geometric Visual Correspondence (GVC-Seg), which exploits the correspondence between 3D geometric cues and 2D visual cues to mitigate the confidence bias. Additionally, a 3D proposal generation module and a mask-aware CLIP feature extraction module are introduced during the instance mask generation and instance semantic reasoning, respectively. In this way, GVC-Seg enhances proposal quality assessment, ensuring unbiased ensemble learning across different models. Extensive experiments demonstrate that our method achieves state-of-the-art performance on several challenging benchmarks, while also exhibiting strong potential in open-vocabulary semantic segmentation settings.

中文摘要翻译: 点云数据中的精确3D实例分割对于机器视觉应用至关重要。最近的进展利用多个预训练的基础模型生成3D提议，随后应用提议聚合方法，显著提升了性能。然而，由于不同分割模型间置信度水平的内在差异，这些方法通常会得到次优结果，导致偏向于置信度更高的模型。这种偏差本质上是模型依赖的，并受到数据预处理技术和训练策略等因素的影响。为了解决这一偏差，我们提出了一种新颖的、无需训练的3D实例分割方法——基于几何视觉对应的GVC-Seg，该方法利用3D几何线索与2D视觉线索之间的对应关系来缓解置信度偏差。此外，在实例掩码生成和实例语义推理阶段分别引入了3D提议生成模块和掩码感知的CLIP特征提取模块。通过这种方式，GVC-Seg增强了提议质量评估，确保了不同模型间的无偏集成学习。大量实验表明，我们的方法在多个具有挑战性的基准数据集上实现了最先进的性能，同时在开放词汇语义分割设置中也展现出强大的潜力。

</details>


### [54] [IEA: Amateur-Friendly Conversational Image Editing Agent via Three Stages of Multitask Alignment](https://arxiv.org/abs/2606.08016)
*Zichen Zhu,Yuheng Sun,Mingxuan Zhu,Wenjie Ma,Situo Zhang,Zhexiang Wang,Ziyue Yang,Danyang Zhang,Kunyao Lan,Zihan Zhao,Dingye Liu,Siqi Xiang,Lu Chen,Kai Yu*

Main category: cs.CV

TL;DR: 本文提出IEA，一个对话式图像编辑智能体，通过显式、可解释的工具操作空间进行图像编辑。IEA经过三阶段多任务训练（SFT、GRPO、大规模合成微调），能逐步操作16种编辑工具，生成透明可调试的编辑轨迹，在编辑任务上像素距离更低，在摘要任务上ROUGE-L更高，用户研究中指令跟随和感知质量均优于基线方法。

<details>
  <summary>Details</summary>

Motivation: 现有图像编辑软件依赖固定滤镜或专家调参，业余用户意图与结果之间存在鸿沟；生成式模型的编辑结果可能存在伪影、不合理细节或风格偏移，且缺乏可解释性，用户无法理解编辑为何被做出。

Method: 提出IEA（对话式图像编辑智能体），采用三阶段多任务训练流水线：(1) 在蒸馏专家编辑数据上进行监督微调（SFT）；(2) 使用GRPO（组相对策略优化）结合图像相似度奖励、工具有用性奖励和意图摘要奖励进行强化学习；(3) 大规模合成数据微调，联合掌握图像编辑、细化和用户意图摘要。IEA能逐步操作16种参数化编辑工具，生成透明的编辑轨迹。

Result: 定量实验：编辑任务上像素距离更低，摘要任务上ROUGE-L高于强基线。用户研究：在工具调用方法中指令跟随排名最佳，在感知质量上超越生成式方法。

Conclusion: 可解释的、以工具为中心的视觉语言模型（VLM）是实现人类指令引导的图像润色的可靠路径。

Original Abstract: Current image editing software often hinges on fixed filters or expert tuning, leaving a gap between amateur users' intent and outcomes. Creations by generative models may contain artifacts, implausible details, or stylistic drift away from photorealism and offer little insight into why an edit was made. We propose IEA, a conversational Image Editing Agent that learns to operate parameterized tools in an explicit, interpretable action space. IEA is trained via a three-stage multitask pipeline: (1) SFT on distilled expert edits, (2) GRPO with rewards for likeness improvement, tool usefulness, and intent summarization, and (3) large-scale synthetic fine-tuning to jointly master image editing, refinement, and user intent summarization. By manipulating 16 editing tools step by step, IEA produces transparent edit traces that can be inspected and debugged. In quantitative experiments, it attains a lower pixel distance on the edit task and a higher ROUGE-L on the summary task than strong baselines. In user studies, it ranks best among tool-calling methods for instruction following while surpassing generative methods in overall perceptual quality. Our results validate interpretable, tool-centric VLMs as a reliable path to human instruction-guided image retouching.

中文摘要翻译: 当前的图像编辑软件通常依赖于固定滤镜或专家调参，导致业余用户的意图与最终结果之间存在差距。生成式模型创建的图像可能包含伪影、不合理细节或偏离逼真感的风格漂移，且无法提供为何做出编辑的洞见。我们提出了IEA，一个对话式图像编辑智能体，它学习在显式、可解释的动作空间中操作参数化工具。IEA通过三阶段多任务流水线进行训练：(1) 在蒸馏后的专家编辑数据上进行监督微调（SFT）；(2) 使用组相对策略优化（GRPO），结合图像相似度提升奖励、工具有用性奖励和意图摘要奖励进行训练；(3) 大规模合成数据微调，以联合掌握图像编辑、细化和用户意图摘要。通过逐步操作16种编辑工具，IEA生成透明的编辑轨迹，可被检查和调试。在定量实验中，IEA在编辑任务上取得了更低的像素距离，在摘要任务上取得了比强基线更高的ROUGE-L分数。在用户研究中，它在工具调用方法中指令跟随排名最佳，同时在整体感知质量上超越了生成式方法。我们的结果验证了可解释的、以工具为中心的视觉语言模型（VLM）作为人类指令引导的图像润色的可靠路径。

</details>


### [55] [Vision-Language Asymmetry in Bistable Image Captioning](https://arxiv.org/abs/2606.08031)
*Arohan Agate*

Main category: cs.CV

TL;DR: 该论文通过行为实验和稀疏自编码器分析，研究了视觉-语言模型（LLaVA-1.6-7B）在处理韦特根斯坦鸭-兔等双稳图像时，模型在哪一层面对图像做出特定解读的承诺。发现视觉塔层可同时激活两种解读，而最终的语言输出偏向由视觉塔下游的瓶颈决定，无法通过因果干预翻转力平衡刺激的解读。

<details>
  <summary>Details</summary>

Motivation: 探究视觉-语言模型在处理双稳图像（如鸭-兔图）时，模型内部哪个层级决定了最终输出的单一解读，以及视觉表征与语言输出之间的承诺差距如何体现“看见”与“看作”的认知区别。

Method: 1) 构建3,320次生成的行为基线，覆盖83个双稳刺激，在自然与强制选择提示下定义三种机制（默认主导、力主导、力平衡）；2) 训练TopK稀疏自编码器（SAE）于LLaVA-1.6-7B实际消费的CLIP层（验证EV 0.93）；3) 在69个双稳刺激上分析每方面特征池的激活情况；4) 在CLIP第22层进行因果操控以翻转标题。

Result: 72%（50/69）的双稳刺激在视觉塔层显示两方面特征池的同步激活，包括12/12的默认主导鸭/兔刺激和7/8的力平衡年轻/老人刺激。因果操控可翻转默认主导刺激的标题（33%翻转率，流畅性保护下），但无法在任何测试系数下翻转力平衡的年轻/老人刺激的标题。

Conclusion: 解读的主导瓶颈位于视觉塔下游；视觉侧的表征叠加与语言侧的承诺之间存在差距，这一差距可作为“看见”与“看作”之分的经验操作性抓手。同时指出，TopK SAE输出的基于秩的统计需要进行等秩校正以避免行序偏差。

Original Abstract: Wittgenstein's duck-rabbit poses a question for vision-language models: when a model captions an ambiguous image, where in the model is the commitment to one aspect made? We address this with a 3,320-generation behavioral baseline over 83 bistable stimuli that surfaces three regimes (default-dominant, force-dominant, force-balanced) under neutral vs forced-choice prompting, then probe the underlying representations using a TopK sparse autoencoder we train on the CLIP layer that LLaVA-1.6-7B actually consumes (validation EV 0.93). Across 69 bistable stimuli with both per-aspect feature pools available, 72% (50/69) show simultaneous activation of both pools at the vision tower, including 12/12 default-dominant duck/rabbit and 7/8 force-balanced young/old. Causal steering at CLIP layer 22 flips captions on default-dominant stimuli (33% rabbit-flip rate under a fluency guard) but cannot flip captions on force-balanced young/old at any tested coefficient, despite their vision-side superposition. The dominance bottleneck lives downstream of the vision tower; the gap between vision-side representation and language-side commitment is an empirical handle on the seeing/seeing-as distinction. We also flag a methodological note: rank-based statistics on TopK SAE outputs require tie-corrected ranking to avoid silent row-order bias.

中文摘要翻译: 维特根斯坦的鸭-兔图向视觉-语言模型提出了一个问题：当一个模型对一幅模棱两可的图像生成标题时，模型内部是在哪里做出朝向某个方面的承诺的？我们通过一个包含3,320次生成的行为基线来应对这一问题，该基线覆盖83个双稳刺激，在自然提示与强制选择提示下浮现出三种机制（默认主导、力主导、力平衡），然后使用我们在LLaVA-1.6-7B实际消费的CLIP层上训练的TopK稀疏自编码器（验证解释方差0.93）来探测底层表征。在69个具有可用每方面特征池的双稳刺激中，72%（50/69）在视觉塔层显示两个特征池的同步激活，包括12/12的默认主导鸭/兔刺激和7/8的力平衡年轻人/老人刺激。在CLIP第22层进行因果操控，可以翻转默认主导刺激的标题（在流畅性保护下33%的兔子翻转率），但无法在任何测试系数下翻转力平衡年轻人/老人刺激的标题，尽管它们在视觉侧存在叠加。主导瓶颈位于视觉塔下游；视觉侧表征与语言侧承诺之间的差距是"看见"与"看作"之区分的经验性操作抓手。我们还指出一个方法论注意点：基于秩的TopK SAE输出统计数据需要进行等秩校正以避免隐式的行序偏差。

</details>


### [56] [Balancing Real and Synthetic Data for CNN-based Masonry Crack Detection](https://arxiv.org/abs/2606.08033)
*Mattia Forlesi,Alfonso Esposito,Ivan Zyrianoff,Alessandro Marzani,Marco Di Felice*

Main category: cs.CV

TL;DR: 本文研究了利用合成裂缝数据增强 masonry 墙体裂缝检测模型训练的方法。实验表明，合成数据结合少量（20%）真实数据即可达到甚至超过纯真实数据训练的模型性能（F1=76%，mIoU=80%），有效降低了数据采集成本。

<details>
  <summary>Details</summary>

Motivation: 深度学习模型在裂缝检测中性能依赖大规模多样化数据集，而 masonry 等复杂表面的真实裂缝数据采集耗时且公开数据不充分，亟需探索合成数据来补充训练数据并减少采集负担。

Method: 使用裂缝叠加工具在背景图像上以受控方向和位置生成合成裂缝数据。选择 InceptionV4 作为最优架构，设计六种不同真实/合成数据比例的训练场景，在真实图像测试集上用 F1-score 和 mIoU 评估模型性能。

Result: 仅合成数据训练加 20% 真实数据即可达到与纯真实数据训练相当的结果；20%合成/80%真实场景取得了 76% F1-score 和 80% mIoU，优于纯真实数据训练。

Conclusion: 合成数据能够有效减少真实数据采集工作量，同时提升裂缝检测的准确性，验证了合成数据在结构健康监测中增强深度学习模型训练的可行性。

Original Abstract: Cracks are a critical indicator of building health, and early stage identification is fundamental to prevent harmful damages. Advances in deep learning (DL), particularly convolutional neural networks (CNNs), have enabled scalable solutions for automated crack detection. However, CNN performance strongly depends on the availability of large and diverse datasets, which is particularly challenging for complex surfaces such as masonry. Collecting sufficient real data is time-consuming, while publicly available datasets may not be adequate. To address this limitation, we explored generating synthetic crack data, which complements real data and improves training effectiveness. The real dataset consists of masonry crack images collected from buildings in Bologna and surrounding areas. In contrast, the synthetic dataset was generated using a crack overlay tool that adds cracks to background images in a controlled orientation and placement. The real dataset was used to train several DL architectures, to identify the best-performing model (InceptionV4) employed for experiments with generated data. Six training scenarios were tested in InceptionV4 by varying the ratio of real and synthetic data, with evaluation performed on a test set composed of real images using the F1-score and mean Intersection over Union (mIoU) metrics. Results show that training on synthetic data plus a modest addition of 20% real data achieves results comparable to training on real data only. Moreover, the 20/80 scenario (synthetic/real) achieved an 76% F1-score and 80% mean IoU, outperforming the real-only case. As can be seen, the method demonstrates the potential of synthetic data to reduce collection efforts while enhancing crack detection accuracy.

中文摘要翻译: 裂缝是建筑健康的关键指标，早期识别对于防止有害损伤至关重要。深度学习（DL）的进步，特别是卷积神经网络（CNN），为自动化裂缝检测提供了可扩展的解决方案。然而，CNN的性能在很大程度上依赖于大规模多样化数据集的可用性，这对于砖石等复杂表面而言尤其具有挑战性。收集足够的真实数据耗时费力，而公开数据集可能并不充分。为应对这一限制，我们探索生成了合成裂缝数据，该数据可补充真实数据并提升训练效果。真实数据集包含从博洛尼亚及周边地区建筑中收集的砖石裂缝图像。相比之下，合成数据集使用裂缝叠加工具生成，该工具以受控的方向和位置将裂缝添加到背景图像中。使用真实数据集训练了多种深度学习架构，以确定表现最佳的模型（InceptionV4），进而用于生成数据的实验。在InceptionV4上测试了六种训练场景，通过改变真实与合成数据的比例，并在由真实图像组成的测试集上使用F1-score和平均交并比（mIoU）指标进行评估。结果表明，在合成数据基础上仅添加20%的真实数据进行训练，即可达到与仅使用真实数据训练相当的结果。此外，20/80（合成/真实）场景取得了76%的F1-score和80%的平均IoU，优于纯真实数据的情况。可见，该方法展示了合成数据在减少采集工作量的同时提升裂缝检测精度的潜力。

</details>


### [57] [Sci-Rho: A Multilingual Visually-Grounded Symbolic Benchmark for STEM Problems](https://arxiv.org/abs/2606.08034)
*Muhammad Falensi Azmi,Ikhlasul Akmal Hanif,Vallerie Alexandra Putra,Adi Yeltay,Abdullah Mubarak,Fajri Koto*

Main category: cs.CV

TL;DR: 本文提出Sci-Rho，一个面向多语言、多学科、视觉化STEM问题的动态基准，评估17个前沿视觉语言模型后发现最差精度与平均精度之间存在显著差距，揭示了现有静态基准的局限性。

<details>
  <summary>Details</summary>

Motivation: 现有符号化基准主要局限于数学推理、缺乏视觉定位，且多为英文，无法全面评估视觉语言模型在多样化STEM问题上的鲁棒性。

Method: 构建涵盖5个学科、7种语言、4,242个问题模板（每语言606个）的动态基准Sci-Rho，每个模板由领域专家（含奥赛奖牌得主）编写为可执行的Python代码，通过变化数值、视觉模式、几何形状、配色方案和函数类型生成多样化但等价的实例，总计42,420个实例，每个实例配有推理步骤和真值答案。

Result: 评估17个SOTA VLM发现：(1) 最差精度与平均精度之间存在显著差距；(2) 小模型在多语言场景下性能明显下降，而大模型和专有模型保持鲁棒；(3) 步骤级评估中平均F1与最差F1同样存在显著差距；(4) 注意力头分析显示不同语言间图像token与文本token的相对注意力分配存在显著差异。

Conclusion: 该工作凸显了超越静态基准进行评估对于衡量VLM质量的重要性。

Original Abstract: Symbolic benchmarks have emerged as a key approach to assess model robustness under minor modifications to STEM-related questions. However, existing symbolic benchmarks mostly remain limited to mathematical reasoning, lack visual grounding, and are predominantly in English. In this work, we introduce Sci-Rho (Science Rhobustness), a dynamic benchmark for visually-grounded STEM problems spanning five subjects and seven languages, comprising 4,242 problem templates (606 per language) crafted by domain experts, including Olympiad medalists. Each template is implemented as executable Python code that generates diverse but equivalent problem instances by varying numerical values, visual patterns, geometric shapes, color schemes, and function types, resulting in 42,420 instances in total, each paired with reasoning steps and ground-truth solutions. We evaluated 17 state-of-the-art VLMs and discovered a noticeable gap between worst-case accuracy (defined as the proportion of problem templates that a model answers correctly across every generated variation) and average accuracy. We also discovered that smaller models show noticeable performance degradation across languages, whereas proprietary and larger models remain robust. Step-level evaluation reflects this same trend, revealing a significant gap between average F1 and worst-case F1 scores. Finally, our inspection of attention heads of a VLM reveals substantial cross-lingual variation in the relative attention allocated to image tokens compared to text tokens. Our work highlights the importance of evaluation beyond static benchmarks as a metric to measure the quality of VLMs.

中文摘要翻译: 符号化基准已成为评估模型在STEM相关问题微小修改下鲁棒性的关键方法。然而，现有符号化基准大多局限于数学推理、缺乏视觉定位，且主要以英文为主。在本工作中，我们提出了Sci-Rho（Science Robustness），一个面向视觉定位的STEM问题的动态基准，涵盖5个学科和7种语言，包含由领域专家（包括奥赛奖牌得主）精心设计的4,242个问题模板（每语言606个）。每个模板实现为可执行的Python代码，通过变化数值、视觉模式、几何形状、配色方案和函数类型生成多样化但等价的实例，总计42,420个实例，每个实例配有推理步骤和真值答案。我们评估了17个最先进的视觉语言模型，发现最差精度（定义为模型在所有生成的变体上均能正确回答的问题模板比例）与平均精度之间存在显著差距。我们还发现，较小的模型在不同语言上表现出明显的性能下降，而专有模型和较大模型则保持鲁棒。步骤级评估反映了相同的趋势，揭示了平均F1与最差F1分数之间的显著差距。最后，我们对VLM注意力头的检查显示，图像token相比于文本token的相对注意力分配在不同语言之间存在显著的跨语言差异。我们的工作凸显了超越静态基准进行评估对于衡量视觉语言模型质量的重要性。

</details>


### [58] [DyCo-RL: Dynamic Cross-Modal Coordination for Visual Reasoning](https://arxiv.org/abs/2606.08035)
*Hangui Lin,Yan Shu,Zhengyang Liang,Chi Liu,Xiangrui Liu,Minghao Qin,Teng Long,Zheng Liu,Nicu Sebe*

Main category: cs.CV

TL;DR: 提出DyCo-RL，通过Fisher-Rao测度衡量注意力偏移并分配功能角色，将跨模态协调纳入RLVR优化，提升MLLM在视觉推理中的表现。

<details>
  <summary>Details</summary>

Motivation: 现有RLVR方法仅优化推理结果，忽略了生成过程中的细粒度跨模态协调（视觉证据提取与文本上下文综合的动态交替），而这种协调失败是导致推理错误的因果原因。

Method: DyCo-RL: (1) 使用Fisher-Rao测度衡量Token在视觉和文本模态之间的注意力偏移，将Token划分为视觉导向或文本导向的功能角色；(2) 评估Token实际注意力分配与其分配角色的对齐程度；(3) 利用对齐分数进行优势加权重标定，指导策略优化。

Result: 在Qwen2.5-VL-3B/7B上，DyCo-RL一致性地提升了四种代表性RLVR算法在七个基准（涵盖视觉中心和数学推理）上的表现。

Conclusion: 将动态跨模态协调显式纳入RLVR优化是提升MLLM视觉推理能力的有效途径，DyCo-RL作为算法无关的插件方法具有通用性和有效性。

Original Abstract: Reinforcement Learning with Verifiable Rewards (RLVR) has emerged as a leading paradigm for enhancing visual reasoning in Multimodal Large Language Models (MLLMs). However, existing RLVR methods optimize primarily for the reasoning outcome, fundamentally overlooking the fine-grained cross-modal coordination required during the generation process. Through token-level analyses and controlled interventions, we reveal that during Chain-of-Thought (CoT) reasoning, MLLMs frequently fail to dynamically alternate between extracting visual evidence and synthesizing textual context-a coordination breakdown that is causally linked to reasoning failures. Motivated by these findings, we propose DyCo-RL, which integrates dynamic cross-modal coordination into RLVR optimization. Specifically, DyCo-RL uses the Fisher-Rao geodesic distance to measure within-modality attention shifts, assigning tokens to either visually-oriented or text-oriented functional roles. It then evaluates the alignment between a token's actual attention allocation and its assigned role, leveraging this score for alignment-guided advantage reweighting during policy optimization. Extensive experiments demonstrate that the algorithm-agnostic DyCo-RL, when applied to Qwen2.5-VL-3B/7B, consistently improves four representative RLVR algorithms across seven benchmarks spanning visual-centric and mathematical reasoning.

中文摘要翻译: 基于可验证奖励的强化学习（RLVR）已成为增强多模态大语言模型（MLLMs）视觉推理能力的主导范式。然而，现有RLVR方法主要针对推理结果进行优化，从根本上忽略了生成过程中所需的细粒度跨模态协调。通过token级分析和控制性干预实验，我们发现在链式思维（Chain-of-Thought, CoT）推理过程中，MLLMs经常无法在提取视觉证据和综合文本上下文之间进行动态交替——这种协调失效与推理失败之间存在因果关系。受上述发现启发，我们提出了DyCo-RL，它将动态跨模态协调纳入RLVR优化。具体而言，DyCo-RL利用Fisher-Rao测地距离衡量模态内的注意力偏移，将token分配为视觉导向或文本导向的功能角色；然后评估token实际注意力分配与其分配角色之间的对齐程度，并利用该对齐分数在策略优化过程中进行对齐引导的优势加权重标定。大量实验表明，算法无关的DyCo-RL在应用于Qwen2.5-VL-3B/7B时，能够一致性地提升四种代表性RLVR算法在涵盖视觉中心和数学推理的七个基准上的表现。

</details>


### [59] [Robust-U1: Can MLLMs Self-Recover Corrupted Visual Content for Robust Understanding?](https://arxiv.org/abs/2606.08063)
*Jiaqi Tang,Jianmin Chen,Youyang Zhai,Wei Wei,Runtao Liu,Mengjie Zhao,Xiangyu Wu,Qingfa Xiao,Qifeng Chen*

Main category: cs.CV

TL;DR: 本文提出Robust-U1框架，赋予多模态大语言模型显式的视觉自恢复能力，通过监督微调、强化学习（像素级SSIM+语义级CLIP相似度双重奖励）和多模态推理三个阶段，有效提升模型在真实世界视觉损坏下的鲁棒性。

<details>
  <summary>Details</summary>

Motivation: 多模态大语言模型在视觉理解上表现出色，但在真实世界的视觉损坏下性能显著下降。现有鲁棒性增强方法存在局限：黑盒特征对齐缺乏可解释性，白盒基于文本的推理无法恢复丢失的像素级细节。因此，本文探究一个基础研究问题：MLLMs能否自行恢复损坏的视觉内容？

Method: 提出Robust-U1框架，包含三个核心阶段：(1) 监督微调用于初始重建；(2) 强化学习，使用双重奖励（像素级SSIM和语义级CLIP相似度）来对齐高质量视觉重建；(3) 多模态推理，联合考虑损坏输入和恢复后的图像。

Result: 在真实世界损坏基准上达到最先进的鲁棒性，在通用VQA基准的对抗性损坏下保持优越性能。分析证实高质量视觉恢复直接提升推理性能。

Conclusion: 自恢复机制是鲁棒视觉理解的关键机制，高质量视觉恢复能直接增强推理性能。

Original Abstract: Multimodal Large Language Models (MLLMs) have demonstrated remarkable success in visual understanding, yet their performance degrades significantly under real-world visual corruptions. While existing robustness enhancement approaches exist, they are limited: black-box feature alignment lacks interpretability, and white-box text-based reasoning cannot restore lost pixel-level details. This work investigates a fundamental research question: Can MLLMs recover corrupted visual content by themselves? To address this, we propose Robust-U1, a novel framework that equips MLLMs with explicit visual self-recovery capability for robust understanding. The approach comprises three core stages: supervised fine-tuning for initial reconstruction, reinforcement learning with dual rewards (pixel-level SSIM and semantic-level CLIP similarity) for aligning high visual quality, and multimodal reasoning that jointly considers both the corrupted input and the recovered image. Extensive experiments demonstrate that Robust-U1 achieves state-of-the-art robustness on the real-world corruption benchmark and maintains superior performance under adversarial corruptions on general VQA benchmarks. Analysis confirms that high-quality visual recovery directly enhances reasoning performance, establishing self-recovery as a critical mechanism for robust visual understanding. The source code is available at https://github.com/jqtangust/Robust-U1.

中文摘要翻译: 多模态大语言模型在视觉理解方面取得了显著成功，但其在真实世界的视觉损坏下性能会大幅下降。尽管现有的鲁棒性增强方法存在，但它们都有局限性：黑盒特征对齐缺乏可解释性，而白盒基于文本的推理无法恢复丢失的像素级细节。本文研究了一个基础性问题：多模态大语言模型能否自行恢复损坏的视觉内容？为此，我们提出Robust-U1，一种新颖的框架，赋予多模态大语言模型显式的视觉自恢复能力以实现鲁棒理解。该方法包含三个核心阶段：监督微调用于初始重建、具有双重奖励（像素级SSIM和语义级CLIP相似度）的强化学习用于对齐高质量视觉重建、以及联合考虑损坏输入和恢复图像的多模态推理。大量实验表明，Robust-U1在真实世界损坏基准上达到了最先进的鲁棒性，并在通用VQA基准的对抗性损坏下保持优越性能。分析证实，高质量视觉恢复直接提升了推理性能，确立了自恢复作为鲁棒视觉理解的关键机制。源代码可在https://github.com/jqtangust/Robust-U1获取。

</details>


### [60] [VideoWeaver: Evaluating and Evolving Skills for Agentic Long Video Generation](https://arxiv.org/abs/2606.08091)
*Jianhui Wei,Jie Tan,Hengchuan Zhu,Xiaotian Zhang,Yan Zhang,Ziyi Chen,Daoan Zhang,Wei Xu,Zuozhu Liu*

Main category: cs.CV

TL;DR: VideoWeaver是一个用于长视频生成的智能体框架和基准测试，支持智能体自主构建工作流、通过智能体作为裁判进行评估，并通过技能进化算法优化技能，实验表明组合技能、技能进化和裁判机制均有效。

<details>
  <summary>Details</summary>

Motivation: 现有如Claude Code、Codex等智能体框架在工具使用和编排方面表现出色，但它们在长视频生成这一长程多模态任务上的能力尚未被充分探索。与之前手工设计管线的视频智能体不同，这些框架能够自主构建和优化工作流，因此需要一个专门的基准和评估方法来衡量和提升它们的表现。

Method: 提出了VideoWeaver框架，包含三部分：(1) 一个涵盖16个任务类别、285个测试用例的基准测试集；(2) 一种智能体作为裁判（agent-as-judge）的评估方法，同时检查执行轨迹和最终视频，基于元数据和中间文件提供评分证据；(3) 一种技能进化算法，用于优化和合并智能体的技能。

Result: 实验表明：(1) 使用显式组合技能比仅使用基础技能能改善生成过程；(2) 技能进化算法进一步提升输出质量；(3) 不同框架和模型之间的性能存在显著差异；(4) 智能体作为裁判的评估方法与人类判断高度一致，尤其在过程指标上表现良好。

Conclusion: VideoWeaver验证了让智能体自主构建工作流进行长视频生成的可行性，证明了组合技能和技能进化的有效性，并提供了一个与人类判断高度一致的自动评估方法。

Original Abstract: Recent agent frameworks such as Claude Code, Codex, and OpenClaw are strong at tool use and orchestration, but whether they can handle long video generation, a long-horizon multimodal task, remains underexplored. Unlike earlier video agents whose pipeline is handcrafted, these frameworks can build and refine their own workflows. We introduce VideoWeaver, an agent harness and benchmark that evaluates and evolves skills for long video generation, where an agent turns a single instruction into a long video by composing foundation skills into its own workflow rather than following a predefined pipeline. The benchmark has 16 task categories and 285 cases, with references spanning text, image, audio, video, and their combinations. Because errors can arise at any stage and not just in the final video, we propose an agent-as-judge that inspects both the execution trace and the final video, grounding its scores in evidence such as metadata and intermediate files. Using this feedback, we further design a skill evolution algorithm that refines and merges the agent's skills. Across multiple frameworks and models, we find that an explicit composition skill improves the generation process over using foundation skills alone, that skill evolution further improves output quality, and that performance varies notably across harness and model choices. The proposed agent-as-judge also aligns well with human judgments, especially on process metrics. Code and dataset is available at https://github.com/JianhuiWei7/VideoWeaver

中文摘要翻译: 最近的智能体框架如Claude Code、Codex和OpenClaw在工具使用和编排方面表现出色，但它们能否处理长视频生成这一长程多模态任务，仍未被充分探索。与早期使用手工设计管线的视频智能体不同，这些框架能够自主构建和优化自己的工作流程。我们提出了VideoWeaver，一个智能体框架和基准测试，用于评估和进化长视频生成的技能。在该框架中，智能体通过组合基础技能形成自己的工作流程而非遵循预定义的管线，从而将单条指令转化为长视频。该基准测试包含16个任务类别和285个测试用例，参考信息涵盖文本、图像、音频、视频及其组合。由于错误可能出现在任何阶段而不仅仅是最终视频中，我们提出了一种智能体作为裁判的方法，该方法检查执行轨迹和最终视频，并将其评分依据于元数据和中间文件等证据。利用这一反馈，我们进一步设计了一种技能进化算法，用于优化和合并智能体的技能。在多种框架和模型上的实验表明，显式的组合技能比仅使用基础技能更能改善生成过程，技能进化进一步提升了输出质量，并且不同框架和模型之间的性能存在显著差异。所提出的智能体作为裁判的方法也与人类判断高度一致，尤其在过程指标上。代码和数据集可在https://github.com/JianhuiWei7/VideoWeaver获取。

</details>


### [61] [Trustworthy Visual Predicates for Robust Manipulation Understanding under Degradation](https://arxiv.org/abs/2606.08121)
*Fatemeh Ziaeetabar*

Main category: cs.CV

TL;DR: 本文提出谓词级可靠性框架，用于分析视觉退化（模糊、遮挡、光照变化、低分辨率、丢帧、检测噪声）对操作理解中关键视觉谓词（接触、支撑、包含、运动耦合、抓取、释放、主动手参与）的影响。实验表明，静态空间谓词较鲁棒，而接触敏感和动态谓词（如抓取、释放）更脆弱，严重退化下操作理解准确率从0.89降至0.58。

<details>
  <summary>Details</summary>

Motivation: 现有操作理解模型广泛使用接触、支撑、抓取等视觉谓词，但这些谓词在视觉退化条件下的可靠性鲜有直接分析，缺乏系统性的谓词级可靠性评估框架。

Method: 提出谓词级可靠性框架，包括：结构化谓词语义体系、置信度感知的谓词估计方法、以及多维度可靠性指标（谓词保持率、退化灵敏度、时序一致性、置信加权稳定性、下游影响）。

Result: 在VISOR/EPIC-KITCHENS、H2O、ARCTIC等数据集上实验发现：谓词失效是结构化的而非均匀的；静态空间谓词较鲁棒，接触敏感和动态谓词更脆弱；检测噪声、遮挡和丢帧导致最强可靠性损失；退化使操作理解准确率从0.89降至0.58，中等退化下去除置信加权使准确率从0.74降至0.64。

Conclusion: 谓词可靠性框架为视觉感知与结构化操作推理之间提供了诊断层级，揭示谓词失效的结构化模式，有助于设计更鲁棒的操作理解系统。

Original Abstract: Manipulation understanding requires reliable relational evidence, such as contact, support, containment, motion coupling, grasp, release, and active-hand involvement. Although these visual predicates are widely used in event-chain, graph-based, and neuro-symbolic models, their reliability under visual degradation is rarely analyzed directly. This paper introduces a predicate-level reliability framework for robust manipulation understanding under blur, occlusion, illumination change, low resolution, frame dropping, and detection noise. The framework defines a structured predicate vocabulary, confidence-aware predicate estimation, and reliability metrics for predicate preservation, degradation sensitivity, temporal consistency, confidence-weighted stability, and downstream impact. Experiments on controlled manipulation videos and public egocentric or bimanual datasets, including VISOR/EPIC-KITCHENS, H2O, and ARCTIC, show that predicate failures are structured rather than uniform. Static spatial predicates remain comparatively robust, whereas contact-sensitive, dynamic, and derived predicates such as grasp and release are more fragile. Under severe degradation, detection noise, occlusion, and frame dropping cause the strongest reliability losses. Downstream analysis shows that degraded predicates reduce manipulation-understanding accuracy from 0.89 to 0.58, while removing confidence weighting under moderate degradation reduces accuracy from 0.74 to 0.64. These results show that predicate reliability provides a diagnostic layer between visual perception and structured manipulation reasoning.

中文摘要翻译: 操作理解需要可靠的关联性证据，例如接触、支撑、包含、运动耦合、抓取、释放和主动手参与。尽管这些视觉谓词被广泛用于事件链、基于图的和神经符号模型中，但它们在视觉退化条件下的可靠性很少被直接分析。本文提出了一种谓词级可靠性框架，用于在模糊、遮挡、光照变化、低分辨率、丢帧和检测噪声下实现鲁棒的操作理解。该框架定义了结构化的谓词语义体系、置信度感知的谓词估计，以及用于谓词保持率、退化灵敏度、时序一致性、置信加权稳定性和下游影响的多项可靠性指标。在受控操作视频及公开的自我中心或双手数据集（包括VISOR/EPIC-KITCHENS、H2O和ARCTIC）上进行的实验表明，谓词失效是结构化的而非均匀的。静态空间谓词保持相对鲁棒，而接触敏感型、动态型和派生型谓词（如抓取和释放）则更为脆弱。在严重退化下，检测噪声、遮挡和丢帧导致最强的可靠性损失。下游分析显示，退化谓词将操作理解准确率从0.89降至0.58，而在中等退化下去除置信加权使准确率从0.74降至0.64。这些结果表明，谓词可靠性为视觉感知与结构化操作推理之间提供了一个诊断层级。

</details>


### [62] [Human-Centered Benchmarking of Driver Monitoring Models](https://arxiv.org/abs/2606.08123)
*Ruben Dario Florez-Zela*

Main category: cs.CV

TL;DR: 本文提出人本基准测试框架(HCBF)，从准确性、可解释性、效率和鲁棒性四个维度评估驾驶员监控系统，发现即便模型在干净集上准确率相近，各维度表现差异显著，综合排名第一的ShuffleNetV2在传感器噪声下表现脆弱，凸显了多维度人本评估的必要性。

<details>
  <summary>Details</summary>

Motivation: 现有基于视觉的驾驶员监控系统几乎仅以分类准确率作为评估标准，但仅凭准确率不足以反映模型在真实部署场景中的适用性，亟需更全面的评估方法。

Method: 提出人本基准测试框架(HCBF)，从准确性、可解释性、效率和鲁棒性四个维度对模型进行评估，并在MRL眼部数据集上对MobileNetV3、ShuffleNetV2、EfficientNet-B0和DeiT-Tiny四种轻量级架构进行眼部状态分类实验，同时设计了三个面向部署场景的加权方案计算人本评分。

Result: 四种模型在干净集上准确率几乎无差别，但每个模型在一个维度上领先且均处于帕累托边界；人本评分下ShuffleNetV2在所有场景中排名第一，但其在传感器噪声下性能保留不足一半，且将闭眼状态误判为睁眼，而Transformer模型保持鲁棒。

Conclusion: 综合排名可能掩盖特定维度的关键脆弱性，而这些维度在操作层面具有决定性作用，强调了多维度、以人为本的评估方法的重要性。

Original Abstract: Vision-based driver monitoring systems are increasingly deployed in safety-critical intelligent transportation settings, yet they are almost always compared on classification accuracy alone. This paper argues that accuracy is insufficient to characterize a model's fitness for real-world deployment, and proposes the Human-Centered Benchmarking Framework (HCBF), which evaluates models across four dimensions: accuracy, explainability, efficiency, and robustness. The framework is applied to four representative lightweight architectures, MobileNetV3, ShuffleNetV2, EfficientNet-B0, and DeiT-Tiny, on the MRL Eye Dataset for eye-state classification. While the models are nearly indistinguishable on clean-set accuracy, each leads in exactly one dimension, and all four lie on the Pareto frontier. A Human-Centered Score computed under three deployment-oriented weighting scenarios ranks ShuffleNetV2 first throughout. However, this aggregate winner retains less than half of its performance under sensor noise and fails by classifying closed eyes as open, whereas the transformer remains robust. These findings show that aggregate ranking can mask dimension-specific vulnerabilities that are operationally decisive, underscoring the value of multi-dimensional, human-centered evaluation.

中文摘要翻译: 基于视觉的驾驶员监控系统正越来越多地部署在安全关键的智能交通场景中，然而它们几乎总是仅凭分类准确率进行比较。本文认为，仅靠准确率不足以表征模型在真实部署场景中的适用性，并提出了人本基准测试框架（Human-Centered Benchmarking Framework, HCBF），该框架从四个维度评估模型：准确性、可解释性、效率和鲁棒性。该框架被应用于四种代表性的轻量级架构——MobileNetV3、ShuffleNetV2、EfficientNet-B0和DeiT-Tiny，在MRL眼部数据集上进行眼部状态分类任务。虽然这些模型在干净测试集上的准确率几乎难以区分，但每个模型恰好在一个维度上领先，并且所有四个模型均位于帕累托前沿。在三种面向部署场景的加权方案下计算的人本评分（Human-Centered Score）显示，ShuffleNetV2在所有场景中均排名第一。然而，这一综合优胜者在传感器噪声下保留的性能不足一半，并且将闭眼状态错误分类为睁眼状态，而Transformer模型则保持鲁棒。这些发现表明，综合排名可能掩盖特定维度的脆弱性，而这些脆弱性在操作层面具有决定性作用，从而凸显了多维度、以人为本的评估方法的重要价值。

</details>


### [63] [One Stone, Three Birds: Self-adaptive Optimal Transport for Multi-VLM Selection, Adaptation, and Ensembling](https://arxiv.org/abs/2606.08126)
*Qiyu Xu,Zhanxuan Hu,Yu Duan,Yonghang Tai,Huafeng Li,Quanxue Gao,Xiangyong Cao*

Main category: cs.CV

TL;DR: 提出一个无需训练的多VLM部署框架OSTB，通过自适应性最优输运统一解决模型选择、目标适应和预测集成三个问题。

<details>
  <summary>Details</summary>

Motivation: 现有单骨干VLM部署范式隐含一个关键假设：所选VLM已与目标域兼容。在真实跨域部署中，多个通用或领域特化VLM都可能适用，但缺乏实例级标签来选择可靠模型，因此需要联合解决模型选择、目标适应和预测集成问题。

Method: 提出OSTB（一石三鸟）框架，基于自适应性最优输运。从多个冻结候选VLM中估计一个共识的样本-类别输运计划，该计划同时用于：1）通过语义和视觉可靠性排序进行模型选择；2）通过拟合输运条件视觉分类器进行目标适应；3）通过可靠性感知概率集成进行预测融合。

Result: 在自然图像、遥感影像和医学病理三个基准上，OSTB在异构候选池下显著提升了模型排序、适应稳定性和集成鲁棒性。

Conclusion: 从系统级多VLM视角出发，证明模型选择、目标适应和预测集成三个决策依赖于同一个隐变量（可信的样本-类别结构），并可通过统一的最优输运框架同时解决。

Original Abstract: Vision-language models (VLMs) enable visual recognition from semantic class descriptions, which makes them attractive when target annotations are scarce or unavailable. Most deployment pipelines, however, first choose a single VLM and then adapt that model to the unlabeled target set. This single-backbone paradigm hides a critical assumption: the selected VLM is already compatible with the target domain. In realistic cross-domain deployment, several general-purpose and domain-specialized VLMs may be plausible, yet no instance-level target labels are available to identify the reliable ones. Deployment therefore requires a coupled solution for model selection, target adaptation, and prediction integration. We revisit this problem from a system-level multi-VLM perspective. Our central observation is that the three decisions above depend on the same latent object: a trustworthy sample-class structure in the target set. Different VLMs may encode different transfer biases and produce conflicting predictions, but their outputs can still provide complementary evidence for estimating this structure. We propose One Stone, Three Birds, a training-free framework based on self-adaptive optimal transport. Given a pool of frozen candidate VLMs, OSTB estimates a consensus sample-to-class transport plan without updating VLM parameters. The learned transport structure is then reused for all deployment objectives: model selection is performed by ranking the combined semantic and visual reliability induced by the consensus plan; target adaptation is obtained by fitting transport-conditioned visual classifiers; and ensembling is implemented through reliability-aware probabilistic integration. Extensive experiments on natural-image, remote-sensing, and medical-pathology benchmarks show that OSTB improves model ranking, adaptation stability, and ensemble robustness under heterogeneous candidate pools.

中文摘要翻译: 视觉-语言模型（VLM）能够通过语义类别描述实现视觉识别，这使其在目标标注稀缺或不可用时颇具吸引力。然而，大多数部署流程首先选择一个单一VLM，然后将该模型适配到未标注的目标集上。这种单骨干范式隐藏了一个关键假设：所选VLM已经与目标域兼容。在真实的跨域部署场景中，多个通用型和领域特化型VLM可能都看似合理，但由于缺乏实例级的目标标签，无法甄别出可靠的模型。因此，部署需要一个耦合的解决方案来处理模型选择、目标适配和预测集成。我们从系统级的多VLM视角重新审视这一问题。我们的核心观察是，上述三个决策依赖于同一个隐变量：目标集中可信的样本-类别结构。不同的VLM可能编码不同的迁移偏差并产生冲突的预测，但它们的输出仍可为估计这一结构提供互补证据。我们提出"一石三鸟"（OSTB），一种基于自适应性最优输运的免训练框架。给定一个冻结的候选VLM池，OSTB在不更新VLM参数的情况下估计一个共识性的样本到类别输运计划。学习到的输运结构随后被重用于所有部署目标：通过排序共识计划所诱导的组合语义与视觉可靠性来实现模型选择；通过拟合输运条件视觉分类器来实现目标适配；通过可靠性感知的概率集成来实现预测融合。在自然图像、遥感影像和医学病理基准上的大量实验表明，OSTB在异构候选池下显著提升了模型排序、适配稳定性和集成鲁棒性。

</details>


### [64] [Phase Marginalization for Patch-Grid Instability in Vision Transformers](https://arxiv.org/abs/2606.08132)
*Oğuzhan Ercan*

Main category: cs.CV

TL;DR: 本文提出Phase Marginalization方法，用于解决Vision Transformer因patch网格相位变化导致的密集预测不稳定问题。该方法无需重新训练，通过聚合不同相位下的输出来提升分割、深度估计和局部匹配性能，在K=4时达到最佳性价比。

<details>
  <summary>Details</summary>

Motivation: Vision Transformer依赖固定的patch网格划分，这种划分方式在密集预测任务中会导致相位相关的预测不稳定性：改变patch划分边界会改变像素可获取的token证据，尤其是在边界附近。作者将patch网格相位形式化为一个干扰变量（nuisance variable），旨在消除其对密集预测的影响。

Method: 提出Phase Marginalization（相位边缘化）方法，一种事后边缘化方法。具体过程包括：(1) 评估结构化的patch网格相位（即改变patch划分方式）；(2) 将不同相位下的密集预测输出逆对齐（inverse-align）回原始图像坐标系；(3) 在原始图像坐标系中聚合这些结果。核心变体为Uniform Phase Marginalization with K=4，无需额外训练。

Result: 在Cityscapes实验上，Uniform Phase Marginalization（K=4）相比通用的基于平移的四次测试时增强（TTA）方法，在mIoU上高出0.31。扩展实验表明K=4是性价比最优的选择：K=8结果基本不变，K=16精度提升极小但延迟显著增加。

Conclusion: patch网格相位是可测量的干扰变量，Phase Marginalization可作为密集ViT预测任务的简单诊断工具和事后边缘化基线方法。

Original Abstract: Vision Transformers operate on fixed patch grids, which can introduce phase-dependent instability for dense prediction: changing the patch partition can change the token evidence available to a pixel, especially near boundaries. We formalize patch-grid phase as a nuisance variable and propose Phase Marginalization, a post-hoc marginalization method that evaluates structured patch-grid phases, inverse-aligns dense outputs, and aggregates them in the original image coordinate system. The central variant, Uniform Phase Marginalization with K = 4, is training-free and improves over the canonical K = 1 baseline across measured segmentation, depth, and local matching settings. In a controlled Cityscapes experiment, Uniform Phase Marginalization provides a modest compute-matched advantage over generic shift-based four-forward test-time augmentation (TTA) (+0.31 mean Intersection-over-Union over the strongest tested generic row). A scaling study further shows that K = 4 is a practical cost-accuracy trade-off: K = 8 is essentially unchanged and K = 16 adds little accuracy at much higher latency. These results position patch-grid phase as a measurable nuisance variable and Phase Marginalization as a simple diagnostic and post-hoc marginalization baseline for dense ViT prediction.

中文摘要翻译: Vision Transformer在固定的patch网格上运行，这会在密集预测任务中引入相位相关的不稳定性：改变patch划分方式会改变像素可获取的token证据，尤其是在边界附近。本文将patch网格相位形式化为一个干扰变量，并提出相位边缘化（Phase Marginalization）方法——一种事后边缘化方法，它评估结构化的patch网格相位，逆对齐密集输出，并在原始图像坐标系中聚合它们。核心变体——均匀相位边缘化（K=4）——无需训练，且在所测的分割、深度估计和局部匹配任务上均优于标准的K=1基线。在受控的Cityscapes实验中，均匀相位边缘化相较于通用的基于平移的四前向测试时增强（TTA）方法，在计算量匹配的情况下提供了适度优势（在最强通用行上平均交并比高出0.31）。进一步扩展研究表明，K=4是一个实用的性价比权衡点：K=8结果基本不变，K=16精度提升极小但延迟显著增加。这些结果表明，patch网格相位是一个可测量的干扰变量，而相位边缘化可作为密集ViT预测任务的一个简单诊断和事后边缘化基线方法。

</details>


### [65] [Gravity-guided Contact Dynamics Estimation from 3D Human Motions](https://arxiv.org/abs/2606.08133)
*Cuong Le,Urs Waldmann,Bastian Wandt,Mårten Wadenbäck*

Main category: cs.CV

TL;DR: 本文提出GraCE，一种基于重力引导的全身体接触动力学估计方法，通过人体重心与身体距离估算地面接触，利用接触概率与质心轨迹计算的外力乘积估算作用力，在GroundLink和MOYO数据集上分别优于现有方法的地面反作用力估计和接触压力预测。

<details>
  <summary>Details</summary>

Motivation: 现有方法依赖测力台或压力垫采集地面接触动力学，限制了其在受控环境之外的应用；而基于运动捕捉数据的估计方法仅通过身体与地面的垂直距离粗略估算，无法捕捉复杂接触点的压力分布。

Method: 利用人体重心与身体各部位之间的相对距离来估计地面接触；通过预测的接触概率与从质心轨迹计算的总外力相乘来估算每个接触点上的作用力，同时考虑了身体质量分布和重力的真实影响。

Result: 在GroundLink数据集上，地面反作用力估计优于相关工作；在MOYO数据集上，详细接触压力预测也优于现有方法。

Conclusion: GraCE能够更准确地从运动捕捉数据中估计地面接触动力学，捕捉复杂接触点压力分布，且方法具有可扩展性，代码将在论文接收后公布。

Original Abstract: Ground contact forces acting on the human body, are crucial for biomechanics studies or sport performance analysis. Prior methods rely on force plates or pressure mats to collect ground contact dynamics, limiting their applicability to carefully controlled settings. A more scalable solution is to estimate the dynamics directly from motion capture data. Recent approaches only roughly estimate the ground contact dynamics from the vertical distance between the body and the ground plane, which cannot capture the complex pressure distribution of all contact points. To this end, we propose GraCE -- Gravity-guided Contact Dynamics Estimation, a novel full-body contact dynamics model for human motions using a realistic influence of body mass distribution and gravity. We use the human's center of gravity to estimate the ground contacts based on its relative distance to the human body. The applied force on each contact is estimated via the product of predicted contact probabilities and the total exterior force computed from the center of mass trajectory. We outperform related work on the GroundLink dataset for ground reaction force estimation, and on the MOYO dataset for detailed contact pressure prediction. The code is published upon acceptance.

中文摘要翻译: 作用于人体的地面接触力对于生物力学研究或运动性能分析至关重要。现有方法依赖测力台或压力垫来采集地面接触动力学，这限制了其在精心控制的实验室环境之外的应用。更具可扩展性的解决方案是直接从运动捕捉数据中估计动力学。近期方法仅通过身体与地面平面之间的垂直距离来粗略估计地面接触动力学，无法捕捉所有接触点的复杂压力分布。为此，我们提出GraCE——重力引导的接触动力学估计（Gravity-guided Contact Dynamics Estimation），一种新颖的全身体接触动力学模型，利用身体质量分布和重力的真实影响来估计人体运动。我们利用人体重心与身体各部位之间的相对距离来估计地面接触。每个接触点上的作用力通过预测的接触概率与从质心轨迹计算的总外力的乘积来估计。我们在GroundLink数据集上的地面反作用力估计任务以及MOYO数据集上的详细接触压力预测任务中均优于相关工作。代码将在论文接收后公布。

</details>


### [66] [IMAGINE: Adaptive Schema-Imagery Enhanced Composition for Composed Video Retrieval](https://arxiv.org/abs/2606.08144)
*Jiale Huang,Zixu Li,Zhiwei Chen,Zhiheng Fu,Chunxiao Wang,Yupeng Hu*

Main category: cs.CV

TL;DR: 本文提出IMAGINE网络，通过动态多模态原型捕获隐式语义（模式意象），弥合显式视觉内容与隐式检索意图之间的差距，实现组合视频/图像检索中的最优性能。

<details>
  <summary>Details</summary>

Motivation: 现有组合视频检索方法假设修改文本描述的物体直接出现在视频中，但实际中修改文本常描述非显式呈现的概念（如"蛋糕"暗示"生日派对"），现有方法仅对齐显式特征表征，忽略了关键的潜在关联。

Method: 提出IMAGINE网络，通过动态多模态原型捕获隐式语义（称为模式意象），自适应地调制视觉特征，将隐式引导注入检索过程，弥合显式视觉内容与隐式检索意图之间的鸿沟。

Result: 在三个广泛使用的基准数据集上，IMAGINE在组合视频检索（CVR）和组合图像检索（CIR）任务中均取得了最先进的性能。

Conclusion: 通过动态多模态原型捕获隐式语义能够有效提升组合检索的性能，IMAGINE通过桥接显式内容与隐式意图的差距，在CVR和CIR任务上均达到SOTA。

Original Abstract: Composed Video Retrieval (CVR) is designed to retrieve a target video that matches a reference video modified by a modification text. While existing methods explore cross-modal correspondences, they often assume modified objects appear directly in videos. However, modification texts frequently describe concepts not explicitly presented but implicitly expressed through semantically related visual cues (e.g., "cake" implying "birthday party"). Current approaches typically rely on aligning explicit feature representations within the concrete space, neglecting critical latent associations. To address this, we propose an adaptIve scheMa-ImAGery enhanced composItional NEtwork (IMAGINE). Unlike standard explicit matching, IMAGINE materializes implicit semantics (termed schema imagery) via dynamic multimodal prototypes. These prototypes capture shared latent concepts to adaptively modulate visual features, effectively injecting implicit guidance into the retrieval process. By bridging the gap between explicit visual contents and implicit retrieval intentions, IMAGINE achieves state-of-the-art performance in both CVR and Composed Image Retrieval (CIR) across three widely used benchmarks.

中文摘要翻译: 组合视频检索（CVR）旨在检索与参考视频经过修改文本修改后相匹配的目标视频。现有方法虽然探索了跨模态对应关系，但它们通常假设被修改的物体直接出现在视频中。然而，修改文本经常描述并非显式呈现、而是通过语义相关的视觉线索隐含表达的概念（例如，"蛋糕"暗示"生日派对"）。当前方法通常依赖在具体空间中对齐显式特征表征，忽略了关键的潜在关联。为解决这一问题，我们提出了一种自适应模式-意象增强的组合网络（IMAGINE）。与标准的显式匹配不同，IMAGINE通过动态多模态原型实现隐式语义（称为模式意象）的具象化。这些原型捕获共享的潜在概念，自适应地调制视觉特征，有效将隐式引导注入检索过程。通过弥合显式视觉内容与隐式检索意图之间的鸿沟，IMAGINE在三个广泛使用的基准数据集上的组合视频检索（CVR）和组合图像检索（CIR）任务中均取得了最先进的性能。

</details>


### [67] [Property-Informed Diffusion-Based Text-to-Microstructure Generation](https://arxiv.org/abs/2606.08150)
*Bingxuan Dai,Hongsong Wang,Jie Gui*

Main category: cs.CV

TL;DR: 提出了一种基于扩散模型和文本描述的属性引导网络（PropDiff-TMG），用于直接根据文本输入生成物理可行的3D超材料微结构，并通过对比对齐和奖励引导双重对齐策略保证结构与文本的一致性。

<details>
  <summary>Details</summary>

Motivation: 现有的3D超材料微结构逆向设计方法通常需要大量领域知识、迭代仿真和人工调参，且存在设计多样性不足、生成结构物理可行性难以保证的问题。因此，亟需一种能够从文本描述直接生成多样化且物理可行的3D微结构的方法。

Method: 提出了一种属性引导的扩散网络（PropDiff-TMG），利用文本输入中的语义和物理属性信息作为引导，实现3D微结构合成。采用双重对齐策略：对比式文本-结构对齐（contrastive text-structure alignment）和测试时奖励引导对齐（test-time reward-guided alignment），以增强生成结构与文本提示的一致性。

Result: 实验结果表明，该模型能够在多种材料类别中生成语义上有意义且物理上合理的微结构，验证了方法的有效性和泛化能力。

Conclusion: 该方法在交互式微结构设计中具有良好的应用潜力，为结合语言界面与逆向材料发现开辟了新方向。

Original Abstract: Designing 3D metamaterial microstructures that meet the intended functions remains a major challenge, as it typically requires domain expertise, iterative simulations, and extensive manual tuning. Existing work on inverse design that automatically generates microstructures based on desired target properties often suffers from limited design diversity and faces challenges in ensuring the physical feasibility of the generated structures. To address this issue, a property-informed diffusion-based network is proposed that enables the generation of 3D microstructures directly from textual descriptions. Unlike traditional property conditioning methods, our approach leverages rich guidance in terms of semantics and physical properties in the text input to support diverse structure synthesis. To enforce consistency between the generated structures and the target textual prompts, a dual alignment strategy is adopted, including contrastive text-structure alignment and test-time reward-guided alignment. Experimental results show that the model is capable of generating semantically meaningful and physically plausible structures across a wide range of material categories. Our approach has good potential for interactive microstructure design and opens up new directions for combining language-based interfaces with inverse material discovery. Code is available at: https://github.com/hongsong-wang/PropDiff-TMG

中文摘要翻译: 设计满足预期功能的三维超材料微结构仍然是一项重大挑战，因为这通常需要领域专业知识、迭代仿真和大量的人工调参。现有的基于目标属性自动生成微结构的逆向设计工作往往设计多样性有限，并且在确保生成结构的物理可行性方面面临困难。为了解决这一问题，本文提出了一种属性引导的扩散网络，能够直接从文本描述生成三维微结构。与传统的属性条件化方法不同，我们的方法利用文本输入中丰富的语义和物理属性引导，支持多样化的结构合成。为了增强生成结构与目标文本提示之间的一致性，我们采用了一种双重对齐策略，包括对比式文本-结构对齐和测试时奖励引导对齐。实验结果表明，该模型能够在广泛材料类别中生成语义有意义且物理合理的三维微结构。我们的方法在交互式微结构设计中具有良好的潜力，并为结合语言界面与逆向材料发现开辟了新方向。代码已开源：https://github.com/hongsong-wang/PropDiff-TMG

</details>


### [68] [RAPID: Layer-Wise Redundancy-Aware Pruning and Importance-Driven Token Merging for Efficient ViT](https://arxiv.org/abs/2606.08156)
*Kyumin Choi,Ikbeom Jang*

Main category: cs.CV

TL;DR: RAPID提出了一种深度感知的令牌缩减框架，根据网络深度调整缩减策略：浅中层使用冗余相似度感知剪枝，深层则转为重要性相似度感知合并，在ImageNet-1K上取得了优于ToMe和ToFu的精度-压缩帕累托边界。

<details>
  <summary>Details</summary>

Motivation: Vision Transformers (ViTs) 因二次复杂度的自注意力机制导致计算成本过高，现有令牌缩减方法（如剪枝、合并）未考虑令牌表示随网络深度演化的特性，导致压缩策略与特征层级不匹配。

Method: 提出深度感知的令牌缩减框架RAPID，采用分叉策略：浅到中层使用基于冗余相似度的剪枝指标去除过度表示的局部模式；深层特征转为全局语义概念后，切换为基于重要性相似度的合并机制，利用CLS令牌注意力权重保护语义关键令牌并融合不重要的相似邻居。

Result: 在ImageNet-1K上使用ViT和DeiT架构，RAPID建立了优于ToMe和ToFu的精度-压缩帕累托前沿，在极端压缩率下比ToMe准确率高出4.29%。

Conclusion: RAPID提供了一种免训练模板，通过将缩减策略与层级特征演化对齐来优化视觉模型，特别在激进压缩场景下鲁棒性强。

Original Abstract: Vision Transformers (ViTs) achieve strong performance but suffer from high computational costs due to quadratic self-attention complexity. Although token reduction techniques such as pruning and merging mitigate this, they typically overlook how representations evolve across network depth. We propose RAPID, a depth-aware token reduction framework that adapts reduction strategies to the layer-wise characteristics of token representations. The primary methodological contribution is a bifurcated strategy: in shallow-to-middle layers, RAPID employs a redundancy-similarity aware pruning metric to eliminate over-represented local patterns. As features transition to global semantic concepts in deeper layers, the framework shifts to an importance-similarity aware merging mechanism. This stage leverages classification (CLS) token attention weights to protect semantically critical tokens while fusing less important but similar neighbors. Empirical validation on ImageNet-1K using ViT and DeiT architectures demonstrates that RAPID establishes a superior accuracy-compression Pareto frontier compared to plug-and-play baselines such as ToMe and ToFu. RAPID is particularly robust in aggressive compression regimes, achieving up to 4.29% higher accuracy than ToMe at extreme reduction rates. Our framework provides a training-free template for optimizing vision models by aligning reduction strategies with hierarchical feature evolution.

中文摘要翻译: 视觉Transformer（ViTs）取得了强劲的性能，但由于二次复杂度的自注意力机制而面临高昂的计算成本。尽管剪枝和合并等令牌缩减技术缓解了这一问题，但它们通常忽略了表示如何随网络深度演化。我们提出RAPID，一种深度感知的令牌缩减框架，能够根据令牌表示的逐层特征自适应调整缩减策略。主要的 methodological 贡献是一种分叉策略：在浅层到中间层，RAPID采用冗余相似度感知的剪枝指标来消除过度表示的局部模式。当特征在更深层过渡到全局语义概念时，该框架切换为重要性相似度感知的合并机制。这一阶段利用分类（CLS）令牌注意力权重来保护语义关键令牌，同时融合不重要但相似的邻居。在ImageNet-1K上使用ViT和DeiT架构的实证验证表明，与ToMe和ToFu等即插即用基线相比，RAPID建立了优越的精度-压缩帕累托前沿。RAPID在激进压缩场景下尤为鲁棒，在极端缩减率下比ToMe准确率高出4.29%。我们的框架提供了一种免训练模板，通过将缩减策略与层级特征演化的对齐来优化视觉模型。

</details>


### [69] [How Much MRI Preprocessing Is Enough? A Cost-Utility Study for Brain MRI Foundation Models](https://arxiv.org/abs/2606.08164)
*Jiangshuan Pang,Wangyang Tang,Jing Yan,Zhixuan Cheng,Youzhe He,Zhenkun Zhuang,Tao Zhou,Shiping Liu*

Main category: cs.CV

TL;DR: 本文系统研究了不同预处理强度对3D脑MRI自监督预训练的影响，发现“预处理越强越好”并不成立，预处理应被视为一种下游感知的成本-效用决策。

<details>
  <summary>Details</summary>

Motivation: MRI预处理通常被视为常规数据清洗而非建模选择，但其决定了下游模型的输入分布。现有研究缺乏对预处理强度与自监督预训练成本效益关系的系统评估。

Method: 在固定语料库、3D ViT骨干网络、掩码协议和下游评估的条件下，比较了P0-P7共8个等级的预处理谱系，分别用于掩码自编码（MAE）和联合嵌入预测学习（JEPA）两种自监督方法，在20,000个异质性脑MRI体数据上进行预训练，然后迁移到IDH预测、MCI分类、脑龄回归和脑肿瘤分割四个下游任务上评估。

Result: P0/P1数值不稳定，P2是最低可行预处理等级；超过P2后，选择最佳预处理等级相比P2仅提升MAE 3.4个百分点、JEPA 1.8个百分点，且多数差异统计不显著。强预处理仅在特定任务中有益：IDH任务略有提升，AGE和GLI/PED任务在P2附近表现最佳，MCI任务在P7有最明显的增益。跨等级的MCI迁移实验表明，P7的大部分优势可通过在下游应用更强预处理来恢复。

Conclusion: MRI预处理应被视为一种下游感知的成本-效用决策，而非默认的升级流水线。简单“越多越好”的预处理规则不成立。

Original Abstract: MRI preprocessing defines the input distribution seen by brain MRI foundation models, yet it is usually treated as routine data cleaning rather than a modeling choice. We ask how much preprocessing is worth its computational cost for self-supervised 3D MRI pretraining. Keeping the corpus, 3D ViT backbone, masking protocol, and downstream evaluations fixed, we compare a graded P0-P7 preprocessing spectrum for masked autoencoding (MAE) and joint-embedding predictive learning (JEPA) on 20,000 heterogeneous brain MRI volumes, then transfer the encoders to IDH prediction, MCI classification, brain age regression, and GLI/PED tumor segmentation. The results do not support a simple "more is better" rule. P0/P1 are numerically unstable, making P2 the lowest-cost feasible level; beyond P2, choosing the best feasible preprocessing level improves aggregate utility by only 3.4 percentage points for MAE and 1.8 percentage points for JEPA, with most paired gains statistically unresolved. Stronger preprocessing is beneficial only in selected regimes: IDH improves modestly, AGE and GLI/PED are often near or best at P2, and MCI shows the clearest empirical P7 gain. Cross-level MCI transfer further shows that much of the P7 advantage can be recovered by applying stronger preprocessing downstream, without requiring P7 throughout pretraining. These findings recast MRI preprocessing as a downstream-aware cost-utility decision rather than a default escalation pipeline. Code is available at https://github.com/PangJiangShuan/PreBrain.

中文摘要翻译: MRI预处理定义了脑MRI基础模型所看到的输入分布，然而它通常被视为常规数据清洗而非建模选择。本文探究对于自监督3D MRI预训练，多大程度的预处理值得其计算成本。在固定语料库、3D ViT骨干网络、掩码协议和下游评估的条件下，我们在20,000个异质性脑MRI体数据上比较了P0-P7共8个等级的预处理谱系，分别用于掩码自编码（MAE）和联合嵌入预测学习（JEPA），然后将编码器迁移到IDH预测、MCI分类、脑龄回归和GLI/PED肿瘤分割任务上。结果并不支持简单的“越多越好”规则。P0/P1数值不稳定，因此P2是成本最低的可行等级；超过P2后，选择最佳可行预处理等级相比P2，MAE的聚合效用仅提升3.4个百分点，JEPA仅提升1.8个百分点，且多数成对增益统计上不显著。强预处理仅在特定场景中有益：IDH任务有适度提升，AGE和GLI/PED任务通常在P2附近或表现最佳，而MCI任务在P7上展现出最清晰的实证增益。跨等级的MCI迁移实验进一步表明，P7的大部分优势可以通过在下游应用更强的预处理来恢复，无需在预训练全程使用P7。这些发现将MRI预处理重新定义为一种下游感知的成本-效用决策，而非默认的升级流水线。代码已开源：https://github.com/PangJiangShuan/PreBrain。

</details>


### [70] [Empowering Feed-Forward Reconstruction Models with Metric Scale via Satellite Images](https://arxiv.org/abs/2606.08205)
*Xianghui Ze,Yongjian Luo,Mengjun Chao,Zhenbo Song,Jianfeng Lu,Yujiao Shi*

Main category: cs.CV

TL;DR: 提出一种卫星引导的前馈3D重建框架，利用卫星影像作为全局度量参考，解决前馈3D重建中的尺度模糊问题，实现绝对尺度恢复、几何精化和度量相机位姿估计。

<details>
  <summary>Details</summary>

Motivation: 现有前馈3D重建模型虽泛化性强，但重建几何存在未知全局尺度模糊，限制了依赖度量理解的现实应用；而现有的度量重建方法依赖大规模度量标注或精确相机标定，成本高且不可靠。

Method: 给定粗略相机位姿，检索局部卫星图像块，通过双向交叉视图交互机制将卫星图像与前馈重建骨干网络集成，强制重建场景与卫星参考之间的一致性，从而推断绝对尺度、细化场景几何并估计度量坐标系下的相机位姿。

Result: 在KITTI、nuScenes和Oxford RobotCar数据集上，该方法在度量深度估计、多视角点云重建和跨视图相机定位方面均取得一致改进，同时保持跨数据集和地理区域的强泛化能力。

Conclusion: 卫星影像可作为可靠且易获取的全局度量参考，有效解决前馈3D重建的尺度模糊问题，为无需昂贵标注或精确标定的度量重建提供了新范式。

Original Abstract: Feed-forward 3D reconstruction models have recently shown strong generalization across diverse scenes, yet most of them recover geometry only up to an unknown global scale. This scale ambiguity limits their use in applications that require metric understanding of the environment. Existing metric reconstruction methods commonly rely on large-scale metric annotations or accurate camera calibration, both of which are costly or unreliable in many real-world settings. We propose a satellite-guided framework for resolving scale ambiguity in feed-forward 3D reconstruction. The key idea is to use readily available satellite imagery as a global metric reference. Given a coarse camera pose, our method retrieves a local satellite patch and integrates it with a feed-forward reconstruction backbone through bidirectional cross-view interaction. By enforcing consistency between the reconstructed scene and the satellite reference, the model infers absolute scale, refines scene geometry, and estimates camera pose in a metric coordinate frame. Experiments on KITTI, nuScenes, and Oxford RobotCar show consistent improvements in metric depth estimation, multi-view point-cloud reconstruction, and cross-view camera localization, while preserving strong generalization across datasets and geographic regions.

中文摘要翻译: 前馈式三维重建模型近年来在多样化场景中展现出强大的泛化能力，但大多数方法仅能恢复出存在未知全局尺度的几何结构。这种尺度模糊性限制了其在需要对环境进行度量理解的应用中的使用。现有的度量重建方法通常依赖大规模的度量标注或精确的相机标定，而在许多实际场景中，这两者成本高昂或不可靠。我们提出了一种卫星引导的框架，用于解决前馈式三维重建中的尺度模糊问题。核心思想是利用易于获取的卫星影像作为全局度量参考。给定一个粗略的相机位姿，我们的方法检索局部的卫星图像块，并通过双向交叉视图交互机制将其与前馈重建骨干网络集成。通过强制重建场景与卫星参考之间的一致性，模型能够推断绝对尺度、细化场景几何，并在度量坐标框架下估计相机位姿。在KITTI、nuScenes和Oxford RobotCar上的实验表明，该方法在度量深度估计、多视角点云重建和跨视图相机定位方面均取得了一致的改进，同时保持了跨数据集和地理区域的强泛化能力。

</details>


### [71] [SegmentAnyTreeV2: Scaling Transformer-Based Tree Instance Segmentation Across Sensors, Platforms, and Forests](https://arxiv.org/abs/2606.08206)
*Maciej Wielgosz,Stefano Puliti,Rasmus Astrup*

Main category: cs.CV

TL;DR: SegmentAnyTreeV2 是一个与传感器和平台无关的森林点云语义与实例分割框架，采用 Point Transformer v3 骨干网络、轻量语义头与树关注交叉注意力掩码头，结合实例感知查询初始化、一对多种子监督与非对称掩码评分，在包含427场景、26,496棵标注树的 FOR-instance v3 基准上达到90.5%精度、80.2%召回、85.0% F1、90.7%覆盖率和87.6%语义mIoU，并展现出强跨域零样本泛化能力。

<details>
  <summary>Details</summary>

Motivation: 现有森林点云分割方法在密集复杂林分的实例分离能力不足，且缺乏覆盖多种生物群落、森林结构和LiDAR平台的统一基准，限制了方法的泛化性评估。

Method: 采用序列化Point Transformer v3作为骨干网络，结合轻量语义头与树关注交叉注意力掩码解码器；语义预测将实例解码限制在树木类体素上；引入实例感知查询初始化、一对多种子监督和不对称掩码评分以提升密集复杂林分中的分离效果。

Result: 在FOR-instance v2测试集上达到90.5%精度、80.2%召回、85.0% F1、90.7%覆盖率和87.6%语义mIoU，超越此前学习方法；在独立站点上的零样本评估展示了强跨域泛化能力。

Conclusion: SegmentAnyTreeV2在森林点云实例分割上达到了当前最优性能，且具备良好的跨域泛化能力；FOR-instance v3基准为未来研究提供了更全面、多样化的评估平台。

Original Abstract: We present SegmentAnyTreeV2, a sensor- and platform-agnostic framework for semantic and instance segmentation of forest point clouds. The model combines a serialization-based Point Transformer v3 backbone with a lightweight semantic head and a tree-focused cross-attention mask decoder. Semantic predictions restrict instance decoding to tree-class voxels, while instance-aware query initialization, one-to-many seed supervision, and asymmetric mask scoring improve separation in dense and structurally complex stands. We further introduce FOR-instance v3, an expanded benchmark comprising 427 scenes and 26,496 annotated trees across diverse biomes, forest structures, and LiDAR platforms. On the FOR-instanceV2 test split, SegmentAnyTreeV2 achieves 90.5% precision, 80.2% recall, 85.0% F1, 90.7% coverage, and 87.6% semantic mIoU, outperforming previous learning-based methods in both instance detection and mask completeness. Zero-shot evaluation on independent sites further demonstrates strong cross-domain generalization.

中文摘要翻译: 我们提出了SegmentAnyTreeV2，一个与传感器和平台无关的森林点云语义与实例分割框架。该模型结合了基于序列化的Point Transformer v3骨干网络、轻量级语义头和一个面向树木的交叉注意力掩码解码器。语义预测将实例解码限制在树木类体素上，而实例感知查询初始化、一对多种子监督和非对称掩码评分改善了密集且结构复杂林分中的分离效果。我们进一步引入了FOR-instance v3，一个扩展的基准数据集，包含来自不同生物群落、森林结构和LiDAR平台的427个场景和26,496棵标注树木。在FOR-instance v2测试集上，SegmentAnyTreeV2达到了90.5%的精确率、80.2%的召回率、85.0%的F1分数、90.7%的覆盖率和87.6%的语义平均交并比，在实例检测和掩码完整性上均优于此前基于学习的方法。在独立站点上的零样本评估进一步展示了其强大的跨域泛化能力。

</details>


### [72] [Test-Time Scaling in Multimodal Foundation Models: A Comprehensive Survey of Generation and Reasoning](https://arxiv.org/abs/2606.08231)
*Cong Wan,Ying He,Zhongzhan Huang,Hefeng Wu*

Main category: cs.CV

TL;DR: 本文首次系统综述了多模态基础模型（MFMs）的测试时扩展（TTS）研究，提出了一个统一的分类框架，将现有方法归纳为基于采样、基于反馈和基于搜索三大策略，并总结了相关应用、基准、挑战与未来方向。

<details>
  <summary>Details</summary>

Motivation: 尽管测试时扩展（TTS）在多模态基础模型（MFMs）中取得了快速进展，但该领域缺乏系统性综述和统一的理论框架来描绘其发展全貌，本文旨在填补这一空白。

Method: 提出了一个统一的分类学框架，将多模态TTS的现有方法分为三类策略：基于采样（sampling-based）、基于反馈（feedback-based）和基于搜索（search-based）的方法。并在此基础上总结了代表性应用、常用基准，以及讨论了开放挑战与未来方向。

Result: 系统梳理并分类了多模态TTS的现有研究，归纳了三类核心策略及其在生成与推理任务中的典型应用和评估基准。

Conclusion: 本文为多模态基础模型的测试时扩展研究提供了系统化的路线图，明确了当前进展与未来研究方向，有助于推动该领域的进一步发展。

Original Abstract: Test-time Scaling (TTS) has emerged as a pivotal research direction for enhancing model performance by dynamically allocating computational resources during inference. Recent advancements have adapted this paradigm to Multimodal Foundation Models (MFMs), unlocking their potential in multimodal reasoning and generation. Despite rapid progress, the field lacks a systematic survey and unified theoretical framework to delineate the developmental landscape of multimodal TTS. To bridge this gap, we present the first comprehensive review of TTS research for MFMs, proposing a unified taxonomic framework that categorizes existing methodologies into three distinct strategies: sampling-based, feedback-based, and search-based approaches. We further summarize representative applications and benchmarks commonly utilized to evaluate multimodal TTS capabilities in generation and reasoning tasks. Finally, this survey discusses open challenges and outlines future research directions, providing a systematic roadmap for subsequent studies in this rapidly evolving field.

中文摘要翻译: 测试时扩展（Test-time Scaling, TTS）已成为一个关键研究方向，通过在推理过程中动态分配计算资源来提升模型性能。近期研究已将这一范式适配到多模态基础模型（Multimodal Foundation Models, MFMs），释放了其在多模态推理与生成方面的潜力。尽管进展迅速，该领域仍缺乏系统性综述和统一的理论框架来描绘多模态TTS的发展全貌。为填补这一空白，本文首次对MFMs的TTS研究进行了全面综述，提出了一个统一的分类学框架，将现有方法归纳为三种不同策略：基于采样的方法、基于反馈的方法和基于搜索的方法。本文进一步总结了在多模态TTS生成与推理任务中常用的代表性应用与基准。最后，本综述讨论了当前面临的开放性挑战，并勾勒了未来研究方向，为这一快速发展领域后续研究提供了系统化的路线图。

</details>


### [73] [Light-WAM: Efficient World Action Models with State-Fusion Action Decoding](https://arxiv.org/abs/2606.08242)
*Ziang Li,Dongzhou Cheng,Yibin Wang,Shiyue Wang,Xiaoyang Xu,Lingxuan Weng,Juan Wang,Jiaqi Wang*

Main category: cs.CV

TL;DR: Light-WAM 提出了一种轻量级世界动作模型，通过紧凑视频骨干网络、降维潜在空间视频监督和 StateFusionActionExpert 机制，在保持表征学习能力的同时，显著降低了训练成本与推理延迟，实现了高效的机器人操作闭环策略。

<details>
  <summary>Details</summary>

Motivation: 现有世界动作模型（WAM）依赖大规模生成式架构，训练成本高、推理延迟大，难以作为高效的闭环策略部署。

Method: 采用紧凑视频骨干网络构建模型；在降採样的潜在空间中进行未来视频监督，降低视频协同训练成本；提出 StateFusionActionExpert，从多层骨干网络中读取适应后的状态，通过学习查询池化进行融合，并在单次前向传播中直接预测动作块。

Result: 在 LIBERO 上保持强性能，在 RoboTwin 2.0 上实现可用的多任务性能；仅使用 0.44B 可训练参数；推理延迟 72.03ms，峰值 GPU 内存 4.1GiB，训练吞吐量得到提升。

Conclusion: Light-WAM 验证了通过轻量化设计可以在不牺牲性能的前提下大幅降低世界动作模型的计算开销，为机器人操作中高效闭环策略的部署提供了可行方案。

Original Abstract: World Action Models (WAMs) extend robot policy learning by incorporating future prediction as an additional training objective, encouraging the policy to encode task-relevant temporal structure in its representations. Current WAMs often rely on large-scale generative architectures that incur high training costs and inference latency, making them difficult to deploy as efficient closed-loop policies. We propose Light-WAM, a lightweight World Action Model for efficient robot manipulation. Specifically, it is built with a compact video backbone and performs future-video supervision in a downsampled latent space, reducing the cost of video co-training while retaining its benefits for representation learning. For action prediction, Light-WAM introduces the StateFusionActionExpert, which reads adapted states from multiple backbone layers, fuses them through learned-query pooling, and directly predicts action chunks in a single forward pass. This design provides an efficient interface between video backbone representations and robot actions, avoiding the need for heavy generative action experts. Experiments demonstrate that Light-WAM maintains strong performance on LIBERO and achieves usable multi-task performance on RoboTwin 2.0, while using only 0.44B trainable parameters. It also achieves 72.03ms inference latency with 4.1GiB peak GPU memory and improved training throughput.

中文摘要翻译: 世界动作模型（WAM）通过将未来预测作为额外的训练目标，扩展了机器人策略学习，鼓励策略在其表征中编码与任务相关的时间结构。当前的 WAM 通常依赖大规模生成式架构，导致训练成本高、推理延迟大，难以部署为高效的闭环策略。我们提出 Light-WAM，一种用于高效机器人操作的轻量级世界动作模型。具体来说，它采用紧凑的视频骨干网络构建，并在降採样的潜在空间中进行未来视频监督，从而在降低视频协同训练成本的同时保留其对表征学习的益处。在动作预测方面，Light-WAM 引入了 StateFusionActionExpert，该模块从多层骨干网络中读取适应后的状态，通过学习查询池化进行融合，并在单次前向传播中直接预测动作块。这种设计为视频骨干网络的表征与机器人动作之间提供了高效的接口，避免了使用繁重的生成式动作专家。实验表明，Light-WAM 在 LIBERO 上保持了强性能，在 RoboTwin 2.0 上实现了可用的多任务性能，同时仅使用了 0.44B 可训练参数。它还实现了 72.03ms 的推理延迟、4.1GiB 的峰值 GPU 内存占用以及更高的训练吞吐量。

</details>


### [74] [TIDE: Task-Isolated Diffusion for Unified Video Editing and Generation](https://arxiv.org/abs/2606.08260)
*Qi Liu,Gang Yue,Mingyu Yin,Lisai Zhang,Yidi Wu,Yaole Wang,Yaohui Wang,Chang Yao,Jingyuan Chen,Lin Ma*

Main category: cs.CV

TL;DR: TIDE是一个统一的视频生成与编辑框架，通过引入逐令牌任务嵌入、双路径条件机制和多任务渐进训练策略，在多个视频编辑和生成基准上取得最优性能。

<details>
  <summary>Details</summary>

Motivation: 当前视频生成与编辑能力由分离的、特定任务模型处理，缺乏统一框架；现有统一尝试要么需要专用辅助编码器，要么缺乏区分异构条件令牌的显式机制，当视觉条件的数量和类型跨任务变化时表现不佳。

Method: 1) 引入逐令牌任务嵌入（per-token task embeddings），为每个输入令牌分配任务特定标识符，显式区分目标、源和参考令牌；2) 设计双路径条件方案（dual-path conditioning），耦合视觉语言模型与VAE潜路径以获取互补信号；3) 提出多任务渐进训练策略，逐步引入复杂度递增的任务，协调多样目标并实现跨异构任务分布的平滑泛化。

Result: 在多个视频编辑和生成基准上的大量实验表明，TIDE在所有评估任务上均达到了最先进的性能。

Conclusion: TIDE成功构建了一个统一的框架，能够同时支持指令引导编辑、参考引导编辑和多参考生成等多种视频任务，并通过精心设计的机制有效解决了异构条件令牌区分和多样化目标协调的挑战。

Original Abstract: Recent advances in Diffusion Transformers have driven rapid progress in video generation and editing, yet these capabilities are still handled by separate, task-specific models. Building a unified framework that supports diverse video tasks remains an open challenge: existing unified attempts either require dedicated auxiliary encoders or lack explicit mechanisms to distinguish heterogeneous conditioning tokens, struggling when the number and type of visual conditions vary across tasks. We propose TIDE, a unified framework that integrates instruction-based editing, reference-guided editing, and multi-reference generation. At its core, we introduce per-token task embeddings that assign each input token a task-specific identifier, enabling the model to explicitly disambiguate target, source, and reference tokens. To simultaneously capture high-level semantic understanding and fine-grained structural fidelity, we design a dual-path conditioning scheme that couples a vision-language model with a VAE latent path for complementary signals. We further devise a multi-task progressive training strategy that incrementally introduces tasks of increasing complexity, effectively harmonizing diverse objectives and enabling smooth generalization across heterogeneous task distributions. Extensive experiments on multiple video editing and generation benchmarks demonstrate that TIDE achieves state-of-the-art performance across all evaluated tasks. Our project page is available at https://LittleWork123.github.io/tide.

中文摘要翻译: 近年来扩散Transformer的进展推动了视频生成和编辑的快速发展，但这些能力仍然由分离的、特定任务模型处理。构建一个支持多样化视频任务的统一框架仍然是一个开放挑战：现有的统一尝试要么需要专用的辅助编码器，要么缺乏区分异构条件令牌的显式机制，在视觉条件的数量和类型跨任务变化时表现不佳。我们提出TIDE，一个统一的框架，集成了指令引导编辑、参考引导编辑和多参考生成。其核心是，我们引入了逐令牌任务嵌入，为每个输入令牌分配任务特定标识符，使模型能够显式区分目标、源和参考令牌。为了同时捕捉高层语义理解和细粒度结构保真度，我们设计了一种双路径条件方案，耦合视觉语言模型与VAE潜路径以获取互补信号。我们进一步设计了一种多任务渐进训练策略，逐步引入复杂度递增的任务，有效协调多样化的目标，并实现跨异构任务分布的平滑泛化。在多个视频编辑和生成基准上的大量实验表明，TIDE在所有评估任务上均达到了最先进的性能。我们的项目页面可在https://LittleWork123.github.io/tide获取。

</details>


### [75] [Remember with Confidence: Uncertainty Quantification for Spatio-temporal Memory with Probabilistic Guarantees](https://arxiv.org/abs/2606.08277)
*Harry Zhang,Nicolas Gorlo,Luca Carlone*

Main category: cs.CV

TL;DR: 本文提出目标级语义不确定性量化方法（UQ-DAAAM），用于检测和修复多视角VLM记忆中的不可靠描述，通过主动选择高质量视角并融合多视角描述来降低不确定性，实验表明该方法在4D空间-时间记忆中显著提升了可靠性和问答性能。

<details>
  <summary>Details</summary>

Motivation: 现有的场景图与检索增强系统将VLM描述持久化为3D实体语义，但VLM生成的描述存在噪声且视角不一致，现有方法缺乏检测不可靠存储描述的机制，导致记忆系统不可靠。

Method: 提出目标级语义不确定性分数，量化多视角VLM记忆中的跨视角语义散度并识别语义未解决的目标；在此基础上构建UQ-DAAAM记忆系统，在固定查询预算下主动选择高质量视角并对不确定目标进行精炼，融合多视角描述为单一目标描述。

Result: 在OC-NaVQA基准上，UQ-DAAAM相比基线实现了大幅更高的不确定性降低和更好的时空问答性能，并提供了概率论保证证明该方法选择的高质量视角更可能降低不确定性。

Conclusion: 不确定性量化可以使具身4D记忆系统更加可靠和有效，通过主动检测和修复不可靠的VLM描述，提升了长期机器人操作中的空间-时间记忆能力。

Original Abstract: Long-horizon robot operation requires spatio-temporal memory to record the environment state and recall it for downstream reasoning. Scene graphs and retrieval-augmented systems ground VLM descriptions to persistent 3D entities with rich semantic descriptions. However, VLM captions are noisy and viewpoint-inconsistent, and existing systems treat them as an oracle with no mechanism to detect unreliable stored descriptions. We introduce object-level semantic uncertainty for multi-view VLM memory: a score that measures object-centric cross-view semantic scatter of captions and identifies semantically unresolved objects. Then, we include our uncertainty scores in an advanced spatial-semantic memory system, that we dub UQ-DAAAM. UQ-DAAAM uses this score to actively refine uncertain objects under a fixed query budget by selecting high-quality views and fusing the resulting multi-view captions into a single object description. We also derive probabilistic guarantees showing that higher-quality candidate views (as selected by our approach) are more likely to reduce uncertainty. Our experiments show that uncertainty quantification can make embodied 4D memory systems more reliable and more effective. In particular, on the OC-NaVQA benchmark, UQ-DAAAM achieves substantially larger uncertainty reduction and better spatio-temporal question answering performance than baselines.

中文摘要翻译: 长期机器人操作需要时空记忆来记录环境状态并在下游推理中调用。场景图和检索增强系统将VLM描述持久化到带有丰富语义描述的3D实体上。然而，VLM描述存在噪声且视角不一致，现有系统将其视为完美先验，缺乏检测不可靠存储描述的机制。我们提出了面向多视角VLM记忆的目标级语义不确定性：一种衡量以目标为中心的跨视角语义散度并识别语义未解决目标的分数。随后，我们将不确定性分数纳入名为UQ-DAAAM的高级空间-语义记忆系统中。UQ-DAAAM使用该分数在固定查询预算下主动精炼不确定的目标，通过选择高质量视角并将多视角描述融合为单一目标描述。我们还推导了概率论保证，表明由我们方法选择的高质量候选视角更可能降低不确定性。实验表明，不确定性量化可以使具身4D记忆系统更加可靠和有效。特别是在OC-NaVQA基准上，UQ-DAAAM相比基线实现了大幅更高的不确定性降低和更好的时空问答性能。

</details>


### [76] [G2G: Exploiting Intra-Group Geometry for Inter-Group Pose Estimation](https://arxiv.org/abs/2606.08284)
*Yufei Wei,Shuhao Ye,Chenxiao Hu,Yiyuan Pan,Dongyu Feng,Rong Xiong,Yue Wang,Yanmei Jiao*

Main category: cs.CV

TL;DR: 本文提出了一种基于冻结预训练多视图基础模型的轻量化6-DoF相对位姿估计方法，通过仅添加约3%可训练参数（32M）的跨组桥接模块，在跨序列重定位和多相机里程计任务上达到SOTA性能。

<details>
  <summary>Details</summary>

Motivation: 现有模型将多视图视为无结构集合，缺乏跨组（cross-group）的显式推理机制；同时希望在不重新训练大规模预训练模型的前提下，高效利用其强大的几何特征表达能力。

Method: 保持预训练多视图基础模型完全冻结，仅添加三个轻量可训练模块：(1) Perceiver Resampler，(2) 带合并自注意力的跨组桥接模块（Cross-Group Bridge），(3) 多帧位姿头（Multi-frame Pose Head）。总可训练参数约32M（不到完整模型的6%），仅需相对位姿作为监督信号。

Result: 在四个数据集（室内/室外仿真、真实跨季节数据、零样本sim-to-real迁移）上，在跨序列重定位和多相机里程计两个任务中均达到SOTA精度，且所有基线均使用完整原始监督信号重新训练。

Conclusion: 通过轻量级可训练模块即可有效桥接两组图像间的几何信息，在冻结大模型的情况下实现精准的相对位姿估计，为高效利用预训练视觉基础模型提供了新范式。

Original Abstract: Recovering the relative 6-DoF pose between two image groups underlies cross-sequence relocalization and multi-camera rig odometry. Each group carries known intra-group geometry from visual odometry or rig calibration, and pretrained multi-view backbones already fuse such geometry into visual features. Yet current models treat all views as an unstructured set, leaving cross-group reasoning as the missing piece. We introduce \ours{}, which keeps the foundation model entirely frozen and adds three lightweight trainable modules to bridge the two groups: a perceiver resampler, a cross-group bridge with merged self-attention, and a multi-frame pose head. The trainable footprint totals about 32M parameters, under 6\% of the full model, and is supervised only by relative poses. Across four datasets that span indoor and outdoor simulation, real-world cross-season capture, and zero-shot sim-to-real transfer, \ours{} attains state-of-the-art accuracy on both tasks, while every baseline is retrained with its full original supervision. Code is available at https://github.com/WeiYuFei0217/G2G.

中文摘要翻译: 恢复两组图像之间的相对6-DoF位姿是跨序列重定位和多相机里程计的核心问题。每组图像通过视觉里程计或相机标定已知组内几何结构，且预训练的多视图基础模型已能将此类几何信息融入视觉特征中。然而，现有模型将所有视图视为无结构集合，缺乏跨组推理的关键能力。本文提出\ours{}，该方法保持基础模型完全冻结，仅添加三个轻量级可训练模块来桥接两组图像：感知器重采样器（Perceiver Resampler）、带合并自注意力的跨组桥接模块（Cross-Group Bridge）以及多帧位姿头（Multi-frame Pose Head）。可训练参数量总计约32M，不到完整模型的6%，且仅需相对位姿作为监督信号。在四个数据集（涵盖室内/室外仿真、真实跨季节数据以及零样本sim-to-real迁移）上，\ours{}在两个任务中均达到了最先进的精度，而所有基线方法均使用完整的原始监督信号重新训练。代码已开源：https://github.com/WeiYuFei0217/G2G。

</details>


### [77] [HACK++: Towards More Effective Head-Aware Key-Value Compression for Efficient Visual Autoregressive Modeling](https://arxiv.org/abs/2606.08302)
*Ziran Qin,Yuchen Jiang,Mingbao Lin,Youru Lv,Hang Guo,Wen Fei,Weiyao Lin*

Main category: cs.CV

TL;DR: 本文提出HACK++，一种针对视觉自回归（VAR）模型的无训练、头部感知的键值缓存压缩框架。通过将注意力头分类为上下文头和结构头，并利用头部特定的先验知识和自适应缓存预算分配，HACK++在极低的注意力和缓存预算下实现了近乎无损的图像生成性能。

<details>
  <summary>Details</summary>

Motivation: 现有VAR模型因跨尺度的键值（KV）缓存累积，导致注意力复杂度过高和严重的内存开销。已有的“一刀切”压缩方法在VAR模型上表现不佳，因为VAR注意力头可稳定分为两类功能不同的头（上下文头和结构头），且它们对历史尺度的依赖存在差异，需要自适应缓存预算分配。

Method: HACK++方法包括：1）通过一次性离线校准分类注意力头类型并导出头部特定的先验知识；2）在推理时，将注意力与缓存压缩解耦，在独立预算下运作；3）通过模式特定的策略和依赖感知的预算分配，限制当前尺度的注意力成本，同时更激进地压缩累积的缓存。

Result: 在多个VAR模型（包括文本到图像、类别条件和统一理解与生成任务）上的实验表明：在Infinity-2B/8B上，HACK++仅使用30%注意力预算和10%缓存预算即可维持近乎无损的生成质量，甚至在1%缓存预算下依然鲁棒。

Conclusion: HACK++有效解决了VAR模型中的KV缓存压缩难题，验证了头部感知压缩策略在下一尺度预测范式中的有效性和泛化性。

Original Abstract: Visual Autoregressive (VAR) models adopt a next-scale prediction paradigm, offering high-quality generation with substantially fewer decoding steps. However, existing VAR models suffer from significant attention complexity and severe memory overhead due to the accumulation of key-value (KV) caches across scales. In this paper, we tackle this challenge by introducing KV cache compression into the next-scale paradigm. We begin with an in-depth analysis of VAR attention and observe that attention heads can be stably divided into two functionally distinct categories: Contextual Heads focus on maintaining semantic consistency, while Structural Heads preserve spatial coherence. Their functional divergence makes existing one-size-fits-all compression methods perform poorly on VAR models. We further find that the two head types differ markedly in their reliance on historical scales, and that this reliance shifts across layers and generation steps, arguing for an adaptive cache budget allocation. To address these challenges, we propose HACK++, a training-free Head-Aware key-value Compression frameworK for VAR models. From a one-time offline calibration, HACK++ classifies head types and derives head-specific priors. At inference, it decouples attention from cache compression under independent budgets, bounding the current-scale attention cost while compressing the accumulated cache far more aggressively, via pattern-specific strategies and a reliance-aware budget allocation. Extensive experiments on multiple VAR models across text-to-image, class-conditional, and unified understanding-and-generation tasks validate the effectiveness and generalizability of HACK++. For example, on Infinity-2B/8B, HACK++ maintains near-lossless generation with only a 30% attention budget and a 10% cache budget, and remains robust even under a 1% cache budget.

中文摘要翻译: 视觉自回归（VAR）模型采用下一尺度预测范式，能以显著更少的解码步骤实现高质量生成。然而，现有VAR模型因跨尺度的键值（KV）缓存累积，面临着严重的注意力复杂度和内存开销问题。本文通过将KV缓存压缩引入下一尺度范式来应对这一挑战。我们首先对VAR注意力进行深入分析，观察到注意力头可稳定分为两类功能不同的类别：上下文头聚焦于保持语义一致性，而结构头则维持空间连贯性。它们的功能差异使得现有“一刀切”压缩方法在VAR模型上表现不佳。我们进一步发现，两类注意力头对历史尺度的依赖存在显著差异，且这种依赖随层和生成步骤而变化，这要求自适应的缓存预算分配。为应对这些挑战，我们提出HACK++，一种针对VAR模型的无训练、头部感知的键值缓存压缩框架。通过一次性离线校准，HACK++分类头类型并导出头部特定的先验知识。在推理时，它将注意力与缓存压缩在独立预算下解耦，通过模式特定的策略和依赖感知的预算分配，在限制当前尺度注意力成本的同时更激进地压缩累积缓存。在多个VAR模型上进行的文本到图像、类别条件以及统一理解与生成任务的广泛实验，验证了HACK++的有效性和泛化性。例如，在Infinity-2B/8B上，HACK++仅使用30%注意力预算和10%缓存预算即可维持近乎无损的生成，即使在1%缓存预算下依然保持鲁棒。

</details>


### [78] [Set-Based Transformer for Atmospheric Compensation in Standoff LWIR Hyperspectral Imaging](https://arxiv.org/abs/2606.08324)
*Fabian Perez,Nicolas Quintero,Jeferson Acevedo,Hoover Rueda-Chacon*

Main category: cs.CV

TL;DR: 本文提出一种轻量级基于集合的深度学习框架，以不同距离下的多次辐射测量为输入，联合估计大气透过率、路径辐射和共同的下行辐射光谱，解决了被动长波红外高光谱成像中大气补偿的难题。

<details>
  <summary>Details</summary>

Motivation: 被动长波红外高光谱成像受大气吸收、发射和反射辐射影响，大气补偿对获取目标信息至关重要，但因实际和建模困难而被长期忽视。

Method: 提出一种轻量级基于集合的深度学习框架，以不同距离下的多次辐射测量数据为输入，联合估计透过率、大气路径辐射和共享下行辐射光谱；并利用稀疏自编码器分析学习到的表征。

Result: 在MODTRAN生成的数据集上，所有估计产品均表现出低光谱失真；稀疏自编码器分析发现，若干潜在特征在没有位置监督的情况下，对测试数据中地理上一致的子集产生激活。

Conclusion: 本文提出的方法有效解决了被动LWIR高光谱成像中的大气补偿问题，且学习到的表征具有地理一致性，数据集和代码已公开。

Original Abstract: Passive long-wave infrared (LWIR) hyperspectral imaging under a standoff geometry depends on atmospheric absorption and emission, as well as reflected radiance, thus making atmospheric compensation essential to get knowledge of a target of interest. Despite its importance, this compensation has been largely overlooked due to its practical and modeling difficulty. In this paper, we present a lightweight set-based deep learning framework that takes multiple radiance measurements, collected at different standoff ranges, as input and jointly estimates transmittance, atmospheric path radiance, and a shared downwelling spectrum. We analyze the learned representation with a sparse autoencoder and observe that several latent features do activate on geographically coherent subsets of the test data despite the absence of location supervision. Experiments on a MODTRAN generated standoff LWIR dataset demonstrate low spectral distortion across all estimated products. The dataset and code is publicly available at: https://factral.co/SAE-LWIR/

中文摘要翻译: 被动长波红外（LWIR）高光谱成像在远距离几何下依赖于大气吸收、发射以及反射辐射，因此大气补偿对于获取目标信息至关重要。尽管其重要性显著，但由于实际和建模上的困难，这一补偿在很大程度上被忽视了。本文提出一种轻量级基于集合的深度学习框架，以不同距离下采集的多次辐射测量数据作为输入，联合估计大气透过率、大气路径辐射以及共享的下行辐射光谱。我们利用稀疏自编码器分析学习到的表征，观察到若干潜在特征在没有位置监督的情况下，对测试数据中地理上一致的子集产生了激活。在MODTRAN生成的远距离LWIR数据集上的实验表明，所有估计产品均具有低光谱失真。数据集和代码已在 https://factral.co/SAE-LWIR/ 公开。

</details>


### [79] [SMI: Efficient Self-Supervised Learning via Mutual-Information-Inspired Dependency Optimization](https://arxiv.org/abs/2606.08332)
*Pritam Mishra,Coloma Ballester,Dimosthenis Karatzas*

Main category: cs.CV

TL;DR: 提出语义互信息（SMI）轻量级自监督学习目标函数，通过非线性变换的样本级依赖矩阵优化，在降低计算复杂度的同时取得了与SOTA方法竞争的性能，并在多个低资源基准上提升迁移表现。

<details>
  <summary>Details</summary>

Motivation: 现有自监督学习方法（如依赖大batch size、记忆库、动量编码器或全局同步机制）计算成本高、训练复杂度大，需要一种更轻量高效的自监督学习目标。

Method: 在高斯假设下基于互信息启发提出语义互信息（SMI）目标函数，通过对样本级成对相关性进行非线性变换构建依赖矩阵并优化，而非在高层特征相关矩阵上操作。

Result: 在ImageNet上用ResNet-50达到与SOTA自监督方法竞争的线性评估性能，显著降低计算复杂度；在多个低资源基准上一致提升迁移性能（尤其细粒度数据集）；优化动态和表示几何分析显示更好的对齐-冗余平衡、更大的特征多样性和更空间局域的语义表示。

Conclusion: 非线性依赖优化为基于相关性的自监督学习目标提供了一种有效且计算高效的替代方案。

Original Abstract: Self-supervised learning (SSL) has achieved remarkable representation learning performance, but many existing methods rely on large batch sizes, memory banks, momentum encoders, or global synchronization mechanisms that substantially increase computational cost and training complexity. In this work, we propose Semantic Mutual Information (SMI), a lightweight self-supervised objective derived from a mutual-information-inspired dependency formulation under Gaussian assumptions. Unlike conventional correlation matching objectives that operate on high-dimensional feature correlation matrices, SMI performs optimization on a sample-level dependency matrix through a nonlinear transformation of pairwise correlations. This formulation induces distinct optimization dynamics that emphasize strongly dependent semantic pairs while maintaining representation diversity. Experimental results on ImageNet using a ResNet-50 backbone demonstrate that SMI achieves competitive linear evaluation performance relative to state-of-the-art SSL approaches while substantially reducing computational complexity. Across multiple low-resource benchmarks, SMI consistently improves transfer performance over Barlow Twins, particularly on fine-grained datasets. Furthermore, analyses of optimization dynamics and representation geometry suggest improved alignment--redundancy balance, greater feature diversity, and more spatially localized semantic representations. These results indicate that nonlinear dependency optimization provides an effective and computationally efficient alternative to conventional correlation-based self-supervised learning objectives.

中文摘要翻译: 自监督学习在表示学习方面取得了显著性能，但许多现有方法依赖于大批量大小、存储库、动量编码器或全局同步机制，这显著增加了计算成本和训练复杂度。本文提出语义互信息（Semantic Mutual Information, SMI），一种在高斯假设下基于互信息启发的依赖公式推导出的轻量级自监督目标。与在高层特征相关矩阵上操作的传统相关匹配目标不同，SMI通过成对相关性的非线性变换在样本级依赖矩阵上执行优化。该公式引入了独特的优化动态，在保持表示多样性的同时强调强依赖的语义对。使用ResNet-50骨干在ImageNet上的实验结果表明，SMI在显著降低计算复杂度的同时，取得了与最先进自监督方法相竞争的性能。在多个低资源基准上，SMI相较于Barlow Twins持续提升迁移性能，尤其是在细粒度数据集上。此外，对优化动态和表示几何的分析表明，SMI实现了更好的对齐-冗余平衡、更大的特征多样性以及更空间局域的语义表示。这些结果表明，非线性依赖优化为基于相关性的自监督学习目标提供了一种有效且计算高效的替代方案。

</details>


### [80] [Beyond Raw Signals: Undecoded Generative Latents as Privileged Synthetic Data](https://arxiv.org/abs/2606.08336)
*Cristian Sbrolli,Nicolas Michel,Matteo Matteucci,Toshihiko Yamasaki*

Main category: cs.CV

TL;DR: 提出直接潜在空间增强（DLA）和多层显式模拟联觉（MESSy），利用未解码的生成式潜在特征作为特权信息，避免多模态集成中的“解码-编码循环”低效问题，训练出具有“联觉”潜在结构的纯视觉学生模型。

<details>
  <summary>Details</summary>

Motivation: 多模态集成虽能提升视觉模型性能，但推理成本高昂且需要稀缺的完美配对数据集。现有方法通过生成式AI合成缺失模态，却引入了“解码-编码循环”的低效问题——信息丰富的生成式潜在特征被解码为含噪原始信号，迫使下游分类器浪费容量重新编码。

Method: 提出直接潜在空间增强（DLA），直接使用未解码的生成式潜在特征作为特权信息；引入多层显式模拟联觉（MESSy），采用预测目标而非刚性的表示匹配，让学生模型安全地内化物理先验知识。

Result: 实验表明，该框架显著优于原始数据增强和传统蒸馏方法，训练出的单模态学生模型具有与从未直接观测过的物理属性内在对齐的“联觉”潜在结构。

Conclusion: 通过避免解码-编码循环并采用预测式知识迁移，该方法能够高效训练出高精度的单模态学生模型，使其潜在结构隐式对齐多模态物理先验。

Original Abstract: While multimodal integration significantly improves computer vision models, deploying them incurs prohibitive inference costs and requires scarce, perfectly paired datasets. Recent methods address this data bottleneck by synthesizing missing modalities via generative AI, yet they introduce a severe inefficiency: the Decode-Encode Loop. Specifically, information-rich generative latents are decoded into noisy raw signals, forcing the downstream classifier to waste capacity re-encoding them. To bypass this bottleneck, we propose Direct Latent Augmentation (DLA), utilizing undecoded generative latents directly as privileged information. Furthermore, to transfer this dense knowledge to a purely visual student, we introduce Multilayer Explicit Simulated Synesthesia (MESSy). Instead of enforcing rigid representation matching, which forces the student to distort its native visual features to accommodate complex multimodal topologies, MESSy uses a predictive objective to safely internalize these physical priors. Empirical results demonstrate that our framework significantly outperforms raw data augmentation and traditional distillation. Ultimately, our approach yields highly accurate unimodal students with ``synesthetic'' latent structures that are inherently aligned with physical properties they have never directly observed.

中文摘要翻译: 尽管多模态集成显著提升了计算机视觉模型的性能，但其部署成本高昂，且需要稀缺的完美配对数据集。近期方法通过生成式AI合成缺失模态来应对这一数据瓶颈，但它们引入了一个严重的低效问题：解码-编码循环。具体而言，信息丰富的生成式潜在特征被解码为含噪的原始信号，迫使下游分类器浪费容量重新编码。为绕过这一瓶颈，我们提出了直接潜在空间增强（DLA），直接利用未解码的生成式潜在特征作为特权信息。此外，为了将这种密集知识迁移给纯视觉学生模型，我们引入了多层显式模拟联觉（MESSy）。MESSy不是强制执行刚性的表示匹配（这会使学生模型扭曲其原生视觉特征以适应复杂的多模态拓扑结构），而是使用预测目标来安全地内化这些物理先验。实验结果表明，我们的框架显著优于原始数据增强和传统蒸馏方法。最终，我们的方法训练出了高度准确且具有“联觉”潜在结构的单模态学生模型，这些结构与它们从未直接观测到的物理属性内在地对齐。

</details>


### [81] [Self-Supervised Vision Transformers for CBCT-Based Detection of Temporomandibular Joint Osteoarthritis](https://arxiv.org/abs/2606.08364)
*Shradhdha Trivedi,Vrundan Sojitra,Mariela Padilla*

Main category: cs.CV

TL;DR: 本文研究DINO系列自监督视觉Transformer（DINOv1、DINOv2、DINOv2+reg、RAD-DINO）迁移至CBCT进行颞下颌关节骨关节炎自动检测的效果，提出基于轴向切片的ViT骨干+MIL聚合流水线，发现部分解冻最后两个Transformer块是决定性因素，将AUC从0.671提升至0.902，表明适应策略比骨干选择更重要。

<details>
  <summary>Details</summary>

Motivation: 颞下颌关节骨关节炎（TMJ OA）在CBCT上的骨性变化往往很细微，使得自动检测面临挑战。现有研究表明DINO系列自监督ViT在自然图像和放射学领域表现出色，但其在CBCT上的迁移效果及所需骨干适应策略尚不明确，因此本文旨在探索不同DINO变体的迁移性能以及骨干适应方式的影响。

Method: 提出基于轴向切片的流水线：使用ViT骨干对每个CBCT轴向切片进行编码（冻结或部分解冻），然后通过基于注意力的多实例学习（MIL）聚合切片级特征，进行患者级二分类（OA/正常）。在多个来源的CBCT数据集上系统消融不同解冻策略和聚合设计。

Result: 部分解冻最后两个Transformer块是提升性能的决定性因素，将AUC从完全冻结DINOv2的0.671提升至0.902；该结果优于DINOv1（0.867）、DINOv2+reg（0.774）和监督ImageNet ViT-B/16基线（0.843）。

Conclusion: 在低数据医学影像场景下，适应策略（部分解冻策略）比骨干选择本身更能驱动性能提升，为DINO系列基础模型迁移至CBCT提供了实用指导。

Original Abstract: Temporomandibular joint osteoarthritis (TMJ OA) is a prevalent degenerative condition whose osseous changes are often subtle on cone-beam CT (CBCT), making automated detection challenging. We study how well the DINO family of self-supervised vision transformers -- DINOv1, DINOv2, DINOv2+reg, and RAD-DINO (a radiology-pretrained variant) -- transfers to CBCT, asking how much backbone adaptation is needed and of what kind. We propose a simple slice-based pipeline using Vision Transformer (ViT) backbones: axial CBCT slices are encoded per-slice by a frozen or partially adapted ViT and aggregated via attention-based multiple instance learning (MIL) for patient-level binary OA/Normal classification. Through systematic ablation across unfreezing strategies and aggregation designs on a multi-source CBCT dataset, we find that partial unfreezing of the final two transformer blocks is the decisive factor, improving AUC from 0.671 (fully frozen DINOv2) to 0.902. This outperforms DINOv1 (0.867), DINOv2+reg (0.774), and a supervised ImageNet ViT-B/16 baseline (0.843). Our results provide practical guidance for adapting DINO-family foundation models in low-data medical imaging settings, showing that adaptation strategy is a stronger driver of performance than backbone choice alone.

中文摘要翻译: 颞下颌关节骨关节炎（TMJ OA）是一种常见的退行性疾病，其骨性变化在锥形束CT（CBCT）上往往很细微，使得自动化检测具有挑战性。我们研究了DINO自监督视觉Transformer系列——DINOv1、DINOv2、DINOv2+reg以及RAD-DINO（一种放射学预训练变体）——向CBCT迁移的效果，探究需要多少以及何种类型的骨干适应。我们提出了一种简单的基于切片的流水线，使用Vision Transformer（ViT）骨干：轴向CBCT切片由冻结或部分适应的ViT逐切片编码，并通过基于注意力的多实例学习（MIL）聚合，进行患者级二分类（OA/正常）。通过在多个来源的CBCT数据集上对不同解冻策略和聚合设计进行系统消融，我们发现部分解冻最后两个Transformer块是决定性因素，将AUC从0.671（完全冻结的DINOv2）提升至0.902。这优于DINOv1（0.867）、DINOv2+reg（0.774）以及有监督的ImageNet ViT-B/16基线（0.843）。我们的结果为在低数据医学影像场景中适应DINO系列基础模型提供了实用指导，表明适应策略比骨干选择本身对性能的驱动作用更强。

</details>


### [82] [SceneConductor: 3D Scene Generation from Single Image with Multi-Agent Orchestration](https://arxiv.org/abs/2606.08402)
*Jeonghwan Kim,Yushi Lan,Yongwei Chen,Hieu Trung Nguyen,Chuanyu Pan,Xingang Pan*

Main category: cs.CV

TL;DR: 提出一种多智能体编排框架，将单图像3D场景生成分解为场景初始化、环境构建和多智能体精化三个阶段，并引入几何感知的布局预测器以降低对场景级标注的依赖，在几何精度、空间一致性和感知真实性上优于先前方法。

<details>
  <summary>Details</summary>

Motivation: 现有方法依赖整体或弱解耦的流水线，纠缠多种因素且需要大量场景级监督，限制了其在复杂真实环境中的泛化能力。因此需要一种更具结构化的分解方式来实现鲁棒的单图像3D场景生成。

Method: 提出多智能体编排框架，包含三个阶段：(1) 场景初始化：提取图像级对象掩码，构建对象级3D表示，预测初始空间布局；(2) 环境构建：结合初始化和点图几何，构建支撑面、房间边界、材质和光照等环境骨架；(3) 多智能体精化：规划器智能体识别结构/视觉不一致，直接进行简单修正，并派发专业智能体处理复杂局部修正后重新整合。此外引入由点图稀疏几何先验监督的几何感知布局预测器。

Result: 在基准数据集上的大量实验表明，该方法在几何精度、空间一致性和感知真实性方面持续优于先前方法。布局预测器可从分割级数据训练，并鲁棒地泛化到多样化的真实场景。

Conclusion: 通过多智能体编排的结构化分解和几何感知布局预测器的引入，有效降低了场景级标注需求，同时提升了生成场景的几何精度、空间一致性和真实感，在复杂真实环境中具有更好的泛化能力。

Original Abstract: Generating complete 3D scenes from a single image requires inferring globally consistent geometry, object relationships, and environmental context from inherently ambiguous visual evidence. Despite recent progress in joint layout-and-mesh generation, existing methods often rely on holistic or weakly decomposed pipelines that entangle many factors at once and demand extensive scene-level supervision, limiting their generalization to complex real-world environments. We propose a multi-agent orchestration framework that decomposes single-image 3D scene generation into three structured stages: scene initialization, environment construction, and multi-agent refinement. The initialization stage extracts image-derived object masks, builds object-level 3D representations, and predicts an initial spatial layout to form a coarse 3D scene. The environment-construction stage then leverages this initialization together with point-map geometry to build an environmental scaffold of supporting surfaces, room boundaries, materials, and illumination. Finally, in the refinement stage, a planner agent identifies structural and visual inconsistencies, applies simple corrections directly, and dispatches specialist agents for complex localized revisions that are reintegrated into the global scene. To provide reliable structural initialization while reducing reliance on scene-level annotations, we further introduce a geometry-aware layout predictor supervised by sparse geometric priors derived from point maps. Unlike fully supervised layout generators, the predictor can be trained from segmentation-level data and generalizes robustly to diverse real-world scenes. Extensive experiments on benchmark datasets show that our method consistently outperforms prior approaches in geometric accuracy, spatial consistency, and perceptual realism.

中文摘要翻译: 从单张图像生成完整3D场景需要从本质上模糊的视觉证据中推断全局一致的几何结构、物体关系和环境上下文。尽管在联合布局与网格生成方面取得了近期进展，现有方法通常依赖整体或弱解耦的流水线，纠缠了多种因素并需要大量场景级监督，限制了其在复杂真实环境中的泛化能力。我们提出一种多智能体编排框架，将单图像3D场景生成分解为三个结构化阶段：场景初始化、环境构建和多智能体精化。初始化阶段提取图像导出的对象掩码，构建对象级3D表示，并预测初始空间布局以形成粗略3D场景。环境构建阶段则利用该初始化结果结合点图几何，构建包含支撑面、房间边界、材质和光照的环境骨架。最后，在精化阶段，规划器智能体识别结构和视觉不一致性，直接进行简单修正，并派发专业智能体处理复杂的局部修订，随后将结果重新整合到全局场景中。为了在减少场景级标注依赖的同时提供可靠的结构初始化，我们进一步引入了一种几何感知布局预测器，该预测器由从点图导出的稀疏几何先验进行监督。与全监督的布局生成器不同，该预测器可从分割级数据训练，并鲁棒地泛化到多样化的真实场景。在基准数据集上的大量实验表明，我们的方法在几何精度、空间一致性和感知真实性方面持续优于先前方法。

</details>


### [83] [Geometry-Driven Flow Analysis of Brain Sulcal Pattern](https://arxiv.org/abs/2606.08404)
*Moo K. Chung,Luigi Maccotta,Aaron Struck*

Main category: cs.CV

TL;DR: 提出一种基于泊松方程的皮层折叠建模框架，通过平均曲率驱动的几何流表征脑沟回组织，用于检测青少年肌阵挛癫痫（JME）中细微的皮层异常。

<details>
  <summary>Details</summary>

Motivation: 现有皮层折叠分析大多依赖间接标量摘要，未显式建模折叠几何本身；JME患者的皮层异常微妙、空间分布广泛，传统形态学测量难以检测。

Method: 基于泊松方程的框架，将皮层折叠建模为源于皮层流形上平均曲率的几何驱动流；将折叠模式视为稳态源-汇结构，生成平滑的全局平衡势场，其表面梯度定义物理可解释的通量。

Result: 该方法能实现脑沟回折叠组织的空间连贯分析，为JME中几何驱动的皮层结构提供有原则的表征。

Conclusion: 该框架为皮层折叠分析提供了一种几何显式建模的新途径，可有效检测JME等神经疾病中传统方法难以捕捉的细微皮层结构异常。

Original Abstract: Cortical folding reflects coordinated neurodevelopmental processes and is increasingly recognized as a sensitive marker of neurological disease. However, most existing analyses rely on indirect scalar summaries that do not explicitly model folding geometry itself. In juvenile myoclonic epilepsy (JME), a common genetic epilepsy, cortical abnormalities are often subtle, spatially distributed, and difficult to detect using conventional morphometric measures. We introduce a Poisson-equation-based framework that models cortical folding as a geometry-driven flow derived from mean curvature on the cortical manifold. By treating folding patterns as a stationary source-sink structure, the proposed approach yields a smooth, globally balanced potential field whose surface gradient defines a physically interpretable flux. This framework enables spatially coherent analysis of sulcal-gyral folding organization and provides a principled representation of geometry-driven cortical structure in JME.

中文摘要翻译: 皮层折叠反映了协调的神经发育过程，并日益被认为是神经系统疾病的敏感标记。然而，大多数现有分析依赖于间接的标量摘要，并未显式建模折叠几何本身。在青少年肌阵挛癫痫（JME）——一种常见的遗传性癫痫——中，皮层异常通常是细微的、空间分布广泛的，且难以通过传统形态学测量检测。我们引入了一种基于泊松方程的框架，将皮层折叠建模为源于皮层流形上平均曲率的几何驱动流。通过将折叠模式视为稳态源-汇结构，所提出的方法生成了一个平滑的全局平衡势场，其表面梯度定义了物理可解释的通量。该框架能够实现脑沟回折叠组织的空间连贯分析，并为JME中几何驱动的皮层结构提供有原则的表征。

</details>


### [84] [CoVEBench: Can Video Editing Models Handle Complex Instructions?](https://arxiv.org/abs/2606.08415)
*Jiangtao Wu,Jiaming Wang,Yiwen He,Yuanxing Zhang,Shihao Li,Dunyuan Liu,Xuedong Zhao,Jialu Chen,Zekun Moore Wang,Jiaheng Liu*

Main category: cs.CV

TL;DR: 本文提出CoVEBench——首个面向组合式视频编辑的综合基准，包含416个视频、626个多点编辑指令和9,990个细粒度检查项，揭示了现有模型在处理复杂组合编辑任务时存在遗漏编辑、违反保留约束或引入伪影等根本性挑战。

<details>
  <summary>Details</summary>

Motivation: 现有文本引导视频编辑模型擅长单一操作（如风格迁移、物体插入），但真实用户需求高度组合化，单个提示常需同时修改主体、动作和相机视角，同时严格保留无关时空内容。现有基准受限于孤立编辑和粗粒度全局指标，无法诊断模型在复杂工作流中的表现。

Method: 构建CoVEBench基准：包含416个精心策划的源视频、626个多点编辑指令和9,990个细粒度检查项；评估方式采用多模态大语言模型（MLLM）判断指令遵从度和视频保真度，辅以自动指标评估视频质量。

Result: 实验表明组合式编辑仍是重大挑战：当前模型执行多操作时频繁遗漏编辑、违反保留约束或引入伪影。

Conclusion: CoVEBench提供了一个具有挑战性、可诊断的测试平台，推动视频编辑向真实用户工作流迈进。

Original Abstract: While recent text-guided video editing models excel at elementary tasks (e.g., style transfer, object insertion), real-world user requests are highly compositional. A single prompt often demands multiple coupled edits, such as modifying subjects, actions, and camera views, while strictly preserving unrelated spatiotemporal content. Existing benchmarks, heavily constrained by isolated edits and coarse global metrics, fail to diagnose how models handle such complex workflows. To address this gap, we introduce CoVEBench, a compositional video editing benchmark comprising 416 curated source videos, 626 multi-point editing instructions, and 9,990 fine-grained checklist items. Covering diverse editing dimensions, CoVEBench evaluates models via MLLM-judged instruction compliance and video fidelity, alongside automated metrics for video quality. Extensive experiments reveal that compositional editing remains a profound challenge: current models frequently omit edits, violate preservation constraints, or introduce artifacts when handling multiple operations simultaneously. CoVEBench provides a challenging, diagnostic testbed to advance video editing toward realistic user workflows.

中文摘要翻译: 虽然近期基于文本引导的视频编辑模型在基础任务（如风格迁移、物体插入）上表现出色，但真实用户请求高度组合化。单个提示通常要求执行多种耦合编辑，例如同时修改主体、动作和相机视角，同时严格保留无关的时空内容。现有基准严重受限于孤立编辑和粗粒度全局指标，无法诊断模型如何处理这类复杂工作流。为弥补这一空白，我们提出了CoVEBench——一个组合式视频编辑基准，包含416个精心策划的源视频、626个多点编辑指令和9,990个细粒度检查项。CoVEBench涵盖多种编辑维度，通过多模态大语言模型（MLLM）评判指令遵从度和视频保真度，辅以视频质量的自动指标进行评估。大量实验表明，组合式编辑仍是一个深刻挑战：当前模型在处理多操作同时执行时，频繁遗漏编辑、违反保留约束或引入伪影。CoVEBench提供了一个具有挑战性且可诊断的测试平台，推动视频编辑向真实用户工作流迈进。

</details>


### [85] [CheXanatomy: Anatomy-Aware Vision-Language Modeling for Chest Radiographs](https://arxiv.org/abs/2606.08420)
*Sergios Gatidis,Curtis Langlotz,Christian Bluethgen*

Main category: cs.CV

TL;DR: 本文提出Chexanatomy框架，通过自回归词元空间监督将显式解剖知识集成到预训练的视觉-语言模型中，使其能够生成解剖分割掩码，在保持与专用卷积模型相当性能的同时，提高了域转移下的几何鲁棒性和样本效率。

<details>
  <summary>Details</summary>

Motivation: 现有的视觉-语言模型虽然在大规模图像-文本对上预训练后具备强大的图像级理解能力，但主要针对全局对齐优化，未能显式编码细粒度解剖结构，限制了其在分割等空间精确任务中的适用性。

Method: 提出Chexanatomy框架，通过在预训练VLM中引入自回归词元空间监督来集成显式解剖知识。模型通过下一词元预测训练生成解剖分割掩码，而非添加特定任务解码头。为实现可扩展监督，从CT体数据合成逼真胸片，并将CT分割标签前向投影以获得解剖一致性的2D掩码。

Result: 在合成和真实胸片上与U-Net基线对比，自回归解剖监督在分布内数据上达到与专用卷积模型相当的性能，在域转移到真实CXR数据时展现出更好的几何鲁棒性。解剖预训练模型在有限监督下适应新定位任务时表现出更高的样本效率。更大模型和更高输入分辨率提升性能，视觉编码器微调效果有限。

Conclusion: 将解剖结构直接嵌入生成式目标函数能够促进空间基础表示，支持解剖感知的医学视觉-语言建模。

Original Abstract: Vision-language models (VLMs) pretrained on large-scale image-text pairs demonstrate strong image-level understanding, but are primarily optimized for global alignment and do not explicitly encode fine-grained anatomical structure, limiting their suitability for spatially precise tasks such as segmentation. We introduce CheXanatomy, a framework that integrates explicit anatomical knowledge into a pretrained VLM through autoregressive token-space supervision. Instead of adding task-specific decoder heads, the model is trained to generate anatomical segmentation masks via next-token prediction. To enable scalable supervision, we synthesize realistic chest radiographs from CT volumes and forward-project CT segmentation labels to obtain anatomically consistent 2D masks. We evaluate the approach on synthetic and real chest radiographs against a U-Net baseline, including ablations on model scale, input resolution, and vision encoder fine-tuning. Autoregressive anatomical supervision achieves performance comparable to specialized convolutional models in-distribution and demonstrates improved geometric robustness under domain shift to real CXR data. In addition, anatomy-pretrained models exhibit improved sample efficiency when adapting to novel localization tasks under limited supervision. Larger models and higher input image resolution improve performance, while vision encoder fine-tuning has limited effect. These results show that embedding anatomical structure directly into the generative objective promotes spatially grounded representations and supports anatomy-aware medical vision-language modeling.

中文摘要翻译: 在大规模图像-文本对上预训练的视觉-语言模型展现出强大的图像级理解能力，但主要针对全局对齐进行优化，未能显式编码细粒度解剖结构，限制了其在分割等空间精确任务中的适用性。本文提出Chexanatomy，一个通过自回归词元空间监督将显式解剖知识集成到预训练VLM中的框架。该模型不添加任务特定的解码头，而是通过下一词元预测训练生成解剖分割掩码。为实现可扩展的监督，我们从CT体数据中合成逼真的胸片，并将CT分割标签进行前向投影，以获得解剖一致性的二维掩码。我们在合成胸片和真实胸片上，以U-Net为基线对该方法进行评估，包括模型规模、输入分辨率和视觉编码器微调的消融实验。自回归解剖监督在分布内数据上达到了与专用卷积模型相当的性能，并且在域转移到真实CXR数据时展现出更好的几何鲁棒性。此外，解剖预训练模型在有限监督下适应新的定位任务时表现出更高的样本效率。更大模型和更高输入图像分辨率能提升性能，而视觉编码器微调效果有限。这些结果表明，将解剖结构直接嵌入生成式目标函数能够促进空间基础表示，并支持解剖感知的医学视觉-语言建模。

</details>


### [86] [Segmentation-Assisted Brain MRI Synthesis with Cross-Image Multi-Contrast Feature Memory Bank Retrieval Augmentation](https://arxiv.org/abs/2606.08421)
*Wenwei Huang,Jia Wei,Jianlong Zhou*

Main category: cs.CV

TL;DR: 提出了一种基于检索增强合成的闭环框架（SSCF），用于多对比度脑MRI缺失模态合成，通过分割分支引导肿瘤区域感知和双库检索增强，在BraTs2020和UCSF-BMSR数据集上取得了优于现有方法的性能。

<details>
  <summary>Details</summary>

Motivation: 现有方法在多对比度脑MRI合成中难以有效合成关键肿瘤区域，且未能充分利用多对比度图像中的上下文信息。

Method: 基于生成对抗架构的合成-分割辅助闭环框架，包含：1）辅助分割分支预测肿瘤掩膜并反馈为语义条件指导合成；2）双库检索增强策略，动态查询肿瘤掩膜记忆库（提供肿瘤上下文）和跨图像对比特征记忆库（提供全局风格信息）。

Result: 在BraTs2020和UCSF-BMSR两个公开多对比度脑MRI数据集上验证，该方法在处理医学脑图像合成任务中有效，性能优于以往方法。

Conclusion: 所提出的闭环框架通过肿瘤感知表示学习和检索增强合成，有效提升了缺失对比度图像的合成质量，尤其在肿瘤区域。

Original Abstract: Multi-contrast brain MRI provide complementary soft-tissue characteristics that aid in the screening and diagnosis of diseases. However, limited scanning time, image corruption and various imaging protocols often result in incomplete multi-contrast images. While current approaches excel in image synthesis, they often struggle to synthesize critical tumor regions and exploit contextual information in multi-contrast brain MRI effectively. To address this issue, we propose a synthesis-centric, segmentation-assisted closed-loop framework with retrieval augmentation synthesis. Our method overall takes a generative adversarial architecture, which aims to synthesize missing contrasts from any combination of available ones with a single model. To explicitly capture tumor semantics and focus synthesis on tumor regions, we add an auxiliary segmentation branch that predicts tumor masks and feeds them back as semantic conditioning in synthesis branch, thereby learning tumor-aware representations in the model and improving synthesis fidelity. Furthermore, we propose a dual-bank retrieval augmentation strategy. It dynamically queries two external knowledge bases, namely a tumor masks memory bank for crucial tumor context and cross-image contrast feature memory bank for global style information, to augment synthesis. Verified on two public multi-contrast magnetic resonance brain datasets: BraTs2020 and UCSF-BMSR, the proposed method is effective in handling medical brain images synthesis tasks and shows superior performance compared to previous methods. Code is available at:https://github.com/iBizzard/SSCF.git

中文摘要翻译: 多对比度脑部磁共振成像提供互补的软组织特征，有助于疾病的筛查与诊断。然而，有限的扫描时间、图像损坏以及不同的成像协议常导致多对比度图像不完整。虽然现有方法在图像合成方面表现出色，但它们通常难以有效合成关键肿瘤区域，也无法充分利用多对比度脑MRI中的上下文信息。为解决这一问题，我们提出了一种以合成为中心、分割辅助的闭环框架，并引入检索增强合成策略。该方法整体采用生成对抗架构，旨在通过单一模型从任意可用对比度的组合中合成缺失的对比度。为了显式捕捉肿瘤语义并使合成聚焦于肿瘤区域，我们增加了一个辅助分割分支，用于预测肿瘤掩膜并将其作为语义条件反馈至合成分支，从而使模型学习肿瘤感知表征并提升合成保真度。此外，我们提出了一种双库检索增强策略，该策略动态查询两个外部知识库——肿瘤掩膜记忆库（用于关键肿瘤上下文）和跨图像对比特征记忆库（用于全局风格信息）——以增强合成效果。在两个公开的多对比度脑部磁共振数据集（BraTs2020和UCSF-BMSR）上的验证结果表明，所提方法能有效处理医学脑图像合成任务，且性能优于以往方法。代码已开源：https://github.com/iBizzard/SSCF.git

</details>


### [87] [Reinforcing Temporal Answer Grounding in Instructional Video via Candidate-Aware Causal Reasoning](https://arxiv.org/abs/2606.08436)
*Muge Qi,Rong Fu,Pengbin Feng,Xianda Li,Yu Cai,Yifu Guo,Shizhe Zhang,Simon James Fong,Lei Ma,Bin Li*

Main category: cs.CV

TL;DR: 提出了一种候选感知因果推理（CACR）框架，通过视觉-语言预训练的候选选择算法和时序逻辑推理模块，结合拒绝奖励机制和GRPO优化，在长视频时序答案定位任务上达到了最先进性能。

<details>
  <summary>Details</summary>

Motivation: 现有方法对无关内容敏感，且视觉推理能力不足，难以处理语义复杂的查询和未修剪视频与短目标片段之间的长度不匹配问题。

Method: 首先采用基于视觉-语言预训练的候选选择（VBCS）算法高效生成K个候选片段，然后使用时序逻辑推理模块（增强拒绝奖励机制，并通过分组相对策略优化（GRPO）优化）进行鲁棒推理。

Result: 在六个基准数据集上的大量实验表明，该方法在平均交并比（mIoU）指标上达到了最先进的性能。

Conclusion: 该方法为长视频中基于推理的检索提供了新视角。

Original Abstract: The task of temporal answer grounding in instructional video (TAGV), which aims to locate precise video segments that respond to natural language queries, is increasingly important for direct video answer retrieval. This task remains challenging due to the need to comprehend semantically complex questions and to address the significant length mismatch between untrimmed videos and short target moments. Existing methods often suffer from sensitivity to irrelevant content or insufficient visual reasoning capabilities. To tackle these limitations, we propose a Candidate-Aware Causal Reasoning (CACR) framework. Our approach first employs a Visual-Language Pre-training based Candidate Selection (VBCS) algorithm to efficiently generate K candidate segments, then applies a temporal logic reasoning module enhanced by a rejection reward mechanism and optimized via Group Relative Policy Optimization (GRPO) for robust inference. Extensive experiments on six benchmarks demonstrate that our method achieves state-of-the-art performance in terms of mean Intersection-over-Union (mIoU), providing a new perspective for reasoning-based retrieval in long videos.

中文摘要翻译: 教学视频中的时序答案定位任务（TAGV）旨在定位精确的视频片段以响应自然语言查询，对于直接视频答案检索越来越重要。由于需要理解语义复杂的问题，以及处理未修剪视频与短目标片段之间的显著长度不匹配，该任务仍具有挑战性。现有方法通常对无关内容敏感或视觉推理能力不足。为了解决这些局限性，我们提出了一种候选感知因果推理（CACR）框架。我们的方法首先采用基于视觉-语言预训练的候选选择（VBCS）算法高效生成K个候选片段，然后应用由拒绝奖励机制增强并通过分组相对策略优化（GRPO）优化的时序逻辑推理模块进行鲁棒推理。在六个基准上的大量实验表明，我们的方法在平均交并比（mIoU）指标上达到了最先进的性能，为长视频中基于推理的检索提供了新视角。

</details>


### [88] [TVI-CoT: Text-Visual Interleaved Chain-of-Thought Reasoning for Multimodal Understanding](https://arxiv.org/abs/2606.08464)
*Lianyu Hu,Xiaoyu Ma,Zeqin Liao,Yang Liu*

Main category: cs.CV

TL;DR: 提出文本-视觉交错思维链（TVI-CoT），通过可学习控制令牌<THINK>、<LOOK>和<ANSWER>在推理过程中动态访问视觉特征，突破多模态大模型中“视觉盲推理”的局限，在多个基准上取得SOTA结果。

<details>
  <summary>Details</summary>

Motivation: 现有CoT方法在多模态大模型中存在根本性缺陷：推理过程完全在文本空间进行，初始视觉编码后图像信息不可再访问，迫使模型仅依赖初始描述中的有限信息进行推理，形成“视觉盲推理”范式，限制了细粒度视觉提取、错误验证和自适应注意力。

Method: 提出TVI-CoT框架，引入可学习控制令牌<THINK>（文本推理）、<LOOK>（视觉特征访问）和<ANSWER>（答案生成），允许模型在推理过程中根据当前推理状态动态切换文本推理与视觉特征访问，从而实现对图像相关区域的注意力聚焦。

Result: 在八个基准上达到基于MLLM的CoT方法中的SOTA结果，相较于基线模型：MMMU提升+6.1%，MathVerse提升+3.8%，MathVista提升+3.4%，ScienceQA提升+3.4%。

Conclusion: 文本与视觉特征的显式交错推理能够有效克服多模态大模型中的视觉盲推理问题，显著提升多模态推理性能。

Original Abstract: Chain-of-thought (CoT) reasoning has proven effective for enhancing problem-solving in large language models. However, when applied to multimodal LLMs (MLLMs), existing CoT approaches suffer from a fundamental limitation: they perform reasoning entirely in text without accessing visual features during the reasoning process. After initial visual encoding, image information becomes inaccessible, forcing models to reason based solely on whatever was captured in the initial description, which forms a `vision-blind reasoning' paradigm that limits fine-grained visual extraction, error verification, and adaptive attention. We propose Text-Visual Interleaved Chain-of-Thought (TVI-CoT), a framework that enables explicit interleaving of textual reasoning and visual feature access through learnable control tokens <THINK>, <LOOK> and <ANSWER>. These tokens allow dynamic switching between reasoning and visual grounding, attending to relevant image regions conditioned on the evolving reasoning state. Experiments on eight benchmarks demonstrate state-of-the-art results among MLLM-based CoT methods and notable performance boost compared to the baseline: +6.1% on MMMU, +3.8% on MathVerse, +3.4% on MathVista, and +3.4% on ScienceQA. Code is available at https://github.com/hulianyuyy/TVI-CoT.

中文摘要翻译: 思维链（CoT）推理已被证明能有效增强大语言模型的问题解决能力。然而，当应用于多模态大语言模型（MLLMs）时，现有CoT方法存在一个根本性局限：它们在推理过程中完全在文本空间中进行，无法访问视觉特征。在初始视觉编码之后，图像信息变得不可访问，迫使模型仅基于初始描述中捕获的有限信息进行推理，形成了一种“视觉盲推理”范式，这限制了细粒度的视觉提取、错误验证和自适应注意力。我们提出文本-视觉交错思维链（TVI-CoT），这是一个通过可学习控制令牌<THINK>、<LOOK>和<ANSWER>实现文本推理与视觉特征访问显式交错的框架。这些令牌允许在推理与视觉定位之间动态切换，根据演化中的推理状态关注相关的图像区域。在八个基准上的实验表明，该方法在基于MLLM的CoT方法中取得了最先进的结果，相比基线模型性能显著提升：MMMU提升+6.1%，MathVerse提升+3.8%，MathVista提升+3.4%，ScienceQA提升+3.4%。代码已开源：https://github.com/hulianyuyy/TVI-CoT。

</details>


### [89] [Seeing is Believing: Aligning Prompt Rewriting with Visual Anchors for Text-to-Image Generation](https://arxiv.org/abs/2606.08492)
*Xuanyi Liu,Deyi Ji,Junyu Lu,Jing Wang,Qianxiong Xu,Xuhang Chen,Tianrun Chen,Siwei Ma*

Main category: cs.CV

TL;DR: FaithRewriter是一个新颖的提示增强框架，通过引入多模态大模型生成中间视觉线索，再结合大语言模型产生视觉 grounded 的增强描述，最后蒸馏到小模型实现高效部署，从而缩小文本到图像生成中的意图-生成差距。

<details>
  <summary>Details</summary>

Motivation: 现有文本到图像生成模型存在意图-生成差距，用户提示往往过于简短和模糊，而现有提示增强方法仅优化流畅性和可读性，缺乏视觉 grounding，可能导致过度推断缺失细节，反而加剧意图与生成结果之间的差距。

Method: 首先利用多模态大语言模型（MLLM）从原始提示生成图像作为中间视觉线索；然后将该视觉线索与原始提示结合，输入大规模语言模型（LLM）产生视觉 grounded 的增强描述；最后将这些增强描述蒸馏到小规模语言模型中，实现高效部署。

Result: 实验表明，FaithRewriter生成的提示比强基线方法更忠实于用户意图，视觉上更合理，有效缩小了意图-生成差距。

Conclusion: FaithRewriter通过引入视觉 grounding 的提示增强机制，有效解决了文本到图像生成中的意图-生成差距问题，兼顾了生成质量与部署效率。

Original Abstract: Despite the impressive capabilities of text-to-image (T2I) models, an intent-generation gap often persists due to the brevity and ambiguity of user prompts. Existing approaches primarily polish the prompt for fluency and readability. However, the enhancement process still lacks visual grounding. As a result, the rewriter may over-infer missing details, causing an intent-generation gap. To address this limitation, we propose FaithRewriter, a novel prompt-enhancement framework for T2I generation. Specifically, FaithRewriter first leverages a multimodal MLLM to generate an image from the original prompt as an intermediate visual cue. This cue is then combined with the prompt and fed into a large-scale LLM to produce visually grounded augmentations that better reflect how the intended content should appear in images. Finally, these augmentations are distilled into a small-scale LLM for efficient deployment, enhancing its ability to generate effective T2I prompts. Experiments show that FaithRewriter yields prompts that are more faithful to the user intent and more visually plausible than strong baselines, helping narrow the intent-generation gap.

中文摘要翻译: 尽管文本到图像（T2I）模型具备令人印象深刻的能力，但由于用户提示的简短性和模糊性，意图-生成差距（intent-generation gap）仍然普遍存在。现有方法主要对提示进行润色以提高流畅性和可读性。然而，这一增强过程仍然缺乏视觉 grounding（视觉锚定）。因此，重写器可能会过度推断缺失的细节，导致意图-生成差距。为了解决这一局限，我们提出FaithRewriter，一种用于T2I生成的新型提示增强框架。具体而言，FaithRewriter首先利用多模态大语言模型（MLLM）从原始提示生成一张图像作为中间视觉线索。然后，将该视觉线索与提示结合，输入大规模语言模型（LLM），以产生视觉 grounded 的增强描述，从而更好地反映预期内容在图像中应如何呈现。最后，将这些增强描述蒸馏到小规模语言模型中，以实现高效部署，增强其生成有效T2I提示的能力。实验表明，FaithRewriter生成的提示比强基线方法更忠实于用户意图且视觉上更合理，有助于缩小意图-生成差距。

</details>


### [90] [Look Less, Reason More: Block-wise Attention Skipping for Efficient Multimodal LLMs](https://arxiv.org/abs/2606.08511)
*Jie Ma,Zhike Qiu,Jiayi Ji,Xiaoshuai Sun,Rongrong Ji*

Main category: cs.CV

TL;DR: 提出V-Skip，一种无需训练的推理范式，通过选择性跳过饱和的视觉自注意力模块实现块级结构化稀疏，在保持94.16%-100.31%性能的同时显著降低MLLM推理开销。

<details>
  <summary>Details</summary>

Motivation: 多模态大语言模型（MLLMs）中，视觉token序列上的自注意力存在二次方计算开销，导致推理瓶颈。作者发现视觉token在早期层就快速建立了空间结构和模态内关系，深层视觉自注意力存在计算冗余。

Method: V-Skip将空间交互与语义演化解耦：对饱和的视觉自注意力模块进行选择性跳过（保留FFN），并通过轻量级少样本校准动态选择任务最优的稀疏路径，实现块级结构化稀疏。

Result: 在多种MLLM上，V-Skip保持94.16%至100.31%的性能保留率，有效绕过冗余视觉注意力实现块级稀疏化。

Conclusion: 模型不需要丢弃看到的视觉信息，只需在合适的深度“少看”即可更高效地推理。

Original Abstract: Multimodal Large Language Models (MLLMs) face a significant inference bottleneck due to the quadratic computational cost of self-attention over long visual token sequences. However, we identify a critical inefficiency in current architectures: Visual Attention Saturation. Our analysis reveals that visual tokens rapidly establish their spatial structure and intra-modal relationships in early layers, rendering visual-to-visual self-attention in deeper layers computationally redundant. Conversely, Feed-Forward Networks (FFNs) in these layers remain essential for projecting visual features into the evolving textual semantic space. Leveraging this insight, we present Visual-Skip (V-Skip), a training-free inference paradigm that decouples spatial interaction from semantic evolution. Rather than discarding tokens, V-Skip imposes block-wise structured sparsity by selectively bypassing saturated visual self-attention modules. Furthermore, recognizing that varying downstream tasks demand distinct reasoning depths, V-Skip employs a lightweight, few-shot calibration to dynamically route the task-optimal sparsity path. Extensive experiments demonstrate that V-Skip effectively bypasses redundant vision attention to achieve block-wise sparsity, maintaining a 94.16% to 100.31% performance retention across diverse MLLMs. Ultimately, we prove that to reason more effectively, models do not need to discard what they see -- they simply need to "look less" at the right depth.

中文摘要翻译: 多模态大语言模型（MLLMs）因长视觉token序列上自注意力的二次计算开销而面临显著的推理瓶颈。然而，我们发现了当前架构中的一个关键低效问题：视觉注意力饱和（Visual Attention Saturation）。我们的分析表明，视觉token在早期层就快速建立了其空间结构和模态内关系，使得深层层的视觉-视觉自注意力在计算上变得冗余。相反，这些层中的前馈网络（FFNs）对于将视觉特征投影到不断演化的文本语义空间中仍然至关重要。基于这一洞见，我们提出Visual-Skip（V-Skip），一种无需训练的推理范式，将空间交互与语义演化解耦。V-Skip并非丢弃token，而是通过选择性跳过饱和的视觉自注意力模块来施加块级结构化稀疏。此外，考虑到不同下游任务需要不同的推理深度，V-Skip采用轻量级的少样本校准来动态路由任务最优的稀疏路径。大量实验表明，V-Skip能够有效绕过冗余的视觉注意力以实现块级稀疏，在多种MLLM上保持94.16%至100.31%的性能保留率。最终，我们证明：为了更有效地推理，模型不需要丢弃它们所看到的内容——它们只需在合适的深度“少看”即可。

</details>


### [91] [OmniTryOn: Video Try-On Anything at Once!](https://arxiv.org/abs/2606.08514)
*Changliang Xia,Chengyou Jia,Minnan Luo,Zhuohang Dang,Xin Shen,Bowen Ping*

Main category: cs.CV

TL;DR: 本文提出Try-On Anything任务、OmniTryOn框架及TryAny-Bench基准，实现无需外部先验的单次推理多物品视频虚拟试穿，显著优于现有方法。

<details>
  <summary>Details</summary>

Motivation: 现有视频虚拟试穿方法存在两个根本局限：一是仅限于单件衣物迁移，无法同时试穿多个物品；二是严重依赖显式外部先验（如衣物掩码），破坏了物理动态并降低了视觉质量。

Method: 提出OmniTryOn生成式框架：1) 首帧可穿戴缓存策略(First Frame Wearable Cache)通过视频首帧直接提供多样化可穿戴物品；2) 时空一致RoPE(STC-RoPE)建立鲁棒的时空锚点以保持复杂人体运动和背景动态的一致性；3) 渐进式试穿(GTO)训练策略使模型逐步掌握多物体合成能力。

Result: 在TryAny-Bench上的大量实验表明，OmniTryOn显著优于现有专门的视频虚拟试穿模型和通用视频编辑基线，为Try-On Anything任务建立了强有力的新标准。

Conclusion: 本文通过提出Try-On Anything新任务、配套基准TryAny-Bench以及无外部先验的OmniTryOn框架，成功突破了现有视频虚拟试穿在单物品限制和先验依赖方面的瓶颈，实现了高质量的多物品同时视频试穿。

Original Abstract: Although video virtual try-on (VVT) has achieved significant progress, existing methods still exhibit two fundamental limitations: first, they are restricted to single-garment transfer, rendering simultaneous multi-object try-on highly impractical; second, their heavy reliance on explicit external priors (e.g., garment masks) inevitably destroys crucial physical dynamics and degrades visual quality. To bridge this gap, this paper proposes the novel Try-On Anything task, which aims to simultaneously transfer diverse wearable objects onto a person in a video in a single inference pass. To support and standardize this paradigm, we introduce TryAny-Bench, a comprehensive benchmark encompassing a paired video dataset alongside a tailored evaluation protocol. Furthermore, we present OmniTryOn, an external-prior-free generative framework designed to tackle this task. Specifically, OmniTryOn employs a First Frame Wearable Cache strategy, which directly provides diverse wearable objects for the generation process through the initial video frame. To maintain consistency, we propose the Spatiotemporally Consistent RoPE (STC-RoPE), which inherently establishes robust spatiotemporal anchors to strictly preserve complex human motions and background dynamics. Optimized by the proposed Gradual Try-On (GTO) training strategy, our model progressively masters robust multi-object synthesis. Extensive experiments on TryAny-Bench demonstrate that OmniTryOn significantly outperforms existing specialized video virtual try-on models and general video editing baselines, establishing a powerful new standard for the Try-On Anything task. Our dataset, code, and models are available at https://github.com/xcltql666/OminTryOn.

中文摘要翻译: 尽管视频虚拟试穿（VVT）已取得显著进展，现有方法仍存在两个根本性局限：首先，它们局限于单件衣物迁移，使得同时进行多物品试穿极不现实；其次，它们对显式外部先验（如衣物掩码）的严重依赖不可避免地破坏了关键的物理动态并降低了视觉质量。为弥补这一差距，本文提出了新颖的Try-On Anything任务，旨在单次推理过程中同时将多种可穿戴物品迁移到视频中的人物身上。为支持并标准化这一范式，我们引入了TryAny-Bench，一个包含配对视频数据集及定制评估协议的综合基准。此外，我们提出了OmniTryOn，一个无外部先验的生成式框架以解决该任务。具体而言，OmniTryOn采用了首帧可穿戴缓存（First Frame Wearable Cache）策略，通过视频首帧直接为生成过程提供多样化的可穿戴物品。为保持一致性，我们提出了时空一致RoPE（STC-RoPE），其内在建立了鲁棒的时空锚点以严格保留复杂的人体运动和背景动态。通过所提出的渐进式试穿（GTO）训练策略进行优化，我们的模型逐步掌握了鲁棒的多物体合成能力。在TryAny-Bench上的大量实验表明，OmniTryOn显著优于现有的专门视频虚拟试穿模型和通用视频编辑基线，为Try-On Anything任务建立了强有力的新标准。我们的数据集、代码和模型可在https://github.com/xcltql666/OminTryOn获取。

</details>


### [92] [DriveReward: A Comprehensive Dataset and Generative Vision-Language Reward Model for Autonomous Driving](https://arxiv.org/abs/2606.08525)
*Qimao Chen,Fang Li,Yuechen Luo,Zehan Zhang,Haiyang Sun,Fangzhen Li,Bing Wang,Guang Chen,Yang Ji,Jiong Deng,Hongwei Xie,Hangjun Ye,Long Chen,Yi Zhang*

Main category: cs.CV

TL;DR: 本文提出DriveReward——一个通过时间维度的视觉引导和反事实数据标注构建的推理轨迹评估数据集，并基于此训练了一个专门的1B视觉语言奖励模型，在自动驾驶的RL微调和多模态轨迹选择中取得了与基于规则的奖励计算相当的性能。

<details>
  <summary>Details</summary>

Motivation: 现有的自动驾驶奖励模型依赖人工规则或感知真值，泛化能力差且难以实现数据规模化扩展；而视觉语言模型（VLM）在其他领域已展现出作为奖励模型的可行性，但在驾驶任务中的有效性尚未被充分探索。

Method: (1) 构建DriveReward数据集：通过时间维度的视觉引导进行严格标注，并加入反事实数据标注方案以覆盖不同驾驶风格和错误行为；(2) 训练专门的视觉语言奖励模型（1B参数），用于驾驶轨迹评估。

Result: 评测显示现有开源和商业VLM均无法在所有任务上表现出色；专门训练的1B奖励模型在任务特定奖励对齐上超越了更大的VLM；将该奖励模型集成到RL微调和多模态轨迹评分中，在开环和闭环评估中达到了与基于规则的奖励计算相当的性能。

Conclusion: 通过领域特化的数据构建和模型训练，小规模奖励模型可以超越大规模通用VLM，并在实际自动驾驶任务中取得与规则方法相当的效果，验证了VLM作为驾驶奖励模型的有效性。

Original Abstract: Reward models play a pivotal role in reinforcement learning (RL) and multi-modal trajectory selection for autonomous driving. However, acquiring such rewards typically relies on hand-crafted rule-based objectives or perception ground truth, which hinders generalization for data-scaling. While Vision-Language Models (VLMs) have demonstrated feasibility as reward models in other domains, their effectiveness in driving tasks remains underexplored. In this work, we bridge this gap by (1) introducing DriveReward, a reasoning trajectory evaluation dataset rigorously labeled via temporally-grounded visual guidance, and augmented with counterfactual driving behaviors., (2) alongside a specialized Vision-Language Reward Model. To address the scarcity of failure cases in conventional datasets, we propose a counterfactual data annotation scheme to construct cases encompassing diverse driving styles and erroneous behaviors. Evaluations on our proposed benchmark reveal that even leading open-source and proprietary VLMs fail to excel across all tasks, highlighting significant room for improvement in existing models. Building on these findings, we subsequently tailor a specialized 1B reward model that outperforms larger VLMs on task-specific reward alignment. Finally, we validate our reward model's effectiveness by integrating it into RL finetuning and multi-modal trajectory scoring across multiple baselines, achieving performance comparable to rule-based reward calculations in both open-loop and closed-loop evaluation.

中文摘要翻译: 奖励模型在强化学习（RL）以及自动驾驶的多模态轨迹选择中扮演着关键角色。然而，获取此类奖励通常依赖于基于人工规则的目标函数或感知真值，这阻碍了数据规模化扩展的泛化能力。尽管视觉语言模型（VLM）在其他领域已被证明可作为奖励模型，但其在驾驶任务中的有效性仍未得到充分探索。本文通过以下两方面填补了这一空白：(1) 提出了DriveReward——一个推理轨迹评估数据集，该数据集通过时间维度的视觉引导进行严格标注，并辅以反事实驾驶行为来增强；(2) 同时提出了一个专门的视觉语言奖励模型。为了解决传统数据集中失败案例稀缺的问题，我们提出了一种反事实数据标注方案，构建了涵盖不同驾驶风格和错误行为的场景。在我们提出的基准测试上的评估显示，即使是领先的开源和商业VLM也无法在所有任务上表现优异，这突显了现有模型仍有很大的改进空间。基于这些发现，我们随后定制了一个专门的1B奖励模型，其在任务特定奖励对齐上优于更大的VLM。最后，我们通过将该奖励模型集成到多个基线的RL微调和多模态轨迹评分中，验证了其有效性，在开环和闭环评估中均取得了与基于规则的奖励计算相当的性能。

</details>


### [93] [NGram-MoSE: Efficient Remote Sensing Super-Resolution via N-Gram Context and Mixture-of-Experts](https://arxiv.org/abs/2606.08535)
*Yun-Hsuan Huang,Trong-An Bui,Chih-Hung Chuang*

Main category: cs.CV

TL;DR: 本文提出一种轻量级Transformer超分辨率架构NGram-MoSE，通过N-Gram上下文注入和混合专家前馈设计，在遥感图像超分辨率任务中实现高效纹理重建与跨尺度鲁棒性，以14倍FLOPs降低达到31.68 dB PSNR，并在滑坡分割下游任务中提升4.47% mAP@50。

<details>
  <summary>Details</summary>

Motivation: 遥感图像存在空间-时间分辨率权衡：高空间细节图像获取频率低，而高时间分辨率图像空间细节粗糙。现有Transformer超分辨率模型计算开销大，且对有限或地理偏斜的训练数据敏感，在分布外条件下鲁棒性差。

Method: 提出NGram-MoSE轻量级Transformer架构，包含两个核心设计：(1) N-Gram上下文注入（N-Gram Context Injection），增强跨窗口局部一致性并缓解窗口边界伪影；(2) 混合专家前馈设计（Mixture-of-Experts, MoE），通过稀疏激活扩展模型容量而不等比增加推理成本。

Result: 在地理分布外测试集上，NGram-MoSE达到31.68 dB PSNR，FLOPs降低14倍；在滑坡分割下游任务中，将退化输入恢复至检测器训练尺度后，mAP@50绝对值提升4.47%，且展示了更强的跨尺度一致性。

Conclusion: NGram-MoSE为资源受限的遥感管线提供了有效的超分辨率模块，具备良好的鲁棒泛化能力。

Original Abstract: Remote sensing applications for environmental monitoring and disaster management are frequently constrained by a spatial--temporal trade-off: imagery with fine spatial detail is often acquired less frequently, whereas more temporally available observations are typically coarser. Single-image super-resolution provides a practical means to enhance coarse imagery without changing acquisition schedules, yet many Transformer-based SR models remain computationally expensive and can be sensitive to limited or geographically biased training data, which degrades robustness under out-of-distribution conditions. This paper presents NGram-MoSE, a lightweight Transformer architecture designed to improve both efficiency and texture continuity. NGram-MoSE introduces N-Gram Context Injection to strengthen cross-window local consistency and mitigate window-boundary artifacts, and incorporates a Mixture-of-Experts (MoE) feed-forward design to scale capacity through sparse activation without proportional growth in inference cost. Experiments on a geographically disjoint OOD test set show that NGram-MoSE achieves 31.68\,dB PSNR while reducing FLOPs by \(14\times\) relative to a heavyweight Transformer reference. Downstream evaluation on a landslide segmentation benchmark further demonstrates that restoring degraded inputs to the detector training scale improves performance, yielding a 4.47\% absolute gain in mAP@50 over bicubic upsampling, and exhibits stronger cross-scale consistency under scale extrapolation. These results indicate that NGram-MoSE provides an effective SR module for resource-constrained remote sensing pipelines requiring robust generalization.

中文摘要翻译: 环境监测与灾害管理中的遥感应用常常受限于空间-时间分辨率的权衡：具有精细空间细节的图像通常获取频率较低，而时间维度上更易获取的观测数据往往分辨率较粗。单图像超分辨率提供了一种在不改变采集计划的情况下增强粗分辨率图像的实用手段，然而许多基于Transformer的超分辨率模型计算开销仍然较大，且对有限或地理偏斜的训练数据敏感，从而在分布外条件下鲁棒性下降。本文提出NGram-MoSE，一种旨在提升效率与纹理连续性的轻量级Transformer架构。NGram-MoSE引入N-Gram上下文注入以增强跨窗口局部一致性并缓解窗口边界伪影，同时采用混合专家前馈设计，通过稀疏激活扩展模型容量而不等比增加推理成本。在地理分离的分布外测试集上的实验表明，NGram-MoSE在PSNR达到31.68 dB的同时，相较于重型Transformer基准模型FLOPs降低14倍。滑坡分割基准上的下游评估进一步证明，将退化输入恢复至检测器训练尺度可提升性能，相比双三次上采样在mAP@50上获得4.47%的绝对增益，且在尺度外推下展现出更强的跨尺度一致性。这些结果表明，NGram-MoSE为需要鲁棒泛化的资源受限遥感管线提供了一个有效的超分辨率模块。

</details>


### [94] [Towards Accurate Emotion-Attributed Video Captioning via Fine-grained Emotion-Cause Pair Extraction](https://arxiv.org/abs/2606.08566)
*Weidong Chen,Cheng Ye,Zhendong Mao,Liping Wang,Xinyan Liu,Yongdong Zhang*

Main category: cs.CV

TL;DR: 本文提出细粒度情感-原因对提取框架，通过概念感知视觉语义分解和视觉引导情感可解释学习模块，分两轮实现情感与原因特征的提取与耦合，显著提升情感视频描述的情感感知与语义理解能力，在多个数据集上取得SOTA结果。

<details>
  <summary>Details</summary>

Motivation: 现有情感视频描述方法依赖整体视觉特征挖掘全局情感线索，忽略了视觉情感由特定动机性原因触发且仅隐含在核心视频片段中的关键特性，导致信息冗余和情感线索不准确。因此需要细粒度的视觉原因提取来同时促进情感感知和情感归因描述生成。

Method: 提出细粒度情感-原因对提取框架，分两轮实现：1）概念感知视觉语义分解模块（CVSD）通过探索场景、物体和运动概念增强视觉特征；视觉引导情感可解释学习模块（VGIL）利用视觉时间动态性引导情感细化，并用VAD向量约束增强可解释性。2）通过交叉耦合细化前后的视觉与情感特征实现情感-原因对提取，并利用对比损失实现语义强制对齐。

Result: 在EVC-MSVD等三个数据集上取得最优性能，例如在EVC-MSVD上BLEU-2提升4.4%，ROUGE-L提升5.4%。

Conclusion: 该方法优化了视频的复杂语义理解和情感感知，在情感视频描述任务上表现出色，各模块均有效。

Original Abstract: Emotional Video Captioning (EVC) is a challenging task that aims to generate factually accurate and emotionally rich descriptions for videos. Existing EVC methods leverage holistic visual features to mine global emotional cues, and then aggregate multimodal features to guide the emotional caption generation, which ignores the critical characteristic of the EVC task. Visual emotions are evoked by specific motivational causes, which are usually only implied in core video segments. The holistic mining brings significant information redundancy and inaccurate emotional cues. Thus, fine-grained visual cause extraction has a facilitative effect on both emotion perception and emotion-attributed caption generation. To this end, we propose a fine-grained emotion-cause pair extraction framework for emotion-attributed video captioning. Specifically, we learn pair-wise emotion and cause features in two rounds: 1) We propose a Concept-aware Visual Semantic Decomposition module to augment visual features by exploring scene, object, and motion concepts. Besides, to enhance emotional features, we propose a Visual-guided Emotion Interpretable Learning module, which guides emotion refinement with visual temporal dynamics, and augments the interpretable refinement process by reliable VAD-vector constraints. 2) We achieve emotion-cause pair extraction by cross-coupling the visual and emotional features before and after refinement, and leverage contrastive loss to achieve semantic forced alignment. Overall, our approach optimizes complex semantic understanding and emotion perception of videos, leading to a promising performance in emotional captioning. Extensive experiments on three challenging datasets demonstrate the superiority of our approach and each proposed module, e.g., achieving the best performances with +4.4% and +5.4% w.r.t. BLEU-2 and ROUGE-L, respectively, on the EVC-MSVD dataset.

中文摘要翻译: 情感视频描述（EVC）是一项具有挑战性的任务，旨在为视频生成事实准确且情感丰富的描述。现有的EVC方法利用整体视觉特征挖掘全局情感线索，然后聚合多模态特征来引导情感描述生成，但这忽略了EVC任务的关键特性。视觉情感是由特定的动机性原因引发的，而这些原因通常仅隐含在核心视频片段中。整体挖掘带来了显著的信息冗余和不准确的情感线索。因此，细粒度的视觉原因提取对情感感知和情感归因描述生成都具有促进作用。为此，我们提出了一种用于情感归因视频描述的细粒度情感-原因对提取框架。具体地，我们在两轮中学习成对的情感与原因特征：1）我们提出了概念感知视觉语义分解模块，通过探索场景、物体和运动概念来增强视觉特征。此外，为增强情感特征，我们提出了视觉引导的情感可解释学习模块，该模块利用视觉时间动态性引导情感细化，并通过可靠的VAD向量约束增强可解释的细化过程。2）我们通过交叉耦合细化前后的视觉与情感特征来实现情感-原因对提取，并利用对比损失实现语义强制对齐。总体而言，我们的方法优化了视频的复杂语义理解和情感感知，在情感描述任务中取得了有前景的性能。在三个具有挑战性的数据集上进行的大量实验证明了我们方法及每个提出模块的优越性，例如，在EVC-MSVD数据集上，BLEU-2和ROUGE-L分别实现了+4.4%和+5.4%的最佳性能提升。

</details>


### [95] [OmniCap-IF: Benchmarking and Improving Instruction Following Abilities for Omni-Video Captioning](https://arxiv.org/abs/2606.08572)
*Jiahao Wang,An Ping,Yanghai Wang,Yuanxing Zhang,Shihao Li,Hanyan Bian,Yichi Ren,Yize Zhang,Han Wang,Haowen Chen,Junze Li,Jiaqi Wang,Yiyang Hu,Zhuze Xu,Zijie Zhang,Jiaheng Liu*

Main category: cs.CV

TL;DR: 提出了OmniCap-IF基准和OmniCaptioner-IF模型，用于评估和提升全模态大语言模型在复杂指令遵循方面的能力，揭示了格式复杂度与内容质量之间的权衡关系。

<details>
  <summary>Details</summary>

Motivation: 现有全模态大语言模型在多模态处理上表现优异，但缺乏对其在复杂、多维度用户指令遵循能力方面的系统性评估，已有基准无法捕捉模态间交互与用户约束的复杂关系。

Method: 构建了OmniCap-IF基准，从格式正确性和内容正确性两个维度评估字幕生成；包含50种约束类型，覆盖纯视觉、纯音频和音视频模态，并引入时间定位能力评估。同时构建了54K指令微调数据集OmniCap-IF-54K并训练了OmniCaptioner-IF模型。

Result: 对主流模型在1920个高质量样本上的评估揭示了显著的性能差异，并发现"格式-内容权衡"现象：增加格式复杂度会直接降低模型的跨模态推理能力。

Conclusion: OmniCaptioner-IF在复杂指令遵循和全模态字幕生成方面均取得了显著改进，推动了全模态指令遵循领域的发展。

Original Abstract: While Omni-modal Large Language Models (OLLMs) have demonstrated impressive capabilities in jointly processing audio and visual streams, their ability to strictly adhere to complex, multi-faceted user instructions remains largely unexplored. Existing benchmarks primarily focus on holistic video understanding or text-only instruction following, failing to capture the intricate interplay between modalities and user constraints. To bridge this gap, we introduce OmniCap-IF, the first comprehensive benchmark specifically designed to evaluate instruction-following capabilities in omni-modal captioning. OmniCap-IF incorporates a systematic framework that assesses captions on two dimensions: format correctness and content correctness. Our benchmark encompasses 50 distinct constraint types across pure visual, pure audio, and audio-visual modalities, while integrating Temporal Grounding to assess spatio-temporal precision. Extensive evaluations of prominent models on 1,920 high-quality samples reveal significant performance disparities. Furthermore, our analysis uncovers a critical "format-content tradeoff", demonstrating that increasing formatting complexity directly degrades models' omni-modal reasoning abilities. Finally, to advance the field, we curate a 54K instruction-tuning dataset, OmniCap-IF-54K and present OmniCaptioner-IF, which achieves notable improvements in both complex instruction adherence and general omni-modal captioning performance.

中文摘要翻译: 尽管全模态大语言模型在联合处理音频和视觉流方面展现了令人印象深刻的能力，但它们严格遵循复杂、多维度用户指令的能力仍 largely 未被探索。现有基准主要关注整体视频理解或纯文本指令遵循，未能捕捉模态与用户约束之间的复杂交互。为弥补这一空白，我们提出了OmniCap-IF，这是第一个专为评估全模态字幕生成中指令遵循能力而设计的综合基准。OmniCap-IF采用系统化框架，从格式正确性和内容正确性两个维度评估字幕。我们的基准涵盖了纯视觉、纯音频和音视频模态中的50种不同约束类型，同时集成了时间定位能力以评估时空精度。对主流模型在1920个高质量样本上的广泛评估揭示了显著的性能差异。此外，我们的分析发现了一个关键的"格式-内容权衡"现象，表明增加格式复杂度会直接降低模型的跨模态推理能力。最后，为推动该领域发展，我们整理了54K指令微调数据集OmniCap-IF-54K，并提出了OmniCaptioner-IF，该模型在复杂指令遵循和通用全模态字幕生成性能方面均取得了显著改进。

</details>


### [96] [Facial Expression Recognition in the Deep Learning Era: A Systematic Multi-Criteria Review of Methods, Models, Datasets, Performance, Challenges, and Future Research Directions](https://arxiv.org/abs/2606.08612)
*Spyridon Georgiou,Aggelos Psiris,Spyridon Evangelatos,Thomas Lagkas,Vasileios Argyriou,Panagiotis Sarigiannidis,Iraklis Varlamis,Georgios Th. Papadopoulos*

Main category: cs.CV

TL;DR: 一篇关于深度学习面部表情识别（FER）的全面综述，提出了多阶段演化框架、多准则分类法，并对任务、数据集、方法和未来方向进行了系统性分析。

<details>
  <summary>Details</summary>

Motivation: 现有FER综述仅沿狭窄的任务、架构或应用维度展开，缺乏对深度学习时代FER进展的整体性、系统性梳理，且未将其与更广泛的面部情感识别（FAR）领域明确关联。

Method: 采用多准则分类法，沿七条互补轴（识别任务、输入模态、人脸预处理流程、网络架构、学习策略、采集设置、应用领域）分析文献；提出FER演进的五个阶段框架；按任务组织公开数据集；对代表性SOTA方法进行跨任务定量对比。

Result: 给出了每类方法在非受控条件下的优势与局限的批判性分析；整理了各类任务的性能指标并在主流基准上进行了定量比较；汇编了FER数据集及其标注方案、模态和评估协议。

Conclusion: 总结了当前挑战和有前景的未来方向，强调该综述填补了深度学习FER领域缺乏系统性全貌综述的空白。

Original Abstract: Facial Expression Recognition (FER) has advanced rapidly over the last decade, driven by the shift from handcrafted descriptors and shallow classifiers to deep convolutional, attention-based, vision-language, and foundation-model architectures, and by the parallel growth of large-scale in-the-wild benchmarks spanning categorical, dimensional, compound, micro-expression, Action Unit (AU), and intensity-estimation tasks. Yet the deep learning-based FER landscape has so far been reviewed only along narrow task-, architecture-, or application-specific axes, leaving a holistic, systematically organized account of its recent advances missing. This survey addresses that gap with a comprehensive review of recent deep learning-based FER, explicitly linked to the wider Facial Affect Recognition (FAR) domain. Its main contributions are: a) A description of FER's evolution into five distinct phases, from handcrafted features and classical machine learning to attention-based, vision-language, and foundation-model approaches, with the key milestone works of each, b) A multi-criteria taxonomy analyzing the literature along seven complementary axes: recognition task, input modality, face pre-processing pipeline, network architecture, learning strategy, acquisition setting, and application domain, c) A per-criterion comparative analysis, with critical insights into the strengths and limitations of each category under in-the-wild conditions, d) A task-organized review of public FER datasets, with their annotation schemes, modalities, and evaluation protocols, e) A compilation of performance metrics and a per-task quantitative comparison of representative state-of-the-art methods on widely adopted benchmarks, and f) A discussion of current challenges and promising future directions.

中文摘要翻译: 面部表情识别（FER）在过去十年中取得了快速进展，这得益于从手工设计的描述子和浅层分类器向深度卷积、基于注意力的、视觉-语言和基础模型架构的转变，以及大规模非受控场景基准数据集的并行增长，这些基准涵盖了分类、维度、复合、微表情、动作单元（AU）和强度估计等任务。然而，基于深度学习的FER领域迄今仅沿着狭窄的任务、架构或应用特定轴线被综述，缺少对其近期进展的整体性、系统性梳理。本综述填补了这一空白，对近期基于深度学习的FER进行了全面回顾，并将其与更广泛的面部情感识别（FAR）领域明确关联。其主要贡献包括：a) 描述了FER演进的五个不同阶段——从手工特征和经典机器学习到基于注意力、视觉-语言和基础模型的方法，以及每个阶段的关键里程碑工作；b) 一个多准则分类法，沿七条互补轴分析文献：识别任务、输入模态、人脸预处理流程、网络架构、学习策略、采集设置和应用领域；c) 对每个准则进行比较分析，提供每类方法在非受控条件下优势与局限的批判性见解；d) 按任务组织的公开FER数据集综述，包含其标注方案、模态和评估协议；e) 性能指标汇编以及代表性最先进方法在广泛采用的基准上的逐任务定量比较；f) 对当前挑战和有前景的未来方向的讨论。

</details>


### [97] [Harnessing Streaming Video in the Wild](https://arxiv.org/abs/2606.08615)
*Dingyu Yao,Shuhuan Gu,Qingyi Si,Junhao Zhou,Chenxu Yang,Chuanyu Qin,Naibin Gu,Zheng Lin,Weiping Wang,Nan Duan,Jiaqi Wang*

Main category: cs.CV

TL;DR: 本文针对现有视觉-语言模型（VLM）在流式视频理解中缺乏专用基础设施的问题，提出了三方面贡献：构建流式训练数据集Streaming-Train-248K并设计新训练目标、开发即插即用系统Streaming Harness（支持主动交互、长时记忆和实时处理）、建立评估基准Streaming-Eval，实验表明该方法在所有核心流式能力上均取得一致性提升。

<details>
  <summary>Details</summary>

Motivation: 现有VLM在离线视频理解上表现优异，但缺乏流式处理能力（如主动交互、长时记忆、实时处理），且没有专用于流式部署的基础设施，无法满足视频通话助手、实况解说、具身机器人等场景中处理无界视频流的需求。

Method: (i) 数据集与训练：构建Streaming-Train-248K数据集，并设计适配流式交互与理解的新型训练目标；(ii) 部署系统：开发Streaming Harness即插即用系统，赋予任意VLM三种核心能力——主动交互（每秒响应决策）、长时记忆（12小时上下文保持）、实时处理（亚秒级延迟）；(iii) 评估基准：设计Streaming-Eval基准，衡量模型在多样化野外场景中的流式能力。

Result: 大量实验表明，该方法在所有视频流式理解所需的核心能力上均取得一致性提升。

Conclusion: 通过数据、训练目标、部署系统和评估基准的全面贡献，推动社区从离线视频理解转向可部署的流式智能。所有数据、代码和基准将开源。

Original Abstract: Vision-Language Models (VLMs) are increasingly required to process unbounded video streams in applications such as video-call assistants, live commentary, and embodied robots. An ideal streaming system should support proactive interaction, long-horizon memory, and real-time processing, while resting on a VLM backbone capable of handling diverse in-the-wild streaming tasks. However, existing VLMs excel at offline video understanding but fall short in streaming capabilities and lack dedicated infrastructure for streaming deployment. We address this gap on three fronts. (i) For backbone capability, we construct \textbf{Streaming-Train-248K}, a streaming dataset paired with a novel training objective for adapting VLMs to streaming interaction and understanding. (ii) For real-world deployment, we introduce \textbf{Streaming Harness}, a plug-and-play system that endows any VLM with three core abilities: proactive interaction (per-second response decisions), long-term memory (12-hour context retention), and real-time processing (sub-second latency). (iii) To drive continued community progress on streaming capabilities, we design \textbf{Streaming-Eval}, a benchmark that reflects models' capabilities across diverse in-the-wild scenarios. Extensive experiments demonstrate consistent gains from our approach across all core capabilities required for streaming video understanding. We will open-source our data, code, and benchmark to advance the community's shift from offline video understanding to deployable streaming intelligence.

中文摘要翻译: 视觉-语言模型（VLM）在视频通话助手、实况解说和具身机器人等应用中，正日益需要处理无界的视频流。理想的流式系统应支持主动交互、长时记忆和实时处理，同时基于能够处理多样化野外流式任务的VLM骨干网络。然而，现有VLM擅长离线视频理解，但在流式能力上存在不足，且缺乏专用于流式部署的基础设施。我们从三个方面弥补这一差距。(i) 针对骨干能力，我们构建了\textbf{Streaming-Train-248K}，这是一个流式数据集，配以新颖的训练目标，用于将VLM适配到流式交互与理解任务中。(ii) 针对实际部署，我们引入了\textbf{Streaming Harness}，一个即插即用的系统，赋予任意VLM三种核心能力：主动交互（每秒响应决策）、长时记忆（12小时上下文保持）和实时处理（亚秒级延迟）。(iii) 为推动社区在流式能力上的持续进展，我们设计了\textbf{Streaming-Eval}，一个反映模型在多样化野外场景中能力的基准测试。大量实验表明，我们的方法在视频流式理解所需的所有核心能力上均取得一致性提升。我们将开源数据、代码和基准测试，以推动社区从离线视频理解向可部署的流式智能转变。

</details>


### [98] [SSAFE: Simple and Strong AI-Generated Image Detection via Frozen Vision Encoders](https://arxiv.org/abs/2606.08634)
*Seunghyun Lee,Byoungkwon Kim,Jaehyun Nam,Kyungmin Lee,Jinwoo Shin*

Main category: cs.CV

TL;DR: 本文发现冻结的多模态视觉编码器能在嵌入空间中天然区分真实与合成图像，因此提出一种表示感知的数据筛选策略，仅用1万张训练图像即可实现高效、鲁棒的AI生成图像检测，并引入新基准RealWorldBench进行验证。

<details>
  <summary>Details</summary>

Motivation: 现有深度伪造检测方法依赖大规模真实-伪造数据集，但随着生成模型不断涌现，维护这些数据集日益困难，亟需更高效、鲁棒的检测方案。

Method: 1) 利用冻结的多模态视觉编码器提取图像嵌入，无需微调；2) 设计表示感知的数据筛选策略，从代表性生成器中选取紧凑的训练集；3) 训练简单线性分类器进行真假判别；4) 构建包含现代相机照片、当代库存图像和最新商业生成器输出的RealWorldBench基准。

Result: 仅用1万张训练图像（对比AIGIBench的28.8万张和OpenFake的400万张）即实现强检测性能，同时对未见过的生成器和分布偏移表现出更优的鲁棒性。

Conclusion: 将冻结的多模态表示与精心筛选的训练数据相结合，为AI生成图像检测提供了一种简单而有效的方法。

Original Abstract: The rapid advancement of generative models has blurred the boundary between synthetic and real imagery, creating an urgent need for reliable deepfake detection. Yet most existing approaches rely on massive real--fake datasets, which are increasingly difficult to maintain as new generators continue to emerge. In this work, we investigate how much information about image authenticity is already encoded in modern multimodal vision representations. We find that frozen multimodal encoders naturally separate real and synthetic images in their embedding space, enabling a simple linear classifier to achieve strong performance without task-specific fine-tuning. Motivated by this observation, we develop a representation-aware data curation strategy that selects a compact set of representative generators for training. The resulting training set contains only 10K images, compared to 288K in AIGIBench and 4M in OpenFake, while improving robustness to unseen generators and distribution shifts. We additionally introduce RealWorldBench, a benchmark consisting of modern camera photographs, contemporary stock images, and outputs from recent commercial generators. Experiments across multiple benchmarks show that combining frozen multimodal representations with carefully curated training data provides a simple and effective approach to AI-generated image detection.

中文摘要翻译: 生成模型的快速发展使得合成图像与真实图像之间的边界逐渐模糊，从而产生了对可靠深度伪造检测的迫切需求。然而，现有的大多数方法依赖海量的真实-伪造数据集，而随着新生成器不断涌现，这些数据集的维护日益困难。在本工作中，我们探究了现代多模态视觉表示中已经编码了多少关于图像真实性的信息。我们发现，冻结的多模态编码器能够在其嵌入空间中自然地分离真实图像与合成图像，这使得一个简单的线性分类器无需任务特定的微调即可实现强劲的性能。基于这一观察，我们提出了一种表示感知的数据筛选策略，选择一组紧凑的代表性生成器用于训练。最终得到的训练集仅包含1万张图像，而AIGIBench包含28.8万张、OpenFake包含400万张，但我们的方法对未见过的生成器和分布偏移具有更强的鲁棒性。此外，我们引入了RealWorldBench基准，该基准由现代相机照片、当代库存图像以及近期商业生成器的输出组成。在多个基准上的实验表明，将冻结的多模态表示与精心筛选的训练数据相结合，为AI生成图像检测提供了一种简单而有效的方法。

</details>


### [99] [Learnable Token Sparsification for Efficient Gigapixel Whole Slide Image Reasoning](https://arxiv.org/abs/2606.08641)
*Jingzhi Chen,Landi He,Zhuo Chen,Shawn Young,Lijian Xu*

Main category: cs.CV

TL;DR: 本文提出可训练的令牌稀疏化框架，将全切片图像压缩至仅32个视觉令牌（原始长度的0.78%），通过可微的Soft Top-K算子实现梯度传播，在SlideBench上达到73.32%准确率，解决了千兆像素病理图像中视觉令牌过多导致的细粒度诊断信号稀释问题。

<details>
  <summary>Details</summary>

Motivation: 现有方法依赖空间下采样或启发式剪枝策略（无训练），会丢弃散落在组织中的稀疏但临床意义明确的病理模式，缺乏可学习的最优选择机制。

Method: 提出解耦路由架构，包含SparseLearn组件：训练时使用方差保持噪声门控+可微Soft Top-K算子实现梯度传播，配合对角注意力去噪器恢复扰动表示；推理时丢弃SparseLearn，用确定性Hard Top-K保留最高分的32个令牌，零额外计算开销。

Result: 在SlideBench(TCGA)上总体准确率73.32%，超越基于采样的基线和通用视觉语言模型；在SlideBench(BCNB)和WSI VQA*上展现出强零样本泛化能力。

Conclusion: 通过将令牌缩减建模为可训练稀疏化问题，解决了视觉上下文瓶颈并防止稀疏诊断证据被稀释，为端到端千兆像素全切片图像推理提供了高效范式。

Original Abstract: The processing of gigapixel whole slide images within vision language models faces a major difficulty due to an excessive number of visual tokens. Existing solutions typically rely on spatial downsampling or heuristic pruning strategies that operate without training, and these methods often discard subtle but clinically meaningful patterns because pathological evidence is scattered irregularly across the tissue. To overcome this limitation, we reformulate token reduction in whole slide images as a trainable sparsification problem, allowing the model to learn an optimal selection strategy instead of following fixed heuristics. We propose a decoupled routing architecture. To enable gradient propagation through the nondifferentiable pruning operation during training, we introduce a component called SparseLearn. This component uses a variance-preserving noise gate that regulates the information flow of each patch via a differentiable Soft Top-K operator, together with a diagonal attention denoiser that recovers perturbed representations without leaking spatial information. At inference time, the SparseLearn module is entirely discarded, and the trained scorer applies a deterministic Hard Top-K operator to keep only the highest scoring 32 tokens, incurring no extra computation. By compressing the visual sequence down to a sparse set of just 32 tokens, which represents as little as 0.78% of the original length, our framework achieves 73.32% overall accuracy on SlideBench (TCGA), consistently surpassing sampling-based baselines and general-purpose vision language models. It also demonstrates strong zero shot generalization on SlideBench (BCNB) and WSI VQA*. By resolving the visual context bottleneck and preventing the dilution of sparse diagnostic evidence, this work provides a highly efficient paradigm for end to end gigapixel whole slide image reasoning.

中文摘要翻译: 在视觉语言模型中处理千兆像素全切片图像面临一个主要困难：视觉令牌数量过多。现有解决方案通常依赖空间下采样或启发式剪枝策略（无需训练），但这些方法往往会丢弃细微但具有临床意义的模式，因为病理证据在组织中不规则地稀疏分布。为克服这一局限，我们将全切片图像中的令牌缩减重新表述为一个可训练的稀疏化问题，使模型能够学习最优选择策略，而非遵循固定启发式规则。我们提出一种解耦路由架构。为实现训练过程中梯度通过不可微剪枝操作进行传播，我们引入一个名为SparseLearn的组件。该组件使用方差保持噪声门控，通过可微的Soft Top-K算子调节每个区块的信息流，并配合对角注意力去噪器恢复受扰动的表示，同时不泄露空间信息。在推理时，SparseLearn模块被完全丢弃，训练好的评分器应用确定性Hard Top-K算子，仅保留得分最高的32个令牌，不引入额外计算开销。通过将视觉序列压缩至仅32个令牌的稀疏集（仅占原始长度的0.78%），我们的框架在SlideBench（TCGA）上达到73.32%的总体准确率，持续超越基于采样的基线和通用视觉语言模型。该框架还在SlideBench（BCNB）和WSI VQA*上展现出强大的零样本泛化能力。通过解决视觉上下文瓶颈并防止稀疏诊断证据被稀释，本文为端到端千兆像素全切片图像推理提供了一种高效范式。

</details>


### [100] [FiberTune: Preserving Action-Fiber Visual Residuals in Vision-Language-Action Fine-Tuning](https://arxiv.org/abs/2606.08653)
*Haihao Lin,Xiangsheng Huang,Xiao Yang,Weibang Zhou,Yiqi Zhang,Bo Yang,Simin Zeng,Jiawei Yang,Zhengyang Wang,Jiahui Du*

Main category: cs.CV

TL;DR: 提出FiberTune方法，通过保持视觉残差结构来防止VLA策略微调中的视觉坍塌，在不增加推理开销的情况下提升任务成功率。

<details>
  <summary>Details</summary>

Motivation: 现有VLA策略的动作监督微调会限制与预测动作相关的方向，但允许动作等价状态下视觉结构自由坍塌（即残差视觉坍塌），损害了视觉表示的质量。

Method: FiberTune使用在线动作探针估计动作预测特征方向，从中层视觉token表示中过滤掉这些方向，然后将探针过滤后的残差与冻结的视觉教师模型对齐，同时正则化其有效秩。

Result: 在六个受控仿真设置（涵盖两个基准和两种架构pi_0.5、OpenVLA-OFT）以及真实SO-101拾取放置任务中均优于基线；CALVIN长时域任务SR(5)提升+10.7个百分点，物理SO-101成功率从72.7%提升至78.1%。

Conclusion: FiberTune通过保持教师结构的视觉残差并提升有效秩，有效缓解了VLA微调中的视觉坍塌问题，显著提升了策略性能。

Original Abstract: Action-supervised fine-tuning of vision-language-action (VLA) policies fits demonstrations effectively but constrains only the directions that change predicted actions, leaving visual structure consistent across action-equivalent states free to collapse. We formalize this as residual visual collapse along local action fibers and propose FiberTune, a training-time objective that preserves teacher-structured visual residuals without adding inference-time overhead. FiberTune uses an online action probe to estimate action-predictive feature directions, filters them from intermediate visual-token representations, and aligns the resulting probe-filtered residuals to a frozen visual teacher while regularizing their effective rank. Under identical training conditions, FiberTune improves over task-loss-only fine-tuning in every one of six controlled simulation settings spanning two benchmarks and two architectures (pi_0.5 and OpenVLA-OFT), as well as on physical SO-101 pick-place; representative gains include +10.7 percentage points SR(5) on long-horizon CALVIN ABC-to-D and physical SO-101 task success rising from 72.7% to 78.1%. Residual diagnostics show that these gains coincide with increased probe-filtered residual teacher alignment and effective rank, consistent with the action-fiber motivation.

中文摘要翻译: 动作监督下的视觉-语言-动作（VLA）策略微调能够有效地拟合 demonstrations，但仅约束改变预测动作的方向，使得动作等价状态下保持一致的视觉结构自由坍塌。我们将此现象形式化为沿局部动作纤维的残差视觉坍塌，并提出FiberTune——一种训练时目标函数，在不增加推理开销的前提下保留教师结构的视觉残差。FiberTune使用在线动作探针估计动作预测的特征方向，从中层视觉token表示中过滤掉这些方向，然后将探针过滤后的残差与冻结的视觉教师模型对齐，同时正则化其有效秩。在相同的训练条件下，FiberTune在六个受控仿真设置（涵盖两个基准和两种架构pi_0.5和OpenVLA-OFT）以及物理SO-101拾取放置任务中均优于仅使用任务损失微调的基线；代表性提升包括长时域CALVIN ABC-to-D任务SR(5)提升+10.7个百分点，物理SO-101任务成功率从72.7%提升至78.1%。残差诊断表明，这些提升与探针过滤后残差的教师对齐度和有效秩的增加相一致，验证了动作纤维动机的有效性。

</details>


### [101] [WaveDiT: Distribution-Aware Wavelet Flow Matching for Efficient 3D Brain MRI Synthesis](https://arxiv.org/abs/2606.08670)
*Danilo Danese,Angela Lombardi,Giuseppe Fasano,Matteo Attimonelli,Tommaso Di Noia*

Main category: cs.CV

TL;DR: WaveDiT提出了一种基于3D Haar离散小波变换系数空间的条件流匹配框架，通过分解式空间-深度注意力机制和基于高阶小波统计的带状异方差不确定性建模，在单GPU上实现全分辨率3D脑MRI合成，并提升了下游脑龄预测和区域解剖一致性。

<details>
  <summary>Details</summary>

Motivation: 现有全分辨率3D脑MRI合成方法要么计算成本过高，要么依赖有损潜在压缩导致解剖细节丢失，实际生成增强通常需要专用计算基础设施。因此，需要一种在有限计算资源下实现高质量全分辨率3D脑部MRI生成的实用方法。

Method: 提出WaveDiT，在3D Haar离散小波变换系数空间中运行的条件流匹配框架。模型结合了分解式空间-深度注意力机制（factorized spatio-depth attention）和基于高阶小波统计导出的带状异方差不确定性建模（band-wise heteroscedastic uncertainty modeling）。预测的对数方差直接集成到流目标函数和条件通路中，实现与解剖细节的重尾和输入相关方差结构相适应的自适应精度。

Result: 在多站点队列上的评估表明，WaveDiT在生成分布与真实MRI分布的对齐、下游脑龄预测性能和区域级解剖一致性方面均优于扩散模型、潜在模型和基于小波的基线方法。

Conclusion: WaveDiT在单GPU上实现了全分辨率3D脑MRI的实用合成，通过小波系数空间中的流匹配和异方差不确定性建模，在生成质量和下游应用效果上均超越了现有方法。

Original Abstract: Large and demographically balanced datasets are essential for reliable neuroimaging biomarkers. Full-resolution 3D brain MRI synthesis can support data augmentation in this setting, but existing approaches either incur prohibitive computational cost at volumetric scale or rely on lossy latent compression that may compromise anatomical detail. As a result, practical 3D generative augmentation often requires specialized compute infrastructure. We propose WaveDiT, a conditional flow matching framework operating in the coefficient space of a 3D Haar Discrete Wavelet Transform. The model combines factorized spatio-depth attention with band-wise heteroscedastic uncertainty modeling derived from higher-order wavelet statistics. Predicted log-variance is integrated directly into both the flow objective and conditioning pathway, enabling adaptive precision consistent with the heavy-tailed and input-dependent variance structure of anatomical detail. This formulation supports full-resolution 3D synthesis under practical memory and time constraints on a single modern GPU. Evaluation on a multi-site cohort demonstrates improved alignment between generated and real MRI distributions, together with enhanced downstream brain age prediction and region-level anatomical agreement relative to diffusion, latent, and wavelet-based baselines. Code is available at https://github.com/sisinflab/WaveDiT

中文摘要翻译: 大规模且人口统计学平衡的数据集对于可靠的神经影像学生物标志物至关重要。全分辨率3D脑部MRI合成可以在此场景下支持数据增强，但现有方法要么在体素级规模上产生过高的计算成本，要么依赖可能损害解剖细节的有损潜在压缩。因此，实用的3D生成增强通常需要专门的计算基础设施。我们提出WaveDiT，一个在3D Haar离散小波变换系数空间中运行的条件流匹配框架。该模型将分解式空间-深度注意力机制与基于高阶小波统计导出的带状异方差不确定性建模相结合。预测的对数方差直接集成到流目标函数和条件通路中，实现了与解剖细节的重尾和输入相关方差结构相一致的自适应精度。该公式支持在单个现代GPU上，在实用的内存和时间约束下进行全分辨率3D合成。在多站点队列上的评估表明，与扩散模型、潜在模型和基于小波的基线方法相比，生成分布与真实MRI分布的对齐程度有所提高，同时下游脑龄预测和区域级解剖一致性也得到增强。代码可在https://github.com/sisinflab/WaveDiT获取。

</details>


### [102] [Learning to Solve Generative ODEs Beyond the Linear Span](https://arxiv.org/abs/2606.08672)
*Sihyeon Kim,Seunghun Lee,Vikas Singh,Hyunwoo J. Kim*

Main category: cs.CV

TL;DR: 本文提出SpanLift，一种轻量级神经求解器，通过在标量系数更新基础上添加空间残差算子来突破生成模型求解器学习中的结构瓶颈（每步更新局限于速度评估的跨度空间），在保持预训练主干模型不变且不增加NFE的情况下，实现了最先进的少步采样性能。

<details>
  <summary>Details</summary>

Motivation: 现有扩散和流生成模型的求解器学习方法仅调整标量系数或时间步长，但每步更新受限于缓冲速度评估的跨度空间（span-limited），只能拟合跨度内分量而无法修正跨度外残差，这构成了结构性瓶颈。

Method: 提出SpanLift，将固定基求解器作为跨度内先验，并学习一个作用于状态和速度缓冲区的空间残差算子来增强标量系数更新。该算子通过端点教师匹配（endpoint teacher matching）训练，保持预训练主干模型不变且不增加模型NFE。

Result: 学习到的校正可跨基求解器迁移且主要位于跨度外。在像素空间扩散、潜在流匹配和降水临近预报任务中，SpanLift实现了最先进的少步采样性能。仅用3次NFE，CIFAR-10的FID从8.16提升至5.69，ImageNet的FID从17.37提升至11.83。

Conclusion: SpanLift通过引入空间残差算子有效克服了求解器学习中的跨度限制瓶颈，在保持计算效率的同时显著提升了少步采样质量，且具有良好的跨求解器迁移能力。

Original Abstract: Diffusion and flow generative models sample by integrating a learned ODE, but high quality still requires many sequential model evaluations. Solver learning reduces this cost by adapting scalar coefficients, timesteps, or both, while keeping the backbone model fixed. In this work, we identify a structural bottleneck in this update family: each step remains span-limited. Since the scalar-coefficient update lies in the span of buffered velocity evaluations, it can fit only the in-span component while leaving any out-of-span residual unreachable by scalar recombination alone. We propose SpanLift, a lightweight neural solver that augments scalar-coefficient updates with a spatial residual operator. SpanLift keeps a fixed base solver as an in-span prior and learns a spatial residual operator over the state and velocity buffer. The operator is trained by endpoint teacher matching, preserves the pretrained backbone, and adds no model NFEs. Empirically, the learned correction transfers across base solvers and is predominantly out-of-span. Across pixel-space diffusion, latent flow matching, and precipitation nowcasting, SpanLift achieves state-of-the-art few-step sampling. With only 3 NFE, it improves CIFAR-10 FID from 8.16 to 5.69 and ImageNet FID from 17.37 to 11.83.

中文摘要翻译: 扩散和流生成模型通过对学习到的ODE进行积分来采样，但高质量采样仍需要大量顺序模型评估。求解器学习通过调整标量系数、时间步长或两者同时调整来降低这一成本，同时保持主干模型固定。在这项工作中，我们识别出这种更新家族中的一个结构性瓶颈：每一步的更新仍然受限于跨度空间。由于标量系数更新处于缓冲速度评估的跨度空间中，它只能拟合跨度内分量，而任何跨度外残差都无法通过标量重组单独达到。我们提出了SpanLift，一种轻量级神经求解器，它用空间残差算子增强标量系数更新。SpanLift将固定的基求解器作为跨度内先验，并学习一个作用于状态和速度缓冲区的空间残差算子。该算子通过端点教师匹配训练，保留预训练的主干模型，且不增加模型NFE。实验表明，学习到的校正可跨基求解器迁移，并且主要位于跨度外。在像素空间扩散、潜在流匹配和降水临近预报任务中，SpanLift实现了最先进的少步采样性能。仅用3次NFE，它就将CIFAR-10的FID从8.16提升至5.69，ImageNet的FID从17.37提升至11.83。

</details>


### [103] [BioVid: Autoregressive Video Generation with Biological Behavior Semantic Comprehension](https://arxiv.org/abs/2606.08674)
*Tsung-Wei Pan,Jung-Hua Wang*

Main category: cs.CV

TL;DR: BioVid 提出了一种数据驱动的自回归视频生成框架，通过学习生物行为的自然时间长度分布，使用 FSQ-R3GAN 分词器和因果 Transformer 实现可变时长视频生成，在人类饮酒行为数据集上达到1.24的 Wasserstein-1 距离，优于固定长度基线（6.05）和 VideoGPT（15.48）。

<details>
  <summary>Details</summary>

Motivation: 现有视频生成框架将时长视为外部预设参数（固定帧数或文本提示），生成的片段时间边界与真实行为数据的统计结构脱节，这与生物行为的自然时长变异性质不匹配。生物行为中动作时长在个体和实例间自然变化，且这种信息编码在数据本身中，因此需要一种能直接从数据中学习时间结构（包括自然时长分布）的视频生成框架。

Method: 两阶段框架：第一阶段使用 FSQ-R3GAN 分词器将每帧编码为紧凑离散表示，结合 R3GAN 的稳定相对论训练目标和 FSQ 的保证码本利用率实现高保真空间重建且无码本崩溃；第二阶段使用因果 Transformer 对生成的 token 序列进行自回归建模，学习在行为事件达到语义闭合时发射序列结束（EOS）token，终止分布从训练数据中自然涌现而非人为指定。

Result: 在 NTU RGB+D 人类饮酒行为数据集（A001，n=94）上：BioVid 生成的长度分布与保留测试数据高度匹配，Wasserstein-1 距离为1.24，而固定长度基线为6.05，VideoGPT 为15.48，同时保持了具有竞争力的空间保真度。

Conclusion: 数据驱动的自回归视频生成框架能够有效学习生物行为的自然时间结构和长度分布，生成与真实数据更为一致的变长视频序列，显著优于固定长度和现有视频生成基线方法。

Original Abstract: Existing video generation frameworks treat sequence duration as an externally prescribed parameter -- fixed frame counts or text prompts -- producing clips whose temporal boundaries are decoupled from the statistical structure of real behavioral data. This assumption is fundamentally misaligned with biological behavior, where action duration varies naturally across individuals and instances and is encoded in the data itself. We present BioVid, a data-driven autoregressive video generation framework that learns the temporal structure of biological behaviors directly from training data, including their natural length distributions. In the first stage, a Finite Scalar Quantization GAN (FSQ-R3GAN) tokenizer encodes each video frame into a compact discrete representation, combining the stabilized relativistic training objective of R3GAN with FSQ's guaranteed codebook utilization to achieve high-fidelity spatial reconstruction without codebook collapse. In the second stage, a causal Transformer models the resulting token sequences autoregressively and learns to emit an End-of-Sequence (EOS) token when the behavioral event reaches semantic closure, with the termination distribution emerging naturally from the training data rather than any human-specified constraint. Experiments on a human drinking behavior dataset (NTU RGB+D, A001, n=94) demonstrate that BioVid's generated length distribution closely matches that of held-out test data, achieving a Wasserstein-1 distance of 1.24 against the ground truth -- compared to 6.05 for a fixed-length baseline and 15.48 for VideoGPT -- while maintaining competitive spatial fidelity.

中文摘要翻译: 现有的视频生成框架将序列时长视为外部指定的参数——即固定的帧数或文本提示——从而生成时间边界与真实行为数据的统计结构相脱节的片段。这一假设从根本上与生物行为不符，因为生物行为中动作时长在个体和实例间自然变化，且该信息编码在数据本身中。我们提出了 BioVid，一个数据驱动的自回归视频生成框架，直接从训练数据中学习生物行为的时间结构，包括其自然长度分布。在第一阶段，有限标量量化生成对抗网络（FSQ-R3GAN）分词器将每一帧视频编码为紧凑的离散表示，结合了 R3GAN 的稳定相对论训练目标与 FSQ 的保证码本利用率，在不发生码本崩溃的前提下实现高保真空间重建。在第二阶段，因果 Transformer 对生成的 token 序列进行自回归建模，并学习在行为事件达到语义闭合时发射序列结束（EOS）token，其中终止分布从训练数据中自然涌现，而非由任何人指定的约束决定。在人类饮酒行为数据集（NTU RGB+D，A001，n=94）上的实验表明，BioVid 生成的长度分布与保留测试数据高度匹配，Wasserstein-1 距离达到1.24，相比之下固定长度基线为6.05，VideoGPT 为15.48，同时保持了具有竞争力的空间保真度。

</details>


### [104] [Distortion-Aware PETR for BEV Object Detection with Mixed Pinhole-Fisheye Cameras](https://arxiv.org/abs/2606.08680)
*Xiangzhong Liu*

Main category: cs.CV

TL;DR: 本文提出DAPETR，一种面向混合针孔-鱼眼相机设置的无投影3D目标检测器，通过可学习的畸变感知位置编码和双向特征-几何协同调制模块，在KITTI-360基准上超越现有方法，并揭示了可学习适应与显式几何重参数化之间的负面互作。

<details>
  <summary>Details</summary>

Motivation: 鱼眼相机成本低、视场全覆盖，但严重的径向畸变破坏了大多数BEV检测器依赖的均匀采样假设，导致其潜力在3D目标检测中未被充分利用。

Method: 提出DAPETR检测器，包含两个关键的可学习自适应模块：(1) 统一畸变感知位置嵌入，将位置编码与鱼眼几何协调一致；(2) 双向特征-几何协同调制模块，实现图像特征与3D位置编码的相互自适应。

Result: 在转换后的KITTI-360基准上，DAPETR优于极坐标下的PETR基线；同时发现可学习适应与显式几何重参数化（极坐标）两种策略结合时存在负面互作，导致性能下降。

Conclusion: DAPETR显著推进了鱼眼BEV检测研究，为除图像校正之外的有效畸变感知3D感知设计提供了关键见解。

Original Abstract: Fisheye cameras are widely deployed in autonomous driving perception suites for their low cost and full-coverage field of view (FOV), yet their potential remains underleveraged in 3D object detection. Severe radial distortion challenges most BEV detectors by violating the fundamental assumption of uniform sampling. To bridge this gap, we propose Distortion-Aware PETR (DAPETR), a projection-free detector tailored for mixed pinhole-fisheye camera setups. DAPETR incorporates two key learned-adaptive modules: a unified distortion-aware positional embedding that harmonizes positional encodings for image representations with fisheye geometry, and a bidirectional feature-geometry co-modulation module that mutually adapts image features and 3D positional embeddings. In our experiments on a converted KITTI-360 benchmark, we systematically compare our learned adaptive approach against PETR in polar coordinates (PolarPETR). We find that while both methods improve over the baseline, our learned modules achieve superior performance. Crucially, we uncover a negative interaction when combining both strategies, revealing that learned adaptation and explicit geometric reparameterization can conflict. Our final DAPETR model significantly advances the research and benchmark for fisheye BEV detection, providing critical insights into effective distortion-aware 3D perception design other than image rectification.

中文摘要翻译: 鱼眼相机因其低成本和全覆盖视场而被广泛部署于自动驾驶感知套件中，但其在3D目标检测中的潜力仍未得到充分利用。严重的径向畸变破坏了大多数BEV检测器所依赖的均匀采样基本假设，导致其性能受限。为弥补这一差距，我们提出无投影检测器DAPETR（畸变感知PETR），专为混合针孔-鱼眼相机设置设计。DAPETR包含两个关键的可学习自适应模块：一个统一的畸变感知位置嵌入，将图像表示的位置编码与鱼眼几何协调一致；以及一个双向特征-几何协同调制模块，实现图像特征与3D位置编码的相互自适应。在转换后的KITTI-360基准上的实验中，我们系统地将我们的可学习自适应方法与极坐标下的PETR（PolarPETR）进行了比较。我们发现，两种方法均优于基线，而我们的可学习模块取得了更优的性能。关键的是，我们发现了两种策略结合时的负面互作，揭示了可学习适应与显式几何重参数化之间可能存在冲突。我们的最终DAPETR模型显著推进了鱼眼BEV检测的研究和基准，为除图像校正之外的有效畸变感知3D感知设计提供了关键见解。

</details>


### [105] [BLUE: Toward Better Language Use in Efficient Vision-Language-Action Models for Autonomous Driving](https://arxiv.org/abs/2606.08684)
*George Ling,Lijin Yang,Hao Yang,Zhongzhan Huang*

Main category: cs.CV

TL;DR: BLUE提出一种轻量门控机制，通过冻结VLA模型隐状态来逐帧决策是否激活语言生成，在保持语言优势的同时实现2.54倍加速和最高76.2%的成功率。

<details>
  <summary>Details</summary>

Motivation: 现有VLA模型每帧都生成语言，但语言仅在少数路线上有益，造成大量无效计算；而场景复杂度和运动学特征难以预测哪些帧需要语言。

Method: 在冻结的VLA模型隐状态上训练一个0.11M参数的轻量门控网络，逐帧判断是否激活语言生成；若不需要语言则直接预测动作，无需修改骨干网络或额外人工标注。

Result: 在Bench2Drive上达到76.2%成功率，在Longest6 v2上达到36驾驶分数；相比骨干网络实现2.54倍推理加速和8.9%成功率提升，且仅需0.11M参数的门控。

Conclusion: VLA模型可以通过逐帧选择性使用语言来大幅降低成本，BLUE提供了高效语言增强自动驾驶的实用路径。

Original Abstract: We present BLUE, a minimal method for better language use in vision-language-action (VLA) models for autonomous driving (AD). Through extensive analysis, we reveal that language matters on only a small fraction of routes, but on those routes it can greatly improve or degrade performance. Generating language at every frame is therefore inefficient, since most computation is spent on frames that do not benefit from language. We further show that pretrained VLA hidden states potentially already encode whether language will benefit a given frame, even though scene complexity and kinematic features alone struggle to predict this. Based on this finding, BLUE trains a lightweight gate on frozen VLA hidden states to decide per frame whether to activate language generation or predict actions directly, without modifying the backbone or requiring additional human annotation. With just a 0.11M-parameter gate, BLUE sets a new state of the art on both benchmarks, achieving 76.2% success rate on Bench2Drive and 36 driving score on Longest6 v2, while delivering 2.54x inference speedup and 8.9% success rate improvement over the backbone. BLUE provides a practical path toward efficient language-augmented AD, showing that VLA models can retain the benefits of language at a fraction of the cost. Our code, data, logs and checkpoints are fully available on https://github.com/George-Ling3/BLUE.

中文摘要翻译: 我们提出BLUE，一种用于提升自动驾驶视觉-语言-动作（VLA）模型中语言运用效率的极简方法。通过大量分析，我们发现语言仅在少数路线上有价值，但在这些路线上它能显著提升或降低性能。因此，每帧都生成语言是低效的，因为大部分计算消耗在那些无法从语言中受益的帧上。我们进一步表明，预训练VLA模型的隐状态已经编码了语言是否对当前帧有益的信息，而场景复杂度和运动学特征本身难以预测这一点。基于这一发现，BLUE在冻结的VLA隐状态上训练一个轻量门控网络，逐帧决策是激活语言生成还是直接预测动作，无需修改骨干网络或额外人工标注。仅凭一个0.11M参数的门控，BLUE在两个基准上均达到了新的最优水平：在Bench2Drive上实现76.2%的成功率，在Longest6 v2上获得36的驾驶分数，同时相比骨干网络实现2.54倍推理加速和8.9%成功率提升。BLUE为高效的语言增强自动驾驶提供了一条实用路径，表明VLA模型能够以极低的成本保留语言带来的优势。我们的代码、数据、日志和模型权重已完全开源在https://github.com/George-Ling3/BLUE。

</details>


### [106] [Shift-Dependent Asymmetry: Orthogonal Inverse Low-Rank Adaptation for Federated Medical Segmentation](https://arxiv.org/abs/2606.08687)
*Xingyue Zhao,Wenke Huang,Linghao Zhuang,Haoran Wu,Anwen Jiang,Zhifeng Wang,Wenwen He,Ming Feng,Mang Ye,Bo Xu*

Main category: cs.CV

TL;DR: 针对联邦LoRA在医学图像分割中编码器-解码器不对称导致泛化差的问题，提出逆非对称调优（IAT）与子空间正交正则化器，通过个性化模块级组件和约束共享-局部更新方向共线性，有效缓解站点特异性偏差泄露，提升联邦分割性能。

<details>
  <summary>Details</summary>

Motivation: 现有联邦LoRA方法采用统一聚合规则，无法处理医学分割中编码器（受外观偏移主导）与解码器（受监督差异主导）之间的不对称性，导致共享解剖结构与站点特异性偏差纠缠，损害泛化能力。

Method: 提出逆非对称调优（IAT）：(1) 在编码器中个性化模块级组件以吸收外观偏移，在解码器中个性化组件以适配站点依赖的监督，同时保留共享通路传递可迁移共识；(2) 引入子空间正交正则化器，惩罚有效更新空间中共享方向与局部方向的共线性，缓解LoRA双线性参数化下的站点特异性泄漏，且无需额外通信开销。

Result: 在多个联邦医学图像分割任务上，IAT一致优于强联邦LoRA基线及其他参数高效联邦学习方法。

Conclusion: 通过对编码器-解码器不对称性的针对性设计以及子空间正交约束，IAT有效解耦了共享解剖与站点偏差，提升了联邦医学图像分割的泛化性能。

Original Abstract: Low-Rank Adaptation (LoRA) enables efficient federated fine-tuning of segmentation foundation models for medical imaging. However, most federated LoRA methods adopt a uniform aggregation rule, which breaks under the encoder-decoder asymmetry in medical segmentation: the encoder is dominated by appearance shifts, while the decoder is dominated by supervision variations. This mismatch entangles shared anatomy with site-specific biases and harms generalization. To address this, we propose Inverse Asymmetric Tuning (IAT). IAT aligns adaptation with heterogeneity sources by personalizing module-specific components in the encoder to absorb appearance shifts and in the decoder to accommodate site-dependent supervision, while retaining a shared pathway for transferable consensus. However, structural separation alone is insufficient under LoRA's bilinear parameterization, where multiplicative coupling can still cause site-specific updates to leak into the shared direction. We therefore introduce a Subspace Orthogonality Regularizer that penalizes shared-local collinearity in the effective update space, mitigating leakage without extra communication. Experiments show consistent improvements over strong federated LoRA and parameter-efficient FL baselines.

中文摘要翻译: 低秩适配（LoRA）能够实现医学影像分割基础模型的高效联邦微调。然而，大多数联邦LoRA方法采用统一的聚合规则，这在医学分割的编码器-解码器不对称性下失效：编码器主要受外观偏移影响，而解码器主要受监督差异影响。这种错配将共享的解剖结构与站点特异性偏差纠缠在一起，损害了泛化能力。为解决这一问题，我们提出逆非对称调优（IAT）。IAT通过将异质性来源与适配对齐：在编码器中个性化模块级组件以吸收外观偏移，在解码器中个性化组件以适应站点依赖的监督，同时保留用于传递可迁移共识的共享通路。然而，在LoRA的双线性参数化下，仅靠结构分离是不够的，因为乘法耦合仍可能导致站点特异性更新泄漏到共享方向中。因此，我们引入子空间正交正则化器，惩罚有效更新空间中共享方向与局部方向的共线性，在不增加额外通信开销的情况下缓解泄漏。实验表明，IAT在强联邦LoRA和参数高效联邦学习基线上取得了一致的改进。

</details>


### [107] [PRPO: Perception-Reinforced Policy Optimization via Token-Level Dynamic Advantage Reshaping](https://arxiv.org/abs/2606.08708)
*Qiming Li,Tianlun Li,Xiaolong Cheng,Hangyu Li,Ruiyan Gong,Kangning Niu,Kaitao Jiang,Mu Xu*

Main category: cs.CV

TL;DR: PRPO是一种基于token级强化学习的框架，通过识别和增强多模态推理轨迹中的关键感知token，解决了现有RLVR方法粗粒度信用分配与视觉推理不匹配的问题，在7个多模态推理基准上平均提升21-23%。

<details>
  <summary>Details</summary>

Motivation: 现有RLVR方法仅在轨迹层面分配奖励信号，导致所有生成token获得相同的学习信号，无法区分真正依赖于视觉证据的关键感知token与受语言先验或推理模板支配的token，使得前者监督信号弱，影响了多模态推理性能。

Method: 提出PRPO框架，包含两个核心组件：(1) 鲁棒视觉依赖（RVD）指标，用于识别既依赖视觉信息又对扰动稳定的token；(2) 感知优势重塑（PAR）技术，在token层面放大感知信息丰富的token的信用分配信号，同时对非感知token保持稳定梯度。

Result: 在7个多模态推理基准上，PRPO在3B和7B模型规模下分别平均提升23.3%和21.1%，取得了最先进的结果，同时训练效率更高、跨任务泛化能力更强。

Conclusion: 细粒度的token级信用分配对于可扩展的多模态强化学习至关重要，精确识别和增强关键感知token能显著提升LVLM的推理能力。

Original Abstract: Reinforcement Learning with Verifiable Rewards (RLVR) has become an effective paradigm for improving the reasoning capability of Large Vision-Language Models (LVLMs). However, existing RLVR methods primarily rely on trajectory-level outcome rewards, which assign identical learning signals across all generated tokens. This coarse-grained credit assignment is fundamentally mismatched to multimodal reasoning, where only a sparse subset of tokens is causally grounded in visual evidence. Consequently, these pivotal perceptual tokens receive weak supervision and are often overwhelmed by language priors or reasoning-template tokens. To address this limitation, we propose Perception-Reinforced Policy Optimization (PRPO), a token-level reinforcement learning framework that explicitly identifies and reinforces pivotal perceptual tokens within long-horizon multimodal reasoning trajectories. PRPO introduces Robust Visual Dependency (RVD), a principled metric that identifies tokens whose predictions are both visually grounded and perturbation-stable, filtering out brittle or noisy visual tokens. Based on RVD, we further propose Perceptual Advantage Reshaping (PAR), a token-level credit assignment technique that amplifies perceptually informative tokens while preserving stable gradients for non-perceptual tokens. Extensive experiments on seven multimodal reasoning benchmarks demonstrate that PRPO consistently outperforms strong LVLM baselines across both 3B and 7B model scales, achieving average gains of 23.3% and 21.1%, respectively. PRPO achieves state-of-the-art performance with improved training efficiency and stronger cross-task generalization. Our findings highlight the importance of fine-grained credit assignment for scalable multimodal reinforcement learning.

中文摘要翻译: 基于可验证奖励的强化学习（RLVR）已成为提升大型视觉-语言模型（LVLMs）推理能力的有效范式。然而，现有的RLVR方法主要依赖轨迹级别的结果奖励，为所有生成的token分配相同的学习信号。这种粗粒度的信用分配从根本上与多模态推理不匹配，因为在多模态推理中，只有稀疏的token子集在因果上依赖于视觉证据。因此，这些关键的感知token受到弱监督，往往被语言先验或推理模板token所淹没。为了解决这一局限性，我们提出了感知增强策略优化（PRPO），一种token级别的强化学习框架，它能够显式地识别并增强长时程多模态推理轨迹中的关键感知token。PRPO引入了鲁棒视觉依赖（RVD），这是一个原则性的度量指标，用于识别其预测既具有视觉基础又对扰动稳定的token，从而过滤掉脆弱或噪声大的视觉token。基于RVD，我们进一步提出了感知优势重塑（PAR），一种token级别的信用分配技术，它在放大感知信息丰富的token的同时，为非感知token保留稳定的梯度。在七个多模态推理基准上的大量实验表明，PRPO在3B和7B模型规模下均一致优于强LVLM基线，分别实现了23.3%和21.1%的平均增益。PRPO在提升训练效率和跨任务泛化能力的同时，达到了最先进的性能。我们的研究结果突显了细粒度信用分配对于可扩展的多模态强化学习的重要性。

</details>


### [108] [Thinking Without Images: Internalizing Visual Manipulation with On-Policy Self-Distillation](https://arxiv.org/abs/2606.08719)
*Yishuo Cai,Jiahui Liu,Yuanxin Liu,Haobo Deng,Linli Yao,Yuhao Zheng,Kun Ouyang,Zhimo Li,Ziyue Wang,Xu Sun,Haoli Bai,Xiaohui Li*

Main category: cs.CV

TL;DR: 提出Imagine-OPD框架，通过“用想象思考”内化“用图像思考”的推理能力，在不调用外部工具的情况下实现细粒度视觉推理，显著降低推理开销。

<details>
  <summary>Details</summary>

Motivation: “用图像思考”虽能通过显式放大相关区域进行细粒度视觉推理，但存在冗余工具调用和更长推理轨迹的问题，且仅从结果奖励学习时中间步骤可能带有噪声。因此希望将这种推理能力内化为模型自身的想象过程。

Method: 提出Imagine-OPD，一种在策略自蒸馏框架：训练时教师扮演“用图像思考”的推理者（获得特权放大的证据视图），监督模型自身的想象推理轨迹；无需外部教师或高质量的想象演示。

Result: 在视觉中心基准上达到最佳平均性能，同时相比“用图像思考”方法显著降低推理开销。

Conclusion: 通过自蒸馏将外部工具辅助的推理能力内化为模型的内部想象过程，可以在保持推理性能的同时大幅减少推理时的计算成本。

Original Abstract: ''Thinking with Images'' has emerged as an effective paradigm for fine-grained visual reasoning: by explicitly zooming into relevant regions and reasoning over crops, models can access local evidence that is difficult to recover from a single global image. However, this benefit comes with redundant tool invocations and longer inference traces. Moreover, when such behaviors are learned mainly from outcome reward, the resulting intermediate crops or visual cues can be noisy or fail to faithfully capture task-relevant visual evidence. In this work, we ask whether the reasoning benefits of ''Thinking with Images'' can be internalized through Thinking with Imagination: an internal process that decides where to look and imagines what visual cues closer inspection would reveal without actually invoking tools. We propose Imagine-OPD, an on-policy self-distillation framework in which a teacher plays the role of a ''Thinking with Images'' reasoner during training: it receives privileged zoomed evidence views derived from annotated regions, and supervises the model's own imagination reasoning trajectories. Imagine-OPD does not require an external teacher or high-quality imagination demonstrations. Experiments on vision-centric benchmarks show that Imagine-OPD achieves the best average performance among compared models while significantly reducing inference overhead compared with ''Thinking with Images'' methods.

中文摘要翻译: “用图像思考”已成为细粒度视觉推理的一种有效范式：通过显式放大相关区域并对裁剪区域进行推理，模型可以获取从单一全局图像中难以恢复的局部证据。然而，这种优势伴随着冗余的工具调用和更长的推理轨迹。此外，当此类行为主要从结果奖励中学习时，产生的中间裁剪区域或视觉线索可能带有噪声，或未能忠实捕捉任务相关的视觉证据。在这项工作中，我们探究“用图像思考”的推理优势是否可以通过“用想象思考”——一种决定关注何处并想象更仔细检查会揭示何种视觉线索的内部过程（无需实际调用工具）——来内化。我们提出Imagine-OPD，一种在策略自蒸馏框架，其中教师模型在训练时扮演“用图像思考”的推理者：它接收源自标注区域的特权放大的证据视图，并监督模型自身的想象推理轨迹。Imagine-OPD不需要外部教师或高质量的想象演示。在视觉中心基准上的实验表明，Imagine-OPD在比较模型中取得了最佳平均性能，同时与“用图像思考”方法相比显著降低了推理开销。

</details>


### [109] [AUCp: Pseudo-AUC for Inference Model Selection with Unlabeled Validation Data in Abnormality Detection](https://arxiv.org/abs/2606.08742)
*Md Mahfuzur Rahman Siddiquee,Fazle Rafsani,Jay Shah,Teresa Wu,Catherine D Chong,Todd J Schwedt,Baoxin Li*

Main category: cs.CV

TL;DR: 本文提出AUCp，一种无需标注验证集即可在无监督/自监督异常检测中选择最优模型的新指标，通过将测试集所有样本视为正例计算AUC来评估检测性能，实验证明其优于传统指标。

<details>
  <summary>Details</summary>

Motivation: 现有无监督/自监督异常检测方法虽不依赖标注训练集，但仍需标注验证集从多次训练迭代中选择最佳推理模型，而获取标注数据耗时且在许多疾病中不可行。本文旨在消除这一瓶颈。

Method: 提出AUCp指标：将测试集中所有未标注样本的伪标签设为异常/正类，通过传统AUC计算方式得到AUCp分数，用于无监督和自监督异常检测方法中的模型选择。

Result: 在神经系统疾病检测任务中，使用两种无监督方法和多种数据集上的自监督方法进行实验，结果表明AUCp分数能够有效识别最优推理模型，显著提升异常与疾病检测性能，且优于传统指标。

Conclusion: AUCp是一种有效的无监督/自监督异常检测模型选择指标，无需标注数据即可提升检测性能。在给定足够大且具代表性的正常样本训练集时，基于AUCp的模型选择在数学和实证上均优于传统指标。

Original Abstract: Abnormality detection is a crucial yet challenging task in medical image analysis. Distinguishing abnormalities from normal data by learning to reconstruct normal-only data alleviates the reliance on labeled datasets. However, many studies, even if unsupervised, rely on a labeled validation set to select the best model for inference from multiple training iterations. For many diseases labeled data are unavailable and substantially time consuming to obtain. To address this, AUCp - a novel metric that supports abnormality detection for unsupervised and self-supervised methods is proposed. Instead of evaluating the realism of reconstructed images to select the best of model for inference, it focuses on actual detection performance and without requiring an annotated test set. Assuming the pseudo ground truth of all unannotated samples in the test set as abnormal/positive and using traditional AUC calculation, AUCp scores are derived. Given a large and representative training set of normal samples, we show mathematical and empirical evidence that model selection using AUCp scores improves disease detection in terms of unsupervised and self-supervised methods over conventional metrics. Using two unsupervised methods for neurologic disease detection and self-supervised methods on diverse datasets, our results demonstrate that the AUCp score effectively identifies the optimal model for inference, significantly enhancing abnormality and disease detection. The corresponding implementations are available in https://github.com/mahfuzmohammad/AUCp.

中文摘要翻译: 异常检测是医学图像分析中一项至关重要但具有挑战性的任务。通过学习仅重构正常数据来区分异常与正常数据，减少了对标注数据集的依赖。然而，许多研究即使采用无监督方法，仍需要依赖标注验证集从多次训练迭代中选择最佳推理模型。对于许多疾病而言，标注数据不可获取且获取过程极其耗时。为解决这一问题，本文提出AUCp——一种支持无监督和自监督方法异常检测的新型评价指标。该指标不依赖于评估重构图像的逼真度来选择最佳推理模型，而是聚焦于实际的检测性能，且无需标注测试集。通过将测试集中所有未标注样本的伪标签设为异常/正类，并采用传统AUC计算方法，得到AUCp分数。在给定足够大且具代表性的正常样本训练集条件下，我们从数学和实证两方面证明，基于AUCp分数的模型选择相较于传统指标能够提升无监督和自监督方法的疾病检测性能。通过将两种无监督方法应用于神经系统疾病检测，并在多样化数据集上采用自监督方法，我们的结果表明，AUCp分数能够有效识别最优推理模型，显著增强异常与疾病检测能力。相关实现代码可在https://github.com/mahfuzmohammad/AUCp获取。

</details>


### [110] [MB-Loc: Multi-planar Bird's-eye-view Localization in outdoor LiDAR scenes](https://arxiv.org/abs/2606.08744)
*Ayaan Choudhury,Preet Savalia,Anirudh Pydah,Avinash Sharma*

Main category: cs.CV

TL;DR: MB-Loc提出了一种轻量级且对视角鲁棒的场景坐标回归（SCR）框架，通过将LiDAR点云投影为2.5D多平面鸟瞰图（BEV）表示，结合KL正则化隐层瓶颈和3D空间增强，在NCLT数据集上实现了超越当前最先进方法的实时定位性能，并显著提升了计算效率。

<details>
  <summary>Details</summary>

Motivation: 现有基于场景坐标回归（SCR）的LiDAR全局定位方法存在两大瓶颈：一是处理原始3D几何结构时计算效率极低，二是在传感器视角变化下性能显著下降。本文旨在解决这些问题，实现轻量且视角鲁棒的定位。

Method: 1) 将输入LiDAR扫描投影为2.5D多平面鸟瞰图（BEV）表示，沿Z轴切片并将有符号深度映射到离散2D平面，保留3D几何结构的同时利用标准2D CNN的高效计算能力；2) 引入KL正则化的隐层瓶颈，显式建模空间不确定性而不注入随机噪声；3) 在平面投影前应用3D空间增强，使网络隐式学习视角不变特征。

Result: 在公开NCLT数据集上的实验表明，MB-Loc超越了当前最先进方法，能以实时推理速度运行，且在计算效率上显著优于传统3D-SCR架构。

Conclusion: MB-Loc通过2.5D BEV投影、KL正则化隐层瓶颈和3D空间增强，成功解决了SCR框架的计算效率低下和视角敏感性问题，实现了实时、高精度且视角鲁棒的LiDAR全局定位。

Original Abstract: Global LiDAR localization is a fundamental task for autonomous navigation systems. Recent methods perform Scene Coordinate Regression (SCR) and achieve superior accuracy over Absolute Pose Regression (APR) solutions by predicting dense 3D world coordinates. However, SCR approaches introduce two major bottlenecks: severe computational inefficiency from processing raw 3D geometries and significant performance degradation under varying sensor viewpoints. To address these limitations, we present MB-Loc, a lightweight and viewpoint-robust SCR framework. Instead of relying on heavy 3D convolutions, we project the input LiDAR scan into a 2.5D Multi-planar Bird's-Eye View (BEV) representation. By slicing the point-cloud along the Z-axis and mapping signed depths into discrete 2D planes, MB-Loc retains essential 3D geometric structures while exploiting the computational tractability of standard 2D CNNs. To handle the inherent sparsity of outdoor LiDAR, we introduce a KL-regularized latent bottleneck that explicitly models spatial uncertainty without injecting stochastic noise. Finally, to ensure rotation robustness, we apply 3D spatial augmentations prior to planar projection, forcing the network to implicitly learn viewpoint-invariant features. We perform extensive experiments on the publicly available NCLT dataset and demonstrate that our proposed method outperforms the current state-of-the-art. Operating at real-time inference speeds, MB-Loc significantly outperforms traditional 3D-SCR architectures in computational efficiency.

中文摘要翻译: 全局LiDAR定位是自主导航系统的一项基础任务。近期方法通过执行场景坐标回归（SCR）并预测密集的3D世界坐标，取得了优于绝对姿态回归（APR）方法的精度。然而，SCR方法存在两大瓶颈：处理原始3D几何结构时计算效率极低，以及在传感器视角变化下性能显著下降。为解决这些限制，我们提出了MB-Loc——一种轻量级且对视角鲁棒的SCR框架。我们不依赖庞大的3D卷积，而是将输入LiDAR扫描投影为2.5D多平面鸟瞰图（BEV）表示。通过沿Z轴对点云进行切片并将有符号深度映射到离散的2D平面，MB-Loc在保留关键3D几何结构的同时，充分利用了标准2D CNN的计算便捷性。为处理室外LiDAR固有的稀疏性，我们引入了一个KL正则化的隐层瓶颈，在不注入随机噪声的情况下显式建模空间不确定性。最后，为确保旋转鲁棒性，我们在平面投影之前应用3D空间增强，迫使网络隐式学习视角不变特征。我们在公开的NCLT数据集上进行了大量实验，结果表明我们提出的方法优于当前最先进的方法。MB-Loc以实时推理速度运行，在计算效率上显著超越了传统的3D-SCR架构。

</details>


### [111] [Stain-Aware Wavelet Regularization for Instant Adversarial Purification in Histopathology](https://arxiv.org/abs/2606.08745)
*Zhe Li,Bernhard Kainz*

Main category: cs.CV

TL;DR: 提出了一种基于Haar小波变换的染液感知小波正则化（SAWR）方法，用于净化组织病理图像中的对抗扰动，在不损失纹理和频谱保真度的前提下提升鲁棒性达10.69%。

<details>
  <summary>Details</summary>

Motivation: 深度学习在计算病理学中广泛应用，但神经网络对对抗扰动敏感，而组织病理图像中高频对抗噪声与细微诊断相关组织结构难以区分，现有方法无法有效解耦两者，亟需一种能保留诊断结构信息的同时去除对抗扰动的方法。

Method: 提出染液感知小波正则化（SAWR），基于Haar变换进行多级小波域正则化，分层解耦对抗扰动与诊断结构信息；进一步将频谱约束扩展到单个组织学通道，实现对苏木精和伊红染液通道的特异性频率调控。

Result: 在即时净化框架下，SAWR相较于基线方法将对抗鲁棒性提升高达10.69%，同时保持纹理和频谱保真度。

Conclusion: SAWR通过小波域正则化与染液感知频率调控，有效净化对抗扰动，提升了组织病理学中深度学习模型的可靠性与安全性。

Original Abstract: Deep learning has become prevalent in computational pathology pipelines that support tasks such as cancer screening and digital pathology analysis. However, the susceptibility of neural networks to adversarial perturbations raises safety concerns for reliable deployment in clinical practice. In histopathological images, this challenge is exacerbated by the difficulty of distinguishing high-frequency adversarial noise from subtle and diagnostically relevant tissue structures. To address this issue, we propose Stain-Aware Wavelet Regularization (SAWR), an adversarial purification framework that leverages multi-level wavelet-domain regularization based on Haar transform to hierarchically disentangle adversarial perturbations from diagnostic structural information. This spectral constraint is further extended to individual histological channels, enabling stain-specific frequency regulation consistent with the biological properties of Hematoxylin and Eosin. When integrated into an instant purification framework, SAWR improves adversarial robustness by up to 10.69\% over the baseline approach, while maintaining texture and spectral fidelity under adversarial perturbations.

中文摘要翻译: 深度学习已广泛应用于支持癌症筛查和数字病理分析等任务的计算病理学流程中。然而，神经网络对对抗扰动的敏感性给其在临床实践中的可靠部署带来了安全隐患。在组织病理学图像中，由于难以区分高频对抗噪声与细微且具有诊断价值的组织结构，这一挑战更加严峻。为解决这一问题，我们提出染液感知小波正则化（Stain-Aware Wavelet Regularization, SAWR），一种基于Haar变换的多级小波域正则化对抗净化框架，能够分层解耦对抗扰动与诊断结构信息。该频谱约束进一步扩展到单个组织学通道，实现了与苏木精和伊红生物染色特性相一致的染液特异性频率调控。当集成到即时净化框架中时，SAWR相较于基线方法将对抗鲁棒性提升高达10.69%，同时在对adversarial perturbations下保持纹理和频谱保真度。

</details>


### [112] [Less Is More: Training-Free Acceleration Framework of 3D Diffusion Models for Low-Count PET Denoising via Global-Local Trajectory Reduction](https://arxiv.org/abs/2606.08751)
*Yuhan Liu,Scott M. Leonard,Marlee Crews,Muhannad Fadhel,Jinkui Hao,Tianqi Chen,Ryan J. Avery,Bo Zhou*

Main category: cs.CV

TL;DR: 提出一种无需训练的全局-局部跳跃策略（Global-Local Skipping Strategy），加速基于扩散模型的3D PET去噪，在不重新训练的前提下显著降低推理步数和计算量，实现数量级加速且保持或提升重建质量。

<details>
  <summary>Details</summary>

Motivation: 低计数PET可减少辐射剂量和扫描时间，但图像质量下降。基于扩散模型的恢复方法虽有效，但其迭代采样过程在高分辨率3D PET体数据上计算代价极高、推理延迟大，限制了临床部署。

Method: 提出一种即插即用的全局-局部跳跃策略：(i) 全局去噪步跳跃——利用噪声一致性变换将低计数PET输入映射到中间去噪步，从而大幅减少所需去噪步数；(ii) 局部特征复用捷径——在相邻去噪步间复用变化缓慢的高层U-Net特征，减少每步计算量，同时保持图像保真度。

Result: 在18F-FDG PET、68Ga-DOTATATE PET和18F-PSMA PET等多个示踪剂的内外部数据集上，实现超过一个数量级的加速，重建性能相比全步基线改善或相当。盲法读者研究进一步证实了临床信心和感知诊断质量的提升。

Conclusion: 所提方法无需重新训练或修改模型架构，即可显著加速扩散模型在3D PET去噪中的推理过程，同时保持甚至提升重建质量和临床诊断价值，具有良好的临床转化潜力。

Original Abstract: Accurate quantification and uptake measurement in PET are critical for assessing disease progression and supporting clinical decision-making. While high-count PET provides reliable image quality, the associated radiation dose and prolonged acquisition remain significant clinical concerns, motivating the adoption of low-count protocols. Diffusion-model-based methods have demonstrated strong potential for restoring low-count PET to near high-count quality, but their iterative sampling procedure becomes prohibitively expensive when applied to high-resolution 3D PET volumes, introducing substantial inference latency that limits practical clinical deployment. To address these challenges, we propose a training-free Global-Local Skipping Strategy that accelerates diffusion model-based 3D PET denoising while simultaneously improving reconstruction quality. The proposed method is plug-and-play and directly applicable to pre-trained diffusion models without retraining or architectural modification. Specifically, we introduce: (i) a global denoising step skipping strategy that initializes the reverse diffusion process from an intermediate denoising step using a noise-consistent transformation of the low-count input, substantially reducing the number of required denoising steps; and (ii) a local feature reuse shortcut that reuses slowly-varying high-level U-Net features across neighboring denoising steps, further reducing per-step computation while preserving image fidelity. We evaluate the proposed approach on multiple PET tracers from in-house and public datasets, including 18F-FDG PET, 68Ga-DOTATATE PET, and 18F-PSMA PET, demonstrating consistent acceleration of over an order of magnitude alongside improved or comparable reconstruction performance relative to the full-step baseline. Blinded reader studies further confirm enhanced clinical confidence and perceived diagnostic quality.

中文摘要翻译: 在正电子发射断层扫描（PET）中，准确定量和摄取测量对于评估疾病进展和支持临床决策至关重要。虽然高计数PET可提供可靠的图像质量，但相关辐射剂量和延长采集时间仍然是重要的临床问题，推动了低计数方案的应用。基于扩散模型的方法在将低计数PET恢复至接近高计数质量方面展现出强大潜力，但其迭代采样过程在应用于高分辨率3D PET体数据时变得极其昂贵，产生了显著的推理延迟，限制了实际的临床部署。为解决这些挑战，我们提出了一种无需训练的全局-局部跳跃策略（Global-Local Skipping Strategy），可在加速基于扩散模型的3D PET去噪的同时提升重建质量。所提方法即插即用，可直接应用于预训练扩散模型，无需重新训练或修改架构。具体而言，我们引入：(i) 全局去噪步跳跃策略——利用低计数输入的噪声一致性变换，从中间去噪步初始化逆向扩散过程，大幅减少所需的去噪步数；(ii) 局部特征复用捷径——在相邻去噪步间复用变化缓慢的高层U-Net特征，进一步减少每步计算量，同时保持图像保真度。我们在来自内部和公开数据集的多个PET示踪剂上评估了所提方法，包括18F-FDG PET、68Ga-DOTATATE PET和18F-PSMA PET，结果表明相比全步基线，实现了超过一个数量级的持续加速，同时重建性能优于或持平基线。盲法读者研究进一步证实了临床信心和感知诊断质量的提升。

</details>


### [113] [Beyond Consistency: Preserving Temporal Structure in Zero-Shot Video Editing](https://arxiv.org/abs/2606.08780)
*Deyin Liu,Yisheng Ding,Zhe Jin,Xiatian Zhu,Anjan Dutta,Lin Wu*

Main category: cs.CV

TL;DR: 本文提出了一种零样本视频编辑方法，通过自适应划分语义片段、选取锚定帧、以及片段自适应token合并策略，首次显式保留源视频的时间结构，解决了现有方法在长视频编辑中叙事不一致和语义模糊的问题，在保真度和计算效率上达到SOTA。

<details>
  <summary>Details</summary>

Motivation: 现有零样本视频编辑方法虽然实现了空间控制和基本时间一致性，但无法保留视频原有的时间结构（即高层叙事、节奏和语义流），导致编辑后的视频（尤其是长视频）叙事不连贯、语义模糊。本文旨在解决这一关键局限性。

Method: 1) 基于特征相似性将视频自适应划分为语义不同的片段；2) 为每个片段选取代表性锚定帧；3) 设计片段自适应token合并策略，利用锚定帧的语义主导性稳定编辑并提升计算效率；4) 采用交替组合策略确保片段间无缝过渡且保持语义区分性。

Result: 大量实验表明，该方法在平衡原始时间结构保留与计算效率方面达到SOTA结果，为零样本视频编辑保真度设立了新基准。

Conclusion: 本文方法成功解决了零样本视频编辑中时间结构丢失的核心问题，通过语义片段划分和锚定帧策略实现了叙事连贯性与语义清晰度的大幅提升，同时兼顾了计算效率。

Original Abstract: Existing zero-shot video editing methods rely on pre-trained diffusion models, successfully achieving spatial control and basic temporal consistency but fundamentally fail to preserve the video's original temporal structure.This distinction is critical: temporal consistency ensures visual smoothness, but temporal structure dictates the video's high-level narrative, rhythm, and semantic flow. Without this preservation, the edited output, especially for long videos with complex semantic variations, becomes narratively incoherent and semantically ambiguous. To address this limitation, we introduce a novel zero-shot editing approach that, for the first time, explicitly focuses on preserving the source video's temporal structure. We achieve this by adaptively partitioning the video into semantically distinct clips based on feature similarity and selecting a representative anchor frame for each clip. To enhance both intra-clip fidelity and computational efficiency, we design a clip-adaptive token merging strategy which leverages the anchor's semantic dominance to stabilize the editing. Furthermore, we employ an alternating combination strategy that ensures seamless inter-clip transitions while maintaining semantic distinction. Extensive experiments demonstrate that our method achieves state-of-the-art results, successfully balancing the preservation of original temporal structure with computational efficiency, and setting a new benchmark for zero-shot video editing fidelity.

中文摘要翻译: 现有的零样本视频编辑方法依赖于预训练的扩散模型，成功实现了空间控制和基本的时间一致性，但根本上无法保留视频原有的时间结构。这一区别至关重要：时间一致性确保视觉平滑性，而时间结构则决定了视频的高层叙事、节奏和语义流。若不保留时间结构，编辑后的输出（尤其是具有复杂语义变化的长视频）将变得叙事不连贯、语义模糊。为解决这一局限，我们提出了一种新颖的零样本编辑方法，首次显式聚焦于保留源视频的时间结构。我们通过基于特征相似性自适应地将视频划分为语义不同的片段，并为每个片段选取一个代表性锚定帧来实现这一点。为增强片段内保真度和计算效率，我们设计了一种片段自适应的token合并策略，利用锚定帧的语义主导性来稳定编辑过程。此外，我们采用交替组合策略，确保片段间的无缝过渡，同时保持语义区分性。大量实验表明，我们的方法在保留原始时间结构与计算效率之间取得了良好平衡，达到了SOTA结果，为零样本视频编辑保真度设立了新基准。

</details>


### [114] [DeepMine-Mamba: Mitigating Information Dilution in Mamba-Based State Space Models for Document Image Binarization](https://arxiv.org/abs/2606.08781)
*Sheng-Wei Chan,Yung-Che Wang,Hsin-Jui Pan,Chia-Min Lin,Jen-Shiun Chiang*

Main category: cs.CV

TL;DR: 本文提出DeepMine-Mamba，一种基于Mamba状态空间模型的文档图像二值化框架，并设计了抗稀释门控模块来解决Mamba长程建模中弱前景线索被稀释的问题，在DIBCO/H-DIBCO基准上取得了竞争性性能。

<details>
  <summary>Details</summary>

Motivation: 现有文档图像二值化方法多基于卷积、Transformer或生成式架构，Mamba-based状态空间模型在该任务中尚未被充分探索。然而，直接使用Mamba进行状态空间传播会在长程建模中稀释弱前景线索（如淡墨痕迹、破碎字符、边界敏感笔画细节），因此需要设计专门的机制来抑制这种稀释效应。

Method: 提出DeepMine-Mamba框架，核心创新是Anti-Dilution Gate（抗稀释门控），该门控能够估计传播引起的特征变化，选择性恢复笔画敏感的局部响应，同时抑制不必要的背景增强。

Result: 在DIBCO/H-DIBCO基准上采用严格的留一年份测试协议，DeepMine-Mamba在平均FM和Fps指标上均达到竞争性性能。消融实验进一步验证了抗稀释门控能够改善笔画保留效果并减少感知上显著的二值化误差。

Conclusion: Mamba状态空间模型可有效应用于文档图像二值化任务，但需解决长程传播中的特征稀释问题；提出的抗稀释门控是一种有效的解决方案，能提升笔画细节保留和整体二值化质量。

Original Abstract: Document image binarization aims to separate foreground text from degraded backgrounds while preserving thin, broken, and low-contrast strokes. Although deep learning methods have improved binarization performance, most existing approaches rely on convolutional, transformer-based, or generative architectures, while Mamba-based state space models remain largely unexplored for this task. In this work, we investigate Mamba-based feature propagation and observe that direct state-space propagation may dilute weak foreground cues during long-range modeling, especially faint ink traces, fragmented characters, and boundary-sensitive stroke details. To address this problem, we propose DeepMine-Mamba, a Mamba-based binarization framework equipped with a novel Anti-Dilution Gate that estimates propagation-induced feature changes and selectively restores stroke-sensitive local responses while suppressing unnecessary background enhancement. Experiments on DIBCO/H-DIBCO benchmarks under a strict leave-one-year-out protocol show that DeepMine-Mamba achieves competitive overall performance, with strong average FM and Fps across benchmark years. Ablation results further demonstrate that the Anti-Dilution Gate improves stroke preservation and reduces perceptually significant binarization errors.

中文摘要翻译: 文档图像二值化旨在从退化的背景中分离出前景文本，同时保留细薄、断裂和低对比度的笔画。尽管深度学习方法已经提升了二值化性能，但大多数现有方法依赖于卷积、Transformer或生成式架构，而基于Mamba的状态空间模型在该任务中仍未得到充分探索。本研究探索了基于Mamba的特征传播，并观察到直接的状态空间传播可能会在长程建模过程中稀释弱前景线索，特别是淡墨痕迹、破碎字符以及边界敏感的笔画细节。为解决该问题，我们提出了DeepMine-Mamba，一种配备有新颖抗稀释门控（Anti-Dilution Gate）的Mamba二值化框架，该门控能够估计传播引起的特征变化，并选择性地恢复笔画敏感的局部响应，同时抑制不必要的背景增强。在DIBCO/H-DIBCO基准上采用严格的留一年份测试协议进行的实验表明，DeepMine-Mamba在基准年份上取得了具有竞争力的整体性能，平均FM和Fps指标表现优异。消融实验进一步证明，抗稀释门控改善了笔画保留效果，并减少了感知上显著的二值化误差。

</details>


### [115] [MaskAlign: Token-Subset Representation Alignment for Efficient Diffusion Training](https://arxiv.org/abs/2606.08788)
*Lianyu Pang,Tianlin Pan,Cheng Da,Changqian Yu,Huan Yang,Kun Gai,Song Guo,Wenhan Luo*

Main category: cs.CV

TL;DR: 该论文提出MaskAlign方法，通过随机子集令牌对齐和轻量级预掩码令牌混合模块，解决了扩散Transformer训练中全令牌表示对齐导致的噪声-干净图像特征不匹配问题，从而提升收敛速度和生成质量。

<details>
  <summary>Details</summary>

Motivation: 现有的表示对齐方法将扩散模型的中间特征与自监督视觉编码器提取的干净图像特征进行对齐，但扩散模型处理的是带噪输入，不同时间步的可利用信息量不同，而参考特征来自干净图像，这种不匹配导致对齐目标对所有令牌并非均匀施加影响，可能促使模型过度依赖完整的干净图像令牌集。

Method: 提出MaskAlign方法：(1) 在训练过程中对随机采样的令牌子集施加表示对齐，减少对齐对完整令牌集的依赖；(2) 引入轻量级预掩码令牌混合模块，在掩码前跨令牌共享信息，缓解直接丢弃令牌带来的信息损失。

Result: 通过令牌级分析发现，在全令牌表示对齐下，对齐梯度范数较大的令牌表现出稳定的空间偏好，表明对齐目标对令牌的影响不均匀。MaskAlign通过随机子集对齐使模型在令牌子集扰动下表现出更稳定的对齐行为。

Conclusion: MaskAlign通过令牌子集表示对齐和预掩码信息混合，有效缓解了噪声-干净特征不匹配问题，提升了扩散Transformer的训练效率和生成质量。

Original Abstract: Representation alignment with pretrained vision models has recently shown strong potential for accelerating diffusion transformer training. By aligning intermediate diffusion features with clean-image representations from self-supervised vision encoders, existing methods improve convergence and generation quality. However, such alignment also introduces a non-trivial constraint: diffusion models operate on noisy inputs whose usable information varies across timesteps, while the reference features are extracted from clean images. In this paper, we revisit this mismatch from a token-level perspective. We find that, under full-token representation alignment, tokens with large alignment-gradient norms exhibit a stable spatial preference, suggesting that the alignment objective does not affect all tokens uniformly and may encourage the model to rely on the complete set of clean-image tokens. To address this issue, we propose MaskAlign, a token-subset representation alignment method that applies alignment to randomly sampled token subsets during training. By exposing the model to different token subsets across iterations, MaskAlign reduces the dependence of representation alignment on the complete token set and encourages alignment behavior that is more stable under token-subset perturbations. To mitigate the information loss caused by directly dropping tokens, we further introduce a lightweight pre-mask token mixing block that shares information across tokens before masking.

中文摘要翻译: 与预训练视觉模型进行表示对齐最近在加速扩散Transformer训练方面展现出强大潜力。通过将扩散模型的中间特征与自监督视觉编码器提取的干净图像表示进行对齐，现有方法提升了收敛速度和生成质量。然而，这种对齐也引入了一个重要约束：扩散模型处理的是带噪输入，其可用信息在不同时间步上有所不同，而参考特征是从干净图像中提取的。本文从令牌级别的角度重新审视了这一不匹配问题。我们发现，在全令牌表示对齐下，对齐梯度范数较大的令牌表现出稳定的空间偏好，这表明对齐目标并非对所有令牌均匀施加影响，并且可能促使模型依赖完整的干净图像令牌集。为了解决这个问题，我们提出了MaskAlign，一种令牌子集表示对齐方法，该方法在训练过程中对随机采样的令牌子集施加对齐。通过在每次迭代中让模型接触不同的令牌子集，MaskAlign减少了表示对齐对完整令牌集的依赖，并鼓励在令牌子集扰动下更稳定的对齐行为。为了缓解直接丢弃令牌造成的信息损失，我们还引入了一个轻量级的预掩码令牌混合模块，该模块在掩码前跨令牌共享信息。

</details>


### [116] [PairWise Image Finder: An Open-source Tool for Finding Visually Aligned Street-Level Image Pairs for Urban Perception Studies](https://arxiv.org/abs/2606.08795)
*Jussi Torkko*

Main category: cs.CV

TL;DR: 本文提出了一种名为PairWise image finder的工具，通过结合特征检测与匹配、语义分割掩码来量化不同时期街景图像对的视觉对齐质量，从而可靠地筛选出视觉对齐的图像对，用于纵向变化分析和感知研究。

<details>
  <summary>Details</summary>

Motivation: 仅凭元数据难以可靠地找到视觉对齐的图像对，现有方法无法有效量化不同时期街景图像的视觉对齐程度，需要一种可扩展的工具来辅助筛选高质量图像对。

Method: 该方法集成特征检测与匹配技术，并利用语义分割掩码来量化两幅不同时期图像的视觉对齐程度。工具输出匹配关键特征的占比、匹配特征距离与覆盖度、以及语义掩码的对齐度，用户可根据对齐质量和具体用例筛选图像对。

Result: 通过纵向变化的对比实验展示了工具的可用性，强调了视角在量化变化时的重要性。该工具能够生成视觉对齐的图像对，用于精确研究显式纵向变化，并减少人工标注的工作量。

Conclusion: 该方法为研究者和利益相关者提供了一个可扩展、开放的工具，用于为城市分析、感知研究及相关应用寻找高质量的图像对。

Original Abstract: Change detection and scene recognition techniques have been widely applied to Street View Imagery (SVI) to understand changes in scenes across the years. However, metadata alone is often insufficient to reliably find visually aligned image pairs. This study introduces the PairWise image finder, a tool that integrates feature detection and matching, supported by semantic segmentation masks to quantify the visual alignment of two images of varying time periods. The tool outputs the share of matched key features, the matched feature distance and coverage, and the alignment of semantic masks, which enables the user to filter image pairs depending on the alignment quality and use case. The visually aligned pairs derived from the tool can be used to accurately study explicit longitudinal change and help reduce manual effort for perception studies. The usability of the tool is demonstrated through a comparison of longitudinal changes, highlighting the importance of perspective when quantifying changes. The proposed method provides a scalable and open tool for researchers and stakeholders to find high-quality image pairs for urban analysis, perception and related applications.

中文摘要翻译: 变化检测与场景识别技术已被广泛应用于街景图像（Street View Imagery, SVI）中，以理解跨年份的场景变化。然而，仅凭元数据往往难以可靠地找到视觉对齐的图像对。本研究引入了PairWise image finder工具，该工具集成了特征检测与匹配，并辅以语义分割掩码（semantic segmentation masks）来量化不同时期两幅图像的视觉对齐程度。该工具输出匹配关键特征的占比、匹配特征的距离与覆盖度，以及语义掩码的对齐度，使用户能够根据对齐质量和具体用例筛选图像对。基于该工具得到的视觉对齐图像对可用于精确研究显式纵向变化，并有助于减少感知研究中的人工工作量。通过纵向变化的对比实验验证了该工具的可用性，强调了视角在量化变化时的重要性。所提出的方法为研究者和利益相关者提供了一个可扩展且开放的工具，用于城市分析、感知研究及相关应用中寻找高质量的图像对。

</details>


### [117] [Classifying galaxies in the Galaxy10 DECals dataset using Inception and Residual CNNs](https://arxiv.org/abs/2606.08826)
*Lanz Anthonee A. Lagman,Prospero C. Naval,Reinabelle C. Reyes*

Main category: cs.CV

TL;DR: 本文对比分析了ResNet101和InceptionV4两种深度学习架构在星系形态分类任务上的性能表现，实验基于空间增强的Galaxy10 DECals数据集，两种模型均达到约90%的准确率，其中ResNet101在综合性能指标上优于InceptionV4。

<details>
  <summary>Details</summary>

Motivation: 随着未来几年星系形态图像数据在数量和质量上的预期增长，探索哪些适用于图像分类任务的深度学习架构更具成本效益具有重要意义。

Method: 使用ResNet101和InceptionV4两种卷积神经网络架构，在空间增强的Galaxy10 DECals数据集上进行十类星系形态分类实验，并对每类图像数量进行了调整。

Result: ResNet101和InceptionV4模型均达到约90%的分类准确率，与文献中报告的性能相当；在性能指标方面，ResNet101优于InceptionV4。

Conclusion: 这两种CNN架构均可作为未来巡天项目中星系图像分类专用流水线的稳健基础。

Original Abstract: Image data regarding galactic morphology is expected to increase both in quantity and quality for the next foreseeable years; thus it is important to explore which deep learning architectures adapted for image classification tasks are cost-effective. Residual and Inception networks are ideal for exploring classification convolutional neural networks (CNNs) due to their computational efficiency, achieved through techniques such as residual connections and parallelized inception modules, enabling deeper networks without excessively increasing computational complexity. In this work, we analyze the performance of ResNet101 and InceptionV4 on a spatially-augmented Galaxy10 DECals dataset. Retaining the ten-class classification of galaxies, we modify the image count of each class. We find that ResNet101 and InceptionV4 models achieved accuracies of $\sim$ 90%, comparable with reported performance in the literature. In terms of performance metrics, ResNet101 is superior to InceptionV4. Our results indicate that either of these CNN architectures could serve as a robust foundation for specialized pipelines for classification of galaxy images from upcoming surveys.

中文摘要翻译: 预计在未来数年内，关于星系形态的图像数据在数量和质量上都将持续增长；因此，探索哪些适用于图像分类任务的深度学习架构具有成本效益至关重要。Residual网络和Inception网络因其计算效率而成为探索分类卷积神经网络（CNN）的理想选择，它们通过残差连接和并行化Inception模块等技术，能够在不过度增加计算复杂度的前提下构建更深的网络。在本工作中，我们分析了ResNet101和InceptionV4在空间增强的Galaxy10 DECals数据集上的性能表现。我们保留了原始的十类星系分类体系，但对每个类别的图像数量进行了调整。结果表明，ResNet101和InceptionV4模型均达到了约90%的准确率，与文献中报道的性能相当。在性能指标方面，ResNet101优于InceptionV4。我们的结果表明，这两种CNN架构均可作为未来巡天项目中星系图像分类专用流水线的稳健基础。

</details>


### [118] [CSFlow: Aligning Flow Matching with Human Contrast Sensitivity](https://arxiv.org/abs/2606.08833)
*Malgorzata Galinska,Bart Pogodzinski,Jan Eric Lenssen*

Main category: cs.CV

TL;DR: 本文提出CSFlow，一种将人类视觉对比敏感度函数（CSF）与流匹配迭代去噪步骤相结合的加权方案，通过在傅里叶空间中利用软自回归结构并依人类视觉敏感度分配时间步权重，提升了生成图像的质量与真实感。

<details>
  <summary>Details</summary>

Motivation: 真实世界图像的能量集中在低频空间频率，在连续扩散过程中低频成分比高频成分更早达到高信噪比，这导致生成过程在傅里叶空间中呈现软自回归结构（先粗后细）。同时，人类视觉系统对不同空间频率的敏感度不均——极低和极高频率需要更高对比度才能被感知。现有方法未将这两种现象结合起来，因此本文旨在利用视觉感知特性来指导扩散/流匹配模型的生成过程，提升生成质量。

Method: (1) 提出一个度量指标，用于估计每个反向流区间内生成了哪些频率成分；(2) 通过将各噪声水平下生成的频率与人类对比敏感度函数对齐，设计时间步权重（CSFlow weights）。该方法可仅通过推理时的时间步修改或短时微调即可应用。

Result: 实验表明，CSFlow权重将FID降低4.7%，Inception Score提升2.2%，GenEval分数提升2.5%。定性分析显示，生成图像具有更好的视觉真实感，卡通化程度降低。

Conclusion: 将人类视觉对比敏感度先验融入流匹配的迭代去噪过程，能够有效提升生成模型的性能与视觉真实感，验证了感知引导生成方案的有效性。

Original Abstract: We introduce Contrast Sensitive Flow (CSFlow), a weighting scheme that connects the human eye's Contrast Sensitivity Function (CSF) to the iterative denoising steps of flow matching. Because real-world images concentrate signal at low spatial frequencies, these components reach high signal-to-noise ratio earlier during continuous diffusion than high-frequency components. When generating images with diffusion or flow matching models, this induces a soft autoregressive structure in Fourier space, where coarse image content stabilizes before fine detail. Meanwhile, the human visual system is unequally sensitive to spatial frequencies: very low and very high frequencies require significantly higher contrast to be perceived. We for the first time merge these observations through two contributions: (1) a metric that estimates which frequencies are generated at each reverse flow interval and (2) timestep weights obtained by aligning the frequencies generated at each noise level with human contrast sensitivity. We validate our contributions experimentally showing that these weights can improve generative performance by lowering FID by 4.7%, increasing Inception Score by 2.2% and improving GenEval scores by 2.5% using inference-only timestep modification or short fine-tuning. Qualitatively, we find that our CSFlow weights lead to better visual realism and less cartoonish appearance of generated images.

中文摘要翻译: 我们提出了对比敏感度流（CSFlow），一种将人类视觉对比敏感度函数（CSF）与流匹配的迭代去噪步骤相结合的加权方案。由于真实世界图像的能量集中在低空间频率，这些分量在连续扩散过程中比高频分量更早达到高信噪比。在使用扩散或流匹配模型生成图像时，这会在傅里叶空间中诱导出一种软自回归结构，其中粗略图像内容先于细节稳定。与此同时，人类视觉系统对空间频率的敏感度不均——极低和极高频率需要显著更高的对比度才能被感知。我们首次通过两个贡献将这些观察融合在一起：(1) 一个用于估计每个反向流区间内生成了哪些频率的度量指标；(2) 通过将各噪声水平下生成的频率与人类对比敏感度对齐得到的时间步权重。我们通过实验验证了这些贡献，表明这些权重可以仅通过推理时的时间步修改或短时微调来提升生成性能：FID降低4.7%，Inception Score提高2.2%，GenEval分数提高2.5%。定性分析发现，我们的CSFlow权重使生成图像具有更好的视觉真实感，卡通化外观得到改善。

</details>


### [119] [Geometry-Aware Fisheye-LiDAR Fusion for Robust 3D Object Detection in Low-Overlap Setups](https://arxiv.org/abs/2606.08844)
*Xiangzhong Liu,Xihao Wang,Hao Shen*

Main category: cs.CV

TL;DR: 提出GA-HF框架，通过极坐标BEV网格和畸变感知LSS模块融合鱼眼相机与LiDAR，解决稀疏视场下几何畸变与特征失配问题，在三个基准数据集上取得最优结果。

<details>
  <summary>Details</summary>

Motivation: 自动驾驶从高成本机器人出租车扩展到低成本物流场景时，传感器配置转为注重覆盖-成本比，稀疏视场设置（双鱼眼相机+顶置LiDAR）导致严重几何挑战（极端径向畸变、小重叠区域、球面投影与笛卡尔网格不对齐），现有BEV融合方法强制将图像和点云模态统一到笛卡尔网格，造成鱼眼相机大幅特征畸变和信息损失。

Method: 提出GA-HF（Geometry-Aware Hybrid Fusion）框架：1）畸变感知LSS模块将鱼眼特征提升至极坐标BEV网格，保留原生角度密度；2）LiDAR特征在原生笛卡尔空间中处理以保证度量保真度；3）双注意力扭曲校正模块在融合前对扭曲相机特征施加空间和通道注意力，抑制低质量外围区域伪影同时增强高质量语义线索。

Result: 在KITTI-360上NDS提升4.2%；在Dur360BEV上超越LiDAR-only和BEVFusion，显著降低方向误差；在Fisheye3DOD上取得所有融合方法最高检测分数。

Conclusion: GA-HF是首个探索LiDAR-鱼眼相机融合的方法，通过显式建模鱼眼几何和BEV特征畸变，有效解决了稀疏视场下的多模态融合难题。

Original Abstract: As autonomous systems expand from capital-intensive robotaxis to cost-sensitive logistics, sensor configurations are increasingly optimized for coverage-per-cost. A prevalent sparse-view setup utilizes dual-fisheye cameras with a roof-mounted LiDAR, introducing severe geometric challenges: extreme radial distortion, minimal overlap, and misalignment between spherical projections and rectilinear grids. BEV fusion algorithms typically force image and point cloud modalities into unified Cartesian grids early in the pipeline, causing significant feature distortion and information loss for wide-view fisheye cameras. To address this, we propose a Geometry-Aware Hybrid Fusion (GA-HF) framework that explicitly accounts for fisheye geometry and BEV feature distortion, where fisheye features are lifted into a polar BEV grid via a Distortion-Aware Lift-Splat-Shoot (LSS) module to preserve native angular density, while LiDAR features are processed in native Cartesian space for metric fidelity of bounding box regression. To bridge these heterogeneous streams, we introduce a Dual-Attention Warping Correction module that applies spatial and channel attention to the warped camera features before fusion, explicitly suppressing artifacts in low-quality peripheral regions while enhancing high-quality semantic cues. GA-HF is evaluated on three benchmarks: KITTI-360, Dur360BEV, and Fisheye3DOD datasets. To the best of our knowledge, it is the first approach to explore LiDAR-fisheye camera fusion. On KITTI-360, GA-HF improves NDS by 4.2% over Cartesian baselines; on Dur360BEV, it surpasses both LiDAR-only and BEVFusion, while significantly reducing orientation error despite the geometric distortions; on Fisheye3DOD, it attains the highest detection score among all fusion methods.

中文摘要翻译: 随着自主系统从资本密集型的机器人出租车扩展到成本敏感的物流场景，传感器配置日益优化以提升覆盖-成本比。一种流行的稀疏视场设置采用双鱼眼相机搭配车顶LiDAR，引入了严峻的几何挑战：极端径向畸变、极小重叠区域以及球面投影与笛卡尔网格之间的不对齐。现有的BEV融合方法通常在流程早期强制将图像和点云模态统一到笛卡尔网格中，导致广视角鱼眼相机出现显著的特征畸变和信息损失。为解决这一问题，我们提出了一种几何感知混合融合（GA-HF）框架，该框架显式建模鱼眼几何和BEV特征畸变。其中，鱼眼特征通过畸变感知的Lift-Splat-Shoot（LSS）模块提升至极坐标BEV网格，以保留原生角度密度；而LiDAR特征则在原生笛卡尔空间中处理，以保证边界框回归的度量保真度。为桥接这些异质流，我们引入了一个双注意力扭曲校正模块，在融合前对扭曲后的相机特征施加空间和通道注意力，显式抑制低质量外围区域中的伪影，同时增强高质量语义线索。GA-HF在三个基准数据集上进行了评估：KITTI-360、Dur360BEV和Fisheye3DOD。据我们所知，这是首个探索LiDAR-鱼眼相机融合的方法。在KITTI-360上，GA-HF相比笛卡尔基线将NDS提升了4.2%；在Dur360BEV上，它超越了仅LiDAR方法和BEVFusion，同时尽管存在几何畸变，仍显著降低了方向误差；在Fisheye3DOD上，它在所有融合方法中取得了最高的检测分数。

</details>


### [120] [BLM-SGAN: Bidirectional Language Modeling for Semantic-Spatial Text-to-Image Generation](https://arxiv.org/abs/2606.08847)
*Ahmed Abdelmoneim Mazrou,Haidy Maher El-Amir,Ali Hamdi*

Main category: cs.CV

TL;DR: 提出BLM-SGAN模型，利用BERT的双向语言建模和注意力机制改进基于GAN的文本到图像生成，解决长程依赖捕获、梯度消失和序列处理限制等问题，在鸟类图像生成任务上达到SOTA性能（IS=5.45±0.08）。

<details>
  <summary>Details</summary>

Motivation: 现有基于GAN的文本到图像（T2I）模型在捕获长程依赖关系、处理梯度消失以及序列处理的局限性方面仍面临关键挑战，亟需更有效的解决方案来提升生成图像的质量和一致性。

Method: 提出BLM-SGAN模型，采用BERT的双向语言建模（Bidirectional Language Modeling）和注意力机制来捕获丰富的上下文信息，并高效管理长序列，从而实现语义-空间（Semantic-Spatial）的文本到图像生成。

Result: BLM-SGAN在鸟类图像生成任务上取得Inception Score (IS)为5.45±0.08，超越了SSA-GAN、DF-GAN、SD-GAN和AttnGAN等竞争模型，达到当时最先进水平。

Conclusion: BLM-SGAN通过引入BERT的双向语言建模有效改进了GAN-based T2I模型的性能，能够从详细的文本描述中生成高度逼真的鸟类图像。

Original Abstract: Despite the success of image generation from text descriptions, it still faces challenges that are difficult to overcome in domains such as natural language processing (NLP) and computer vision (CV). Recent advancements in text-to-image (T2I) models, particularly those utilizing generative adversarial networks (GANs), have significantly improved the synthesis of realistic images across various domains. However, existing GAN-based T2I models still encounter key challenges, such as difficulty in capturing long-range dependencies, vanishing gradients, and the limitations of sequential processing. To address these issues, we introduce BLM-SGAN, a novel model that incorporates Bidirectional Language Modeling for Semantic-Spatial Text-to-Image Generation. BLM-SGAN leverages BERT's attention mechanisms to capture rich contextual information and efficiently manage extended sequences. Our model demonstrates state-of-the-art performance, with an Inception Score (IS) of 5.45 +/- 0.08, surpassing several competitive models such as SSA-GAN, DF-GAN, SD-GAN, and AttnGAN. BLM-SGAN effectively generates highly realistic images of birds from detailed text descriptions. The implementation code is available at: https://github.com/haidy-maher/BLM-SGAN-Text-to-Image-Generation.

中文摘要翻译: 尽管从文本描述生成图像取得了成功，但在自然语言处理（NLP）和计算机视觉（CV）等领域，它仍然面临着难以克服的挑战。近年来，文本到图像（T2I）模型的进展，特别是利用生成对抗网络（GAN）的模型，显著提升了跨领域合成逼真图像的能力。然而，现有的基于GAN的T2I模型仍然面临关键挑战，例如难以捕获长程依赖关系、梯度消失以及序列处理的局限性。为了解决这些问题，我们引入了BLM-SGAN，一种结合了双向语言建模（Bidirectional Language Modeling）用于语义-空间文本到图像生成的新型模型。BLM-SGAN利用BERT的注意力机制来捕获丰富的上下文信息并高效管理长序列。我们的模型展现了最先进的性能，Inception Score（IS）达到5.45±0.08，超越了多个竞争模型，如SSA-GAN、DF-GAN、SD-GAN和AttnGAN。BLM-SGAN能够从详细的文本描述中有效生成高度逼真的鸟类图像。实现代码可在以下网址获取：https://github.com/haidy-maher/BLM-SGAN-Text-to-Image-Generation。

</details>


### [121] [Intelligent Character Recognition of Handwritten Forms with Deep Neural Networks](https://arxiv.org/abs/2606.08858)
*Hartwig Grabowski*

Main category: cs.CV

TL;DR: 提出一种单任务深度神经网络，将手写字符的检测与分类合二为一，通过人工合成训练数据，在真实考试数据上达到88.28%的识别率，优于传统两任务方法。

<details>
  <summary>Details</summary>

Motivation: 传统手写表格处理需要先检测再分类两个独立步骤，流程复杂且依赖人工标注。作者希望将两步骤合并为单任务，同时解决训练数据标注成本高的问题。

Method: 采用深度神经网络将手写字符的检测与分类合并为单一任务；训练数据不依赖人工标注，而是利用现有数据集与表格模板人工合成生成；使用EMNIST数据集作为基础，并针对其局限性进行了额外定制化处理。

Result: 在来自书面考试的真实数据上达到了88.28%的整体识别率，且该单任务方法优于传统的两任务方法。

Conclusion: 单任务深度神经网络在手写表格处理中优于传统两任务方法，且通过人工合成训练数据可避免人工标注成本；但EMNIST数据集存在局限性，需要进一步定制化改进。

Original Abstract: The automatic processing of handwritten forms remains a challenging task, wherein detection and subsequent classification of handwritten characters are essential steps. We describe a novel approach, in which both steps -- detection and classification -- are executed in one task through a deep neural network. Therefore, training data is not annotated by hand, but manufactured artificially from the underlying forms and yet existing datasets. It can be demonstrated that this single-task approach is superior in comparison to the state-of-the-art two-task approach. The current study focuses on hand-written Latin letters and employs the EMNIST data set. However, limitations were identified with this data set, necessitating further customization. Finally, an overall recognition rate of 88.28 percent was attained on real data obtained from a written exam.

中文摘要翻译: 手写表格的自动处理仍然是一项具有挑战性的任务，其中手写字符的检测及随后的分类是关键步骤。本文描述了一种新颖的方法，通过深度神经网络将检测和分类这两个步骤合并为单一任务执行。因此，训练数据并非通过人工标注，而是从基础表格模板及已有的数据集中人工合成生成。实验表明，这种单任务方法优于传统的最优两任务方法。本研究聚焦于手写拉丁字母，使用了EMNIST数据集。然而，该数据集存在一些局限性，需要进一步的定制化处理。最终，在一份来自书面考试的真实数据上，该方法达到了88.28%的整体识别率。

</details>


### [122] [Vision-Language Work Zone Intelligence for Safety-Critical Speed Regulation of Mixed-Autonomy Vehicles in Dynamic Environments](https://arxiv.org/abs/2606.08860)
*Angel Martinez-Sanchez,Kianna Ng,Wesley Maia,Laura Fleig,Maitrayee Keskar,Erika Maquiling,Yash Tandon,Parthib Roy,Mohan Trivedi,Ross Greer*

Main category: cs.CV

TL;DR: 本文提出了一种基于车载实时感知的施工区车速限制检测系统，通过融合目标检测、语义验证与迟滞状态转换，在低成本嵌入式硬件上实现了高召回率（96.5%）的施工区检测与高精度（95.45%）的限速识别，摆脱了对地图或基础设施的依赖。

<details>
  <summary>Details</summary>

Motivation: 临时施工区的限速标志存在视觉不一致性，且常缺失于数字地图中，给人类驾驶和自动驾驶系统带来安全风险。现有方法依赖地图或基础设施信息，缺乏实时、可靠的自主感知方案。

Method: 提出一种实时车载感知流程：融合目标检测与语义验证，结合时间平滑与基于迟滞的状态转换机制，减少动态场景中的误激活和闪烁现象，并在低成本嵌入式硬件上全流程运行。

Result: 在ROADWork数据集（490个序列）上，施工区内事件级召回率达96.5%，精确率68.7%；在35分钟自有驾驶数据上，限速识别精确率95.45%，召回率53.85%，无错误限速分类，仅有一个假阳性。

Conclusion: 该研究证明了基于车载实时感知实现施工区限速意识的可行性和可扩展性，为替代地图或基础设施依赖提供了实用化方案。

Original Abstract: Temporary work-zone speed limits are communicated through visually inconsistent signage and are often missing from digital maps, creating safety risks for human drivers and automated vehicle systems. We present a real-time, onboard perception pipeline that detects active work zones, recognizes associated temporary speed limits, and outputs a law-aware work-zone state and speed value suitable for driver alerts or downstream automated control. The system fuses object detections with semantic verification and temporally smoothed, hysteresis-based state transitions to reduce false activations and flicker in dynamic scenes, and runs fully on low-cost embedded hardware. Evaluated manually on a annotated subset of the ROADWork dataset (490 sequences), the system achieves inside-work-zone event-level recall of 96.5% and event-level precision of 68.7%. Speed-limit recognition evaluated on 35 minutes of in-house driving data attains 95.45% precision and 53.85% recall, with no incorrect speed classifications and a single false positive. These results demonstrate a practical, scalable approach for grounding work-zone speed awareness directly in onboard perception rather than maps or infrastructure. We release our source code for the proposed system pipeline on our GitHub repository: https://github.com/Mi3-Lab/workzone

中文摘要翻译: 临时施工区的限速通过视觉不一致的标志进行沟通，且常缺失于数字地图中，给人类驾驶员和自动驾驶系统带来了安全风险。我们提出了一种实时、车载感知流程，能够检测活动施工区、识别相关的临时限速，并输出一个符合法规的施工区状态和速度值，适用于驾驶员警示或下游自动驾驶控制。该系统融合了目标检测与语义验证，并结合了时间平滑与基于迟滞的状态转换机制，以减少动态场景中的误激活和闪烁现象，且完全在低成本嵌入式硬件上运行。在ROADWork数据集标注子集（490个序列）上的人工评估中，系统在施工区内的事件级召回率达到96.5%，事件级精确率为68.7%。限速识别在35分钟的内部驾驶数据上评估，实现了95.45%的精确率和53.85%的召回率，无错误限速分类，仅有一个假阳性。这些结果表明，这是一种实用且可扩展的方法，能够直接在车载感知层面实现施工区限速意识，而非依赖地图或基础设施。我们在GitHub仓库上发布了所提出系统流程的源代码：https://github.com/Mi3-Lab/workzone

</details>


### [123] [CHROMA: Detecting AI-Generated Images through Inter-Channel Color-Space Correlations](https://arxiv.org/abs/2606.08864)
*Juan Pablo Sotelo,Marina Gardella,Pablo Musé*

Main category: cs.CV

TL;DR: 本文提出Chroma，一种利用通道间颜色相关性（跨通道相关性特征）来检测AI生成图像的轻量级检测器，通过将相关性特征图增强到RGB输入中，使用固定CNN骨干网络在有限计算预算下训练，在标准基准上取得了与最新检测器竞争的性能。

<details>
  <summary>Details</summary>

Motivation: 现有AI生成图像检测器在泛化到未见过的生成器时表现脆弱，而通道间颜色相关性作为一种轻量级且未被充分利用的法医线索，值得深入探索。

Method: 首先通过LPIPS感知度量在不同颜色空间参数化下对扰动的不一致响应，揭示跨通道统计信息未被常见感知训练目标统一约束。然后分析多个颜色空间中成对通道间相关性特征的分布，发现RGB和Lab颜色空间能最清晰地区分真实与生成图像。基于此，提出Chroma检测器，将标准RGB输入与通道间相关性图拼接，使用固定CNN骨干网络在有限计算预算下训练。

Result: LPIPS对选择性改变通道依赖的扰动表现不一致；RGB和Lab颜色空间的通道间相关性分布能有效区分真实与生成图像；相关性增强的输入在单生成器训练和有限多生成器监督下均提升了检测性能，达到了与最新检测器竞争的水平。

Conclusion: 通道间颜色相关性是一种有效且轻量级的法医线索，相关性增强的输入能提高AI生成图像检测的鲁棒性，且该方法架构简单、训练开销低。

Original Abstract: The rapid adoption of diffusion and large-scale generative models has made it increasingly challenging to distinguish synthetic imagery from real photographs. While automated detectors have been proposed, their generalization to unseen generators remains brittle. To address this limitation, we investigate inter-channel color correlations, a lightweight and underexploited forensic cue. We first demonstrate that LPIPS, a widely used perceptual metric, exhibits inconsistent responses to perturbations that selectively alter channel dependence across different color-space parameterizations, indicating that cross-channel statistics are not uniformly constrained by common perceptual training objectives. Motivated by this, we analyze the distributions of pairwise inter-channel correlation features across multiple color spaces. Our analysis reveals systematic, generator-specific differences in these distributions, with RGB and Lab color spaces providing the most apparent separation between real and generated images. Building on this, we introduce Chroma, a detector of AI-generated images which augments standard RGB inputs with inter-channel correlation maps and employs a fixed CNN backbone trained with a modest computational budget. We assess its robustness under both single-generator training and a limited multi-generator supervision regime, where only a few samples from additional generators are available. Across a standard benchmark protocol, correlation-augmented inputs improve real-vs-generated discrimination and robustness, yielding performance competitive with recent detectors while maintaining a simple architecture and training procedure. Code is available at https://github.com/JPSoteloSilva/CHROMA

中文摘要翻译: 扩散模型和大规模生成模型的快速普及使得区分合成图像与真实照片变得越来越具有挑战性。尽管已有自动化检测器被提出，但它们对未见过的生成器的泛化能力仍然脆弱。为解决这一局限，我们研究了通道间颜色相关性——一种轻量级且未得到充分利用的法医线索。我们首先证明，广泛使用的感知度量LPIPS对选择性改变不同颜色空间参数化下通道依赖性的扰动表现出不一致的响应，这表明跨通道统计量并未受到常见感知训练目标的统一约束。受此启发，我们分析了多个颜色空间中成对通道间相关性特征的分布。我们的分析揭示了这些分布中存在的、生成器特异性的系统性差异，其中RGB和Lab颜色空间在真实图像与生成图像之间提供了最明显的分离。基于此，我们提出了Chroma——一种AI生成图像检测器，它将标准RGB输入与通道间相关性图相结合，并采用在适度计算预算下训练的固定CNN骨干网络。我们在单生成器训练和有限多生成器监督机制（仅从额外生成器获取少量样本）下评估其鲁棒性。在标准基准协议下，相关性增强的输入改善了真实图像与生成图像的判别能力及鲁棒性，在保持简单架构和训练流程的同时，取得了与最新检测器竞争的性能。代码已开源：https://github.com/JPSoteloSilva/CHROMA

</details>


### [124] [Generalizing Geometry-Guided Mamba as a Plug-and-Play Context Module for CNN-based Semantic Segmentation](https://arxiv.org/abs/2606.08866)
*Sheng-Wei Chan,Hsin-Jui Pan,Chun-Po Shen,Chia-Min Lin,Yung-Che Wang,Jen-Shiun Chiang*

Main category: cs.CV

TL;DR: 本文提出将方向性几何Mamba（G-Mamba）作为即插即用的上下文聚合模块，替代六种代表性CNN语义分割模型（DeepLabV3+、DANet、CCNet、PSPNet、PSANet、OCRNet）的原始上下文头，在Cityscapes上以较少额外计算量实现了一致的mIoU提升。

<details>
  <summary>Details</summary>

Motivation: 现有CNN语义分割网络的上下文头（如ASPP、PPM、注意力模块）虽能扩大感受野，但存在计算量大、内存开销高或边界泄露等问题，亟需更高效的即插即用替代方案。

Method: 将DGM-Net中的方向性几何Mamba（G-Mamba）重新设计为即插即用的上下文聚合模块：在选择性扫描过程中注入几何引导（边界线索和向心流线索），使长距离特征传播受到调制，从而替代原有上下文头。

Result: 在Cityscapes数据集上，使用ResNet-101骨干网络，替换六种模型的上下文头后均获得一致的mIoU增益，且在高分辨率（1024×1024）下仅引入适度的额外GFLOPs。

Conclusion: 几何引导的SSM模块可作为传统CNN上下文头的实用替代或增强方案，在提升分割性能的同时保持计算效率。

Original Abstract: CNN-based semantic segmentation networks usually rely on context heads such as ASPP, PPM, or attention modules to enlarge the receptive field. These heads are effective but may introduce heavy computation, memory cost, or boundary leakage. This paper revisits Directional Geometric Mamba (G-Mamba) from DGM-Net and studies it as a plug-and-play context aggregation module rather than a complete new segmentation architecture. The key idea is to inject geometric guidance into the selective scan process, allowing long-range feature propagation to be modulated by boundary and centripetal-flow cues. We replace the original context heads of six representative CNN segmentation models, including DeepLabV3+, DANet, CCNet, PSPNet, PSANet, and OCRNet, while keeping the ResNet-101 backbone unchanged. Results on Cityscapes show consistent mIoU gains with only moderate extra GFLOPs at $1024\times1024$ resolution, suggesting that geometry-guided SSM modules can serve as practical alternatives or enhancements to conventional CNN context heads.

中文摘要翻译: 基于CNN的语义分割网络通常依赖ASPP、PPM或注意力模块等上下文头来扩大感受野。这些上下文头虽然有效，但可能引入高昂的计算量、内存开销或边界泄露问题。本文重新审视了DGM-Net中的方向性几何Mamba（G-Mamba），并将其作为一个即插即用的上下文聚合模块而非完整的新型分割架构进行研究。其关键思想是将几何引导注入选择性扫描过程，使长距离特征传播能够被边界线索和向心流线索所调制。我们替换了六种代表性CNN分割模型（包括DeepLabV3+、DANet、CCNet、PSPNet、PSANet和OCRNet）的原始上下文头，同时保持ResNet-101骨干网络不变。在Cityscapes上的结果表明，在1024×1024分辨率下，仅引入适度的额外GFLOPs即可获得一致的mIoU提升，这表明几何引导的SSM模块可以作为传统CNN上下文头的实用替代方案或增强模块。

</details>


### [125] [Are Reasoning Vision-Language Models Robust to Semantic Visual Distractions?](https://arxiv.org/abs/2606.08894)
*Yizheng Sun,Mochuan Zhan,Yanan Ma,Jia Tong See,Yifan Wang,Ziyi Wang,Hao Li,Yang Cui,Wenhao Cai,Jingyu Sun,Chenghua Lin,Riza Batista-Navarro,Jingyuan Sun*

Main category: cs.CV

TL;DR: 提出Distract-Bench基准，评估推理型视觉语言模型对语义视觉干扰的鲁棒性。实验表明推理型VLM对感知退化鲁棒性与非推理基模型相近，但对语义干扰的鲁棒性显著更低，且干扰会进入推理过程导致错误答案。

<details>
  <summary>Details</summary>

Motivation: 现有VLMs鲁棒性评估主要关注输入噪声、模糊、天气效果等感知退化，忽略了另一类关键失效模式：模型可能正确感知证据，却从看似合理但与任务无关的干扰证据中推理，并将错误传播到最终答案。

Method: 构建Distract-Bench基准，定义语义视觉干扰为保留真实答案的同时向输入中添加有意义但与任务无关的视觉线索。评估8个开源和2个闭源VLMs，对比传统视觉退化与语义干扰下的鲁棒性。

Result: 1) Distract-Bench暴露了与视觉退化不同的鲁棒性失效模式；2) 推理型VLM在感知退化下与非推理基模型表现相近，但对语义干扰的鲁棒性始终更低；3) 语义干扰常进入推理过程，被当作证据，导致错误答案。

Conclusion: 推理型VLMs的鲁棒性评估应从感知退化转向语义干扰，后者是真实世界视觉推理中更关键的鲁棒性挑战。

Original Abstract: Reasoning Vision-Language Models (VLMs) achieve strong performance on complex multimodal tasks, but reliable real-world application requires handling visual inputs that are messier than clean, curated benchmarks. Existing works mainly evaluate such reliability of VLMs through input corruptions, such as noise, blur and weather effects, which make visual evidence harder to perceive. This leaves a critical reliability failure mode underexplored: a model may perceive the evidence correctly, yet reason from plausible but irrelevant and distracting evidence and propagate this mistake to its final answer. To address this gap, we introduce \textbf{Distract-Bench}, a benchmark for evaluating VLM robustness to \textbf{semantic visual distractions}, defined as meaningful but task-irrelevant visual cues added to inputs while preserving the ground-truth answer. We comprehensively evaluate eight leading open-source and two closed-source VLMs across conventional vision corruptions and Distract-Bench. Our results show that Distract-Bench exposes a robustness failure distinct from vision corruptions: reasoning VLMs largely track their non-reasoning base models under perceptual degradation, but show consistently lower robustness to semantic distractions. Further analysis shows that these distractions often enter the reasoning process of VLMs, are treated as evidence, and lead to incorrect answers. Together, these findings reframe robustness evaluation for reasoning VLMs, shifting the focus from degraded perception to distractions for reliable real-world visual reasoning. Our data and code are available at https://github.com/Yizheng-Sun/Distract-Bench.

中文摘要翻译: 推理型视觉语言模型（VLMs）在复杂的多模态任务上表现出色，但可靠的现实应用需要处理比干净、精心设计的基准更为杂乱的视觉输入。现有工作主要通过输入退化（如噪声、模糊和天气效果）来评估VLMs的此类可靠性，这些退化使得视觉证据更难以感知。这留下了一种关键可靠性失效模式未被充分探索：模型可能正确感知证据，却从看似合理但与任务无关的干扰证据中推理，并将该错误传播到最终答案。为填补这一空白，我们提出了\textbf{Distract-Bench}，一个用于评估VLM对\textbf{语义视觉干扰}鲁棒性的基准，其中语义视觉干扰定义为在保留真实答案的同时向输入中添加有意义但与任务无关的视觉线索。我们全面评估了8个领先的开源和2个闭源VLM，涵盖传统视觉退化与Distract-Bench两个方面。结果表明，Distract-Bench暴露了一种与视觉退化不同的鲁棒性失效：推理型VLM在感知退化下与其非推理基模型的表现大致相当，但对语义干扰的鲁棒性始终更低。进一步分析表明，这些干扰常进入VLMs的推理过程，被当作证据，并导致错误答案。综上，这些发现重新定义了推理型VLMs的鲁棒性评估，将关注点从感知退化转向干扰，以实现可靠的现实世界视觉推理。我们的数据和代码见https://github.com/Yizheng-Sun/Distract-Bench。

</details>


### [126] [A multi-agent system for spine MRI report generation from multi-sequence imaging](https://arxiv.org/abs/2606.08897)
*Zhiping Xiao,Junwei Yang,Gongbo Sun,Han Zhang,Hanwen Xu,Yi Yao,Zachary D. Miller,William E. King,Mohammed M. Kanani,Jalal B. Andre,Sammy Chu,Ming Zhang,Paul E. Kinahan,Nathan M. Cross,Sheng Wang*

Main category: cs.CV

TL;DR: SpineAgent是一个基于多序列基础模型的多智能体框架，用于脊柱MRI报告生成，在32,047名患者的大规模临床数据上训练，通过DINOv3编码器和持续训练策略整合多序列信息，在报告生成、病理定位和跨厂商泛化方面达到最优性能。

<details>
  <summary>Details</summary>

Motivation: 脊柱MRI解读复杂且耗时，需要整合多序列和多个解剖区域的信息。现有自动分析方法在有效结合多序列数据的同时保留序列特定诊断信息方面仍面临挑战。

Method: 首先在T1和T2加权序列上分别预训练两个DINOv3编码器；然后引入持续训练策略，学习一个合成器利用T1和T2编码器嵌入其他序列的图像，生成整合多序列信号的患者级嵌入；最后将这些嵌入能力集成到37个专业智能体中，其输出作为结构化标记输入到端到端训练的医学报告智能体中进行报告生成。

Result: 在跨厂商和跨队列评估中展现了强泛化能力；实现了病理定位（识别相关切片和分割病理区域）；支持多模态图像-报告检索；通过自动评估指标和五位放射科专家的评估，在脊柱MRI报告生成任务上达到领先性能。

Conclusion: SpineAgent实现了可扩展、可解释的脊柱MRI报告生成，为自动化医学影像报告提供了坚实框架。

Original Abstract: Spinal pathology is a leading cause of pain and disability worldwide. Spine MRI is central to clinical evaluation, yet its interpretation remains complex and time-consuming, requiring integration of information across multiple imaging sequences and anatomical regions. Despite recent advances in automated MRI analysis, effectively combining multi-sequence data while preserving sequence-specific diagnostic information remains an open challenge. Here we present SpineAgent, a multi-agent framework for spine MRI report generation built upon a multi-sequence foundation model trained on routine clinical data from 32,047 patients and 453,683 MRI series, comprising a total of 13,441,191 MRI slices. To accommodate diverse modalities of sequences, we first pre-train two DINOv3-based encoders separately on T1- and T2-weighted sequences. We then introduce a continual training strategy that learns a synthesizer to embed images of other sequences using the T1 and T2 encoders, producing patient-level embedding that integrates various signals across MRI sequences. Using these embeddings, SpineAgent achieves state-of-the-art performance, and demonstrates strong generalizability under cross-manufacturer and cross-cohort evaluation. Beyond classification, SpineAgent enables pathology localization by identifying findings-relevant slices and segmenting pathological regions. It also supports multimodal image-report retrieval, providing a solid foundation for scalable and explainable MRI report generation. We further integrate these validated capabilities of SpineAgent into 37 specialized agents. Finally, we incorporate their outputs as structured tokens within a Medical Report Agent trained end-to-end for report generation. Through both automated metrics and expert evaluation by five radiologists, SpineAgent achieves leading performance in spine MRI report generation.

中文摘要翻译: 脊柱病理是全球范围内导致疼痛和残疾的主要原因之一。脊柱MRI是临床评估的核心手段，但其解读复杂且耗时，需要整合来自多个成像序列和解剖区域的信息。尽管近年来自动MRI分析取得了进展，但如何有效结合多序列数据同时保留序列特定的诊断信息仍然是一个开放的挑战。本文提出了SpineAgent，一个基于多序列基础模型的脊柱MRI报告生成多智能体框架，该基础模型在32,047名患者和453,683个MRI序列（共计13,441,191张MRI切片）的常规临床数据上训练。为了适应多样的序列模态，我们首先在T1和T2加权序列上分别预训练两个基于DINOv3的编码器。然后，我们引入一种持续训练策略，学习一个合成器利用T1和T2编码器嵌入其他序列的图像，从而生成整合了跨MRI序列多种信号的患者级嵌入。利用这些嵌入，SpineAgent取得了最先进的性能，并在跨厂商和跨队列评估中展现出强大的泛化能力。除了分类任务，SpineAgent还能通过识别与病变相关的切片并分割病理区域来实现病理定位。它还支持多模态图像-报告检索，为可扩展且可解释的MRI报告生成提供了坚实基础。我们进一步将SpineAgent的这些已验证能力集成到37个专业智能体中。最后，我们将它们的输出作为结构化标记整合到一个端到端训练的医学报告智能体中用于报告生成。通过自动评估指标和五位放射科专家的评估，SpineAgent在脊柱MRI报告生成方面取得了领先性能。

</details>


### [127] [DifferSeg: Towards Diverse Multimodal Binary Segmentation via Differential Perception and Frequency Guidance](https://arxiv.org/abs/2606.08906)
*Qiangqiang Zhou,Jiawei Xu,Yong Chen,Dandan Zhu,Yugen Yi,Xiaoqi Zhao*

Main category: cs.CV

TL;DR: 提出一个通用多模态二值分割框架DifferSeg，通过差分感知融合（DPF）模块自适应对齐多模态特征，以及频率引导解码器（FGD）平衡高低频表征，在29个公共数据集、18个下游任务上超越67种SOTA方法。

<details>
  <summary>Details</summary>

Motivation: 现有方法依赖固定特征拼接进行跨模态交互，且解码器设计以低频语义为主，缺乏处理模态差异与互补性的自适应机制，以及平衡高低频表征的高效解码策略。

Method: DifferSeg包含两个核心模块：（1）差分感知融合（DPF）模块，使用可学习的差分算子自适应对齐多模态特征，并通过残差融合增强互补性；（2）频率引导解码器（FGD），构建跨频率交互和多路径上采样，维持高频细节结构与低频语义表征的一致性。

Result: 在29个公开数据集、18个下游任务上，超越67种当前最先进方法，展现优异的泛化能力和分割精度。

Conclusion: DifferSeg是一个简单通用的多模态二值分割框架，有效解决了模态差异融合和频率平衡两大挑战，适用于自然图像和医学影像等多种模态。

Original Abstract: In many binary segmentation tasks, most multimodal methods rely on fixed feature concatenation for cross-modal interaction and straightforward decoder designs dominated by low-frequency semantics. %ToDO: %
However, they ignore two key challenges: one is the lack of an adaptive mechanism to handle modality discrepancies and complementarity, and the other is the absence of an efficient decoding strategy to balance both high- and low-frequency representations. %
In this work, we propose a simple yet general multimodal binary segmentation framework, termed DifferSeg, to address both problems simultaneously. With the help of the differential perception fusion (DPF) module, DifferSeg employs learnable differential operators to adaptively align multimodal features and enhance their complementarity through residual fusion, effectively mitigating modality mismatch and fusion redundancy. %
In addition, we design a frequency-guided decoder (FGD) that builds cross-frequency interactions and multi-path upsampling to maintain consistency between detailed high-frequency structures and semantic low-frequency representations, ensuring fine-grained boundary recovery and noise suppression. %
Benefiting from these designs, DifferSeg can be easily generalized to diverse binary segmentation tasks, including both natural and medical modalities. Without bells and whistles, it consistently surpasses 67 state-of-the-art methods across 29 public datasets involving 18 downstream tasks, demonstrating superior generalization and segmentation accuracy.Code and pretrained models will be available at the Link.

中文摘要翻译: 在许多二值分割任务中，大多数多模态方法依赖于固定的特征拼接进行跨模态交互，并采用由低频语义主导的简单解码器设计。然而，它们忽略了两个关键挑战：一是缺乏处理模态差异与互补性的自适应机制，二是缺少平衡高低频表征的高效解码策略。本文提出一个简单却通用的多模态二值分割框架DifferSeg，同时解决上述两个问题。借助差分感知融合（DPF）模块，DifferSeg利用可学习的差分算子自适应对齐多模态特征，并通过残差融合增强其互补性，有效缓解了模态不匹配和融合冗余问题。此外，我们设计了频率引导解码器（FGD），构建跨频率交互和多路径上采样，以保持高频细节结构与低频语义表征之间的一致性，确保精细边界恢复和噪声抑制。得益于这些设计，DifferSeg可以轻松泛化到多种二值分割任务，涵盖自然图像和医学影像模态。无需繁琐的辅助操作，它便在涉及18个下游任务的29个公共数据集上持续超越67种最先进方法，展现出卓越的泛化能力和分割精度。代码与预训练模型将在此链接公开。

</details>


### [128] [Failure-Aware Refinement of Vision-Language Model for Lithography Defect Detection](https://arxiv.org/abs/2606.08908)
*Pangyun Jeong,Jiyeong Kong,Yuehua Hu,Dohee Jeong,Kyung-Tae Kang*

Main category: cs.CV

TL;DR: 提出了一种两阶段视觉-语言框架用于半导体光刻缺陷检测，第一阶段用LoRA微调Qwen3-VL进行初始检测，第二阶段通过修正第一阶段的预测错误来训练精化模块，从而提升检测性能。

<details>
  <summary>Details</summary>

Motivation: 直接微调的视觉-语言模型在光刻缺陷检测中仍会产生误检、漏检和缺陷类型错误等常见测试时错误，需要额外的精化机制来修正这些错误。

Method: 两阶段视觉-语言框架：第一阶段使用LoRA微调Qwen3-VL作为视觉-语言适配器，预测缺陷数量、类别和归一化边界框；第二阶段利用第一阶段的预测失败样本及其修正标签训练一个精化模块，对初始输出进行复审和修正。

Result: 通过从初始适配器失败的案例中学习，精化过程能够在单阶段微调基础上进一步改善缺陷推理性能。

Conclusion: 两阶段框架通过引入基于失败案例学习的精化机制，有效弥补了单阶段视觉-语言微调在半导体缺陷检测中的不足，提升了检测的准确性和鲁棒性。

Original Abstract: Semiconductor lithography inspection requires reliable detection of small pattern defects such as bridge, burr, pinch, and contamination. In this study, we propose a two-stage vision-language framework that combines initial defect detection with prediction refinement. In the first stage, Qwen3-VL is fine-tuned with LoRA as a vision-language adapter to predict defect counts, defect categories, and normalized bounding boxes from lithography images. However, direct fine-tuning may still produce common test-time errors, including false positives, missed defects, and incorrect defect types. To address this limitation, the second stage trains a refinement module using first-stage prediction failures and their corrected labels, allowing the model to review and revise initial outputs. By learning from cases where the initial adapter fails, the refinement process improves defect inference beyond single-stage fine-tuning.

中文摘要翻译: 半导体光刻检测需要可靠地检测出桥接、毛刺、凹陷和污染等微小图案缺陷。本研究提出了一种两阶段视觉-语言框架，将初始缺陷检测与预测精化相结合。在第一阶段，使用LoRA对Qwen3-VL进行微调，作为视觉-语言适配器，从光刻图像中预测缺陷数量、缺陷类别和归一化边界框。然而，直接微调仍可能产生常见的测试时错误，包括误检、漏检和错误缺陷类型。为解决这一局限性，第二阶段利用第一阶段的预测失败样本及其修正标签训练一个精化模块，使模型能够复审和修正初始输出。通过从初始适配器失败的案例中学习，精化过程在单阶段微调的基础上进一步提升了缺陷推理能力。

</details>


### [129] [When Vision Misleads, Let Location Speak: A Worldwide Image Geo-Localization Method via Location Attention Mechanism and Large Multimodal Models](https://arxiv.org/abs/2606.08918)
*Junchao Cui,Wenqi Shi,Xuanzi Ma,Nan Wu,Shaoyong Du,Xiangyang Luo*

Main category: cs.CV

TL;DR: TransGeoCLIP 提出了一种结合位置注意力机制和大规模多模态模型的检索式图像地理定位框架，通过在CLIP中引入位置编码的Transformer来区分视觉相似但地理不同的图像，显著提升了全局定位精度。

<details>
  <summary>Details</summary>

Motivation: 现有图像地理定位方法在处理视觉相似但来自不同地理区域的图像时容易产生误匹配，限制了实际应用中的可靠性，亟需一种能够有效区分视觉相似场景中地理特征差异的方法。

Method: 提出两阶段框架TransGeoCLIP：1）检索数据库构建阶段，使用带有位置注意力机制的Transformer编码GPS坐标，通过CLIP实现图像-文本-GPS联合嵌入；2）检索增强推理阶段，利用大规模多模态模型（LMMs）从检索结果中推断最终位置预测。

Result: 在IM2GPS、IM2GPS3k、YFCC4k和YFCC26k数据集上，TransGeoCLIP在1公里误差内的街景级定位精度分别超越现有最优方法1.5%、1.07%、7.18%和9.75%。

Conclusion: TransGeoCLIP通过位置注意力机制与多模态大模型的结合，有效解决了视觉相似图像的地理混淆问题，显著提升了全局图像地理定位的可靠性。

Original Abstract: Worldwide image geo-localization aims to determine the capture location of an image on a global scale. Existing methods often mislocalize images by matching them to visually similar scenes from different geographic regions, which limits reliability in practical applications. To address this issue, we propose TransGeoCLIP, a novel retrieval-based framework that integrates a location attention mechanism and large multimodal models (LMMs). Using the Transformer encoder with location attention to encode GPS coordinates, TransGeoCLIP can effectively distinguish geographic features among visually similar images. The framework consists of two stages: 1) Retrieval database construction, which employs Transformers equipped with location attention mechanisms to encode labeled GPS coordinates and enhance location semantics, subsequently enables joint image-text-GPS embedding through CLIP; 2) Retrieval-augmented inference, which leverages LMMs to infer the final image location prediction from retrieved database results. Extensive experimental results on diverse datasets, including IM2GPS, IM2GPS3k, YFCC4k, and YFCC26k, demonstrate that TransGeoCLIP significantly enhances localization performance for visually similar images. Particularly, street-level localization accuracy (within 1 km error) is substantially improved, surpassing state-of-the-art methods by 1.5%, 1.07%, 7.18%, and 9.75% on these benchmarks, respectively.

中文摘要翻译: 全球图像地理定位旨在确定一张图像在全球范围内拍摄位置。现有方法通常因将图像与来自不同地理区域的视觉相似场景进行匹配而导致定位错误，限制了实际应用中的可靠性。为解决这一问题，我们提出TransGeoCLIP，一种新颖的基于检索的框架，它融合了位置注意力机制和大规模多模态模型（LMMs）。通过使用带有位置注意力的Transformer编码器对GPS坐标进行编码，TransGeoCLIP能够有效区分视觉相似图像中的地理特征。该框架包含两个阶段：1）检索数据库构建，采用配备位置注意力机制的Transformer对标注的GPS坐标进行编码并增强位置语义，进而通过CLIP实现图像-文本-GPS联合嵌入；2）检索增强推理，利用大规模多模态模型从检索到的数据库结果中推断最终的图像位置预测。在IM2GPS、IM2GPS3k、YFCC4k和YFCC26k等多个数据集上的广泛实验结果表明，TransGeoCLIP显著提升了视觉相似图像的定位性能。特别地，街景级定位精度（1公里误差内）在这些基准上分别超越现有最优方法1.5%、1.07%、7.18%和9.75%。

</details>


### [130] [PolyBuild: An End-to-End Method for Polygonal Building Contour Extraction from High-Resolution Remote Sensing Images](https://arxiv.org/abs/2606.08920)
*Yaoteng Zhang,Julin Zhang,Guangshuai Wang,Jiwei Deng,Hui Sheng,Yasir Muhammad,Shiqing Wei*

Main category: cs.CV

TL;DR: 本文提出PolyBuild，一种端到端的建筑物矢量多边形直接提取方法，通过初始轮廓生成模块和轮廓优化模块，无需后处理，在三个数据集上优于现有方法。

<details>
  <summary>Details</summary>

Motivation: 现有主流的建筑物提取方法依赖像素级分割加多重后处理，计算量大且易出错，缺乏能够直接从遥感图像端到端提取矢量多边形的高效方法。

Method: PolyBuild包含两个核心模块：1) 初始轮廓生成模块(ICGM)，利用拼接的子区域中心特征同时进行目标检测和初始轮廓提取；2) 轮廓优化模块(COM)，在基于Transformer的解码器中迭代融合CNN特征与轮廓位置信息，实现轮廓精细化。整体采用混合CNN-Transformer架构。

Result: 在三个建筑物数据集上的大量实验表明，PolyBuild显著优于包括基于掩膜和基于轮廓在内的最先进方法。

Conclusion: PolyBuild作为端到端方法，无需后处理即可直接提取高质量建筑物矢量多边形，混合CNN-Transformer架构有效捕捉了局部与全局空间关系，在建筑轮廓提取任务上取得了显著性能提升。

Original Abstract: Extracting building polygon contours from high-resolution remote sensing images is a fundamental task for various mapping applications. However, the presence of varying imaging conditions and complex building structures, makes automatic contour extraction extremely challenging. Mainstream approaches for building extraction often rely on pixel-level segmentation followed by multiple post-processing steps to produce building contour, which can be computationally intensive and prone to errors. In this paper, we propose an end-to-end method named PolyBuild, which can directly extract building vector polygons from high-resolution remote sensing images without the need for any post-processing operations. The proposed method leverages two primary modules: an Initial Contour Generation Module (ICGM) and a Contour Optimization Module (COM). The ICGM is designed to generate an initial building contour by utilizing concatenated sub-region center features for each building instance. It performs simultaneous object detection and initial contour extraction by generating bounding boxes and using the center features of four sub-regions to represent each building. The Contour Optimization Module (COM) further refines the generated building contours by iteratively integrating Convolutional Neural Network (CNN) features and contour positional information in a Transformer-based decoder. The hybrid CNN-Transformer architecture effectively captures both local and global spatial relationships within the building contour, ensuring high-quality boundary delineation. Extensive experiments are conducted on three building datasets to evaluate the performance of PolyBuild. The results demonstrate that PolyBuild significantly outperforms state-of-the-art methods, including mask-based and contour-based approaches.

中文摘要翻译: 从高分辨率遥感图像中提取建筑物多边形轮廓是各类测绘制图应用的基础任务。然而，多变的成像条件与复杂的建筑结构使得自动轮廓提取极具挑战性。主流的建筑物提取方法通常依赖像素级分割及后续多重后处理步骤来生成建筑轮廓，这种方法计算量大且容易出错。本文提出一种名为PolyBuild的端到端方法，可直接从高分辨率遥感图像中提取建筑物矢量多边形，无需任何后处理操作。该方法利用两个核心模块：初始轮廓生成模块（ICGM）和轮廓优化模块（COM）。ICGM通过利用每个建筑实例的拼接子区域中心特征来生成初始建筑轮廓，它通过生成边界框并使用四个子区域的中心特征来表示每个建筑，从而同时完成目标检测和初始轮廓提取。轮廓优化模块（COM）则在基于Transformer的解码器中，通过迭代融合卷积神经网络（CNN）特征与轮廓位置信息来进一步优化生成的建筑轮廓。这种混合CNN-Transformer架构有效捕捉了建筑轮廓内部的局部与全局空间关系，确保了高质量的边界描绘。在三个建筑物数据集上进行了大量实验以评估PolyBuild的性能。结果表明，PolyBuild显著优于包括基于掩膜和基于轮廓的方法在内的最先进方法。

</details>


### [131] [NutriMLLM: Multimodal Large Language Models for Dietary Micronutrient Analysis](https://arxiv.org/abs/2606.08948)
*Runze Yan,Minxiao Wang,Jiaying Lu,Darren Liu,Xiao Hu,Hanqi Luo*

Main category: cs.CV

TL;DR: 本文提出了一种利用24小时饮食回忆数据驱动文本生成图像，构建约110万图像-描述-营养素三元组的合成语料库，并微调视觉语言模型得到NutriMLLM系列模型，实现了从食物图像中全面估计65种微量营养素，性能媲美甚至超越GPT-5等专有模型。

<details>
  <summary>Details</summary>

Motivation: 现有多模态大语言模型（MLLMs）在从食物图像估计膳食微量营养素方面不可靠，常出现拒绝回答或返回统计上不合理的值，且该任务缺乏大规模、完整营养素标注的多模态数据集。

Method: 利用过去十年的大规模24小时饮食回忆数据，将其构建为结构化提示用于文本到图像生成，生成约110万图像-描述-营养素三元组。然后在该合成语料库上微调Qwen3-VL（2B/4B/8B/30B）和GLM-4.6V-Flash，得到NutriMLLM系列视觉语言模型。

Result: NutriMLLM系列模型在真实食物图像上对所有65种营养素实现了接近完全的覆盖，最大变体在大多数营养素上的准确性与GPT-5、Gemini 3、Claude Sonnet 4.5等专有基线模型相当或更优。

Conclusion: 回忆驱动的合成监督方法使基于图像的全面微量营养素估计成为可处理的工程问题，可支持膳食评估、个性化营养指导和人群规模的微量营养素监测。

Original Abstract: Comprehensive estimation of dietary micronutrients from food images could improve clinical nutrition care, but training such models requires large multimodal datasets linking diverse foods to complete nutrient profiles. We first show that existing multimodal large language models (MLLMs), including leading proprietary models, are unreliable for this task. Across five model families and four independent evaluation benchmarks (ASA24, SNAPMe, FNDDS, and NutriBench), models frequently abstained or returned statistically implausible values. To address this gap without costly expert annotation, we repurposed a decade of population-scale 24-hour dietary recalls as structured prompts for text-to-image generation. This pipeline produced a synthetic corpus of about 1.1 million image-description-nutrient triplets, each pairing a generated food image with a complete 65-nutrient label. To our knowledge, this is the largest synthetic food-image corpus with comprehensive micronutrient annotation planned for public release upon publication. Fine-tuning Qwen3-VL (2B/4B/8B/30B) and GLM-4.6V-Flash on this corpus yielded NutriMLLM, the first family of vision-language models specialized for comprehensive dietary micronutrient estimation. We evaluate these models with a four-component framework that separately measures abstention, hallucination, overall usability, and per-nutrient numerical accuracy. On real food images, every NutriMLLM variant achieved near-complete coverage across all 65 nutrients, and the largest variant matched or exceeded proprietary baselines (GPT-5, Gemini 3, and Claude Sonnet 4.5) in accuracy on most nutrients. These results show that recall-driven synthetic supervision can make image-based comprehensive micronutrient estimation a tractable engineering problem and support dietary assessment, personalized nutrition guidance, and population-scale micronutrient surveillance.

中文摘要翻译: 从食物图像中全面估计膳食微量营养素可以改善临床营养护理，但训练此类模型需要连接多样化食物与完整营养素概况的大规模多模态数据集。我们首先证明了现有的多模态大语言模型（MLLMs），包括领先的专有模型，在此任务上不可靠。在五个模型系列和四个独立评估基准（ASA24、SNAPMe、FNDDS和NutriBench）上，模型经常拒绝回答或返回统计上不合理的值。为解决这一差距而不进行昂贵的人工标注，我们将过去十年的人群规模24小时饮食回忆重新利用为文本到图像生成的结构化提示。该流程产生了约110万图像-描述-营养素三元组的合成语料库，每个三元组将生成的食品图像与完整的65种营养素标签配对。据我们所知，这是计划在发表后公开发布的最大规模、具有全面微量营养素标注的合成食品图像语料库。在该语料库上微调Qwen3-VL（2B/4B/8B/30B）和GLM-4.6V-Flash，得到了NutriMLLM——首个专门用于全面膳食微量营养素估计的视觉语言模型家族。我们使用一个四组件框架评估这些模型，该框架分别衡量弃答、幻觉、整体可用性和每种营养素的数值准确性。在真实食物图像上，每个NutriMLLM变体在所有65种营养素上都实现了接近完全的覆盖，最大的变体在大多数营养素上的准确性与专有基线模型（GPT-5、Gemini 3和Claude Sonnet 4.5）相当或更优。这些结果表明，回忆驱动的合成监督可以使基于图像的全面微量营养素估计成为一个可处理的工程问题，并支持膳食评估、个性化营养指导和人群规模的微量营养素监测。

</details>


### [132] [Rethinking 3D Shape Generation: Diffusion over Superquadrics](https://arxiv.org/abs/2606.08957)
*Zhiyang Liu,Wanze Li,Yuwei Wu,Chengran Yuan,Jiawei Sun,Rui Zheng,Marcelo H Ang*

Main category: cs.CV

TL;DR: 提出将扩散模型从高维稠密几何表示迁移到紧凑的超二次曲面参数（7KB），实现高效、可扩展的3D形状生成，支持无分辨率点云解码、部件级编辑和约束设计，每形状生成仅需0.6秒。

<details>
  <summary>Details</summary>

Motivation: 现有基于扩散模型的3D形状生成方法大多在高基数空间（如体素/SDF网格、网格或点云）中进行去噪，计算和内存开销巨大，难以在更高分辨率和更强可控性方面扩展。

Method: 将3D形状表示为少量超二次曲面（superquadrics）的紧凑集合，利用仅7KB的超二次曲面参数（姿态、大小和形状）替代传统的成千上万的几何表示值，在紧凑的几何基元空间而非稠密几何空间中进行扩散过程。

Result: 在标准基准上取得具有竞争力的表面保真度和分布性能，支持无分辨率点云解码、部件级编辑和基于约束的设计，每种条件下每形状生成仅需0.6秒。

Conclusion: 扩散到紧凑几何基元（超二次曲面）的策略大幅降低了扩散状态的维度和每步计算/内存开销，提升了可扩展性，同时保持了生成质量和效率。

Original Abstract: Diffusion models have advanced 3D shape generation, yet most methods still denoise in high-cardinality spaces (e.g., voxel/SDF grids, meshes, or point clouds), which is computationally and memory intensive and makes it difficult to scale in terms of both higher resolution and stronger controllability. We rethink the diffusion representation and propose to move diffusion from dense geometry to compact geometric primitives, representing each shape as a small set of superquadrics. Instead of operating on thousands to millions of geometric representation values, we leverage 7KB superquadric parameters (pose, size, and shape), drastically reducing diffusion-state dimensionality and per-step compute/memory. Our diffusion-over-superquadrics improves scalability by supporting broader capabilities (e.g., resolution-free point-cloud decoding, part-level editing, and constraint-based design) and achieving competitive surface-fidelity and distributional performance on standard benchmarks after point-cloud decoding, while enabling efficient generation within 0.6s per shape for most conditions.

中文摘要翻译: 扩散模型推动了3D形状生成的发展，然而大多数方法仍在高基数空间（如体素/SDF网格、网格或点云）中进行去噪，这不仅计算和内存开销巨大，而且在更高分辨率和更强可控性方面难以扩展。我们重新思考了扩散表示，提出将扩散从稠密几何转移到紧凑几何基元，将每个形状表示为少量超二次曲面的集合。不同于在成千上万个几何表示值上操作，我们利用仅7KB的超二次曲面参数（姿态、大小和形状），大幅降低了扩散状态的维度以及每一步的计算/内存开销。这种基于超二次曲面的扩散方法通过支持更广泛的能力（如无分辨率点云解码、部件级编辑和基于约束的设计）提升了可扩展性，在点云解码后的标准基准上取得了具有竞争力的表面保真度和分布性能，同时在大多数条件下实现了每形状0.6秒内的高效生成。

</details>


### [133] [ChinaHeritaQA: A Culturally-Grounded Visual Question Answering Dataset for World Heritage Sites in China](https://arxiv.org/abs/2606.08959)
*Yi Zhang,Bolei Ma,Yong Cao,Chengyan Wu,Daniel Hershcovich,Anna-Carolina Haensch*

Main category: cs.CV

TL;DR: 提出了中国文化遗产基准数据集ChinaHeritaQA，用于评估视觉语言模型在联合国教科文组织世界遗产上的文化推理能力。包含2,279张图像和14,133个双语多选题，涵盖七个认知维度。评估显示顶级模型平均超越人类，但在文化推理任务上表现不佳，视觉检索能力强但文化和历史理解弱。

<details>
  <summary>Details</summary>

Motivation: 现有视觉语言模型在文化理解方面存在不足，缺乏专门针对中国世界文化遗产的多模态基准数据集来系统评估模型的文化推理能力。

Method: 构建了包含2,279张图像和14,133个双语（中/英）多选题的ChinaHeritaQA数据集，基于联合国教科文组织遗产本体论进行标注，涵盖七个认知维度，并通过严格人工标注确保质量。

Result: 顶级VLM在平均性能上超越人类，但在文化推理任务上存在显著差异；模型在视觉识别任务上表现出色，但在文化历史理解方面表现不佳；性能因朝代和地区而异。

Conclusion: 视觉语言模型强大的视觉检索能力并不能推广到文化和历史理解领域，需要进一步研究文化感知的多模态学习。

Original Abstract: We introduce ChinaHeritaQA, a multimodal benchmark dataset for evaluating the cultural reasoning abilities of vision-language models (VLMs) on UNESCO World Heritage sites in China. The dataset comprises 2,279 in-the-wild images paired with 14,133 bilingual (Chinese/English) multiple-choice QA pairs spanning seven cognitive dimensions, from basic identity recognition to historical periodization and architectural analysis. Guided by a UNESCO-aligned heritage ontology and verified through rigorous human annotation, the dataset ensures linguistic quality and factual consistency. Evaluations of state-of-the-art VLMs reveal that while top models exceed human performance on average, substantial task-level variation emerges: models excel at visual recognition but struggle with culturally grounded reasoning. Performance also varies by dynasty and region. ChinaHeritaQA reveals that strong visual retrieval does not extend to cultural and historical understanding. We release the dataset to support future research on culturally aware multimodal learning.

中文摘要翻译: 我们介绍了ChinaHeritaQA，一个用于评估视觉语言模型在联合国教科文组织中国世界遗产上文化推理能力的多模态基准数据集。该数据集包含2,279张野外图像，配以14,133个双语（中/英）多选题对，涵盖从基本身份识别到历史分期和建筑分析等七个认知维度。在联合国教科文组织遗产本体论的指导下，并通过严格的人工标注进行验证，数据集确保了语言质量和事实一致性。对最先进视觉语言模型的评估显示，虽然顶级模型在平均性能上超越人类，但出现了显著的任务级差异：模型在视觉识别方面表现出色，但在基于文化的推理方面存在困难。性能也因朝代和地区而异。ChinaHeritaQA揭示了强大的视觉检索能力并不能推广到文化和历史理解。我们发布该数据集以支持未来关于文化感知多模态学习的研究。

</details>


### [134] [EPS3D: End-to-End Feed-Forward 3D Panoptic Segmentation](https://arxiv.org/abs/2606.08980)
*Runsong Zhu,Jiaxin Guo,Xiaoyang Guo,Zhengzhe Liu,Ka-Hei Hui,Wei Yin,Kai Chen,Wei Chen,Weiqiang Ren,Yunhui Liu,Pheng-Ann Heng,Chi-Wing Fu*

Main category: cs.CV

TL;DR: EPS3D是一个端到端的前馈框架，用于开放词汇3D全景分割，通过蒸馏训练策略和语义-实例互增强模块，在Replica等基准上实现了SOTA性能（语义mIoU提升13%），且每场景仅需1秒。

<details>
  <summary>Details</summary>

Motivation: 现有开放词汇3D全景分割方法依赖额外的预处理步骤（如2D分割或3D重建），导致误差累积和3D一致性差。

Method: 设计端到端架构，采用蒸馏训练策略从多视图图像预测3D感知的语义和实例特征；提出互增强模块（Ins2Sem：实例内语义对齐；Sem2Ins：语义引导实例特征细化），实现语义-实例内在一致性。

Result: 在Replica基准上语义mIoU提升13%；每场景推理仅需1秒；在机器人操作和3D场景编辑等任务上表现优异。

Conclusion: EPS3D通过端到端设计和语义-实例互增强机制，有效提升了开放词汇3D全景分割的精度和效率。

Original Abstract: This paper introduces EPS3D, a new end-to-end feed-forward framework for open-vocabulary 3D panoptic segmentation. Unlike existing methods relying on additional preprocessing, we design an end-to-end architecture, with a distillation-based training strategy on diverse 3D scenes to predict 3D-aware semantic and instance features from multi-view images, improving 3D consistency and avoiding error accumulation. We further propose a mutual enhancement module to enforce inherent semantic-instance consistency. By aligning semantics within instances (Ins2Sem) and refining instance features with semantic guidance (Sem2Ins), we achieve more coherent 3D scene understanding. Ultimately, EPS3D outperforms SOTA baselines on two benchmarks (e.g., +13% mIoU for semantics on Replica) with high efficiency (e.g., 1s per scene), supporting tasks like robotic manipulation and 3D scene editing.

中文摘要翻译: 本文提出了EPS3D，一个用于开放词汇3D全景分割的新型端到端前馈框架。与依赖额外预处理的现有方法不同，我们设计了一个端到端架构，结合基于蒸馏的训练策略，在多样化的3D场景中从多视图图像预测3D感知的语义和实例特征，从而提升3D一致性并避免误差累积。我们进一步提出了一个互增强模块，以强制实现内在的语义-实例一致性。通过在实例内对齐语义（Ins2Sem）以及通过语义引导细化实例特征（Sem2Ins），我们实现了更连贯的3D场景理解。最终，EPS3D在两个基准测试上超越了最先进的基线方法（例如，在Replica上语义mIoU提升13%），且具有高效率（例如，每场景1秒），支持机器人操作和3D场景编辑等任务。

</details>


### [135] [Scaling by Diversified Experience for Vision-Language-Action Models](https://arxiv.org/abs/2606.09009)
*Leiyu Wang,Zhaofengnian Wang,Xueqi Li,Luoyi Fan,Cewu Lu,Nanyang Ye*

Main category: cs.CV

TL;DR: SyVLA提出了一种意图解耦算法和基于相似样本引导的强化学习流程，解决了VLA模型中高层推理与低层控制纠缠以及策略优化不稳定的问题，在真实机器人任务和多模态基准测试中实现了更高的成功率和更强的分布外泛化能力。

<details>
  <summary>Details</summary>

Motivation: 现有VLA模型在真实部署中面临两大挑战：高层推理与低层控制相互纠缠导致控制不精准，以及策略优化不稳定带来的分布偏移问题。

Method: 1) 意图解耦算法：将控制相关特征从推理上下文中分离出来；2) 相似样本引导的强化学习流程：通过相似样本引导稳定策略更新并缓解分布偏移。

Result: 在真实机器人任务和多模态基准测试中，SyVLA取得了更高的任务成功率和更强的分布外泛化能力，同时有效保留了核心的视觉-语言能力。

Conclusion: SyVLA通过解耦控制和推理、引入稳定化的RL训练，有效提升了VLA模型的鲁棒性和泛化能力，为VLA模型在真实场景中的可靠部署提供了有效方案。

Original Abstract: Vision-Language-Action models face significant challenges in real-world deployment due to the entanglement of high-level reasoning with low-level control, and the instability of policy optimization. In this paper, we introduce SyVLA, a robust VLA model trained with diversified experiences. We propose an Intention Decoupling algorithm to isolate control-relevant features from reasoning contexts and a similar-sample guided RL pipeline to stabilize policy updates and mitigate distribution shift. Extensive experiments on real-world robotic tasks and multi-modal benchmarks demonstrate that SyVLA achieves superior task success rates and stronger out-of-distribution generalization compared to existing methods, while effectively preserving core vision-language capabilities. Codes and Datasets is released on \href{https://sy-vla.github.io/}{project page}.

中文摘要翻译: 视觉-语言-动作模型在实际部署中面临严峻挑战，原因在于高层推理与低层控制之间的纠缠以及策略优化的不稳定性。本文提出SyVLA，一种通过多样化经验训练的鲁棒VLA模型。我们提出了一种意图解耦算法，用于将控制相关特征从推理上下文中分离，并设计了一种基于相似样本引导的强化学习流程，以稳定策略更新并缓解分布偏移。在真实机器人任务和多模态基准测试上的大量实验表明，与现有方法相比，SyVLA实现了更高的任务成功率和更强的分布外泛化能力，同时有效保留了核心的视觉-语言能力。代码和数据集已发布在项目主页上。

</details>


### [136] [ATM: Action-Consistency Transfer Matrix for Diagnosing and Improving Latent World Models](https://arxiv.org/abs/2606.09028)
*Jiaheng Chen*

Main category: cs.CV

TL;DR: 提出ATM（动作一致性迁移矩阵）和AITS，用于诊断和提升潜在世界模型的动作语义保持能力，无需耗时的规划器耦合评估，实现百倍以上加速。

<details>
  <summary>Details</summary>

Motivation: 现有潜在世界模型的表示质量评估需要与规划器（如CEM）耦合进行仿真，耗时从分钟到小时不等，且是黑箱评估、依赖模型复杂度，缺乏快速、可解释的诊断工具。

Method: ATM通过轻量级事后探针，比较真实编码转移与模型预测转移中的动作信息，构建可解释的迁移矩阵，揭示表示质量、转移域不一致性和失败模式；并可折叠为简单筛选分数，用于跨检查点/变体的任务内排序。AITS将动作可识别性作为训练信号，直接提升下游规划性能。

Result: ATM在真实成功率差距显著时实现高度可靠的成对排序，将数分钟至数小时的CEM评估缩减为秒级转移分析，实现百倍以上加速。AITS证明动作可识别性不仅可用于诊断，还可作为训练信号改善规划性能。

Conclusion: ATM是一种高效、可解释的潜在世界模型诊断工具，AITS进一步表明动作可识别性可作为训练目标提升规划性能，两者共同推动潜在世界模型的实用化评估与训练。

Original Abstract: Latent world models are increasingly used for control and goal-conditioned planning, yet assessing whether their learned representations are useful for planning usually requires slow, planner-coupled simulator evaluation with CEM or similar planners. Such evaluation is black-box and model-complexity-dependent: under the same protocol, different world models may require minutes to hours per checkpoint. In this work, we propose ATM, an Action-Consistency Transfer Matrix for diagnosing whether latent transitions preserve action semantics relevant to planning. ATM compares action information in real encoded transitions and model-predicted transitions through lightweight post-hoc probes, producing an interpretable matrix that reveals representation quality, transition-domain inconsistency, and failure modes without simulator rollout. It can also be collapsed into a simple screening score for within-task ranking across checkpoints, variants, and world models. When the true success gap is non-trivial, ATM achieves highly reliable pairwise ranking, while reducing minutes-to-hours CEM evaluation to seconds-level transition analysis, yielding more than 100x speedup in our setup. We further introduce AITS, showing that action-identifiability is not only diagnostic but also a useful training signal for improving downstream planning without changing the planner.

中文摘要翻译: 潜在世界模型越来越多地被用于控制和目标条件规划，然而评估其学习到的表示是否对规划有用，通常需要与CEM等规划器耦合进行缓慢的仿真评估。这种评估是黑箱的且依赖于模型复杂度：在同一协议下，不同的世界模型每个检查点可能需要数分钟到数小时的评估时间。在这项工作中，我们提出ATM（动作一致性迁移矩阵），用于诊断潜在地转移是否保持了与规划相关的动作语义。ATM通过轻量级事后探针，比较真实编码转移和模型预测转移中的动作信息，生成一个可解释的矩阵，无需仿真器 rollout 即可揭示表示质量、转移域不一致性和失败模式。它还可以折叠成一个简单的筛选分数，用于跨检查点、变体和世界模型的任务内排序。当真实成功率差距显著时，ATM实现了高度可靠的成对排序，同时将数分钟到数小时的CEM评估缩减为秒级的转移分析，在我们的设置中实现了超过100倍的加速。我们进一步提出AITS，证明动作可识别性不仅可用于诊断，还可以作为有用的训练信号，在无需改变规划器的情况下改善下游规划性能。

</details>


### [137] [Frequency Decoupled Framework for Screen Content Image Super-Resolution](https://arxiv.org/abs/2606.09029)
*Xufei Wang,Qicheng Zhang,Qi Wu,Ziyang Gu,Shizhuang Weng*

Main category: cs.CV

TL;DR: 提出频解耦框架(FDF)，从相量角度重新思考屏幕内容图像超分辨率，通过振幅-相位分解网络(APFN)和振荡-非谐隐式拟合网络(OAIF-Net)联合利用结构化能量与关系连续性，在多个SCI数据集上达到SOTA性能。

<details>
  <summary>Details</summary>

Motivation: 现有基于隐式神经表示的方法在SCISR中忽略了图像固有的频率特性，导致性能次优。本文旨在从相量角度（振幅捕获结构化能量，相位捕获关系连续性）重新思考SCISR，以忠实恢复屏幕内容图像的规则纹理和全局配置。

Method: 提出频解耦框架(FDF)，包含两个核心模块：(1) 振幅-相位分解网络(APFN)，将图像分解为振幅流和相位流，振幅聚类模块(ACM)组织稀疏高能振幅响应为代表原型以提取周期模式，相位一致性自注意力(PCSA)通过连续一致性传播逐步增强配置；(2) 振荡-非谐隐式拟合网络(OAIF-Net)，集成周期性和相干性隐式表示，高效利用SCI中的周期模式与相干上下文。

Result: 在四个公开SCI数据集上的多尺度实验表明，FDF达到了SOTA的SCISR性能。消融实验验证了各组件在提取和利用周期模式与相干上下文方面的有效性。

Conclusion: 所提出的频解耦框架通过显式建模频率特性（振幅的周期性能量结构和相位的连续性关系），结合定制的隐式表示，能够有效提升屏幕内容图像超分辨率的重建质量。

Original Abstract: Methods based on implicit neural representations have demonstrated superior performance in Screen Content Image Super-Resolution (SCISR) . However, they overlooked the inherent frequency characteristics, leading to suboptimal performance. We propose a frequency decoupled framework (FDF) that rethinks SCISR from a phasor perspective by capturing structured energy in amplitude and relational continuity in phase, and jointly exploiting them with bespoke implicit representations to faithfully recover the regular textures and global configuration of Screen Content Image (SCI).
  Amplitude-Phase Factorization Network (APFN) first separates images into amplitude and phase streams, where Amplitude Clustering Module (ACM) organizes sparse yet high-energy amplitude responses into representative prototypes for periodic pattern extraction, while Phase Consistency Self-Attention (PCSA) progressively reinforces configuration through continuous consistency propagation.
  And Oscillation-Anharmonic Implicit Fitting Network (OAIF-Net) integrates periodic and coherent implicit representations for efficient exploitation of the periodic patterns and coherent context embedded in SCI.
  Experimental results show FDF achieves state-of-the-art SCISR performance at multiple scales across four public SCI datasets. Ablation experiments further demonstrate the effectiveness of each component in extracting and exploiting periodic patterns and coherent context.

中文摘要翻译: 基于隐式神经表示的方法在屏幕内容图像超分辨率（SCISR）中展现了优越的性能。然而，这些方法忽略了固有的频率特性，导致性能次优。本文从相量角度重新思考SCISR，提出了一种频解耦框架（FDF），通过捕获振幅中的结构化能量和相位中的关系连续性，并利用定制的隐式表示联合挖掘它们，以忠实恢复屏幕内容图像（SCI）的规则纹理和全局配置。振幅-相位分解网络（APFN）首先将图像分离为振幅流和相位流，其中振幅聚类模块（ACM）将稀疏但高能的振幅响应组织为代表性原型以提取周期模式，而相位一致性自注意力（PCSA）通过连续一致性传播逐步增强配置。振荡-非谐隐式拟合网络（OAIF-Net）集成了周期性和相干性隐式表示，以高效利用SCI中嵌入的周期模式和相干上下文。实验结果表明，FDF在四个公开SCI数据集上的多尺度SCISR性能均达到了最先进水平。消融实验进一步证明了各组件在提取和利用周期模式与相干上下文方面的有效性。

</details>


### [138] [CRANE: Knowledge Editing for Reasoning MLLMs](https://arxiv.org/abs/2606.09033)
*Han Huang,Hao Wang,Mengqi Zhang,Shu Wu,Qiang Liu,Liang Wang*

Main category: cs.CV

TL;DR: 本文揭示了推理多模态大语言模型（MLLMs）中知识编辑的三个失败模式（结构坍塌、认知失调、浅层内化），提出了CoT感知的评估基准ReasonEdit-Bench，并设计了无需修改参数的检索增强框架CRANE，通过双库检索和两阶段训练（SFT + GRPO认知路由奖励）在冲突场景下达到96.9%的Grounded Success。

<details>
  <summary>Details</summary>

Motivation: 现有的知识编辑方法在传统指标（教师强制准确率）下表现良好，但在推理MLLMs的链式思维（CoT）过程中存在严重失败（Grounded Success低至0%），因此需要揭示这些失败模式并设计新的评估协议和方法。

Method: 提出CRANE框架，包含模态感知的双库检索系统（modality-aware dual-library retrieval）和两阶段训练策略：先进行SFT结构初始化，再使用GRPO结合认知路由奖励（Cognitive Routing Reward），训练模型在视觉先验和注入编辑事实之间进行仲裁。

Result: 在ReasonEdit-Bench上，CRANE在冲突场景下Grounded Success达96.9%，多跳链中中间实体使用率达96.9%，文本局部性97.6%，图像局部性68.1%；在分布外基准MMEVOKE上，使用黄金检索达到87.0%。

Conclusion: 针对推理MLLMs的知识编辑需要CoT感知的评估方法，CRANE通过检索增强和认知路由机制有效解决了结构坍塌、认知失调和浅层内化问题，在无需修改参数的情况下实现了优异的编辑性能。

Original Abstract: The emergence of reasoning multimodal large language models (MLLMs), which generate explicit chain-of-thought (CoT) reasoning before producing answers, has introduced a new challenge for knowledge editing: methods that appear successful under traditional metrics (teacher-forcing accuracy up to 100%) can fail severely when the model's reasoning process is examined (Grounded Success as low as 0%). We identify three failure modes: (1) Structural Collapse, where weight-modifying methods destroy the CoT format; (2) Cognitive Dissonance, where the model's reasoning chain actively rejects the injected edit fact based on visual evidence; and (3) Shallow Internalization, where methods succeed on exact queries but fail on rephrase or multi-hop variants. On reasoning MLLMs, these modes interact: methods that generalize (FT, LoRA) trigger format collapse, while methods without deep modification cannot generalize. To expose these failures, we propose a CoT-aware evaluation protocol and construct ReasonEdit-Bench, with conflict stratification, multi-level probes, and multi-hop portability tests.
  We propose CRANE, a retrieval-augmented framework that requires no per-edit parameter modification. CRANE combines a modality-aware dual-library retrieval system with a two-phase training strategy: Supervised Fine-Tuning (SFT) for structural initialization, followed by GRPO with a Cognitive Routing Reward that trains the model to arbitrate between visual priors and injected edit facts. On ReasonEdit-Bench, CRANE achieves 96.9% Grounded Success on conflict scenarios and 96.9% intermediate entity usage in multi-hop chains, with 97.6% text-locality and 68.1% image-locality Edit Independence. On the out-of-distribution MMEVOKE benchmark, CRANE reaches 87.0% under gold retrieval.

中文摘要翻译: 推理多模态大语言模型（MLLMs）的兴起——这些模型在生成答案之前会产生显式的链式思维（CoT）推理——为知识编辑带来了新的挑战：在传统指标（教师强制准确率高达100%）下看似成功的方法，在检查模型的推理过程时可能严重失败（Grounded Success低至0%）。我们识别出三种失败模式：（1）结构坍塌，即权重修改方法破坏CoT格式；（2）认知失调，即模型的推理链基于视觉证据主动拒绝注入的编辑事实；（3）浅层内化，即方法在精确查询上成功，但在改写或多跳变体上失败。在推理MLLMs中，这些模式相互交织：泛化能力较好的方法（FT、LoRA）会引发格式坍塌，而缺乏深度修改的方法则无法泛化。为揭示这些失败，我们提出了一种CoT感知的评估协议，并构建了ReasonEdit-Bench，包含冲突分层、多级探针和多跳可迁移性测试。我们提出CRANE，一个检索增强框架，无需逐次编辑的参数修改。CRANE结合了模态感知的双库检索系统和两阶段训练策略：用于结构初始化的监督微调（SFT），随后是结合认知路由奖励的GRPO，该奖励训练模型在视觉先验和注入编辑事实之间进行仲裁。在ReasonEdit-Bench上，CRANE在冲突场景下达到96.9%的Grounded Success，多跳链中中间实体使用率达96.9%，文本局部性97.6%，图像局部性68.1%的编辑独立性。在分布外基准MMEVOKE上，CRANE在黄金检索条件下达到87.0%。

</details>


### [139] [Leveraging NeRF-Rendered Images for 3D Gaussian Splatting](https://arxiv.org/abs/2606.09034)
*Mizuki Morikawa,Yuta Shimizu,Chunyu Li,Yusuke Monno,Masatoshi Okutomi*

Main category: cs.CV

TL;DR: 本文提出利用NeRF渲染的图像来增强3DGS在街景场景中的渲染质量，同时保持3DGS的快速渲染速度。通过使用NeRF渲染图像去除瞬态物体并生成鸟瞰视图，结合扩散模型增强图像质量，实现了兼顾速度和质量的街景渲染。

<details>
  <summary>Details</summary>

Motivation: NeRF和3DGS在新视角合成任务中具有互补性能：3DGS渲染速度快但质量相对较低，NeRF渲染质量高但速度慢。本文旨在利用NeRF的高质量渲染来提升3DGS在街景场景中的渲染效果，同时保持3DGS的快速渲染速度。

Method: 1) 使用预训练的街景专用NeRF方法生成训练图像；2) 利用NeRF渲染图像去除输入视图中瞬态物体（如车辆、行人）；3) 使用NeRF生成鸟瞰视图作为3DGS训练的额外视角；4) 引入基于扩散模型的图像增强方法提升额外视图的图像质量。

Result: 在1个合成数据集和2个真实数据集上的实验结果表明，所提方法在保持3DGS渲染速度和NeRF渲染质量的同时，有效改善了街景场景的渲染效果。

Conclusion: 本文提出的方法成功地将NeRF的高质量渲染能力融入3DGS，在不牺牲3DGS快速渲染优势的前提下，提升了街景场景的新视角合成质量。

Original Abstract: Neural radiance field (NeRF) and 3D Gaussian splatting (3DGS) are two mainstream approaches for novel view synthesis. They often show complementary performance, i.e., 3DGS demonstrating faster rendering speed and NeRF demonstrating higher rendering quality. Motivated by this, we propose leveraging NeRF-rendered images for 3DGS. Specifically, we target street scenes and utilize a pre-trained street-specific NeRF method to produce training images for a target 3DGS method. In our 3DGS training, NeRF-rendered images are used to remove transient objects in street-level input views and to generate bird's-eye views as additional views, inheriting the higher-quality rendering of NeRF into 3DGS. We further incorporate a diffusion-based image enhancement to improve the image quality of the additional views. Experimental results on one synthetic and two real datasets demonstrate that our proposed method improves street-scene rendering while preserving the speed of 3DGS and the quality of NeRF.

中文摘要翻译: 神经辐射场（NeRF）和三维高斯泼溅（3DGS）是新颖视角合成的两种主流方法。它们通常展现出互补的性能，即3DGS具有更快的渲染速度，而NeRF具有更高的渲染质量。受此启发，我们提出利用NeRF渲染的图像来增强3DGS。具体而言，我们针对街景场景，使用预训练的街景专用NeRF方法为目标3DGS方法生成训练图像。在我们的3DGS训练中，NeRF渲染的图像被用于移除街景输入视图中的瞬态物体，并生成鸟瞰视图作为额外视角，从而将NeRF更高质量的渲染继承到3DGS中。我们进一步引入基于扩散模型的图像增强方法，以提升额外视图的图像质量。在1个合成数据集和2个真实数据集上的实验结果表明，我们提出的方法在改善街景场景渲染效果的同时，保持了3DGS的速度和NeRF的质量。

</details>


### [140] [MilliVid: Hierarchical Latents for Long-Range Consistency in Video Generation](https://arxiv.org/abs/2606.09056)
*Ishaan Preetam Chandratreya,David Charatan,Basile Van Hoorick,Sergey Zakharov,Vitor Guizilini,Phillip Isola,Vincent Sitzmann*

Main category: cs.CV

TL;DR: 本文提出一种多尺度层级化token空间的粗到细生成方法，通过预训练自编码器将视频帧压缩为多层级token，再训练视频扩散模型按粗到细顺序生成token，从而在降低计算成本的同时实现长程一致的视频生成。

<details>
  <summary>Details</summary>

Motivation: 视频生成模型虽日益强大，但长程一致性仍难以实现，因为即使生成几十帧也需要过长的Transformer序列长度，计算上不切实际。

Method: 首先预训练一个自编码器，将每帧压缩为层级化token（从典型潜变量分辨率到每帧仅几个token）；粗层级捕获场景布局和语义等重要信息，细层级补充高频外观和纹理。然后训练视频扩散模型按粗到细的顺序生成这些token，在每一步生成中精细控制帧的细节层次及上下文使用方式。

Result: 在长Minecraft视频数据集上验证，该方法相比现有基线生成了更加一致的视频展开结果，显著提升了长程几何一致性和物体恒存性。

Conclusion: 粗到细的层级化token生成策略能有效兼顾长程一致性与计算效率，在保持重要语义一致性的同时减少对感知上不重要的细节进行长程一致性的计算开销。

Original Abstract: Video generative models have become increasingly powerful, but long-range consistency remains challenging to achieve because even a few dozen frames require impractically long transformer sequence lengths. We show that this issue can be mitigated by generating video using coarse-to-fine rollout within a multi-scale token space. Our approach is simple: first, we pre-train an autoencoder that compresses each frame into a hierarchy of tokens, with levels ranging from the typical latent resolution to only a handful of tokens per frame. The coarsest levels capture the most consequential information, such as scene layout and semantics, while finer levels add high-frequency appearance and texture. Then, we train a video diffusion model to generate these tokens using coarse-to-fine rollout. By carefully controlling the level of detail at which frames are generated and used as context during each rollout step, we are able to preserve long-range consistency in geometry and object permanence while spending less compute on the long-range consistency of less perceptually relevant details. We validate this approach using a custom dataset of long Minecraft videos, where it produces substantially more consistent rollouts compared to existing baselines.

中文摘要翻译: 视频生成模型已变得越来越强大，但长程一致性仍然是一个难以实现的目标，因为即使仅生成几十帧也需要过长的Transformer序列长度，这在实际中不可行。我们表明，通过在多尺度token空间内进行粗到细的展开生成，可以缓解这一问题。我们的方法很简单：首先，预训练一个自编码器，将每一帧压缩为层级化的token，层级范围从典型的潜变量分辨率到每帧仅几个token。最粗的层级捕获最重要的信息，如场景布局和语义，而更细的层级则补充高频外观和纹理。然后，我们训练一个视频扩散模型，按照粗到细的顺序生成这些token。通过精细控制每一步展开生成时帧的细节层次以及用作上下文的帧，我们能够在几何一致性和物体恒存性方面保持长程一致性，同时将计算开销更多地用于保持感知上不重要的细节的长程一致性上。我们使用一个自定义的长Minecraft视频数据集验证了该方法，实验结果表明，与现有基线相比，该方法生成了更加一致的视频展开结果。

</details>


### [141] [See More, Think Deeper: Query-Expanded Visual Evidence and Answer-Clue Guided Reflection for Long Video Understanding](https://arxiv.org/abs/2606.09064)
*Shuning Wang,Zhiheng Wu,YiNuo Lu,Naiming Liu,Chen Jia,Bowen Liu,Shuo Nie,Weijie Zhu,Yumeng Zhang*

Main category: cs.CV

TL;DR: CoVER是一个面向长视频理解的综合视觉证据与反思框架，通过动态收集查询扩展的视觉证据和基于答案的视觉反馈验证机制，分别实现“看得更多”和“思考更深”，将长视频理解从以答案为中心转变为以证据为中心和视觉可验证的推理过程。CoVER-7B在多个指标上显著超越同规模模型，甚至超过部分闭源模型。

<details>
  <summary>Details</summary>

Motivation: 现有Video-LLM在长视频理解中存在两个关键局限：证据获取通常依赖单一的搜索意图，且答案生成缺乏有效的视觉反馈机制。因此，需要一种能够动态获取更全面视觉证据并进行可验证推理的框架。

Method: 提出CoVER框架，包含两个核心机制：(1) See More：通过查询扩展动态收集更全面的视觉证据；(2) Think Deeper：通过针对答案的视觉反馈验证草稿答案，实现可验证的视觉推理。

Result: CoVER-7B显著优于同参数规模的模型，并在某些指标上超越了最先进的闭源模型。

Conclusion: 通过将长视频理解从以答案为中心转变为以证据为中心并引入视觉可验证的推理机制，CoVER有效提升了Video-LLM的长视频理解能力。

Original Abstract: Recent advances in Video Large Language Models (Video-LLMs) have enabled performance on long-video understanding tasks. However, existing methods still face two key limitations: evidence acquisition often relies on a single search intent, and answer generation lacks an effective visual feedback mechanism. To address these limitations, we propose \textbf{CoVER}, a Comprehensive Visual Evidence and Reflection framework for long-video understanding. CoVER enables Video-LLMs to \textbf{See More} by dynamically gathering query-expanded visual evidence, and \textbf{Think Deeper} by verifying draft answers with effective answer-specific visual feedback. Together, these mechanisms shift long-video understanding from answer-centric generation to evidence-centric and visually verifiable reasoning. Experimental results show that CoVER-7B substantially outperforms models with the same parameter scale and even surpasses state-of-the-art closed-source models on certain metrics.

中文摘要翻译: 近年来，视频大语言模型（Video-LLMs）的进展使其在长视频理解任务上取得了良好性能。然而，现有方法仍面临两个关键局限：证据获取通常依赖单一的搜索意图，且答案生成缺乏有效的视觉反馈机制。为解决这些问题，我们提出了\textbf{CoVER}——一个面向长视频理解的综合视觉证据与反思框架。CoVER通过动态收集查询扩展的视觉证据使Video-LLM能够\textbf{看得更多}，并通过基于答案的视觉反馈验证草稿答案使其\textbf{思考更深}。这两种机制共同将长视频理解从以答案为中心的生成转变为以证据为中心且可视觉验证的推理过程。实验结果表明，CoVER-7B显著优于同参数规模的模型，并在某些指标上甚至超越了最先进的闭源模型。

</details>


### [142] [REFINE: Super-efficient 3D Gaussian Splatting Pruning via Rendering-Free Primitive Importance](https://arxiv.org/abs/2606.09074)
*Zhang Chen,Shuai Wan,Mengting Yu,Fuzheng Yang,Junhui Hou*

Main category: cs.CV

TL;DR: 提出REFINE，一种基于渲染无关的原始重要性度量的3D高斯泼溅快速剪枝框架，通过解析近似的、感知渲染的Hessian场量化每个原始体移除带来的预期感知误差，在保持高质量渲染的同时实现比SOTA方法3000倍的计算复杂度降低。

<details>
  <summary>Details</summary>

Motivation: 现有的3D高斯泼溅(3DGS)剪枝方法存在严重的质量退化或过高的计算开销问题，亟需一种既能保持渲染质量又能大幅降低计算复杂度的剪枝框架。

Method: 提出一种无需渲染的原始重要性度量方法。通过解析近似的、感知渲染的Hessian场来量化移除单个原始体带来的预期感知误差。该方法联合建模了可见性、投影几何以及内容自适应超参数的调制，完全绕过了耗时的前向渲染过程，推导出一个各向异性的感知权重场作为原始体重要性的高保真代理。

Result: 在多个基准数据集上的广泛实验表明，REFINE在保持高度竞争力的渲染质量的同时，与最先进的剪枝方法相比，在剪枝相关计算复杂度上实现了前所未有的3000倍降低。

Conclusion: REFINE通过渲染无关的Hessian场近似方法，在剪枝质量和计算效率之间取得了突破性平衡，为3DGS的高效剪枝提供了新范式。

Original Abstract: Existing pruning methods for 3D Gaussian splatting (3DGS) suffer from either severe quality degradation or prohibitive computational overhead. In this paper, we propose REFINE, a highly accelerated 3DGS pruning framework centered on a novel rendering-free primitive importance metric. Our approach leverages an analytically approximated, rendering-aware Hessian field to quantify the expected perceptual error induced by the removal of individual primitives. By modeling the joint modulation of visibility, projection geometry and the content adaptive hyperparameter, we entirely bypass costly forward rendering passes and derive an anisotropic perceptual weight field that serves as a high-fidelity proxy for primitive importance. Extensive experiments across multiple benchmark datasets demonstrate that REFINE maintains highly competitive rendering quality while achieving an unprecedented $3,000\times$ reduction in pruning-related computational complexity compared to state-of-the-art pruning methods.

中文摘要翻译: 现有的3D高斯泼溅（3DGS）剪枝方法要么面临严重的质量退化，要么计算开销过大。本文提出REFINE，一个高度加速的3DGS剪枝框架，其核心是一种新颖的、无需渲染的原始体重要性度量。我们的方法利用解析近似的、感知渲染的Hessian场来量化移除单个原始体所导致的预期感知误差。通过联合建模可见性、投影几何以及内容自适应超参数的调制，我们完全绕过了昂贵的前向渲染过程，推导出一个各向异性的感知权重场，作为原始体重要性的高保真代理。在多个基准数据集上的大量实验表明，REFINE在保持高度竞争力的渲染质量的同时，与最先进的剪枝方法相比，在剪枝相关计算复杂度上实现了前所未有的3000倍降低。

</details>


### [143] [Beyond Scalar Rewards by Internalizing Reasoning into Score Distributions](https://arxiv.org/abs/2606.09076)
*Xin Jin,Huanqia Cai,Zhen Li,Zechao Zhan,Dengyang Jiang,Aiming Hao,Yuming Jiang,Chunle Guo,Peng Gao,Ming-Ming Cheng,Steven C. H. Hoi*

Main category: cs.CV

TL;DR: 提出 Z-Reward，一个解耦推理与高效部署的教师-学生奖励模型框架，教师用大VLM推理生成评分分布，学生通过蒸馏获得紧凑模型，在文本到图像后训练中显著提升人类偏好对齐效果。

<details>
  <summary>Details</summary>

Motivation: 现有奖励模型将视觉偏好压缩为确定性标量或成对分数，过度压缩了不确定性；推理型奖励虽判断更强但部署成本高，难以直接用于优化信号。需要一种既能保留推理能力又能高效部署的奖励建模方法。

Method: 提出 Z-Reward 框架：(1) 教师模型：大VLM，使用 Group-wise Direct Score Optimization (GDSO) 训练，结合策略梯度奖励与点对点/成对监督，推理生成符合评分标准的分数分布；(2) 学生模型：通过 Reasoning-Internalized Score Distillation (RISD) 蒸馏教师的推理条件分数分布，推理时无需显式推理链。

Result: 27B GDSO 教师模型达到 89.6% 人类偏好准确率，优于 SFT、RewardDance、GRPO；9B RISD 学生模型达到 88.6%，优于 OPD 基线并接近大教师模型。作为可微奖励信号用于文生图优化，相比 SFT 基线提升 41.3% 净人类偏好。

Conclusion: Z-Reward 通过教师-学生框架成功解耦了推理型奖励的判断能力与高效部署需求，在文本到图像后训练中实现了更优的人类偏好对齐，且蒸馏后的紧凑模型接近大模型的性能。

Original Abstract: Reward models are central to text-to-image post-training, but visual preference is subjective and better represented as a distribution over rubric scores than as a deterministic scalar. Existing scalar, score-token, and pairwise reward models over-compress uncertainty and fine-grained score differences, while reasoning-based generative rewards provide stronger judgments but are costly to deploy and difficult to use as direct optimization signals. We propose Z-Reward, a teacher-student reward modeling framework that decouples reasoning-heavy judgment from efficient reward deployment. The teacher is a large VLM that uses reasoning to infer rubric-aligned score distributions, and is trained with Group-wise Direct Score Optimization (GDSO), which combines policy-gradient rewards from distribution expectations with direct pointwise and pairwise supervision on score distributions and score gaps. The student is trained with Reasoning-Internalized Score Distillation (RISD), which transfers the teacher's reasoning-conditioned score distribution into a compact VLM without requiring explicit reasoning chains at inference time. On our internally annotated evaluation set, the 27B GDSO teacher reaches 89.6% human preference accuracy, outperforming SFT, RewardDance, and GRPO, while the 9B RISD student reaches 88.6%, outperforming the OPD baseline and closely matching the larger teacher. We further show that Z-Reward can serve as a differentiable reward signal for text-to-image optimization, yielding a 41.3% net human-preference improvement over the SFT baseline.

中文摘要翻译: 奖励模型是文本到图像后训练的核心，但视觉偏好是主观的，更适合表示为评分标准的分布而非确定性标量。现有的标量、分数token和成对奖励模型过度压缩了不确定性和细粒度分数差异，而基于推理的生成式奖励虽能提供更强的判断，但部署成本高且难以用作直接优化信号。我们提出 Z-Reward，一个将推理密集型判断与高效奖励部署解耦的教师-学生奖励建模框架。教师模型是一个大型VLM，通过推理推断符合评分标准的分数分布，并使用组级直接分数优化（GDSO）进行训练，该方法将来自分布期望的策略梯度奖励与分数分布和分数差距上的直接逐点及成对监督相结合。学生模型通过推理内化分数蒸馏（RISD）进行训练，将教师基于推理的分数分布迁移到一个紧凑的VLM中，无需在推理时提供显式推理链。在我们内部标注的评估集上，27B的GDSO教师模型达到89.6%的人类偏好准确率，优于SFT、RewardDance和GRPO；而9B的RISD学生模型达到88.6%，优于OPD基线并接近更大的教师模型。我们进一步证明，Z-Reward可以作为文本到图像优化的可微奖励信号，相比SFT基线带来41.3%的净人类偏好提升。

</details>


### [144] [Edge-Constrained UAV Small-Object Detection with P2 Enhancement and Quantum-Inspired Lightweight Structure Search](https://arxiv.org/abs/2606.09081)
*Wuming Lei,Yanbin Gao,Mingyan Sun,Xiaobin Li,Xuechen Liang*

Main category: cs.CV

TL;DR: 本文提出通过P2高分辨率检测分支结合量子启发进化算法(QIEA)来优化YOLOX-Nano在无人机边缘部署场景下的小目标检测性能，实验表明P2分支能显著提升小目标AP，QIEA可用于轻量级候选筛选和精度-成本分析。

<details>
  <summary>Details</summary>

Motivation: 轻量级网络中重复下采样会削弱浅层空间信息，导致小目标检测困难；而手动添加注意力或融合模块可能增加计算成本且收益不稳定。因此需要一种自动化的轻量级结构搜索方法，同时兼顾精度、计算量、延迟、内存和召回率等多目标约束。

Method: 结合P2高分辨率检测分支与量子启发进化算法(QIEA)进行轻量级结构筛选。搜索空间由轻量级优先级和任务特异性定义，评估指标综合考量精度、FLOPs、延迟、内存消耗和召回率。

Result: 在VisDrone数据集上，P2分支使AP_small比YOLOX-Nano基线提升31.10%；与NanoDet-Plus相比，YOLOX-Nano+-P2的AP_s0.5提升17.5%，AP_small提升44.9%。QIEA筛选的候选获得最高Recall_s0.5，但+P2经完整训练后仍是AP最优变体。100轮完整验证显示代理排名不一定迁移到最终AP_s0.5。

Conclusion: P2分支可作为小目标增强的主要路径，QIEA可作为轻量级候选筛选和精度-成本分析的有效工具。

Original Abstract: Unmanned aerial vehicle (UAV) object detection requires compact detectors that retain small-object details under onboard computation and memory constraints. Repeated downsampling inlightweight networks weakens shallow spatial information, while manually adding attention orfusion modules may increase cost without stable gains. This study analyzes YOLOX-Nano underedge-deployment constraints by combining a P2 high-resolution detection branch with a quantum-inspired evolutionary algorithm (QIEA) for lightweight structure screening. The search space isdefined by lightweight priority and task specificity, and the evaluation jointly considers accuracy,floating-point operations (FLOPs), latency, memory consumption, and recall. On VisDrone, theP2 branch increases APamall by 31.10% over the YOLOX-Nano baseline. Compared with NanoDet-Plus with similar model size, YOLOX-Nano+-P2 improves APs0.ss by 17.5% and APamal by 44.9%.The QIEA-selected candidate obtains the highest Recallso, but +P2 remains the strongest AP-oriented variant after full training. Full 100-epoch verification of Random-best, GA-best, andSA/QUBO-best candidates further shows that proxy rankings do not necessarily transfer to finalAPse9s. These results support using P2 as the main small-object enhancement path and QIEA as alightweight tool for candidate screening and accuracy-cost analysis. The source code, configurationfiles, diagnostic scripts, and summarized results are available at https://github.com/Ming23233/UAV-QIEA-Edge-Detection

中文摘要翻译: 无人机目标检测需要轻量级检测器在机载计算和内存约束下保留小目标细节。轻量级网络中重复的下采样会削弱浅层空间信息，而手动添加注意力或融合模块可能增加成本且收益不稳定。本研究在边缘部署约束下分析YOLOX-Nano，将P2高分辨率检测分支与量子启发进化算法(QIEA)相结合进行轻量级结构筛选。搜索空间由轻量级优先级和任务特异性定义，评估指标综合考量精度、浮点运算数(FLOPs)、延迟、内存消耗和召回率。在VisDrone数据集上，P2分支使AP_small较YOLOX-Nano基线提升31.10%。与模型大小相近的NanoDet-Plus相比，YOLOX-Nano+-P2的AP_s0.5提升17.5%，AP_small提升44.9%。QIEA筛选的候选获得最高Recall_s0.5，但+P2经完整训练后仍是AP最优变体。对Random-best、GA-best和SA/QUBO-best候选的100轮完整验证进一步表明，代理排名不一定迁移到最终AP_s0.5。这些结果支持将P2作为小目标增强的主要路径，并将QIEA作为轻量级候选筛选和精度-成本分析的工具。源代码、配置文件、诊断脚本和汇总结果可在https://github.com/Ming23233/UAV-QIEA-Edge-Detection获取。

</details>


### [145] [Driving Video Retrieval for Complex Queries with Structured Grounding](https://arxiv.org/abs/2606.09109)
*Manyi Yao,Sparsh Garg,Christian Shelton,Amit Roy-Chowdhury,Abhishek Aich*

Main category: cs.CV

TL;DR: STRIVE-D提出了一种数据校准的检索框架，通过弱标注域内视频估计查询规则的可靠性，并融合视觉-语言和基于关键词的检索信号，在自动驾驶视频检索中实现了高达84%的top-1准确率提升。

<details>
  <summary>Details</summary>

Motivation: 现有的视觉-语言和基于关键词的检索方法难以捕捉自动驾驶视频中的动态事件（如切入、急刹车），因为这些运动信息未被文本显式描述或通过词汇重叠捕捉；而基于规则的检索虽能直接编码这些事件，但规则脆弱，当假设与真实驾驶数据不匹配时容易失败。

Method: STRIVE-D包含三个核心步骤：1) 使用弱标注的域内视频估计查询规则的可靠性；2) 自适应调整与观测数据不匹配的规则；3) 将校准后的规则分数与视觉-语言和基于关键词的检索信号进行融合。

Result: 在三个驾驶基准数据集上，包括新发布的人类标注事件数据DrivingDojo，STRIVE-D相比现有最优方法在top-1准确率上实现了高达84%的相对提升。

Conclusion: 数据校准的规则检索方法能够有效弥补纯视觉-语言和关键词检索在捕捉动态事件方面的不足，显著提升自动驾驶视频检索的性能。

Original Abstract: Video retrieval at scale is central to data curation and safety validation in autonomous driving, where users want to find not only scenes but also dynamic events such as cut-ins and hard braking. Existing vision-language and keyword-based retrieval methods often miss these events because the relevant motion may not be explicitly described in text or captured by lexical overlap. Rule-based retrieval can encode such events more directly, but it is brittle: generated or hand-written rules often fail when their assumptions do not match real driving data. We propose STRIVE-D, a data-calibrated retrieval framework for driving videos. It uses weakly labeled in-domain videos to estimate when a query rule is reliable, adapt rules that mismatch observed data, and fuse calibrated rule scores with vision-language and keyword-based retrieval signals. Across three driving benchmarks, including newly released human-annotated event data on DrivingDojo, STRIVE-D delivers up to 84% relative improvement in top-1 accuracy over state-of-the-art methods.

中文摘要翻译: 大规模视频检索对于自动驾驶中的数据整理和安全验证至关重要，用户不仅希望找到场景，还想找到诸如切入和急刹车等动态事件。现有的视觉-语言和基于关键词的检索方法常常遗漏这些事件，因为相关的运动信息可能未被文本显式描述或通过词汇重叠捕捉。基于规则的检索虽然能更直接地编码此类事件，但其鲁棒性不足：生成或手工编写的规则在假设条件与真实驾驶数据不匹配时常常失效。我们提出了STRIVE-D，一种面向驾驶视频的数据校准检索框架。它利用弱标注的域内视频来估计查询规则的可靠性，自适应调整与观测数据不匹配的规则，并将校准后的规则分数与视觉-语言和基于关键词的检索信号进行融合。在三个驾驶基准数据集上，包括新发布的DrivingDojo人工标注事件数据，STRIVE-D相比现有最优方法在top-1准确率上实现了高达84%的相对提升。

</details>


### [146] [HDRAgent: An Agentic Framework for Multi-Exposure HDR Imaging](https://arxiv.org/abs/2606.09110)
*Weiyu Zhou,Tao Hu,Yijian Wang,Xiaogang Xu,Ruixing Wang,Qingsen Yan*

Main category: cs.CV

TL;DR: 提出HDRAgent，首个基于智能体的HDR成像框架，通过多模态大语言模型驱动的场景感知、上下文知识匹配和自适应策略选择，解决多曝光HDR在复杂动态场景中的鬼影伪影问题。

<details>
  <summary>Details</summary>

Motivation: 现有大多数多曝光HDR方法遵循固定的前馈重建范式，在复杂动态场景中容易产生鬼影伪影，缺乏根据当前场景条件自适应选择重建策略的能力。

Method: 1) 提出细粒度上下文知识匹配(FCM)模块，利用MLLM的场景感知能力检索历史案例和工具知识，构建结构化证据用于自适应工具调度；2) 提出感知-失真反馈机制，将执行后的质量评估和伪影诊断转化为结构化反馈，累积到历史记忆中辅助后续策略选择；3) 设计智能体引导的生成式对齐策略，利用MLLM动态区域解析在参考帧引导下重建非参考帧中的不可靠内容。

Result: 实验表明，HDRAgent有效减少鬼影和局部伪影，同时达到具有竞争力或更优的客观性能和视觉质量。

Conclusion: 基于智能体的自适应框架能有效克服传统固定范式在复杂动态场景中的局限性，通过场景感知和策略选择显著提升HDR成像质量。

Original Abstract: Most existing multi-exposure HDR methods follow a fixed feed-forward reconstruction paradigm, making them prone to ghosting artifacts in complex dynamic scenes. To address this issue, we propose HDRAgent, the first agent-driven framework for HDR imaging, which adaptively selects reconstruction strategies according to the current scene conditions. Specifically, to provide scene-specific prior knowledge, we introduce a fine-grained contextual knowledge matching (FCM) module. This module leverages multimodal large language model (MLLM)-derived scene perception to retrieve relevant historical cases and tool knowledge, organizing them into structured evidence for MLLM-based adaptive tool scheduling. In addition, we propose a perception--distortion feedback mechanism that transforms post-execution quality assessment and artifact diagnosis into structured feedback, which is accumulated in historical memory to help subsequent contextual knowledge refinement and strategy selection. Furthermore, considering that extreme motion can invalidate alignment methods, we design an agent-guided generative alignment strategy that uses MLLM-based dynamic-region parsing to reconstruct unreliable contents in non-reference frames under reference-frame guidance. Experiments demonstrate that HDRAgent effectively reduces ghosting and local artifacts while achieving competitive or superior objective performance and visual quality.

中文摘要翻译: 大多数现有的多曝光高动态范围（HDR）方法遵循固定的前馈重建范式，使其在复杂动态场景中容易产生鬼影伪影。为解决这一问题，我们提出HDRAgent——首个面向HDR成像的智能体驱动框架，该框架能够根据当前场景条件自适应地选择重建策略。具体而言，为提供场景特定的先验知识，我们引入了一个细粒度上下文知识匹配（FCM）模块。该模块利用多模态大语言模型（MLLM）衍生的场景感知能力，检索相关的历史案例和工具知识，并将其组织为结构化证据，用于基于MLLM的自适应工具调度。此外，我们提出了一种感知-失真反馈机制，将执行后的质量评估和伪影诊断转化为结构化反馈，并累积到历史记忆中，以帮助后续的上下文知识优化和策略选择。进一步地，考虑到极端运动可能导致对齐方法失效，我们设计了一种智能体引导的生成式对齐策略，该策略利用基于MLLM的动态区域解析，在参考帧引导下重建非参考帧中的不可靠内容。实验表明，HDRAgent有效减少了鬼影和局部伪影，同时实现了具有竞争力或更优的客观性能与视觉质量。

</details>


### [147] [Illumination-Invariant Anomaly Detection for Sub-Canopy UAV Multispectral Point Clouds](https://arxiv.org/abs/2606.09111)
*Likun Chen,Yanfeng Gu,Xian Li*

Main category: cs.CV

TL;DR: 提出一种无先验的异常检测框架，通过逆优化太阳角估计和无光照稀疏表示，解决无人机多光谱点云在植被阴影下的光照不均问题，实现林下目标检测。

<details>
  <summary>Details</summary>

Motivation: 无人机多光谱点云在植被阴影下存在严重的光照异质性，导致目标检测性能显著下降，现有方法依赖飞行元数据且难以区分暗目标与真实阴影。

Method: 1) 将太阳角估计建模为逆优化问题，耦合光谱指数与射线追踪模型实现无先验阴影提取；2) 提出光照一致性稀疏表示机制，从相同光照状态的邻域构建背景字典，解耦光谱反射率与光照变化。

Result: 该方法在复杂森林环境中显著提高了异常与背景的可分离性，性能优于现有最先进基线。

Conclusion: 该框架特别适用于识别伪装军事目标、测绘倒木树干以及发现密集植被下隐藏的考古遗迹。

Original Abstract: Unmanned Aerial Vehicle (UAV) multispectral point clouds (MPC) provide high-dimensional spatial-spectral data for sub-canopy target detection; however, their efficacy is significantly compromised by severe illumination heterogeneity caused by vegetation shadows. To address this, we propose a prior-free anomaly detection framework capable of robustly handling lighting variations. First, we formulate solar angle estimation as an inverse optimization problem. By coupling spectral indices with a ray-tracing model, this strategy achieves Prior-Free Shadow Extraction without relying on flight metadata, effectively distinguishing dark objects from true shadows. Second, to mitigate spectral distortions, we introduce an Illumination-Consistent Sparse Representation mechanism. Unlike standard reconstruction methods, we construct a background dictionary strictly from neighbors sharing the same illumination state. This constraint effectively disentangles spectral reflectance from lighting variations, ensuring that targets are represented solely by physically consistent background points. Experimental results indicate that the proposed method significantly improves the separability between anomalies and background in complex forest environments, demonstrating superior performance over state-of-the-art baselines. This framework is particularly suited for identifying camouflaged military targets, mapping fallen tree trunks, and uncovering archaeological ruins hidden beneath dense foliage.

中文摘要翻译: 无人机多光谱点云为林下目标检测提供了高维空间-光谱数据，但其有效性因植被阴影导致的严重光照异质性而显著降低。为解决这一问题，我们提出了一种无需先验的异常检测框架，能够稳健地处理光照变化。首先，我们将太阳角估计建模为一个逆优化问题。通过将光谱指数与射线追踪模型耦合，该策略实现了无需飞行元数据的无先验阴影提取，有效区分了暗目标与真实阴影。其次，为减轻光谱畸变，我们引入了一种光照一致性稀疏表示机制。与标准重构方法不同，我们严格从具有相同光照状态的邻域构建背景字典。这一约束有效解耦了光谱反射率与光照变化，确保目标仅由物理一致性的背景点表示。实验结果表明，所提方法显著提高了复杂森林环境中异常与背景的可分离性，展现出优于现有最先进基线的性能。该框架特别适用于识别伪装军事目标、测绘倒木树干以及发现密集植被下隐藏的考古遗迹。

</details>


### [148] [An Enhanced Geometric-Spectral Feature Learning Framework for Airborne Multispectral Point Cloud Classification](https://arxiv.org/abs/2606.09123)
*Xian Li,Yanfeng Gu,Aleksandra Pižurica*

Main category: cs.CV

TL;DR: 本文针对机载多光谱点云（MPC）分类中高维异构特征、样本分布不均和类间光谱相似等挑战，提出了一种基于注意力机制的增强型几何-光谱特征学习框架，包含双流特征融合、残差注意力融合模块和联合损失函数，在两个数据集上取得了优于现有方法的分类性能。

<details>
  <summary>Details</summary>

Motivation: 现有MPC分类模型受限于高维异构的空间-光谱信息、样本分布不均衡以及机载MPC的类间光谱相似性，导致模型表征能力不足，亟需一种能有效融合几何和光谱特征并处理不平衡样本的深度学习框架。

Method: 提出基于注意力的增强型几何-光谱特征学习框架：(1) 双流特征融合，第一流通过融合自注意力提取位置编码的全局光谱特征，第二流通过多核点卷积和特征聚合注意力提取光谱引导的几何特征；(2) 残差注意力融合块整合两流中最具信息量的几何-光谱特征；(3) 联合损失函数提升对不平衡和类间相似样本的学习能力。

Result: 在两个机载MPC数据集上的实验结果表明，所提方法在分类性能上优于当前最先进的方法。

Conclusion: 提出的基于注意力机制的双流特征融合框架和联合损失函数能有效解决MPC分类中的高维异构特征、样本不平衡和类间相似性问题，显著提升分类精度。

Original Abstract: Multispectral point cloud (MPC) is composed of 3D spatial-spectral information, which holds tremendous potential for accurate land-cover classification. However, the representation power of classification models is limited by inherent high-dimensional and heterogeneous spatial-spectral information, unbalanced sample distribution, and inter-class spectral similarity of airborne MPCs. We build two MPC datasets and propose an enhanced geometric-spectral feature learning framework based on attentions for airborne MPC classification. A key component in our model is a two-stream feature fusion method with attention mechanisms, which enhances the representation capability of spatial-spectral features from high-dimensional heterogeneous MPCs. The first stream aims to extract position-encoded global spectral features with fusion self-attention, and the second stream comprises a multikernel point convolution and feature aggregation attention to extract spectral-guided geometric features. We then develop a residual attention fusion block to integrate the most informative geometric-spectral features from the two parallel streams. Another important contribution of this work is a joint loss function to improve the learning ability on unbalanced and interclass similar samples. Experimental results on two airborne MPC datasets demonstrate the effectiveness of the proposed method compared with the state-of-the-art methods. Furthermore, the codes and datasets used in this paper will be made available freely at https://github.com/HITlixian/TGRS_GSFF.

中文摘要翻译: 多光谱点云由三维空间-光谱信息构成，在精确地物覆盖分类方面具有巨大潜力。然而，由于机载多光谱点云固有的高维异构空间-光谱信息、不均衡的样本分布以及类间光谱相似性，分类模型的表征能力受到限制。我们构建了两个多光谱点云数据集，并提出了一种基于注意力的增强型几何-光谱特征学习框架用于机载多光谱点云分类。该模型的一个关键组件是带有注意力机制的双流特征融合方法，该方法增强了从高维异构多光谱点云中提取的空间-光谱特征的表示能力。第一流旨在通过融合自注意力提取位置编码的全局光谱特征，第二流由多核点卷积和特征聚合注意力组成，用于提取光谱引导的几何特征。随后，我们开发了一个残差注意力融合模块，用于整合来自两个并行流的最具信息量的几何-光谱特征。本工作的另一个重要贡献是提出了一个联合损失函数，以提升在不平衡和类间相似样本上的学习能力。在两个机载多光谱点云数据集上的实验结果表明，与最先进的方法相比，所提方法具有有效性。此外，本文使用的代码和数据集将在 https://github.com/HITlixian/TGRS_GSFF 上免费提供。

</details>


### [149] [A Geometric Framework for Absolute Pose and Velocity Estimation with Event Cameras](https://arxiv.org/abs/2606.09139)
*Zibin Liu,Shunkun Liang,Banglei Guan,Yang Shang,Qifeng Yu,Ji Zhao*

Main category: cs.CV

TL;DR: 本文提出一种基于事件相机和3D线特征的几何框架，用于同时估计绝对位姿和速度，仅需最少三个事件-线对应关系，并通过线面正交性和点线共线性两个几何约束实现线性或多项式求解。

<details>
  <summary>Details</summary>

Motivation: 现有基于事件的运动估计方法主要关注速度估计，而绝对位姿估计（对机器人导航和增强现实等应用至关重要）研究不足，同时从事件流中恢复绝对位姿和速度仍是一个开放且具有挑战性的问题。

Method: 利用场景中的3D直线及其触发的事件，构建两个核心几何约束：3D直线与其对应事件平面法向量之间的正交性，以及事件与其关联直线的2D投影之间的共线性。基于这些约束，提出用于绝对位姿估计的线性求解器和多项式求解器（后者可全局最优求解旋转），以及用于速度估计的线性求解器和基于优化的精确求解器。

Result: 在仿真和真实数据集上的实验表明，该方法达到了最先进水平，在精度和计算效率上相比现有方法有显著提升，且最少仅需三个事件-线对应关系即可独立确定6自由度绝对位姿或速度。

Conclusion: 提出的几何框架有效解决了事件流中绝对位姿与速度的联合估计问题，算法在精度和效率上均优于现有方法，且代码已开源。

Original Abstract: Despite the rapid advancements in event-based motion estimation, current geometric methods primarily focus on velocity estimation. However, absolute pose estimation, which is equally crucial for key applications such as robotic navigation and augmented reality, remains relatively underexplored. Consequently, the simultaneous recovery of absolute pose and velocity from event streams remains an open and challenging problem. To address this gap, we propose a geometric framework for absolute pose and velocity estimation by leveraging 3D lines in the scene and the events they trigger. At the core of the framework lie two key geometric constraints: the orthogonality between a 3D line and the normal vector of its corresponding event plane, and the collinearity of an event with the 2D projection of its associated line. Based on these constraints, we present both linear and polynomial solvers for absolute pose estimation. The former enables efficient computation, while the latter provides a globally optimal solution for rotation. For velocity estimation, we develop an efficient linear solver and a more accurate optimization-based solver to recover both angular and linear velocities. Notably, our methods require a minimum of three event-line correspondences to determine the 6-DoF absolute pose or velocities independently. Extensive experiments in simulation and on real-world datasets demonstrate that our methods achieve state-of-the-art performance, with significant improvements in accuracy and computational efficiency compared to existing methods. The demo code is publicly available at https://github.com/Zibin6/EventPoseVelocity.

中文摘要翻译: 尽管基于事件的运动估计取得了快速进展，当前的几何方法主要集中在速度估计上。然而，绝对位姿估计——对于机器人导航和增强现实等关键应用同样至关重要——仍然相对未被充分探索。因此，从事件流中同时恢复绝对位姿和速度仍然是一个开放且具有挑战性的问题。为填补这一空白，我们提出了一种几何框架，通过利用场景中的3D直线及其触发的事件来实现绝对位姿和速度的估计。该框架的核心是两个关键的几何约束：3D直线与其对应事件平面的法向量之间的正交性，以及事件与其关联直线的2D投影之间的共线性。基于这些约束，我们提出了用于绝对位姿估计的线性求解器和多项式求解器。前者能够实现高效计算，而后者为旋转提供了全局最优解。对于速度估计，我们开发了一个高效的线性求解器和一个基于优化的更精确求解器，以恢复角速度和线速度。值得注意的是，我们的方法最少仅需三个事件-线对应关系即可独立确定6自由度绝对位姿或速度。在仿真和真实数据集上的大量实验表明，我们的方法达到了最先进的性能，与现有方法相比在精度和计算效率上均有显著提升。演示代码已在https://github.com/Zibin6/EventPoseVelocity 公开。

</details>


### [150] [DiffSight-Former: Modeling Structural Differences and Temporal Dynamics for Glaucoma Progression Prediction](https://arxiv.org/abs/2606.09140)
*Yi Huang,Lei Bi,Jinman Kim*

Main category: cs.CV

TL;DR: 提出DiffSight-Former框架，利用时序眼底图像和时变特征提取、多结构差异建模及时间感知Transformer进行青光眼进展预测，在两个纵向数据集上取得优异性能。

<details>
  <summary>Details</summary>

Motivation: 现有深度学习模型多依赖单时间点眼底图像，无法捕获疾病进展中纵向的结构与血管变化；而现有序列模型难以检测早期进展信号，常依赖固定长度输入或已青光眼图像的诊断线索，限制了早期预测的临床效用。

Method: DiffSight-Former框架包含三个核心模块：(1)基于眼底基础模型的时变特征提取模块，获取鲁棒的解剖表示；(2)多结构差异建模模块，量化视盘/视杯区域及视网膜血管的进展相关变化；(3)时间感知Transformer，整合上述表示与时序间隔嵌入，建模疾病进展并预测未来青光眼发病概率。

Result: 在SIGF数据集上AUC达91.54%，敏感性92.16%；在GRAPE数据集上三种临床视野进展标准下平均准确率87.48%。在不同时序设置下表现稳健，优于现有方法。

Conclusion: DiffSight-Former通过建模时序眼底图像的纵向变化，在青光眼进展预测和早期风险评估中展现出强大性能和鲁棒性，具有重要的临床应用潜力。

Original Abstract: Glaucoma is a leading cause of irreversible blindness worldwide, and early detection from fundus images is critical for effective disease management. While deep learning has achieved promising performance in fundus image analysis, most existing methods rely on single time-point images and fail to capture longitudinal structural and vascular changes associated with disease progression. Sequential fundus images acquired during clinical follow-up provide valuable temporal information; however, current sequential models often struggle to detect subtle early progression signals and commonly depend on fixed-length inputs or diagnostic cues from already glaucomatous images, limiting their clinical utility for early prediction. To address these limitations, we propose DiffSight-Former, a framework for glaucoma progression prediction from sequential fundus images. It incorporates a time-variant feature extraction module based on a fundus-specific foundation model to obtain robust anatomical representations. A multi-structure difference modeling module is introduced to quantify progression-related changes in the optic disc/cup region and retinal vasculature. These representations are integrated with temporal interval embeddings and processed by a time-aware Transformer to model disease progression and estimate the probability of future glaucoma onset. Experiments were conducted on two longitudinal datasets, SIGF (405 sequences) and GRAPE (263 sequences). On SIGF, DiffSight-Former achieved an AUC of 91.54% and a sensitivity of 92.16% for progression prediction. On GRAPE, it achieved an average accuracy of 87.48% across three clinical visual-field progression criteria. Compared with existing approaches, DiffSight-Former demonstrates strong performance and robustness across different temporal settings, highlighting its potential for longitudinal glaucoma monitoring and early risk prediction.

中文摘要翻译: 青光眼是全球范围内导致不可逆失明的主要原因，而通过眼底图像进行早期检测对于有效疾病管理至关重要。虽然深度学习在眼底图像分析中取得了令人瞩目的性能，但现有方法大多依赖单时间点图像，未能捕获与疾病进展相关的纵向结构和血管变化。临床随访过程中获取的时序眼底图像提供了宝贵的时间信息；然而，当前的序列模型往往难以检测微弱的早期进展信号，且通常依赖于固定长度输入或已出现青光眼图像的诊断线索，限制了其在早期预测中的临床实用性。为解决这些局限性，我们提出了DiffSight-Former——一个基于时序眼底图像进行青光眼进展预测的框架。该框架包含一个基于眼底专用基础模型的时变特征提取模块，以获取鲁棒的解剖表示；引入多结构差异建模模块，量化视盘/视杯区域及视网膜血管中与进展相关的变化；这些表示与时间间隔嵌入相整合，并由时间感知Transformer处理，以建模疾病进展并估计未来青光眼发病的概率。我们在两个纵向数据集SIGF（405个序列）和GRAPE（263个序列）上进行了实验。在SIGF上，DiffSight-Former的进展预测AUC达到91.54%，敏感性为92.16%。在GRAPE上，它在三种临床视野进展标准下实现了平均87.48%的准确率。与现有方法相比，DiffSight-Former在不同时序设置下均展现出优异的性能和鲁棒性，突显了其在纵向青光眼监测和早期风险预测方面的潜力。

</details>


### [151] [Decoding Pedestrian Crossing Intention from Egocentric Vision via Vision Language Models](https://arxiv.org/abs/2606.09142)
*Danya Li,Xiang Su,Yan Feng,Rico Krueger*

Main category: cs.CV

TL;DR: 本文研究从短时自我中心视频中解码行人过街意图，将任务建模为闭集视觉问答（VQA）问题，使用视觉语言模型（VLM）进行预测。零样本设置下VLM表现有限，通过参数高效微调显著提升性能，结合自我运动、车辆运动和眼动等上下文线索后，微调后的Qwen3-VL-2B模型相比专用Transformer基线准确率提升14.5%，达到新SOTA。

<details>
  <summary>Details</summary>

Motivation: 自我中心视觉在交通场景中尚未被充分探索，现有方法缺乏对行人过街意图的深层推理能力，亟需利用视觉语言模型并结合多模态上下文线索来提升预测性能。

Method: 将行人意图解码任务建模为闭集视觉问答（VQA）问题；首先在零样本设置下评估三类VLM，随后采用参数高效微调（PEFT）方法适配目标任务，并融入自我运动、车辆运动和眼动等额外上下文线索。

Result: 零样本VLM仅略优于随机猜测；微调模型大幅超越零样本版本，相比专用Transformer基线准确率提升9%；融入眼动和自我运动线索后，微调Qwen3-VL-2B模型准确率提升14.5%，达到新SOTA。

Conclusion: 参数高效微调能有效增强VLM对行人过街意图的推理能力；多模态上下文线索（眼动、自我运动）的加入可进一步显著提升预测性能，证明了自我中心视觉在交通安全性预测中的巨大潜力。

Original Abstract: Egocentric vision offers a first-person view of human perception and decision making, yet its potential for traffic-safety prediction remains underexplored. In this work, we study the decoding of pedestrian crossing intentions from short egocentric video clips. We approach this by formulating the task as a closed-ended visual question answering (VQA) problem and leveraging vision language models (VLMs) to predict the pedestrians' intent. We first benchmark three families of state-of-the-art VLMs in a zero-shot setting, finding that they achieve moderate gains over random guessing but exhibit limited higher-level traffic reasoning. Motivated by these findings, we further adapt VLMs to the target task using parameter-efficient fine-tuning. Our results show that the fine-tuned models substantially outperform their zero-shot counterparts and achieve a 9\% accuracy improvement over a specialized transformer-based baseline. Finally, we demonstrate that incorporating additional contextual cues, including ego motion, vehicle motion, and eye gaze, further improves predictive performance. In particular, the fine-tuned Qwen3-VL-2B model guided by eye gaze and ego motion achieves a 14.5% accuracy improvement over the transformer baseline, establishing a new state of the art for egocentric pedestrian intent decoding.

中文摘要翻译: 自我中心视觉提供了人类感知与决策的第一人称视角，但其在交通安全预测方面的潜力尚未得到充分探索。本文研究如何从短时自我中心视频片段中解码行人过街意图。我们将该任务建模为闭集视觉问答问题，并利用视觉语言模型预测行人意图。首先，我们在零样本设置下对三类最先进的视觉语言模型进行基准测试，发现它们相比随机猜测有中等程度提升，但高层交通推理能力有限。基于此发现，我们进一步采用参数高效微调将视觉语言模型适配到目标任务上。结果表明，微调后的模型大幅超越零样本版本，相比专用Transformer基线准确率提升9%。最后，我们证明融入额外上下文线索（包括自我运动、车辆运动和眼动）可进一步提升预测性能。其中，由眼动和自我运动引导的微调Qwen3-VL-2B模型相比Transformer基线准确率提升14.5%，为自我中心行人意图解码任务建立了新的最优水平。

</details>


### [152] [CAMF-Det: Closure-Aware Multimodal Fusion for LiDAR-Camera 3D Object Detection on UAV Platforms](https://arxiv.org/abs/2606.09143)
*Yanze Jiang,Yanfeng Gu,Xian Li*

Main category: cs.CV

TL;DR: CAMF-Det提出了一种面向无人机平台的闭合感知多模态融合框架，通过基于比尔-朗伯定律的物理建模显式构建双模态遮挡强度先验，并将其注入数据增强、特征编码、融合与检测头等全流程，有效提升了无人机俯视场景下受树冠遮挡影响的多模态3D检测性能。

<details>
  <summary>Details</summary>

Motivation: 现有基于LiDAR与相机的多模态3D检测在地面车辆场景表现优异，但无人机俯视场景下树冠遮挡导致地面物体频繁被遮挡，且遮挡程度随空间位置和模态不同而变化。现有融合框架未显式建模这种遮挡，限制了检测性能。

Method: 1) 双模态闭合建模模块：基于比尔-朗伯定律启发公式和建筑物掩码校正，离线构建双模态遮挡强度真值图；2) 双模态预测网络：以真值图为监督，在单帧推理下在线预测遮挡强度；3) 将真值与预测遮挡强度注入数据增强、特征编码、多模态融合和检测头，实现自适应检测。

Result: 在两个自建无人机多模态数据集SI3D-DI和SI3D-DII上，CAMF-Det在所有难度等级上取得最佳性能，困难级别的BEV平均精度分别比最优竞争方法提升9.43%和4.88%。

Conclusion: 显式遮挡先验建模与利用在无人机场景下的鲁棒多模态3D检测中具有显著有效性。

Original Abstract: Multimodal 3D object detection based on LiDAR and cameras has demonstrated excellent performance in ground-vehicle scenarios, but has not been explored for Unmanned Aerial Vehicle (UAV) platforms. In UAV top-down scenes, frequent groundobject occlusion dominated by tree canopies causes spatially varying and modality-dependent information degradation. Existing multimodal fusion frameworks neither explicitly model such ground-object occlusion nor embed occlusion awareness into the detection pipeline, limiting their performance in occluded UAV scenes. To address these challenges, we propose CAMF-Det, a closure-aware multimodal fusion framework for LiDAR-camera 3D object detection on UAV platforms, which derives dual-modal occlusion intensity through physics-inspired modeling and embeds them as priors throughout the detection pipeline. First, a dual-modal closure modeling module explicitly constructs occlusion intensity ground truth for both modalities offline via a Beer-Lambert-inspired formulation and building-mask correction. Second, using these ground-truth maps as supervision, a dual-modal prediction network converts the offline modeling results into online occlusion intensity predictions under single-frame inference. Third, both ground-truth and predicted occlusion intensity are injected into data augmentation, feature encoding, multimodal fusion, and detection head, enabling adaptive detection under spatially varying and modality-dependent information degradation. Experiments on two self-built UAV-based multimodal datasets, SI3D-DI and SI3D-DII, demonstrate that CAMF-Det achieves the best performance across all difficulty levels, with hard-level mAP$_{\mathrm{BEV}}$ improvements of 9.43% and 4.88% over the best competing methods, respectively. These results confirm the effectiveness of explicit occlusion prior modeling and exploitation for robust multimodal 3D detection in UAV scenes.

中文摘要翻译: 基于LiDAR和相机的多模态三维目标检测在地面车辆场景中表现出色，但尚未在无人机平台上得到探索。在无人机俯视场景中，以树冠遮挡为主的地面物体频繁遮挡导致了空间变化且模态相关的信息退化。现有的多模态融合框架既未显式建模这种地面物体遮挡，也未将遮挡感知嵌入检测流程，限制了它们在遮挡无人机场景中的性能。为应对这些挑战，我们提出了CAMF-Det——一种面向无人机平台的闭合感知多模态融合框架，通过物理启发的建模推导出双模态遮挡强度，并将其作为先验嵌入整个检测流程。首先，双模态闭合建模模块通过基于比尔-朗伯定律启发公式和建筑物掩码校正，离线构建两种模态的遮挡强度真值。其次，以这些真值图为监督，双模态预测网络在单帧推理下将离线建模结果转化为在线遮挡强度预测。第三，将真值和预测的遮挡强度注入数据增强、特征编码、多模态融合和检测头，从而在空间变化和模态相关的信息退化下实现自适应检测。在两个自建的无人机多模态数据集SI3D-DI和SI3D-DII上的实验表明，CAMF-Det在所有难度等级上均取得最佳性能，困难级别的BEV平均精度分别比最优竞争方法提升9.43%和4.88%。这些结果证实了显式遮挡先验建模与利用在无人机场景下实现鲁棒多模态三维检测的有效性。

</details>


### [153] [Ultra Flash: Scaling Real-Time Streaming Video Generation to High Resolutions](https://arxiv.org/abs/2606.09150)
*Luxury,Jie Huang,Zihao Fan,Xiaoxiao Ma,Yuming Li,Jun-hao Zhuang,Zeyue Xue,Siming Fu,Haoran Li,Mingchen Zhong,Guohui Zhang,Shichen Ma,Yijun Liu,Jiaqi Shi,Yanwen Ma,Yaofeng Su,Haoyu Wang,Yaowei Li,Songchun Zhang,Weiyang Jin,Yuxuan Bian,Shiyi Zhang,Haojun Xu,Shuai Lu,Xin Han,Wei Tang,Haoyang Huang,Nan Duan*

Main category: cs.CV

TL;DR: Ultra Flash提出级联流式框架，通过三项关键创新在单GPU上实现1K分辨率~30 FPS、2K分辨率~18 FPS的实时高清视频生成，在保持高质量的同时显著提升效率。

<details>
  <summary>Details</summary>

Motivation: 现有自回归视频扩散模型虽能实现流式生成，但分辨率被限制在480P，缺乏高效、可扩展的实时高分辨率视频生成方案。

Method: (1) 架构保持的T2V-to-TV2V超分辨率训练范式及面向AIGC的数据退化管道；(2) 因果流式潜在上采样器与高分辨率解码器；(3) 级联高分辨率流式视频生成优化方案，包括混合奖励增强的稀疏因果化、单步蒸馏、流式自强制偏好优化及动态缓存管理。

Result: 在单GPU上实现1K分辨率约30 FPS、2K分辨率约18 FPS的实时生成，视觉质量和效率均达最优。

Conclusion: Ultra Flash能够可靠地生成超高清流式视频，同时保持最先进的视觉质量和优越的效率。

Original Abstract: While recent autoregressive video diffusion models achieve remarkable streaming quality, they remain confined to low resolutions (e.g., 480P), leaving efficient, scalable, real-time high-resolution video generation a fundamental open challenge. To bridge this gap, we present Ultra Flash, a cascaded streaming framework capable of real-time high-resolution video generation. Ultra Flash achieves ~30 FPS at 1K resolution and ~18 FPS at 2K resolution on a single GPU through three key contributions: (1) an architecture-preserving T2V-to-TV2V super-resolution training paradigm coupled with an AIGC-oriented data degradation pipeline that effectively preserves the generative capability of the base model, enabling enhanced high-resolution detail when cascaded after mainstream low-resolution generative models; (2) a causal streaming latent upsampler paired with a high-resolution decoder, which enhances spatiotemporal coherence while enabling efficient latent spatial scaling and precise high-resolution decoding with negligible computational overhead; and (3) a cascade high-resolution streaming video generation optimization scheme that first performs hybrid-reward-enhanced sparse causalization and single-step distillation of the super-resolution model, then introduces cascaded streaming self-forcing preference optimization with dynamic cache management, jointly enhancing overall coherence, improving quality, and enabling real-time high-resolution streaming video generation. Extensive experiments demonstrate that Ultra Flash reliably produces ultra-high-resolution streaming video while maintaining state-of-the-art visual quality and superior efficiency.

中文摘要翻译: 尽管最近的自回归视频扩散模型在流式质量方面取得了显著成果，但它们仍被限制在低分辨率（如480P），使得高效、可扩展的实时高分辨率视频生成仍是一个基础性的开放挑战。为弥补这一差距，我们提出了Ultra Flash，一种能够实现实时高分辨率视频生成的级联流式框架。Ultra Flash在单GPU上实现了1K分辨率下约30 FPS和2K分辨率下约18 FPS的生成速度，这得益于三项关键贡献：(1) 一种架构保持的T2V-to-TV2V超分辨率训练范式，结合面向AIGC的数据退化管道，有效保留了基础模型的生成能力，从而在级联到主流低分辨率生成模型后能够增强高分辨率细节；(2) 一种因果流式潜在上采样器与高分辨率解码器，在增强时空连贯性的同时，以可忽略的计算开销实现高效的潜在空间缩放和精确的高分辨率解码；(3) 一种级联高分辨率流式视频生成优化方案，首先对超分辨率模型进行混合奖励增强的稀疏因果化和单步蒸馏，随后引入具有动态缓存管理的级联流式自强制偏好优化，共同增强整体连贯性、提升质量，并实现实时高分辨率流式视频生成。大量实验表明，Ultra Flash能够可靠地生成超高清流式视频，同时保持最先进的视觉质量和优越的效率。

</details>


### [154] [OmniGen-AR: AutoRegressive Any-to-Image Generation](https://arxiv.org/abs/2606.09156)
*Junke Wang,Xun Wang,Qiushan Guo,Peize Sun,Weilin Huang,Zuxuan Wu,Yu-Gang Jiang*

Main category: cs.CV

TL;DR: OmniGen-AR 是一个统一的任意输入到图像生成的自回归框架，通过共享视觉分词器和解耦因果注意力机制，在单个模型中支持文本、空间信号和视觉上下文等多种条件输入，在多个基准上达到最先进或竞争力的结果。

<details>
  <summary>Details</summary>

Motivation: 现有自回归视觉生成模型通常局限于单一模态条件（如文本），无法满足实际应用中需要从多种控制信号合成图像的场景需求。

Method: 提出 OmniGen-AR 框架：1) 通过共享视觉分词器将各种视觉条件离散化，结合文本分词器处理文本提示，实现多种条件输入的统一建模；2) 提出解耦因果注意力（DCA），将全序列因果掩码分离为条件因果注意力和内容因果注意力，作为训练时正则化项防止条件令牌到内容令牌的信息泄露，推理时仍保持标准下一令牌预测。

Result: 在 GenEval 上达到 0.63，在 VBench 上达到 80.02，在多个基准上取得新的最先进或具有竞争力的结果。

Conclusion: OmniGen-AR 通过统一框架和解耦因果注意力机制，有效实现了灵活、高保真的多条件视觉生成。

Original Abstract: Autoregressive (AR) models have demonstrated strong potential in visual generation, offering superior performance with simple architectures and optimization objectives. However, existing methods are typically limited to single-modality conditions, e.g., text, restricting their applicability in real-world scenarios that demand image synthesis from diverse controls. In this work, we present OmniGen-AR, a unified autoregressive framework for Any-to-Image generation. By discretizing various visual conditions through a shared visual tokenizer and text prompts with a text tokenizer, OmniGen-AR supports a broad spectrum of conditional inputs within a single model, including text (text-to-image generation), spatial signals (segmentation-to-image and depth-to-image), and visual context (image editing, frame prediction, and text-to-video generation). To mitigate the risk of information leakage from condition tokens to content tokens, we introduce Disentangled Causal Attention (DCA), which separates the full-sequence causal mask into condition causal attention and content causal attention. It serves as a training-time regularizer without affecting the standard next-token prediction during inference. With this design, OmniGen-AR achieves new state-of-the-art or at least competitive results across a range of benchmark, e.g., 0.63 on GenEval and 80.02 on VBench, demonstrating its effectiveness in flexible and high-fidelity visual generation.

中文摘要翻译: 自回归（AR）模型在视觉生成领域展现出强大潜力，凭借简单的架构和优化目标即可实现优越的性能。然而，现有方法通常局限于单一模态条件（如文本），限制了其在需要从多种控制信号合成图像的现实场景中的应用。在这项工作中，我们提出了 OmniGen-AR，一个统一的任意输入到图像生成的自回归框架。通过共享视觉分词器将各种视觉条件离散化，并利用文本分词器处理文本提示，OmniGen-AR 在单个模型中支持广泛的条件输入，包括文本（文本到图像生成）、空间信号（分割图到图像和深度图到图像）以及视觉上下文（图像编辑、帧预测和文本到视频生成）。为了缓解条件令牌到内容令牌的信息泄露风险，我们引入了解耦因果注意力（DCA），它将全序列因果掩码分离为条件因果注意力和内容因果注意力。该机制作为训练时的正则化项，不影响推理时的标准下一令牌预测。通过这一设计，OmniGen-AR 在多个基准上取得了新的最先进或至少具有竞争力的结果，例如 GenEval 上 0.63 和 VBench 上 80.02，展示了其在灵活、高保真视觉生成中的有效性。

</details>


### [155] [Zero-Parameter Geometric Gating for Temporally Stable Low-Altitude UAV Video Semantic Segmentation](https://arxiv.org/abs/2606.09162)
*Jingpu Yang,Fengxian Ji,Zhengzhao Lai,Juanfan Wu,Mingxuan Cui,Yufeng Wang*

Main category: cs.CV

TL;DR: 提出一种零参数几何门控机制，利用RANSAC单应性内点比率将低空无人机视频语义分割中的区域路由至单应性或光流变换，在无额外训练参数的前提下有效提升时序一致性。

<details>
  <summary>Details</summary>

Motivation: 低空无人机视频语义分割需要时序一致性，但稠密光流在占主导的平面区域会引入空间结构化噪声，现有方法难以有效处理这一问题。

Method: 提出零参数几何门控：在16×16空间网格上计算RANSAC单应性内点比率，通过中位数阈值二值决策将每个区域路由至单应性变换或光流变换，再经语义相似性传播（SSP）融合；门控本身无学习参数，仅SSP融合层引入211K可训练参数。

Result: 在合成UAVid数据集上，两种架构（SegFormer-b2和Hiera-S+UPerNet）的mIoU分别提升+4.24%和+4.91%；机制分析表明平面区域光流残差存在空间自相关（Moran's I=0.32, p<0.001），可预测边界不稳定性（Spearman ρ=0.66），刚性化在单应性有效区域将时序一致性从62%提升至92%（+29.5pp）。

Conclusion: 所提零参数几何门控在几乎不增加参数的情况下，通过区域自适应的变换选择有效提升了无人机视频语义分割的时序一致性和分割精度。

Original Abstract: Video semantic segmentation for low-altitude UAVs requires temporal consistency, yet dense optical flow introduces spatially structured noise in the planar regions that dominate aerial imagery. We propose a zero-parameter geometric gate that uses RANSAC homography inlier ratios on a $16\times16$ spatial grid to route each region to either homography or optical flow warp before fusion via Semantic Similarity Propagation. The gate requires no learned parameters -- only a median-threshold binary decision on RANSAC statistics -- adding only 211K trainable parameters (the SSP fusion layer) to a frozen backbone. On synthetic UAVid, the method achieves +4.24--4.91\% mIoU improvement over base models across two architectures (SegFormer-b2 and Hiera-S+UPerNet). Mechanism diagnostics reveal that flow residuals in planar regions are spatially autocorrelated (Moran's I = 0.32, $p < 0.001$), predict boundary instability (Spearman $ρ= 0.66$), and that rigidification recovers temporal consistency from 62\% to 92\% (+29.5pp) in homography-valid regions.

中文摘要翻译: 低空无人机视频语义分割需要时序一致性，然而稠密光流在占据航拍图像主体的平面区域会引入空间结构化噪声。本文提出一种零参数几何门控机制，利用在16×16空间网格上计算的RANSAC单应性内点比率，将每个区域路由至单应性变换或光流变换，再通过语义相似性传播（SSP）进行融合。该门控无需任何学习参数——仅对RANSAC统计量进行中位数阈值二值决策——在冻结骨干网络的基础上仅增加211K可训练参数（即SSP融合层）。在合成UAVid数据集上，该方法在两种架构（SegFormer-b2和Hiera-S+UPerNet）下相较于基础模型分别实现了+4.24%和+4.91%的mIoU提升。机制诊断表明：平面区域的光流残差存在空间自相关（Moran's I=0.32, p<0.001），可预测边界不稳定性（Spearman ρ=0.66），而刚性化处理在单应性有效区域将时序一致性从62%恢复至92%（提升29.5个百分点）。

</details>


### [156] [Vision-Language Guided Hyperspectral Object Tracking via Semantics Fusion and Contextual Template Updating](https://arxiv.org/abs/2606.09167)
*Rui Yao,Yuhong Zhang,Kunyang Sun,Hancheng Zhu,Jiaqi Zhao,Zhiwen Shao,Abdulmotaleb El Saddik*

Main category: cs.CV

TL;DR: 提出VLHTrack，一种高光谱视觉-语言联合跟踪框架，通过语言引导的波段选择模块（LBSM）解决光谱冗余问题，并利用动态模板更新模块（DTUM）应对目标形变，在HOT2023和HOT2024上达到SOTA性能。

<details>
  <summary>Details</summary>

Motivation: 高光谱目标跟踪中，光谱冗余严重限制了模型泛化性和跟踪性能；同时动态场景中目标遮挡、光照变化等导致的外观剧烈变化，使得现有时序建模方法难以有效应对帧间大形变。

Method: 1) Language-Guided Band Selection Module (LBSM)：利用大语言模型描述建立语义到光谱的映射，减少冗余并突出判别性光谱特征；2) Multi-Modal Vision-Language Fusion Module：融合视觉和语言嵌入，学习跨模态协同表示；3) Dynamic Template Update with Mamba (DTUM)：通过选择性状态空间建模学习帧间依赖，实现模板特征的动态更新。

Result: 在HOT2023和HOT2024数据集上，VLHTrack优于现有最先进方法。

Conclusion: 引入语言先验能有效解决高光谱跟踪中的光谱冗余问题，动态模板更新策略能应对长序列中的目标形变，多模态融合进一步提升了跟踪性能。

Original Abstract: Hyperspectral object tracking (HOT) leverages the rich spectral information provided by hyperspectral videos (HSVs), offering substantial potential for object tracking. However, efficiently extracting and exploiting spectral information from redundant spectral bands remains a fundamental challenge, which severely limits model generalization and tracking performance. Moreover, in dynamic scenes, targets often experience drastic appearance variations due to factors such as occlusion and illumination changes. These variations lead to large deformations between the current frame and the template. Such discrepancies pose major challenges for existing temporal modeling approaches. In this work, we propose VLHTrack, a novel hyperspectral vision-language (VL) joint tracking framework. Specifically, we incorporate language priors to address the fundamental challenge of spectral redundancy by designing a Language-Guided Band Selection Module (LBSM). By leveraging Large Language Model (LLM) descriptions, LBSM establishes a semantic-to-spectral mapping that mitigates redundancy and accentuates discriminative spectral features. A Multi-Modal Vision-Language Fusion Module is then employed to seamlessly integrate visual and linguistic embeddings, harnessing their complementary advantages to learn coherent cross-modal representations. To address target deformation in long-term sequences, we propose a dynamic update template feature strategy implemented via the Dynamic Template Update with Mamba (DTUM) module. By leveraging selective state space modeling, DTUM learns inter-frame dependencies to update template feature, ensuring efficient template feature evolution guided by temporal context. Experiments on HOT2023 and HOT2024 demonstrate that VLHTrack outperforms state-of-the-art (SOTA) methods.

中文摘要翻译: 高光谱目标跟踪（HOT）利用高光谱视频（HSV）提供的丰富光谱信息，在目标跟踪领域展现出巨大潜力。然而，如何从冗余的光谱波段中高效提取和利用光谱信息仍然是一个基本挑战，严重限制了模型的泛化性和跟踪性能。此外，在动态场景中，目标常因遮挡和光照变化等因素出现剧烈的外观变化，导致当前帧与模板之间存在较大的形变。这种差异对现有的时序建模方法构成了重大挑战。本文提出VLHTrack，一种新颖的高光谱视觉-语言（VL）联合跟踪框架。具体而言，我们引入语言先验，通过设计语言引导的波段选择模块（LBSM）来解决光谱冗余这一基本问题。借助大语言模型（LLM）描述，LBSM建立了语义到光谱的映射，减少了冗余并突出了判别性光谱特征。随后采用多模态视觉-语言融合模块，将视觉嵌入与语言嵌入无缝集成，利用其互补优势学习一致的跨模态表示。为了解决长序列中的目标形变问题，我们提出了一种通过Mamba动态模板更新（DTUM）模块实现的动态更新模板特征策略。DTUM利用选择性状态空间建模学习帧间依赖关系以更新模板特征，确保在时序上下文引导下模板特征的高效演化。在HOT2023和HOT2024上的实验表明，VLHTrack优于现有最先进（SOTA）方法。

</details>


### [157] [Claude Code-Driving Scenario Mining for the Argoverse 2 Challenge](https://arxiv.org/abs/2606.09180)
*Wei Deng,Caoshengzhe Xue,Shuaikun Liu,Zhaohong Liu,Mengshi Qi,Huadong Ma*

Main category: cs.CV

TL;DR: 本文提出了一个四阶段流水线系统，用于CVPR 2026 Argoverse 2场景挖掘挑战，采用Claude Code代理自动生成代码、迭代筛选训练集、语义代码审查以及Qwen3-VL场景级验证，以提高场景挖掘的准确性和鲁棒性。

<details>
  <summary>Details</summary>

Motivation: 解决场景挖掘挑战中自动化代码生成和少样本学习的准确性问题，减少人工注释成本和减少误检。

Method: (1) 基于GLM~5.1的Claude Code代理自主生成代码；(2) 使用Timestamp Balanced Accuracy阈值0.8迭代筛选训练集，构建少样本示例；(3) 独立Claude Code会话进行语义代码审查；(4) 使用Qwen3-VL进行场景级验证以过滤误报。

Result: 在Argoverse 2测试集上报告了实验结果。

Conclusion: 所提出的四阶段流水线方法能够有效提升场景挖掘任务的性能，通过自动化代码生成与多阶段质量保障机制实现了可靠的检测结果。

Original Abstract: We present our submission to the CVPR 2026 Argoverse 2 Scenario Mining Challenge. Our system uses a four-stage pipeline: (1) autonomous code generation via a Claude Code agent powered by GLM~5.1, (2) iterative training set screening with Timestamp Balanced Accuracy threshold 0.8 to curate few-shot examples, (3) semantic code review by a separate Claude Code session, and (4) Qwen3-VL scene-level verification to filter false positives. We report results on the Argoverse 2 test set.

中文摘要翻译: 我们提交了参加CVPR 2026 Argoverse 2场景挖掘挑战赛的成果。我们的系统采用四阶段流水线：(1) 由基于GLM~5.1驱动的Claude Code代理自主生成代码，(2) 使用Timestamp Balanced Accuracy阈值0.8迭代筛选训练集以精选少样本示例，(3) 由独立的Claude Code会话进行语义代码审查，(4) 使用Qwen3-VL进行场景级验证以过滤误报。我们在Argoverse 2测试集上报告了实验结果。

</details>


### [158] [Counterfactual Reasoning for Fine-Grained Evidence Disentanglement in VideoQA](https://arxiv.org/abs/2606.09181)
*Zhou Du,Hamid Krim,Xiao Wu,Zhaoquan Yuan,Liangwei Li,Keisuke Fujii*

Main category: cs.CV

TL;DR: 提出反事实推理框架CREDiT，通过结构因果模型将视频问答中的视觉表征显式分解为因果成分和非因果成分，在细粒度证据定位下提升推理可靠性和答案准确性。

<details>
  <summary>Details</summary>

Motivation: 现有视频问答模型常依赖虚假统计关联而非因果线索，导致推理不可靠；已有方法依赖跨模态相关性、昂贵标注数据或不足的因果约束，且只在时间区间层面操作，无法显式解开因果视觉线索与混杂因素。

Method: 基于结构因果模型构建CREDiT框架，在独立性和最小性约束下将跨模态表征显式分解为因果与非因果成分；引入特征级因果干预并构建反事实输入，近似因果效应同时抑制非因果关联。

Result: 在NExT-GQA、SportsQA和SPORTU-video三个数据集上，CREDiT在通用和复杂体育场景下均一致提升了答案准确率和推理可靠性。

Conclusion: CREDiT通过细粒度因果证据解耦，有效提升了VideoQA系统的可信度，实现了更忠实可靠的推理。

Original Abstract: Recent advances in video multimodal models have significantly improved VideoQA performance. However, these systems often rely on spurious statistical correlations rather than answer-relevant causal evidence, resulting in unfaithful and brittle reasoning, especially in complex real-world scenarios. Existing methods either rely on cross-modality correlations, costly curated training resources, or insufficient causal assumptions and constraints, and typically operate at the time-interval level. As a result, they fail to explicitly disentangle causal visual cues from confounders and provide limited fine-grained evidence localization. To address this issue, we propose a Counterfactual Reasoning framework for fine-grained Evidence Disentanglement (CREDiT). CREDiT formulates the VideoQA process using a structural causal model and learns cross-modality representations that are explicitly decomposed into causal and non-causal components under independence and minimality constraints. To facilitate faithful disentanglement, we introduce feature-level causal interventions and construct counterfactual inputs that approximate causal effects while suppressing non-causal correlations. Extensive experiments on NExT-GQA, SportsQA, and SPORTU-video demonstrate that CREDiT consistently improves answer accuracy and reasoning reliability across both generic and complex sports scenarios, leading to more trustworthy VideoQA systems.

中文摘要翻译: 近期视频多模态模型的进展显著提升了视频问答（VideoQA）的性能。然而，这些系统往往依赖虚假的统计相关性而非与答案相关的因果证据，导致推理不忠实且脆弱，尤其在复杂的现实场景中。现有方法要么依赖跨模态相关性、昂贵的策划训练资源，要么依赖不足的因果假设与约束，且通常在时间区间层面操作。因此，它们无法显式地将因果视觉线索从混杂因素中解耦出来，且提供的细粒度证据定位有限。为解决这一问题，我们提出了一种面向细粒度证据解耦的反事实推理框架（CREDiT）。CREDiT利用结构因果模型对视频问答过程进行建模，并在独立性和最小性约束下，将跨模态表征显式分解为因果成分和非因果成分。为促进忠实的解耦，我们引入特征级因果干预并构建反事实输入，从而在抑制非因果相关性的同时近似因果效应。在NExT-GQA、SportsQA和SPORTU-video上的大量实验表明，CREDiT在通用场景和复杂体育场景下均一致地提升了答案准确率和推理可靠性，从而构建出更可信的视频问答系统。

</details>


### [159] [CP4D: Compositional Physics-aware 4D Scene Generation](https://arxiv.org/abs/2606.09187)
*Hanxin Zhu,Cong Wang,Tianyu He,Long Chen,Xin Jin,Chen Gao,Zhibo Chen*

Main category: cs.CV

TL;DR: CP4D提出了一个将静态3D环境与物理可信的动态物体相结合的新型4D生成范式，通过三阶段流程（静态背景与前景物体生成、混合运动合成、自动合成）生成物理一致且视觉逼真的可探索、可交互4D场景。

<details>
  <summary>Details</summary>

Motivation: 现有的4D生成方法虽然取得了显著进展，但通常无法捕捉潜在的物理原理，生成的物体轨迹和交互缺乏物理一致性，导致结果在视觉上不真实。

Method: CP4D采用三阶段流程：1) 利用预训练生成模型分别生成高保真3D静态环境和前景物体；2) 提出混合运动合成策略，融合物理模拟器的先验知识与视频扩散模型的常识来生成物理可信的物体轨迹和交互；3) 开发自动合成机制，将静态环境与动态物体无缝融合为物理一致的4D场景。

Result: 大量实验表明CP4D能够生成视觉保真度高、物理可信性强、细粒度可控的可探索、可交互4D场景，显著优于现有方法。

Conclusion: 通过将4D生成重新表述为静态3D环境与物理可信的动态物体的组合，并引入混合运动合成策略，CP4D有效解决了现有4D生成中物理不一致的问题。

Original Abstract: 4D generation (\textit{i.e.}, dynamic 3D generation) has recently emerged as a rapidly growing research frontier due to its powerful spatiotemporal modeling capabilities. However, despite notable advances, existing approaches typically fail to capture the underlying physical principles, producing results that are both physically inconsistent and visually implausible. To overcome this limitation, we present CP4D, a novel paradigm for photorealistic 4D scene synthesis with faithful adherence to complex physical dynamics. Drawing inspiration from the compositional nature of real-world scenes, where immutable static backgrounds coexist with dynamic, physically plausible foregrounds, CP4D reformulates 4D generation as the integration of a static 3D environment with physically grounded dynamic objects. On this basis, our framework follows a three-stage pipeline: \textbf{1)} Firstly, we leverage pre-trained expert models to generate high-fidelity 3D representations of the environment and foreground objects respectively. \textbf{2)} Subsequently, to produce physically plausible trajectories and realistic interactions for these objects, we propose a hybrid motion synthesis strategy that integrates priors from physical simulators with the common sense embedded in video diffusion models. \textbf{3)} Finally, we develop an automated composition mechanism that seamlessly fuses the static environment and dynamic objects into coherent, physically consistent 4D scenes. Extensive experiments demonstrate that CP4D can generate explorable and interactive 4D scenes with high visual fidelity, strong physical plausibility, and fine-grained controllability, significantly outperforming existing methods. The project page: https://anonymous.4open.science/w/CP4D/.

中文摘要翻译: 4D生成（即动态3D生成）因其强大的时空建模能力，近期已成为一个快速发展的研究前沿。然而，尽管取得了显著进展，现有方法通常无法捕捉潜在的物理原理，生成的结果既物理不一致又视觉上不可信。为了克服这一局限，我们提出了CP4D，一种新颖的光逼真4D场景合成范式，能够忠实遵循复杂的物理动力学。受真实世界场景的组合性质启发——其中不变的静态背景与动态的、物理可信的前景物体共存——CP4D将4D生成重新表述为静态3D环境与物理基础动态物体的集成。在此基础上，我们的框架采用三阶段流程：1) 首先，利用预训练专家模型分别生成高保真的环境和前景物体的3D表示。2) 其次，为了为这些物体生成物理可信的轨迹和真实的交互，我们提出了一种混合运动合成策略，该策略融合了物理模拟器的先验知识与视频扩散模型中嵌入的常识。3) 最后，我们开发了一种自动合成机制，将静态环境与动态物体无缝融合为连续、物理一致的4D场景。大量实验表明，CP4D能够生成具有高视觉保真度、强物理可信性和细粒度可控性的可探索、可交互4D场景，显著优于现有方法。项目页面：https://anonymous.4open.science/w/CP4D/。

</details>


### [160] [Event-driven dynamic trajectories reconstruction and measurement of mechanical parameters for fragments](https://arxiv.org/abs/2606.09208)
*Haoyang Li,Banglei Guan,Muxi Zha,Yifei Bian,Minzu Liang,Yang Shang,Qifeng Yu*

Main category: cs.CV

TL;DR: 本文提出了一种基于事件相机的碎片动态轨迹重建与力学参数测量方法，通过多相机几何约束和概率模型，解决了强闪光、烟雾干扰下高速碎片的精确测量难题。

<details>
  <summary>Details</summary>

Motivation: 战斗部爆炸时产生的高速碎片被强闪光和烟雾严重遮挡，导致其力学参数（位置、速度、动能）难以准确获取，现有方法在此极端条件下性能不足。

Method: 构建多事件相机视觉系统，采用三种几何约束：时间关联极线约束寻找潜在匹配点对，三焦张量线约束和局部单应性约束剔除误匹配；建立综合概率模型，用熵权法确定各约束权重量化过滤误匹配；通过空间线-线交会和非线性优化实现3D轨迹重建，进而计算速度和动能。

Result: 实验表明该方法能在强闪光和烟雾干扰下，利用事件相机微秒级时间分辨率和高动态范围特性，实现高速碎片轨迹的精确重建与力学参数测量。

Conclusion: 该方法为战斗部碎片场的机械损伤评估和战术防护设计提供了可靠的技术支持。

Original Abstract: During warhead detonation, high-density, high-speed, and mutually occluded fragments are generated. Their mechanical parameters (position, velocity, kinetic energy) directly determine the lethality of the warhead fragment field. However, high-intensity flash and smoke in detonation scenarios severely hinder the accurate acquisition of these mechanical parameters. To address this challenge, this paper integrates experimental mechanics approaches and presents an event-driven method for reconstructing the dynamic trajectories of fragments and measuring their mechanical parameters. As a novel brain-inspired visual sensor, event cameras offer microsecond-level temporal resolution and high dynamic range lighting change perception, overcoming the difficulty of accurately measuring high-speed targets under strong flash interference. The method constructs a multi-event-camera vision system, adopting three geometric constraints: time-correlated epipolar constraint to find potential matching event point pairs, and trifocal tensor line constraint plus local homography constraint to eliminate mismatches. A comprehensive probability model is established, with entropy weight method determining the weight of each constraint's probability to quantitatively filter mismatches. 3D trajectory reconstruction is achieved via spatial line-line intersection and nonlinear optimization. Finally, the velocity and kinetic energy of the fragments are calculated based on the reconstructed trajectory. This method provides reliable technical support for the mechanical damage evaluation of warhead fragment fields and the tactical protection design.

中文摘要翻译: 在战斗部起爆过程中，会产生高密度、高速且相互遮挡的碎片。这些碎片的力学参数（位置、速度、动能）直接决定了战斗部碎片场的杀伤力。然而，爆炸场景中的高强度闪光和烟雾严重阻碍了这些力学参数的准确获取。针对这一挑战，本文融合实验力学方法，提出了一种事件驱动的碎片动态轨迹重建与力学参数测量方法。作为新型仿生视觉传感器，事件相机提供微秒级时间分辨率和高动态范围的光照变化感知能力，克服了在强闪光干扰下高速目标难以精确测量的困难。该方法构建了多事件相机视觉系统，采用三种几何约束：时间关联极线约束用于寻找潜在匹配事件点对，三焦张量线约束和局部单应性约束用于剔除误匹配。建立了综合概率模型，采用熵权法确定各约束概率的权重，以定量过滤误匹配。通过空间线-线交会和非线性优化实现3D轨迹重建。最后，基于重建轨迹计算碎片的速度和动能。该方法为战斗部碎片场的机械损伤评估与战术防护设计提供了可靠的技术支持。

</details>


### [161] [Minimal Solvers for Full-DoF Motion Estimation from Asynchronous Differential SfM](https://arxiv.org/abs/2606.09218)
*Shuo Pan,Banglei Guan,Bin Li,Zhenbao Yu,Zibin Liu,Zi Wang,Yang Shang,Qifeng Yu*

Main category: cs.CV

TL;DR: 提出一种直接从异步光流估计完整六自由度（角速度+线速度）自运动的方法，通过解耦对极约束并推导异步形式，实现了首个代数最小5点求解器，在高速场景下具有更高的精度和鲁棒性。

<details>
  <summary>Details</summary>

Motivation: 事件相机输出异步数据流，传统基于帧的同步算法难以直接处理；现有方法多假设已知角速度或仅处理部分自由度，缺乏直接从异步数据恢复完整六自由度自运动的方案。

Method: 1) 将微分对极约束解耦为角速度和线速度分量，推导其异步数据形式；2) 基于该形式开发至少5点即可求解的优化算法；3) 对旋转动力学进行一阶近似，将约束方程转化为多项式形式，得到首个代数最小5点求解器；4) 通过截断高阶角速度项提出加速求解器以保证高速场景实时性。

Result: 在合成和真实数据集上的大量评估表明，异步方法在精度和对时空噪声的鲁棒性方面均优于传统同步方法。

Conclusion: 该工作为高速机器人应用中高效、准确的连续时间运动估计奠定了关键基础。

Original Abstract: As a bio-inspired intelligent sensor, event cameras have introduced a new paradigm in the intelligent perception of spatiotemporal information and visual motion estimation, characterized by their high temporal resolution, low latency, and minimal power consumption. However, their asynchronous data streams present significant challenges to traditional synchronous, frame-based algorithms. To address these challenges, this paper presents a novel framework for full degree of freedom (DoF) egomotion estimation directly from asynchronous optical flow, specifically targeting the joint recovery of angular and linear velocities. We decouple the differential epipolar constraint into distinct angular and linear velocity components, and derive its formulation for asynchronous data. Based on this formulation, an optimization algorithm is developed that enables full-DoF egomotion estimation leveraging at least five points. Furthermore, by applying a first-order approximation to rotational dynamics, we transform the constraint equations into a polynomial form, resulting in the first algebraic minimal 5-point solver for this formulation. To ensure real-time performance in high-speed scenarios, we additionally propose an accelerated solver achieved by truncating high-order angular velocity terms. Extensive evaluations on both synthetic and real-world datasets demonstrate that the asynchronous approach outperforms traditional synchronous methods, particularly in its accuracy and robustness to spatiotemporal noise. We believe that this work establishes a critical foundation for efficient and accurate continuous-time motion estimation in high-speed robotics applications.

中文摘要翻译: 作为一种受生物启发的智能传感器，事件相机以其高时间分辨率、低延迟和极低功耗的特点，为时空信息智能感知与视觉运动估计引入了新范式。然而，其异步数据流对传统的同步帧基算法构成了重大挑战。为解决这些问题，本文提出了一种直接从异步光流进行完整六自由度自运动估计的新型框架，专门针对角速度和线速度的联合恢复。我们将微分对极约束解耦为独立的角速度和线速度分量，并推导了其在异步数据下的形式。基于该形式，我们开发了一种优化算法，能够利用至少五个点实现完整六自由度自运动估计。此外，通过对旋转动力学应用一阶近似，我们将约束方程转化为多项式形式，得到了该公式下首个代数最小5点求解器。为确保高速场景下的实时性能，我们还通过截断高阶角速度项提出了一种加速求解器。在合成数据集和真实数据集上的大量评估表明，异步方法在精度和对时空噪声的鲁棒性方面均优于传统同步方法。我们相信，这项工作为高速机器人应用中高效、准确的连续时间运动估计奠定了关键基础。

</details>


### [162] [Semi-supervised Source Detection in Astronomical Images: New Benchmark and Strong Baseline](https://arxiv.org/abs/2606.09219)
*Longhan Feng,Zihuang Cao,Ali Luo,Yuanhao Guo,Shuilian Yao,Yixin Guo,Qi Jia,Yu Liu*

Main category: cs.CV

TL;DR: 本文针对天文图像中高密度、小尺寸、低信噪比源检测困难及标注稀缺的问题，构建了包含18,400幅图像和728,898个源实例的LAMOST-DET基准数据集，并提出了一种名为Nova Teacher的半监督学习框架，结合光源增强模块、置信度引导的伪监督和跨视角互补挖掘，在两个半监督设置下分别提升了4.04%和5.22%的mAP。

<details>
  <summary>Details</summary>

Motivation: 天文图像具有高密度、点扩散函数效应和低信噪比等特性，给现有先进目标检测器带来了巨大挑战；同时，由于对密集、小尺寸和暗弱源进行标注极其困难，全监督检测方法在实践中几乎不可行，因此需要一种能够在稀疏标注下有效检测密集源的半监督学习方法。

Method: 提出了Nova Teacher半监督学习框架，采用双教师范式，包含三个核心模块：(1) 光源增强模块，用于提升暗弱源的信号强度；(2) 置信度引导的伪监督，利用教师模型生成高质量的伪标签；(3) 跨视角互补挖掘，从不同视角提取互补信息以增强检测能力。

Result: 在LAMOST-DET数据集上，Nova Teacher在两种半监督设置下分别比先前方法提升了4.04%和5.22%的mAP；在自然图像数据集上的实验也验证了方法的泛化能力。

Conclusion: Nova Teacher能够有效利用稀疏标注实现密集天文源的精准检测，并在不同场景下具有良好的泛化性能，为解决天文图像中标注稀缺问题提供了可行的半监督学习方案。

Original Abstract: Source detection in modern observational astronomy is a cornerstone for localizing and identifying stellar sources accurately. It is crucial for studies such as stellar population synthesis and cosmological parameter estimation. However, the characteristics of astronomical images, including high density, the effect of point spread functions and low signal-to-noise ratios, significantly challenge the latest advanced object detectors. Besides, fully-supervised detection methods are hardly practical, due to the significant difficulty in annotating dense, small, and faint sources in astronomical images. To tackle the scarcity of astronomical datasets, we introduce a new comprehensive benchmark (LAMOST-DET), comprising 18,400 astronomical images and 728,898 source instances. Upon the dataset, we further devise a novel semi-supervised learning framework coined Nova Teacher, capable of detecting dense sources effectively given sparse annotations. It integrates source light enhancement module, confidence-guided pseudo-supervision, and cross-view complementary mining in a dual-teacher paradigm. Extensive experiments on LAMOST-DET show that, Nova Teacher consistently improves previous competitors by 4.04% and 5.22% mAP under two semi-supervised settings. Additionally, our method competes against other detectors on a natural image dataset, validating its generalization ability to various scenarios. The source code is available at https://github.com/AcWiz/NovaTeacher.

中文摘要翻译: 在现代观测天文学中，源检测是准确定位和识别恒星源的关键基础，对于恒星群体合成和宇宙学参数估计等研究至关重要。然而，天文图像的特性，包括高密度、点扩散函数效应和低信噪比，给当前最先进的物体检测器带来了巨大挑战。此外，由于在天文图像中标注密集、小尺寸和暗弱源存在显著困难，全监督检测方法几乎不具实用性。为解决天文数据集的稀缺问题，我们引入了一个新的综合性基准数据集（LAMOST-DET），包含18,400张天文图像和728,898个源实例。基于该数据集，我们进一步设计了一种名为Nova Teacher的新型半监督学习框架，能够在稀疏标注下有效检测密集源。该框架在双教师范式中集成了光源增强模块、置信度引导的伪监督以及跨视角互补挖掘。在LAMOST-DET上的大量实验表明，Nova Teacher在两种半监督设置下分别比先前方法持续提升了4.04%和5.22%的mAP。此外，我们的方法在自然图像数据集上也与其他检测器相媲美，验证了其在不同场景下的泛化能力。源代码已开源在https://github.com/AcWiz/NovaTeacher。

</details>


### [163] [EgoTactile: Learning Grasp Pressure for Everyday Objects from Egocentric Video](https://arxiv.org/abs/2606.09243)
*Yuan Zeng,Yujia Shi,Tiao Tan,Xingting Li,Yaqi Qin,Zongqing Lu,Wenming Yang,Jing-Hao Xue,Qingmin Liao*

Main category: cs.CV

TL;DR: 本文提出了EgoTactile基准——首个将自中心视频与全手抓握压力配准的数据集，并基于此提出了EgoPressureFormer作为判别基线，以及EgoPressureDiff条件扩散框架，利用预训练视频扩散先验和物理感知特征修正层，从自中心视频中推断全手抓握压力分布。

<details>
  <summary>Details</summary>

Motivation: 现有方法依赖侵入式硬件进行密集触觉传感，基于视觉的方法多局限于平面表面或指尖接触，无法泛化到复杂3D物体交互。因此，需要一种能从自中心视频中估计全手抓握压力的非侵入式方法。

Method: 1) 构建EgoTactile基准，包含自中心视频与全手压力标签，并设裸手迁移子集。2) 提出EgoPressureFormer作为判别式基线。3) 提出EgoPressureDiff条件扩散框架，适配大规模预训练视频扩散主干，结合世界知识先验与物理感知特征修正层注入语义约束，推断合理的接触模式并解决视觉-物理歧义。

Result: 实验表明所提方法在基准上取得优越性能，并展现出鲁棒的野外场景迁移能力。

Conclusion: 条件扩散框架结合物理先验能有效从自中心视频中估计全手压力分布，并为沉浸式VR和机器人操作提供了可行的非侵入式解决方案。

Original Abstract: Estimating full-hand grasp pressure from egocentric video is critical for immersive VR and robotic manipulation, yet dense tactile sensing often relies on intrusive hardware. Existing vision-based methods predominantly rely on planar surfaces or fingertip contacts, failing to generalize to complex 3D object interactions. Therefore, we introduce EgoTactile, a benchmark pairing egocentric video with full-hand pressure supervision for diverse everyday objects, incorporating a bare-hand transfer subset to enable generalization to natural scenarios. Leveraging this benchmark, we first establish EgoPressureFormer as a discriminative baseline. Beyond this, to explicitly address the uncertainty in partial observations, we propose EgoPressureDiff, a conditional diffusion framework that adapts a large-scale pre-trained video diffusion backbone. By combining rich world knowledge priors with a Physically-Informed Feature Rectification layer to inject semantic constraints, our approach effectively infers plausible contact patterns and resolves visual-physical ambiguities. Extensive experiments demonstrate that our method achieves superior performance on the benchmark and robust transferability to in-the-wild scenarios. Our project page is available at https://egotactile.github.io/.

中文摘要翻译: 从自中心视频中估计全手抓握压力对于沉浸式虚拟现实和机器人操作至关重要，然而密集触觉感知通常依赖侵入式硬件。现有的基于视觉的方法主要局限于平面表面或指尖接触，无法泛化到复杂的3D物体交互。因此，我们提出了EgoTactile——一个将自中心视频与全手压力监督配准的基准，涵盖多种日常物体，并包含一个裸手迁移子集以实现对自然场景的泛化。基于该基准，我们首先建立了EgoPressureFormer作为判别式基线。此外，为显式处理部分观测中的不确定性，我们提出了EgoPressureDiff，一个条件扩散框架，适配大规模预训练视频扩散主干。通过将丰富的世界知识先验与物理感知特征修正层相结合以注入语义约束，我们的方法有效推断出合理的接触模式并解决了视觉-物理歧义。大量实验表明，我们的方法在基准上取得了优越性能，并展现出对野外场景的鲁棒迁移能力。项目页面见https://egotactile.github.io/。

</details>


### [164] [Proposal Refinement for Few-Shot Object Detection](https://arxiv.org/abs/2606.09245)
*Yuan Zeng,Bin Song,Jie Guo,Yuwen Chen*

Main category: cs.CV

TL;DR: 本文针对小样本目标检测中区域提议在基类和新类之间分布不平衡的问题，提出了分阶段的提议优化方法，在不增加推理时间的情况下，在现有基准上提升了约1%~6%的性能，达到了新的最优水平。

<details>
  <summary>Details</summary>

Motivation: 现有小样本目标检测算法大多依赖小样本分类性能，但忽略了区域提议在基类和新类之间分布不平衡的问题，这限制了检测性能的提升。

Method: 提出分阶段的提议优化方法：(1) 在基类训练阶段，设计优化损失以增强模型对新类别的敏感性；(2) 在微调阶段，引入优化分支作为RPN的辅助分支，以生成更多新类别的提议。通过重新平衡提议分布来改善检测性能。

Result: 在现有基准上，所提方法相较于基线方法提升了约1%~6%的性能，且不增加任何推理时间，达到了小样本目标检测任务的最优水平。

Conclusion: 通过重新平衡提议分布，本文方法在不增加推理成本的前提下显著提升了小样本目标检测性能，建立了新的最优方法。

Original Abstract: Few-shot object detection has gained widely attention in recent years. Some excellent algorithms have been proposed to handle this task. However, most of these algorithms rely on the performance of few-shot classification. Unlike previous attempts, our work focuses on the problem of unbalanced distribution of region proposals between the novel classes and the base classes. In order to alleviate this unbalanced distribution, we propose the proposal refinement approach for different training phases. Specifically, refinement loss is designed for the base training phase to enhance sensitivity of the model to novel classes, and refinement branch is introduced as an auxiliary branch for RPN (Region Proposal Networks) to generate more novel proposals in the fine-tuning phase. By rebalancing the proposal distribution, the proposed approach outperforms the baselines methods by roughly 1\%$\sim$6\% on current benchmarks without increasing any inference time. Through extensive experiments, we prove that we establish a new state-of-the-art method for the few-shot object detection task.

中文摘要翻译: 小样本目标检测近年来获得了广泛关注。已有一些优秀算法被提出以处理该任务。然而，大多数这些算法依赖于小样本分类的性能。与以往尝试不同，本文聚焦于基类（base classes）与新类（novel classes）之间区域提议（region proposals）分布不平衡的问题。为缓解这种不平衡分布，我们针对不同训练阶段提出了提议优化方法（proposal refinement approach）。具体而言，在基类训练阶段设计了优化损失（refinement loss）以增强模型对新类别的敏感性；在微调阶段引入了优化分支（refinement branch）作为区域提议网络（Region Proposal Networks, RPN）的辅助分支，以生成更多新类别的提议。通过重新平衡提议分布，所提方法在现有基准上相较基线方法提升了约1%~6%，且不增加任何推理时间。大量实验证明，我们为小样本目标检测任务建立了一种新的最优方法。

</details>


### [165] [SOMA: From Surface Observations to Muscle Anatomy](https://arxiv.org/abs/2606.09246)
*Eduardo Alvarado,Emily Kim,Gerrit Nolte,Friedemann Runte,Mario Botsch,Marc Habermann,Christian Theobalt*

Main category: cs.CV

TL;DR: 提出SOMA和SKIM，从多视角RGB观测中推断肌肉形变，实现可扩展、低成本的解剖学动画生成。

<details>
  <summary>Details</summary>

Motivation: 现有参数化人体模型仅捕捉皮肤表面，无法反映内部的生物力学结构；传统软组织模拟（如FEM）精确但计算昂贵且不可扩展，而现有生物力学工具虽能模拟肌肉力但与外部形状变化脱节。因此需要一种能从表面观测（皮肤/姿态）反推肌肉形变的新方法。

Method: 提出SOMA（从表面观测到肌肉解剖）模型，从RGB相机采集的多视角表面信号中推断时空肌肉行为；同时构建SKIM数据集，用于软组织形变的受试者特异性建模。

Result: 该方法能生成基于解剖学的人体动画，无需传统仿真的高复杂度，具有可扩展性和低成本优势。

Conclusion: SOMA是首个从多视角RGB数据恢复肌肉形变的方法，为解剖学动画提供了可扩展、低成本的替代方案。

Original Abstract: With the growing demand for realistic virtual humans, parametric body models have become a cornerstone of modern medicine, sports, and entertainment applications. However, most of these models are inherently limited: they only capture the 3D surface of the skin, offering no insight into the complex bio-mechanical structures that generate motion. As more applications expand towards biomechanics, the need for virtual human models that go beyond the skin has become increasingly evident. Traditional soft-tissue simulations, such as FEM, are accurate but non-scalable and too computationally expensive for most common applications. Alternatively, existing biomechanical tools can simulate muscular forces and activations, but do not model changes in external shape, restricting how activations correlate with actual observable anatomy. This motivates a novel inverse research problem: recovering muscle deformations directly from visible surface observations - i.e., from the skin, and thus the pose. In this work, we present SOMA (from Surface Observations to Muscle Anatomy), a person-specific model that infers spatio-temporal muscle behavior from surface signals obtained using RGB cameras, and SKIM, a subject-specific soft-tissue deformation dataset. To the best of our knowledge, this is the first method that attempts to recover muscle deformations from multi-view RGB data. We show how our method provides anatomically grounded animations without the complexity of traditional simulations, leading to a scalable and cost-effective solution. Data and code are available.

中文摘要翻译: 随着对逼真虚拟人体需求的不断增长，参数化人体模型已成为现代医学、体育和娱乐应用的基石。然而，大多数此类模型存在固有局限：它们仅捕捉皮肤的三维表面，无法揭示产生运动的复杂生物力学结构。随着越来越多应用向生物力学领域扩展，对超越皮肤层面的虚拟人体模型的需求日益凸显。传统的软组织模拟方法（如有限元法）虽然精确，但不可扩展且计算成本过高，难以适用于大多数常见应用。另一方面，现有的生物力学工具可以模拟肌肉力和激活状态，但无法建模外部形状的变化，限制了激活状态与实际可观察解剖结构之间的关联。这促使我们提出一个新颖的逆研究问题：直接从可见的表面观测（即皮肤，进而从姿态）恢复肌肉形变。在本工作中，我们提出了SOMA（从表面观测到肌肉解剖），一个从RGB相机获取的表面信号推断时空肌肉行为的受试者特异性模型，以及SKIM，一个受试者特异性的软组织形变数据集。据我们所知，这是首个尝试从多视角RGB数据恢复肌肉形变的方法。我们展示了该方法如何在无需传统仿真复杂性的情况下提供基于解剖学的动画，从而提供一种可扩展且低成本的解决方案。数据和代码已公开。

</details>


### [166] [Temporal-Aware Reasoning Optimization for Video Temporal Grounding](https://arxiv.org/abs/2606.09248)
*Minghang Zheng,Zihao Yin,Yi Yang,Yuxin Peng,Yang Liu*

Main category: cs.CV

TL;DR: TaRO提出了一种时序感知推理优化框架，通过构造性推理探索和时序敏感奖励来增强多模态大模型在视频时序定位中的推理质量，取得了SOTA性能。

<details>
  <summary>Details</summary>

Motivation: 现有MLLM在视频时序定位中常产生浅层推理，对精确定位帮助有限。问题源于：(1)低效的随机探索；(2)奖励函数只关注答案正确性而忽略推理质量。

Method: (1)构造性推理探索：利用预生成的密集描述构建基于显式视觉线索和时间戳的推理路径；(2)时序敏感奖励：通过扰动推理路径中的事件边界，观察logit下降程度来评判推理质量；(3)渐进式课程学习：先用奖励筛选构造的推理路径，再过渡到自由探索阶段。

Result: 在视频时序定位（VTG）基准上取得了最先进的性能。

Conclusion: 通过显式引入时间感知的推理优化，可以显著提升MLLM在时序定位任务上的表现。

Original Abstract: Multi-modal Large Language Models (MLLMs) have achieved remarkable progress in video temporal grounding with reinforcement learning for generating reasoning paths. However, existing models often produce superficial reasoning, which offers limited guidance for precise temporal localization. This limitation stems from (1) inefficient random exploration and (2) reward functions that focus solely on the answer correctness while ignoring reasoning quality. To address these issues, we propose TaRO (Temporal-Aware Reasoning Optimization), a framework that explicitly enhances the model's ability of thinking with time. First, we introduce a Constructive Reasoning Exploration that leverages pre-generated dense captions to construct reasoning paths grounded in explicit visual cues and timestamps, enabling efficient exploration of high-quality time-aware reasoning. Second, to evaluate reasoning quality, we design a Temporal-Sensitivity Reward. High-quality reasoning should be anchored to specific events and timestamps. If the event boundary under thinking is disrupted, such reasoning should become invalid, leading to a drop in the logit of the reasoning path. We utilize this drop as a critique of reasoning quality. Finally, TaRO follows a progressive curriculum, which starts by utilizing this reward to select better constructed reasoning paths, and evolves to a free exploration phase where the model autonomously generates effective reasoning. Experiments demonstrate that TaRO achieves state-of-the-art performance on VTG benchmarks. Code is available at https://github.com/oceanflowlab/TaRO.

中文摘要翻译: 多模态大语言模型（MLLMs）在结合强化学习生成推理路径的视频时序定位方面取得了显著进展。然而，现有模型常产生浅层推理，对精确定位仅提供有限指导。这一局限源于：(1)低效的随机探索；(2)仅关注答案正确性而忽略推理质量的奖励函数。为解决这些问题，我们提出TaRO（时序感知推理优化），一个显式增强模型时间思考能力的框架。首先，我们引入构造性推理探索，利用预生成的密集描述构建基于显式视觉线索和时间戳的推理路径，从而高效探索高质量的时间感知推理。其次，为评估推理质量，我们设计了时序敏感奖励。高质量的推理应锚定于特定事件和时间戳；若推理中的事件边界被扰动，该推理应失效，导致推理路径的logit下降。我们利用这一下降作为推理质量的评判标准。最后，TaRO采用渐进式课程学习，先利用该奖励筛选构造的推理路径，再过渡到模型自主生成有效推理的自由探索阶段。实验表明，TaRO在视频时序定位基准上达到了最先进性能。代码已开源：https://github.com/oceanflowlab/TaRO。

</details>


### [167] [MAGIS: Evidence-Based Multi-Agent Reasoning for Interpretable Strabismus Clinical Decision-Making](https://arxiv.org/abs/2606.09249)
*Xikai Tang,Yifan Wang,Jiafan Zhuang,Li Luo,Jinming Guo,Xiaoling Xie,Jiacheng Liu,Peiwei Wei,Lihao Zhong,Xiaoli Kang,Jie Cen,Guangqiang Yin,Kunliang Qiu,Ce Zheng,Zhun Fan*

Main category: cs.CV

TL;DR: 提出MAGIS框架，通过多智能体推理和双重证据约束机制，将斜视诊断从黑箱端到端生成转变为结构化、可解释的循证诊断过程，在细粒度斜视基准上将加权F1从72.0%提升至91.3%，并显著提高诊断报告的临床可靠性。

<details>
  <summary>Details</summary>

Motivation: 现有深度学习方法主要提供黑箱诊断预测，缺乏透明推理；而大视觉语言模型虽能进行图像理解和报告生成，但在证据敏感且规则驱动的医学任务中极易产生幻觉。因此，亟需一种既能保持高诊断精度又具备可解释性和循证推理能力的斜视诊断系统。

Method: MAGIS框架包含三个核心机制：(1) Dual-Evidence Constrained Context (DECC)机制，联合组织九方位眼位照片的视觉证据与基于证据的临床诊断规则，构建受约束的推理上下文；(2) Evidence-Based Corrective Verification (EBCV)机制，验证当前诊断假设是否被视觉证据、热力图视觉线索和临床规则支持，检测到不一致时触发假设修正；(3) 结构化诊断流程：候选假设生成→双重证据约束上下文→循证纠正验证→报告生成。

Result: 在细粒度斜视诊断基准上，MAGIS的加权F1分数从72.0%提升至91.3%，显著优于其他最先进的诊断系统；同时大幅提升了生成诊断报告在一致性、对齐性和完整性方面的临床可靠性。

Conclusion: MAGIS为构建准确、循证且临床可解释的斜视诊断系统提供了有效解决方案，证明了将多智能体推理与双重证据约束相结合的框架可显著提升医学影像诊断的准确性和可解释性。

Original Abstract: Strabismus is a common ocular disorder that requires fine-grained subtype diagnosis for individualized treatment planning. However, existing deep learning methods mainly provide diagnostic predictions without transparent reasoning, while recent large vision-language models (LVLMs), although promising for joint image understanding and report generation, remain highly prone to hallucination in this evidence-sensitive and rule-driven medical task. To address these challenges, we propose MAGIS, an evidence-based Multi-AGent reasoning for Interpretable Strabismus diagnosis framework. MAGIS transforms black-box end-to-end generation into a structured diagnostic process consisting of candidate hypothesis generation, dual-evidence constrained context, evidence-based corrective verification, and report generation. Specifically, we introduce a Dual-Evidence Constrained Context (DECC) mechanism that jointly organizes visual evidence from the photograph of the nine cardinal positions of gaze and evidence-based clinical diagnostic rules into a constrained context for reliable diagnostic reasoning. We further develop an Evidence-Based Corrective Verification (EBCV) mechanism that verifies whether the current diagnostic hypothesis is supported by visual evidence, heatmap-based visual cues, and evidence-based clinical diagnostic rules. Hypothesis refinement is triggered when inconsistency is detected. Experiments on a fine-grained strabismus benchmark demonstrate that MAGIS not only significantly outperforms other state-of-the-art diagnostic systems, improving the weighted F1 score from 72.0% to 91.3%, but also substantially improves the clinical reliability (consistency, alignment, and completeness) of generated diagnostic reports. These results demonstrate that MAGIS provides an effective solution for building accurate, evidence-based, and clinically interpretable strabismus diagnosis systems.

中文摘要翻译: 斜视是一种常见的眼科疾病，需要细粒度的亚型诊断以制定个性化治疗方案。然而，现有的深度学习方法主要在缺乏透明推理的情况下提供诊断预测，而近期的大视觉语言模型尽管在联合图像理解和报告生成方面前景广阔，但在这种对证据敏感且规则驱动的医学任务中仍然极易产生幻觉。为解决这些挑战，我们提出MAGIS——一种基于证据的多智能体推理框架，用于可解释的斜视诊断。MAGIS将黑箱的端到端生成转变为结构化的诊断流程，包括候选假设生成、双重证据约束上下文、基于证据的纠正性验证以及报告生成。具体而言，我们引入了一种双重证据约束上下文机制，该机制将九方位注视眼位照片的视觉证据与基于证据的临床诊断规则联合组织成受约束的上下文，以实现可靠的诊断推理。我们进一步开发了基于证据的纠正性验证机制，该机制验证当前诊断假设是否被视觉证据、基于热力图的视觉线索以及基于证据的临床诊断规则所支持。当检测到不一致时，触发假设修正。在细粒度斜视基准上的实验表明，MAGIS不仅在诊断准确率上显著优于其他最先进的诊断系统（将加权F1分数从72.0%提升至91.3%），而且大幅提高了生成诊断报告的临床可靠性（包括一致性、对齐性和完整性）。这些结果表明，MAGIS为构建准确、循证且临床可解释的斜视诊断系统提供了有效的解决方案。

</details>


### [168] [LiteVSR: Lightweight Adaptation of Frozen Diffusion Transformers for Video Super-Resolution](https://arxiv.org/abs/2606.09250)
*Yu Cao,Ziquan Liu,Zhensong Zhang,Jiankang Deng,Shaogang Gong,Jifei Song*

Main category: cs.CV

TL;DR: 提出LiteVSR，一种极简框架，利用完全冻结的扩散变压器和轻量级状态感知适配器（仅11.25%可训练参数，12 GPU小时训练），通过流匹配将视频超分任务简化为学习固定注入模式，实现高质量且高效的单步采样VSR。

<details>
  <summary>Details</summary>

Motivation: 现有方法存在计算成本高（全微调）、偏离生成式公式（直接LQ→HQ映射）、或在现代扩散变压器下ControlNet风格适配器效率低下（因无编码器-解码器层级结构，需复制整个骨干网络）等问题。流匹配为解决跨域VSR适应提供了一种原则性替代方案。

Method: 基于流匹配的洞察（预测恒定速度场，将适应任务简化为学习固定注入模式），提出LiteVSR框架：采用完全冻结的扩散变压器，搭配轻量级状态感知适配器。适配器使用双流架构，分别从LQ输入提取静态结构线索、从中间去噪状态提取动态线索，通过时间依赖的交叉注意力对齐两者，实现随去噪过程从结构对齐到纹理细化的自适应过渡。

Result: 仅需11.25%可训练参数和单张A100上12 GPU小时训练，达到有竞争力的恢复质量，并支持低至单步的快速采样。

Conclusion: LiteVSR证明了利用完全冻结的扩散变压器进行高效视频超分的可行性，流匹配策略和状态感知适配器设计能够以极低的训练成本实现高质量的VSR效果。

Original Abstract: Adapting large-scale pre-trained video generators for Video Super-Resolution (VSR) in novel domains remains computationally prohibitive. Methods that reformulate generation as direct Low-Quality to High-Quality mappings deviate from the original generative formulation, demanding extensive fine-tuning. ControlNet-style adapters lose their efficiency under modern Diffusion Transformers since the absence of encoder-decoder hierarchy forces duplication of the entire backbone. We observe that flow matching offers a principled alternative for cross-domain VSR adaptation. By predicting a constant velocity field across all timesteps, the adaptation task reduces to learning a fixed injection pattern rather than time-varying transformations. Building on this insight, we propose LiteVSR, a minimalist framework that performs VSR using a completely frozen Diffusion Transformer with a lightweight State-Aware Adapter. The adapter employs a dual-stream architecture that extracts static structural cues from the LQ input and dynamic cues from intermediate denoising states, aligning them through time-dependent cross-attention to enable adaptive transition from structural alignment to texture refinement as denoising proceeds. LiteVSR achieves competitive restoration quality with only 11.25% trainable parameters and 12 GPU-hours of training on a single A100, while maintaining fast sampling (down to a single step) compatibility.

中文摘要翻译: 将大规模预训练视频生成器适配到新领域的视频超分（VSR）在计算上仍然非常昂贵。那些将生成任务重新表述为直接从低质量到高质量映射的方法偏离了原始的生成公式，需要大量的微调。ControlNet风格的适配器在现代扩散变压器下会失去效率，因为缺乏编码器-解码器层级结构，导致需要复制整个骨干网络。我们观察到，流匹配为跨域VSR适应提供了一种原则性的替代方案。通过预测所有时间步上的恒定速度场，适应任务被简化为学习一个固定的注入模式，而非随时间变化的变换。基于这一见解，我们提出了LiteVSR，一个极简框架，使用完全冻结的扩散变压器和轻量级状态感知适配器执行VSR。该适配器采用双流架构，从低质量输入中提取静态结构线索，从中间去噪状态中提取动态线索，通过时间依赖的交叉注意力对齐它们，从而实现随着去噪过程从结构对齐到纹理细化的自适应过渡。LiteVSR仅用11.25%的可训练参数和单张A100上12 GPU小时的训练就达到了有竞争力的恢复质量，同时保持了低至单步的快速采样兼容性。

</details>


### [169] [A practical probabilistic framework for deformable image registration uncertainty in radiotherapy dose propagation](https://arxiv.org/abs/2606.09253)
*Stefan Heldmann,Sven Kuckertz,Nasim Givehchi,Thomas Coradi,Mikel Byrne,Ben Archibald-Heeren,Nils Papenberg*

Main category: cs.CV

TL;DR: 本文提出了一种轻量级、可解释的概率框架，用于将可变形图像配准（DIR）的不确定性传播到放疗剂量评估中，生成剂量概率、期望剂量、置信区间和DVH包络等可解释指标，并通过前列腺癌病例验证了其有效性。

<details>
  <summary>Details</summary>

Motivation: 可变形图像配准（DIR）在放疗剂量传播与累积中广泛应用，但底层形变的不确定性会显著影响临床相关剂量估计，现有方法缺乏将这种不确定性透明地传播到剂量指标中的实用框架。

Method: 将每个体素的映射对应关系建模为随机变量，由局部确定性图（可基于安全边界、结构边界不匹配或保守不确定性值定义）控制；引入结构引导的入/出策略作为可选优化，将映射概率限制在解剖学合理的靶区；通过简单参数化和概率核实现轻量级、可解释的计算。

Result: 在前列腺癌病例研究中发现：确定性图的设计对剂量和DVH不确定性边界的影响显著强于概率核的选择；结构引导的入/出策略的附加收益因病例而异，在当前示例中效果有限。

Conclusion: 该框架提供了一种透明的方式将DIR不确定性纳入放疗剂量评估，并能研究建模选择如何影响传播后的剂量指标。

Original Abstract: Deformable image registration (DIR) is widely used in radiotherapy for dose propagation and accumulation, but uncertainty in the underlying deformation can substantially affect clinically relevant dose estimates. We present a practical probabilistic framework for propagating DIR uncertainty to voxel-wise dose statistics and dose-volume histograms (DVHs). The method models the mapped correspondence at each voxel as a random variable governed by a transparent local certainty map that can be defined by simple safety margins, structure-boundary mismatch, or structure-wise conservative uncertainty values. This yields interpretable quantities such as dose probabilities, expected dose, confidence bounds, and induced DVH envelopes.
  The framework is designed to remain lightweight and interpretable: it avoids complex biomechanical or ensemble-based uncertainty models and instead emphasizes simple parameterization, computational feasibility, and transparent dose metrics. We further introduce a structure-guided in/out strategy as an optional refinement that restricts mapping probabilities to anatomically plausible target regions. The approach is demonstrated on a prostate radiotherapy case study and used to compare different certainty-map strategies and probability kernels. The experiments show that the certainty-map design has a stronger effect on resulting dose and DVH uncertainty bounds than the specific kernel choice, while the additional benefit of the in/out strategy is case-dependent and modest in the present example. Overall, the proposed framework provides a transparent way to incorporate DIR uncertainty into radiotherapy dose assessment and to study how modelling choices affect propagated dose metrics.

中文摘要翻译: 可变形图像配准（DIR）广泛应用于放疗中的剂量传播与累积，但底层形变的不确定性会显著影响临床相关的剂量估计。本文提出一种实用的概率框架，用于将DIR不确定性传播到体素级剂量统计量和剂量-体积直方图（DVH）中。该方法将每个体素的映射对应关系建模为随机变量，由一个透明的局部确定性图控制，该确定性图可通过简单的安全边界、结构边界不匹配或结构级保守不确定性值来定义。该方法产生可解释的量化指标，如剂量概率、期望剂量、置信区间和诱导的DVH包络。该框架设计为轻量级且可解释：它避免复杂的生物力学或基于集成的模型，而是强调简单的参数化、计算可行性和透明的剂量指标。我们进一步引入一种结构引导的入/出策略作为可选优化，将映射概率限制在解剖学合理的靶区。该方法在前列腺癌放疗病例研究中得到验证，并用于比较不同的确定性图策略和概率核。实验表明，确定性图的设计对剂量和DVH不确定性边界的影响显著强于具体核的选择，而结构引导的入/出策略的附加收益因病例而异，在当前示例中效果有限。总体而言，该框架提供了一种透明的方式将DIR不确定性纳入放疗剂量评估，并研究建模选择如何影响传播后的剂量指标。

</details>


### [170] [Self-supervised Learning Matters: A Simple Ensemble Solution for Micro-Gesture Recognition](https://arxiv.org/abs/2606.09261)
*Tingyi Liu,Kun Li,Fei Wang,Junjie Chen,Zhiliang Wu,Jihao Gu,Haixu Liu,Dan Guo*

Main category: cs.CV

TL;DR: 本文提出了XInsight Lab在IJCAI 2026第四届MiGA挑战赛微手势分类赛道中获得第一名的方法——一种多模态集成框架，结合自监督RGB模型（在120K未标注片段上通过掩码视频建模预训练）与有监督多流模型，在iMiGUE测试集上达到74.419%的top-1准确率，超越此前最优方法1.206个百分点。

<details>
  <summary>Details</summary>

Motivation: 微手势识别面临标注数据稀缺的挑战，现有监督方法性能受限于标注规模。作者希望通过自监督学习从未标注的域内视频中学习可迁移的通用表示，以提升微手势识别的性能。

Method: 提出多模态集成框架，包含两个分支：(1) 自监督RGB模型，在120K未标注片段上通过掩码视频建模进行预训练，再在iMiGUE数据集上微调；(2) 来自先前解决方案的有监督多流模型。将自监督RGB模型作为互补分支与多流模型集成。

Result: 自监督RGB基线模型在iMiGUE测试集上达到69.224% top-1准确率；最终集成模型达到74.419% top-1准确率，超越此前最优方法1.206个百分点。

Conclusion: 自监督RGB表示学习对微手势识别任务有效，通过集成可进一步提升性能。消融实验验证了集成策略及自监督预训练的有效性。

Original Abstract: In this paper, we present XInsight Lab's solution to the micro-gesture classification track of the 4th MiGA Challenge at IJCAI 2026, in which our solution ranked first and achieved a new state-of-the-art result. We propose a multimodal ensemble framework that integrates a self-supervised RGB-based model with supervised multi-stream models from previous solutions. The self-supervised RGB model is pretrained on 120K unlabeled clips via masked video modeling and then fine-tuned on iMiGUE. This simple yet effective RGB baseline achieves 69.224% top-1 accuracy on the iMiGUE test set, demonstrating the benefit of learning transferable representations from unlabeled in-domain videos. By incorporating this model as a complementary branch, the final ensemble reaches 74.419% top-1 accuracy, surpassing the previous state of the art by 1.206 percentage points. Experimental results on iMiGUE, including ablation studies on the ensemble strategy, validate the effectiveness of self-supervised RGB representation learning for micro-gesture recognition.

中文摘要翻译: 本文介绍了XInsight Lab在IJCAI 2026第四届MiGA挑战赛微手势分类赛道中的解决方案，该方案获得第一名并取得了新的最优结果。我们提出了一种多模态集成框架，该框架集成了一个基于自监督的RGB模型与来自先前解决方案的有监督多流模型。自监督RGB模型通过掩码视频建模在120K未标注片段上预训练，然后在iMiGUE数据集上微调。这一简单而有效的RGB基线在iMiGUE测试集上达到了69.224%的top-1准确率，证明了从未标注的域内视频中学习可迁移表示的优势。通过将该模型作为互补分支引入，最终集成模型达到了74.419%的top-1准确率，超越此前最优方法1.206个百分点。在iMiGUE上的实验结果（包括关于集成策略的消融研究）验证了自监督RGB表示学习对微手势识别的有效性。

</details>


### [171] [See More, Match Better: Multi-Source Feature Fusion for Two-View Correspondence Learning](https://arxiv.org/abs/2606.09262)
*Xiaojie Li,Xin Jiang,Luanyuan Dai,Jinnan Yang,Yongdong Zhang,Zechao Li*

Main category: cs.CV

TL;DR: TriMatch是一种多源特征融合框架，通过联合提取几何、纹理语义和结构语义特征，并引入语义引导的对应调制模块，有效解决了重复结构、无纹理区域等场景中伪一致外点难以区分的问题，显著提升了两视图对应学习的鲁棒性和泛化能力。

<details>
  <summary>Details</summary>

Motivation: 现有基于坐标几何一致性的两视图对应学习方法在处理重复结构、无纹理区域或局部相似几何模式时，难以区分伪一致外点（即几何上看似一致但语义不符的对应点），亟需引入多源语义信息来增强对应关系的判别能力。

Method: TriMatch包含特征提取和特征精化两部分。特征提取阶段联合提取几何、纹理语义和结构语义特征，通过纹理-几何对齐和结构-几何对齐模块弥合语义与几何特征之间的差异，并引入语义引导的对应调制模块利用语义信息调制几何特征以抑制几何合理但语义不一致的对应。特征精化阶段采用层次化语义增强的对应精化策略，逐步建模对应依赖关系并重新校准多上下文特征响应。

Result: 大量实验表明TriMatch在有效性、鲁棒性和泛化能力方面均表现优异。

Conclusion: 通过融合几何特征与多源语义特征并进行语义引导的调制，TriMatch有效解决了伪一致外点问题，为两视图对应学习提供了一种更可靠的解决方案。

Original Abstract: Two-view correspondence learning aims to distinguish true correspondences (inliers) from false ones (outliers) in image pairs by leveraging their underlying differences. Existing methods mainly rely on coordinate-based geometric consistency. However, they often struggle with pseudo-consistent outliers in scenes containing repetitive structures, textureless regions, or locally similar geometric patterns. To address this limitation, we propose TriMatch, a multi-source feature fusion framework for two-view correspondence learning, which consists of two parts: feature extraction and feature refinement. In feature extraction, TriMatch jointly extracts geometric, texture semantic, and structural semantic features to provide complementary evidence for correspondence discrimination. To bridge the gap between semantic and geometric features, texture and structural semantic features are aligned with geometric features through dedicated Texture-Geometric Alignment and Structural-Geometric Alignment modules, respectively. We further introduce a Semantic-Guided Correspondence Modulation module, which modulates geometric features using semantic information to suppress geometrically plausible but semantically inconsistent correspondences. In feature refinement, a Hierarchical Semantic-Enhanced Correspondence Refinement strategy progressively models correspondence dependencies and recalibrates multi-context feature responses, enabling more reliable inlier-outlier discrimination. Extensive experiments demonstrate the effectiveness, robustness, and generalization capability of TriMatch.

中文摘要翻译: 两视图对应学习旨在通过利用图像对中潜在差异来区分真实对应（内点）与虚假对应（外点）。现有方法主要依赖于基于坐标的几何一致性，但在包含重复结构、无纹理区域或局部相似几何模式的场景中，它们往往难以处理伪一致的外点。为解决这一局限性，我们提出了TriMatch，一种用于两视图对应学习的多源特征融合框架，其包括两部分：特征提取和特征精化。在特征提取中，TriMatch联合提取几何、纹理语义和结构语义特征，为对应关系判别提供互补证据。为弥合语义特征与几何特征之间的差距，纹理和结构语义特征分别通过专用的纹理-几何对齐和结构-几何对齐模块与几何特征对齐。我们进一步引入了语义引导的对应调制模块，该模块利用语义信息调制几何特征，以抑制几何上合理但语义不一致的对应。在特征精化中，层次化语义增强的对应精化策略逐步建模对应依赖关系并重新校准多上下文特征响应，实现更可靠的内点-外点判别。大量实验证明了TriMatch的有效性、鲁棒性和泛化能力。

</details>


### [172] [EditSSC: Toward Editable Semantic Occupancy Scenes with Unconditional Diffusion Models](https://arxiv.org/abs/2606.09273)
*Fatima Balde,Raoul de Charette,Alexandre Boulch*

Main category: cs.CV

TL;DR: EditSSC 提出利用2D BEV表示和预训练的Stable Diffusion模型进行3D语义场景生成与编辑，无需复杂3D专用架构，并在SemanticKITTI上超越现有3D基线方法。

<details>
  <summary>Details</summary>

Motivation: 现有3D语义场景生成方法大多依赖复杂的3D专用架构（如triplane编码器和适配扩散网络），导致实现复杂且缺乏编辑能力。作者希望利用成熟的2D架构来简化和赋能3D场景生成与编辑。

Method: 将3D语义占用网格重塑为多通道BEV图像，使用Stable Diffusion的量化自编码器和UNet，在量化后的潜空间上进行扩散，利用码本中类别到编码的对应关系实现无需重训练的编辑（草图引导生成、修补、外推）。

Result: 在SemanticKITTI数据集上，EditSSC在无条件生成任务上超越所有现有的3D专用基线方法。

Conclusion: 成熟的2D架构（如BEV表示和Stable Diffusion）可以有效地迁移到3D场景生成与编辑任务中，在简化设计的同时获得更优的性能和灵活的编辑能力。

Original Abstract: 3D semantic scene generation is crucial for autonomous driving applications, yet most methods rely on complex 3D-specific architectures such as triplane encoders and adapted diffusion networks, limiting both their simplicity and their editing capabilities. We propose EditSSC, an editing-ready method for 3D semantic scene generation using 2D Bird's Eye View (BEV) representations and off-the-shelf latent diffusion network. Our approach reshapes 3D semantic occupancy grids into multi-channel BEV images and leverages the quantized autoencoder and UNet from Stable Diffusion with minimal modifications. We perform diffusion on the latents after quantization, which enables training-free editing capabilities. By exploiting class-to-code correspondences in the codebook, our method supports sketch-guided generation, inpainting, and outpainting without any retraining. On SemanticKITTI, EditSSC outperforms existing 3D-specific baselines on unconditional generation, demonstrating that well-established 2D architectures can be effectively repurposed for 3D scene generation and editing.

中文摘要翻译: 3D语义场景生成对于自动驾驶应用至关重要，然而大多数方法依赖于复杂的3D专用架构，如三平面编码器和适配的扩散网络，这限制了它们的简洁性和编辑能力。我们提出EditSSC，一种基于2D鸟瞰图（BEV）表示和现成潜扩散网络的、支持编辑的3D语义场景生成方法。我们的方法将3D语义占用网格重塑为多通道BEV图像，并利用Stable Diffusion的量化自编码器和UNet，仅需最小限度的修改。我们在量化后的潜变量上进行扩散，从而实现了无需训练的编辑能力。通过利用码本中类别与编码之间的对应关系，我们的方法支持草图引导生成、修补和外推，且无需任何重新训练。在SemanticKITTI数据集上，EditSSC在无条件生成任务上优于现有的3D专用基线方法，这表明成熟的2D架构可以有效地用于3D场景生成与编辑任务。

</details>


### [173] [Visual Para-Thinker++: A Single-Policy Multi-Agent Framework for Visual Reasoning](https://arxiv.org/abs/2606.09290)
*Haoran Xu,Hongyu Wang,Yifei Gao,Jiaze Li,Zizhao Tong,Xiaofeng Zhang,Xiaosong Yuan*

Main category: cs.CV

TL;DR: 提出Visual Para-Thinker++，一种单策略多智能体框架，通过共享MLLM策略实例化为角色条件代理（Main、Worker、Summary），实现并行视觉推理，在多个基准上超越单轨迹和并行基线。

<details>
  <summary>Details</summary>

Motivation: 视觉推理需要整合分布在不同区域、属性和关系中的证据，单链推理容易产生早期感知承诺和幻觉，因此需要一种能够并行推理并协调多视角证据的方法。

Method: 采用单策略多智能体框架：Main Agent按固定分配模式分解任务；Worker Agents在上下文隔离下并行推理；Summary Agent整合完整推理轨迹而非多数投票。通过多智能体能力注入和角色解耦多智能体优化训练共享策略，为不同角色分配专属奖励和优势以减小梯度冲突。原生推理引擎通过共享视觉前缀和KV缓存复用实现高效多智能体部署。

Result: 在V*、CountBench、RefCOCO系列和HallusionBench上，Visual Para-Thinker++始终优于单轨迹和推理时并行基线，在幻觉敏感的视觉推理任务上提升尤为显著。

Conclusion: 所提出的并行多智能体推理框架能有效缓解早期感知承诺和幻觉问题，在视觉推理任务上取得一致且显著的性能提升。

Original Abstract: Visual reasoning requires integrating evidence distributed across regions, attributes, and relations, making single-chain reasoning prone to early perceptual commitment and hallucination. We propose Visual Para-Thinker++, a single-policy multi-agent framework in which one shared MLLM policy is instantiated as role-conditioned Main, Worker, and Summary Agents. The Main Agent decomposes the task with fixed allocation patterns; Worker Agents reason in parallel under context isolation; and the Summary Agent reconciles full Worker reasoning traces rather than majority-voting on final labels. The shared policy is trained by Multi-Agent Capability Injection and Role-Decoupled Multi-Agent Optimization, which assign role-specific rewards and advantages to corresponding token segments to reduce gradient conflict among collaborative roles. A native inference engine enables efficient multi-agent rollout through shared visual prefix and KV cache reuse. Across V*, CountBench, the RefCOCO family, and HallusionBench, Visual Para-Thinker++ consistently outperforms single-trajectory and inference-time parallel baselines, with especially strong gains on hallucination-sensitive visual reasoning.

中文摘要翻译: 视觉推理需要整合分布在不同区域、属性和关系中的证据，这使得单链推理容易产生早期感知承诺和幻觉。我们提出Visual Para-Thinker++，一种单策略多智能体框架，其中共享的多模态大语言模型(MLLM)策略被实例化为带有角色条件的Main、Worker和Summary智能体。Main Agent按照固定分配模式分解任务；Worker Agents在上下文隔离下并行推理；Summary Agent整合完整的Worker推理轨迹，而非对最终标签进行多数投票。该共享策略通过多智能体能力注入和角色解耦多智能体优化进行训练，为对应的token片段分配角色特定的奖励和优势，以减少协作角色间的梯度冲突。原生推理引擎通过共享视觉前缀和KV缓存复用实现高效的多智能体部署。在V*、CountBench、RefCOCO系列和HallusionBench上，Visual Para-Thinker++始终优于单轨迹和推理时并行基线，在幻觉敏感的视觉推理任务上尤其取得了显著提升。

</details>


### [174] [Virtual-point-based Solutions to Handle Generalized Absolute Pose Problem](https://arxiv.org/abs/2606.09294)
*Bin Li,Banglei Guan,Shunkun Liang,Yang Shang*

Main category: cs.CV

TL;DR: 本文提出了一种虚拟点公式，将标准PnP与广义位姿问题统一，并由此推导出三种广义位姿求解器（VGPc、VGPq、VGPr），分别在精度、全局最优性和计算效率上超越现有方法。

<details>
  <summary>Details</summary>

Motivation: 现有PnP求解器无法处理多相机系统中的多个投影中心，而广义位姿求解器存在效率或精度不足的问题，亟需一种能够统一标准PnP和广义位姿问题的框架。

Method: 提出虚拟点公式，将标准PnP与广义位姿问题进行统一；基于此框架，利用Cayley参数化、四元数参数化和旋转矩阵参数化，分别推导出VGPc、VGPq和VGPr三种求解器。

Result: VGPc在异方差噪声条件下估计精度更高；VGPq保持全局最优性；VGPr在精度不下降的前提下提供更优的计算效率。三种求解器均显著优于现有广义求解器，同时继承了原始PnP算法的精度和效率。

Conclusion: 所提出的虚拟点公式有效桥接了标准PnP与广义位姿问题，所推导的三种求解器在不同需求场景下均展现出优越性能，为多相机系统的位姿估计提供了统一高效的解决方案。

Original Abstract: Multi-camera systems are increasingly adopted in robotics and autonomous navigation for their wide field of view, flexibility, and fault tolerance. Nevertheless, existing PnP solvers fail to handle multiple projection centers. This paper introduces a virtual point formulation that bridges the standard PnP and generalized pose problems, enabling a unified pipeline that transforms existing PnP solvers into generalized pose solvers. Based on this framework, we derive three Virtual-point-based Generalized Pose solvers, namely VGPc, VGPq, and VGPr, leveraging Cayley, quaternion, and rotation-matrix parameterizations, respectively. Extensive experiments demonstrate that the proposed solvers inherit the accuracy and efficiency of original PnP algorithms while significantly outperforming existing generalized solvers. Specifically, VGPc achieves higher estimation accuracy under heteroscedastic noise conditions, VGPq maintains global optimality, whereas VGPr provides superior computational efficiency without accuracy degradation.

中文摘要翻译: 多相机系统因其宽视场、灵活性和容错性，在机器人和自主导航领域得到日益广泛的应用。然而，现有的PnP求解器无法处理多个投影中心。本文提出一种虚拟点公式，桥接了标准PnP与广义位姿问题，从而构建了一条统一流程，可将现有PnP求解器转化为广义位姿求解器。基于此框架，我们利用Cayley参数化、四元数参数化和旋转矩阵参数化，分别推导了三种基于虚拟点的广义位姿求解器——VGPc、VGPq和VGPr。大量实验表明，所提出的求解器继承了原始PnP算法的精度和效率，同时显著优于现有的广义求解器。具体而言，VGPc在异方差噪声条件下具有更高的估计精度，VGPq保持全局最优性，而VGPr在不损失精度的情况下提供了更优的计算效率。

</details>


### [175] [Reason Twice: Segmentation via Candidate Discovery and Comparative Reasoning](https://arxiv.org/abs/2606.09303)
*Xinyan Gao,Haoran Hao,Xiangyu Yue*

Main category: cs.CV

TL;DR: 提出Rea2Seg两阶段框架，将图像分割转化为候选掩码发现+判别性掩码选择；同时构建ReasonSeg-SGDR基准，全面评估多维度推理能力。

<details>
  <summary>Details</summary>

Motivation: 现有基于多模态大语言模型的分割方法受限于训练数据不足以及MLLM与掩码生成模块之间存在差距，难以充分发挥MLLM的感知与推理能力。

Method: 两阶段框架Rea2Seg：第一阶段基于分割MLLM的注意力图识别潜在区域作为候选掩码；第二阶段使用MLLM对问题和候选掩码进行推理并打分，通过重排序选择最高分掩码作为最终结果，将分割重构为候选发现+判别性选择。

Result: 在提出的ReasonSeg-SGDR基准和ReasonSeg上的实验验证了统一掩码生成与选择框架的有效性。

Conclusion: Rea2Seg通过解耦掩码生成与推理选择，有效弥合了MLLM与掩码生成模块之间的差距，提升了复杂推理分割的性能。

Original Abstract: The rapid development of pretrained foundation models has enabled more general image segmentation. Multimodal large language models (MLLMs) have been widely explored for image segmentation with complex queries that require high-level reasoning. Despite promising progress, existing methods are often constrained by limited training data and the gap between MLLMs and mask generation modules. To better transfer MLLMs' perception and reasoning ability to complex reasoning-based segmentation tasks, we propose a two-stage framework Rea2Seg for mask generation and selection. Specifically, the framework first identifies potential regions as candidate masks based on the attention maps of a segmentation MLLM. It then employs an MLLM to reason over the question and candidate masks and assign scores to each mask. The final segmentation result is obtained by reranking the candidates and selecting the highest-scoring mask, reformulating image segmentation as candidate discovery followed by discriminative mask selection.
  We also notice that a large portion of questions in existing benchmarks focus on commonsense reasoning, and these questions usually do not fully require joint visual observation and reasoning. To address this issue, we introduce a new benchmark called ReasonSeg-SGDR that comprehensively evaluates a model's perception, grounding, and reasoning abilities across multiple dimensions, including discriminative recognition, spatial reasoning, geometric reasoning, and multi-step reasoning, with fine-grained mask generation.
  In addition, we collect training data to enhance MLLMs' ability to jointly understand multimodal queries and candidate masks, and to assign scores through reasoning. Experimental results on the proposed benchmark and ReasonSeg demonstrate the effectiveness of the unified mask generation and selection framework.

中文摘要翻译: 预训练基础模型的快速发展使得更通用的图像分割成为可能。多模态大语言模型（MLLMs）已被广泛探索用于需要高级推理的复杂查询图像分割任务。尽管取得了令人鼓舞的进展，现有方法往往受限于训练数据不足以及MLLM与掩码生成模块之间的差距。为了更好地将MLLM的感知和推理能力迁移到基于复杂推理的分割任务中，我们提出了一个两阶段框架Rea2Seg，用于掩码生成与选择。具体来说，该框架首先基于分割MLLM的注意力图识别潜在区域作为候选掩码；然后利用MLLM对问题和候选掩码进行推理，并为每个掩码分配分数；最终通过重排序候选掩码并选择最高分掩码作为分割结果，将图像分割重构为候选发现后接判别性掩码选择。我们还注意到，现有基准中的大部分问题集中于常识推理，这些问题通常不需要完整的联合视觉观察与推理。为解决此问题，我们引入了一个新基准ReasonSeg-SGDR，从判别性识别、空间推理、几何推理和多步推理等多个维度全面评估模型的感知、定位和推理能力，并生成细粒度掩码。此外，我们收集训练数据以增强MLLM联合理解多模态查询与候选掩码、并通过推理分配分数的能力。在提出的基准和ReasonSeg上的实验结果表明，统一的掩码生成与选择框架是有效的。

</details>


### [176] [IB-HFN: Information Bottleneck-Driven SAR-Optical Fusion Network for High-Fidelity Cloud Removal](https://arxiv.org/abs/2606.09347)
*Haojun Guo,Fan Feng,Ziquan Wang*

Main category: cs.CV

TL;DR: 本文提出了一种信息瓶颈驱动的高保真网络（IB-HFN），用于SAR辅助的光学云去除，通过双流骨干网络、空间信息瓶颈融合模块和联合优化策略，有效抑制SAR散斑噪声并保留光学纹理细节，在SEN12MS-CR数据集上取得了优于现有方法的结构保持和光谱保真度。

<details>
  <summary>Details</summary>

Motivation: 现有SAR辅助光学云去除的多模态融合方法通常依赖直接空间拼接和像素级监督，这会导致SAR散斑噪声传播到光学重建中并产生过度平滑的结果。为克服这些局限性，本文提出了IB-HFN方法。

Method: IB-HFN采用双流骨干网络在深度融合前保留各模态特有表示，避免早期跨模态污染；融合阶段引入空间信息瓶颈融合模块，通过通道级变分信息瓶颈压缩SAR特征以抑制非结构化散斑噪声；同时采用局部-全局门控机制预测晴空区域并通过Dirac初始化的跳跃连接传递可靠光学细节，实现噪声抑制与纹理保持的解耦；最后采用联合优化策略整合特征级瓶颈正则化与图像级约束（重构精度、结构一致性、光谱保真度和对比锐度），并通过动态权重调度稳定训练。

Result: 在SEN12MS-CR数据集上，在具有挑战性的时空划分设置下，IB-HFN在结构保持和光谱保真度方面优于现有方法。

Conclusion: 本文提出的信息瓶颈驱动方法能有效解决SAR散斑噪声传播和过度平滑问题，实现高质量的光学云去除重建。

Original Abstract: Synthetic aperture radar (SAR)-assisted optical cloud removal aims to recover surface information obscured by clouds in optical remote sensing images by exploiting complementary SAR observations. Existing multimodal fusion methods typically rely on direct spatial concatenation and pixel-wise supervision, which can propagate SAR speckle noise into optical reconstruction and lead to over-smoothed results. To address these limitations, we propose an Information Bottleneck-driven High-Fidelity Network (IB-HFN) for SAR-assisted optical cloud removal. IB-HFN employs a dual-stream backbone to preserve modality-specific representations before deep semantic fusion, thereby mitigating premature cross-modal contamination. At the fusion stage, we introduce a Spatial Information Bottleneck Fusion module that compresses SAR features through a channel-wise variational information bottleneck to suppress unstructured speckle noise. In parallel, a local-global gating mechanism predicts clear-sky regions and routes reliable optical details through a Dirac-initialized skip connection, decoupling noise suppression from texture preservation. We further develop a joint optimization strategy that integrates feature-level bottleneck regularization with image-level constraints on reconstruction accuracy, structural consistency, spectral fidelity, and contrastive sharpness. A dynamic weighting schedule balances these objectives to stabilize training and reduce hazy artifacts. Experiments on the SEN12MS-CR dataset under challenging spatio-temporal splits demonstrate that IB-HFN achieves superior structural preservation and spectral fidelity over existing methods.

中文摘要翻译: 合成孔径雷达（SAR）辅助的光学云去除旨在利用互补的SAR观测来恢复光学遥感图像中被云层遮挡的地表信息。现有多模态融合方法通常依赖直接空间拼接和像素级监督，这会将SAR散斑噪声传播到光学重建中并导致过度平滑的结果。为解决这些局限性，我们提出了一种信息瓶颈驱动的高保真网络（IB-HFN），用于SAR辅助的光学云去除。IB-HFN采用双流骨干网络，在深度语义融合前保留各模态特有的表示，从而减轻过早的跨模态污染。在融合阶段，我们引入了空间信息瓶颈融合模块，通过通道级变分信息瓶颈压缩SAR特征以抑制非结构化散斑噪声。同时，局部-全局门控机制预测晴空区域，并通过Dirac初始化的跳跃连接传递可靠的光学细节，实现噪声抑制与纹理保持的解耦。我们进一步开发了一种联合优化策略，整合了特征级瓶颈正则化与图像级约束，包括重构精度、结构一致性、光谱保真度和对比锐度。动态权重调度平衡这些目标以稳定训练并减少雾状伪影。在SEN12MS-CR数据集上具有挑战性的时空划分下的实验表明，IB-HFN在结构保持和光谱保真度方面优于现有方法。

</details>


### [177] [Beyond Humans: Multispecies Animal Face Recognition Using Transfer Learning](https://arxiv.org/abs/2606.09353)
*Maria De Marsico,Anil K. Jain,Annalaura Miglino*

Main category: cs.CV

TL;DR: 本文探索使用预训练模型（FaceNet和Vision Transformer）进行迁移学习，在三种动物面部数据集（狗、灵长类、牛）上实现个体识别，验证了该方法在非侵入式动物识别中的可行性，其中ViT在狗数据集上达到最佳性能（验证准确率96.85%），在牛数据集上优于现有技术。

<details>
  <summary>Details</summary>

Motivation: 传统动物个体识别依赖微芯片等物理设备，存在操作不便、侵入性强等问题。远程面部识别可以非侵入、远距离工作且难以伪造，但现有动物面部数据集规模不足以训练当前深度学习架构，因此需要探索迁移学习的可能性。

Method: 采用迁移学习方法，利用预训练网络模型作为骨干网络。对比了两种预训练模型：专门在人脸大数据上训练的FaceNet，以及在ImageNet（物体类别）上预训练的Vision Transformer（ViT）。在三个差异显著的动物面部数据集（狗、灵长类、牛）上进行实验评估。

Result: 狗数据集表现最佳：ViT达到96.85%的平均验证准确率和84.34%的Rank-1识别率。灵长类数据集结果尚可，但性能因动物类别和任务不同而异，并非始终优于现有技术。牛数据集上ViT结果优于现有技术，FaceNet仍具竞争力。

Conclusion: 迁移学习结合预训练模型在动物面部识别中具有可行性，ViT在多数场景下表现优异，但性能受数据集质量和动物类别影响，并非在所有任务中都能超越针对特定任务训练的深度网络。

Original Abstract: Individual animal recognition can be useful in the search for lost or stolen pets, the tracking of individuals of endangered species, and the recognition of animals in crowded farms. Present recognition techniques mostly use physical devices, e.g., microchips, often impractical and difficult to apply. These could be replaced by remote recognition via the animal's face; if accurate enough, it provides several advantages: it is non-invasive, can work at a distance, and is difficult to counterfeit, as, for instance, in the case of substituting sick animals for healthy ones in the food industry. The few existing datasets with sufficient per-subject images annotated with a single animal identity are not large enough to train current deep learning architectures. We rather investigate the possibility of transfer learning, exploiting pre-trained network models as backbones. Our experiments compared FaceNet, which is specifically trained on large databases of human faces, with the Vision Transformer (ViT) pre-trained on ImageNet, i.e., on object categories. We used three face datasets of very different animals: dogs, primates (lemurs, golden monkeys, and chimpanzees), and cattle. We report the results and, for each dataset, compare them with the state of the art (SOTA) ad hoc-trained deep networks. The capture conditions differ among the three datasets. Image quality (resolution, motion blur, diverse poses, etc.) decreases from dogs to cattle to primates. The best performance was achieved with dogs, where ViT reached a mean verification accuracy of 96.85% and a Rank-1 Identification Rate of 84.34%. The results for endangered primates are still encouraging, but performance varies across animal classes and tasks (verification or identification), and does not always outperform SOTA. For cattle, the ViT results outperform SOTA, while FaceNet is still competitive.

中文摘要翻译: 个体动物识别可用于寻找丢失或被盗的宠物、追踪濒危物种个体以及识别拥挤农场中的动物。当前识别技术多采用物理设备（如微芯片），通常不切实际且难以应用。这些方法可以被通过动物面部进行的远程识别所取代；如果足够精确，该方法具有多种优势：非侵入性、可远距离工作且难以伪造——例如在食品行业中替换病畜为健康畜的情况。现有少数包含足够每张图像标注单一动物身份的数据集，其规模不足以训练当前的深度学习架构。我们转而研究迁移学习的可能性，利用预训练网络模型作为骨干网络。我们的实验比较了在大规模人脸数据库上专门训练的FaceNet和在ImageNet（物体类别）上预训练的Vision Transformer（ViT）。我们使用了三种差异显著的动物面部数据集：狗、灵长类（狐猴、金丝猴和黑猩猩）以及牛。我们报告了实验结果，并将每种数据集的结果与当前最优的专门训练深度网络进行了比较。三个数据集的拍摄条件各不相同。图像质量（分辨率、运动模糊、姿态多样性等）从狗到牛再到灵长类依次下降。狗数据集上取得了最佳性能，ViT达到了96.85%的平均验证准确率和84.34%的Rank-1识别率。濒危灵长类的结果仍令人鼓舞，但性能因动物类别和任务（验证或识别）而异，且并非始终优于现有技术。对于牛数据集，ViT结果优于现有技术，而FaceNet仍具有竞争力。

</details>


### [178] [ExDet: Open-Domain Open-Vocabulary Detection with Cross-modal Extrapolation and Rectification](https://arxiv.org/abs/2606.09360)
*Yupeng Zhang,Yuzhong Feng,Ruize Han,Zhiwei Chen,Wei Feng,Liang Wan*

Main category: cs.CV

TL;DR: ExDet 提出了一种轻量级的类别-域协同泛化框架，通过文本引导外推（TGE）、检测器兼容修正（DCR）和 ExRPN 模块，在不重新训练检测器的情况下实现开放域开放词汇检测（ODOVD）的跨类别和跨域泛化，取得了 SOTA 性能。

<details>
  <summary>Details</summary>

Motivation: 现有开放域开放词汇检测方法通常从头训练检测器与域泛化模块，训练成本高昂。因此，本文旨在设计一种轻量级框架，在不重新训练检测器的情况下增强其跨类别和跨域泛化能力。

Method: ExDet 包含三个核心模块：(1) Text-Guided Extrapolation (TGE)：利用视觉-语言模型的 DeltaSpace 特性，从文本中推断类别感知和域感知的代理视觉原型；(2) Detector-Compatible Rectification (DCR)：以无检测器训练、无真实数据的方式从 TGE 生成的原型中学习，推理时插入分类头之后，将表征修正到检测器兼容的源域视觉分布，提升对未见类别和未见域目标的分类；(3) ExRPN：结合语义相似度与 RPN 置信度重新校准提议分数，提升对新颖和域偏移目标的召回率。

Result: ExDet 在 OD-LVIS、OV-LVIS、Objects365 和 MSOSB 四个基准上均取得了 SOTA 性能。

Conclusion: ExDet 通过轻量级的类别-域协同泛化框架，有效解决了开放域开放词汇检测问题，在不增加训练成本的前提下实现了跨类别和跨域的强泛化能力，验证了利用 VLM DeltaSpace 特性和轻量修正模块的可行性。

Original Abstract: Open-domain open-vocabulary detection (ODOVD) requires detectors to generalize to both novel categories and unseen domains, making it more challenging than open-vocabulary detection. Existing methods typically train open-vocabulary detectors together with domain generalization modules from scratch, leading to high training cost. we propose ExDet, a lightweight category-domain collaborative generalization framework for ODOVD that enhances the cross-category and cross-domain generalization of existing detectors. ExDet consists of Text-Guided Extrapolation (TGE), a lightweight Detector-Compatible Rectification (DCR) module, and ExRPN. Specifically, TGE exploits the DeltaSpace property of vision-language models (VLMs) to infer category- and domain-aware proxy visual prototypes from text. DCR is learned from the TGE-generated prototypes in a detector training-free and real-data-free manner, and is inserted after the classification head at inference to rectify representations toward a detector-compatible source-domain visual distribution, thereby enhancing classification for targets from novel categories and unseen domains. ExRPN recalibrates proposal scores by combining semantic similarity with RPN confidence, improving recall for novel and domain-shifted objects while providing better support for subsequent classification and DCR. ExDet achieves SOTA performance on OD-LVIS, OV-LVIS, Objects365, and MSOSB.

中文摘要翻译: 开放域开放词汇检测（ODOVD）要求检测器能够泛化到未见类别和未见领域，这使其比开放词汇检测更具挑战性。现有方法通常从头训练开放词汇检测器与域泛化模块，导致训练成本高昂。本文提出 ExDet，一种用于 ODOVD 的轻量级类别-域协同泛化框架，能够增强现有检测器的跨类别和跨域泛化能力。ExDet 包含文本引导外推（TGE）、轻量级检测器兼容修正（DCR）模块以及 ExRPN。具体而言，TGE 利用视觉-语言模型（VLM）的 DeltaSpace 特性，从文本中推断类别感知和域感知的代理视觉原型。DCR 以无检测器训练、无真实数据的方式从 TGE 生成的原型中学习，并在推理时插入分类头之后，将表征修正到检测器兼容的源域视觉分布，从而提升对来自未见类别和未见域目标的分类性能。ExRPN 通过结合语义相似度与 RPN 置信度来重新校准提议分数，提高了对新颖和域偏移目标的召回率，同时为后续分类和 DCR 提供更好的支持。ExDet 在 OD-LVIS、OV-LVIS、Objects365 和 MSOSB 上取得了 SOTA 性能。

</details>


### [179] [Zero-Shot Semantic Re-Identification for Autonomous Driving: A VLM Baseline Study](https://arxiv.org/abs/2606.09362)
*Eduardo Borges,Manuel Abreu,Luís Garrote,Urbano J. Nunes*

Main category: cs.CV

TL;DR: 提出了一种基于视觉-语言模型（VLM）的零样本流程，将自动驾驶中的目标重识别（ReID）问题转化为语义文本描述匹配任务，利用类别、颜色、形状、姿态等结构化属性替代传统视觉特征，实现可解释且性能接近监督CNN基线的重识别，同时揭示了跨视角属性不一致和细粒度区分不足等挑战。

<details>
  <summary>Details</summary>

Motivation: 纯视觉表示对视角、遮挡、光照和传感器域变化敏感，在复杂驾驶场景中可解释性和鲁棒性受限，因此探索利用VLMs生成语义文本描述来支持身份匹配，以提升重识别的可解释性和鲁棒性。

Method: 提出一个零样本流程，利用视觉-语言模型（VLMs）对检测到的交通参与者生成结构化语义文本描述（包括类别、颜色、形状、姿态、可见部分、空间上下文和显著视觉线索），并基于这些描述进行身份匹配（重识别），替代传统的基于低层视觉相似性的方法。

Result: 零样本语义描述能够支持有效的目标重识别，检索性能与有监督CNN基线相当，同时通过显式身份线索提供了更强的可解释性；但也暴露出跨视角属性不一致以及视觉相似实例之间细粒度区分能力有限的挑战。

Conclusion: 语言-based重识别在自动驾驶场景中具有潜力，零样本语义描述可作为有效且可解释的替代方案，但当前VLM在属性一致性和细粒度判别上仍存在不足，需进一步改进。

Original Abstract: Re-Identification (ReID) in autonomous driving is typically formulated as a visual matching problem, where observations of vehicles, pedestrians, and cyclists are associated across time, frames, or camera views using learned appearance embeddings, often complemented by motion, geometric, or multimodal cues. However, purely visual representations may be sensitive to viewpoint, occlusion, illumination, and sensor-domain variations, limiting their interpretability and robustness in complex driving scenes. We propose a baseline study of a zero-shot pipeline using Vision-Language Models (VLMs) to generate textual descriptions of detected traffic participants and evaluate whether these descriptions can support identity matching across observations. Instead of relying only on low-level visual similarity, the proposed formulation represents each object through structured semantic attributes, including category, color, shape, pose, visible parts, spatial context, and distinctive visual cues. This study provides an initial benchmark for language-based re-identification in autonomous-driving scenarios, discussing and evaluating the strengths and limitations of current VLMs for this task. Results demonstrate that zero-shot semantic descriptions can support effective object re-identification, achieving retrieval performance comparable to a supervised CNN baseline while offering greater interpretability through explicit identity cues. However, the experiments also reveal important challenges, including attribute inconsistency across viewpoints and limited fine-grained discrimination between visually similar instances.

中文摘要翻译: 自动驾驶中的重识别（ReID）通常被表述为一个视觉匹配问题，即利用学习到的外观嵌入（常辅以运动、几何或多模态线索），在时间、帧或相机视角之间关联车辆、行人及骑行者的观测。然而，纯视觉表示可能对视角、遮挡、光照和传感器域变化敏感，限制了其在复杂驾驶场景中的可解释性和鲁棒性。我们提出了一项基线研究，采用基于视觉-语言模型（VLM）的零样本流程，为检测到的交通参与者生成文本描述，并评估这些描述能否支持跨观测的身份匹配。该公式不依赖低层视觉相似性，而是通过结构化语义属性来表示每个目标，包括类别、颜色、形状、姿态、可见部分、空间上下文以及显著视觉线索。本研究为自动驾驶场景下基于语言的重识别提供了一个初始基准，讨论并评估了当前VLM在该任务上的优势与局限。结果表明，零样本语义描述能够支持有效的目标重识别，其检索性能与有监督CNN基线相当，同时通过显式身份线索提供了更强的可解释性。然而，实验也揭示了重要挑战，包括跨视角的属性不一致性以及视觉相似实例之间有限的细粒度区分能力。

</details>


### [180] [RT-SDGOD: Real-Time Single-Domain Generalized Object Detection](https://arxiv.org/abs/2606.09367)
*Yupeng Zhang,Fangzhuo Gao,Ruize Han,Wei Feng,Liang Wan*

Main category: cs.CV

TL;DR: 提出了RT-SDGOD问题设定和RT-SDGDet方法，通过训练时多查询协作建模来增强实时目标检测器在零额外推理开销下的跨域泛化能力，解决域偏移导致的漏检问题。

<details>
  <summary>Details</summary>

Motivation: 现有单域广义目标检测方法很少研究在严格实时约束下检测器的泛化能力，尤其是在零额外推理开销的前提下。作者观察到基于DETR的实时检测器在域偏移下主要因目标级判别证据不足且不稳定而导致漏检增加，因此亟需一种仅通过训练时表示学习来提升泛化能力的方法。

Method: 提出RT-SDGDet多证据协作建模框架，包含三个核心组件：(1) 一对多(O2M)监督构建稳定的目标特定查询组；(2) 判别性证据多样性学习(DEDL)扩展目标级证据覆盖范围；(3) 双视角证据一致性学习(DvECL)增强外观扰动下证据建模的稳定性。所有组件仅在训练时引入，不增加推理开销。

Result: 在多个未见过的目标域上，所提方法相比现有方法取得了更好的泛化性能。

Conclusion: 通过训练时多查询协作建模证据多样性与一致性，可以在不增加推理开销的前提下有效提升实时检测器的跨域泛化能力。

Original Abstract: In real-world deployment under strict real-time constraints, weather and imaging variations induce significant distribution shifts, severely degrading detectors. Single-Domain Generalized Object Detection aims to mitigate this issue, yet existing methods rarely investigate-at the level of problem formulation-the generalization capability of real-time detectors under such constrained inference budgets. To this end, we introduce Real-Time Single-Domain Generalized Object Detection (RT-SDGOD), which focuses on how real-time detectors can achieve cross-domain generalization under zero extra inference overhead by relying solely on training-time representation learning. We observe that, under domain shift, DETR-based real-time detectors mainly degrade through increased missed detections, rooted in limited and unstable object-level discriminative evidence. Based on this, we propose RT-SDGDet, a multi-evidence collaborative modeling framework for RT-SDGOD. The core idea is to enable multiple queries of the same object to collaboratively cover more sufficient discriminative evidence while maintaining the stability of such evidence modeling across views. Specifically, we use one-to-many (O2M) supervision to construct stable object-specific query groups, and further design Discriminative Evidence Diversity Learning (DEDL) and Dual-view Evidence Consistency Learning (DvECL) to expand object-level evidence coverage and improve evidence stability under appearance perturbations, respectively. Since all components are introduced only during training, our method incurs no extra inference overhead. Extensive experiments show that the proposed method achieves better generalization performance than existing approaches across multiple unseen target domains.

中文摘要翻译: 在严格实时约束的实际部署中，天气和成像变化会导致显著的分布偏移，严重降低检测器性能。单域广义目标检测旨在缓解这一问题，然而现有方法很少在问题公式化层面研究实时检测器在有限推理预算下的泛化能力。为此，我们提出了实时单域广义目标检测（RT-SDGOD），重点关注实时检测器如何仅依靠训练时的表示学习，在零额外推理开销下实现跨域泛化。我们观察到，在域偏移下，基于DETR的实时检测器主要通过漏检增加而退化，其根源在于目标级判别证据有限且不稳定。基于此，我们提出了RT-SDGDet，一种面向RT-SDGOD的多证据协作建模框架。其核心思想是使同一目标的多个查询能够协作覆盖更充分的判别性证据，同时保持跨视角下这种证据建模的稳定性。具体而言，我们使用一对多（O2M）监督构建稳定的目标特定查询组，并进一步设计判别性证据多样性学习（DEDL）和双视角证据一致性学习（DvECL），分别用于扩展目标级证据覆盖范围和提升外观扰动下证据的稳定性。由于所有组件仅在训练时引入，我们的方法不增加任何推理开销。大量实验表明，所提方法在多个未见过的目标域上比现有方法取得了更好的泛化性能。

</details>


### [181] [PhysScene: A Scene Graph Dataset for Scientific Visual Reasoning in Physics Experiments](https://arxiv.org/abs/2606.09368)
*Minghao Zou,Qingtian Zeng,Shangkun Liu,Yanda Meng,Guanghui Yue,Baoquan Zhao,Abdulmotaleb El Saddik,Wei Zhou*

Main category: cs.CV

TL;DR: 提出了PhysScene，首个面向物理实验的场景图数据集，聚焦高关系密度和强语义约束的实验场景，为科学视觉推理提供新的基准。

<details>
  <summary>Details</summary>

Motivation: 现有场景图数据集主要关注通用自然场景，缺乏面向科学实验场景的领域特定数据集，限制了场景图在智能监控、分析等科学实验场景中的应用与发展。

Method: 构建了PhysScene数据集，包含专业仪器、结构化实验装置和功能关系，强调强语义约束和高关系密度，而非追求大规模数据量。

Result: 广泛分析和实验表明，PhysScene补充了现有基准，为现有场景解析算法带来了新挑战，并为推进科学视觉推理提供了有价值的测试平台。

Conclusion: PhysScene填补了领域特定场景图数据集的空白，通过强语义约束和高关系密度为场景解析算法提出新挑战，有助于推动科学实验场景的智能分析发展。

Original Abstract: Scene Graphs (SGs) provide structured representations of visual scenes by modeling objects and their pairwise relationships. Despite recent progress, existing datasets primarily focus on generic natural contexts, leaving domain-specific and function-oriented scenes largely underexplored. This limitation restricts the evaluation of relational reasoning in scientific experimental scenes, thereby hindering the development of intelligent monitoring, analysis, and related applications in such scenes. To address this gap, we introduce PhysScene, the first SG dataset tailored to physics experiments. PhysScene encompasses specialized instruments, structured experimental setups, and functional relations intrinsic to experimental environments, enabling reasoning that extends beyond spatial co-occurrence to logical dependencies. Rather than pursuing large data scale, PhysScene focuses on strong semantic constraints and high relation density in experimental scenes, posing new challenges for existing scene parsing algorithms while offering opportunities for further improvements. Extensive analyses and experiments show that PhysScene complements existing benchmarks and establishes a valuable testbed for advancing scientific visual reasoning. The dataset is publicly available at https://github.com/ZMH-SDUST/PhysScene.

中文摘要翻译: 场景图通过建模物体及其成对关系，提供视觉场景的结构化表示。尽管近期取得了进展，现有数据集主要关注通用自然场景，领域特定和功能导向的场景仍然很大程度上未被探索。这一局限性限制了对科学实验场景中关系推理的评估，从而阻碍了此类场景中智能监控、分析及相关应用的发展。为填补这一空白，我们提出了PhysScene——首个面向物理实验的场景图数据集。PhysScene涵盖了专业仪器、结构化实验装置以及实验环境固有的功能关系，使推理能够超越空间共现扩展到逻辑依赖关系。PhysScene并非追求大规模数据量，而是聚焦于实验场景中的强语义约束和高关系密度，这对现有场景解析算法提出了新的挑战，同时也提供了改进的契机。广泛的分析和实验表明，PhysScene补充了现有基准，并为推进科学视觉推理建立了宝贵的测试平台。该数据集公开于https://github.com/ZMH-SDUST/PhysScene。

</details>


### [182] [Echo-DM: Ultrasound Marker Removal via Conditional Latent Diffusion and Region-Aware Fusion](https://arxiv.org/abs/2606.09378)
*Zhiwei Wang,Tao Huang,Wentao Jiang,Muyi Li,Jianxin Liu,Jian Chen,Jie Zou,Yong Luo,Bo Du,Jing Zhang*

Main category: cs.CV

TL;DR: Echo-DM是一种基于条件潜扩散和区域感知融合的超声图像伪影（如测量标尺、文字）去除框架，采用编码器-扩散-解码器架构，在无需掩码的条件下实现高保真标记去除，并在大规模配对超声数据集上展现出优于两阶段基线方法的性能。

<details>
  <summary>Details</summary>

Motivation: 临床超声图像中的人工标记（测量标尺、文字等）会在下游自动分析中引入捷径偏差，导致深度学习模型依赖标记相关线索而非临床有意义的解剖结构。现有去除方法要么依赖掩码且易受误差传播影响，要么无掩码但会过度平滑超声纹理并扰动未受影响的背景区域。

Method: Echo-DM采用编码器-扩散-解码器流水线，核心包括：1）基于DiT的条件潜扩散网络负责全局恢复；2）区域感知融合模块在端到端无掩码推理下执行保义感知的图像空间精炼。此外，分别用VAE和RAE潜模块实例化为Echo-DM-V和Echo-DM-R两种变体。

Result: 在Echo-PAIR大规模配对临床超声数据集上的实验表明，Echo-DM在标记去除效果和解剖保真度上优于代表性两阶段基线方法，并在不同部署场景下实现了良好的质量-效率权衡。

Conclusion: Echo-DM框架有效解决了超声图像中人工标记的去除问题，避免了下游分析的捷径偏差，且该架构兼容不同的潜模块实例化，具有良好的可扩展性和实用价值。

Original Abstract: Clinical ultrasound images often contain artificial markers, such as measurement calipers and text, to assist diagnostic interpretation and comparison. However, these markers can introduce shortcut bias in downstream automated analysis, encouraging deep learning models to rely on marker-related cues rather than clinically meaningful anatomy. Existing marker removal methods are either mask-dependent and vulnerable to error propagation, or mask-free deterministic restorers that may over-smooth ultrasound texture and perturb unaffected background regions. To address these challenges, we present Echo-DM, a framework for ultrasound marker removal via conditional latent diffusion and region-aware fusion. Echo-DM follows a common encoder-diffusion-decoder pipeline, where a DiT-based conditional latent diffusion network performs global restoration and a region-aware fusion module enforces preservation-aware image-space refinement under end-to-end mask-free inference. Building on this fixed core design, we further instantiate Echo-DM-V and Echo-DM-R with VAE-based and RAE-based latent modules, respectively, which demonstrates that the Echo-DM architecture is compatible with diverse latent-module instantiations. Extensive experiments on Echo-PAIR, a large-scale paired clinical ultrasound dataset, demonstrate superior marker removal and strong anatomical fidelity compared with representative two-stage baselines, while providing favorable quality--efficiency trade-offs across deployment settings. Data, code and models will be released at https://github.com/MiliLab/Echo-DM.

中文摘要翻译: 临床超声图像通常包含人工标记（例如测量标尺和文字），以辅助诊断判读和对比。然而，这些标记可能会在下游自动化分析中引入捷径偏差，导致深度学习模型依赖与标记相关的线索而非临床有意义的解剖结构。现有的标记去除方法要么依赖掩码且易受误差传播影响，要么是无掩码的确定性恢复器，但可能会过度平滑超声纹理并扰动未受影响的背景区域。为应对这些挑战，我们提出了Echo-DM，一种基于条件潜扩散和区域感知融合的超声标记去除框架。Echo-DM遵循通用的编码器-扩散-解码器流水线，其中基于DiT的条件潜扩散网络执行全局恢复，区域感知融合模块在端到端无掩码推理下强制执行保义感知的图像空间精炼。基于这一固定核心设计，我们进一步分别用基于VAE和基于RAE的潜模块实例化了Echo-DM-V和Echo-DM-R，这表明Echo-DM架构兼容不同的潜模块实例化。在Echo-PAIR（一个大规模配对的临床超声数据集）上进行的大量实验表明，与代表性两阶段基线方法相比，Echo-DM具有更优的标记去除效果和强大的解剖保真度，同时在多种部署场景下提供了良好的质量-效率权衡。数据、代码和模型将在https://github.com/MiliLab/Echo-DM开源。

</details>


### [183] [An Opticalmechanics Framework for Dynamic Estimation of Multibody Systems](https://arxiv.org/abs/2606.09383)
*Banglei Guan,Xuanyu Bai,Qingquan Chen,Zibin Liu,Dongcai Tan,Zhenbao Yu,Yang Shang,Qifeng Yu*

Main category: cs.CV

TL;DR: 提出了一种光机运动-动力学集成估计框架，利用图像测量作为无接触输入，结合遗传算法优化辨识多体系统关节力矩，在气浮平台上验证了腕关节力矩估计精度（MAE 0.46 Nm）和角速度预测精度（MAE 0.006 rad/s）。

<details>
  <summary>Details</summary>

Motivation: 传统人体动力学分析依赖接触力/力矩传感器和受控实验室环境，限制了实际场景中的应用。为突破这一限制，该研究探索了基于图像测量的无接触动态估计方法。

Method: 建立约束多体模型描述系统动力学；利用图像测量获取运动学量作为非接触输入；通过遗传算法优化，最小化模型预测与图像测量运动学量之间的差异，从而辨识未知关节力矩。

Result: 在气浮平台上实验验证：图像数据估计的腕关节力矩与传感器测量相比，平均绝对误差为0.46 Nm；前向预测中，模型预测角速度与图像测量结果相比，平均绝对误差为0.006 rad/s。

Conclusion: 该研究展示了在难以直接测量力和力矩的场景中，将图像测量与机械建模相结合进行无接触动态估计的潜力。

Original Abstract: Conventional dynamics analysis of the human body is often constrained by the need for contact force and torque sensors and controlled laboratory environments. To address this issue, this study proposes an opticalmechanics kinematic-dynamic integrated estimation framework for multibody systems. Specifically, a constrained multibody model is established to describe the system dynamics, while image-measured kinematic quantities are used as non contact inputs for dynamic estimation. The unknown joint torque is then identified through a genetic-algorithm based optimization by minimizing the discrepancy between model-predicted and image-measured kinematic quan tities. Experimental validation on an air-bearing platform showed that the wrist joint torque estimated from image data achieved a mean absolute error of 0.46 Nm compared with sensor measurements. In the forward prediction test, the model-predicted angular velocity achieved a mean absolute error of 0.006 rad/s relative to the image-measured results. This study demonstrates the potential of combining image measurement and mechanical modeling for non-contact dynamic estimation in scenarios where direct force and torque measurement is difficult.

中文摘要翻译: 人体常规动力学分析通常受限于对接触力与力矩传感器以及受控实验室环境的需求。为解决这一问题，本研究提出了一种面向多体系统的光机运动-动力学集成估计框架。具体而言，建立约束多体模型以描述系统动力学，同时将图像测量的运动学量作为动态估计的无接触输入。然后，通过基于遗传算法的优化，最小化模型预测与图像测量运动学量之间的差异，从而辨识未知关节力矩。在气浮平台上的实验验证表明，由图像数据估计的腕关节力矩与传感器测量值相比，平均绝对误差为0.46 Nm。在前向预测测试中，模型预测的角速度与图像测量结果相比，平均绝对误差为0.006 rad/s。该研究证明了在难以直接测量力和力矩的场景中，将图像测量与机械建模相结合进行无接触动态估计的潜力。

</details>


### [184] [Real-time body pose non-verbal communication with a consistency-based reliability measure](https://arxiv.org/abs/2606.09390)
*Alina Marcu,Dragos Costea,Cristina Lazar,Marius Leordeanu*

Main category: cs.CV

TL;DR: 本文研究仅从2D人体姿态识别通信意图，构建了一个包含十种通信意图的真实全身姿态数据集，在嵌入式GPU上对多种模型进行基准测试，并证明了模型的自回归自一致性可作为无监督可靠性信号。

<details>
  <summary>Details</summary>

Motivation: 现有情感语料库混合了身体、面部、声音和文本信号，而骨骼动作识别基准仅标注了动作本身而非传递的信息，缺乏专门用于从身体姿态识别通信意图的资源。在远距离救援等场景中，身体运动是可靠信号，但现有资源无法单独提取该信号。

Method: 1) 释放了一个包含十种通信意图的真实全身姿态数据集；2) 将其与真实(IPC)和合成(MotionLCM, VEO3.1, Kimodo)数据对比；3) 对多种模型（从骨架图分类器到联合运动预测网络）进行基准测试；4) 提出模型的自回归自一致性作为无监督可靠性信号，并给出理论证明。

Result: 在嵌入式GPU(NVIDIA Orin Nano)上报告了多种模型的性能指标和帧率；证明了自回归自一致性可作为可靠性信号，给出了正确性概率的界限，证明了该概率随一致步数增长而增加，并识别了自信预测仍可能错误的条件。

Conclusion: 身体运动是远距离人-机器人通信中可靠的信号；自回归自一致性可作为有效的无监督可靠性信号，有助于在资源受限的机器人平台上实现实时通信意图识别。

Original Abstract: Body movement communicates intent at distances and in conditions where neither the face, nor speech can be captured. We study the recognition of communicative intent from 2D body pose alone. We argue that body motion is a reliable signal especially in scenarios that require real time low-cost on-device person-to-robot communication in long distance environments, such as rescue missions. However, existing resources do not isolate this signal. Affective corpora combine body, face, voice and text, while skeleton action-recognition benchmarks label the action performed rather than the message conveyed. We release a dataset of real frames of full-body pose covering ten communicative intents and we compare it against other real (IPC) and synthetic (MotionLCM, VEO3.1, Kimodo) ones that span a range of difficulty. We target systems that can run on a robot's limited onboard hardware. We benchmark multiple models, from skeleton graph classifiers to joint motion-forecasting networks, and report performance metrics together with frame rate on an embedded GPU (NVIDIA Orin~Nano), since speed matters as much as accuracy in our scenario. Finally, we show that a model's own autoregressive self-consistency works as an unsupervised reliability signal. We give a short proof that bounds the probability that a self-consistent prediction is correct, show that this probability grows with the number of consistent steps, and identify the conditions under which a confident prediction can still be false, benchmarked against industry-standard metrics.

中文摘要翻译: 身体运动能够在远距离以及无法捕捉面部或语音的条件下传达意图。我们研究仅从2D人体姿态识别通信意图。我们认为，身体运动是一种可靠的信号，尤其适用于需要实时、低成本、设备端人-机器人通信的远距离环境，例如救援任务。然而，现有资源无法单独提取这一信号。情感语料库融合了身体、面部、语音和文本，而骨骼动作识别基准标注的是所执行的动作而非所传达的信息。我们发布了一个包含十种通信意图的真实全身姿态帧数据集，并将其与其他真实（IPC）和合成（MotionLCM、VEO3.1、Kimodo）数据集进行对比，这些数据集覆盖了不同的难度范围。我们针对可在机器人有限板载硬件上运行的系统。我们对多种模型进行了基准测试，从骨架图分类器到联合运动预测网络，并在嵌入式GPU（NVIDIA Orin Nano）上报告了性能指标和帧率，因为在我们的场景中速度与准确性同等重要。最后，我们证明了模型自身的自回归自一致性可作为无监督可靠性信号。我们给出了一个简短证明，限定了自一致性预测正确的概率上界，证明了该概率随一致步数增加而增长，并识别了自信预测仍可能错误的条件，最后以行业标准指标进行了基准测试。

</details>


### [185] [CapRL++: Unified Reinforcement Learning with Verifiable Rewards for Dense Image and Video Captioning](https://arxiv.org/abs/2606.09393)
*Penghui Yang,Long Xing,Xiaoyi Dong,Yuhang Zang,Yuhang Cao,Yibin Wang,Yujie Zhou,Jiazi Bu,Jianze Liang,Qidong Huang,Jiaqi Wang,Feng Wu,Dahua Lin*

Main category: cs.CV

TL;DR: 提出CapRL++，一种基于可验证奖励的强化学习（RLVR）的无参考训练框架，通过让非视觉语言模型基于生成的描述回答多选题来评估描述质量，替代传统监督微调（SFT），在20+图像/视频基准上提升密集描述质量，使小模型达到接近大模型（如Qwen2.5-VL-72B）的性能。

<details>
  <summary>Details</summary>

Motivation: 传统监督微调（SFT）依赖昂贵且不可扩展的人工标注，导致模型记忆固定答案、泛化性差、缺乏多样性描述能力。为克服这些局限，作者提出将可验证奖励的强化学习（RLVR）应用于开放式的多模态描述生成任务。

Method: 提出CapRL++无参考训练框架，采用解耦的两阶段流程：（1）LVLM生成描述；（2）由独立的、无视觉输入的语言模型仅基于该描述回答多选题，以回答准确率作为奖励信号，从而评估描述质量，无需人工标注或真实描述作为参考。

Result: 在20+图像和视频基准上，CapRL++提升了密集描述质量；增强了基于描述的预训练在下游空间和时间理解任务中的效果；使用CapRL++标注的可扩展数据集进行预训练获得显著下游收益；在Prism评估框架下，小模型达到与Qwen2.5-VL-72B、Qwen3-VL-235B-A22B等大模型相当的密集描述性能。

Conclusion: CapRL++有效训练模型生成可泛化、高保真度的描述，为超越传统SFT局限建立了鲁棒基础，证明了RLVR在多模态描述生成中的有效性。

Original Abstract: Image and video captioning are fundamental tasks that bridge the visual and linguistic domains, playing a critical role in pre-training Large Vision-Language Models (LVLMs). Current state-of-the-art captioning models are typically trained with Supervised Fine-Tuning (SFT), a paradigm that relies on expensive, non-scalable annotations and often causes models to memorize specific ground-truth answers, limiting their generality and ability to generate diverse, creative descriptions. To overcome these limitations, we propose applying Reinforcement Learning with Verifiable Rewards (RLVR) to the open-ended task of multimodal captioning. We introduce Captioning Reinforcement Learning++ (CapRL++), a novel reference-free training framework that redefines caption quality through its utility: a high-quality caption should enable a non-visual language model to accurately answer questions about the corresponding visual content. CapRL++ employs a decoupled two-stage pipeline where an LVLM generates a caption, and the objective reward is derived from the accuracy of a separate, vision-free LLM answering Multiple-Choice Questions based solely on that caption. Evaluations on more than 20 image and video benchmarks show that CapRL++ improves dense caption quality and strengthens caption-based pretraining across tasks such as spatial and temporal understanding. Pretraining on scalable image and video caption datasets annotated by CapRL++ yields substantial downstream gains. Furthermore, within the Prism Framework for caption quality evaluation, compact models trained with CapRL++ achieve dense captioning performance comparable to substantially larger models such as Qwen2.5-VL-72B and Qwen3-VL-235B-A22B. These results validate that CapRL++ effectively trains models to produce generalizable, high-fidelity descriptions, establishing a robust foundation beyond the limitations of traditional SFT.

中文摘要翻译: 图像和视频描述是连接视觉与语言领域的基础任务，在大规模视觉-语言模型（LVLM）的预训练中扮演关键角色。当前最先进的描述模型通常采用监督微调（SFT）进行训练，这种范式依赖昂贵且不可扩展的人工标注，常导致模型记忆特定真实答案，限制了其泛化能力和生成多样化、创造性描述的能力。为克服这些局限，我们提出将可验证奖励的强化学习（RLVR）应用于开放式的多模态描述任务。我们引入CapRL++，一种新颖的无参考训练框架，通过描述的效用重新定义描述质量：高质量的描述应使非视觉语言模型能够准确回答关于对应视觉内容的问题。CapRL++采用解耦的两阶段流程，其中LVLM生成描述，而目标奖励则基于一个独立的、无视觉输入的LLM仅根据该描述回答多项选择题的准确率得出。在20多个图像和视频基准上的评估表明，CapRL++提升了密集描述质量，并增强了基于描述的预训练在空间和时间理解等任务中的效果。使用CapRL++标注的可扩展图像和视频描述数据集进行预训练，可获得显著的下游收益。此外，在Prism描述质量评估框架下，使用CapRL++训练的紧凑模型实现了与Qwen2.5-VL-72B和Qwen3-VL-235B-A22B等显著更大模型相当的密集描述性能。这些结果验证了CapRL++有效训练模型生成可泛化、高保真度的描述，为超越传统SFT局限建立了鲁棒基础。

</details>


### [186] [vesselFM-CT: Segmenting All Blood Vessels in CT Images for System-Level Cardiovascular Analysis](https://arxiv.org/abs/2606.09400)
*Bastian Wittmann,Chinmay Prabhakar,Suprosanna Shit,Bjoern Menze*

Main category: cs.CV

TL;DR: 本文提出VesselFM-CT，首个能够从3D CT图像中稳健分割所有血管（从大动脉到微小肠系膜血管）的模型，通过迭代多步训练和TubeLoss损失函数解决了血管系统的异质性挑战。

<details>
  <summary>Details</summary>

Motivation: 人体血管网络在半径、长度、拓扑结构和分支模式上存在巨大差异，且受解剖背景变化影响，现有研究多聚焦于孤立狭窄的血管段，无法实现对整个心血管系统的系统性健康评估和功能完整性分析。

Method: VesselFM-CT采用迭代多步训练过程，并优化所提出的TubeLoss损失函数，以有效应对心血管系统固有的异质性。

Result: VesselFM-CT在所有基线方法中表现最优，实现了从CT图像中自动化、精确地提取整个心血管系统，并支持疾病分类和合成CT图像生成等临床应用。

Conclusion: VesselFM-CT填补了血管网络全尺度分割的空白，推动临床诊断和血管生理学基础研究，为自动化疾病分类和合成CT生成开辟了新方向。

Original Abstract: The vascular network in the human body is characterized by blood vessels exhibiting drastic structural variations in radius, length, topological properties, and branching patterns. This heterogeneity, together with location-specific anatomical background variations, poses a significant challenge for robust, large-scale analysis of the entire cardiovascular system. As a result, most research has focused on narrow, isolated segments of the vascular network. While such targeted studies provide valuable insights, they inherently limit the ability to assess the systemic health and functional integrity of the vascular network as a whole. In this work, we aim to bridge this gap to advance both clinical diagnostics and our fundamental understanding of vascular physiology. We propose the task of segmenting all vessels in CT images, ranging from the largest components of the cardiovascular system to even minuscule mesenteric vessels. To this end, we introduce vesselFM-CT, the first model capable of robustly segmenting all blood vessels in 3D CT images. VesselFM-CT is trained via an iterative, multi-step process and optimizes our proposed TubeLoss loss function, effectively addressing the inherent heterogeneity of the cardiovascular system. We demonstrate that vesselFM-CT outperforms all baselines and enables automated, precise extraction of the cardiovascular system from CT images, thereby unlocking a wide range of clinical and technical perspectives, including automated disease classification and synthetic CT image generation.

中文摘要翻译: 人体内的血管网络具有显著的结构异质性，血管在半径、长度、拓扑特性和分支模式上存在巨大差异。这种异质性，加之位置特异的解剖背景变化，给整个心血管系统的鲁棒、大规模分析带来了重大挑战。因此，大多数研究仅关注血管网络中狭窄、孤立的片段。尽管此类针对性研究提供了有价值的见解，但本质上限制了对血管网络整体系统健康水平和功能完整性的评估能力。在本工作中，我们旨在弥合这一差距，以推动临床诊断和血管生理学的基础研究。我们提出了一项新任务：在CT图像中分割所有血管——从心血管系统中最粗大的组件到极其微小的肠系膜血管。为此，我们引入了VesselFM-CT，这是首个能够在3D CT图像中稳健分割所有血管的模型。VesselFM-CT通过迭代的多步过程进行训练，并优化我们提出的TubeLoss损失函数，有效应对了心血管系统固有的异质性。我们证明，VesselFM-CT在所有基线方法中表现最优，能够从CT图像中自动化、精确地提取整个心血管系统，从而解锁了广泛的临床和技术应用前景，包括自动化疾病分类和合成CT图像生成。

</details>


### [187] [Leveraging Morphology for Historical Script Metrological Analysis](https://arxiv.org/abs/2606.09446)
*Malamatenia Vlachou Efstathiou,Raphaël Baena,Dominique Stutzmann,Mathieu Aubry*

Main category: cs.CV

TL;DR: 本文提出了一种基于Transformer检测架构和原型行重建模块的形态学分析方法，仅需行级转录标注即可学习字符原型及其变形与定位，为古文字学提供可扩展、有意义且稳定的定量测量手段，并在14世纪手稿上验证了区分和发现细微书写变体的能力。

<details>
  <summary>Details</summary>

Motivation: 现有手写文本识别技术虽能大规模转录历史文档，但无法为古文字学提供可解释的视觉测量指标，缺乏从行级转录中学习字符形态原型的有效方法。

Method: 基于Transformer的检测架构结合原型行重建模块，从行级转录监督中学习可解释的字符原型及其出现位置、变形和定位信息，无需字符级标注。

Result: 在14世纪四名抄写员完成的160页手稿上，该方法仅需单列文本即可逐页计算测量指标，有效区分不同抄写员的图形轮廓特征，并发现了此前未注意的细微书写变体。

Conclusion: 所提方法在古文字学测量方面显著优于Learnable Typewriter基线，具备可扩展性和数据高效性（仅需极少量训练数据），为大规模历史手稿的形态学分析提供了实用工具。

Original Abstract: Advances in handwritten text recognition have enabled large-scale transcription of historical documents, but still provide limited access to interpretable visual measurements for paleography, the study of historical scripts. In this paper, our main insight is that morphological script analysis, in particular the capacity to learn character prototypes from line-level transcriptions, enables the definition of scalable, meaningful, and stable paleographic measurements. More precisely, we leverage a transformer-based detection architecture together with a prototype-based line reconstruction module to learn prototypical characters and their occurrence, deformation, and positioning.
  Our contributions are twofold. First, we introduce a deep architecture and learning methodology that enables efficient character modeling with only line-level transcription supervision, significantly improving over the Learnable Typewriter baseline and enabling accurate character bounding box prediction, unlocking its potential for paleographic measurements. Second, we introduce and demonstrate the paleographical relevance of automatic measurements enabled by our architecture for characters, bi-grams, and spaces between graphical units. For this demonstration, we extend the annotations of the codex Paris, BnF, fr. 2813, commissioned in the late fourteenth century by Charles V and copied by four hands, to 160 pages. We visualize our measurements over these pages, showing how they enable us not only to differentiate graphical profiles, but also to discover and analyze subtle variations. This case study outlines the scalability of our approach and its frugality in terms of required training data, since a single column of text is sufficient to compute our measurements on each of the 160 pages.
  Data and code are publicly available at: https://malamatenia.github.io/morphology4metrology-analysis.

中文摘要翻译: 手写文本识别的进展使历史文档的大规模转录成为可能，但仍难以为古文字学（历史文字研究）提供可解释的视觉测量手段。本文的核心洞见在于：形态学文字分析——特别是从行级转录中学习字符原型的能力——能够定义可扩展、有意义且稳定的古文字学测量指标。具体而言，我们利用基于Transformer的检测架构以及基于原型的行重建模块，来学习原型字符及其出现位置、变形和定位信息。我们的贡献有两方面：第一，提出了一种仅需行级转录监督即可实现高效字符建模的深度架构与学习方法，显著优于Learnable Typewriter基线，实现了准确的字符边界框预测，释放了其在古文字学测量中的潜力；第二，引入并展示了由我们的架构自动生成的字符、双字母组及图形单元间距测量的古文字学相关性。为此，我们将14世纪末由查理五世委托制作、四名抄写员抄写的巴黎法典（BnF fr. 2813）的标注扩展到160页。我们在这些页面上可视化测量结果，展示其不仅能区分图形轮廓特征，还能发现并分析细微变体。该案例研究展示了我们方法的可扩展性及其对训练数据的需求之低——在160页的每一页上，仅需单列文本即可完成测量。数据和代码公开于：https://malamatenia.github.io/morphology4metrology-analysis。

</details>


### [188] [GD-MIL: Grade-Disentangled Multiple Instance Learning for Multimodal Biochemical Recurrence Prediction in Prostate Cancer](https://arxiv.org/abs/2606.09453)
*Dasari Naga Raju*

Main category: cs.CV

TL;DR: 本文系统评估了H&E全切片图像在前列腺癌生化复发预测中的预后价值，发现特征提取器比MIL聚合器影响更大，并提出了等级解缠MIL（GD-MIL）方法，通过梯度反转实现Gleason等级不变性，显著超越了临床基线和纯影像模型。

<details>
  <summary>Details</summary>

Motivation: 现有前列腺癌生化复发风险分层几乎完全依赖Gleason等级等临床变量，但H&E全切片图像是否包含超越等级的预后信号，以及多实例学习能否提取这些信号，尚未得到明确结论。同时，许多现有管线的评估方式存在交叉验证泄漏问题，导致结果被高估。

Method: 构建了严格的TCGA-PRAD基准（487名患者，101个BCR事件），采用五折交叉验证重复五次并严格使用out-of-fold评分。提出了Grade-Disentangled MIL（GD-MIL），一种基于门控注意力MIL编码器，通过梯度反转等级对抗训练，使切片表示对Gleason等级具有不变性，再与临床变量进行晚期融合。

Result: MIL聚合器（ABMIL、CLAM、TransMIL、PatchGCN）影响很小（C-index 0.61-0.64），特征提取器是主导因素（ResNet50 0.566 vs 病理基础模型最高0.639）。临床Cox模型达到0.687，无影像模型显著超越它。GD-MIL达到C-index 0.704，显著优于临床基线（Δc=+0.029, p=0.0005）和最佳纯影像模型（Δc=+0.062, p=0.039），中位风险分层在BCR-free生存上达到log-rank p<0.0001（五年约20% vs 70%）。

Conclusion: H&E组织形态学包含超越Gleason等级的预后互补信息；通过等级解缠MIL能够有效提取这些信号，实现优于临床基线的风险分层。

Original Abstract: Biochemical recurrence (BCR) after radical prostatectomy is a critical endpoint in prostate cancer, yet risk stratification relies almost entirely on variables dominated by Gleason grade. Whether H&E whole slide images (WSIs) carry prognostic signal beyond grade, and whether multiple instance learning (MIL) can recover it, remains unsettled. A key obstacle is that many pipelines select model checkpoints on the evaluation fold, artificially inflating concordance. We construct a rigorous benchmark on TCGA-PRAD (487 patients, 101 BCR events) using strict out-of-fold scoring over five-fold cross-validation repeated across five seeds. The choice of MIL aggregator (ABMIL, CLAM, TransMIL, PatchGCN) has little effect (C-index 0.61-0.64 with UNI2-h), while the feature extractor is the dominant factor (ResNet50 0.566 versus pathology foundation models up to 0.639). A clinical Cox model on grade, stage, and age reaches 0.687; no imaging-only model significantly outperforms it (p > 0.10). We introduce Grade-Disentangled MIL (GD-MIL), a gated-attention MIL encoder trained with a gradient-reversal grade adversary that encourages the slide representation to be invariant to Gleason grade before late fusion with clinical variables. GD-MIL achieves C-index 0.704, significantly outperforming both the clinical baseline (delta-c = +0.029, p = 0.0005) and the best imaging-only model (delta-c = +0.062, p = 0.039), suggesting H&E morphology contains prognostic information complementary to grade. A median risk split yields log-rank p < 0.0001 separation in BCR-free survival (~20% vs ~70% at five years).

中文摘要翻译: 根治性前列腺切除术后的生化复发（BCR）是前列腺癌的一个关键终点，然而风险分层几乎完全依赖于以Gleason等级为主导的变量。H&E全切片图像（WSI）是否携带超越等级的预后信号，以及多实例学习（MIL）能否恢复这些信号，这一问题仍无定论。一个主要障碍是许多管线在评估折上选择模型检查点，人为提高了一致性指数。我们在TCGA-PRAD数据集（487名患者，101个BCR事件）上构建了一个严格的基准，采用五次重复的五折交叉验证，并使用严格的折外评分。MIL聚合器的选择（ABMIL、CLAM、TransMIL、PatchGCN）影响很小（使用UNI2-h时C-index为0.61-0.64），而特征提取器是主导因素（ResNet50为0.566，病理基础模型最高达0.639）。基于等级、分期和年龄的临床Cox模型达到0.687；没有纯影像模型显著超越它（p > 0.10）。我们提出了等级解缠MIL（GD-MIL），这是一种门控注意力MIL编码器，通过梯度反转等级对抗训练，使切片表示对Gleason等级具有不变性，再与临床变量进行晚期融合。GD-MIL达到C-index 0.704，显著优于临床基线（Δc = +0.029，p = 0.0005）和最佳纯影像模型（Δc = +0.062，p = 0.039），表明H&E组织形态学包含与等级互补的预后信息。中位风险分层在无BCR生存上达到log-rank p < 0.0001的显著分离（五年时约20% vs 70%）。

</details>


### [189] [Training-Free Generalized Few-Shot Segmentation through Open-Vocabulary Semantic Arbitration](https://arxiv.org/abs/2606.09474)
*Silas Kwabla Gah,Ebenezer Owusu*

Main category: cs.CV

TL;DR: 提出Open-V，一种无需训练的GFSS框架，通过在推理阶段协调冻结的基础模型（SAM3和CLIP）来解决广义少样本语义分割问题，无需任何参数微调，在多个基准上超越需要任务特定训练的最强基线。

<details>
  <summary>Details</summary>

Motivation: 传统GFSS方法需要任务特定的参数微调来融入新类别，而近期基础模型已具备强大的开放词汇识别能力。本文探究是否可以通过推理阶段协调冻结的语义先验来解决GFSS，而非进行参数适应。

Method: 提出Open-V框架，结合SAM3的可提示概念分割（PCS）与K-shot CLIP支持质心，通过校准的逐像素语义仲裁机制在推理阶段融合支持信息，无需任何可训练组件。

Result: 在PASCAL-5i（1-shot）上达到base/novel/harmonic mIoU为78.4/77.5/77.9，超越最强训练基线+17.7 HM。在PASCAL-5i、COCO-20i和ADE-OW上均验证有效性。

Conclusion: GFSS可以通过推理阶段协调冻结的语义先验来解决；支持信息通过推理阶段语义接地融入，且当文本先验较弱时贡献更大；发现基础模型分割中存在预处理和评估空间不匹配的可重复性混淆因素。

Original Abstract: Generalized Few-Shot Semantic Segmentation (GFSS) has traditionally been approached as a representation-learning problem, requiring task-specific adaptation to incorporate novel classes from limited support examples. Recent foundation models, however, already exhibit strong open-vocabulary recognition and segmentation capabilities, raising a different question: can GFSS be solved through inference-time coordination of frozen semantic priors rather than parameter adaptation? We answer this question with Open-V, a training-free GFSS framework that combines Segment Anything (SAM3) Promptable Concept Segmentation (PCS) with a K-shot CLIP support centroid through calibrated per-pixel semantic arbitration. OpenV introduces no trainable components and supports arbitrary semantic categories at inference time. Beyond segmentation performance, our study contributes three broader findings. First, we show that support information can be incorporated through inference-time semantic grounding, and that its contribution increases as foundation-model text priors weaken on label-disjoint vocabularies. Second, we identify a reproducibility confound in foundationmodel segmentation, demonstrating that preprocessing and evaluation-space mismatches can silently distort reported performance. Finally, we validate Open-V across PASCAL5i, COCO-20i, and ADE-OW, showing that training-free coordination of foundation-model priors generalizes across both conventional GFSS and open-vocabulary evaluation settings. On PASCAL-5i (1-shot), Open-V attains base/novel/harmonic mIoU of 78.4/77.5/77.9, without GFSS-specific training surpassing the strongest trained baseline by +17.7 HM.

中文摘要翻译: 广义少样本语义分割（GFSS）传统上被视为表示学习问题，需要通过任务特定适应来从有限的支持样本中融入新类别。然而，近期的基础模型已经展现出强大的开放词汇识别和分割能力，这提出了一个不同的问题：GFSS是否可以通过推理阶段协调冻结的语义先验来解决，而非参数适应？我们通过Open-V回答了这个问题，这是一种无需训练的GFSS框架，它将Segment Anything（SAM3）的可提示概念分割（PCS）与K-shot CLIP支持质心相结合，通过校准的逐像素语义仲裁进行推理。Open-V不引入任何可训练组件，并在推理时支持任意语义类别。除了分割性能外，我们的研究还贡献了三个更广泛的发现。第一，我们表明支持信息可以通过推理阶段的语义接地来融入，并且随着基础模型文本先验在标签不相交的词汇表上变弱，其贡献会增大。第二，我们识别出基础模型分割中的一个可重复性混淆因素，证明预处理和评估空间的不匹配会无声地扭曲报告的性能。最后，我们在PASCAL-5i、COCO-20i和ADE-OW上验证了Open-V，表明无需训练地协调基础模型先验能够在传统的GFSS和开放词汇评估设置中泛化。在PASCAL-5i（1-shot）上，Open-V达到了78.4/77.5/77.9的基类/新类/调和mIoU，无需GFSS特定训练，超越了最强训练基线+17.7 HM。

</details>


<div id='eess.SP'></div>

# eess.SP [[Back]](#toc)

### [190] [FADRW: A Feature-Aware Modulated and Dynamically Reweighted Loss for Few-Shot Linguistic Steganalysis](https://arxiv.org/abs/2606.07655)
*Shuo Liu,Xianghong Lin,Yukun Wei,Zhongliang Yang*

Main category: eess.SP

TL;DR: 提出一种名为FADRW的新型损失函数框架，通过动态重加权缓解决策偏差、特征感知调制增强隐写特征可分离性，以解决社交平台恶意语言隐写检测中样本极不平衡和生成式隐写特征微弱导致的优化难题，在少样本场景下显著优于现有方法。

<details>
  <summary>Details</summary>

Motivation: 社交平台上恶意语言隐写术泛滥带来严重安全风险，但现有检测方法面临两个根本性优化难题：一是极端类别不平衡（隐写样本不足1%）导致强烈决策偏差；二是生成式隐写特征与正常文本几乎不可区分，加之样本极为稀少，导致微弱隐写信号被完全淹没（特征边缘化）。

Method: 提出FADRW损失函数框架，包含两个核心模块：1）动态重加权模块，逐步抵消决策偏差；2）特征感知调制模块，从结构上重塑特征空间，增强微弱隐写特征的可分离性，从而防止特征边缘化。

Result: 在来自三个真实社交平台的数据集上进行的大量实验表明，FADRW在少样本隐写检测场景下显著优于最先进的现有方法。

Conclusion: FADRW损失函数框架能够有效应对极端类别不平衡和特征边缘化挑战，为少样本语言隐写分析提供了一种高性能解决方案。

Original Abstract: The ubiquity of social media platforms facilitates malicious linguistic steganography, posing significant security risks. However, detection is severely hampered by two fundamental issues during model training. Firstly, extreme class imbalance (less than 1% steganographic samples) induces a strong decision bias. Secondly, the invisibility of generative steganography means its features are nearly indistinguishable from benign text; this similarity, compounded by their extreme rarity, leads to severe feature marginalization, where faint steganographic signals are completely overwhelmed. To directly address these optimization-level challenges, we propose FADRW (Feature-Aware Modulated and Dynamically Reweighted Loss), a novel loss function framework engineered for few-shot steganalysis. FADRW employs Dynamic Reweighting to progressively counteract decision bias, and a Feature-Aware Modulation module to structurally reshape the feature space, preventing feature marginalization by enhancing the separability of these subtle features. Extensive experiments on datasets from three real-world social platforms demonstrate that FADRW significantly outperforms state-of-the-art methods, particularly in the challenging few-shot steganographic sample scenario.

中文摘要翻译: 社交媒体平台的普遍性为恶意语言隐写术提供了便利，带来了显著的安全风险。然而，在模型训练过程中，两个基本问题严重阻碍了检测。首先，极端类别不平衡（隐写样本不足1%）导致了强烈的决策偏差。其次，生成式隐写的不可见性意味着其特征与正常文本几乎无法区分；这种相似性，加上其极端的稀有性，导致了严重的特征边缘化，使得微弱的隐写信号被完全淹没。为了直接解决这些优化层面的挑战，我们提出了FADRW（特征感知调制与动态重加权损失），一种专为少样本隐写分析设计的新型损失函数框架。FADRW采用动态重加权逐步抵消决策偏差，并利用特征感知调制模块从结构上重塑特征空间，通过增强这些细微特征的可分离性来防止特征边缘化。在来自三个真实社交平台数据集上进行的大量实验表明，FADRW显著优于最先进的现有方法，尤其是在具有挑战性的少样本隐写样本场景下。

</details>


### [191] [Hessian-matching Based Weighting for Attitude Determination Using Short-Range DoA Measurements with IMU Assistance](https://arxiv.org/abs/2606.07719)
*Chenxin Tu,Hengchuan Zhang,Xiaowei Cui,Gang Liu,Mingquan Lu*

Main category: eess.SP

TL;DR: 本文提出了一种基于全最小二乘（TLS）的短距离姿态确定方法，通过Hessian匹配的标量加权策略逼近TLS解，并引入IMU重力矢量辅助，显著提升了GNSS拒止环境下无人系统的姿态估计精度与鲁棒性。

<details>
  <summary>Details</summary>

Motivation: 在GNSS拒止环境中，短距离无线阵列提供的到达角（DoA）测量用于姿态确定时，导航系方向矢量受锚点/载体位置误差影响显著，且方向矢量误差各向异性，现有方法未能有效建模这些误差。本文旨在建立符合TLS视角的优化框架，并兼顾计算效率与数值鲁棒性。

Method: (1) 识别短距离AD中导航系方向矢量误差不可忽略的特性，建立TLS一致性的优化模型；(2) 通过流形上的高斯-牛顿法求解协方差加权正交Procrustes问题；(3) 提出基于Hessian匹配的标量加权策略（全姿态策略和关注方向策略），使闭式Wahba解的Hessian逼近TLS解；(4) 引入IMU重力矢量作为额外方向矢量对，扩展为扩展Wahba和扩展TLS公式。

Result: 仿真结果表明：所提出的Hessian匹配加权方法相比现有基线方法在精度和鲁棒性上有显著提升；重力矢量增强进一步降低了姿态误差，并在锚点数量有限时提高了解的可获得性。

Conclusion: 本文提出的TLS一致性框架配合Hessian匹配加权策略，能够有效处理短距离AD中方向矢量误差的各向异性问题，且重力矢量的引入可作为有效的静态初始化手段，提升系统在受限条件下的性能。

Original Abstract: Accurate and reliable attitude determination (AD) is essential for unmanned vehicles operating in Global Navigation Satellite System (GNSS)-denied environments. Short-range wireless arrays can provide direction-of-arrival (DoA) measurements from multiple anchors, enabling AD by aligning corresponding direction vectors (DVs) expressed in the body and navigation frames. In short-range scenarios, navigation-frame DVs inherit non-negligible uncertainty induced by anchor/vehicle position errors in addition to DoA-induced errors in body-frame DVs. Moreover, due to projection and unit-norm normalization, the DV errors are generally anisotropic, which motivates a total least squares (TLS) viewpoint. This paper identifies the key modeling distinction in short-range AD, develops a TLS-consistent formulation based on the total DV error and solves the resulting covariance-weighted orthogonal Procrustes problem via a manifold Gauss--Newton method. To retain the efficiency and numerical robustness of the closed-form weighted Wahba solution, we further propose Hessian-matching based scalar weighting strategies that approximate the Hessian of Wahba formulation to the TLS formulation, including a full-attitude strategy for overall accuracy and a direction-of-interest (DOI) strategy for prioritizing a selected attitude component. Finally, we incorporate IMU-derived gravity as an additional DV pair for static initialization, leading to extended Wahba and extended TLS formulations. Simulation results demonstrate that the proposed Hessian-matching weighting improves accuracy and robustness compared with existing baselines, and that gravity-DV augmentation further reduces attitude errors and improves solution availability under limited anchor availability.

中文摘要翻译: 对于在GNSS拒止环境中运行的无人系统而言，准确可靠的姿态确定至关重要。短距离无线阵列可提供来自多个锚点的到达角测量，通过对齐机体坐标系和导航坐标系中表达的方向矢量来实现姿态确定。在短距离场景下，除了机体坐标系方向矢量中由DoA引起的误差外，导航坐标系方向矢量还会继承由锚点/载体位置误差引起的不可忽略的不确定性。此外，由于投影和单位范数归一化，方向矢量误差通常是各向异性的，这促使采用全最小二乘的观点。本文识别了短距离姿态确定中的关键建模区别，基于总方向矢量误差建立了TLS一致性公式，并通过流形上的高斯-牛顿法求解所得的协方差加权正交Procrustes问题。为保留闭式加权Wahba解的效率和数值鲁棒性，我们进一步提出了基于Hessian匹配的标量加权策略，使Wahba公式的Hessian逼近TLS公式，包括用于整体精度的全姿态策略和用于优先考虑选定姿态分量的关注方向策略。最后，我们引入IMU导出的重力矢量作为额外的方向矢量对用于静态初始化，从而得到扩展Wahba和扩展TLS公式。仿真结果表明，与现有基线方法相比，所提出的Hessian匹配加权方法提高了精度和鲁棒性，且重力矢量增强进一步降低了姿态误差，并在锚点可用性有限的情况下提高了解的可获得性。

</details>


### [192] [Optimal Wiener-Filter Solutions for Denoising of Graph Signals on Directed Graphs](https://arxiv.org/abs/2606.07876)
*Chun Hei Michael Chan,Alexandre Cionca,Dimitri Van De Ville*

Main category: eess.SP

TL;DR: 本文针对有向图上的图信号去噪问题，提出了维纳滤波解决方案，并在不同平稳性假设（包括不相关和相关噪声条件）下给出了最优解，通过温度图数据验证了概念可行性。

<details>
  <summary>Details</summary>

Motivation: 图信号处理为经典去噪问题开辟了新途径，但现有方法多针对无向图，缺乏对有向图场景下维纳滤波器的系统研究。

Method: 基于图信号处理框架，在有向图上提出维纳滤波去噪方法，考虑了不相关和相关噪声条件等多种平稳性假设，推导出最优解。

Result: 在不同平稳性假设下（不相关噪声和相关噪声条件）推导出了最优解，并在温度图数据上成功完成了概念验证实验。

Conclusion: 提出的有向图维纳滤波器在多种噪声条件下均能获得最优去噪效果，温度图上的概念验证表明了该方法的有效性。

Original Abstract: Graph signal processing has opened new avenues to the canonical denoising problem in interesting settings. Specifically, here we propose a Wiener-filter solution for graph signals on directed graphs. Under various stationarity assumptions combining uncorrelated and correlated noise conditions, we show optimal solutions, including a successful proof-of-concept for temperature graph.

中文摘要翻译: 图信号处理为经典去噪问题开辟了新途径。具体而言，本文针对有向图上的图信号提出了一种维纳滤波解决方案。在结合不相关和相关噪声条件的多种平稳性假设下，我们给出了最优解，并在温度图上成功完成了概念验证。

</details>


### [193] [A Double Proportionate Sparse Adaptive Filter for Impulsive Noise Environments](https://arxiv.org/abs/2606.08225)
*Mohammad Salman,Falah Ibrahim Al Hassan,Hadi Zayyani,Felipe A. P. de Figueiredo,Hasan Abu Hilal*

Main category: eess.SP

TL;DR: 提出双比例稀疏自适应滤波器(DP-SAF)，通过两个独立对角增益矩阵在单一O(M)更新中同时实现稀疏自适应和脉冲噪声鲁棒性。

<details>
  <summary>Details</summary>

Motivation: 现有稀疏自适应滤波器和脉冲噪声鲁棒算法各自独立发展，缺乏同时满足稀疏性和脉冲噪声鲁棒性需求的算法。

Method: 引入两个独立对角增益矩阵：一个按系数幅度成比例缩放自适应步长，另一个对非活跃抽头施加幅度依赖的零吸引；采用符号误差更新实现脉冲噪声鲁棒性；基于最小范数优化框架推导增益矩阵。

Result: 在伯努利脉冲噪声模型下，DP-SAF在稳态MSD上持续优于竞争算法，同时匹配或超越其收敛速度。

Conclusion: DP-SAF以计算复杂度O(M)高效填补了稀疏自适应与脉冲噪声鲁棒性之间的空白。

Original Abstract: Sparse adaptive filters and impulsive noise robust algorithms have largely been developed along separate tracks, leaving a gap when both properties are needed simultaneously. This letter proposes the double proportionate sparse adaptive filter (DP-SAF), which closes this gap within a single $\mathcal{O}(M)$ update. Two independent diagonal gain matrices are introduced; one scales the adaptation step proportionately to coefficient magnitudes, and the other applies a magnitude-dependent zero-attraction that is strongest for inactive taps. A sign-error update provides robustness against impulsive corruptions. Both gain matrices are derived from a minimum-norm optimization framework. Simulations under a Bernoulli impulsive noise model show that DP-SAF consistently achieves a better steady-state MSD than the competing algorithms while matching or exceeding their convergence speeds.

中文摘要翻译: 稀疏自适应滤波器和脉冲噪声鲁棒算法长期以来沿着两条独立的轨迹发展，导致当需要同时具备两种特性时存在空白。本文提出双比例稀疏自适应滤波器（DP-SAF），以单一的O(M)更新填补了这一空白。该算法引入两个独立的对角增益矩阵：一个按系数幅度成比例缩放自适应步长，另一个对非活跃抽头施加最强的幅度依赖零吸引。符号误差更新提供了对脉冲破坏的鲁棒性。两个增益矩阵均源自最小范数优化框架。在伯努利脉冲噪声模型下的仿真表明，DP-SAF在稳态均方偏差上持续优于竞争算法，同时匹配或超越其收敛速度。

</details>


### [194] [CG-MambaNet: A spatiotemporal framework for cross-patient epileptic seizure prediction using CNN-GCN-Mamba-BiLSTM with event-level clinical evaluation](https://arxiv.org/abs/2606.08226)
*Mufeng Chen,Qi Wu,Bingchao Huang,Xiwen Lai,Zekai Chen,Xinge Ouyang,Tingyao Zhang,Xufang Yang,Quansheng Ren*

Main category: eess.SP

TL;DR: 提出CG-MambaNet，一种针对头皮EEG癫痫发作预测的时空框架，通过可分离卷积提取多尺度频谱特征、图卷积网络建模通道间功能同步、双向Mamba与LSTM建模时序动态，在严格留一患者交叉验证下超越现有跨患者方法，并将误报率降至0.32次/小时。

<details>
  <summary>Details</summary>

Motivation: 现有深度学习方法存在三个局限性：独立建模EEG通道而忽略通道间空间同步、直接在原始时域样本上处理未进行频率分解、多数研究采用的数据划分方式存在患者级信息泄露导致性能估计过于乐观且无法泛化到未见患者。

Method: CG-MambaNet包含三个串行模块：1）深度可分离CNN前端将每个EEG片段分解为多尺度频谱-时间特征，捕获delta到gamma波段动态；2）两层图卷积网络（GCN）配合可学习邻接矩阵捕获通道间功能同步，不依赖特定导联坐标，适用于双极和参考导联；3）双向Mamba编码器接双向LSTM建模长短时程时序动态，最后由两层MLP输出癫痫概率。

Result: 在严格留一患者交叉验证（5个独立随机种子）下，CHB-MIT数据集（n=22）上AUC-ROC达0.8152±0.0176，SIENA数据集（n=6）上达0.7104±0.0261，超越所有已发表的跨患者方法（无域自适应）。事件级评估中，通过持续滤波器合并连续告警窗口，CHB-MIT上误报率降至0.32次/小时。

Conclusion: CG-MambaNet通过频率分解先于空间混合、空间混合先于时间整合的串行层次结构，有效解决了现有方法的三大局限，在跨患者泛化上取得显著优势，且临床告警负担可控。

Original Abstract: Epileptic seizure prediction from scalp EEG is critical for closed-loop neurostimulation therapy. Existing deep-learning methods share two architectural limitations: they model EEG channels independently, neglecting inter-channel spatial synchrony, and process raw time-domain samples without frequency decomposition. A methodological limitation also affects the field: most studies use data splits that permit patient-level information leakage, yielding optimistic estimates that do not generalise to unseen patients. We present CG-MambaNet, a spatiotemporal seizure prediction framework addressing all three limitations. A depthwise separable CNN front-end decomposes each EEG patch into multi-scale spectro-temporal features, capturing delta-to-gamma band dynamics before sequence modelling. A two-layer graph convolutional network with a learnable adjacency matrix captures inter-channel functional synchrony without montage-specific coordinates, applicable to bipolar (CHB-MIT) and referential (SIENA) montages. A bidirectional Mamba encoder followed by a bidirectional LSTM models long- and short-range temporal dynamics, and a two-layer MLP produces the final seizure probability. This serial hierarchy ensures frequency decomposition precedes spatial mixing, which precedes temporal integration. Under strict leave-one-patient-out cross-validation with five independent random seeds, CG-MambaNet achieves AUC-ROC of 0.8152+/-0.0176 on CHB-MIT (n=22) and 0.7104+/-0.0261 on SIENA (n=6), surpassing all published cross-patient methods without domain adaptation. An event-level evaluation framework merging consecutive alarmed windows via a persistence filter reduces false predictions to 0.32 alarms/hour on CHB-MIT, demonstrating clinically meaningful alarm burden.

中文摘要翻译: 从头皮脑电图（EEG）进行癫痫发作预测对于闭环神经刺激治疗至关重要。现有的深度学习方法存在两个架构局限：它们独立建模EEG通道，忽略了通道间的空间同步性，并且在处理原始时域样本时未进行频率分解。该领域还存在一个方法论局限：大多数研究采用的数据划分方式允许患者级信息泄露，从而产生过于乐观的估计，无法泛化到未见患者。我们提出CG-MambaNet，一个解决上述三个局限的时空癫痫发作预测框架。其深度可分离卷积神经网络前端将每个EEG片段分解为多尺度频谱-时间特征，在序列建模之前捕获从delta到gamma频带的动态。一个具有可学习邻接矩阵的双层图卷积网络捕获通道间的功能同步性，无需特定导联坐标，适用于双极导联（CHB-MIT）和参考导联（SIENA）。双向Mamba编码器后接双向长短期记忆网络建模长程和短程时序动态，双层多层感知机输出最终癫痫概率。这一串行层次结构确保频率分解先于空间混合，空间混合先于时间整合。在严格的留一患者交叉验证及五个独立随机种子下，CG-MambaNet在CHB-MIT数据集（n=22）上达到AUC-ROC为0.8152±0.0176，在SIENA数据集（n=6）上达到0.7104±0.0261，超越了所有已发表的跨患者方法且无需域自适应。通过持续滤波器合并连续告警窗口的事件级评估框架将错误预测降至CHB-MIT上0.32次告警/小时，展示了临床意义上的告警负担。

</details>


### [195] [A dual-system approach for epilepsy diagnosis: integrating mamba-Bi-LSTM architecture with SHAP-based verification](https://arxiv.org/abs/2606.08240)
*Mufeng Chen,Jia Xie,Fuchang Luo,Quansheng Ren*

Main category: eess.SP

TL;DR: 本文提出了一种基于深度学习的医疗AI辅助诊断系统，采用Mamba架构与Bi-LSTM结合的主判别系统及SHAP验证系统，用于癫痫智能诊断。实验表明，系统准确率从92.6%提升至98.7%，F1分数从0.895提升至0.992，验证系统平均处理时间仅220ms，整体诊断准确率提升5.1%。

<details>
  <summary>Details</summary>

Motivation: 癫痫发作具有突发性和不可预测性，传统诊断方法依赖医生人工判读脑电图，耗时且依赖经验。为此，本研究旨在开发一种基于深度学习的医疗AI辅助诊断系统，以提升癫痫诊断的准确性和效率，并增强诊断结果的可解释性。

Method: 设计了一个双系统智能诊断框架：(1) 主判别系统采用融合Mamba架构与Bi-LSTM结构的深度学习模型，对多源异构数据（脑电图信号与临床文本数据）进行集成分析；(2) 验证系统通过SHAP方法提供可解释的诊断依据。系统建立跨模态数据库，基于诊断一致性和置信度水平输出结果，高置信度预测可作为自动反馈源优化模型。

Result: 主判别模型准确率从92.6%提升至98.7%，F1分数从0.895提升至0.992，均超过现有最优方法；验证系统反馈集成的平均处理时间仅为220ms，整体诊断准确率提升5.1%。

Conclusion: 本文提出的双系统智能诊断框架在癫痫诊断中取得了显著优于现有方法的性能，验证系统在极短处理时间内有效提升了诊断准确率，验证了该框架在临床辅助诊断中的可行性与优越性。

Original Abstract: This study develops a medical AI-assisted diagnosis system based on deep learning, which provides intelligent diagnostic solutions for epilepsy, a disease that seriously threatens the life and health of patients. Epilepsy has sudden and unpredictable seizures. Traditional diagnostic methods mainly rely on doctors' manual interpretation of EEG, which is time-consuming and dependent by experience. In response to the above challenges, this study designed a dual-system intelligent diagnosis framework, which includes two core components: the main discrimination system and the verification system. The main discrimination system uses a deep learning model that combines the innovative Mamba architecture with the Bi-LSTM structure to integrate and analyze heterogeneous data to achieve extremely high diagnostic accuracy; the verification system provides an explainable diagnostic basis through the SHAP method to enhance the credibility of the results. This system establishes a cross-modal database to realize intelligent analysis of multi-source heterogeneous data-fusion EEG signals and clinical text data for epilepsy. The system outputs results based on diagnostic consistency and confidence levels, and high-confidence predictions can also be used as automatic feedback sources to optimize the model. The experimental results show that the accuracy of the main discriminant model of the intelligent diagnosis system for epilepsy has increased from 92.6% to 98.7% and the F1 score has increased from 0.895 to 0.992, all of which have exceeded the existing optimal methods; the average processing time for verification system feedback integration is only 220 ms, which increases the overall diagnostic accuracy by 5.1%.

中文摘要翻译: 本研究开发了一种基于深度学习的医疗AI辅助诊断系统，为严重威胁患者生命健康的癫痫疾病提供智能诊断方案。癫痫发作具有突发性和不可预测性，传统诊断方法主要依赖医生人工判读脑电图，耗时且依赖经验。针对上述挑战，本研究设计了一个双系统智能诊断框架，包含两个核心组件：主判别系统和验证系统。主判别系统采用融合创新Mamba架构与Bi-LSTM结构的深度学习模型，对异构数据进行集成分析，以实现极高的诊断准确率；验证系统通过SHAP方法提供可解释的诊断依据，增强结果的可信度。本系统建立了跨模态数据库，实现对多源异构数据——融合脑电图信号与临床文本数据——的智能分析，用于癫痫诊断。系统基于诊断一致性和置信度水平输出结果，高置信度预测还可作为自动反馈源以优化模型。实验结果表明，智能诊断系统的主判别模型在癫痫诊断中的准确率从92.6%提升至98.7%，F1分数从0.895提升至0.992，均超过了现有最优方法；验证系统反馈集成的平均处理时间仅为220毫秒，使整体诊断准确率提升了5.1%。

</details>


### [196] [Spatio-Sequential Recurrent Network for 3-D Tunnel Propagation Modeling](https://arxiv.org/abs/2606.08246)
*Jiahao Li,Jingxin Xue,Keqi Ni,Kunyu Wu,Hao Qin,Xinyue Zhang,Xingqi Zhang*

Main category: eess.SP

TL;DR: 本文提出UG-SSRNN网络，联合超分辨横向切片与建模纵向演化，实现对隧道电磁场的三维联合增强，显著降低建模时间并保持高保真度。

<details>
  <summary>Details</summary>

Motivation: 细网格抛物线波动方程模拟保真度高但耗时，限制了实时隧道传播分析；现有机器学习辅助的隧道模型仅提供一维纵向或二维横向细化，缺乏三维联合增强能力。

Method: 提出U形门控时空序贯递归神经网络（UG-SSRNN），采用滑动窗口上下文编码和K层卷积递归主干（含共享传播上下文状态与对角反馈），并设计预测感知上采样头利用历史预测提升切片间一致性，实现横向切片超分辨与纵向演化联合建模。

Result: 在四种隧道截面、未见材料与未见频率测试以及Massif Central隧道验证中，该方法与细网格PWE参考结果高度吻合，显著减少了隧道电磁建模时间。

Conclusion: UG-SSRNN能够高效实现隧道电磁场的三维时空联合重建，在保证高保真度的前提下大幅降低计算成本，具备良好的泛化能力。

Original Abstract: Fine-mesh parabolic wave equation (PWE) simulations are high-fidelity but time-consuming, which limits real-time tunnel propagation analysis and motivates coarse-to-fine reconstruction. Existing machine learning (ML)-assisted tunnel models typically provide only one-dimensional (1-D) longitudinal refinement or two-dimensional (2-D) cross-sectional refinement, rather than joint 3-D enhancement. Motivated by this gap, this letter proposes a U-shaped gated spatio-sequential recurrent neural network (UG-SSRNN), a spatio-sequential reconstruction model for tunnel electromagnetic fields. UG-SSRNN jointly super-resolves transverse slices and models longitudinal evolution. It uses sliding-window context encoding and a K-layer convolutional recurrent backbone with a shared propagation-context state and diagonal feedback. A prediction-aware upsampling head leverages the previous prediction to improve slice-to-slice consistency. Experiments on four tunnel cross sections, unseen-material and unseen-frequency tests, and validation in the Massif Central tunnel show close agreement with fine-mesh PWE references. The proposed approach significantly reduces tunnel electromagnetic modeling time.

中文摘要翻译: 细网格抛物线波动方程（PWE）模拟保真度高但耗时长，这限制了实时隧道传播分析，并促使研究者采用由粗到细的重建方法。现有的机器学习辅助隧道模型通常仅提供一维纵向细化或二维截面细化，而非三维联合增强。受此空白启发，本文提出一种U形门控时空序贯递归神经网络（UG-SSRNN），一种用于隧道电磁场的时空序贯重建模型。UG-SSRNN联合实现横向切片的超分辨与纵向演化的建模。它采用滑动窗口上下文编码和一个带有共享传播上下文状态及对角反馈的K层卷积递归主干。预测感知上采样头利用先前的预测来提升切片间的一致性。在四种隧道截面、未见材料和未见频率测试以及Massif Central隧道中的验证结果表明，该方法与细网格PWE参考结果高度吻合。所提出的方法显著减少了隧道电磁建模时间。

</details>


### [197] [Feedforward Nonlinear Equalizer for Short- to Medium-Reach Wireline Links](https://arxiv.org/abs/2606.08313)
*Kunmo Kim,Paul Kwon,Elad Alon,Ali Niknejad*

Main category: eess.SP

TL;DR: 本文提出了一种前馈非线性均衡器（FFNE）框架，用于短至中距离有线链路，消除了判决反馈均衡器（DFE）的反馈时序瓶颈，同时在特征化工作区域内接近噪声裕度优势。该框架将短窗最大似然序列估计简化为紧凑的二元判决规则，实现了无需发射端编码的低复杂度前馈实现。

<details>
  <summary>Details</summary>

Motivation: 消除DFE中反馈路径带来的时序瓶颈问题，同时保持接近DFE的噪声裕度优势，以适用于短至中距离有线链路的高速数据传输。

Method: 提出FFNE框架，将短窗最大似然序列估计简化为紧凑二元判决规则；针对单后光标NRZ情况建立数学基础、硬件实现、抽头自适应、统计分析及均衡极限；扩展至PAM-4调制和前后光标同时均衡，提出基于模式检测的FFNE（PD-FFNE）。

Result: 对于单后光标NRZ情况，建立了与理想1抽头DFE对比的完整理论体系；窗口长度3的FFNE量化了更长序列窗口的性能-复杂度权衡；PD-FFNE在代表性信道条件下优于传统FFE+DFE基线。

Conclusion: FFNE框架能够在不使用发射端编码和反馈结构的情况下，实现接近DFE性能的均衡效果，并可通过PD-FFNE扩展到更复杂的调制和均衡场景。

Original Abstract: This paper presents a feedforward nonlinear equalizer (FFNE) framework for short- to medium-reach wireline links that removes the feedback-timing bottleneck of decision-feedback equalizers (DFEs) while approaching the noise-margin advantage within a characterized operating region. The proposed FFNE reduces short-window maximum-likelihood sequence estimation to a compact binary decision rule, enabling a low-complexity feedforward realization without transmitter-side encoding. For the single-postcursor NRZ case, the mathematical foundation, hardware implementation, tap adaptation, statistical analysis, and equalization limit relative to an ideal 1-tap DFE are established. A window-length-3 FFNE quantifies the performance-complexity tradeoff of longer sequence windows. The framework is further extended to PAM-4 modulation and simultaneous precursor/postcursor equalization through a pattern-detection-based FFNE (PD-FFNE), which outperforms conventional FFE+DFE baselines under representative channel conditions.

中文摘要翻译: 本文提出了一种前馈非线性均衡器（FFNE）框架，用于短至中距离有线链路。该框架消除了判决反馈均衡器（DFE）的反馈时序瓶颈，同时在特征化工作区域内接近噪声裕度优势。所提出的FFNE将短窗最大似然序列估计简化为紧凑的二元判决规则，实现了无需发射端编码的低复杂度前馈实现。针对单后光标不归零（NRZ）情况，建立了数学基础、硬件实现、抽头自适应、统计分析及与理想1抽头DFE对比的均衡极限。窗口长度为3的FFNE量化了更长序列窗口的性能-复杂度权衡。该框架进一步扩展至PAM-4调制以及基于模式检测的FFNE（PD-FFNE）实现前后光标同时均衡，在代表性信道条件下优于传统FFE+DFE基线方案。

</details>


### [198] [Enhanced Wide-Angle Steering with Multi-Mode Multi-Port Aperture Antenna Arrays](https://arxiv.org/abs/2606.08320)
*Tim Hahn,Dirk Manteuffel*

Main category: eess.SP

TL;DR: 本文提出了一种基于多模多端口天线的宽角度扫描新概念，通过开发孔径辐射体多模多端口天线理论、设计天线阵列并实现先进波束赋形算法，在水平与垂直平面上均实现了扫描角度达±77°（相对于3 dB扫描损耗）且无可见栅瓣的宽角扫描性能。

<details>
  <summary>Details</summary>

Motivation: 传统天线阵列在宽角度扫描时容易出现栅瓣和扫描损耗过大的问题，限制了扫描范围。为了突破这一瓶颈，需要利用更高自由度的天线结构来实现更大角度的无栅瓣扫描。

Method: 首先，基于孔径辐射体发展了多模多端口天线理论；然后，利用多模孔径辐射体设计天线阵列；最后，开发并实现了一种先进的波束赋形算法，充分利用多模多端口天线提供的更高自由度来优化波束指向。

Result: 制造的天线阵列在水平面和垂直面均实现了从法向到±77°的宽角扫描（相对于3 dB扫描损耗），且未出现可见的栅瓣。

Conclusion: 基于多模多端口孔径辐射体的天线阵列能够有效实现宽角无栅瓣扫描，验证了所提理论和算法的可行性与有效性。

Original Abstract: A novel concept for wide-angle scanning is proposed based on multi-mode multi-port antennas. The theory of multi-mode multi-port antennas based on aperture radiators is developed and applied towards the design of an antenna array consisting of multi-mode aperture radiators. An advanced beamforming algorithm is developed and implemented, making use of the higher degrees of freedom available to multi-mode multi-port antennas. The manufactured antenna array is measured and compared to the expected performance. Wide-angle steering up to $\pm77^\circ$ from broadside with respect to a scan loss of $3\,\mathrm{dB}$ is achieved in both the horizontal and vertical plane with no visible grating lobe.

中文摘要翻译: 本文提出了一种基于多模多端口天线的宽角度扫描新概念。发展了基于孔径辐射体的多模多端口天线理论，并将其应用于由多模孔径辐射体组成的天线阵列设计。开发并实现了一种先进的波束赋形算法，充分利用多模多端口天线提供的更高自由度。对制造的天线阵列进行了测试，并将结果与预期性能进行了比较。在水平面和垂直面上，相对于3 dB扫描损耗，均实现了从法向到±77°的宽角扫描，且未出现可见的栅瓣。

</details>


### [199] [A Switching Beamformer for Highly Non-Stationary Environments](https://arxiv.org/abs/2606.08385)
*Manan Mittal,Ryan M. Corey,John R. Buck,Andrew C. Singer*

Main category: eess.SP

TL;DR: 提出通用切换波束形成器（USB），通过集成竞争性序列预测，在不依赖显式变化检测或启发式调参的情况下，自动调整有效记忆长度，在高度非平稳干扰环境中同时实现快速跟踪与稳定抑制，并从理论上证明了其后悔上界。

<details>
  <summary>Details</summary>

Motivation: 传统自适应波束形成器在面对复杂、快速变化的干扰时性能急剧下降：短窗估计方差大，长窗无法适应干扰变化，存在固有的记忆权衡问题。

Method: 将竞争性序列预测集成到波束形成架构中。通过线性转移图隐式维护指数级数量的候选协方差历史，并根据累积输出功率动态重新加权，从而自动调整有效记忆长度。

Result: 理论证明了相对于最优分段平稳协方差oracle的后悔上界；在SwellEx-96数据集上的仿真和实验表明，USB兼具短窗估计器的敏捷性和长窗估计器的精确性。

Conclusion: USB为跟踪高度非平稳场景提供了原则性解决方案，在不牺牲稳态性能的前提下实现了对快速变化干扰的适应性。

Original Abstract: Adaptive beamforming is a cornerstone of array signal processing, yet its performance often collapses in the face of complex, rapidly changing interference. When interferers appear or move unpredictably, conventional estimators encounter a fundamental memory trade-off: short windows enable rapid tracking but suffer from high estimation variance, while long windows provide stable rejection but fail to adapt to shifts. This challenge is resolved by introducing the Universal Switching Beamformer (USB), which integrates competitive sequential prediction into the beamforming architecture. By employing a linear transition diagram, the USB implicitly maintains an exponentially large family of candidate covariance histories and dynamically re-weights them based on their cumulative output power. This mechanism allows the beamformer to automatically vary its effective memory length without explicit change detection or heuristic parameter tuning. A theoretical upper bound is proven on the regret relative to an omniscient oracle that selects the best piecewise-stationary covariance model in hindsight. Extensive simulations and experiments on the SwellEx-96 dataset demonstrate that the USB achieves the agility of short-window estimators and the precision of long-term integration, providing a principled solution for tracking highly non-stationary scenes.

中文摘要翻译: 自适应波束形成是阵列信号处理的基石，但其性能在面对复杂、快速变化的干扰时常常崩溃。当干扰源不可预测地出现或移动时，传统估计器面临一个根本性的记忆权衡：短窗口可实现快速跟踪，但估计方差大；长窗口提供稳定的抑制，但无法适应变化。本文通过将竞争性序列预测集成到波束形成架构中，提出了通用切换波束形成器（USB）来应对这一挑战。通过采用线性转移图，USB隐式维护了一个指数级庞大的候选协方差历史族，并根据其累积输出功率动态地重新加权。该机制使波束形成器能够自动改变其有效记忆长度，无需显式变化检测或启发式参数调优。本文证明了相对于一个全知oracle（在事后选择最佳分段平稳协方差模型）的后悔理论上界。在SwellEx-96数据集上的大量仿真和实验表明，USB兼具短窗估计器的敏捷性和长窗积分估计器的精确性，为跟踪高度非平稳场景提供了一种原则性解决方案。

</details>


### [200] [Mixture-of-Experts Transformer for Automatic Modulation Recognition](https://arxiv.org/abs/2606.09085)
*Jiale Wang,Wupeng Xie,Yaxin Mu,Xin Liu,Zhilong Zhao,Jingwei Zhang*

Main category: eess.SP

TL;DR: 提出MoEformer，一种自适应多尺度混合专家Transformer网络，用于自动调制识别（AMR），通过时间重采样构建多尺度专家视图、输入依赖门控机制动态融合专家、以及RoPE位置编码捕捉时序依赖，在三个公开数据集上取得最优性能。

<details>
  <summary>Details</summary>

Motivation: 现有基于深度学习的AMR方法主要依赖静态多尺度融合策略，无法灵活适应调制信号高度动态的时间变化，缺乏处理时序动态特性的能力。

Method: MoEformer通过时间重采样构建多尺度专家视图，采用输入依赖的门控机制实现动态专家融合，并在Transformer编码器中集成旋转位置编码（RoPE）以同时捕捉局部和全局时间依赖关系，直接处理I/Q信号以保留其时序和相位结构。

Result: 在RadioML2016.10a、RadioML2016.10b和RadioML2018.01A三个基准数据集上，平均识别准确率分别达到63.74%、66.24%和64.22%，优于所有对比基线方法，并在识别性能与模型复杂度之间取得了最优平衡。

Conclusion: MoEformer通过自适应多尺度混合专家架构有效解决了静态融合策略的局限性，在AMR任务上实现了领先性能与良好效率的平衡。

Original Abstract: Automatic Modulation Recognition (AMR) is a key enabling technology for cognitive radio and intelligent spectrum management in next-generation wireless systems. However, current deep learning-based AMR methods predominantly rely on static multi-scale fusion strategies, which lack the flexibility to adapt to the highly dynamic temporal variations of modulation signals. To address this limitation, we propose MoEformer, an adaptive Multi-Scale Mixture-of-Experts Transformer network that directly processes I/Q signals to preserve their temporal and phase structures. Specifically, MoEformer constructs multi scale expert views through temporal resampling, employs an input-dependent gating mechanism for dynamic expert fusion, and integrates Rotary Position Embeddings (RoPE) within Transformer encoders to capture both local and global tem poral dependencies. Comprehensive evaluations on three widely adopted benchmarks (RadioML2016.10a, RadioML2016.10b, and RadioML2018.01A) demonstrate that MoEformer outperforms the competitive baselines, achieving superior average recognition accuracies of 63.74%, 66.24%, and 64.22%, respectively. In addition, the proposed method strikes an optimal trade-off between recognition performance and model complexity.

中文摘要翻译: 自动调制识别（AMR）是下一代无线系统中认知无线电和智能频谱管理的关键使能技术。然而，当前基于深度学习的AMR方法主要依赖静态多尺度融合策略，缺乏适应调制信号高度动态时间变化的灵活性。为解决这一局限性，我们提出MoEformer——一种自适应多尺度混合专家Transformer网络，该网络直接处理I/Q信号以保留其时序和相位结构。具体而言，MoEformer通过时间重采样构建多尺度专家视图，采用输入依赖的门控机制实现动态专家融合，并在Transformer编码器中集成旋转位置编码（RoPE）以同时捕捉局部和全局时间依赖关系。在三个广泛采用的基准数据集（RadioML2016.10a、RadioML2016.10b和RadioML2018.01A）上的全面评估表明，MoEformer优于所有竞争基线方法，分别实现了63.74%、66.24%和64.22%的优越平均识别准确率。此外，所提出的方法在识别性能与模型复杂度之间取得了最优平衡。

</details>


### [201] [RSMA Technique for Multi-User Downlink Single-Waveguide Multi-Pinching Antenna Systems](https://arxiv.org/abs/2606.09095)
*Harsh Raj,Mallena Vardhan,Keshav Singh,Sudip Biswas*

Main category: eess.SP

TL;DR: 本文提出将速率分割多址接入（RSMA）集成到基于单波导多捏合天线的SISO架构中，实现低成本、低复杂度的多用户通信，显著提升和速率、用户公平性并降低误码率。

<details>
  <summary>Details</summary>

Motivation: 捏合天线虽能实现灵活的动态辐射，但如何在单射频链路的低成本架构下有效管理多用户干扰、提升系统性能仍是一个关键问题。

Method: 在单输入单输出（SISO）单波导架构中激活多个捏合天线，沿共享波导辐射公共导引信号，并引入速率分割多址接入（RSMA）来管理多用户干扰。

Result: 仿真结果表明，所提框架能够提升系统和速率、增强用户速率公平性，并实现更低的误码率（BER）。

Conclusion: 将RSMA集成到捏合天线系统中，能够在保持低成本、可扩展特性的同时，有效提升多用户通信性能。

Original Abstract: Pinching antennas have recently emerged as a promising technology for reconfigurable wireless systems due to their ability to dynamically radiate signals from flexible positions along a waveguide. This letter investigates a multi-user communication framework by integrating rate-splitting multiple access (RSMA) into a single-input single-output (SISO) single-waveguide architecture equipped with multiple pinching antennas. Multiple antennas are activated along a shared waveguide to radiate a common guided signal toward distributed users, enabling strong near-field line-of-sight (LoS) links with low hardware complexity and a single radiofrequency (RF) chain. To manage multi-user interference, RSMA is employed within the proposed architecture. Simulation results show that the proposed framework improves system sum-rate, enhances user rate fairness, and achieves lower bit error rate (BER) while preserving the low-cost and scalable characteristics of pinching antenna systems (PASS).

中文摘要翻译: 捏合天线因其能够沿波导从灵活位置动态辐射信号的能力，近期已成为可重构无线系统中一项有前景的技术。本文研究了一种将速率分割多址接入（RSMA）集成到配备多个捏合天线的单输入单输出（SISO）单波导架构中的多用户通信框架。多个天线沿共享波导被激活，以向分布式用户辐射公共导引信号，从而以低硬件复杂度和单个射频（RF）链路实现强近场视距（LoS）链路。为管理多用户干扰，在所提架构中采用了RSMA。仿真结果表明，所提框架在保持捏合天线系统（PASS）的低成本和可扩展特性的同时，提升了系统和速率，增强了用户速率公平性，并实现了更低的误码率（BER）。

</details>


### [202] [Towards Intelligent Wireless Networks: The Synergy of Generative AI and Digital Twins](https://arxiv.org/abs/2606.09113)
*Afan Ali,Ali Arshad Nasir,Naveed Iqbal,Daniel Benevides da Costa*

Main category: eess.SP

TL;DR: 提出了一种生成式AI赋能的数字孪生框架，用于6G无线网络的主动式能量感知优化。通过在数字孪生环境中实时同步信道、移动性、流量和能量信息，系统能够预测并提前应对拥塞、干扰和能量需求，而非被动响应。在无人机辅助的非地面网络场景中，相比反应式基线实现了约69.2%的能耗节省，同时保证了服务质量。

<details>
  <summary>Details</summary>

Motivation: 现有AI辅助数字孪生方法本质上是反应式的——仅在性能下降后调整网络参数，或将生成式AI局限于信道估计等孤立信号级任务，缺乏系统级的主动优化能力。

Method: 构建了生成式AI赋能的数字孪生框架，通过实时同步信道状态、移动性动态、流量条件和能量信息，实现系统级闭环主动架构，联合管理通信、移动性和资源动态，进行自主无线控制。

Result: 在无人机辅助非地面网络场景中，相比反应式基线实现了约69.2%的能耗节省，同时在密集和高移动性条件下保持可靠的服务质量。

Conclusion: 所提框架为更广泛的AI原生6G应用提供了可扩展的基础，涵盖空中平台、自主系统、扩展现实、工业自动化及空天地海一体化基础设施。

Original Abstract: This paper proposes a generative AI (GenAI)-enabled digital twin (DT) framework for proactive and energy-aware wireless optimization in future 6G ecosystems. Most existing AI-assisted DT approaches remain fundamentally reactive, adjusting network parameters only after performance degradation occurs or restricting GenAI to isolated signal-level tasks such as channel estimation. This work adopts a proactive approach. Instead of responding to problems after they appear, the proposed framework continuously synchronizes channel states, mobility dynamics, traffic conditions, and energy information within a real-time DT environment, enabling the system to anticipate congestion, interference, and energy demand before they materialize. The result is a closed-loop proactive architecture that operates at the system level, jointly managing communication, mobility, and resource dynamics for autonomous wireless control. Evaluations on a UAV-assisted non-terrestrial network (NTN) scenario show approximately 69.2\% energy savings over reactive baselines while maintaining reliable quality-of-service (QoS) under dense and mobility-intensive conditions. Beyond this specific scenario, the framework offers a scalable foundation for broader AI-native 6G applications, including aerial platforms, autonomous systems, extended reality (XR), industrial automation, and space-air-ground-sea (SAGS) integrated infrastructures.

中文摘要翻译: 本文提出了一种生成式人工智能赋能的数字孪生框架，用于在未来6G生态系统中实现主动式和能量感知的无线优化。现有的大多数AI辅助数字孪生方法本质上仍是反应式的——仅在性能退化后调整网络参数，或将生成式AI局限于信道估计等孤立信号级任务。本工作采用主动式方法。所提框架不是在问题出现后做出响应，而是在实时数字孪生环境中持续同步信道状态、移动性动态、流量条件和能量信息，使系统能够在拥塞、干扰和能量需求出现之前进行预测。其结果是形成了一个运行在系统级的闭环主动架构，能够联合管理通信、移动性和资源动态，实现自主无线控制。在无人机辅助的非地面网络场景上的评估显示，相比反应式基线实现了约69.2%的能耗节省，同时在密集和高移动性条件下保持可靠的服务质量。超越这一特定场景，该框架为更广泛的AI原生6G应用提供了可扩展的基础，包括空中平台、自主系统、扩展现实、工业自动化以及空天地海一体化基础设施。

</details>


### [203] [Block-Term Decomposition Approach to Blind Multi-trial Functional Ultrasound Unmixing](https://arxiv.org/abs/2606.09264)
*Sofia-Eirini Kotti,Eleftherios Kofidis,Borbála Hunyadi*

Main category: eess.SP

TL;DR: 本文提出了一种基于卷积块项张量分解的模型，用于多试验功能超声数据中潜在神经源的盲分离与血流动力学响应反卷积，并通过交替投影梯度下降优化框架实现。

<details>
  <summary>Details</summary>

Motivation: 功能超声信号通过神经血管耦合间接观测神经活动，因此需要盲分离潜在神经源并反卷积其血流动力学响应，现有方法在此方面存在不足。

Method: 提出一种数据驱动的卷积块项张量分解模型，每个源包含低秩空间图与分段常数神经激活信号，后者与试验和源依赖的血流动力学响应函数进行卷积。采用交替投影梯度下降的约束优化框架求解。

Result: 仿真结果表明，该方法能在不同噪声水平下准确恢复空间图，可靠估计激活时间轮廓，同时证实血流动力学响应函数估计仍是该问题最具挑战性的部分。

Conclusion: 所提模型能有效实现功能超声数据中神经源的盲分离与血流动力学反卷积，但HRF估计仍需进一步改进。

Original Abstract: Functional ultrasound (fUS) has emerged as a powerful neuroimaging modality due to its high resolution in both space and time, low cost and potential portability. Nevertheless, fUS signals provide only indirect observations of neuronal activity through the neurovascular coupling, and hence require the blind separation of latent neuronal sources while also deconvolving their hemodynamic responses. In this work, we propose a data-driven convolutive block-term tensor decomposition-based model for multi-trial fUS measurements, where each source has a spatiotemporal representation comprising a low-rank spatial map and a piecewise-constant neuronal activation signal convolved with a trial- and source-dependent hemodynamic response function (HRF) with a physiologically plausible shape. We propose a constrained optimization framework for the model computation, which consists of alternating projected gradient descent iterations. Simulation results are reported that demonstrate accurate recovery of spatial maps and reliable estimation of activation temporal profiles across various noise levels, while confirming that HRF estimation remains the most challenging part of the problem.

中文摘要翻译: 功能超声（fUS）因其在空间和时间上的高分辨率、低成本及潜在便携性，已成为一种强大的神经影像学模态。然而，fUS信号仅通过神经血管耦合提供对神经活动的间接观测，因此需要盲分离潜在神经源并反卷积其血流动力学响应。本文针对多试验fUS测量，提出了一种基于数据驱动的卷积块项张量分解模型，其中每个源具有时空表示，包括一个低秩空间图和一个分段常数的神经激活信号，该信号与具有生理学合理形状的试验依赖和源依赖的血流动力学响应函数（HRF）进行卷积。我们提出了一个约束优化框架用于模型计算，该框架由交替投影梯度下降迭代组成。仿真结果表明，该方法能在不同噪声水平下准确恢复空间图并可靠估计激活时间轮廓，同时证实HRF估计仍是该问题中最具挑战性的部分。

</details>


### [204] [Wearable Single-Lead ECG Detects Fine-Grained Structural Heart Disease Through Echo-Report Supervision](https://arxiv.org/abs/2606.09332)
*Chenyang He,Qinghao Zhao,Shun Huang,Jun Li,Gongzheng Tang,Hao Zhang,Tong Liu,Zhengkai Xue,Jian Liu,Kangyin Chen,Cheng Ding,Shenda Hong*

Main category: eess.SP

TL;DR: 本文提出AnyECG-Echo框架，利用可穿戴单导联心电图通过对比预训练检测细粒度结构性心脏病，在跨机构外部队列（n=16,621）和总计25,222例验证中展现了高AUROC，并证明模型输出可作为生理数字生物标志物，为大规模人群筛查提供了实用方案。

<details>
  <summary>Details</summary>

Motivation: 结构性心脏病（SHD）是心力衰竭和心血管死亡的主要诱因，但早期检测受限于超声心动图的可及性。尽管单导联心电图通过可穿戴设备已广泛普及，现有AI筛查模型多依赖12导联输入、跨机构泛化能力差，或需要大量特定条件的标注数据集，亟需一种可推广、可解释且适用于单导联心电图的筛查方案。

Method: AnyECG-Echo框架基于对比预训练范式，在单导联心电图与超声心动图报告之间建立关联。创新点包括：（1）在地理独立的16,621例外部队列中评估；（2）覆盖13种细粒度SHD亚型（心肌、腔室、瓣膜、大血管病变）；（3）引入双轴机制可解释性，结合基于电生理学的Shapley归因与定量测量指标间的涌现相关性。

Result: 在总计25,222例验证队列中，模型对高影响亚型表现出高AUROC：左室收缩功能降低（0.866-0.924）、全心扩大（0.877-0.931）、二尖瓣狭窄（0.836-0.906）。模型输出与既定医学生理特征对齐，且输出可作为追踪LVEF和心肌壁厚度等客观指标的生理数字生物标志物。

Conclusion: 可穿戴单导联心电图能够有效检测细粒度结构性心脏病，AnyECG-Echo提供了一种面向人群规模筛查的实用方案，具有跨机构泛化能力和可解释性。

Original Abstract: Structural heart disease (SHD) is a primary driver of heart failure and cardiovascular mortality, yet early detection remains constrained by the limited accessibility of echocardiography. While single-lead electrocardiogram (ECG) is ubiquitous through wearables, existing AI screening models often depend on 12-lead inputs, generalize poorly across institutions, or require massive, condition-specific labeled datasets. Recent work has demonstrated the feasibility of contrastive pre-training between single-lead ECGs and echocardiography reports within a single health system. Here, we present AnyECG-Echo, a framework that advance this paradigm toward clinical translation through three key developments: (1) evaluation in a geographically independent external cohort (n = 16,621); (2) diagnostic coverage of 13 fine-grained SHD subtypes spanning myocardial, chamber, valvular, and great-vessel pathologies; and (3) dual-axis mechanistic interpretability combining electrophysiology-grounded Shapley attribution with emergent correlations to quantitative measurements. Across validation cohorts totaling n = 25,222, the model demonstrated high AUROC for high-impact subtypes, including reduced left ventricular systolic function (AUROC 0.866-0.924), global heart enlargement (0.877-0.931), and mitral stenosis (0.836-0.906). Furthermore, we successfully validated the alignment of model outputs with established medical physiological traits, thereby enhancing interpretability. Notably, we discovered that AnyECG-Echo's outputs function as physiologically grounded digital biomarkers that accurately track objective metrics such as LVEF and myocardial wall thickness. These findings prove that wearable single-lead ECGs can effectively detect fine-grained structural heart disease, offering a practical solution for population-scale screening.

中文摘要翻译: 结构性心脏病（SHD）是心力衰竭和心血管死亡的主要驱动因素，然而其早期检测受限于超声心动图的可及性。尽管单导联心电图通过可穿戴设备已无处不在，但现有的人工智能筛查模型往往依赖12导联输入、跨机构泛化能力差，或需要大规模、特定条件的标注数据集。近期研究已证明了在单一医疗系统内，单导联心电图与超声心动图报告之间进行对比预训练的可行性。本文提出AnyECG-Echo框架，通过三项关键进展将该范式推向临床转化：（1）在地理独立的外部队列（n=16,621）中评估；（2）诊断覆盖13种细粒度SHD亚型，包括心肌、腔室、瓣膜和大血管病变；（3）双轴机制可解释性，结合基于电生理学的Shapley归因与定量测量指标间的涌现相关性。在总计n=25,222的验证队列中，模型对高影响亚型表现出高AUROC，包括左室收缩功能降低（AUROC 0.866-0.924）、全心扩大（0.877-0.931）和二尖瓣狭窄（0.836-0.906）。此外，我们成功验证了模型输出与既定医学生理特征的对齐性，从而增强了可解释性。值得注意的是，我们发现AnyECG-Echo的输出可作为基于生理学的数字生物标志物，准确追踪LVEF和心肌壁厚度等客观指标。这些发现证明，可穿戴单导联心电图可有效检测细粒度结构性心脏病，为人群规模筛查提供了一种实用方案。

</details>


### [205] [Orbital Plane Geometry and Information Conditioning for Doppler-Only LEO Positioning](https://arxiv.org/abs/2606.09496)
*Charles E Thornton*

Main category: eess.SP

TL;DR: 本文提出了一种用于静止接收器利用低轨卫星机会信号进行纯多普勒定位的简化信息模型，将每颗卫星的贡献建模为轨道平面上的加权投影，从几何角度揭示了星座几何结构对定位精度的影响。

<details>
  <summary>Details</summary>

Motivation: 多普勒测量主要提供轨道平面内的位置信息，现有方法缺乏对这种几何约束的简洁数学描述，作者希望建立一个简单的解析框架来理解多卫星几何构型如何影响定位性能。

Method: 将每颗卫星的多普勒测量贡献建模为轨道平面上的加权投影算子，多颗卫星的联合信息矩阵表示为各轨道平面投影算子的加权和；推导了信息矩阵的特征值、条件数和最坏情况克拉美-罗下界的闭式表达式。

Result: 对于两颗卫星的情况，条件数由轨道平面间的二面角和两条链路的相对信息强度决定；蒙特卡洛仿真验证了所提代理模型能够捕获与轨道平面多样性相关的主导条件数变化趋势。

Conclusion: 该工作为理解轨道平面几何多样性在多普勒定位系统中的作用提供了一个简洁的几何框架，有助于指导星座几何设计。

Original Abstract: We study an idealized information model for Doppler-only positioning with low earth orbit (LEO) signals of opportunity from a stationary receiver. Motivated by the observation that Doppler measurements from a satellite pass provide information primarily within the associated orbital plane, we model each satellite contribution as a weighted projection onto that plane. Under this model, the combined information matrix from multiple satellites is a sum of orbital-plane projection operators. Closed-form expressions are derived for the eigenvalues, condition number, and worst-case Cramer-Rao lower bound. For two satellites, the conditioning is governed by the dihedral angle between orbital planes and the relative information strengths of the two links. Monte Carlo evaluation of pass-integrated Doppler Fisher information matrices demonstrates that the proposed surrogate captures the dominant conditioning trends associated with orbital-plane diversity. The results provide a simple geometric framework for understanding the role of constellation geometry in Doppler-only positioning systems.

中文摘要翻译: 我们研究了一种用于静止接收器利用低地球轨道机会信号进行纯多普勒定位的理想化信息模型。受"卫星过顶期间的多普勒测量主要提供与该卫星轨道平面相关的信息"这一观察的启发，我们将每颗卫星的贡献建模为该轨道平面上的一个加权投影。在该模型下，多颗卫星的联合信息矩阵是各轨道平面投影算子的加权和。我们推导了特征值、条件数和最坏情况克拉美-罗下界的闭式表达式。对于两颗卫星的情况，条件数由轨道平面之间的二面角和两条链路的相对信息强度决定。对过顶积分多普勒费希尔信息矩阵的蒙特卡洛评估表明，所提出的代理模型能够捕获与轨道平面多样性相关的主要条件数变化趋势。研究结果为一个理解轨道几何构型在纯多普勒定位系统中作用的简洁几何框架提供了支撑。

</details>


### [206] [Hierarchical Federated Learning for Unsupervised Waveform Classification over Tactical MANETs](https://arxiv.org/abs/2606.09504)
*Charles E. Thornton,Daniel J. Jakubisin*

Main category: eess.SP

TL;DR: 本文提出了一种面向战术MANET环境的层次化联邦学习框架，利用无监督去噪卷积自编码器进行波形分类，通过两阶段聚合协议减少传输开销，并发现信道驱动的非IID子采样可作为隐式正则化器，在中等链路损失下提升无监督表示质量。

<details>
  <summary>Details</summary>

Motivation: 在对抗性战术环境中，分布式射频感知面临无持久回程、无标签、通信链路不可靠等挑战，传统联邦学习假设理想链路或有监督训练，不适用于实际部署的MANET平台。

Method: 提出层次化联邦学习框架：每个节点训练局部去噪卷积自编码器对原始IQ观测进行自监督重构；两阶段聚合协议基于连通性选举中继聚合器（与OLSR多点中继选择一致），在转发至移动服务器代理前压缩簇级模型更新。

Result: 在同等分类性能下，网络内聚合相比中继转发联邦平均减少约12%的传输比特数；信道驱动的非IID子采样作为隐式正则化器，MANET条件下的无监督表示质量达到或超过理想联邦平均；中等链路损失可部分补偿异质网络中的客户端漂移。

Conclusion: 提出的层次化联邦学习框架能在恶劣战术通信条件下有效进行无监督波形分类，且通信链路的中等损失能起到正则化作用，缓解非IID数据带来的客户端漂移问题。

Original Abstract: Distributed radio frequency sensing in contested tactical environments demands collaborative learning across mobile nodes. In ad-hoc networks, learning must occur without persistent backhaul, ground truth labels, or reliable communication links. Traditional federated learning approaches assume either ideal link conditions or supervised training objectives, neither of which holds in practice for deployed MANET platforms. This paper presents a hierarchical federated learning framework for unsupervised waveform classification over tactical MANETs subject to Rayleigh fading, random waypoint mobility, and multi-hop routing loss. Each node trains a local denoising convolutional autoencoder on raw IQ observations without label exchange, learning compact representations through a self-supervised reconstruction objective. A two-stage aggregation protocol elects connectivity-based relay aggregators consistent with OLSR multipoint relay selection, compressing cluster-level model updates before forwarding to a mobile server proxy. Simulation results demonstrate that in-network aggregation reduces attempted transmission bits relative to relay-forward federated averaging by around 12% at equivalent classification performance. Notably, stochastic channel-driven subsampling under non-IID data acts as an implicit regularizer, with both MANET conditions matching or exceeding ideal federated averaging on unsupervised representation quality. This suggests that moderate link loss can partially compensate for client drift in heterogeneous networks. Performance is assessed on analysis of the learned latent embeddings using KMeans normalized mutual information and linear probe accuracy.

中文摘要翻译: 在对抗性战术环境中，分布式射频感知需要跨移动节点进行协作学习。在自组织网络中，学习过程必须在没有持久回程、没有真实标签、也没有可靠通信链路的情况下进行。传统联邦学习方法要么假设理想链路条件，要么假设有监督训练目标，这两种假设对于实际部署的MANET平台均不成立。本文提出了一种层次化联邦学习框架，用于在受瑞利衰落、随机路点移动性和多跳路由损耗影响的战术MANET上进行无监督波形分类。每个节点基于原始IQ观测训练一个局部去噪卷积自编码器，通过自监督重构目标学习紧凑表示，无需交换标签。两阶段聚合协议选举基于连通性的中继聚合器（与OLSR多点中继选择一致），在转发给移动服务器代理之前对簇级模型更新进行压缩。仿真结果表明，在同等分类性能下，网络内聚合相比中继转发联邦平均减少了约12%的尝试传输比特数。值得注意的是，在非独立同分布数据下，随机信道驱动的子采样起到了隐式正则化器的作用，MANET条件下的无监督表示质量达到甚至超过了理想联邦平均。这表明中等链路损失可以部分补偿异质网络中的客户端漂移。性能通过KMeans归一化互信息和线性探针精度对所学潜在嵌入进行分析评估。

</details>


### [207] [Bernoulli Filtering for Multi-Sensor Tracking with Thresholded Measurements](https://arxiv.org/abs/2606.09573)
*Gustav Zetterqvist,Fredrik Gustafsson,Gustaf Hendeby*

Main category: eess.SP

TL;DR: 本文针对传感器检测阈值导致的状态依赖型漏检问题，提出了一种基于伯努利滤波器的递归目标跟踪框架，并在仿真实验中相比传统固定检测概率的伯努利滤波器将GOSPA指标降低了62.4%。

<details>
  <summary>Details</summary>

Motivation: 现有漏检检测框架仅针对单次测量，无法处理递归跟踪问题；在多传感器、杂波和目标存在不确定性的场景下，传统的固定检测概率假设不准确，需要更精确的建模方法。

Method: 采用伯努利滤波器（Bernoulli filter）公式化扩展原有的漏检检测框架，联合处理递归目标跟踪、杂波和目标存在不确定性，并使用伯努利粒子滤波器在二维多传感器跟踪场景中进行评估。

Result: 在包含非线性测量、杂波和检测不确定性的仿真场景中，相比传统固定检测概率的伯努利滤波器，本文方法将广义最优子模式分配（GOSPA）指标降低了62.4%，同时更好地平衡了漏检和虚警。

Conclusion: 通过将检测阈值知识融入伯努利滤波框架，可以显著提升多传感器目标跟踪性能，有效缓解状态依赖型漏检带来的挑战。

Original Abstract: Target tracking is challenging when sensor detection thresholds cause state-dependent missed detections, particularly in multi-sensor scenarios with clutter and uncertain target existence. A recently developed missed detection framework models detection probability as a function of target state, sensor characteristics, and detection threshold, but it is limited to individual measurements and does not address the recursive tracking problem. This work extends the framework using a Bernoulli filter formulation to jointly handle recursive target tracking, clutter, and target existence uncertainty. A Bernoulli particle filter is evaluated in a simulated 2D multi-sensor tracking scenario with nonlinear measurements, clutter, and detection uncertainty. Incorporating accurate detection threshold knowledge reduces the generalized optimal subpattern assignment (GOSPA) metric by 62.4% compared to a conventional Bernoulli filter with fixed detection probability, while better balancing missed detections and false alarms.

中文摘要翻译: 当传感器检测阈值导致状态依赖型漏检时，目标跟踪问题变得具有挑战性，尤其是在存在杂波和目标存在不确定性的多传感器场景中。近期发展的一种漏检检测框架将检测概率建模为目标状态、传感器特性和检测阈值的函数，但该框架仅适用于单次测量，未能解决递归跟踪问题。本文通过伯努利滤波器公式对该框架进行扩展，以联合处理递归目标跟踪、杂波和目标存在不确定性。在包含非线性测量、杂波和检测不确定性的仿真二维多传感器跟踪场景中，对所提出的伯努利粒子滤波器进行了评估。与采用固定检测概率的传统伯努利滤波器相比，融入精确检测阈值知识的方法将广义最优子模式分配（GOSPA）指标降低了62.4%，同时更好地平衡了漏检和虚警。

</details>


### [208] [Throughput Analysis for Near-Field Mobile Communications: Beamfocusing or Caustic Beamforming?](https://arxiv.org/abs/2606.09652)
*Jiannan Wang,Xianghao Yu,Robert Schober*

Main category: eess.SP

TL;DR: 本文针对太赫兹通信中极窄波束与用户移动性导致的频繁波束重构开销问题，提出并分析了一种基于艾里光束的连续弯曲波束（caustic beamforming, CB）方案，推导了CB与波束聚焦（BF）两种策略的吞吐量闭式表达式，建立了切换开销阈值，证明在高移动性太赫兹通信中连续CB方案具有优越性。

<details>
  <summary>Details</summary>

Motivation: 太赫兹频段与超大规模天线阵列的引入使无线通信进入辐射近场区，波束聚焦（BF）虽能实现窄波束聚焦，但用户移动性导致需要频繁进行波束重构，产生巨大的切换开销，严重降低系统可达吞吐量。因此，需要探索一种无需频繁波束跟踪的新型波束赋形方案。

Method: 以艾里光束（Airy beam）作为典范模型，建立分析框架；对BF方案，通过建模连续用户运动导致的散焦惩罚推导吞吐量闭式表达式，并解析确定最优波束驻留时间；对CB方案，分析其吞吐量由信噪比和用户轨迹几何决定，与用户速度无关；最后解析建立切换开销阈值以确定两种波束赋形器的性能交叉点。

Result: 1) 推导了BF和CB两种波束赋形策略的吞吐量闭式表达式；2) 解析确定了BF的最优波束驻留时间，量化了用户速度和切换开销的影响；3) 证明了CB方案的吞吐量与用户速度无关，仅取决于信噪比和轨迹几何；4) 建立了切换开销的解析阈值，该阈值在极高频率下渐近消失，使CB成为高移动性太赫兹通信的首选波束设计范式。

Conclusion: 在极高频率的高移动性太赫兹通信场景下，连续弯曲波束赋形（CB）方案因无需波束跟踪且吞吐量与用户速度无关，优于传统的波束聚焦（BF）方案，应作为首选波束设计范式。

Original Abstract: The migration to the Terahertz (THz) band and the deployment of extremely large antenna arrays (ELAAs) are transitioning wireless communications into the radiative near-field regime, fundamentally evolving conventional angular beam steering to beamfocusing (BF). However, the combination of the extremely narrow beamwidth and the mobility of the users necessitates frequent beamfocusing reconfigurations, incurring a significant switching overhead that degrades the system achievable throughput. In this regard, caustic beamforming (CB) is a promising alternative based on the synthesis of a continuous curved beam, which eliminates the need for beam tracking at the expense of a distributed beamforming gain. By leveraging the Airy beam as a canonical model, this paper develops an analytical framework to compare the throughputs achieved by CB and BF. Our main results include closed-form throughput expressions for both beamforming strategies and a performance boundary for paradigm selection. First, we derive the BF throughput by modeling a defocusing penalty induced by continuous user movement. The optimal beam dwell time that maximizes the throughput is analytically determined, and the impact of user speed and switching overhead on the throughput is quantified. For the CB scheme, we demonstrate that its throughput is determined by the signal-to-noise ratio (SNR) and the geometry of the trajectory of the user, yet invariant to the user speed. Finally, we analytically establish a threshold for the switching overhead to define the crossover point of the achievable throughput of both beamformers. Crucially, this threshold asymptotically vanishes at extremely high frequencies, positioning the continuous CB scheme as the preferred beam design paradigm for high-mobility THz communications.

中文摘要翻译: 向太赫兹（THz）频段的迁移以及超大规模天线阵列（ELAAs）的部署，正将无线通信推向辐射近场区，从根本上将传统的角度波束转向演变为波束聚焦（BF）。然而，极窄的波束宽度与用户的移动性相结合，导致需要频繁地进行波束聚焦重构，产生巨大的切换开销，从而降低系统的可达吞吐量。针对这一问题，焦散波束赋形（CB）是一种有前景的替代方案，该方案基于连续弯曲波束的合成，以分布式的波束赋形增益为代价，消除了波束跟踪的需求。本文以艾里光束作为典范模型，建立了一个分析框架来比较CB和BF所实现的吞吐量。我们的主要结果包括两种波束赋形策略的吞吐量闭式表达式，以及用于范式选择的性能边界。首先，我们通过对连续用户运动引起的散焦惩罚进行建模，推导出BF的吞吐量。解析确定了最大化吞吐量的最优波束驻留时间，并量化了用户速度和切换开销对吞吐量的影响。对于CB方案，我们证明其吞吐量由信噪比（SNR）和用户轨迹的几何形状决定，但与用户速度无关。最后，我们解析地建立了一个切换开销阈值，用以定义两种波束赋形器可达吞吐量的交叉点。至关重要的是，该阈值在极高频率下渐近消失，使得连续CB方案成为高移动性太赫兹通信中首选的波束设计范式。

</details>


### [209] [Jamming-Resilient Sparse Delay-Doppler NOMA: Unitary Precoding, Randomized Active Sets, and Superincreasing Power Allocation](https://arxiv.org/abs/2606.09753)
*Michel Kulhandjian,Hovannes Kulhandjian,Theodoros A. Tsiftsis*

Main category: eess.SP

TL;DR: 提出一种稀疏延迟-多普勒NOMA方案，通过随机子集激活和酉预编码抵抗恶意干扰，利用Marchenko-Pastur条件数理论保证随机酉子矩阵的良好条件性，消除传统OTFS-NOMA的部分频带干扰底噪，并通过超递增功率分配实现低复杂度SIC与最大似然检测等效。

<details>
  <summary>Details</summary>

Motivation: 传统OTFS-NOMA在恶意干扰下存在部分频带干扰导致的BER底噪，且SIC检测存在传播天花板问题，亟需一种能有效抵抗恶意干扰的NOMA方案。

Method: 1) 发射端将用户数据置于延迟-多普勒网格的小随机子集上，通过酉预编码器扩展，每帧根据与接收端共享的伪随机种子重新绘制激活子集；2) 接收端检测并丢弃被干扰的bin，通过最小二乘法恢复稀疏信号，再逐bin进行SIC解码；3) 多用户场景使用超递增功率分配（Merkle-Hellman背包），证明低复杂度SIC与最大似然检测等效；4) 四用户以上场景将用户分成对分配到不相交的bin子集（OMA友好的NOMA规则）。

Result: 1) Hadamard、DFT和Haar随机预编码器BER性能基本一致；2) 闭式BER无干扰导致的底噪；3) 种子泄露不影响系统安全性，BER仍保持在无干扰包络内；4) 八用户时在SNR约20 dB达到BER底噪；5) Rician衰落中干扰独立性对任意K因子成立；6) 蒙特卡洛仿真与理论分析偏差在3 dB内，对模式感知干扰至少40 dB BER比改进，对先知干扰累积增益约24 dB。

Conclusion: 提出了一种对恶意干扰具有强鲁棒性的稀疏延迟-多普勒NOMA方案，通过随机子集激活和酉预编码从根本上消除了干扰导致的BER底噪，并解决了多用户SIC的传播天花板问题，具有实用价值。

Original Abstract: We propose a sparse delay-Doppler NOMA scheme resilient to intentional jamming. The transmitter places user data on a small random subset of delay-Doppler bins, spreads the result through a unitary precoder, and re-draws the active subset per frame from a pseudo-random seed
  shared with the receiver. The receiver detects and discards jammed bins, recovers the sparse signal by least squares, and decodes per bin via SIC. Hadamard, DFT, and Haar-random precoders all yield essentially the same BER, because a Marchenko-Pastur conditioning argument
  controls any random unitary submatrix. The closed-form BER has no jammer-induced floor, unlike the well-known partial-band floor of conventional OTFS-NOMA. The same argument shows that compromising the shared seed does not break the system: random unitary submatrices remain
  well-conditioned, so BER stays within the unjammed envelope. For more than two users we use a superincreasing power allocation (Merkle-Hellman knapsack) and prove the resulting low-complexity SIC matches maximum-likelihood detection exactly, removing the usual SIC propagation
  ceiling. For more than four users we partition them into pairs assigned to disjoint bin subsets; this OMA-friendly NOMA rule reaches floor BER at eight users by SNR around 20 dB. We extend the framework to Rician fading and show the jammer-independence property holds for
  arbitrary Rician K-factor. Monte Carlo simulations track the analytical predictions within 3 dB and show at least a 40 dB BER-ratio improvement against pattern-aware jammers, with about 24 dB of cumulative gain over conventional OTFS-NOMA under oracle jamming.

中文摘要翻译: 我们提出了一种对恶意干扰具有鲁棒性的稀疏延迟-多普勒NOMA方案。发射端将用户数据放置于延迟-多普勒网格的一个小随机子集上，通过酉预编码器对结果进行扩展，并根据与接收端共享的伪随机种子每帧重新绘制激活子集。接收端检测并丢弃被干扰的频域单元，通过最小二乘法恢复稀疏信号，并逐单元使用串行干扰消除（SIC）进行解码。Hadamard、DFT和Haar随机预编码器的误码率（BER）性能基本一致，这是因为Marchenko-Pastur条件数论证控制了任意随机酉子矩阵。与已知的传统OTFS-NOMA部分频带干扰底噪不同，所提方案的闭式BER不存在干扰导致的误差底噪。同样的论证表明，共享种子的泄露并不会破坏系统安全性：随机酉子矩阵仍然保持良好的条件性，因此BER仍保持在无干扰包络范围内。对于两个以上用户，我们采用超递增功率分配（Merkle-Hellman背包），并证明所得的低复杂度SIC与最大似然检测完全等效，消除了常规SIC的传播天花板。对于四个以上用户，我们将用户分成对并分配到不相交的频域单元子集；这种OMA友好的NOMA规则在SNR约为20 dB时，八用户达到BER底噪。我们将该框架扩展到Rician衰落信道，并证明干扰独立性对任意Rician K因子均成立。蒙特卡洛仿真与理论分析结果偏差在3 dB以内，且相对于模式感知干扰至少实现了40 dB的BER比改进，在先知干扰条件下相比传统OTFS-NOMA获得了约24 dB的累积增益。

</details>


### [210] [Adaptive Derivative Estimation via Stein's Unbiased Risk](https://arxiv.org/abs/2606.09829)
*Yonathan Murin,Ali Ozer Ercan*

Main category: eess.SP

TL;DR: 提出SURDE，一种基于Stein无偏风险估计(SURE)的因果FIR导数滤波器，通过软组合多个候选长度滤波器输出来平衡噪声放大与平滑偏差，并证明其极小极大最优性。

<details>
  <summary>Details</summary>

Motivation: 从含噪采样数据中估计导数时，因果FIR滤波器的性能依赖于其长度：短滤波器放大噪声，长滤波器引入平滑偏差。现有方法难以动态平衡这一权衡。

Method: 在每个时间步，通过SURE评估一组候选长度的滤波器代价，并利用指数加权软组合它们的输出。推导了软组合估计量的极小极大最优预言不等式，并以闭式求解最优加权温度，唯一的调参参数是噪声方差。

Result: 数值仿真表明SURDE在一阶导数估计上持续优于ICI和AWVE等自适应方法；对噪声方差误指定具有鲁棒性（4倍范围内仅9%性能下降）；在EuRoC MAV真实数据集上同样优于ICI和AWVE。

Conclusion: SURDE是因果的、计算轻量、只需要噪声方差的粗略估计，且具有理论保证的极小极大最优性，是一种实用的导数估计方法。

Original Abstract: Estimating derivatives from noisy sampled data is fundamental to control, human--computer interaction, and biomedical engineering. Causal FIR derivative filters offer a natural approach for this challenge, yet their performance depend on their length. While short filters amplify noise, long filters introduce smoothing bias. We present SURDE (SURE Derivative Estimator), which addresses this tradeoff at each time step by evaluating a data-driven cost derived from Stein's Unbiased Risk Estimator (SURE) across a bank of candidate lengths and soft-combining their outputs via exponential weighting. We prove a minimax-optimal oracle inequality for the soft-combined estimator and use it to derive the optimal weighting temperature in closed form. Thus, the only tuning parameter for SURDE is the noise variance. Via numerical simulations we show that SURDE consistently outperforms alternative adaptive methods (the Intersection of Confidence Intervals (ICI) rule and the Adaptive Windowing Velocity Estimator (AWVE)) for first-derivative estimation. We further show that \surede{} is robust to noise-variance misspecification (9\% degradation over a $4\times$ range), and that it is superior to ICI and AWVE also over real data scenarios (the EuRoC MAV dataset). SURDE is causal, computationally light, and requires only a rough estimate of the noise variance.

中文摘要翻译: 从含噪采样数据中估计导数在控制、人机交互和生物医学工程中至关重要。因果FIR导数滤波器为解决该问题提供了一种自然方法，但其性能取决于滤波器长度：短滤波器会放大噪声，而长滤波器会引入平滑偏差。我们提出了SURDE（SURE导数估计器），它在每个时间步通过评估基于Stein无偏风险估计（SURE）的数据驱动代价，在一组候选长度中筛选，并通过指数加权软组合它们的输出来解决上述权衡。我们证明了软组合估计量满足极小极大最优的预言不等式，并以此推导出最优加权温度的闭式解。因此，SURDE唯一的调参参数是噪声方差。数值仿真表明，SURDE在一阶导数估计中持续优于其他自适应方法（置信区间交叉规则ICI和自适应窗口速度估计器AWVE）。我们进一步证明SURDE对噪声方差误指定具有鲁棒性（在4倍范围内性能仅下降9%），并且在真实数据场景（EuRoC MAV数据集）中也优于ICI和AWVE。SURDE是因果的、计算轻量，且只需要噪声方差的粗略估计。

</details>


<div id='q-bio.NC'></div>

# q-bio.NC [[Back]](#toc)

### [211] [This is how the Neocortex Learns](https://arxiv.org/abs/2606.08720)
*Randall C. O'Reilly*

Main category: q-bio.NC

TL;DR: 本文提出并论证了唯一满足新皮层学习三个必要条件的框架：基于时间导数的错误驱动预测学习，通过皮质-丘脑回路实现，并由竞争性激酶突触可塑性诱导机制支持。该框架已在Axon神经模拟框架中通过脉冲神经元实现，并在多种认知任务上证明了学习能力。

<details>
  <summary>Details</summary>

Motivation: 寻找一个能够同时满足计算、算法和实现三个层面必要条件的新皮层学习理论框架，弥补现有理论在这三个标准上的不足。

Method: 基于时间导数的错误驱动预测学习框架，采用皮质-丘脑回路实现算法机制，利用竞争性激酶突触可塑性诱导机制在神经化学层面实现，并在Axon神经模拟框架中使用脉冲神经元进行实现和验证。

Result: 该框架在Axon神经模拟框架中通过脉冲神经元实现，并在多种具有挑战性的认知任务上展示了学习能力。

Conclusion: 目前唯一能够同时满足新皮层学习三个必要条件的框架是基于时间导数的错误驱动预测学习，结合皮质-丘脑回路和竞争性激酶突触可塑性机制。

Original Abstract: A sufficient account of how the neocortex learns must meet three criteria: 1. Computationally, it must approximate a powerful, general-purpose learning algorithm known to scale to human-level intelligence; 2. Algorithmically, it must be implementable using known, well-established neural circuits within the neocortex and associated brain structures; 3. Implementationally, there must be a detailed account for how all of the algorithmic mechanisms actually function at a neurochemical level. At present, there is only one framework that meets all of these criteria: error-driven predictive learning via temporal derivatives, driven by corticothalamic circuits, based on competitive kinase synaptic plasticity induction mechanisms. This has been implemented in the Axon neural simulation framework using spiking neurons, and demonstrated to learn across a wide range of challenging cognitively motivated tasks.

中文摘要翻译: 要完整解释新皮层的学习机制，必须满足三个标准：1. 在计算层面，它必须近似于一种强大的、通用型学习算法，且该算法已被证明可扩展到人类水平的智能；2. 在算法层面，它必须能够利用新皮层及相关脑结构中已知的、已充分确立的神经回路来实现；3. 在实现层面，必须详细说明所有算法机制在神经化学水平上是如何实际运作的。目前，只有一个框架能够同时满足所有这些标准：基于时间导数的错误驱动预测学习，由皮质-丘脑回路驱动，并依托竞争性激酶突触可塑性诱导机制。该框架已在Axon神经模拟框架中通过脉冲神经元实现，并在多种具有挑战性的认知任务上证明了其学习能力。

</details>


### [212] [Discovering Functionally Selective Brain Regions with a Deep Topographic Multimodal Model](https://arxiv.org/abs/2606.09770)
*Badr AlKhamissi,Johannes Mehrer,Lara Marinov,Ahmed Abdelaal,Abdulkadir Gokce,Martin Schrimpf*

Main category: q-bio.NC

TL;DR: 本文提出Topo-Omni，一种拓扑多模态模型，通过单一连续的皮层板统一视觉、听觉和语言处理，复现了从感觉到认知系统的空间组织，并可用于发现新的皮层网络。

<details>
  <summary>Details</summary>

Motivation: 现有拓扑模型仅模拟单模态、且每层独立施加空间约束，产生碎片化图谱，无法体现皮层处理流的连续性和跨模态整合，亟需一个统一的多模态空间组织模型。

Method: 在预训练基础模型上微调，引入空间平滑性目标函数，使视觉、听觉和语言/认知处理共享一个连续的in-silico皮层板，形成跨模态的聚类结构。

Result: 模型产生的跨模态聚类与人脑神经影像一致；选择性驱动或抑制某聚类可偏向或损害对应感知，类似人类干预研究；利用模型在硅内筛选发现了新的自然景观和动物网络，并在人类数据中验证。

Conclusion: 单一的空间组织原则可以统一不同模态和处理阶段的大脑表征，为皮层组织提供可检验的假设。

Original Abstract: Nearby neurons in cortex share similar response profiles, producing systematic spatial organization across sensory and cognitive systems. Recent topographic models reproduce aspects of this structure but remain unimodal and spatially constrain each layer separately, yielding fragmented maps that capture neither the contiguity of cortical processing streams nor their integration across modalities. We introduce Topo-Omni, a topographic multimodal model in which visual, auditory, and language/cognitive processing share a single contiguous in-silico sheet. Built by fine-tuning a pretrained foundation model with a spatial smoothness objective, this architecture develops clusters across modalities that are consistent with human neuroimaging, from sensory to cognitive systems. Driving or suppressing a cluster selectively biases or impairs perception, paralleling human intervention studies. Finally, we use our model to screen for novel clusters in-silico and discover new natural landscape and animal networks which we validate in human data. A single spatial principle thus organizes representations across modalities and processing stages, yielding testable hypotheses about cortical organization.

中文摘要翻译: 大脑皮层中相邻的神经元具有相似的反应特征，从而在感觉和认知系统中产生系统的空间组织。最近的拓扑模型再现了这种结构的某些方面，但它们仍然是单模态的，并且对每一层分别施加空间约束，产生碎片化的图谱，既无法捕捉皮层处理流的连续性，也无法捕捉跨模态的整合。我们提出了Topo-Omni，一种拓扑多模态模型，其中视觉、听觉和语言/认知处理共享一个单一的连续体外计算皮层板。通过使用空间平滑性目标微调预训练基础模型，该架构形成了与人类神经影像一致的跨模态聚类，覆盖从感觉到认知系统。选择性驱动或抑制某个聚类会有偏向地调节或损害相应的感知，这与人类干预研究相似。最后，我们利用该模型在硅内筛选新的聚类，发现了新的自然景观和动物网络，并在人类数据中进行了验证。因此，单一的空间原则可组织跨模态和处理阶段的表征，为皮层组织提供可检验的假设。

</details>


<div id='eess.IV'></div>

# eess.IV [[Back]](#toc)

### [213] [The Need for Neural ISP in the Small-Pixel Era: How Shrinking Pixels Push Optics to the Limit and Neural Restoration Pushes Back](https://arxiv.org/abs/2606.07675)
*Jingxi Li,Neerja Aggarwal,Laurent Gudemann,Shivansh Rao,Vishal Vinod,Tom E. Bishop,Ziv Attar*

Main category: eess.IV

TL;DR: 本文提出利用学习型神经ISP替代传统ISP，通过建模并反转点扩散函数（PSF）来校正智能手机长焦相机中因像素尺寸缩小（亚0.5微米）导致的几何像差，从而突破“长焦物理墙”。实验表明，在0.35微米像素下，神经ISP的MTF50达到745 cycles/mm，相比传统ISP提升2.5-3倍，LPIPS从0.244降至0.151，且多帧神经ISP在低信噪比下仍能恢复接近亮光单帧的性能。

<details>
  <summary>Details</summary>

Motivation: 智能手机长焦相机面临“长焦物理墙”：像素间距缩小至亚0.5微米时，光学系统受几何像差限制，分辨率提升收益递减。传统ISP由于采用局部、分阶段处理，且没有底层点扩散函数（PSF）的显式模型，无法消除这些像差，因此需要一种能够建模并反转PSF的学习型ISP来突破这一瓶颈。

Method: 通过受控仿真模拟典型长焦模组，评估五种像素间距配置（0.35-0.75微米），保持光圈按比例缩放以固定每像素信噪比和衍射光斑尺寸，从而隔离几何像差和空间采样效应。对比传统ISP与基于学习的神经ISP（训练时使用底层退化模型），并在低信噪比场景下引入多帧神经ISP进行性能恢复。

Result: 1) 传统ISP随像素缩小改进有限，而神经ISP性能显著提升：0.35微米下MTF50达745 cycles/mm（垂直），分辨率提升2.5-3倍；LPIPS从0.244降至0.151，传统ISP则保持平稳。2) 低信噪比（15 dB每帧突发）下，多帧神经ISP性能接近亮光单帧基线，而多帧传统ISP无实质改进，表明小像素下传统流水线的瓶颈是未校正的PSF模糊而非噪声。

Conclusion: 神经ISP能够通过校正残余光学像差而非依赖日益复杂的光学系统，实现高分辨率长焦模组的设计。这指向一种新的设计哲学：利用学习型ISP将小像素设计转化为净优势，突破传统光学与信号处理的局限。

Original Abstract: Smartphone telephoto cameras are approaching a "telephoto physics wall": as pixel pitches shrink toward sub-0.5 micron, the optics remain limited by geometric aberrations, leading to diminishing returns on resolution. Traditional Image Signal Processors (ISPs) cannot eliminate these aberrations, because they operate through local, stage-wise processing with no explicit model of the underlying point spread function (PSF). We demonstrate how a learning-based Neural ISP for image restoration, trained on the underlying degradations, inverts what stage-wise pipelines cannot, turning small-pixel designs into a net advantage.
  We investigate this through a controlled simulation of a representative telephoto module, evaluating five configurations (0.35--0.75 micron pixel pitch). The aperture is scaled proportionally to keep per-pixel SNR and diffraction spot size fixed, thereby isolating geometric aberration and spatial sampling. While the traditional ISP improves only modestly with smaller pixels, the Neural ISP scales substantially: at 0.35 micron} it reaches 745 cycles/mm MTF50 (vertical), a 2.5--3x resolution improvement over the traditional ISP, and LPIPS improves significantly from 0.244 to 0.151 while traditional results stay comparatively flat. In a low-SNR extension (15 dB per-frame bursts at 0.35 micron), a multi-frame Neural ISP recovers performance close to the bright-light single-frame baseline, whereas a multi-frame traditional ISP shows no meaningful improvement -- indicating that traditional pipelines at small pixels are bottlenecked by uncorrected PSF blur rather than by noise. These results point to a design philosophy in which Neural ISPs enable high-resolution telephoto modules by correcting residual optical aberrations rather than requiring increasingly complex optics.

中文摘要翻译: 智能手机长焦相机正逼近一道"长焦物理墙"：随着像素间距缩小至亚0.5微米，光学系统仍受几何像差限制，导致分辨率提升收益递减。传统的图像信号处理器（ISP）无法消除这些像差，因为它们通过局部的、分阶段的处理方式运行，缺乏对底层点扩散函数（PSF）的显式建模。我们展示了一种基于学习的神经ISP如何进行图像复原，通过训练学习底层退化模型，从而反转了分阶段流水线无法处理的问题，将小像素设计转化为净优势。我们通过对一个代表性长焦模组进行受控仿真来研究这一问题，评估了五种配置（0.35-0.75微米像素间距）。光圈按比例缩放以保持每像素信噪比和衍射光斑尺寸固定，从而隔离几何像差和空间采样效应。传统ISP随像素缩小改进有限，而神经ISP性能显著提升：在0.35微米下，其垂直MTF50达到745 cycles/mm，分辨率较传统ISP提升2.5-3倍，LPIPS从0.244显著改善至0.151，而传统ISP结果保持相对平稳。在低信噪比扩展实验（0.35微米下每帧突发15 dB）中，多帧神经ISP恢复的性能接近亮光单帧基线，而多帧传统ISP则无明显改善——这表明传统流水线在小像素下受限于未校正的PSF模糊而非噪声。这些结果指向一种设计理念：神经ISP通过校正残余光学像差而非要求日益复杂的光学系统，从而实现高分辨率长焦模组。

</details>


### [214] [Multi-planar 2D-U-Net Segmentation of 3D-CT Abdominal Organs augmented by Spatial Occurrence Maps](https://arxiv.org/abs/2606.07717)
*Daria Kern,Negar Chabi,Souraj Adhikary,Andre Mastmeyer*

Main category: eess.IV

TL;DR: 提出了一种基于轻量级2D-U-Net的框架，结合多平面预测与模糊3D空间地图，用于在3D CT扫描中分割五个腹部器官，相比基线模型Dice最高提升约4%。

<details>
  <summary>Details</summary>

Motivation: 在大视野3D CT扫描中分割腹部器官面临计算成本高、器官边界模糊等挑战，现有方法难以兼顾效率与精度，因此需要一种轻量级且能利用空间位置信息的有效分割框架。

Method: 方法分两阶段：第一阶段，用2D-U-Net沿轴向遍历整个扫描，检测腹部体积感兴趣区域，确定五个器官的x-y-z最小/最大范围；第二阶段，在边界内使用多平面2D-U-Net架构，并引入模糊3D空间发生图（spatial occurrence maps）提供解剖位置线索以增强分割精度。

Result: 在来自多个公开来源的80个CT扫描上评估，与未使用空间发生图的相同模型相比，Dice系数最高提升约4%。

Conclusion: 所提出的结合多平面2D-U-Net与模糊3D空间发生图的轻量级框架能有效提升腹部器官分割精度，验证了空间位置线索在粗到细分割策略中的价值。

Original Abstract: This work proposes a lightweight 2D-U-Net-based framework for segmenting five abdominal organs in large field-of-view 3D CT scans. The method combines coarse-to-fine segmentation, predictions from multiple anatomical planes, and additional fuzzy 3D spatial maps that provide anatomical location cues to improve segmentation accuracy. We combine multi-planar 2D-U-Net models augmented by a spatial occurrence map. The approach involves two main stages. First, the abdominal volume of interest region is detected by traversing the whole scan axially with a 2D-U-Net and determining the x-y-z-minimum and -maximum extents of the 5 abdominal organs of interest. Second, we use spatial occurrence maps to enhance our multi-planar 2D-U-net architecture inside the bounds from the former stage. The method is evaluated on 80 CT scans from various public sources. The results show Dice improvements of about 4% at maximum compared to the same model trained without spatial occurrence maps.

中文摘要翻译: 本文提出了一种基于轻量级2D-U-Net的框架，用于在大视野3D CT扫描中分割五个腹部器官。该方法结合了粗到细分割、多个解剖平面的预测以及额外的模糊3D空间图（spatial maps），后者提供解剖位置线索以提升分割精度。我们将多平面2D-U-Net模型与空间发生图（spatial occurrence map）相结合。该方法包含两个主要阶段。首先，通过沿轴向用2D-U-Net遍历整个扫描，确定五个目标腹部器官的x-y-z最小和最大范围，从而检测腹部体积感兴趣区域。其次，在前一阶段确定的边界内，使用空间发生图增强我们的多平面2D-U-Net架构。该方法在来自多个公开来源的80个CT扫描上进行了评估。结果表明，与未使用空间发生图训练的相同模型相比，Dice系数最高提升约4%。

</details>


### [215] [Programmable Silicon Retina on Pixel Processor Array](https://arxiv.org/abs/2606.08370)
*Maciej Lewandowski,Prince Philip,Alexandre Marcireau,Chetan Singh Thakur,André van Schaik,Piotr Dudek*

Main category: eess.IV

TL;DR: 本文首次在 SCAMP-5 像素处理器阵列上实现了多阶段硅视网膜模型，并构建了 GPU 仿真框架。实验表明，该生物启发模型在视频显著预测任务上相比标准 DVS 事件表示降低了 13% 的损失，同时减少了约 47% 的事件率，尽管在绝对强度帧重建上效果较差，但展现了更高效的信息蒸馏能力。

<details>
  <summary>Details</summary>

Motivation: 探索在标准动态视觉传感器（DVS）基础上，额外融入生物启发的空间滤波和增益控制等处理阶段，能否为显著预测等下游任务带来性能优势。

Method: 在 SCAMP-5 像素处理器阵列上首次实现了多阶段硅视网膜模型，并构建了 GPU 仿真框架。采用轻量级约 10 万参数的 FireNet 风格网络，将基于事件的重建任务改造为显著预测任务，评估模型在视频强度重建和视频显著预测上的表现。

Result: 生物启发模型在绝对强度帧重建上效果较差；但在显著预测任务上相比标准 DVS 事件表示降低了 13% 的损失，同时事件率减少了约 47%。

Conclusion: 硅视网膜的"信息蒸馏"机制能够为下游神经网络提供更高效的事件表示，特别适用于带宽受限的边缘计算场景。

Original Abstract: Standard dynamic vision sensors approximate retinal processing by detecting temporal contrast changes, offering high speed and high dynamic range. In this work, we explore whether incorporating additional biologically inspired processing stages - specifically spatial filtering and gain control - can offer advantages for certain downstream tasks such as saliency prediction. We present the first implementation of a multi-stage Silicon Retina model on the SCAMP-5 Pixel Processor Array, along with a GPU-based simulation framework. We evaluate the performance of our model on Video Intensity Reconstruction and Video Saliency Prediction. While the bio-inspired model is less effective at reconstructing absolute intensity frames, it achieves a 13\% reduction in saliency prediction loss in comparison to standard DVS event representation, while reducing the event rate by approximately 47\%. These experiments are obtained using a lightweight $\approx 100$k-parameter FireNet-style network, adapted from event-based reconstruction to saliency prediction. These results suggest that the silicon retina's "information distillation" mechanism can achieve a more efficient representation for downstream neural networks, particularly in bandwidth-constrained edge applications.

中文摘要翻译: 标准动态视觉传感器通过检测时域对比度变化来近似视网膜处理，具有高速度和高动态范围的优势。在这项工作中，我们探索了引入额外的生物启发处理阶段——特别是空间滤波和增益控制——是否能为显著预测等下游任务带来优势。我们首次在 SCAMP-5 像素处理器阵列上实现了多阶段硅视网膜模型，并构建了基于 GPU 的仿真框架。我们在视频强度重建和视频显著预测任务上评估了模型性能。虽然生物启发模型在重建绝对强度帧方面效果较差，但与标准 DVS 事件表示相比，它在显著预测损失上降低了 13%，同时事件率减少了约 47%。这些实验采用了一个轻量级的约 10 万参数的 FireNet 风格网络，该网络从基于事件的重建任务改编而来用于显著预测。这些结果表明，硅视网膜的"信息蒸馏"机制能够为下游神经网络提供更高效的表征，特别适用于带宽受限的边缘应用场景。

</details>


### [216] [X-Palm: Paired Multispectral-to-Smartphone Dataset for Cross-Domain Palmprint Authentication](https://arxiv.org/abs/2606.08437)
*Jamal Seyedmohammadi,Pai Chet Ng,Angelo Genovese,Zhixiang Chi,Jeannie Lee,Konstantinos N. Plataniotis*

Main category: eess.IV

TL;DR: X-Palm是一个跨域掌纹数据集，包含103人（206只手）的6006张图像，提供配对身份数据以桥接受控多光谱采集与非约束移动采集之间的域差距。实验表明现有SOTA模型在该数据集上性能严重下降，而基于X-Palm训练的模型展现出跨域鲁棒性。

<details>
  <summary>Details</summary>

Motivation: 现有掌纹数据集多局限于受控环境，无法捕捉真实世界环境的复合变化，导致掌纹识别技术在从受控注册到非约束认证的部署中面临域差距问题。作者旨在构建一个同时包含受控多光谱采集和非约束智能手机采集的配对身份数据集，以弥合这一域差距。

Method: 构建X-Palm跨域数据集：在受控域使用定制扫描仪进行多光谱掌纹采集，在非约束域由参与者自行拍摄智能手机掌纹图像，同时引入硬件、手部姿势、光照、背景、距离、视角和手掌表面条件等多重变化。基于12种SOTA模型进行跨域性能基准测试。

Result: 12种SOTA模型在受控数据上表现良好，但在X-Palm跨域场景下性能严重下降。而基于X-Palm训练的模型在两个域上均展现出持续鲁棒性。

Conclusion: X-Palm作为首个提供配对身份跨域采集的掌纹数据集，是训练面向真实世界跨域泛化模型的重要资源。

Original Abstract: Palmprint modality offers a privacy-preserving biometric solution, yet its deployment is hindered by the domain gap between controlled enrollment and unconstrained authentication. Existing datasets are largely restricted to controlled setups and fail to capture the compound variability of real-world environments. In this paper, we introduce X-Palm, a cross-domain dataset comprising 6,006 palm images from 103 individuals (206 hands). To the best of our knowledge, X-Palm is the first palmprint dataset providing novel paired-identity acquisition specifically designed to bridge the gap between reliably controlled multispectral enrollment and unconstrained mobile authentication while encompassing a broad spectrum of in-the-wild variability. Unlike existing datasets that focus on single to a few variations, X-Palm addresses the massive modality and environmental shifts encountered in practical deployments by capturing paired data for identities across two distinct domains: (1) a controlled Multispectral Palmprint setting using our custom-developed scanner, and (2) an unconstrained smartphone palmprint setting that is participant-driven, incorporating simultaneous variations in hardware, hand pose, illumination, background, camera-to-hand distance, perspective, and palm surface conditions (e.g., moisture and occlusions). Our extensive benchmarks of 12 SOTA models reveal that while existing methods achieve high performance on controlled data, they experience severe performance collapse on X-Palm. Conversely, models trained on X-Palm demonstrate consistent robustness across domains, positioning X-Palm as a valuable resource for training a model towards real-world, cross-domain generalization. Data access instructions and the related benchmarking codes are publicly available at: https://github.com/X-Palm/X-Palm-2026

中文摘要翻译: 掌纹模态提供了一种保护隐私的生物识别解决方案，然而其部署受到受控注册与非约束认证之间域差距的阻碍。现有数据集大多局限于受控设置，无法捕捉真实世界环境的复合变化。本文介绍了X-Palm——一个包含来自103名个体（206只手）的6006张掌纹图像的跨域数据集。据我们所知，X-Palm是首个提供新颖的配对身份采集的掌纹数据集，专门设计用于桥接可靠受控的多光谱注册与非约束移动认证之间的差距，同时涵盖广泛的野外（in-the-wild）变化。与现有侧重于单一或少数变化的数据集不同，X-Palm通过捕获两个不同域中身份的配对数据，解决了实际部署中遇到的大规模模态和环境变化：（1）使用我们定制开发的扫描仪的受控多光谱掌纹设置，以及（2）由参与者驱动的非约束智能手机掌纹设置，该设置同时包含硬件、手部姿势、光照、背景、相机到手部距离、视角以及手掌表面条件（如湿度和遮挡）的变化。我们对12种SOTA模型的广泛基准测试表明，现有方法在受控数据上表现良好，但在X-Palm上性能严重下降。相反，基于X-Palm训练的模型在两个域上均展现出持续的鲁棒性，使X-Palm成为训练面向真实世界跨域泛化模型的宝贵资源。数据和相关基准测试代码可公开获取：https://github.com/X-Palm/X-Palm-2026

</details>


### [217] [Active Source-free Domain Adaptation in Open-set Medical Image Segmentation via Decomposed Uncertainty and Prototype Discrepancy](https://arxiv.org/abs/2606.08749)
*Jin Yang,Yichi Zhang,Peijie Qiu,Xiaobing Yu*

Main category: eess.IV

TL;DR: 本文首次提出主动源自由开放集域适应（ASFOSDA）方法，用于无源数据访问的开放集医学图像分割，通过主动查询策略和半监督自训练，在跨域体积分割任务上优于现有方法。

<details>
  <summary>Details</summary>

Motivation: 现有主动域适应方法仅限于封闭集场景（源域和目标域类别相同），且需访问源数据，但在临床实践中目标域常包含源域未出现的新类别（开放集），且源数据因隐私法规不可用。本文旨在解决这些限制。

Method: 提出ASFOSDA方法，包含：(1) 主动开放集查询策略，基于类别感知分解不确定性（CDU）和类别无关原型差异（CPD）选择最具信息量的目标样本；(2) 目标精炼自训练策略，为未选中样本生成高质量伪标签，结合标注样本进行半监督训练。

Result: 在跨域开放集体积医学图像分割任务上，该方法优于最先进的域适应方法。

Conclusion: ASFOSDA方法通过主动学习与半监督自训练的结合，有效解决了源数据不可用条件下的开放集医学图像分割问题，为临床应用提供了可行方案。

Original Abstract: Deep learning (DL) methods are challenged to demonstrate robust performance across different segmentation datasets due to domain shifts, but active domain adaptation techniques enhance their generalization performance by querying a few samples from target domains for adaptation training. However in clinical practice, target domains often include private classes of new anatomical structures or pathologies that are not presented in the source data, and existing methods implement closed-set segmentation where source and target domains have the same segmentation classes. Additionally, source data are often inaccessible during adaptation due to strict data privacy regulations. To address these limitations, we propose an Active Source-free Open-set Domain Adaptation (ASFOSDA) method which is the first work to implement active learning for adapting DL models in open-set medical image segmentation without the access to source data. This method employs an active open-set query strategy to select the most informative target samples for training models based on Class-aware Decomposed Uncertainty (CDU) and Class-agnostic Prototype Discrepancy (CPD). CDU measures sample aleatoric uncertainty and model epistemic uncertainty by employing test time augmentation in stochastic processes. CPD measures cross-domain and self-domain discrepancy for selecting diverse samples. Subsequently, to boost the adaptation performance by enhancing training samples, a Target-refined Self-training strategy is proposed to generate high-quality pseudo labels for unselected samples, thus combining them with labeled samples for a semi-supervised training. We evaluated our method on cross-domain open-set volumetric medical image segmentation tasks, and it outperformed state-of-the-art adaptation methods.

中文摘要翻译: 深度学习方法因域偏移而在不同分割数据集上难以展现稳健性能，但主动域适应技术通过从目标域中查询少量样本进行适应训练来增强其泛化能力。然而在临床实践中，目标域常包含源数据中未出现的新解剖结构或病理的私有类别，而现有方法实现的是源域与目标域具有相同分割类别的封闭集分割。此外，由于严格的数据隐私法规，适应过程中通常无法访问源数据。为解决这些限制，我们提出了一种主动源自由开放集域适应（ASFOSDA）方法，这是首个在无源数据访问条件下，将主动学习应用于开放集医学图像分割模型适应的工作。该方法采用主动开放集查询策略，基于类别感知分解不确定性（CDU）和类别无关原型差异（CPD）选择最具信息量的目标样本来训练模型。CDU通过随机过程中的测试时增强来测量样本的偶然不确定性和模型的认知不确定性。CPD通过衡量跨域和域内差异来选择多样化的样本。随后，为增强训练样本以提升适应性能，我们提出了一种目标精炼自训练策略，为未选中的样本生成高质量的伪标签，从而将其与标注样本结合进行半监督训练。我们在跨域开放集体积医学图像分割任务上评估了该方法，其性能优于最先进的域适应方法。

</details>


### [218] [Dynamic XR Rendering Offloading Based on Feature-Based Quality Assessment](https://arxiv.org/abs/2606.09330)
*Sige Liu,Zhe Wang,Lavish Kamal Kumar,Yansha Deng*

Main category: eess.IV

TL;DR: 提出了一种边缘辅助的XR渲染测试平台，通过上下文强盗学习控制器动态优化渲染卸载决策，并设计了一种基于深度特征嵌入和余弦相似度的感知质量评估指标，在真实测试中验证了其有效性与交互性能。

<details>
  <summary>Details</summary>

Motivation: 扩展现实（XR）应用对计算和延迟要求极高，尤其是在实时渲染任务中，现有方法难以在本地渲染与边缘渲染之间实现动态、自适应的卸载决策，同时缺乏对头部运动和异步帧到达下感知质量的鲁棒评估指标。

Method: 1）构建了集成HoloLens 2头显、GPU边缘服务器和基于HOLO Stream SDK的自定义远程渲染工具包的测试平台；2）提出基于深度特征嵌入和余弦相似度的感知评价指标，对空间和时间错位具有鲁棒性；3）设计上下文强盗学习控制器，联合优化感知质量和延迟，实时自适应调整渲染放置决策。

Result: 实验结果表明该测试平台在提供高质量、可交互的XR体验方面具有可行性和有效性，验证了动态渲染卸载和自适应决策机制的实际性能。

Conclusion: 本文提出的边缘辅助XR渲染测试平台能够根据网络条件和延迟约束动态卸载渲染任务，结合鲁棒的感知质量指标和自适应学习控制器，有效提升了XR应用的实时交互体验质量。

Original Abstract: Extended Reality (XR) applications demand intensive computation and low latency, especially for real-time rendering tasks. In this letter, we present an edge-aided XR rendering testbed that dynamically offloads rendering workloads between the XR client and the edge server built upon network conditions and latency constraints. The testbed integrates a Microsoft HoloLens 2 headset, a GPU-enabled edge server, and a customized remote rendering toolkit based on the HOLO Stream SDK, enabling seamless switching between local and edge rendering modes in real time. To overcome the limitations of pixel-level quality metrics under head movements and asynchronous frame arrivals, we propose a perceptual evaluation metric based on deep feature embeddings and cosine similarity, which remains robust to spatial and temporal misalignments. Furthermore, we design a contextual bandit learning controller to adapt rendering placement decisions in real time by jointly optimizing perceptual quality and latency. Experimental results demonstrate the feasibility and performance of our testbed, validating its effectiveness in delivering high-quality and interactive XR experiences.

中文摘要翻译: 扩展现实（XR）应用对计算和低延迟有极高要求，尤其是在实时渲染任务中。本文提出了一种边缘辅助的XR渲染测试平台，该平台能够根据网络条件和延迟约束，在XR客户端与边缘服务器之间动态卸载渲染工作负载。该测试平台集成了Microsoft HoloLens 2头显、支持GPU的边缘服务器以及基于HOLO Stream SDK定制的远程渲染工具包，能够实时无缝切换本地渲染与边缘渲染模式。为克服头部运动和异步帧到达条件下像素级质量指标的局限性，我们提出了一种基于深度特征嵌入和余弦相似度的感知评价指标，该指标对空间和时间错位具有鲁棒性。此外，我们设计了一个上下文强盗学习控制器，通过联合优化感知质量和延迟，实时自适应调整渲染放置决策。实验结果证明了该测试平台的可行性和性能，验证了其在提供高质量、可交互的XR体验方面的有效性。

</details>


### [219] [Vendor-agnostic 4D Phase Contrast MRI: a complete open-source pipeline for velocities, displacement, and strain analysis](https://arxiv.org/abs/2606.09444)
*Marta B. Maggioni,Sabine M. Räuber,Katarina Puš,Bostjan Šimunič,Xeni Deligianni,Regina M. M. Schlaeger,Francesco Santini*

Main category: eess.IV

TL;DR: 本文提出了一个完全开源的4D flow PC-MRI管线，集成了PyPulseq实现的压缩感知加速序列、BART重建和应变分析。该管线在两种西门子MRI系统上验证，将手臂和腿部采集时间从35/80分钟分别缩短至5/11分钟，并成功提取了肌肉应变参数。

<details>
  <summary>Details</summary>

Motivation: 虽然相位对比MRI（PC MRI）在组织运动和应变定量评估中日益普及，但目前仍缺乏标准化、跨厂商的加速采集开源管线，现有方案难以实现不同平台间的可重复性和兼容性。

Method: 开发了一套完全开源的4D flow PC-MRI管线，包括：(1) 基于PyPulseq实现压缩感知加速序列；(2) 基于BART进行图像重建；(3) 应变分析模块；(4) 梯度探测序列确保不同扫描仪方向和厂商间速度符号的正确分配。在两种西门子MRI系统（Prisma和Vida Fit）上，分别对前臂（指浅屈肌，n=9）和大腿（股外侧肌，n=10）在神经肌肉电刺激（NMES）诱发的收缩中进行验证。

Result: 压缩感知将手臂和腿部采集时间分别从35/80分钟缩短至5/11分钟；生成了肌肉应变图和Sigmoid拟合应变曲线，可提取峰值应变、平均应变和建立速率；股外侧肌应变比指浅屈肌应变高约一个数量级（中位峰值应变0.49 vs 0.063，平均应变0.31 vs 0.031）。

Conclusion: 该管线展示了多平台兼容性，为定量肌肉成像提供了一个可重复、开放的研究框架。

Original Abstract: Phase contrast MRI (PC MRI) enables quantitative assessment of tissue motion and strain. Although it is increasingly used, standardized, vendor-agnostic pipelines for accelerated acquisitions remain scarce. We present a fully open-source 4D flow PC-MRI pipeline integrating a compressed sensing-accelerated sequence implemented in PyPulseq, BART-based reconstruction, and strain analysis. Additionally, a gradient probing sequence was developed to ensure correct velocity sign assignment across scanner orientations and vendors. The pipeline was validated across two Siemens MRI systems (3T MAGNETOM Prisma and 3T Vida Fit) in two anatomical applications: forearm (Flexor Digitorum Superficialis, n=9) and thigh (Vastus Lateralis, n=10) during Neuromuscular Electrical Stimulation (NMES)-induced contractions. Compressed sensing reduced acquisition times from 35 and 80 minutes to 5 and 11 minutes for the arm and leg acquisitions, respectively. Muscle strain maps and sigmoid-fitted strain curves enabled extraction of peak strain, mean strain, and buildup rate. Strains in the Vastus Lateralis were approximately one order of magnitude higher than in the Flexor Digitorum Superficialis (median peak strain 0.49 vs. 0.063, mean strain 0.31 vs. 0.031). The pipeline demonstrates multi-platform compatibility and provides a reproducible, open framework for quantitative muscle imaging.

中文摘要翻译: 相位对比MRI（PC MRI）能够实现组织运动和应变的定量评估。尽管其应用日益广泛，但标准化的、跨厂商的加速采集管线仍然稀缺。本文提出了一个完全开源的4D flow PC-MRI管线，集成了基于PyPulseq实现的压缩感知加速序列、基于BART的重建以及应变分析。此外，还开发了一个梯度探测序列，以确保在不同扫描仪方向和厂商间速度符号的正确分配。该管线在两种西门子MRI系统（3T MAGNETOM Prisma和3T Vida Fit）上，针对两个解剖部位进行了验证：前臂（指浅屈肌，n=9）和大腿（股外侧肌，n=10），均在神经肌肉电刺激（NMES）诱发的收缩条件下进行。压缩感知将手臂和腿部采集时间分别从35分钟和80分钟缩短至5分钟和11分钟。肌肉应变图和Sigmoid拟合应变曲线能够提取峰值应变、平均应变和建立速率。股外侧肌的应变比指浅屈肌高约一个数量级（中位峰值应变0.49 vs 0.063，平均应变0.31 vs 0.031）。该管线展示了多平台兼容性，并为定量肌肉成像提供了一个可重复的开放框架。

</details>


<div id='cs.LG'></div>

# cs.LG [[Back]](#toc)

### [220] [Offline Reinforcement Learning for Plasma Control in Nuclear Fusion: Codebase and Benchmark](https://arxiv.org/abs/2606.07550)
*Yang Fu,Haomin Bao,Rohit Sonker,Xiaoyan Hu,Aravind Venugopal,Jeff Schneider,Jiayu Chen*

Main category: cs.LG

TL;DR: 本文提出了RL4F，一个面向核聚变等离子体控制的离线强化学习基准，包含基于DIII-D托卡马克历史数据的闭环评估环境和多项基线比较。实验表明离线模型基RL方法在大多数任务上表现最优，但无单一方法在所有任务中占优。

<details>
  <summary>Details</summary>

Motivation: 离线强化学习为从历史托卡马克数据开发等离子体控制器提供了可行途径，但缺乏标准化的离线RL基准来衡量这一方向的进展，尤其是针对核聚变中多执行器、长时域的实际等离子体控制问题。

Method: 构建了RL4F基准，其评估环境基于DIII-D托卡马克历史放电数据的动力学模型，涵盖旋转、密度、温度和压强四种等离子体剖面跟踪任务，并在统一协议下评估了多种模仿学习和离线RL基线方法。

Result: 离线模型基RL方法在大多数任务上取得了最佳平均性能，但没有任何单一方法在所有任务中占优，凸显了在复杂长时域等离子体控制任务中动力学建模的重要性。

Conclusion: RL4F为核聚变社区和离线RL算法开发提供了一个标准化基准，开源了代码、数据集和评估框架，有助于推动该领域的研究进展。

Original Abstract: Offline reinforcement learning (RL) offers a promising route for developing plasma controllers from historical tokamak data, since online trial-and-error on real devices is costly and risky. However, progress in this direction remains difficult to measure due to the lack of a standardized offline RL benchmark for realistic multi-actuator, long-horizon plasma control problems in nuclear fusion. We introduce RL4F, an Offline Reinforcement Learning Benchmark for Plasma Control in Nuclear Fusion, providing closed-loop evaluation environments and baseline comparisons across four full-profile tracking tasks: rotation, density, temperature, and pressure. The dynamics function underlying the evaluation environment is built from historical discharge data from DIII-D, a real-world Tokamak. We evaluate a broad set of imitation learning and offline RL baselines under a unified protocol. We find that offline model-based RL methods obtain the best average performance on most objectives, although no single method dominates all tasks, highlighting the importance of dynamics modeling in complex, long-horizon plasma control tasks. To foster further research, we open-source the codebase, datasets, and evaluation framework, providing a benchmark not only for the fusion community but also for algorithm development in offline RL.

中文摘要翻译: 离线强化学习为从历史托卡马克数据开发等离子体控制器提供了有前景的途径，因为在真实设备上进行在线试错代价高昂且存在风险。然而，由于缺乏针对核聚变中实际多执行器、长时域等离子体控制问题的标准化离线RL基准，这一方向的进展仍难以衡量。我们提出了RL4F——一个面向核聚变等离子体控制的离线强化学习基准，提供了闭环评估环境以及涵盖旋转、密度、温度和压强四种全剖面跟踪任务的基线对比。评估环境背后的动力学函数基于真实托卡马克DIII-D的历史放电数据构建。我们在统一协议下评估了广泛的模仿学习和离线RL基线方法。实验发现，离线模型基RL方法在大多数目标上取得了最佳平均性能，但没有任何单一方法在所有任务中占优，这凸显了动力学建模在复杂、长时域等离子体控制任务中的重要性。为促进进一步研究，我们开源了代码库、数据集和评估框架，为核聚变社区以及离线RL算法开发提供了一个基准平台。

</details>


### [221] [MedicalRec: Medical recommender system for image classification without retraining](https://arxiv.org/abs/2606.07553)
*Roghayeh Taghavi,Aysa Hasanazde Bashkandi,Amir Ali Bengari,Mohammad Amin Raji,Mohammad Salahi Ardekani,Parisa Mardukhian,Parvaneh Rezaei,Ramin Mousa*

Main category: cs.LG

TL;DR: 本文提出了一个基于Transformer的医学图像分类模型推荐系统（MedicalRec），并构建了包含5000多条记录的数据集MedicalRec-Bench，在多种配置下最高HitRate@100达75.5%，旨在帮助研究者减少试错带来的能耗与浪费。

<details>
  <summary>Details</summary>

Motivation: 现有的机器学习/深度学习模型在医学图像分类任务中面临模型选择困难，研究者往往通过反复试错来寻找最优模型，这个过程消耗大量计算资源和能源，并产生电子垃圾和碳排放。因此需要一种推荐系统来高效地为特定任务推荐合适的分类模型。

Method: 构建了一个包含从3000篇医学图像分类论文中提取的5000多条记录的数据集MedicalRec-Bench，涵盖皮肤癌、肿瘤、伤口、乳腺癌和MRI等分类任务。数据集按特征数量分为四种模式（5/9/11/18个特征）。在此基础上，提出了一个基于Transformer架构的推荐模型MedicalRec，用于为给定任务推荐最优模型。

Result: 在MedicalRec-Bench数据集上的评估中，MedicalRec模型在12个基模型对比中表现优异，最高HitRate@100达到75.5%。

Conclusion: 基于Transformer的MedicalRec推荐系统能够有效地为医学图像分类任务推荐合适的模型，减少盲目试错带来的能源消耗和资源浪费。

Original Abstract: The emergence of machine learning and deep learning has revolutionized the efficiency of diagnostic, therapeutic, and administrative systems in healthcare. However, this rapid adoption has come at the cost of requiring significant computing power and energy consumption, as well as e-waste disposal and carbon emissions. One of the challenges of these models is choosing the right model for classification tasks. To this end, researchers attempt to identify the optimal model using their data through trial and error, which involves energy consumption and waste. The goal of this study is to develop a model-based recommender system for medical image classification. For this purpose, a data set was collected from 3,000 articles in the field of medical image classification. This dataset, publicly available under the name MedicalRec-Bench, contains over 5,000 records of models tested in various tasks, including Skin Cancer Classification, Tumour Classification, Wound Classification, Breast Cancer, and MRI classification. The dataset was evaluated in four different modes, depending on the number of features: MedicalRec I (5 features), MedicalRec II (9 features), MedicalRec III (11 features), and MedicalRec IV (18 features). Collecting all values for the features is challenging due to non-reporting by the authors; hence, the dataset contains significant amounts of missing values. The Medical Recommender System (MedicalRec) is a transformer-based model used for item recommendations in this study. This model achieved remarkable results in the evaluation on the dataset and in the evaluation with 12 base models. This model achieved a maximum HitRate@100 of 75.5%. The dataset and implementations are available through the GitHub link: https://github.com/Ramin1Mousa/MedicalRec

中文摘要翻译: 机器学习和深度学习的出现彻底改变了医疗保健中诊断、治疗和行政系统的效率。然而，这种快速普及是以消耗大量计算能力和能源、以及产生电子垃圾和碳排放为代价的。这些模型面临的挑战之一是如何为分类任务选择合适的模型。为此，研究者们试图通过反复试错的方式利用他们的数据来识别最优模型，这一过程涉及能源消耗和浪费。本研究的目标是开发一个基于模型的医学图像分类推荐系统。为此，我们从3000篇医学图像分类领域的论文中收集了一个数据集。该数据集以MedicalRec-Bench的名称公开，包含超过5000条记录，涉及多种任务中测试的模型，包括皮肤癌分类、肿瘤分类、伤口分类、乳腺癌分类和MRI分类。根据特征数量的不同，该数据集在四种模式下进行了评估：MedicalRec I（5个特征）、MedicalRec II（9个特征）、MedicalRec III（11个特征）和MedicalRec IV（18个特征）。由于作者未报告相关信息，收集所有特征值具有挑战性，因此该数据集中包含大量缺失值。Medical Recommender System（MedicalRec）是一个基于Transformer的模型，在本研究中用于项目推荐。该模型在数据集评估以及与12个基模型的对比评估中取得了显著结果，最高HitRate@100达到75.5%。该数据集和实现代码可通过GitHub链接获取：https://github.com/Ramin1Mousa/MedicalRec

</details>


### [222] [SPIN: Decentralized Swarm Control via Tensorized Policy Coordination](https://arxiv.org/abs/2606.07557)
*Zhaowen Fan*

Main category: cs.LG

TL;DR: 本文提出SPIN框架，通过张量网络压缩和混合神经符号控制，解决边缘平台上多智能体群协同的计算与通信瓶颈，实现线性复杂度推理与低功耗运行。

<details>
  <summary>Details</summary>

Motivation: 传统多智能体群协同在资源受限的边缘平台上面临两个根本瓶颈：联合动作空间呈指数爆炸，以及高延迟通信开销。现有方法难以在低功耗边缘设备上实现实时可扩展的协同控制。

Method: 1) 将群拓扑建模为压缩张量网络，利用矩阵乘积态(MPS)链分解局部多智能体团的联合策略张量，将评估复杂度从指数级O(n^m)降至线性级O(m·n·χ²)。2) 引入解耦的混合神经符号控制管道：离线预训练的局部神经网络将手工几何描述子映射为环境目标度量；运行时边缘代理通过Radon-Nikodým导数作为零样本重要性重加权滤波器，实现瞬时行为适应。

Result: 在离散时间多智能体仿真沙箱中，针对跟踪、分散覆盖和多目标协调三个任务进行验证。定性遥测表明：管道实现稳定的目标导向运动、分散约束下的抗塌缩空间扩展，以及多目标下的结构化子群形成。

Conclusion: SPIN提供了一条数学上有依据的路径，实现了在低功耗边缘设备上可计算、可扩展的群智能协同。

Original Abstract: Decentralized multi-agent swarm coordination on resource-constrained edge platforms remains fundamentally bottlenecked by the exponential scaling of joint action spaces and high-latency communication overhead. This paper introduces the Swarm Policy Interference Network (SPIN) framework, an architectural paradigm that bypasses these limitations by modeling swarm topologies as a compressed tensor network. We factorize the joint policy tensors of local multi-agent cliques into Matrix Product State (MPS) chains, reducing the computational complexity of evaluation from an exponential $O(n^m)$ wall to a strictly linear $O(m \cdot n \cdot χ^2)$ constraint. To bridge local continuous spatial geometry with this discrete algebraic backend without requiring power-intensive online training loops, we introduce a decoupled, hybrid neuro-symbolic control pipeline. Local multi-layered neural networks operate as structural coordination encoders, pre-trained offline to nonlinearly map hand-engineered geometric descriptors into abstract environmental target measures. At runtime, edge agents execute instantaneous behavioral adaptations by applying the Radon-Nikodým derivative directly as a zero-shot importance-reweighting filter. We validate the framework within a discrete-time multi-agent simulation sandbox spanning tracking, decentralized dispersion/area coverage, and multi-goal coordination regimes. Qualitative telemetry demonstrates that the integrated pipeline achieves stable target-directed motion, anti-collapse spatial spreading under decentralized constraints, and structured subgroup formation across multiple targets, providing a mathematically grounded route to tractable, low-power edge swarm intelligence.

中文摘要翻译: 在资源受限的边缘平台上，去中心化多智能体群协同面临着联合动作空间指数级扩张和高延迟通信开销的根本瓶颈。本文提出群策略干扰网络(SPIN)框架，这是一种通过将群拓扑建模为压缩张量网络来绕过这些限制的架构范式。我们将局部多智能体团的联合策略张量分解为矩阵乘积态(MPS)链，将评估的计算复杂度从指数级O(n^m)墙降低到严格的线性级O(m·n·χ²)约束。为桥接局部连续空间几何与此离散代数后端之间的鸿沟，且无需高功耗的在线训练循环，我们引入了一种解耦的混合神经符号控制管道。局部多层神经网络作为结构协调编码器，通过离线预训练将手工设计的几何描述子非线性映射为抽象的环境目标度量。在运行时，边缘代理通过直接应用Radon-Nikodým导数作为零样本重要性重加权滤波器，执行瞬时行为适应。我们在一个离散时间多智能体仿真沙箱中验证了该框架，涵盖跟踪、去中心化分散/区域覆盖以及多目标协调等场景。定性遥测表明，该集成管道实现了稳定的目标导向运动、去中心化约束下的抗塌缩空间扩展，以及跨多个目标的结构化子群形成，为在低功耗边缘设备上实现可计算的群智能提供了一条数学上有依据的路径。

</details>


### [223] [Boundary Variance Inflation Causes Acquisition Bias in Gaussian Processes](https://arxiv.org/abs/2606.07561)
*Maria Bånkestad,Sanna Jarl,Jens Sjölund*

Main category: cs.LG

TL;DR: 本文揭示了有界域上平稳核高斯过程边界处后验方差膨胀的根本原因——核相关邻域在边界处被截断，并系统分析了这种几何失真如何导致贝叶斯优化中的三类采集偏差，最后提出了一种与目标函数无关的采集行为诊断工具。

<details>
  <summary>Details</summary>

Motivation: 边界引起的后验方差膨胀是地统计学中长期被认识到的伪影，也是贝叶斯优化中过度探索的来源，但其成因和影响尚未被充分探索。本文旨在揭示其根本机制并量化其对采集函数的影响。

Method: 从几何机制出发，论证核相关邻域在域边界截断是导致观测无关失真的根本原因，且该失真随维度增加而加剧。通过方差最大化、负积分后验方差和期望预测信息增益三类采集函数，分析边界失真的表现形式，并提出一种无需目标函数的采集选择剖面诊断工具。

Result: 方差最大化采集将选择集中在角落；负积分后验方差和期望预测信息增益将选择向内移至轴对齐的内部壳层。这些模式完全独立于目标函数，表明采集行为可能被核几何主导而非任务特定的不确定性。

Conclusion: 边界引起的采集偏差源于核相关邻域截断这一简单的几何机制，该偏差随维度增加而恶化，且会主导采集行为。提出的函数无关诊断工具可适用于任意采集函数、核函数和有界域几何。

Original Abstract: Gaussian processes with stationary kernels on bounded domains exhibit inflated posterior variance near the boundary. Despite being a long-recognized artifact in geostatistics and a source of over-exploration in Bayesian optimization, the causes and effects of boundary-induced acquisition bias are underexplored. We trace the root cause to a simple geometric mechanism: the truncation of the kernel correlation neighborhood at the domain boundary creates an observation-independent distortion that worsens with dimensionality. We show how this distortion manifests across three acquisition classes: variance maximization concentrates selections at the corners, whereas negative integrated posterior variance and expected predictive information gain move selections inward to axis-aligned interior shells. These patterns arise without reference to any objective function, meaning that acquisition behavior can be dominated by kernel geometry rather than the desired task-specific uncertainty. To quantify this, we introduce a function-free selection-profile diagnostic for arbitrary acquisitions, kernels, and bounded-domain geometries.

中文摘要翻译: 具有平稳核的高斯过程在有界域上会表现出边界附近后验方差膨胀的现象。尽管这是地统计学中一个长期被认识的伪影，也是贝叶斯优化中过度探索的一个来源，但边界引起的采集偏差的成因和影响尚未得到充分探索。我们将根本原因追溯到一个简单的几何机制：核相关邻域在域边界处被截断，产生了一种与观测无关的失真，且该失真随维度增加而加剧。我们展示了这种失真如何在三类采集函数中表现出来：方差最大化将选择集中在角落，而负积分后验方差和期望预测信息增益则将选择向内移至轴对齐的内部壳层。这些模式的出现完全不依赖于任何目标函数，意味着采集行为可能被核几何所主导，而非期望的任务特定的不确定性。为了量化这一现象，我们提出了一种无需目标函数的选择剖面诊断工具，适用于任意采集函数、核函数和有界域几何。

</details>


### [224] [Emergence via Phase Transitions: Mechanism Landscapes and Universal Convergence Across Complex Systems](https://arxiv.org/abs/2606.07563)
*Truong Xuan Khanh*

Main category: cs.LG

TL;DR: 提出层级涌现框架（HEF），将涌现建模为机制景观中的相变，通过临界能量阈值Ec区分探索与收敛机制。在111个模算术变换器实验中发现权值范数在grokking前系统性峰值、精度曲线塌缩为tanh扭结等经验指纹，验证了该框架的预测。

<details>
  <summary>Details</summary>

Motivation: 解释跨机器学习、生物学和物理学中独立演化系统趋向于相似高层结构这一普遍现象（如grokking电路收敛、进化路径重现代谢解、重整化流趋近固定点），为这类收敛现象提供一个统一的数学框架。

Method: 提出层级涌现框架（HEF），将涌现建模为受热力学和信息论约束的机制景观中的相变。引入临界能量阈值Ec，区分探索机制竞争阶段与唯一最小成本机制主导的收敛阶段。在结构假设下证明物理可行性、严格度量收缩以及独立于初始条件收敛到唯一不动点，并通过有效信息（EI）和机制竞争熵连接因果涌现结构。

Result: 在111个模算术变换器的延迟泛化（grokking）实验中，92%的实验中观察到权值范数在grokking前系统性达到峰值的Ec转变指纹；归一化精度曲线以R²=0.93塌缩为tanh kink，符合朗道-金兹堡普适类；所有grokked模型收敛到0.9745±0.014，不受初始化、权重衰减或训练分数影响（ANOVA p>0.13）。

Conclusion: HEF并非作为涌现的普适理论提出，而是为跨复杂系统的收敛现象研究提供一个可证伪的数学框架。

Original Abstract: Across machine learning, biology, and physics, independently evolving systems often converge toward strikingly similar high-level structures despite radically different microscopic details. Grokking circuits converge across random seeds, evolutionary lineages rediscover similar metabolic solutions, and renormalization flows approach common fixed points. We propose the Hierarchical Emergence Framework (HEF) as a candidate universality framework for such convergence phenomena. HEF models emergence as a phase transition in a mechanism landscape constrained by thermodynamic and information-theoretic laws. The framework introduces a critical energy threshold Ec separating an exploration regime with competing mechanisms from a convergence regime governed by a unique minimum-cost mechanism. Under structural assumptions, we prove physical feasibility, derive strict metric contraction, and establish convergence toward a unique fixed-point representation independent of initial conditions. We further connect this convergence structure to causal emergence through Effective Information and mechanism competition entropy. To test the framework, we study delayed generalization ("grokking") in modular arithmetic transformers across 111 experiments. We identify a reproducible empirical fingerprint of the Ec transition: the weight norm peaks systematically before grokking in 92% of runs. Normalized accuracy curves collapse onto a tanh kink (R^2=0.93) consistent with a Landau-Ginzburg universality class, and all grokked models converge to 0.9745+/-0.014 regardless of initialization, weight decay, or training fraction (ANOVA p>0.13). HEF is not presented as a universal theory of emergence, but as a falsifiable mathematical scaffold for studying convergence phenomena across complex systems.

中文摘要翻译: 在机器学习、生物学和物理学中，独立演化的系统尽管微观细节截然不同，却常常趋同于高度相似的高层结构。Grokking电路在不同随机种子下收敛，进化谱系重新发现类似的代谢解决方案，重整化流趋近共同的固定点。我们提出层级涌现框架（HEF）作为这类收敛现象的候选普适性框架。HEF将涌现建模为受热力学和信息论约束的机制景观中的相变。该框架引入一个临界能量阈值Ec，将机制竞争探索阶段与唯一最小成本机制主导的收敛阶段区分开来。在结构假设下，我们证明了物理可行性、严格度量收缩，以及独立于初始条件收敛到唯一不动点表示。我们进一步通过有效信息（Effective Information）和机制竞争熵将这一收敛结构与因果涌现联系起来。为了测试该框架，我们在111个实验中研究了模算术变换器的延迟泛化（"grokking"）。我们识别出一个可复现的Ec转变经验指纹：在92%的运行中，权值范数在grokking之前系统性地达到峰值。归一化精度曲线塌缩为一个tanh扭结（R²=0.93），符合朗道-金兹堡普适类，并且所有grokked模型收敛到0.9745±0.014，不受初始化、权重衰减或训练分数的影响（ANOVA p>0.13）。HEF并非作为涌现的普适理论提出，而是作为研究跨复杂系统收敛现象的一个可证伪的数学框架。

</details>


### [225] [STARIXNet: Multivariate and Multi-attribute Deep Learning Approach to Real-Time Resource Allocation in Cloud Platforms](https://arxiv.org/abs/2606.07565)
*Ahmed Abdulaal,Maruf Aytekin,Thilaga kumaran Srinivasan,Tomer Lancewicki*

Main category: cs.LG

TL;DR: STARIXNet是一种轻量级神经网络，通过捕获多系统指标间的时空关系来指导微服务的多维资源扩缩容决策，在沃尔玛生产环境中实现了10%-50%的成本节约，并提升了服务稳定性。

<details>
  <summary>Details</summary>

Motivation: 现有云平台微服务扩缩容方案存在三个局限：①局限于单变量空间（仅依赖CPU使用率）；②将问题视为纯预测任务，忽视低估风险和系统响应延迟；③替代方案计算复杂，难以大规模实时部署。

Method: STARIXNet是一个轻量级神经网络，在多元空间中捕获多个系统指标间的时空关系，建模季节性(S)、时间性(T)、自回归整合(ARI)和外生变量(X)模式，然后通过聚合策略做出扩缩容决策，优先保障服务稳定性，其次是成本效率，最后才是原始预测精度。

Result: 在沃尔玛关键生产微服务中部署，实现了10%到50%的实际成本节约，同时带来服务稳定性和客户体验提升的无形收益。

Conclusion: STARIXNet通过在多元空间中建模多个准依赖属性的时空关系，实现了更优的微服务扩缩容决策，在成本节约和服务稳定性之间取得了更好平衡。

Original Abstract: Intelligent scaling of microservices in cloud platforms is crucial for mitigating escalating compute costs while avoiding service disruptions. Current solutions are limited to the univariate space, typically focusing on CPU usage alone to drive scaling decisions. Moreover, they address the problem as a purely forecasting task, focusing on prediction precision while neglecting the greater risks of underestimation and delays in system responsiveness. Alternative solutions are computationally complex, making them impractical for large-scale, real-time deployments. To address these challenges, we present STARIXNet, a lightweight neural network that guides resource allocation decisions in the multivariate space by capturing spatio-temporal relationships among multiple system metrics. STARIXNet models multiple quasi-dependent attributes, in particular the (S)easonal, (T)emporal, (A)uto-(R)egressive (I)ntegrated, and e(X)ogenous patterns, then implements an aggregation policy to finalize scaling decisions, prioritizing service stability, followed by cost-efficiency, over raw forecast accuracy. We empirically demonstrate the performance of STARIXNet by benchmarking against existing solutions in real-world settings. STARIXNet is deployed for critical production microservices at Walmart achieving tangible savings ranging from 10\% to 50\%, in addition to intangible benefits through improved service stability and customer experience.

中文摘要翻译: 云平台中微服务的智能扩缩容对于缓解日益增长的计算成本同时避免服务中断至关重要。现有方案局限于单变量空间，通常仅依赖CPU使用率来驱动扩缩容决策。此外，它们将问题视为纯粹的预测任务，关注预测精度而忽视低估风险和系统响应延迟的更大风险。替代方案计算复杂，使其难以应用于大规模实时部署。为应对这些挑战，我们提出了STARIXNet，一种轻量级神经网络，通过捕获多个系统指标间的时空关系来指导多元空间中的资源分配决策。STARIXNet对多个准依赖属性进行建模，特别是季节性(S)、时间性(T)、自回归整合(ARI)和外生变量(X)模式，然后实施聚合策略以最终确定扩缩容决策，优先保障服务稳定性，其次是成本效率，最后才是原始预测精度。我们通过在真实环境中对现有方案进行基准测试，实证证明了STARIXNet的性能。STARIXNet目前已部署在沃尔玛的关键生产微服务中，实现了10%到50%的实际成本节约，并通过改进的服务稳定性和客户体验带来了无形收益。

</details>


### [226] [TriHead-GAN: A Generative Adversarial Network with Triple-Head Discriminator for Carbon Emission Time Series Generation](https://arxiv.org/abs/2606.07569)
*Zesen Wang,Lijuan Lan,Yonggang Li,Chunhua Yang*

Main category: cs.LG

TL;DR: TriHead-GAN提出了一种三头判别器的Transformer对抗框架，用于生成高质量的城市级碳排放时间序列数据，解决了现有生成模型在保留跨变量依赖和步态波动方面的不足。

<details>
  <summary>Details</summary>

Motivation: 城市级高频率碳排放监测数据极度稀缺，严重限制了深度学习模型的应用。现有GAN和扩散生成器虽然能匹配边际分布统计量，但无法充分保留CO₂与共排放污染物及气象因素之间的跨变量相关性，且会破坏大气测量数据的一阶差分统计量，生成序列缺乏真实的步态波动。

Method: 提出TriHead-GAN框架：一个基于Transformer的对抗生成模型，其核心是三重头判别器，分别通过Wasserstein判别器监督分布真实性、通过无泄漏回归监督跨变量依赖性、通过相邻差分预测监督步态时间平滑性。生成器结合了全局自注意力与局部时序卷积、逐步噪声注入以及对抗平滑损失（匹配一阶差分统计量）。

Result: 在自收集的长沙碳排放数据集、两个公开碳数据集（中国、美国）以及ETTh1基准上的实验表明，TriHead-GAN在绝大多数设置下优于主流基线方法，生成的合成窗口在低资源碳排放监测场景中有效提升了下游预测精度。

Conclusion: TriHead-GAN通过三重判别器协同监督联合分布的三个互补方面，有效生成了保留跨变量依赖和真实波动特征的高质量碳排放时间序列，为数据稀缺场景下的碳排放监测提供了可行的数据增强方案。

Original Abstract: Accurate carbon emission monitoring is critical for climate policy and emerging regulatory mechanisms such as the EU Carbon Border Adjustment Mechanism, yet city-level high-frequency monitoring data remain extremely scarce, severely limiting data-hungry deep learning models. Time series generation is a natural remedy, but existing GAN and diffusion-based generators often provide limited explicit supervision for the domain structure of carbon emission data: they may match marginal distributional statistics while insufficiently preserving cross-variable correlations between CO$_2$ and co-emitted pollutants and meteorological factors, and tend to collapse the first-difference statistics of atmospheric measurements, producing sequences that are smooth on average but lack the realistic step-wise variability of the underlying signals. We propose TriHead-GAN, a Transformer-based adversarial framework whose triple-head discriminator jointly supervises three complementary aspects of the joint distribution: distributional authenticity via a Wasserstein critic, cross-variable dependency via leakage-free regression of the target variable, and step-wise temporal smoothness via adjacent-difference prediction. The generator combines global self-attention with local temporal convolution, per-step noise injection, and an anti-smoothing loss that matches first-difference statistics. Experiments on the self-collected Changsha Carbon dataset, two public carbon datasets (China, US), and the ETTh1 benchmark show that TriHead-GAN achieves favorable performance over mainstream baselines on the vast majority of settings, and that the resulting synthetic windows improve downstream forecasting accuracy in low-resource carbon monitoring scenarios.

中文摘要翻译: 准确的碳排放监测对于气候政策和新兴监管机制（如欧盟碳边境调节机制）至关重要，然而城市级高频率监测数据仍然极度稀缺，严重限制了数据饥渴型深度学习模型。时间序列生成是一种自然的补救措施，但现有的GAN和扩散生成器通常无法为碳排放数据的领域结构提供足够的显式监督：它们可能匹配边际分布统计量，但未能充分保留CO₂与共排放污染物及气象因素之间的跨变量相关性，并且倾向于破坏大气测量数据的一阶差分统计量，生成在平均意义上平滑但缺乏底层信号真实步态波动的序列。我们提出TriHead-GAN，一种基于Transformer的对抗框架，其三重头判别器联合监督联合分布的三个互补方面：通过Wasserstein判别器监督分布真实性、通过无泄漏目标变量回归监督跨变量依赖性、通过相邻差分预测监督步态时间平滑性。生成器结合了全局自注意力与局部时序卷积、逐步噪声注入以及匹配一阶差分统计量的抗平滑损失。在自收集的长沙碳排放数据集、两个公开碳数据集（中国、美国）以及ETTh1基准上的实验表明，TriHead-GAN在绝大多数设置下取得了优于主流基线方法的性能，且生成的合成窗口在低资源碳排放监测场景中提升了下游预测精度。

</details>


### [227] [Enabling KV Caching of Shared Prefix for Diffusion Language Models](https://arxiv.org/abs/2606.07571)
*Younghun Go,Jaehoon Han,Changyong Shin,Chuk Yoo,Gyeongsik Yang*

Main category: cs.LG

TL;DR: 提出 bicache，首个针对扩散语言模型（DLM）中共享前缀的 KV 缓存技术，通过动态识别浅层安全深度复用缓存，在不造成精度崩溃的前提下显著提升服务吞吐量（36.3%-98.3%）。

<details>
  <summary>Details</summary>

Motivation: 现有为大语言模型（LLM）设计的 KV 缓存技术假设 KVs 一旦计算即保持不变，但扩散语言模型（DLM）的双向注意力机制导致任何 token 更新都会动态改变整个上下文及对应 KVs，直接复用 LLM 缓存会导致模型精度崩溃至接近零。因此需要专门为 DLM 设计共享前缀 KV 缓存技术以实现高吞吐服务。

Method: 基于关键观察——共享前缀 KVs 在浅层保持稳定可复用，而浅层深度取决于每个请求中共享前缀 token 的比例——动态识别可复用共享前缀 KVs 的安全层深度，在该深度范围内复用缓存，消除冗余计算。

Result: 相比现有技术，bicache 将服务吞吐量提升 36.3%-98.3%，且无精度崩溃（精度差异仅 0-1.8%）。

Conclusion: bicache 有效解决了 DLM 中共享前缀 KV 缓存的关键挑战，在保持模型精度的前提下大幅提升服务吞吐量，为高吞吐 DLM 服务解锁了实用技术路径。

Original Abstract: Key-value (KV) caching for shared prefixes is essential for high-throughput large language model (LLM) serving, but it faces critical challenges in emerging diffusion language models (DLMs). In DLMs, bidirectional attention means that updating any token dynamically alters the entire context and its corresponding KVs. Thus, existing caching techniques developed for LLMs, which assume that KVs remain invariant once computed, corrupt the shared prefix KVs. Our experiments show that applying these techniques to DLMs causes model accuracy to collapse to near zero.
  To unlock high-throughput DLM serving, we propose bidirectional prefix caching, bicache, the first KV caching technique for shared prefixes in DLMs. bicache is designed based on key observations from our comprehensive analysis: shared prefix KVs remain stable and reusable in shallow layers, while the depth of shallow layers depends on the fraction of shared prefix tokens in each request. Thus, bicache dynamically identifies a safe layer depth for reusing shared prefix KVs and eliminates redundant computation. Evaluations demonstrate that bicache significantly improves serving throughput by 36.3%-98.3% compared to existing techniques without accuracy collapse (only 0-1.8% difference).

中文摘要翻译: 共享前缀的键值（KV）缓存对于高吞吐量的大语言模型（LLM）服务至关重要，但在新兴的扩散语言模型（DLM）中面临严峻挑战。在 DLM 中，双向注意力机制意味着更新任意 token 会动态改变整个上下文及其对应的 KVs。因此，为 LLM 开发的现有缓存技术（假设 KVs 一旦计算即保持不变）会破坏共享前缀 KVs。我们的实验表明，将这些技术应用于 DLM 会导致模型精度崩溃至接近零。为解锁高吞吐量的 DLM 服务，我们提出了双向前缀缓存 bicache，这是首个针对 DLM 中共享前缀的 KV 缓存技术。bicache 基于我们全面分析的关键观察设计：共享前缀 KVs 在浅层中保持稳定且可复用，而浅层的深度取决于每个请求中共享前缀 token 的比例。因此，bicache 能够动态识别复用共享前缀 KVs 的安全层深度，并消除冗余计算。评估结果表明，与现有技术相比，bicache 在无精度崩溃的情况下（仅 0-1.8% 的差异），将服务吞吐量显著提升了 36.3%-98.3%。

</details>


### [228] [When Should an AI Scientist Stop? Verifiable Experiment Steering and Refusal for Autonomous Discovery](https://arxiv.org/abs/2606.07576)
*Neel Tushar Shah,Manglam Kartik*

Main category: cs.LG

TL;DR: 提出CARTOGRAPH验证层，通过选择、解析、拒绝三大机制实现AI科学家的实验引导与结果验证，在多个测试平台和A-Lab审计中表现出色。

<details>
  <summary>Details</summary>

Motivation: 现有AI科学家在实验设计和结果验证方面缺乏可靠的指导机制，尤其是在高维参数空间中存在未解析子空间、模糊闭合不明确以及库不充分性检测缺失等问题。

Method: 提出CARTOGRAPH验证层，包含三大组件：(1) 使用未解析子空间实验引导（选择）；(2) 显式模糊闭合解析（解析）；(3) 基于残差的库不充分性检测（拒绝）。在局部线性-高斯桥梁假设下，未解析投影是各向同性未解析Fisher信息量迹，CARTOGRAPH-A是精确的未解析A最优规则。

Result: (1) 在五个测试平台上，d=8时CARTOGRAPH-A以129W/0T/15L击败原始投影（p<10^-21）；(2) 识别出3个库外药代动力学机制并随后撤回，1个扰动的库内对照保持识别；(3) 低维药代动力学和过滤EPA设置中理论与实验一致；(4) 在A-Lab系统回顾审计中，拒斥标志标记了所有4个后被判定为不确定的主张，同时通过了32/36个已确认主张。

Conclusion: CARTOGRAPH为AI科学家提供了一种有效的验证框架，能够在实验过程中动态引导、解析歧义并检测库不充分性，显著提升结果可靠性。

Original Abstract: We present CARTOGRAPH, a verification layer for AI scientists that couples unresolved-subspace experiment steering (select), explicit ambiguity closure (resolve), and residual-based library inadequacy detection (refuse). Under a local linear-Gaussian bridge, raw unresolved projection is the isotropic unresolved Fisher-information trace, while CARTOGRAPH-A is the exact unresolved A-optimal rule; closed-form EIG and Box-Hill arise as local comparators rather than global equivalents. Across five testbeds, CARTOGRAPH-A beats raw projection 129W/0T/15L at d = 8 (p < 10^-21) in a replicated structured cascade. More distinctively, the framework tentatively identifies three out-of-library pharmacokinetic mechanisms and then revokes those identifications as residuals expose structural misfit, while one perturbed in-library control stays identified throughout. In low-dimensional pharmacokinetic and filtered EPA settings, near-ties against disagreement are predicted by theory and observed. Finally, in a retrospective audit of 40 positive claims from the published A-Lab autonomous materials system, the refuse guard flags all 4 claims later marked inconclusive under manual reanalysis while passing 32/36 confirmed claims. Code is available at https://github.com/ai4science-boed/cartograph.git

中文摘要翻译: 我们提出了CARTOGRAPH，一个面向AI科学家的验证层，它结合了未解析子空间实验引导（选择）、显式模糊闭合解析（解析）以及基于残差的库不充分性检测（拒绝）。在局部线性-高斯桥梁假设下，原始未解析投影是各向同性的未解析Fisher信息量迹，而CARTOGRAPH-A是精确的未解析A最优规则；闭式EIG和Box-Hill作为局部比较器而非全局等价量出现。在五个测试平台上，CARTOGRAPH-A在复制的结构化级联中以d=8的参数维度，以129胜0平15负的成绩击败原始投影（p<10^-21）。更显著的是，该框架初步识别了三个库外药代动力学机制，随后当残差暴露出结构失配时撤销了这些识别，而一个扰动的库内对照则始终保持识别状态。在低维药代动力学和过滤EPA设置中，理论与实验预测的近似平局与分歧保持一致。最后，在对已发表的A-Lab自主材料系统中40个阳性主张的回顾性审计中，拒斥标志标记了所有4个后来在人工重分析中被判定为不确定的主张，同时通过了32/36个已确认的主张。代码已在 https://github.com/ai4science-boed/cartograph.git 公开。

</details>


### [229] [MST-Direct at Scale: Multivariate and Conditional Geostatistical Simulation via Sinkhorn Optimal Transport](https://arxiv.org/abs/2606.07578)
*Tcharlies Bachmann Schmitz*

Main category: cs.LG

TL;DR: 本文扩展了MST-Direct方法，将其从原始的二元、无条件、小网格设定推广到多元、条件化、大网格的地统计模拟场景，通过稀疏候选限制Sinkhorn匹配器、FFT-MA高斯骨干网和克里金条件化三大改进，实现了精确的多元联合分布保持和硬数据条件化。

<details>
  <summary>Details</summary>

Motivation: 原始MST-Direct方法存在三个主要局限性：(i) 仅支持数千节点的规模，可扩展性差；(ii) 仅限于二元变量，无法处理多元情况；(iii) 仅支持无条件模拟，无法融入硬数据条件化。本文旨在克服这些限制，使其适用于实际的大规模多元条件地统计模拟任务。

Method: (1) 提出稀疏候选限制Sinkhorn匹配器，将内存复杂度降至O(nC)，实现大规模节点扩展；(2) 通过将目标值元组匹配到独立的FFT-MA高斯骨干网上，该骨干网可再现预设的变差函数，实现多元扩展；(3) 通过克里金法条件化骨干网，并将观测数据元组固定在其空间位置上，实现硬数据条件化。传输计划仍保持为目标元组的排列，从而精确保持多元联合分布。

Result: 在与DMS相同的六变量异方差强非线性参考分布上的验证表明：(1) 无条件场景200×200网格和有条件场景100×100网格（200个硬数据样本）下，MST-Direct实现了零直方图误差的联合分布再现；(2) 精确拟合硬数据；(3) 准确再现预设的空间相关结构。相比之下，PPMT方法仍存在近似误差。

Conclusion: MST-Direct方法成功克服了原始方法的三大局限性，在大规模多元条件地统计模拟中实现了精确的联合分布保持和空间相关性再现，优于PPMT这类近似方法。

Original Abstract: This paper extends MST-Direct, a Matching-via-Sinkhorn-Transport approach for multivariate geostatistical simulation, from the original bivariate, unconditional, small-grid formulation to multivariate, conditional, and large-grid settings. We address the three main limitations identified in the original work: (i) scalability beyond a few thousand nodes through a sparse, candidate-restricted Sinkhorn matcher with O(nC) memory complexity; (ii) extension to multiple variables by matching target value tuples onto an independent FFT-MA Gaussian backbone that reproduces a prescribed variogram; and (iii) hard-data conditioning by fixing observed data tuples at their spatial locations while conditioning the backbone through kriging. Because the transport plan remains a permutation of the target tuples, the multivariate joint distribution is preserved exactly.
  The method is validated using the same six-variate, heteroscedastic, strongly nonlinear reference distribution employed in Direct Multivariate Simulation (DMS), under both unconditional (200x200) and conditional (100x100, 200 hard-data samples) scenarios, and is benchmarked against the Projection Pursuit Multivariate Transform (PPMT). Results show that MST-Direct reproduces the joint distribution with zero histogram error, exactly honours hard data, and accurately reproduces the prescribed spatial correlation structure, whereas PPMT remains an approximation.
  Index Terms-Optimal transport, Sinkhorn algorithm, geostatistical simulation, multivariate simulation.

中文摘要翻译: 本文对MST-Direct方法进行了扩展，该方法是一种基于Sinkhorn传输的多元地统计模拟方法，从原始的二元、无条件、小网格设定推广至多元、条件化、大网格场景。我们解决了原始工作中识别的三个主要局限性：(i) 通过稀疏候选限制Sinkhorn匹配器将可扩展性提升至数千节点以上，内存复杂度为O(nC)；(ii) 通过将目标值元组匹配到独立的FFT-MA高斯骨干网上来扩展至多个变量，该骨干网可再现预设的变差函数；(iii) 通过克里金法条件化骨干网，并将观测数据元组固定在其空间位置上，实现硬数据条件化。由于传输计划仍保持为目标元组的排列，因此多元联合分布得以精确保持。采用与直接多元模拟(DMS)相同的六变量异方差强非线性参考分布，在无条件场景(200×200)和条件化场景(100×100，200个硬数据样本)下进行验证，并与投影寻踪多元变换(PPMT)进行基准比较。结果表明，MST-Direct以零直方图误差再现了联合分布，精确拟合硬数据，并准确再现了预设的空间相关结构，而PPMT仍存在近似误差。索引词——最优传输，Sinkhorn算法，地统计模拟，多元模拟。

</details>


### [230] [Training-Inference Kernel Contracts: Bounding Divergence in Post-Training and Deployment](https://arxiv.org/abs/2606.07581)
*Bruce Changlong Xu,Lan Wu*

Main category: cs.LG

TL;DR: 本文提出“内核契约”（kernel contracts）框架，用于规范训练内核与推理内核在有限精度下的数值差异，并推导了从logit漂移到策略梯度偏差的误差链，为RL后训练中的内核一致性提供理论保证。

<details>
  <summary>Details</summary>

Motivation: 现代后训练流程中，训练内核（侧重自动求导）与推理内核（侧重低精度、融合、动态批处理）在有限精度下会诱导出不同分布，且差异集中在基准测试未充分覆盖的切片上，这种不一致性缺乏系统性的界定与管理。

Method: 提出内核契约框架 C = (N, S, R, O, Pi)，包含数值、统计、运行时和可观测性条款及违规升级策略；推导从logit漂移到总变分距离再到有界奖励漂移的误差链；针对RL后训练，推导基于per-token重要性比率漂移的策略梯度偏差上界。

Result: 建立了从数值漂移到策略梯度偏差的完整理论界链；设计了四阶段晋升流水线、在线路由循环和最小YAML DSL契约工件；本文为框架与词汇论文，未报告生产级实证验证。

Conclusion: 内核契约为规范训练/推理内核差异提供了系统化的契约优先框架，并建立了可证明的误差传播界限，为后训练流程中内核一致性治理开辟了新方向。

Original Abstract: A modern post-training pipeline often writes one symbol for its policy, pi_theta, while evaluating it through two different programs: a training kernel optimized for autograd and an inference kernel optimized for low-precision, fused, dynamically batched serving. In finite precision, these kernels can induce different distributions at identical weights, with the gap concentrated on slices that aggregate benchmarks under-represent. This paper proposes kernel contracts: a contract-first framework for specifying acceptable divergence between K_train and K_inf. A contract C = (N, S, R, O, Pi) combines numerical, statistical, runtime, and observability clauses with an escalation policy from violations to routing actions. We derive a chain of bounds from logit drift to total-variation distance to bounded reward drift, and specialize it to RL post-training, where per-token importance-ratio drift yields a bound on policy-gradient bias under explicit support and norm assumptions. We also describe a four-stage promotion pipeline, online routing loop, and minimal YAML DSL for contract artifacts. This is a framework and vocabulary paper; we do not report production-scale empirical validation.

中文摘要翻译: 现代后训练流程通常为其策略 π_θ 编写单一符号，但通过两个不同程序进行评估：一个针对自动求导优化的训练内核，和一个针对低精度、融合、动态批处理服务优化的推理内核。在有限精度下，这些内核在相同权重下可能诱导出不同分布，且差异集中在基准测试未充分覆盖的切片上。本文提出内核契约：一个契约优先的框架，用于规定 K_train 和 K_inf 之间可接受的偏差。契约 C = (N, S, R, O, Pi) 结合了数值、统计、运行时和可观测性条款，以及从违规到路由操作的升级策略。我们推导了从logit漂移到总变分距离再到有界奖励漂移的误差链，并将其专门化到RL后训练场景，其中在显式支撑和范数假设下，per-token重要性比率漂移给出了策略梯度偏差的界。我们还描述了一个四阶段晋升流水线、在线路由循环和用于契约工件的最小YAML DSL。本文是一篇框架与词汇论文；我们不报告生产规模的实证验证。

</details>


### [231] [Customer Churn Prediction on Structured Data Using FT-Transformer and Stacking Ensembles](https://arxiv.org/abs/2606.07582)
*Joyjit Roy,Samaresh Kumar Singh,Laxmi Shaw*

Main category: cs.LG

TL;DR: 本文提出一种融合FT-Transformer与XGBoost的混合架构，通过校准感知堆叠策略解决结构化数据客户流失预测中的类别不平衡、特征交互和概率校准问题，在公开银行数据集上取得了优于MLP基线的性能。

<details>
  <summary>Details</summary>

Motivation: 现有研究在流失预测中存在统计验证不足、概率校准差和可重复性低的问题；结构化数据上的类别不平衡、非线性特征交互和异质特征类型使得传统方法难以同时捕获高阶交互与决策边界。

Method: 采用FT-Transformer（自注意力机制捕获高阶特征交互）与XGBoost（梯度提升决策边界）混合，通过类别加权损失函数处理不平衡问题，并利用out-of-fold堆叠逻辑回归元学习器进行集成校准。

Result: 在公开银行流失数据集上，混合模型达到62.10% F1、0.861 AUC-ROC和0.647 PR-AUC，在5×5交叉验证下F1优于MLP基线3.37个点，AUC提升0.027，且报告了95%置信区间。

Conclusion: 该混合架构在结构化表格数据的流失预测任务中具备可重复性和可扩展性，消融实验证明Transformer组件与堆叠策略均对性能有实质性贡献。

Original Abstract: Customer churn prediction is essential across data-driven industries such as insurance, digital banking, eCommerce, and subscription platforms, where retaining existing customers is typically more cost-effective than acquiring new ones. Predicting churn on structured datasets remains challenging due to class imbalance, nonlinear feature interactions, and heterogeneous feature types. Tree-based ensemble methods consistently demonstrate strong performance in these contexts, often outperforming conventional neural networks. This study introduces a validated hybrid architecture that integrates feature-tokenized transformers (FT-Transformer) with gradient-boosted trees through calibration-aware stacking. The proposed framework addresses persistent gaps in statistical validation, probability calibration, and reproducibility found in prior research. The FT-Transformer captures higher-order feature interactions using self-attention, while XGBoost captures gradient-boosted decision boundaries with complementary inductive biases. Class imbalance is handled using class-weighted loss functions, thereby avoiding synthetic oversampling and preserving minority-class distributions. The models are ensembled using out-of-fold (OOF) stacking with a logistic regression meta-learner, which recalibrates overconfident base model outputs and learns optimal combination weights. On a public bank churn dataset, the hybrid model achieves 62.10% F1, 0.861 AUC-ROC, and 0.647 PR-AUC, outperforming the Multi-Layer Perceptron (MLP) baseline by 3.37 F1 points and 0.027 AUC under 5x5 cross-validation with 95% confidence intervals reported. Ablation studies demonstrate that both the transformer component and stacking strategy contribute materially to performance. The proposed methodology offers a reproducible and extensible reference architecture for contemporary churn prediction on structured tabular data.

中文摘要翻译: 客户流失预测在保险、数字银行、电子商务和订阅平台等数据驱动的行业中至关重要，因为留住现有客户通常比获取新客户更具成本效益。在结构化数据集上预测流失仍然具有挑战性，原因在于类别不平衡、非线性特征交互以及异质特征类型。基于树的集成方法在这些场景中持续展现出强劲性能，通常优于传统神经网络。本研究引入了一种经过验证的混合架构，通过校准感知堆叠将特征标记化Transformer（FT-Transformer）与梯度提升树集成在一起。该框架解决了先前研究中在统计验证、概率校准和可重复性方面存在的长期不足。FT-Transformer利用自注意力机制捕获高阶特征交互，而XGBoost则以互补的归纳偏置捕获梯度提升决策边界。类别不平衡通过类别加权损失函数处理，从而避免合成过采样并保留少数类分布。模型采用out-of-fold堆叠策略进行集成，使用逻辑回归元学习器重新校准过度自信的基模型输出并学习最优组合权重。在一个公开的银行流失数据集上，混合模型实现了62.10%的F1分数、0.861的AUC-ROC和0.647的PR-AUC，在5×5交叉验证下，相较于多层感知机基线提升了3.37个F1点和0.027的AUC，并报告了95%置信区间。消融研究表明，Transformer组件和堆叠策略都对性能有实质性贡献。所提出的方法为当代结构化表格数据的流失预测提供了一种可重复且可扩展的参考架构。

</details>


### [232] [Outage Detection in Self-Healing Smart Grids Using Reinforcement Learning with Spectral Graph Neural Networks](https://arxiv.org/abs/2606.07583)
*Lihui Liu,Mucun Sun,Caisheng Wang*

Main category: cs.LG

TL;DR: 提出了一种基于谱图强化学习的配电网故障管理框架，利用谱图神经网络学习最优电力恢复策略，在多个IEEE测试系统上实现了近最优的实时性能。

<details>
  <summary>Details</summary>

Motivation: 传统机器学习方法响应慢且计算成本高，不适用于智能电网的故障缓解；而现有基于GNN的方法仅在空间域操作，无法捕捉频域中重要的全局结构模式和系统级交互信息。

Method: 提出一种谱图强化学习框架，使用谱图神经网络建模控制策略，在频域中学习配电网的电力恢复策略。

Result: 在IEEE 13节点、34节点和123节点三个改进测试系统上进行评估，方法实现了近最优的实时性能，并在多种故障场景下具有良好的泛化能力。

Conclusion: 谱图强化学习框架能有效利用频域信息，实现配电网故障管理的实时近最优恢复，增强系统韧性。

Original Abstract: Self-healing smart grids can quickly adjust their network configuration during outages to minimize power disruptions. During an outage, several actions can be taken, such as network reconfiguration through switching operations and emergency load shedding. However, traditional machine learning methods for outage mitigation are not well suited for smart grids due to their slow response time and high computational cost. To address these challenges, recent studies have explored reinforcement learning to automatically perform network reconfiguration. In these approaches, the control policy is typically modeled using a graph neural network (GNN). However, conventional GNNs operate in the spatial domain and may fail to capture important relationships in the frequency domain. Frequency-domain information is particularly useful for modeling global structural patterns and system-wide interactions in power networks. In this paper, we propose a spectral graph reinforcement learning framework for outage management in distribution networks to enhance system resilience. Our model learns the optimal power restoration policy using a spectral graph neural network. We evaluate the proposed method on three modified IEEE test systems: the 13-bus, 34-bus, and 123-bus networks. Experimental results show that our approach achieves near-optimal performance in real time and generalizes well across a wide range of outage scenarios.

中文摘要翻译: 自愈智能电网能够在停电期间快速调整其网络配置，以最大程度减少电力中断。在停电期间，可以采取多种措施，例如通过开关操作进行网络重构和紧急负荷削减。然而，传统的用于故障缓解的机器学习方法由于响应速度慢且计算成本高，并不适用于智能电网。为应对这些挑战，近期研究探索了利用强化学习来自动执行网络重构。在这些方法中，控制策略通常使用图神经网络（GNN）进行建模。然而，传统的GNN在空间域中操作，可能无法捕捉频域中的重要关系。频域信息对于建模电力网络中的全局结构模式和系统级交互尤为有用。本文提出了一种面向配电网故障管理的谱图强化学习框架，以增强系统韧性。我们的模型使用谱图神经网络学习最优电力恢复策略。我们在三个改进的IEEE测试系统上评估了所提方法：13节点、34节点和123节点网络。实验结果表明，我们的方法在实时条件下实现了近最优的性能，并在广泛的故障场景中具有良好的泛化能力。

</details>


### [233] [From Human Guidance to Autonomy: Agent Skill System for End-to-End LLM Deployment on Spatial NPUs](https://arxiv.org/abs/2606.07586)
*Jiajie Li,Erwei Wang,Zhiru Zhang,Samuel Bayliss*

Main category: cs.LG

TL;DR: 本文提出一种两阶段方法，在AMD XDNA 2 NPU上实现LLM端到端自动部署：先通过人类引导的智能体辅助开发参考部署并记录优化知识，再将知识蒸馏为智能体技能系统，实现八种解码器LLM的零人工自主部署，其中三种性能达到或超越人工优化基准。

<details>
  <summary>Details</summary>

Motivation: 现有研究多聚焦于单核优化而非端到端LLM部署，且将LLM高效部署到资源受限的空间NPU上仍高度依赖人工劳动，缺乏自动化方法。

Method: 两阶段方法论：（1）人类引导的智能体辅助阶段，开发Llama-3.2-1B参考部署并记录优化轨迹为结构化文档；（2）将文档蒸馏为含八个阶段的智能体技能系统，每个阶段强制执行数值正确性，实现自主优化与调试。

Result: 参考部署在prefill和decode上分别获得2.2倍和4.0倍加速；技能系统成功自主部署8种之前未在AMD NPU上部署过的LLM，每个部署耗时0.5-4小时，几乎无需人工干预，其中3种模型性能达到或超越参考基准。

Conclusion: 所提方法能够将人类优化知识有效迁移为智能体自动化技能，实现面向未见LLM的功能泛化，且自主部署的性能可具备竞争力，无需额外的人工模型特定优化。

Original Abstract: Spatial neural processing units (NPUs) provide an energy-efficient platform for edge LLM inference, but efficiently deploying an LLM end-to-end on such hardware remains labor-intensive. Although AI coding agents have begun to lower this cost, existing studies have largely focused on single-kernel optimization rather than end-to-end LLM deployment on resource-constrained spatial NPUs.
  We present a two-stage methodology, instantiated on the AMD XDNA 2 NPU, that progresses from human-guided development to agent autonomy. In the first stage, we develop a reference deployment of Llama-3.2-1B through human-guided agent assistance. The resulting implementation achieves a speedup of 2.2x on prefill and 4.0x on decode over the hand-optimized baseline, with the optimization trajectory and its lessons recorded as structured documentation throughout. In the second stage, we distill the documentation into an agent skill system consisting of eight phases, orchestrating the optimization and debugging skill sets, with numerical correctness strictly enforced at each phase.
  Using our agent skill system, we autonomously deploy eight additional decoder-only LLMs (Llama-3.2-3B, SmolLM2-1.7B, Qwen2.5-{0.5B, 1.5B, 3B}, Qwen3-{0.6B, 1.7B, 4B}) end-to-end on the AMD XDNA 2 NPU using the open-source compiler stack. To our knowledge, these models have not previously been deployed on AMD NPUs via any open-source software stack. Each deployment completes in 0.5-4 hours of agent wall time with almost no human guidance, and passes the numerical-correctness gates, demonstrating functional generalization to previously unencountered LLMs. Three of the eight match or exceed the sustained performance of our Llama-3.2-1B reference deployment, suggesting that the resulting implementations can be competitive without additional model-specific human engineering.

中文摘要翻译: 空间神经处理单元（NPU）为边缘端大语言模型推理提供了能效优势，但在此类硬件上高效地端到端部署LLM仍需大量人工劳动。尽管AI编码智能体已开始降低这一成本，但现有研究多聚焦于单核优化，而非在资源受限的空间NPU上进行端到端LLM部署。
本文提出一种两阶段方法，并在AMD XDNA 2 NPU上实例化，该方法从人类引导开发逐步过渡到智能体自主部署。在第一阶段，我们通过人类引导的智能体辅助，开发了Llama-3.2-1B的参考部署方案。相较于手工优化的基线，该实现在预填充阶段实现了2.2倍加速，在解码阶段实现了4.0倍加速，整个优化轨迹及其经验教训被记录为结构化文档。在第二阶段，我们将这些文档蒸馏为一个包含八个阶段的智能体技能系统，该系统编排了优化与调试技能集，并在每个阶段严格执行数值正确性检查。
利用我们的智能体技能系统，我们基于开源编译器栈，在AMD XDNA 2 NPU上自主端到端部署了另外八种纯解码器LLM（Llama-3.2-3B、SmolLM2-1.7B、Qwen2.5-{0.5B、1.5B、3B}、Qwen3-{0.6B、1.7B、4B}）。据我们所知，这些模型此前从未通过任何开源软件栈部署在AMD NPU上。每次部署的智能体耗时仅0.5-4小时，几乎无需人工引导，且均通过了数值正确性门控，展现出对之前未见过的LLM的功能泛化能力。其中三种模型的持续性能达到或超越了我们的Llama-3.2-1B参考部署，表明在不依赖额外模型特定人工工程的前提下，自主生成的实现仍可具备竞争力。

</details>


### [234] [The Routing Plateau: Understanding and Breaking the Accuracy Limits of LLM Routers](https://arxiv.org/abs/2606.07587)
*Yifan Lu,Qiyue Zhang,Shenrun Zhang,Zhibo Yu,Zhuang Wang,Hanjie Chen,Jiarong Xing*

Main category: cs.LG

TL;DR: 论文发现LLM路由方法存在“路由平台期”现象：21种方法在5个基准上性能趋同，远低于最优路由。平台期主要由可预测性瓶颈导致——路由器学习全局平均性能而非细粒度查询特定信号。增大训练数据、更强编码器和端到端微调可突破平台期。

<details>
  <summary>Details</summary>

Motivation: 现有LLM路由方法虽然在成本-质量权衡上取得进展，但对各类路由方法的共同局限性缺乏系统性理解。作者希望通过大规模实证研究揭示路由方法的共性瓶颈，为构建更有效的路由系统提供方向。

Method: 对21种路由方法（包括kNN、聚类路由、学习分类器、成对排序、基于置信度的方法等）在5个基准上进行系统性比较分析，发现“路由平台期”现象，并通过实验分析其成因（可预测性瓶颈），进而探索突破平台期的策略（增大训练数据、更强编码器、端到端微调）。

Result: 1) 21种路由方法在5个基准上性能趋同，形成一个狭窄的性能范围，远低于Oracle路由器；2) 平台期主要由可预测性瓶颈导致：路由器主要学习全局平均模型性能趋势而非细粒度查询特定信号，能解决重叠的简单查询，但集体在需要实例级路由决策的难查询上失败；3) 增大训练数据集、使用更强编码器、端到端微调可进一步提升路由精度。

Conclusion: 当前LLM路由方法存在共同局限性——路由平台期，其根本原因是可预测性瓶颈。通过增大训练数据、采用更强编码器和端到端微调可以突破这一瓶颈。这些发现为社区构建更有效的路由系统提供了见解和可操作的方向。

Original Abstract: LLM routing has become a popular approach to improve the cost-quality trade-off of LLM services by dynamically selecting a model for each query. Recent work has explored a broad range of routing methods, including clustering-based routers, learned classifiers, pairwise ranking, and confidence-based approaches. Our extensive study of 21 routing methods across five benchmarks reveals a consistent phenomenon that we call the routing plateau: many methods, including kNN, achieve very similar accuracy and converge to a narrow performance range that remains far below the oracle router. Our investigation shows that the plateau is largely caused by a predictability bottleneck: current routers mainly learn global averaged model-performance trends rather than fine-grained query-specific routing signals. As a result, they solve overlapping easy queries but collectively fail on hard queries that require instance-specific routing decisions. We further study how to move beyond the plateau and find that larger training datasets, stronger encoders, and end-to-end fine-tuning can further improve routing accuracy. These findings characterize the common limits of current routing methods and provide insights and actionable directions for the community to build more effective routing systems.

中文摘要翻译: LLM路由已成为一种流行的方法，通过为每个查询动态选择模型来改善LLM服务的成本-质量权衡。近期工作探索了广泛的路由方法，包括基于聚类的路由器、学习分类器、成对排序和基于置信度的方法。我们对五种基准上的21种路由方法进行了广泛研究，揭示了一个一致的现象，我们称之为"路由平台期"：包括kNN在内的许多方法实现了非常相似的精度，并收敛到一个狭窄的性能范围，该范围远低于Oracle路由器。我们的研究表明，平台期主要由可预测性瓶颈导致：当前路由器主要学习全局平均的模型性能趋势，而非细粒度的查询特定路由信号。因此，它们能解决重叠的简单查询，但集体在需要实例级路由决策的困难查询上失败。我们进一步研究了如何突破平台期，发现更大的训练数据集、更强的编码器和端到端微调可以进一步提高路由精度。这些发现表征了当前路由方法的共同局限性，并为社区构建更有效的路由系统提供了见解和可操作的方向。

</details>


### [235] [Optimality of Sequential Filtering Under Independent Cost and Selectivity Models](https://arxiv.org/abs/2606.07589)
*Hrishikesh Paranjape,Abhishek Mandal,Xian Sun*

Main category: cs.LG

TL;DR: 本文形式化了顺序过滤管道的期望成本优化问题，证明了在独立性假设下，按成本与拒绝概率之比递增的顺序排列过滤器可以最小化期望总成本，并通过蒙特卡洛模拟验证了该最优排序在期望和结果分布上均优于常见启发式方法。

<details>
  <summary>Details</summary>

Motivation: 顺序过滤管道是大规模系统中的常见设计模式，但过滤器的排序通常依赖启发式方法，缺乏形式化保证。本文旨在为该问题提供理论上的最优排序策略。

Method: 将顺序过滤问题建模为期望成本目标优化问题，在独立性模型假设下，通过理论推导证明按成本与拒绝概率之比递增排序可最小化期望总成本，并采用蒙特卡洛模拟进行实证验证。

Result: 蒙特卡洛模拟显示，最优排序在所有运行中均严格优于常见启发式方法，无论是在期望值上还是在整个结果分布上。

Conclusion: 在独立性模型下，按成本与拒绝概率之比递增排序是顺序过滤管道的最优策略，能够在期望和分布意义上均优于启发式方法。

Original Abstract: Sequential filtering pipelines are a common design pattern in large-scale systems, where a large population of items is progressively reduced by a sequence of stages that each incur cost. Despite their prevalence in ranking systems, cascaded machine learning inference, and fraud detection, filter ordering is often determined by heuristics without formal guarantees. We formalize sequential filtering under an expected-cost objective and prove that, under an independence model, ordering filters by increasing ratio of cost to rejection probability minimizes expected total cost. Extensive Monte Carlo simulations show that the optimal ordering strictly dominates common heuristics across all runs, both in expectation and across the full distribution of outcomes.

中文摘要翻译: 顺序过滤管道是大规模系统中的一种常见设计模式，其中大量物品通过一系列各阶段均产生成本的步骤被逐步缩减。尽管这种模式在排序系统、级联机器学习推理和欺诈检测中普遍存在，但过滤器的排序通常依赖启发式方法，缺乏形式化保证。本文在期望成本目标下对顺序过滤进行了形式化，并证明在独立性模型下，按成本与拒绝概率之比递增的顺序排列过滤器可以最小化期望总成本。广泛的蒙特卡洛模拟表明，最优排序在所有运行中均在期望值和整个结果分布上严格优于常见启发式方法。

</details>


### [236] [ResearchClawBench: A Benchmark for End-to-End Autonomous Scientific Research](https://arxiv.org/abs/2606.07591)
*Wanghan Xu,Shuo Li,Tianlin Ye,Qinglong Cao,Yixin Chen,Hengjian Gao,Yiheng Wang,Qi Li,Kun Li,Sheng Xu,Shengdu Chai,Fangchen Yu,Xiangyu Zhao,Zhangrui Zhao,Weijie Ma,Zijie Guo,Haoyu Zhou,Haoxiang Yin,Lixue Cheng,Chaofan Hu,Haoxuan Li,Lu Mi,Xuxuan Xie,Yifan Zhou,Ruizhe Chen,Zhiwang Zhou,Xingjian Guo,Yuhao Zhou,Xuming He,Shengyuan Xu,Xinyu Gu,Jiamin Wu,Mianxin Liu,Chunfeng Song,Fenghua Ling,Dongzhan Zhou,Shixiang Tang,Yuqiang Li,Mao Su,Peng Ye,Siqi Sun,Bin Wang,Xue Yang,Zhenfei Yin,Tianfan Fu,Guangtao Zhai,Wanli Ouyang,Bo Zhang,Lei Bai,Wenlong Zhang*

Main category: cs.LG

TL;DR: 提出了ResearchClawBench基准，用于评估AI智能体在10个科学领域40个任务中的自主科研能力。实验表明当前最强自主智能体（Claude Code）和最强大语言模型（Claude-Opus-4.7）的得分分别仅为21.5和20.7（满分100），距离可靠的科学发现还有很大差距。

<details>
  <summary>Details</summary>

Motivation: 现有的AI编码智能体越来越多地用于科学工作，但它们在端到端自主研究方面的能力仍然难以验证，缺乏一个标准化的评估基准来衡量自主科学研究的进展。

Method: 构建了包含10个科学领域40个任务的ResearchClawBench基准。每个任务基于已发表的真实论文，提供相关文献和原始数据，隐藏目标论文。采用专家策划的多模态评分标准，将目标科学成果分解为加权标准，评估对目标论文级别的重新发现。在统一协议下评估了7个自主研究智能体，并通过轻量级ResearchHarness评估了17个原生LLM。

Result: 最强自主智能体Claude Code平均得分21.5，最强ResearchHarness LLM（Claude-Opus-4.7）平均得分20.7，LLM前沿均值仅26.5。错误分析表明失败主要集中在实验方案不匹配、证据不匹配和缺失科学核心三方面。

Conclusion: 当前自主研究系统远未达到可靠的重新发现水平，ResearchClawBench为衡量自主科学研究的进展提供了一个可复现的评估前沿。

Original Abstract: AI coding agents are increasingly used for scientific work, but their end-to-end autonomous research capability remains difficult to verify. We present ResearchClawBench, a benchmark for evaluating autonomous scientific research across 40 tasks from 10 scientific domains. Each task is grounded in a real published paper, provides related literature and raw data, and hides the target paper during evaluation. Expert-curated multimodal rubrics decompose the target scientific artifacts into weighted criteria, enabling evaluation of target-paper-level re-discovery while leaving room for new discovery. We evaluate seven autonomous research (auto-research) agents under a unified protocol and seventeen native LLMs through the lightweight ResearchHarness. Current systems remain far from reliable re-discovery: the strongest autonomous agent, Claude Code, averages 21.5, and the strongest ResearchHarness LLM, Claude-Opus-4.7, averages 20.7, with an LLM frontier mean of only 26.5. Error analysis shows that failures concentrate in experimental protocol mismatch, evidence mismatch, and missing scientific core. ResearchClawBench provides a reproducible evaluation frontier for measuring progress toward autonomous scientific research.

中文摘要翻译: AI编码智能体越来越多地被用于科学工作，但其端到端的自主研究能力仍然难以验证。我们提出了ResearchClawBench，这是一个用于评估自主科学研究的基准，涵盖来自10个科学领域的40个任务。每个任务基于一篇已发表的真实论文，提供相关文献和原始数据，并在评估过程中隐藏目标论文。专家策划的多模态评分标准将目标科学成果分解为加权标准，使得在评估目标论文级别的重新发现的同时，为新的发现留有空间。我们在统一协议下评估了七个自主研究（auto-research）智能体，并通过轻量级ResearchHarness评估了十七个原生大语言模型（LLM）。当前系统远未达到可靠的重新发现水平：最强的自主智能体Claude Code平均得分为21.5，最强的ResearchHarness大语言模型Claude-Opus-4.7平均得分为20.7，大语言模型前沿均值仅为26.5。错误分析表明，失败主要集中在实验方案不匹配、证据不匹配以及缺失科学核心三个方面。ResearchClawBench为衡量自主科学研究的进展提供了一个可复现的评估前沿。

</details>


### [237] [UNIQ: Conformal Calibration for Adaptive Conservatism in Offline Reinforcement Learning](https://arxiv.org/abs/2606.07592)
*Aditya Upadhyay*

Main category: cs.LG

TL;DR: UNIQ通过共形校准的不确定性估计实现状态自适应的保守性惩罚，在保持接近IQL的低内存开销（约250 MB）的同时，在D4RL MuJoCo任务上持续优于IQL，显著改进了离线强化学习的性能-效率权衡。

<details>
  <summary>Details</summary>

Motivation: 现有离线强化学习方法在所有状态下施加统一的固定保守性惩罚，忽略了不同区域的局部数据覆盖差异，导致在覆盖良好区域过于保守、在覆盖不足区域保守性不足的问题。

Method: 以隐式Q学习（IQL）为骨干，训练一个多期望值价值集成；利用分割共形预测计算无分布假设的不确定性估计；将不确定性信号映射为状态相关的期望值，在覆盖良好的区域放松保守性，在不确定区域加强保守性。

Result: 在D4RL MuJoCo基准测试上，UNIQ相比于IQL持续改进，在Walker2d和重放密集型任务上提升最大；内存开销约250 MB峰值显存，相比EDAC降低约10倍。

Conclusion: UNIQ被定位为一项实用的机制贡献，它通过改进离线强化学习中的性能-效率权衡，而不是追求全面的最优性能，为后续研究提供了一个更高效、更实用的基线方法。

Original Abstract: Offline reinforcement learning requires careful conservatism to mitigate distribution shift, yet most existing methods apply a fixed penalty uniformly across all states regardless of local data coverage. We present UNIQ (Uncertainty-Informed Quantile), an offline RL method that introduces state-adaptive conservatism through conformally calibrated uncertainty estimation. Built on the Implicit Q-Learning (IQL) backbone, UNIQ trains a multi-expectile value ensemble, computes distribution-free uncertainty estimates using split conformal prediction, and maps the resulting signal to a state-dependent expectile that relaxes conservatism in well-covered regions while strengthening it in uncertain regions near the data frontier. On D4RL MuJoCo benchmarks, UNIQ consistently improves over IQL, with the largest gains observed on Walker2d and replay-heavy tasks. At the same time, UNIQ operates at near-IQL memory cost (approximately 250 MB peak VRAM), providing roughly a 10x reduction compared to EDAC. Rather than pursuing overall state-of-the-art performance, we position UNIQ as a practical mechanism contribution that improves the performance-efficiency trade-off in offline reinforcement learning.

中文摘要翻译: 离线强化学习需要谨慎的保守性来缓解分布偏移，然而现有大多数方法在所有状态下都施加固定的惩罚，而不考虑局部数据覆盖情况。我们提出了UNIQ（不确定性感知分位数），一种通过共形校准的不确定性估计引入状态自适应保守性的离线强化学习方法。UNIQ以隐式Q学习（IQL）为骨干，训练一个多期望值价值集成，使用分割共形预测计算无分布假设的不确定性估计，并将所得信号映射到状态相关的期望值，从而在覆盖良好的区域放松保守性，在数据边界附近的不确定区域加强保守性。在D4RL MuJoCo基准测试上，UNIQ相较于IQL持续改进，在Walker2d和重放密集型任务上取得了最大提升。同时，UNIQ的运行内存开销接近IQL（约250 MB峰值显存），相比EDAC实现了约10倍的降低。我们并不追求全面的最优性能，而是将UNIQ定位为一项实用的机制贡献，改进了离线强化学习中性能与效率之间的权衡。

</details>


### [238] [A spectral audit framework reveals task-dependent aperiodic reliance across EEG and ECG deep learning](https://arxiv.org/abs/2606.08583)
*Jasmeet Singh Bindra,Siddharth Panwar,Shubhajit Roy Chowdhury*

Main category: cs.LG

TL;DR: 本文提出一种频谱审计框架，揭示深度学习模型在生理时间序列（如EEG、ECG）中过度依赖非周期性的1/f-like背景信号（而非有生理意义的周期性振荡特征），且该现象在多种任务和架构中普遍存在，建议将非周期性控制作为可解释生理时间序列深度学习的标准实践。

<details>
  <summary>Details</summary>

Motivation: 生理时间序列（如EEG、ECG）的深度学习解释通常聚焦于领域特异性特征（如振荡节律、形态学复合波），但这些信号置于宽频非周期性1/f-like包络之上，该包络随觉醒状态、年龄和病理变化而变化，可能导致模型依赖混淆变量而非有生理意义的特征。

Method: 提出频谱审计框架，包含：（1）非周期/周期分解；（2）相位保持的傅里叶干预（频谱平坦化）；（3）假对照（sham controls）；（4）模拟验证。在六种神经网络架构、七个EEG基础模型以及PTB-XL ECG数据集上进行审计。

Result: （1）睡眠-觉醒分类中非周期性依赖导致平衡准确率下降超过0.42；临床异常检测下降0.07-0.13；运动想象任务影响最小。（2）七个EEG基础模型中有六个表现出经FDR校正显著的非周期性依赖；年龄/性别和记录时期控制可减少但无法消除该效应。（3）PTB-XL ECG中非周期性干预导致准确率下降0.32-0.36，且在人口统计匹配后仍然存在。

Conclusion: 非周期性控制应成为可解释生理时间序列深度学习的标准实践，以消除模型对非特异性背景信号的依赖，确保解释聚焦于真正有生理意义的特征。

Original Abstract: Deep learning on physiological time series is interpreted through domain-specific features -- oscillatory rhythms in EEG, morphological complexes in ECG -- yet these signals sit atop a broadband aperiodic 1/f-like envelope that covaries with arousal, age, and pathology. We introduce a spectral audit framework combining aperiodic/periodic decomposition, phase-preserving Fourier interventions, sham controls, and simulation validation. Aperiodic reliance was task-dependent and architecture-general: across six neural architectures, flattening drops exceeded 0.42 balanced-accuracy points for sleep-wake classification, reached 0.07-0.13 for clinical abnormality detection, and remained minimal for motor imagery. Six of seven EEG foundation models showed FDR-significant aperiodic reliance on clinical EEG; age/sex and recording-era controls reduced but did not eliminate the effect. Applying the audit to PTB-XL ECG revealed neural drops of 0.32--0.36 persisting after demographic matching, confirming this confound class extends beyond EEG. Aperiodic controls should become standard for interpretable physiological time-series deep learning.

中文摘要翻译: 深度学习在解读生理时间序列时，通常依赖领域特异性特征——例如脑电图中的振荡节律、心电图中的形态学复合波——然而这些信号置于宽频带非周期性1/f-like包络之上，该包络随觉醒状态、年龄和病理变化而共变。我们引入一种频谱审计框架，结合非周期/周期分解、相位保持的傅里叶干预、假对照及模拟验证。结果表明，对非周期信号的依赖具有任务依赖性和架构通用性：在六种神经架构中，平坦化处理导致睡眠-觉醒分类的平衡准确率下降超过0.42，临床异常检测下降0.07-0.13，而对运动想象的影响则微乎其微。七个脑电图基础模型中有六个在临床脑电数据上表现出经FDR校正显著的非周期性依赖；年龄/性别和记录时期控制可减少但无法消除该效应。将该审计应用于PTB-XL心电图数据集，发现神经模型的准确率下降0.32-0.36，且在人口统计匹配后仍然存在，证实该混淆类别不仅限于脑电图。非周期性控制应成为可解释生理时间序列深度学习的标准实践。

</details>


### [239] [Shortcuts in the Tail: Debiasing via Post-Hoc Spectral Compression of Fine-Tuning Updates](https://arxiv.org/abs/2606.07596)
*Edward Sun,Dmitrii Troitskii*

Main category: cs.LG

TL;DR: 论文发现微调引入的虚假关联可通过后处理方式消除：对ΔW = W_ft - W_base进行SVD截断尾部奇异值，能在保持任务精度的同时减少模型对虚假特征的依赖，在多个指令微调模型和分类基准上有效。

<details>
  <summary>Details</summary>

Motivation: 微调常常在引入任务知识的同时带来虚假关联（spurious correlations），导致模型在 underrepresented 群体上系统性失效。现有缓解方法需要重新训练、组标签或精心构造的反事实数据，成本高昂。

Method: 提出一种简单后处理方法：对微调前后权重差 ΔW = W_ft - W_base 进行SVD分解，截断其尾部（较小奇异值对应的）奇异向量，仅保留前k个主要奇异分量，从而在不重新训练的情况下减少虚假关联。

Result: 在3个指令微调模型（0.5B-7B）和4个分类基准上，top-k截断在所有实验单元上均减少了虚假组间差距，准确率损失小于2个百分点，在CivilComments数据集上组间差距缩小达5倍。对照实验（bottom-k/random-k截断、matched-rank LoRA）排除了低秩近似或秩约束训练的解释。

Conclusion: ΔW的奇异基是研究微调所学知识的有效坐标系；虚假关联响应集中在ΔW奇异排序的尾部，而该行为机制不依赖于原始奇异值的绝对分布——奇异值本身广泛分布且在各数据集上相似。

Original Abstract: Fine-tuning often introduces spurious correlations alongside task knowledge, causing systematic failures on underrepresented groups. Existing mitigations require retraining, group labels, or curated counterfactual data. We show a simple post-hoc intervention reduces shortcut reliance without any of these: truncating the tail of the SVD of $ΔW = W_\mathrm{ft} - W_\mathrm{base}$ reduces the spurious-group gap while preserving task accuracy. Across three instruction-tuned models ($0.5$B--$7$B) and four classification benchmarks, top-$k$ truncation reduces the gap on every cell at $<2$ pp accuracy loss, by up to $5\times$ on CivilComments. We propose this works because the shortcut response sits in the tail of the singular ordering of $ΔW$, a claim about how truncation behaves rather than about the raw singular values, which are broadly distributed and look the same across all four datasets. A controlled boundary case in which fine-tuning has only a shortcut to learn shows the predicted FT-to-base collapse, and bottom-/random-$k$ and matched-rank LoRA controls rule out generic low-rank approximation and rank-constrained training as the explanation. We read this as preliminary evidence that the singular basis of $ΔW$ is a useful coordinate system for studying what fine-tuning has learned.

中文摘要翻译: 微调常常在引入任务知识的同时引入虚假关联（spurious correlations），导致模型在代表性不足的群体上系统性失效。现有的缓解方法需要重新训练、组标签或精心构造的反事实数据。我们展示了一种简单的后处理方法，无需上述任何条件即可减少对捷径（shortcuts）的依赖：对 ΔW = W_ft - W_base 进行奇异值分解（SVD）并截断其尾部奇异值，可以在保持任务精度的同时缩小虚假关联的群体间差距。在三个指令微调模型（0.5B-7B）和四个分类基准上，top-k截断在所有实验单元上均将组间差距缩小，准确率损失小于2个百分点，在CivilComments数据集上缩小幅度达5倍。我们提出这一方法有效的原因是：捷径响应位于ΔW奇异排序的尾部——这一主张描述的是截断行为本身，而非原始奇异值的分布特征（奇异值广泛分布且在所有四个数据集上看起来相同）。在一个受控边界案例中（微调仅有一条捷径可学习），观察到预期的微调权重向基模型坍缩现象；bottom-k/random-k截断及matched-rank LoRA对照实验排除了通用低秩近似和秩约束训练作为解释的可能性。我们将其解读为初步证据，表明ΔW的奇异基是研究微调所学知识的有效坐标系。

</details>


### [240] [How Much Capacity Does EEG Denoising Need? Ultra-Compact Networks reveal Benchmark Saturation and Metric-Utility Gap](https://arxiv.org/abs/2606.08594)
*Jasmeet Singh Bindra,Siddharth Panwar,Shubhajit Roy Chowdhury*

Main category: cs.LG

TL;DR: 本文发现：脑电去噪架构参数从数万膨胀到数千万，但模型容量饱和点远低于当前水平（3-6.5K参数即达性能拐点），且重构指标无法预测下游BCI任务的实用效果。极小模型（33-46KB）在边缘部署中具备潜力。

<details>
  <summary>Details</summary>

Motivation: 填补两项空白：(1) 此前没有研究将模型容量作为独立实验变量进行隔离控制；(2) 没有验证重构指标是否能预测下游神经信号的实用价值。

Method: 固定架构、损失函数、数据划分和训练策略不变，仅对最小深度可分离卷积U-Net的通道宽度进行扫描（参数范围1.05K至40.26K）。在EEGDenoiseNet基准、跨数据集BCI迁移测试、受控基线重训练以及所有9名BCI Competition IV-2a受试者的运动想象分类任务上评估。

Result: (1) 重构性能在3-6.5K参数时饱和，后续增益极小；(2) 8.46M参数基线模型与40.26K紧凑模型在EOG上表现无差异（200倍参数无优势）；(3) 重构优化的去噪显著降低了CSP+LDA分类性能（最佳去噪准确率0.547 vs 噪声基线0.612），且该现象在自然记录试次中持续存在；(4) 端到端神经解码器效果中性或可变。

Conclusion: 标准EEG去噪基准已远低于当前模型容量饱和点，重构指标无法预测BCI实用性。建议采用容量控制评估、更困难的任务感知基准以及强制性的下游验证。

Original Abstract: Deep learning EEG denoising architectures have scaled from tens of thousands to tens of millions of parameters, yet no prior study has isolated model capacity as the experimental variable or tested whether reconstruction metrics predict downstream neural-signal utility. We address both gaps by fixing architecture, loss, data split, and training recipe while sweeping only channel width from 1.05K to 40.26K parameters in a minimal depthwise-separable convolutional U-Net. Models were evaluated on the EEGDenoiseNet benchmark, cross-dataset BCI transfer tests, controlled baseline retraining, and downstream motor-imagery classification with five decoder families across all nine BCI Competition IV-2a subjects. Reconstruction performance saturated by 3-6.5K parameters, with post-elbow gains of at most 0.015 correlation coefficient per log10-parameter unit. An 8.46M-parameter baseline retrained under the same pipeline matched the 40.26K compact variant on EOG--a 200x parameter gap yielding no advantage--while a Patch-Transformer control reproduced the same diminishing-return shape. Downstream evaluation exposed a classifier-dependent metric-utility gap: reconstruction-optimized denoising significantly degraded CSP+LDA classification across all nine subjects and three artifact types (best denoised accuracy 0.547 vs. 0.612 noisy baseline; Bonferroni p=0.0488), persisting on naturally recorded trials (Delta=-0.047; BH-FDR q=0.0049). End-to-end neural decoders showed variable or neutral effects. Standard EEG denoising benchmarks are saturated far below current model capacity, and reconstruction metrics do not predict BCI utility. Ultra-compact models at 33-46 KB and 1.27-2.61M FLOPs/segment are practical for edge deployment. These findings argue for capacity-controlled evaluation, harder task-aware benchmarks, and mandatory downstream validation.

中文摘要翻译: 深度学习脑电去噪架构的参数规模已从数万膨胀到数千万，但此前没有任何研究将模型容量作为独立实验变量进行隔离控制，也没有检验重构指标是否能预测下游神经信号的实用价值。我们通过固定架构、损失函数、数据划分和训练策略，仅对最小深度可分离卷积U-Net的通道宽度进行从1.05K到40.26K参数的扫描，从而填补了这两个空白。模型在EEGDenoiseNet基准、跨数据集BCI迁移测试、受控基线重训练以及所有9名BCI Competition IV-2a受试者的运动想象分类任务（使用五种解码器族）上进行了评估。重构性能在3-6.5K参数时即达到饱和，肘部之后的增益最多仅为每log10参数单位0.015相关系数。在同一流程下重训练的8.46M参数基线模型与40.26K紧凑变体在EOG上表现相当——200倍参数差距未带来任何优势——而Patch-Transformer对照组复制了相同的边际递减形状。下游评估揭示了分类器相关的度量-效用差距：针对重构优化的去噪显著降低了所有九名受试者和三种伪迹类型下的CSP+LDA分类性能（最佳去噪准确率0.547 vs 噪声基线0.612；Bonferroni校正p=0.0488），该现象在自然记录试次中持续存在（Delta=-0.047；BH-FDR q=0.0049）。端到端神经解码器表现出可变或中性效果。标准脑电去噪基准的饱和点远低于当前模型容量水平，且重构指标无法预测BCI实用价值。超紧凑模型（33-46 KB，每段1.27-2.61M FLOPs）适用于边缘部署。这些发现支持采用容量控制的评估、更困难的任务感知基准以及强制性的下游验证。

</details>


### [241] [Repetition Mismatch: Why Data Mixture Experiments Don't Scale and How to Fix Them](https://arxiv.org/abs/2606.07597)
*Kevin Zhou,Lisa Alazraki,Kris Cao,Marek Rei*

Main category: cs.LG

TL;DR: 本文揭示了预训练数据混合优化中，因高质量数据重复率随训练预算变化而导致的“重复不匹配”问题，并提出一种通过子采样匹配目标重复率的控制方法，显著提升了小规模代理实验的泛化能力。

<details>
  <summary>Details</summary>

Motivation: 小规模实验在预测大规模训练预算下的最优数据混合时经常失败，尤其当高质量数据稀缺需重复使用时，但此前未隔离出失败的根本原因。

Method: 提出重复控制（repetition-controlled）子采样程序，使小规模实验的重复率与目标训练预算下的重复率匹配，从而消除重复不匹配带来的偏移效应。

Result: 在双源场景下，仅用1/16目标token的单次重复控制实验即可将混合比例误差控制在0.05以内（无控制时为0.75）；达到同等精度，无控制方法消耗44%-94%的目标token预算。在三源场景下，两次重复控制实验即可恢复最优混合。

Conclusion: 重复动态（而非规模本身）决定了小规模混合实验能否泛化；数据重复应作为混合优化中的一阶变量，而非有限数据的附带副作用。

Original Abstract: Pre-training data mixtures are commonly tuned by running small-scale experiments and extrapolating to the target training budget. When high-quality data is scarce and must be repeated, this extrapolation frequently fails, but the source of the failure has not been isolated. We show that a primary culprit is a repetition mismatch: because high-quality datasets are small, their repetition rate changes as the training budget grows, shifting the optimal mixture in ways that small-scale proxy experiments do not anticipate. A subsampling procedure that matches the target repetition rate controls for this effect. In a two-source setting combining limited high-quality data with web crawl, a single repetition-controlled experiment using only 1/16 of the target tokens recovers a mixture within 0.05 of the optimum for a 757M parameter model, compared to an error of 0.75 without repetition control. Achieving comparable accuracy without repetition control requires three to four horizons, consuming 44 to 94% of the target token budget. With three data sources, the larger mixture space requires more than a single experiment to constrain, but the approach remains effective: at the 757M scale, just two repetition-controlled horizons recover the optimal mixture, outperforming baselines that instead require the full two-source experiments to construct. Our results reveal that repetition dynamics, not scale alone, shape whether small-scale mixture experiments generalize. More broadly, they suggest that data repetition deserves treatment as a first-class variable in mixture optimization, rather than an inconvenient side effect of limited data.

中文摘要翻译: 预训练数据混合通常通过运行小规模实验并外推至目标训练预算来调优。当高质量数据稀缺且必须被重复使用时，这种外推经常失败，但失败根源此前未被隔离。我们发现一个主要原因是重复不匹配：由于高质量数据集规模小，当训练预算增长时其重复率会变化，从而以小型代理实验无法预期的方式偏移最优混合。一种匹配目标重复率的子采样程序可以控制这一效应。在将有限高质量数据与网络爬取数据相结合的双源场景中，仅使用目标token数1/16的单次重复控制实验，即可为757M参数模型恢复出与最优混合相差0.05以内的混合比例，而无重复控制时的误差为0.75。达到同等精度，无重复控制需要三到四个视野区间，消耗目标token预算的44%至94%。当数据源增至三个时，更大的混合空间需要多于一次实验才能约束，但该方法仍然有效：在757M规模下，仅需两次重复控制的视野区间即可恢复最优混合，优于那些需要完整双源实验才能构建的基线方法。我们的结果表明，决定小规模混合实验能否泛化的因素是重复动态，而非规模本身。更广泛地看，这些发现表明数据重复应作为混合优化中的一阶变量来对待，而非有限数据带来的不便副作用。

</details>


### [242] [A Topological Characterization of Graph Neural Networks via Stochastic Block Model Embeddings on the n-Sphere](https://arxiv.org/abs/2606.07598)
*Gopal Anantharaman*

Main category: cs.LG

TL;DR: 本文提出一个拓扑框架，通过将消息传递神经网络（MPNN）在图论-信号空间上诱导的随机块模型（SBM）映射到单位n-球面上，来比较训练后的图神经网络（GNN）。该框架基于割距图论空间的紧致性、Frieze–Kannan弱正则引理及其图论-信号扩展、以及MPNN关于割距的Lipschitz连续性三大经典理论，从而生成与问题无关的低维GNN“指纹”，支持模型动物园中的近邻搜索和迁移学习候选检索。

<details>
  <summary>Details</summary>

Motivation: 训练后的GNN缺乏统一的比较框架，难以进行跨模型的可视化分析和迁移学习候选检索，因此需要一种问题无关的低维表示方法。

Method: 基于割距图论空间的紧致性、Frieze–Kannan弱正则引理及其图论-信号扩展、MPNN关于割距的Lipschitz连续性，构造显式保测映射将SBM区域映射到单位球面上的互不相交球冠，生成GNN的拓扑指纹。

Result: 对任意容忍度ε>0，训练后的MPNN在大图上可因式分解为有界复杂度的阶梯图论-信号（误差在ε以内）；构建了到单位球面的保测映射，实现了低维指纹的可视化和近邻搜索；讨论了高维中测度集中的障碍。

Conclusion: 所提框架为GNN提供了一种可解释的低维指纹，支持迁移学习候选检索，并指出了五个未来方向：双曲/格拉斯曼替代模型、Gromov–Wasserstein距离、信息几何SBM流形、持续同调嵌入指纹和谱距离基线。

Original Abstract: We propose a topological framework for comparing trained Graph Neural Networks (GNNs) by mapping the Stochastic Block Models (SBMs) induced on the graphon-signal space of a Message Passing Neural Network (MPNN) onto the unit $n$-sphere $\sphere^{n-1}\subset\R^n$. The construction rests on three classical pillars: the \emph{compactness} of the cut-distance graphon space $(\Wo,\cutdist)$ \citep{lovasz2006limits,lovasz2012large}, the Frieze--Kannan \emph{weak regularity lemma} together with its graphon-signal extension due to \citet{levie2023graphon}, and the Lipschitz continuity of MPNNs with respect to the cut-distance. We show that, for any prescribed tolerance $\varepsilon>0$, a trained MPNN $Φ$ acting on a sufficiently large graph factors (up to $\varepsilon$) through a step-graphon-signal of bounded complexity, and we construct an explicit measure-preserving map $Ψ_n\colon[0,1]\to\sphere^{n-1}$ that places the SBM regions on disjoint spherical caps. This produces a problem-agnostic, low-dimensional ``fingerprint'' of a trained GNN that is amenable to visual inspection and to nearest-neighbour search across model zoos, enabling \emph{transfer-learning candidate retrieval} without retraining. We discuss the obstruction posed by concentration of measure in high dimension -- a phenomenon directly relevant to LLM-scale embeddings. We close with five concrete future research directions: hyperbolic and Grassmannian alternatives to the spherical model, Gromov--Wasserstein distances on graphon-signals as an isometry-free alternative to the $n$-sphere map, an information-geometric (Fisher) reformulation of the SBM manifold, persistent-homology fingerprints of layer-wise embedding clouds, and a spectral-distance baseline derived from the graphon eigendecomposition.

中文摘要翻译: 我们提出一种拓扑框架，用于比较训练后的图神经网络（GNN），方法是将消息传递神经网络（MPNN）在图论-信号空间上诱导的随机块模型（SBM）映射到单位n-球面$\sphere^{n-1}\subset\R^n$上。该构建基于三大经典支柱：割距图论空间$(\Wo,\cutdist)$的紧致性、Frieze–Kannan弱正则引理及其由Levie等人提出的图论-信号扩展，以及MPNN关于割距的Lipschitz连续性。我们证明，对任意给定的容忍度$\varepsilon>0$，训练后的MPNN $Φ$在足够大的图上可因式分解为有界复杂度的阶梯图论-信号（误差在$\varepsilon$以内），并构造了一个显式的保测映射$Ψ_n\colon[0,1]\to\sphere^{n-1}$，将SBM区域置于互不相交的球冠上。这产生了与问题无关的训练GNN的低维“指纹”，可进行可视化检查以及跨模型动物园的近邻搜索，从而无需重新训练即可实现迁移学习候选检索。我们讨论了高维中测度集中现象带来的障碍——该现象直接相关于大规模语言模型（LLM）规模的嵌入。最后，我们提出五个具体的研究方向：球面模型的双曲和格拉斯曼替代方案、作为n-球面映射的无等距替代的图论-信号上的Gromov–Wasserstein距离、SBM流形的信息几何（Fisher）重表述、逐层嵌入云的持续同调指纹，以及基于图论特征分解的谱距离基线。

</details>


### [243] [DiffoR: A Unified Continuous Generative Framework for Universal Ordinal Regression](https://arxiv.org/abs/2606.07599)
*Hongxu Ma,Lin Wang,Chenghou Jin,Han Zhou,Jie Zhang,Xiaoyu Yang,Chunjie Chen,Jihong Guan,Shuigeng Zhou*

Main category: cs.LG

TL;DR: 本文提出DiffOR，一种将序数回归建模为连续生成式任务的统一框架，通过扩散模型的迭代去噪和双重解耦策略（空间多尺度增量聚合+时间动态去噪感知），解决了传统方法中的量化伪影和全局序数拓扑感知缺失问题，在12个基准数据集上超越现有方法。

<details>
  <summary>Details</summary>

Motivation: 现有序数回归方法（从朴素回归到基于离散化分类和生成的方法）受限于量化伪影和全局序数拓扑感知缺失，它们通常强制执行刚性边界划分，无法捕捉序数数据中固有的非平稳语义转换。

Method: 提出DiffOR框架：1）将序数回归建模为连续生成式序数回归任务，利用扩散模型通过迭代去噪恢复连续序数值，实现软语义转换的动态学习；2）提出双重解耦策略——空间上通过多尺度增量聚合将目标分解为层次化连续增量，时间上通过动态去噪感知将去噪步与特征频率同步，实现从粗到细的鲁棒细化。

Result: 在涵盖四个领域的12个基准数据集上，DiffOR持续优于现有最先进方法，显著提升了表示能力和机制可解释性。

Conclusion: DiffOR建立了序数回归的新标准，展示了作为通用序数回归解决方案的强大潜力。

Original Abstract: Ordinal Regression (OR) aims to predict target values with inherent order, underpinning critical applications across diverse domains, from recommender systems to computer vision. Though having evolved from naive regression to discretization-based classification and generation, existing paradigms remain fundamentally constrained by quantization artifacts and the lack of global ordinal topological perception. These methods typically enforce rigid boundary delineations, failing to capture the non-stationary semantic transitions inherent to ordinal data. In this paper, we propose a novel paradigm where OR is formulated as a Continuous Generative Ordinal Regression task. Under the novel paradigm, we introduce DiffOR, a unified framework that leverages diffusion models to recover continuous ordinal values via iterative denoising, thereby enabling the dynamic learning of soft semantic transitions. To explicitly preserve ordinal topology, we devise a Dual-Decoupling Strategy: Spatially, Multi-scale Increment Aggregation decomposes targets into hierarchical continuous increments; Temporally, Dynamic Denoising Perception synchronizes denoising steps with feature frequencies, ensuring robust coarse-to-fine refinement. Theoretically, we show that the proposed method can significantly enhance both representation capability and mechanistic interpretability. Extensive experiments on 12 benchmarks across four domains validate DiffOR's consistent superiority over state-of-the-art methods, establishing a new standard that demonstrates strong potential as a general-purpose solution for universal ordinal regression.

中文摘要翻译: 序数回归旨在预测具有内在顺序的目标值，支撑着从推荐系统到计算机视觉等不同领域的关键应用。尽管从朴素回归发展到基于离散化的分类和生成方法，现有范式仍然受限于量化伪影和全局序数拓扑感知的缺失。这些方法通常强制执行刚性边界划分，无法捕捉序数数据中固有的非平稳语义转换。本文提出了一种新范式，将序数回归建模为连续生成式序数回归任务。在该新范式下，我们引入了DiffOR，一个统一框架，利用扩散模型通过迭代去噪恢复连续序数值，从而实现对软语义转换的动态学习。为显式保留序数拓扑结构，我们设计了一种双重解耦策略：在空间上，多尺度增量聚合将目标分解为层次化连续增量；在时间上，动态去噪感知将去噪步与特征频率同步，确保从粗到细的鲁棒细化。理论上，我们证明所提出的方法能显著增强表示能力和机制可解释性。在涵盖四个领域的12个基准数据集上的大量实验验证了DiffOR相对于现有最先进方法的一致优势，建立了新标准，展示了其作为通用序数回归解决方案的强大潜力。

</details>


### [244] [Reachability and asymptotics of Gaussian Transformer dynamics](https://arxiv.org/abs/2606.07600)
*Albert Alcalde,Zhengping Ji,Enrique Zuazua*

Main category: cs.LG

TL;DR: 本文将Transformer中的数据传播建模为概率测度空间上的非线性控制系统，证明了高斯分布在自注意力和前馈层下保持高斯性，从而将无限维的测度动力学降维为有限维的双线性控制系统，并揭示了与Riccati型方程的新联系。理论证明和数值实验验证了该框架下的可达性、稳定性与协方差爆炸现象。

<details>
  <summary>Details</summary>

Motivation: 理解Transformer内部数据分布演化的动力学机制，建立严格的数学框架来分析其表达能力与传播行为，弥补现有研究多依赖经验观察而缺乏理论基础的不足。

Method: 将Transformer中的数据传播建模为概率测度空间上的非线性控制系统；利用高斯分布在自注意力和仿射前馈层下的不变性，将无限维的测度动力学约化为有限维的均值-协方差双线性控制系统；通过控制论和Riccati方程工具分析可达性、稳定性与协方差爆炸。

Result: (1) 对时变控制参数，证明了任何具有相同秩协方差矩阵的目标高斯分布可在有限时间内精确可达；(2) 对时不变参数，推导出谱条件，可导致正定平衡态的渐近稳定或协方差的有限时间爆炸；(3) 数值实验表明，实际Transformer在早期和中间层保持高斯分布接近矩匹配高斯分布，且预设注意力矩阵能复现理论预测的协方差演化行为。

Conclusion: 本文为Transformer的动力学行为提供了严格的控制论分析框架，揭示了高斯不变性、协方差秩守恒等重要理论性质，并为理解Transformer的表达能力和传播稳定性提供了数学基础。

Original Abstract: We formulate data propagation through the Transformer, the machine learning architecture powering large language models, as a nonlinear control system on the space of probability measures. For the mean-field Transformer model with self-attention and affine feed-forward layers, we prove that Gaussian distributions remain exactly Gaussian along the induced flow. This invariance reduces the infinite-dimensional measure dynamics to a finite-dimensional bilinear control system governing the evolution of the mean and covariance, reformulates the expressive capacity of Transformers as a reachability problem for prescribed Gaussian moments, and reveals a novel connection with Riccati-type equations from classical filtering and control.
  For time-varying controls, we prove exact finite-time reachability of any target Gaussian distribution whose covariance matrix has the same rank as the initial one, this rank constraint being an intrinsic invariant of the dynamics. For time-invariant parameters, we derive explicit spectral conditions leading either to asymptotic stability toward positive-definite equilibria or to finite-time blow-up of the covariance.
  Numerical experiments complement the theory by showing that practical Transformers with Gaussian inputs remain close to moment-matched Gaussian distributions through early and intermediate layers, while Transformers with prescribed attention matrices reproduce the predicted covariance regimes: bounded evolution in stabilizing configurations and blow-up in destabilizing ones.

中文摘要翻译: 我们将驱动大语言模型的机器学习架构——Transformer中的数据传播，建模为概率测度空间上的非线性控制系统。对于包含自注意力机制和仿射前馈层的平均场Transformer模型，我们证明了高斯分布在诱导流下严格保持高斯性。这一不变性将无限维的测度动力学约化为有限维的均值-协方差双线性控制系统，将Transformer的表达能力重新表述为关于给定高斯矩的可达性问题，并揭示了与经典滤波和控制理论中Riccati型方程的新联系。对于时变控制参数，我们证明了任何与初始协方差矩阵秩相同的目标高斯分布可在有限时间内精确可达，该秩约束是动力学的一个内在不变量。对于时不变参数，我们推导出显式的谱条件，可导致正定平衡态的渐近稳定或协方差的有限时间爆炸。数值实验对理论进行了补充，表明具有高斯输入的实际Transformer在早期和中间层保持与矩匹配高斯分布接近，而具有预设注意力矩阵的Transformer复现了预测的协方差演化模式：稳定配置下的有界演化和失稳配置下的爆炸行为。

</details>


### [245] [LFNO: Bridging Laplace and Fourier via Transient-Steady Decomposition](https://arxiv.org/abs/2606.07601)
*Jeongun Ha,Sanga Yoon,Donghun Lee*

Main category: cs.LG

TL;DR: 提出拉普拉斯-傅里叶神经算子（LFNO），一种通过融合拉普拉斯和傅里叶神经算子的谱优势来统一建模瞬态和稳态动力系统的框架。采用双分支架构显式分解瞬态与稳态成分，在九个基准测试中显著优于现有方法，尤其在ODE系统上表现突出。

<details>
  <summary>Details</summary>

Motivation: 现有神经算子（如FNO、LNO）分别擅长建模稳态或瞬态动力学，但缺乏统一处理两种时间尺度的能力。作者希望设计一种能同时捕捉瞬态和稳态行为的统一框架，提升模型在不同动力学 regime 下的泛化能力和物理可解释性。

Method: 提出LFNO，采用双分支架构：一个分支使用拉普拉斯神经算子（LNO）捕捉瞬态动力学（指数衰减/增长模式），另一个分支使用傅里叶神经算子（FNO）捕捉稳态动力学（周期/振荡模式）。两个分支的输出通过可学习方式融合，实现系统动力学的显式分解。

Result: 在九个基准测试（三个ODE系统：Duffing、Lorenz、Pendulum；六个PDE系统：Euler-Bernoulli梁、热方程、反应扩散、Brusselator、Burgers、Navier-Stokes）上，LFNO在瞬态主导的ODE系统中显著优于现有算子，在PDE基准上持续超越LNO且与FNO性能相当。此外，LFNO通过分量分解提供了更好的稳定性和物理可解释性。

Conclusion: LFNO提供了一种鲁棒且统一的动力系统学习方法，能够有效处理多时间尺度下的复杂动力学，兼具性能优势和物理可解释性。

Original Abstract: We introduce the Laplace-Fourier Neural Operator (LFNO), a unified framework for modeling dynamical systems across transient and steady-state regimes by integrating the spectral advantages of Laplace and Fourier Neural Operators. LFNO employs a dual-branch architecture that explicitly decomposes system dynamics into transient and steady-state components. We evaluate LFNO on nine benchmarks, including three ODE systems (Duffing, Lorenz, and Pendulum) and six PDE systems (Euler-Bernoulli beam, Heat, Reaction-diffusion, Brusselator, Burgers, and Navier-Stokes). LFNO significantly outperforms existing operators on ODE systems, where transient dynamics dominate, and consistently surpasses LNO while achieving performance competitive with FNO on PDE benchmarks. Furthermore, LFNO offers improved stability and physical interpretability through its component-wise decomposition. These results demonstrate that LFNO provides a robust and unified approach for learning complex dynamical systems across multiple temporal scales.

中文摘要翻译: 我们提出了拉普拉斯-傅里叶神经算子（Laplace-Fourier Neural Operator, LFNO），这是一个通过整合拉普拉斯和傅里叶神经算子的谱优势来统一建模瞬态和稳态动力系统的框架。LFNO采用双分支架构，将系统动力学显式分解为瞬态和稳态分量。我们在九个基准测试上评估了LFNO，包括三个常微分方程系统（Duffing、Lorenz和Pendulum）和六个偏微分方程系统（Euler-Bernoulli梁、热方程、反应扩散方程、Brusselator、Burgers方程和Navier-Stokes方程）。在瞬态动力学主导的ODE系统中，LFNO显著优于现有算子；在PDE基准测试中，LFNO持续超越LNO，并达到与FNO竞争的性能。此外，LFNO通过其分量分解提供了改进的稳定性和物理可解释性。这些结果表明，LFNO为在多个时间尺度上学习复杂动力系统提供了一种鲁棒且统一的方法。

</details>


### [246] [Sample-Efficient Post-Training for LEGO Spatial-Physics Reasoning](https://arxiv.org/abs/2606.07602)
*Yuhuan Yuan,Zhouliang Yu,Minghao Liu,Weiyang Liu,Ge Lin Kan*

Main category: cs.LG

TL;DR: 本文发现LLM生成乐高组装时存在一种数据引发的失效模式PhysHack——组装满足物理约束但几何错位、语义不一致或校准不良。为此提出基于模型的数据选择方法，仅用少量训练数据提升物理基础的乐高组装生成，并引入PVPO强化学习方法，耦合物理可行性与体素空间几何奖励。实验表明，物理有效性不足以保证可靠物理推理，PVPO能改善结构语义对齐、物理有效性、稳定性和校准，减少后验拒绝采样依赖。

<details>
  <summary>Details</summary>

Motivation: 现有LLM在乐高组装生成中存在一种数据引发的失效模式PhysHack：模型生成的组装虽然满足物理有效性约束，但存在几何错位、语义不一致或校准不良的问题，说明物理有效性本身不足以作为可靠物理推理的代理指标。因此需要更精细的方法来提升生成的物理合理性、语义一致性和几何对齐度。

Method: 1) 基于模型的数据选择方法，仅使用少量训练数据即可提升物理基础的乐高组装生成；2) PVPO（物理体素策略优化），一种样本高效的强化学习方法，将物理可行性与体素空间几何奖励相结合。

Result: 1) 物理有效性单独不足以作为可靠物理推理的代理指标，模型可能学会生成有效结构但丧失语义或几何保真度；2) PVPO在多种模型骨干和测试时扩展设置下，改善了结构对齐、语义对齐、物理有效性、结构稳定性和校准；3) PVPO通过使测试时选择更能预测语义和结构质量，缓解了PhysHack失效模式；4) 减少了对大量后验拒绝采样的依赖。

Conclusion: 物理有效性约束并不等同于可靠的物理推理能力，需要结合几何和语义线索来确保真正合理的组装生成。PVPO方法通过耦合物理可行性与几何奖励，仅用少量数据就能显著提升生成质量，缓解PhysHack问题，同时提高样本效率。

Original Abstract: LLM-based LEGO assembly generation requires both semantic grounding and physical feasibility. We identify a data-induced failure mode, PhysHack, in which the assemblies satisfy physical-validity constraints while producing structures that are geometrically misaligned, semantically inconsistent, or poorly calibrated. To address this challenge, we propose a model-based data selection approach that uses only a small fraction of the training data while improving physically grounded LEGO assembly generation. Building on the selected trajectories, we introduce PVPO, a sample-efficient reinforcement learning method that couples physical feasibility with voxel-space geometric rewards. Our results show that physical validity alone is an insufficient proxy for reliable physical reasoning: models can learn to generate valid structures without preserving semantic or geometric fidelity. Experiments across model backbones and test-time scaling settings demonstrate that PVPO improves structural and semantic alignment, physical validity, structural stability, and calibration, while reducing reliance on extensive post-hoc rejection sampling. In particular, results on calibration show that PVPO mitigates PhysHack by making test-time selection more predictive of semantic and structural quality.

中文摘要翻译: 基于大语言模型的乐高组装生成需要同时满足语义基础和物理可行性。我们识别出一种数据引发的失效模式PhysHack，即组装虽满足物理有效性约束，但生成的结构存在几何错位、语义不一致或校准不良的问题。为解决这一挑战，我们提出了一种基于模型的数据选择方法，仅使用少量训练数据即可改善物理基础的乐高组装生成。在所选轨迹的基础上，我们引入了PVPO，一种样本高效的强化学习方法，将物理可行性与体素空间几何奖励相耦合。实验结果表明，物理有效性本身不足以作为可靠物理推理的代理指标：模型可以学会生成有效的结构，但无法保持语义或几何保真度。在多种模型骨干和测试时扩展设置下的实验表明，PVPO改善了结构对齐、语义对齐、物理有效性、结构稳定性和校准，同时减少了对大量后验拒绝采样的依赖。特别是在校准方面的结果表明，PVPO通过使测试时选择更能预测语义和结构质量，缓解了PhysHack问题。

</details>


### [247] [MetaEvo: A Meta-Optimization Framework for Experience-Driven Agent Evolution](https://arxiv.org/abs/2606.07603)
*Bowen Ren,Heyan Huang,Yinghao Li,Yang Gao*

Main category: cs.LG

TL;DR: 提出MetaEvo框架，通过元优化（偏好优化+模块化原则重用）使LLM代理在任务交互中持续进化，解决静态部署和经验驱动方法性能瓶颈问题。

<details>
  <summary>Details</summary>

Motivation: 现有LLM代理多为静态部署，无法通过任务交互持续提升；而经验驱动方法仅依赖记忆或启发式规则，未增强模型的学习能力，导致性能过早饱和、长期提升有限。

Method: 提出MetaEvo两阶段框架：第一阶段利用基于偏好的优化（preference-based optimization）增强模型的原则抽象能力；第二阶段采用模块化代理架构实现这些原则的积累与复用。

Result: 在多个推理基准上，MetaEvo持续优于强基线方法，并在迭代过程中保持可靠的性能提升。

Conclusion: 验证了元优化（meta-optimization）使代理能够从经验中学习并持续增强推理能力的有效性。

Original Abstract: Large language models (LLMs) exhibit strong reasoning capabilities, yet most LLM-based agents are statically deployed and unable to improve through task interactions. Existing experience-driven methods often rely on memory or heuristics without enhancing the model's ability to learn, treating it as a passive executor and leading to early performance plateaus and limited long-term improvement. To address this issue, we propose MetaEvo, a two-stage framework for continual agent evolution that focuses on improving how the model learns from tasks experience, rather than solely on what it stores. MetaEvo first applies preference-based optimization to enhance the model's ability of principle abstraction, then enables the accumulation and reuse of these principles within a modular agent architecture. Experimental results on diverse reasoning benchmarks demonstrate that MetaEvo consistently outperforms strong baselines, maintains reliable improvement across iterations. These findings validate the effectiveness of meta-optimization in enabling agents to learn from experience and continually enhance their reasoning capabilities.

中文摘要翻译: 大型语言模型（LLM）展现出强大的推理能力，然而大多数基于LLM的代理是静态部署的，无法通过任务交互来改进。现有的经验驱动方法通常依赖记忆或启发式规则，而没有增强模型的学习能力，将模型视为被动的执行器，导致其性能过早达到平台期且长期提升有限。为解决这一问题，我们提出了MetaEvo，一个用于代理持续进化的两阶段框架，其重点在于改进模型如何从任务经验中学习，而非仅仅关注存储了什么。MetaEvo首先应用基于偏好的优化来增强模型的原则抽象能力，然后在模块化代理架构中实现这些原则的积累与复用。在多个多样化推理基准上的实验结果表明，MetaEvo持续优于强基线方法，并在各轮迭代中保持可靠的性能提升。这些发现验证了元优化在使代理能够从经验中学习并持续增强其推理能力方面的有效性。

</details>


### [248] [Contribution Weights: A Geometrical Analysis of Self-Attention Transformers](https://arxiv.org/abs/2606.07604)
*Harry Jake Cunningham,Nicola Muca Cirone*

Main category: cs.LG

TL;DR: 本文提出贡献权重（Contribution Weights）作为新的投影基度量，以克服传统注意力权重在解释LLM信息流时忽略值向量几何特性的局限。实验证明该度量能更忠实识别关键语义token，并揭示注意力汇聚（attention sinks）具有主动抑制信息的功能性作用。

<details>
  <summary>Details</summary>

Motivation: 传统注意力权重分析忽略了被聚合的值向量的几何属性（大小和方向），导致对token重要性的度量不够准确。

Method: 提出贡献权重（Contribution Weights），一种基于投影的度量，综合考虑token的注意力权重、值向量大小及其与层输出的方向对齐程度来量化token影响力。

Result: 1) 贡献权重在识别语义关键token方面一致优于基于注意力权重的度量；2) 揭示注意力汇聚具有主动功能性作用——通过汇聚率与输出范数之间的凸关系抑制信息，并通过对抗低置信度token的语义漂移来稳定表征。

Conclusion: 贡献权重是一种更忠实、有效的token重要性度量方法，并为注意力汇聚的主动功能角色提供了新的机制性见解。

Original Abstract: Analyzing attention weights has become a standard approach for interpreting the information flow of Large Language Models (LLMs). However, this approach has significant limitations as it neglects the geometric properties of the value vectors being aggregated. To address this gap, we introduce \emph{Contribution Weights}, a projection-based metric that quantifies a token's influence by accounting for it's attention weight, value magnitude, and directional alignment with the layer output. We demonstrate that contribution weights provide a more faithful measure of token importance, consistently outperforming attention-based metrics in identifying semantically critical tokens across different decoder-only models, tasks, and datasets. Further, our metric enables novel mechanistic analysis of \emph{attention sinks}. While previous work characterized sinks as passive repositories for excess attention, we reveal they serve an active functional role, suppressing information through a convex relationship between sink rate and output norm, stabilizing representations by opposing the semantic drift of low-confidence tokens.

中文摘要翻译: 分析注意力权重已成为解释大型语言模型（LLMs）信息流动的标准方法。然而，这种方法存在显著局限性，因为它忽略了被聚合的值向量的几何属性。为弥补这一不足，我们引入\textit{贡献权重（Contribution Weights）}，一种基于投影的度量，通过综合考虑token的注意力权重、值向量大小及其与层输出的方向对齐程度来量化token的影响力。我们证明贡献权重提供了更忠实的token重要性度量，在多种仅解码器模型、任务和数据集上识别语义关键token时，一致优于基于注意力权重的度量。此外，该度量使我们能够对\textit{注意力汇聚（attention sinks）}进行新颖的机制分析。以往研究将汇聚描述为被动容纳过剩注意力的存储库，而我们揭示其发挥着主动的功能性作用：通过汇聚率与输出范数之间的凸关系来抑制信息，并通过对抗低置信度token的语义漂移来稳定表征。

</details>


### [249] [SRT: Super-Resolution for Time Series via Disentangled Rectified Flow](https://arxiv.org/abs/2606.07605)
*Jufang Duan,Shenglong Xiao,Yuren Zhang*

Main category: cs.LG

TL;DR: 提出一种基于解耦整流流的时间序列超分辨率框架SRT，通过分解趋势与季节成分、隐式神经表示对齐分辨率以及跨分辨率注意力机制，实现了从低分辨率输入重建高分辨率时间序列，并在九个数据集上超越现有方法。

<details>
  <summary>Details</summary>

Motivation: 高时间分辨率的时间序列数据对许多应用至关重要，但采集成本高、可行性受限。现有图像超分辨率方法无法直接迁移到时间序列领域，因此需要专门针对时间序列的超分辨率框架。

Method: 提出SRT框架，包含三个核心模块：(1) 将低分辨率输入分解为趋势成分和季节成分；(2) 使用隐式神经表示将分解后的成分对齐到目标分辨率；(3) 设计跨分辨率注意力机制引导高分辨率细节的生成。此外还推出SRT-large版本，通过大规模预训练实现零样本超分辨率能力。

Result: 在九个公开数据集上的实验表明，SRT和SRT-large在多个尺度因子下均一致优于现有方法，且各组件均有效。SRT-large展现出强大的零样本超分辨率能力。

Conclusion: 提出的SRT框架有效解决了时间序列超分辨率问题，通过解耦整流流和跨分辨率注意力机制，能够从低分辨率输入中重建丢失的时间模式，具有鲁棒性和可扩展性。

Original Abstract: Fine-grained time series data with high temporal resolution is critical for accurate analytics across a wide range of applications. However, the acquisition of such data is often limited by cost and feasibility. This problem can be tackled by reconstructing high-resolution signals from low-resolution inputs based on specific priors, known as super-resolution. While extensively studied in computer vision, directly transferring image super-resolution techniques to time series is not trivial. To address this challenge at a fundamental level, we propose Super-Resolution for Time series (SRT), a novel framework that reconstructs temporal patterns lost in low-resolution inputs via disentangled rectified flow. SRT decomposes the input into trend and seasonal components, aligns them to the target resolution using an implicit neural representation, and leverages a novel cross-resolution attention mechanism to guide the generation of high-resolution details. We further introduce SRT-large, a scaled-up version with extensive pre-training, which enables strong zero-shot super-resolution capability. Extensive experiments on nine public datasets demonstrate that SRT and SRT-large consistently outperform existing methods across multiple scale factors, showing both robust performance and the effectiveness of each component in our architecture.

中文摘要翻译: 高时间分辨率的细粒度时间序列数据对于广泛应用的精确分析至关重要。然而，此类数据的获取往往受限于成本和可行性。这一问题可以通过基于特定先验从低分辨率输入重建高分辨率信号（即超分辨率）来解决。尽管超分辨率在计算机视觉领域已被广泛研究，但直接将图像超分辨率技术迁移到时间序列并不简单。为从根本上应对这一挑战，我们提出了时间序列超分辨率框架SRT，该框架通过解耦整流流来重建低分辨率输入中丢失的时间模式。SRT将输入分解为趋势成分和季节成分，利用隐式神经表示将其对齐到目标分辨率，并设计了一种新颖的跨分辨率注意力机制来引导高分辨率细节的生成。我们进一步推出了SRT-large，即通过大规模预训练扩展的版本，使其具备强大的零样本超分辨率能力。在九个公开数据集上的大量实验表明，SRT和SRT-large在多个尺度因子下均一致优于现有方法，展现出鲁棒的性能，并验证了我们架构中每个组件的有效性。

</details>


### [250] [QDSP: An Interpretable Structured Learning Framework for Predicting Death or Cerebral Palsy in Very Low Birth Weight Infants](https://arxiv.org/abs/2606.07606)
*Ling Wang,Xiaolong Li,Hui Zhou,Jing Shi,Fuhao Zhang,Dapeng Chen,Nan Mu*

Main category: cs.LG

TL;DR: 提出QDSP框架，一种结合配额引导子空间采样与可微决策结构感知的可解释结构化学习方法，用于极低出生体重婴儿出院时预后风险分层，在51例真实队列中AUC达0.9714。

<details>
  <summary>Details</summary>

Motivation: 极低出生体重婴儿（VLBWI）死亡率和严重神经发育障碍风险高，但在高维、小样本临床场景下，出院时可靠的预后分层仍具挑战性。

Method: 提出QDSP框架，包含两个模块：（1）配额引导子空间采样（QSS），通过基于bootstrap的特征一致性估计构建稳定性感知、低冗余的特征子空间；（2）可微决策引导的结构感知（DSP），采用可微软斜决策结构建模非线性临床交互，同时保留可追溯的决策证据。

Result: 在51例VLBWI真实队列上，QDSP准确率0.9200，AUC 0.9714，优于XGBoost、TabNet、TabPFN等基线；在三个公开医疗表格数据集上保持竞争力；SHAP分析和可微决策路径追踪识别出囊性脑室周围白质软化（cPVL）和出生体重等临床相关预测因子。

Conclusion: QDSP为VLBWI出院时风险分层提供了可解释且鲁棒的框架，可支持新生儿重症监护中早期个体化临床决策。

Original Abstract: Very low birth weight infants (VLBWI) are at high risk of mortality and severe neurodevelopmental impairment, including cerebral palsy, yet reliable discharge-time prognostic stratification remains challenging in high-dimensional and data-limited clinical settings. To address this problem, we propose QDSP, an interpretable structured learning framework that integrates Quota-guided Subspace Sampling (QSS) and Differentiable-decision-guided Structure Perception (DSP). The QSS module constructs stability-aware and low-redundancy feature subspaces through bootstrap-based feature consistency estimation, whereas the DSP module employs differentiable soft oblique decision structures to model nonlinear clinical interactions while preserving traceable decision evidence. The proposed framework was evaluated on a real-world VLBWI cohort comprising 51 infants and further validated on three public medical tabular datasets. On the primary cohort, QDSP achieved an accuracy of 0.9200 and an AUC of 0.9714, outperforming representative machine learning and deep tabular learning baselines, including XGBoost, TabNet, and TabPFN. Across external datasets, QDSP maintained competitive discrimination and calibration under varying sample sizes and clinical distributions. In addition, SHAP-based analyses and differentiable decision-path tracing identified clinically relevant predictors, including cystic periventricular leukomalacia (cPVL) and birth weight, consistent with established neonatal pathophysiological evidence. These results suggest that QDSP provides an interpretable and robust framework for discharge-time risk stratification in VLBWI and may support early individualized clinical decision-making in neonatal intensive care settings.

中文摘要翻译: 极低出生体重婴儿（VLBWI）具有较高的死亡率和严重神经发育障碍（包括脑瘫）风险，然而在高维且数据有限的临床场景中，可靠的出院时预后分层仍充满挑战。为解决这一问题，我们提出QDSP，一个可解释的结构化学习框架，该框架集成了配额引导子空间采样（QSS）和可微决策引导的结构感知（DSP）。QSS模块通过基于bootstrap的特征一致性估计构建稳定性感知且低冗余的特征子空间，而DSP模块采用可微软斜决策结构来建模非线性临床交互，同时保留可追溯的决策证据。该框架在一个包含51例婴儿的真实VLBWI队列上进行了评估，并在三个公开的医疗表格数据集上进一步验证。在主要队列上，QDSP达到了0.9200的准确率和0.9714的AUC，优于代表性的机器学习和深度表格学习基线方法，包括XGBoost、TabNet和TabPFN。在外部数据集上，QDSP在不同样本量和临床分布下保持了具有竞争力的判别能力和校准性能。此外，基于SHAP的分析和可微决策路径追踪识别出了临床相关的预测因子，包括囊性脑室周围白质软化（cPVL）和出生体重，这与已有的新生儿病理生理学证据一致。这些结果表明，QDSP为VLBWI出院时风险分层提供了一个可解释且鲁棒的框架，并可能支持新生儿重症监护环境中的早期个体化临床决策。

</details>


### [251] [Position: Genomic Model Research Must Move Beyond Anecdotal Evaluation of Interpretability Methods](https://arxiv.org/abs/2606.07607)
*Shasha Zhou,Mingyu Huang,Ke Li*

Main category: cs.LG

TL;DR: 本文通过转录因子结合位点预测的基准研究，揭示了可解释机器学习（IML）方法在基因组学中存在的三个严重问题：不同IML方法给出矛盾解释、无法定位已知调控基序、不能忠实反映模型内部决策。作者类比临床试验，提出了一个分层验证框架，推动基因组可解释性从"挑例子"式验证转向系统化评估。

<details>
  <summary>Details</summary>

Motivation: 目前基因组学中可解释机器学习方法普遍依赖"轶事验证"——大多数研究仅使用单一IML方法并只报告孤立的成功案例，缺乏系统性的评估标准，导致解释结果不可靠且无法重复。

Method: 通过转录因子结合位点预测任务进行基准测试，系统比较不同IML方法在解释一致性、基序定位能力和决策忠实度三个维度的表现。

Result: (1)不同IML方法对相同预测给出矛盾的解释；(2)多种方法无法定位已知的调控基序；(3)解释结果不能忠实反映模型内部决策过程。

Conclusion: 基因组可解释性需要类似临床试验的严格验证框架，本文提出一个分层框架，引导对基因组IML方法进行一致性、忠实性和生物学有效性的系统化评估与报告。

Original Abstract: Advances in machine learning and computational power have unlocked the predictive potential of the human genome, yet biologists now demand that these models also elucidate the underlying biological mechanisms. While interpretable machine learning (IML) techniques have been increasingly applied to bridge this gap, there has been a pervasive reliance on anecdotal validation: the vast majority of research relies on a single IML method and reports only isolated successful instances. Through a benchmarking study on transcription factor binding, we demonstrate the risks of current practices. We show that different IML methods can often (1) yield contradictory explanations for the same predictions, (2) fail to localize known regulatory motifs, and (3) fail to faithfully reflect the model's internal decision process. In light of this, we argue for a validation framework analogous to clinical trials: just as trials require rigorous design and adverse-event reporting, genomic interpretability must move beyond cherry-picked plausibility toward systematic assessment of consistency, faithfulness, and biological validity. To facilitate this, we propose a tiered framework to guide rigorous evaluation and reporting of genomic IML methods.

中文摘要翻译: 机器学习和计算能力的进步释放了人类基因组的预测潜力，然而生物学家现在要求这些模型也能阐明潜在的生物学机制。尽管可解释机器学习（IML）技术被越来越多地用于弥合这一差距，但一直普遍存在对轶事验证的依赖：绝大多数研究仅依赖单一的IML方法，并且只报告孤立的成功案例。通过对转录因子结合位点的基准研究，我们展示了当前实践的风险。我们发现不同的IML方法往往（1）对相同的预测给出矛盾的解释，（2）无法定位已知的调控基序，以及（3）不能忠实反映模型内部决策过程。有鉴于此，我们主张建立一个类似于临床试验的验证框架：正如试验需要严格的设计和不良事件报告一样，基因组可解释性必须超越"挑例子"式的合理性验证，转向对一致性、忠实性和生物学有效性的系统性评估。为促进这一转变，我们提出了一个分层框架，以指导对基因组IML方法进行严格的评估与报告。

</details>


### [252] [LEAF: Growing Trees Without Branching for Speech-Aware Large Language Model Post-Training](https://arxiv.org/abs/2606.07610)
*Argyrios Gerogiannis,Yekaterina Yegorova,Mark Hasegawa-Johnson,Venugopal V. Veeravalli*

Main category: cs.LG

TL;DR: 提出LEAF方法，通过回溯式树结构进行细粒度信用分配，在语音感知大模型后训练中优于GRPO，且小模型可超越全参数基线。

<details>
  <summary>Details</summary>

Motivation: 现有GRPO风格的语音感知大模型后训练方法存在粗粒度信用分配问题，将相同的终端奖励优势广播给每个token，忽略了rollout批次中响应前缀共享、后续才分叉的有用结构信息。

Method: 提出低秩探索自适应分叉（LEAF）方法，一种基于回溯的树结构强化学习方法。无需在线分支或额外解码，而是采样完整响应、选择高惊讶度边界、按共享前缀对响应分组，并使用后代奖励分配片段级优势。

Result: 在语音问答和语音翻译基准上，相同rollout和低秩适应预算下，LEAF优于GRPO。使用LEAF训练的较小模型性能超越了当前最先进的全参数基线模型。

Conclusion: LEAF通过回溯式树结构实现片段级信用分配，有效提升语音感知大模型后训练效果，证明了细粒度信用分配对强化学习后训练的重要性。

Original Abstract: State-of-the-art GRPO-style methods for speech-aware large language model post-training suffer from coarse credit assignment, broadcasting the same terminal-reward advantage to every token in a response. This ignores useful structure within rollout batches, where speech-conditioned completions often share prefixes before diverging at important decisions. We propose Low-rank Exploration with Adaptive Forking (LEAF), a retrospective tree-based RL method that recovers this structure without online branching or additional decoding. LEAF samples complete responses, selects high-surprisal boundaries, groups responses by shared prefixes, and assigns span-level advantages using descendant rewards. We theoretically justify LEAF's span-level credit assignment and boundary-selection design. Empirically, LEAF improves over GRPO across speech question answering and speech translation benchmarks under the same rollout and low-rank adaptation budget. Notably, smaller LEAF-trained models outperform current state-of-the-art, full-parameter baselines.

中文摘要翻译: 最先进的GRPO风格方法在语音感知大语言模型后训练中存在粗粒度信用分配问题，将相同的终端奖励优势广播给响应中的每个token。这忽略了rollout批次中有用的结构信息——语音条件化的补全序列通常在重要决策点之前共享前缀。我们提出低秩探索自适应分叉（LEAF），一种基于回溯的树结构强化学习方法，无需在线分支或额外解码即可恢复这种结构。LEAF采样完整响应、选择高惊讶度边界、按共享前缀对响应分组，并使用后代奖励分配片段级优势。我们从理论上论证了LEAF的片段级信用分配和边界选择设计。实验上，在相同的rollout和低秩适应预算下，LEAF在语音问答和语音翻译基准上均优于GRPO。值得注意的是，使用LEAF训练的较小模型超越了当前最先进的全参数基线模型。

</details>


### [253] [Measuring Poverty and Inequality with Reduced Data: A Machine Learning Approach Using Nigerian Household Data](https://arxiv.org/abs/2606.07614)
*Vanesa Jordá,Miguel Niño-Zarazúa*

Main category: cs.LG

TL;DR: 本文研究了能否通过简化调查工具（使用随机森林递归特征消除法，RF-RFE）来保留收入与消费的关键分布信息。基于2018/19年尼日利亚面板调查数据，结果显示仅用少量预测变量即可高精度分类贫困状态和不平等线位置，消费五分位分类准确率达80%（季节性）和60-65%（年度），收入贫困状态准确率约90%。这表明机器学习可优化调查设计并降低数据需求。

<details>
  <summary>Details</summary>

Motivation: 低收入和中等收入国家常规监测贫困与不平等需要可靠的收入与消费测量，但完整的入户调查成本高昂且难以定期实施。因此，研究者希望探究能否通过精简的调查工具，在保留关键福利分布信息的前提下降低数据收集成本。

Method: 采用随机森林递归特征消除法（Random Forest Recursive Feature Elimination, RF-RFE）对2018/19年尼日利亚通用家庭调查面板数据进行分析，识别出最能对个体福利分布进行分类的收入来源、消费类别和家庭特征。分析聚焦三个结果变量：贫困状态、五分位分布位置、基于基尼系数的不平等线相对位置。利用调查中种植后和收获后两个时期的数据评估不同季节背景下的表现。

Result: RF-RFE以少量预测变量实现了强分类精度：消费方面，用少量支出类别即可准确预测贫困状态和不平等线位置；五分位分类在季节性消费上准确率约80%，从单次季节访问预测年消费时准确率为60-65%。收入方面，使用五个预测变量贫困状态准确率约90%，不平等线位置主要由劳动收入反映。

Conclusion: 机器学习方法有助于改进调查设计、减少数据需求，同时保留测量和监测贫困与不平等所需的大部分分布信息。

Original Abstract: Reliable measurement of income and consumption is essential for monitoring poverty and inequality in low- and middle-income countries, yet full household surveys are costly and difficult to implement regularly. This paper examines whether reduced survey instruments can preserve key distributional information. We apply Random Forest Recursive Feature Elimination (RF-RFE) to the 2018/19 Nigeria General Household Survey-Panel to identify the income sources, consumption categories and household characteristics that best classify individuals within the welfare distribution. The analysis focuses on three outcomes: poverty status, location in the quintile distribution and position relative to the Gini-based inequality line. The survey's post-planting and post-harvest periods allow us to assess performance under different seasonal contexts. Results show that RF-RFE achieves strong classification accuracy with few predictors. For consumption, poverty status and inequality-line position are accurately predicted using a small set of expenditure categories, while quintile classification reaches about 80 percent accuracy for seasonal consumption and 60--65 percent for annual consumption predicted from a single seasonal visit. For income, poverty status reaches around 90 percent accuracy with five predictors, and inequality-line position is largely captured by labour earnings. The findings suggest that machine-learning methods can help improve survey design and reduce data requirements while retaining much of the distributional information needed to measure and monitor poverty and inequality.

中文摘要翻译: 可靠测量收入与消费对于监测低收入和中等收入国家的贫困与不平等至关重要，但完整的家庭调查成本高昂且难以定期实施。本文研究了精简的调查工具能否保留关键的分布信息。我们运用随机森林递归特征消除法（RF-RFE）对2018/19年尼日利亚通用家庭调查面板数据进行分析，识别出最能对个体福利分布进行分类的收入来源、消费类别和家庭特征。分析聚焦三个结果指标：贫困状态、五分位分布位置以及基于基尼系数的不平等线相对位置。调查涵盖种植后和收获后两个时期，使我们能够评估不同季节背景下的表现。结果显示，RF-RFE以少量预测变量实现了强分类精度。在消费方面，使用少量支出类别即可准确预测贫困状态和不平等线位置，而五分位分类在季节性消费上达到约80%的准确率，从单次季节访问预测年消费时准确率为60-65%。在收入方面，使用五个预测变量时贫困状态准确率约为90%，不平等线位置主要由劳动收入反映。研究结果表明，机器学习方法有助于改进调查设计并减少数据需求，同时保留测量和监测贫困与不平等所需的大部分分布信息。

</details>


### [254] [Structured Neuron Pruning in Deep Neural Networks Using Multi-Armed Bandits](https://arxiv.org/abs/2606.07615)
*Salem Ameen,Sunil Vadera*

Main category: cs.LG

TL;DR: 本文提出一种基于多臂老虎机（MAB）的结构化剪枝框架，通过将每个神经元视为一个臂，在训练过程中动态评估并移除冗余神经元，实验表明UCB1和Thompson Sampling等策略在表格分类、回归及深度学习任务中均能有效压缩模型且性能优于未剪枝模型及传统剪枝方法。

<details>
  <summary>Details</summary>

Motivation: 深度神经网络中常存在冗余隐藏单元，传统的权重剪枝虽然能减少参数量，但非结构化稀疏性难以在标准稠密实现中高效利用，因此需要一种能够直接移除完整神经元的结构化剪枝方法。

Method: 提出基于多臂老虎机（MAB）的结构化剪枝框架：将每个候选神经元视为一个臂，拉动手臂时临时屏蔽该神经元，在小批量样本上测量损失变化，然后恢复该神经元并更新其安全移除奖励估计。支持随机策略（Epsilon-Greedy、Softmax、UCB1、Thompson Sampling）和乘法权重策略（Hedge风格乘法权重、EXP3）。

Result: 在表格分类任务中UCB1获得最高平均排名且优于未剪枝网络；在回归任务中UCB1获得最高平均排名且在R²指标上与多个标准回归模型具有统计竞争力或更优；在深度学习任务中UCB1和Thompson Sampling获得最强排名，多个MAB策略显著优于未剪枝模型、基于幅度的神经元剪枝和贪婪激活变化剪枝。使用Friedman检验和Nemenyi事后检验进行统计比较。

Conclusion: 基于MAB的神经元剪枝是一种有效且计算实用的结构化模型约简方法。

Original Abstract: Deep neural networks often contain redundant hidden units. Removing individual weights can reduce parameter count, but unstructured sparsity is not always easy to exploit in standard dense implementations. This paper develops a structured pruning framework in which complete neurons are removed using multi-armed bandit (MAB) algorithms. Each candidate neuron is treated as an arm; pulling an arm temporarily masks that neuron, measures the change in loss on a sampled mini-batch, restores the neuron, and updates an estimate of its safe-removal reward. The framework supports stochastic policies, including Epsilon-Greedy, Softmax, UCB1 and Thompson Sampling, and multiplicative-weight policies, including Hedge-style multiplicative weights and EXP3. We evaluate the method on tabular classification, tabular regression and deep neural-network benchmarks covering image, text and reasoning tasks. Statistical comparisons using the Friedman test followed by the Nemenyi post-hoc test show significant differences between methods. On tabular classification tasks, UCB1 obtains the highest mean rank among pruning policies and improves on the unpruned neural network. On regression tasks, UCB1 obtains the highest mean rank and is statistically competitive with, or superior to, several standard regression models according to R^2. On deep-learning tasks, UCB1 and Thompson Sampling obtain the strongest ranks, and several MAB policies significantly outperform the unpruned model, magnitude-based neuron pruning and greedy activation-variation pruning. The results show that MAB-based neuron pruning is an effective and computationally practical approach for structured model reduction.

中文摘要翻译: 深度神经网络通常包含冗余的隐藏单元。移除单个权重可以减少参数数量，但非结构化稀疏性并不总是容易在标准稠密实现中利用。本文开发了一个结构化剪枝框架，使用多臂老虎机（MAB）算法移除完整的神经元。每个候选神经元被视为一个臂；拉动手臂会临时屏蔽该神经元，在采样的一个小批量上测量损失变化，然后恢复该神经元并更新其安全移除奖励的估计。该框架支持随机策略（包括Epsilon-Greedy、Softmax、UCB1和Thompson Sampling）以及乘法权重策略（包括Hedge风格的乘法权重和EXP3）。我们在覆盖图像、文本和推理任务的表格分类、表格回归和深度神经网络基准上评估了该方法。使用Friedman检验和Nemenyi事后检验进行的统计比较显示了方法之间的显著差异。在表格分类任务上，UCB1在剪枝策略中获得最高平均排名，且优于未剪枝的神经网络。在回归任务上，UCB1获得最高平均排名，并且根据R²指标，与多个标准回归模型相比具有统计竞争力或更优。在深度学习任务上，UCB1和Thompson Sampling获得最强排名，且多个MAB策略显著优于未剪枝模型、基于幅度的神经元剪枝和贪婪激活变化剪枝。结果表明，基于MAB的神经元剪枝是一种有效且计算实用的结构化模型约简方法。

</details>


### [255] [Item Response Scaling Laws: A Measurement Theory Approach for Efficient and Generalizable Neural Scaling Estimation](https://arxiv.org/abs/2606.07616)
*Sang Truong,Yuheng Tu,Rylan Schaeffer,Sanmi Koyejo*

Main category: cs.LG

TL;DR: 提出项目反应缩放定律（IRSL），将项目反应理论融入缩放定律框架，通过Beta-IRT模型将参数复杂度从O(M×N)降至O(M+N)，在仅需50个问题（减少99.9%）的情况下实现可靠缩放估计。

<details>
  <summary>Details</summary>

Motivation: 传统缩放定律的推导需要在数千个检查点或数百万推理样本上进行评估，成本高昂且难以负担。现有方法将每个模型-基准对孤立处理，参数复杂度为O(M×N)，效率低下。

Method: 提出IRSL框架，将项目反应理论（IRT）融入缩放定律。具体实例化为Beta-IRT模型，利用语言模型的经验概率响应（如预训练中的token概率和测试时采样的通过率）替代二值响应，将潜在模型能力与问题特征解耦，使参数复杂度从O(M×N)降至O(M+N)。

Result: 在预训练下游缩放（6,612个检查点、37,682个问题、10个基准）和测试时缩放（12个LM、120个问题、每个问题最多2,500个样本）两个范式中验证。仅需50个问题（减少99.9%）即可实现与传统方法相当或更优的决策精度，且估计的潜在模型能力可跨基准泛化。

Conclusion: IRSL框架能够大幅降低缩放定律估计所需的评估成本，同时保持甚至提升估计可靠性，并且估计的模型能力具有跨基准的泛化性，可有效预测共享相同测量目标的基准上的性能。

Original Abstract: Scaling laws provide a fundamental framework for understanding the performance of Language Models (LMs), yet deriving them requires prohibitively expensive evaluations across thousands of checkpoints or millions of inference samples. To address this, we introduce Item Response Scaling Laws (IRSL), a unified framework that integrates Item Response Theory (IRT) within the scaling law framework. Unlike traditional approaches that treat each model-benchmark pair in isolation, IRSL disentangles latent model ability from question characteristics, factorizing the scaling law estimation for $M$ models and $N$ questions to significantly reduce parameter complexity from $O(M \times N)$ to $O(M + N)$. We instantiate IRSL with Beta-IRT, which leverages the empirical probability responses of LMs -- such as token probabilities in pre-training and pass rates in test-time sampling -- to capture richer signals than binary responses. We validate our approach across two prevalent scaling paradigms: (1) pre-training downstream scaling, using 6,612 LM checkpoints and 37,682 questions from 10 benchmarks; and (2) test-time scaling, using 12 LMs and 120 questions from 4 benchmarks with up to 2,500 samples per question. Given a one-time calibration on existing model responses, IRSL yields more reliable scaling estimates using only 50 questions per benchmark (a 99.9\% reduction), achieving comparable or superior decision accuracy to traditional approaches. Furthermore, we show that the estimated latent model abilities are generalizable, enabling accurate performance forecasting across benchmarks that share the same measurement objective.

中文摘要翻译: 缩放定律为理解语言模型（LM）的性能提供了基础框架，然而推导缩放定律需要在数千个检查点或数百万推理样本上进行成本高昂的评估。为解决这一问题，我们提出项目反应缩放定律（IRSL），这是一个将项目反应理论（IRT）融入缩放定律框架的统一框架。与传统方法将每个模型-基准对孤立处理不同，IRSL将潜在模型能力与问题特征解耦，将M个模型和N个问题的缩放定律估计参数复杂度从O(M×N)显著降低至O(M+N)。我们以Beta-IRT模型实例化IRSL，该模型利用语言模型的实证概率响应（如预训练中的token概率和测试时采样的通过率）来捕获比二值响应更丰富的信号。我们在两种主流的缩放范式中验证了我们的方法：（1）预训练下游缩放，使用6,612个LM检查点和来自10个基准的37,682个问题；（2）测试时缩放，使用12个LM和来自4个基准的120个问题，每个问题最多2,500个样本。在现有模型响应上进行一次性校准后，IRSL仅需每个基准50个问题（减少99.9%）即可提供更可靠的缩放估计，其决策精度与传统方法相当或更优。此外，我们表明估计的潜在模型能力具有泛化性，能够准确预测共享相同测量目标的基准上的性能。

</details>


### [256] [Query Lens: Interpreting Sparse Key-Value Features with Indirect Effects](https://arxiv.org/abs/2606.07617)
*Hwiyeong Lee,Ingyu Bang,Uiji Hwang,Hyelim Lim,Taeuk Kim*

Main category: cs.LG

TL;DR: 提出Query Lens方法，通过联合考虑编码器侧关键特征和解码器侧值特征，并纳入下游模块的间接效应，实现对稀疏自编码器特征更全面、更忠实的解释。实验表明该方法能解释Logit Lens无法解释的特征，并提出了子空间通道假说。

<details>
  <summary>Details</summary>

Motivation: 稀疏自编码器提供的特征虽然比单个神经元更可解释，但可靠地表征这些特征仍然具有挑战性。现有Logit Lens方法仅捕捉直接效应，无法全面解释特征。

Method: 提出Query Lens方法，它扩展了Logit Lens：(1) 联合考虑编码器侧的关键特征（激活特征的输入）和解码器侧的值特征（特征促进的输出）；(2) 考虑特征经下游模块处理时产生的间接、模块介导的效应，超越Logit Lens仅捕捉的直接效应。

Result: 实验发现，Query Lens能够为那些在Logit Lens下无法解释的特征生成连贯的标记（token）签名。

Conclusion: 提出子空间通道假说（Subspace Channel Hypothesis），认为下游模块通过层特定子空间读取特征。

Original Abstract: While sparse autoencoders provide features more interpretable than individual neurons, reliably characterizing them remains challenging. We propose Query Lens, which extends Logit Lens to enable more comprehensive and faithful interpretations of sparse features. By jointly considering encoder-side key features and decoder-side value features, we identify both the inputs that activate a feature and the outputs it promotes. We also account for indirect, module-mediated effects that arise when the feature is processed by downstream modules, going beyond the direct effect captured by Logit Lens. In experiments, we find that Query Lens yields coherent token signatures for features that remain uninterpretable under Logit Lens. Finally, we propose the Subspace Channel Hypothesis, suggesting that downstream modules read features through layer-specific subspaces.

中文摘要翻译: 尽管稀疏自编码器提供的特征比单个神经元更具可解释性，但可靠地表征这些特征仍然具有挑战性。我们提出Query Lens，它扩展了Logit Lens，能够对稀疏特征进行更全面、更忠实的解释。通过联合考虑编码器侧的关键特征和解码器侧的值特征，我们同时识别了激活特征的输入以及特征所促进的输出。我们还考虑了特征经下游模块处理时产生的间接、模块介导的效应，超越了Logit Lens所捕捉的直接效应。在实验中，我们发现Query Lens能够为那些在Logit Lens下无法解释的特征生成连贯的标记签名。最后，我们提出了子空间通道假说，认为下游模块通过层特定子空间读取特征。

</details>


### [257] [ScaleSweep: Accurate NVFP4 Post-Training Quantization of LLMs via Block Scale Initialization](https://arxiv.org/abs/2606.07618)
*Li Lin,Xiaojun Wan*

Main category: cs.LG

TL;DR: 提出ScaleSweep，一种针对NVFP4格式的块尺度优化方法，通过扫描可行尺度候选并最小化目标函数来提升4-bit量化精度，并在Llama和Qwen模型上验证了有效性。

<details>
  <summary>Details</summary>

Motivation: 现有的NVFP4尺度初始化方法主要依赖AbsMax初始化，与最优解之间存在明显差距。

Method: 提出ScaleSweep方法，扫描可行的块尺度候选集，选择最小化目标函数（如MSE或WMSE）的最佳候选；并给出NVFP4量化的理论分析，推导出MSE和WMSE下所需扫描范围的下界和上界，从而大幅缩减搜索空间。

Result: 在Llama和Qwen模型上，ScaleSweep持续优于现有初始化方法，缩小了与全精度的差距；在权重、激活、KV缓存和查询状态的激进端到端量化下，保留了超过93%的全精度性能。

Conclusion: ScaleSweep是一种简单高效的NVFP4尺度优化方法，通过理论指导的扫描范围裁剪，实现了极低的额外开销，并显著提升了4-bit量化性能。

Original Abstract: NVFP4 is a recently introduced hardware-supported FP4 format that improves the fidelity of 4-bit quantization through fine-grained block scales. However, existing NVFP4 scale initialization methods still primarily rely on AbsMax initialization, which leaves a noticeable gap to the optimal solution. To address this, we propose ScaleSweep, a simple and efficient scale optimization method that sweeps over feasible block scale candidates and selects the candidate that minimizes a target objective. We further provide a theoretical analysis of NVFP4 quantization and derive both lower and upper bounds for the required sweep range under mean square error (MSE) and weighted mean square error (WMSE) between the original tensor and the quantized reconstructed tensor. The proposed bounds substantially reduce the sweep space while preserving the optimal candidate, enabling negligible overhead compared with the baseline quantization operators. Experiments on Llama and Qwen models demonstrate that ScaleSweep consistently improves quantization performance over existing initialization methods and further narrows the gap to full precision. In particular, under aggressive end-to-end quantization of weights, activations, KV cache, and query states, ScaleSweep preserves more than 93% of the full-precision performance.

中文摘要翻译: NVFP4是一种近期引入的硬件支持的FP4格式，通过细粒度的块尺度（block scales）提升了4-bit量化的保真度。然而，现有的NVFP4尺度初始化方法仍然主要依赖AbsMax初始化，与最优解之间存在明显差距。为解决这一问题，我们提出ScaleSweep——一种简单高效的尺度优化方法，该方法扫描可行的块尺度候选集，并选择最小化目标函数的候选值。我们进一步提供了NVFP4量化的理论分析，并推导了在原始张量与量化重建张量之间的均方误差（MSE）和加权均方误差（WMSE）下，所需扫描范围的下界和上界。所提出的边界在保留最优候选的前提下大幅缩减了扫描空间，使得相较基线量化算子的额外开销可忽略不计。在Llama和Qwen模型上的实验表明，ScaleSweep在量化性能上持续优于现有的初始化方法，并进一步缩小了与全精度的差距。特别是在对权重、激活、KV缓存和查询状态进行激进的端到端量化时，ScaleSweep保留了超过93%的全精度性能。

</details>


### [258] [Graph Neural Networks for Predicting Solvability of Finite Groups](https://arxiv.org/abs/2606.07619)
*Tal Weissblat*

Main category: cs.LG

TL;DR: 本文提出一个图神经网络（GNN）框架，利用与有限群关联的图表示（如Cayley图）来分类有限群的可解性，旨在探究GNN能否仅从图结构信息中学习群论中的代数性质。

<details>
  <summary>Details</summary>

Motivation: 探索图神经网络能否从图结构的几何表示中学习有限群的代数性质（如可解性），从而建立代数结构与图表示之间的联系。

Method: 构建基于图神经网络（GNN）的分类框架，使用有限群相关的图表示（特别是Cayley图）作为输入，训练模型仅依据图结构信息区分可解群与不可解群。

Result: 在训练集之外的群上评估该框架，以验证GNN在多大程度上能够学习群论中出现的代数性质。

Conclusion: 本文作为概念验证研究，表明GNN能够从图表示中学习有限群的代数性质，揭示了代数结构与图几何表示之间的关联。

Original Abstract: We present a Graph Neural Network (GNN) framework for the classification of finite groups according to their solvability. Using graph representations associated with finite groups, including Cayley graphs (CG), the proposed model is trained to distinguish solvable and non-solvable groups using structural graph information alone. The framework is evaluated on groups outside the training dataset in order to investigate the extent to which GNNs can learn algebraic properties arising in group theory. More broadly, the present work explores the relationship between algebraic structure and graph-based geometric representations of finite groups. The present study is intended as a proof-of-concept investigation of whether GNNs can learn algebraic properties of finite groups from graph-based representations

中文摘要翻译: 我们提出了一个图神经网络（GNN）框架，用于根据有限群的可解性对其进行分类。利用与有限群关联的图表示（包括凯莱图），该模型被训练为仅依据图结构信息来区分可解群与不可解群。该框架在训练数据集之外的群上进行评估，以探究GNN在多大程度上能够学习群论中产生的代数性质。更广泛地说，本工作探索了代数结构与有限群基于图的几何表示之间的关系。本研究旨在作为一项概念验证探索，检验GNN能否从基于图的表示中学习有限群的代数性质。

</details>


### [259] [HASA: Subnet Allocation for Compute-Constrained Model-Heterogeneous Federated Learning](https://arxiv.org/abs/2606.07621)
*Amir Hossein Shahdadian,Ahmed M. Abdelmoniem,Mahdi Taheri,Samira Nazari,Christian Herglotz*

Main category: cs.LG

TL;DR: 提出HASA方法，根据客户端数据异质性分数分配子网宽度，在固定计算预算下实现更优的模型异构联邦学习性能。

<details>
  <summary>Details</summary>

Motivation: 现有模型异构联邦学习的子网分配策略主要考虑设备资源约束，未显式处理客户端数据分布的统计异质性，导致性能不佳。

Method: 提出异构感知子网分配（HASA），基于客户端本地训练数据计算异质性分数，据此分配不同宽度的子网，同时强制执行固定的大小加权计算预算，实现与替代策略的预算匹配比较。

Result: 在七个客户端的下一个词预测任务中，HASA相比均匀分配将平均测试准确率从13.82%提升至14.32%，并改善了最差客户端准确率；在跨域图像分类实验中，异质性感知分配的有效性取决于异质性分数能否反映客户端对模型宽度的需求。

Conclusion: HASA通过感知数据异质性的子网分配策略，在固定预算下有效提升了模型异构联邦学习的整体性能和尾端性能；且将较小子网分配给异质性更强的客户端会损害性能，表明异质性高的客户端需要更大的模型宽度。

Original Abstract: Edge services increasingly use federated learning to personalize on-device models while keeping sensitive data local. In practice, deployments must handle heterogeneity in both client resources and local data distributions. Model-heterogeneous federated learning lowers client cost by allowing each client to train a subnet of a shared supernet, but most subnet-allocation policies are driven by device constraints and do not explicitly account for statistical heterogeneity. This paper proposes Heterogeneity-Aware Subnet Allocation (HASA), a train-only rule that assigns subnet widths based on client heterogeneity scores computed from local training data while enforcing a fixed size-weighted compute budget. This design enables budget-matched comparisons with alternative allocation policies. On an article-title next-word prediction benchmark with seven clients, HASA improves unweighted mean client test accuracy over uniform allocation across 10 matched seeds, increasing mean client test accuracy from 13.82 percent to 14.32 percent, and improves worst-client accuracy on average. In a matched-budget comparison with representative partial-training baselines, HASA achieves the strongest worst-client and tail-client accuracy on this benchmark. A directionality ablation shows that assigning smaller subnets to more heterogeneous clients degrades both mean and tail performance. A cross-domain image-classification study further shows that the effectiveness of heterogeneity-aware allocation depends on how well the heterogeneity score reflects clients' need for additional model width.

中文摘要翻译: 边缘服务日益采用联邦学习来个性化设备端模型，同时将敏感数据保留在本地。在实践中，部署必须处理客户端资源和本地数据分布的双重异质性。模型异构联邦学习通过允许每个客户端训练共享超网络的一个子网来降低客户端成本，但大多数子网分配策略受设备约束驱动，并未显式考虑统计异质性。本文提出异构感知子网分配（HASA），这是一种仅用于训练的规则，基于从本地训练数据计算得到的客户端异质性分数来分配子网宽度，同时强制执行固定的大小加权计算预算。该设计支持与替代分配策略进行预算匹配的比较。在一个包含七个客户端的文章标题下一个词预测基准测试中，HASA在10个匹配种子上相比均匀分配提高了未加权平均客户端测试准确率，将平均客户端测试准确率从13.82%提升至14.32%，并平均改善了最差客户端准确率。在与代表性部分训练基线的匹配预算比较中，HASA在该基准上取得了最强的最差客户端和尾端客户端准确率。方向性消融实验表明，将较小子网分配给异质性更强的客户端会降低平均性能和尾端性能。跨域图像分类研究进一步表明，异质性感知分配的有效性取决于异质性分数能否反映客户端对额外模型宽度的需求。

</details>


### [260] [Airport Terminal Passenger Queue Forecasting for Departure Gates and Security Checkpoints](https://arxiv.org/abs/2606.07622)
*Juhwan Lee,Seokbin Yoon,Keumjin Lee,Hojong Baik,Seyeon Jung*

Main category: cs.LG

TL;DR: 本文提出了一种基于Transformer的机场航站楼旅客排队预测框架，利用历史运行数据（排队长度、等待时间、旅客吞吐量）预测未来两小时内登机口和安检点的排队情况，为主动拥堵管理和人员调配提供实时决策支持。

<details>
  <summary>Details</summary>

Motivation: 机场航站楼中旅客需求随时间变化，且不同离港设施的使用模式存在异质性，使得排队预测具有挑战性；现有方法难以同时捕捉时间依赖性和设施间关联性。

Method: 采用基于Transformer的架构，将历史排队长度、等待时间以及值机岛的旅客吞吐量作为输入，捕获时间依赖性和跨设施相关性；学到的表示映射到两个设施专用的MLP头部，分别预测登机口和安检点的排队长度与等待时间。

Result: 实验结果表明，该方法能够实现长达两小时的高精度排队预测。

Conclusion: 该框架能够为机场航站楼运营提供实时的主动排队管理和人员再分配决策支持，具有实际应用价值。

Original Abstract: Accurate passenger queue forecasting in airport terminals is essential for efficient departure operations, as it enables proactive congestion management. However, time-varying passenger demand and heterogeneous facility usage across multiple departure facilities make forecasting challenging. In this work, we propose a passenger queue forecasting framework that learns historical passenger flow patterns from operational data. The proposed model employs a Transformer-based architecture to capture temporal dependencies and inter-facility correlations using past queue length and waiting time at departure gates and security checkpoints, together with passenger throughput at check-in islands. The learned representations are mapped to two facility-specific MLP heads to predict queue length and waiting time at departure gates and security checkpoints. Experimental results demonstrate accurate forecasts up to two hours ahead. The proposed approach offers practical real-time decision support for proactive queue management and staff reallocation in airport terminal operations.

中文摘要翻译: 在机场航站楼中，准确预测旅客排队情况对于高效的离港运营至关重要，因为它能够实现主动拥塞管理。然而，随时间变化的旅客需求以及多个离港设施之间的异质性设施使用模式使得预测具有挑战性。在这项工作中，我们提出了一种旅客排队预测框架，该框架从运行数据中学习历史旅客流量模式。所提出的模型采用基于Transformer的架构，利用过去的排队长度、登机口和安检点的等待时间以及值机岛的旅客吞吐量，来捕获时间依赖性和设施间相关性。学习到的表示被映射到两个设施专用的多层感知机头部，以预测登机口和安检点的排队长度和等待时间。实验结果表明，该方法能够实现对长达两小时后的排队情况进行准确预测。所提出的方法为机场航站楼运营中的主动排队管理和人员再分配提供了实用的实时决策支持。

</details>


### [261] [Finite Certificates for In-Context Determinacy and a Threshold Theory of Emergence in Language Models](https://arxiv.org/abs/2606.07623)
*Faruk Alpay,Hamdi Alakkad*

Main category: cs.LG

TL;DR: 本文提出一个模型论框架，用有限语义证书替代基准标签来验证上下文条件化语言模型的行为。核心解决了两个问题：有限确定性（上下文中何时存在强制回答查询的例子）和阈值涌现（基准性能跳变何时反映真实语义转变而非评分映射不连续）。证明了一系列理论结果，包括行空间准则、反幻影定理，并建立了基于Keisler测度的布尔概率测度语义演算。

<details>
  <summary>Details</summary>

Motivation: 现有语言模型验证依赖基准标签，但标签本身缺乏语义保证，难以区分模型真正的语义理解与评分映射的伪影。本文旨在建立严格的模型论基础，通过有限语义证书而非基准标签来验证上下文条件化语言模型的行为，并区分真正的语义涌现与虚假的阈值跳变。

Method: 采用模型论框架，将验证问题形式化为两个子问题：有限确定性（通过行空间准则、残差假设计数等代数方法分析）和阈值涌现（通过反幻影定理和速率敏感穿越界分析）。核心语义对象是可定义事件上的置信泛函，证明其等价于布尔概率测度和Keisler测度，并发展出相应的演算系统。

Result: 证明了有限域线性任务族中的精确行空间准则；计算了残差假设计数；导出了完整识别曲线和查询局部识别曲线；证明提取最小强制子上下文是NP完全问题。证明了反幻影定理，区分了阈值化度量与语义置信度；给出了速率敏感穿越界。证明了置信泛函是布尔概率测度（Keisler测度），其测度一公式形成真滤子，Stone空间表示在定义扩展下不变。

Conclusion: 该框架提供了有限上下文证书、对分隔符撞击集、查询教学维度、提示保持准则和尺度极限见证等实用工具，为语言模型行为验证建立了严格的语义基础。

Original Abstract: This paper develops a model-theoretic framework for verifying context-conditioned language-model behavior by replacing benchmark labels with finite semantic certificates. The first problem is finite determinacy: when do examples in a context force the answer to a query without changing model parameters? In finite-field linear task families, we prove an exact row-space criterion, compute the residual hypothesis count, derive full and query-local identification curves, and show that extracting a smallest forcing subcontext is NP-complete even for binary outputs. The second problem is threshold emergence: when does an apparent benchmark jump reflect a semantic transition rather than a discontinuity of the scoring map? We prove an anti-mirage theorem separating thresholded metrics from semantic confidence and give a rate-sensitive crossing bound for latent commitments becoming visible above threshold. The common semantic object is a confidence functional on definable events. We show that it is a Boolean probability measure, equivalently a Keisler measure on the relevant type space, whose measure-one formulas form a proper filter and whose Stone-space representation is invariant under definitional expansion. The resulting calculus provides finite context certificates, pair-separator hitting sets, query teaching dimension, prompt-preservation criteria, and scale-limit witnesses. Exact-arithmetic ancillary scripts reproduce the finite-field and threshold calculations and generate the data used by the figures.

中文摘要翻译: 本文发展了一个模型论框架，通过用有限语义证书替代基准标签来验证上下文条件化语言模型的行为。第一个问题是有限确定性：在上下文中，何时存在一些例子能在不改变模型参数的情况下强制得出查询答案？在有限域线性任务族中，我们证明了精确的行空间准则，计算了残差假设计数，导出了完整识别曲线和查询局部识别曲线，并证明即使对于二元输出，提取最小的强制子上下文也是NP完全的。第二个问题是阈值涌现：何时一个明显的基准性能跳变反映的是语义转变而非评分映射的不连续性？我们证明了一个反幻影定理，将阈值化度量与语义置信度区分开来，并给出了一个关于潜在承诺在阈值之上变得可见的速率敏感穿越界。共同的语义对象是可定义事件上的置信泛函。我们证明它是一个布尔概率测度，等价于相关类型空间上的Keisler测度，其测度为一的公式形成一个真滤子，且其Stone空间表示在定义扩展下不变。由此产生的演算提供了有限上下文证书、对分隔符撞击集、查询教学维度、提示保持准则和尺度极限见证。精确算术辅助脚本可复现有限域和阈值计算，并生成图表所使用的数据。

</details>


### [262] [Sequential statistical inference for Large Language Models: Representation, validity, and monitoring](https://arxiv.org/abs/2606.07624)
*Yao Xie*

Main category: cs.LG

TL;DR: 本文提出将序贯统计推断用于提升大语言模型（LLM）的可信度，将LLM交互建模为依赖随机过程，并围绕表示、有效性和监控三个任务展开讨论，以实现统计过程控制。

<details>
  <summary>Details</summary>

Motivation: 在LLM部署过程中，系统被反复查询、依赖动态上下文、整合用户或工具反馈，且在模型更新或分布变化后可能出现行为偏移，现有方法缺乏对这些依赖性和适应性的统计保证。

Method: 围绕三个核心任务展开：(1) 表示——将LLM交互建模为依赖随机过程而非孤立的提示-响应对；(2) 有效性——在依赖、重复使用和自适应条件下开发有意义的 uncertainty 保证；(3) 监控——利用序贯警报和变点检测识别校准、幻觉率、拒绝行为、公平性等属性的偏移。

Result: 提出将可信LLM部署视为统计过程控制问题的视角，为校准、幻觉率、拒绝行为、公平性等任务相关属性的偏移检测提供了序贯监控框架。

Conclusion: 序贯统计推断能够自然地服务于LLM可信度，补充了现有综述工作，为可信LLM部署提供了统计过程控制的新视角。

Original Abstract: This discussion argues that sequential statistical inference can naturally contribute to LLM trustworthiness. In deployment, LLM systems are queried repeatedly, conditioned on evolving contexts, and incorporate user or tool feedback, and may exhibit behavioral shifts after model updates or distribution changes. The discussion is organized around three tasks: representation, modeling LLM interactions as dependent stochastic processes rather than isolated prompt--response pairs; validity, developing uncertainty guarantees that remain meaningful under dependence, repeated use, and adaptation; and monitoring, using sequential alarms and change-point detection to identify shifts in calibration, hallucination rates, refusal behavior, fairness, or other task-relevant properties. This perspective complements recent surveys by viewing trustworthy LLM deployment as a problem of statistical process control.

中文摘要翻译: 本文认为序贯统计推断可以自然地促进大语言模型（LLM）的可信度。在部署过程中，LLM系统被反复查询，依赖于不断演变的上下文，整合用户或工具反馈，并在模型更新或分布变化后可能出现行为偏移。本文围绕三个任务展开讨论：表示——将LLM交互建模为依赖随机过程而非孤立的提示-响应对；有效性——在依赖、重复使用和自适应条件下开发有意义的 uncertainty 保证；以及监控——利用序贯警报和变点检测来识别校准、幻觉率、拒绝行为、公平性或其他任务相关属性的偏移。该视角将可信LLM部署视为统计过程控制问题，从而补充了近年来的综述工作。

</details>


### [263] [Learning Transfers: Kan Extensions for Neural Invariants](https://arxiv.org/abs/2606.07627)
*Luciano Melodia*

Main category: cs.LG

TL;DR: 本文提出一个范畴论框架来精确定义迁移学习中应被迁移的“结构不变性”，并引入基于左Kan扩展的迁移差异度量（Transfer Discrepancy），通过逗号范畴索引的有限余核公式计算链复形和持续模上的迁移差异，实验表明该指标能正确识别任务函子并检测出破坏迁移相关拓扑的表示坍塌。

<details>
  <summary>Details</summary>

Motivation: 现有迁移学习评估（如目标精度、分布差异）未能明确说明究竟哪种结构不变性被迁移。论文旨在范畴论框架下，精确定义和量化迁移学习中应传递的“不变性”。

Method: 利用范畴论工具：定义源任务范畴𝒜、目标任务范畴ℬ、任务变换函子J:𝒜→ℬ，源表示F:𝒜→𝒱的泛化迁移不变量定义为左Kan扩展Lan_J F。迁移差异Comp_J(F,G)通过比较左Kan扩展与目标不变量G的差异来度量。对链复形和持续模证明了有限余核公式，对持续值有限型单参数不变量，差异等价于条形码间的瓶颈距离。

Result: 1. 证明了链复形和持续模上左Kan扩展的有限余核公式，索引于逗号范畴J↓b。2. 对持久值有限型单参数不变量，迁移差异可由条形码瓶颈距离精确计算。3. 在神经隐层点云上的控制实验表明，该分数能正确恢复任务函子，并能检测出保持分类精度但破坏迁移相关拓扑的表示坍塌。

Conclusion: 范畴论框架为迁移学习提供了精确的结构不变性定义和度量，迁移差异能比传统精度指标更本质地评估表示质量的迁移特性，并能检测传统指标无法发现的拓扑破坏性坍塌。

Original Abstract: Transfer learning presumes that a representation learned on source tasks carries structure that remains usable on related target tasks. Standard evaluations probe this through target accuracy or distributional discrepancy, yet leave unspecified which structural invariant is meant to transfer. We supply that invariant categorically. A source task category $\mathcal A$, a target task category $\mathcal B$, and a task-change functor $J:\mathcal A\to\mathcal B$ determine, for every invariant-valued source representation $F:\mathcal A\to\mathcal V$, the universal transferred invariant $\operatorname{Lan}J F$. Given a target invariant $G:\mathcal B\to\mathcal V$, we define the transfer discrepancy $\operatorname{Comp}J(F,G)=\sup{b\in\operatorname{Ob}(\mathcal B)} d{\mathcal V}\bigl((\operatorname{Lan}_J F)(b),G(b)\bigr)$, evaluating transfer not by an objectwise comparison of source and target, but by comparing the target invariant against the one forced by the prescribed task transformation. We prove finite cokernel formulas for $(\operatorname{Lan}_J F)(b)$ in chain complexes and persistence modules, indexed by the comma category $J\downarrow b$. For persistence-valued finite-type one-parameter invariants, the discrepancy is computed exactly by bottleneck distances between barcodes. Controlled experiments on neural latent point clouds then test whether the score recovers the correct task functor and flags representation collapses that preserve classification accuracy while destroying transfer-relevant topology.

中文摘要翻译: 迁移学习假设在源任务上学到的表示所携带的结构在相关目标任务上仍然可用。标准评估通过目标精度或分布差异来检验这一假设，但未能说明究竟哪种结构不变性被传递。本文在范畴论框架下提供了这一不变性。一个源任务范畴𝒜、一个目标任务范畴ℬ以及一个任务变换函子J:𝒜→ℬ，对于每个取值于不变量的源表示F:𝒜→𝒱，决定了泛化迁移不变量（左Kan扩展）Lan_J F。给定一个目标不变量G:ℬ→𝒱，我们定义迁移差异Comp_J(F,G)=sup_{b∈Ob(ℬ)} d_𝒱((Lan_J F)(b), G(b))，该评估不是通过源与目标之间的逐对象比较，而是将目标不变量与由预设任务变换所强制得到的不变量进行比较。我们证明了链复形和持续模上(Lan_J F)(b)的有限余核公式，索引于逗号范畴J↓b。对于持续值有限型单参数不变量，差异可由条形码间的瓶颈距离精确计算。在神经隐层点云上的控制实验测试了该分数是否能正确恢复任务函子，并检测出保持分类精度但破坏迁移相关拓扑的表示坍塌。

</details>


### [264] [Large Language Models Should Learn Personalized Rather Than Aggregated Human Preferences](https://arxiv.org/abs/2606.07629)
*Cristina Garbacea*

Main category: cs.LG

TL;DR: 本文论证大型语言模型应从聚合偏好转向个性化偏好学习，指出聚合偏好掩盖了多样性、个体价值观和上下文依赖性，并提出了有界个性化框架作为解决方案。

<details>
  <summary>Details</summary>

Motivation: 当前LLM对齐方法将多样的人类偏好聚合成单一奖励信号，优化的是不具代表性的"平均用户"，忽略了个体差异和偏好多样性，这在理论上（社会选择理论）和实证上均存在根本性局限。

Method: 通过理论分析（社会选择理论）、实证证据（跨人口群体差异）、系统分析人类偏好编码的丰富结构，综述个性化技术方案，并针对可扩展性、共享标准、操纵风险等反驳意见进行系统性回应。

Result: 偏好聚合掩盖了关键信息；个性化存在过滤气泡、价值锁定和心理操纵等安全挑战，但可通过有界个性化框架在保持通用安全约束的同时容纳合理的个体差异。

Conclusion: LLM应学习个性化个体偏好而非聚合偏好，通过有界个性化框架平衡个体自主性与集体安全，并提出了具体的研究与政策议程。

Original Abstract: Current approaches to aligning large language models (LLMs) aggregate diverse human preferences into a single reward signal, effectively optimizing for a hypothetical ``average user'' who represents no real person particularly well. This position paper argues that LLMs should learn personalized, individual preferences rather than aggregated ones. We show that aggregation masks critical information about preference diversity, individual values, and contextual dependencies, which is a limitation both theoretically grounded in social choice theory and empirically evident across demographic groups. We analyze the rich structure that human preferences encode, survey technical approaches to personalization, and systematically address counterarguments on scalability, shared standards, and manipulation risk. While personalization introduces genuine safety challenges including filter bubbles, value lock-in, and psychological manipulation, we argue these are manageable through bounded personalization frameworks that preserve universal safety constraints while accommodating legitimate individual variation. We conclude with a concrete research and policy agenda for developing preference-aware models that respect both individual autonomy and collective safety.

中文摘要翻译: 当前大型语言模型（LLM）对齐方法将多样的人类偏好聚合成单一的奖励信号，实质上是在优化一个不代表任何真实个体的假设性"平均用户"。这篇立场论文认为，LLM应当学习个性化的个体偏好，而非聚合偏好。我们表明，聚合掩盖了关于偏好多样性、个体价值观和上下文依赖性的关键信息——这一局限性既有社会选择理论的理论支撑，也在跨人口群体的实证中得以体现。我们分析了人类偏好编码的丰富结构，综述了个性化的技术方法，并系统地回应了关于可扩展性、共享标准和操纵风险等反驳意见。虽然个性化引入了包括信息过滤气泡、价值锁定和心理操纵在内的真实安全挑战，但我们认为，这些问题可以通过有界个性化框架加以管理，该框架在保持通用安全约束的同时容纳合理的个体差异。我们最后提出了一个具体的研究与政策议程，以开发既尊重个体自主性又保障集体安全的偏好感知模型。

</details>


### [265] [Active Learning with Foundation Model Priors: Efficient Learning under Class Imbalance](https://arxiv.org/abs/2606.07630)
*Jiancheng Zhang,Meiqing Li,Qi Zhang,Yinglun Zhu*

Main category: cs.LG

TL;DR: 提出了一种利用基础模型先验的主动学习框架，在存在类别不平衡和标签噪声的情况下，跨图像和文本领域实现高效样本选择，节省超过50%的标注成本。

<details>
  <summary>Details</summary>

Motivation: 现实数据集常同时存在类别分布不平衡和标注噪声问题，这严重损害模型性能（尤其对少数类）。现有主动学习方法未能系统应对标签噪声与类别不平衡的双重挑战。

Method: 提出一种创新主动学习框架，利用基础模型（foundation model）先验，实现基础模型与小模型之间的不平衡感知联合决策，从而在图像和文本领域中同时处理噪声标签和类别不平衡问题。

Result: 在不平衡数据集上的大量实验表明，该方法相比最优主动学习基线节省超过50%的标注成本，同时保持模型性能和对标签噪声的鲁棒性。

Conclusion: 所提出的主动学习框架能有效应对标签噪声和类别不平衡的双重挑战，显著降低标注成本，并具有良好的跨领域泛化能力。

Original Abstract: Real-world datasets across image and text domains are often characterized by skewed class distributions and noisy annotations, which jointly degrade model performance, particularly on minority classes. Among existing solutions, active learning offers an effective and efficient paradigm by selectively querying the most informative and balanced samples for annotation. We propose an innovative active learning framework that mitigates class imbalance and selects the most informative samples to annotate. Leveraging foundation model priors, our algorithm enables imbalance-aware co-decisions between foundation model and small model to tackle noisy and imbalanced labels across various domains. We introduce the first study to systematically explore active learning under the dual challenges of label noise and class imbalance across image and text domains. Extensive experiments on imbalanced datasets demonstrate that our method achieves substantial annotation savings-over 50% compared to the best active learning baseline-while preserving performance and robustness to label noise.

中文摘要翻译: 在图像和文本领域的真实数据集中，常存在类别分布不平衡和标注噪声的问题，这共同导致模型性能下降，尤其对少数类影响严重。在现有解决方案中，主动学习通过选择性地查询信息量最大且类别平衡的样本进行标注，提供了一种有效且高效的范式。我们提出了一种创新的主动学习框架，能够缓解类别不平衡并选择信息量最大的样本进行标注。利用基础模型先验，我们的算法使基础模型与小模型之间能够进行不平衡感知的联合决策，从而处理不同领域中的噪声和不平衡标签。我们首次系统性地研究了在标签噪声和类别不平衡双重挑战下，跨图像和文本领域的主动学习问题。在不平衡数据集上的大量实验表明，与最优主动学习基线相比，我们的方法在保持性能和对标签噪声鲁棒性的同时，节省了超过50%的标注成本。

</details>


### [266] [Trait-space Monitoring for Emergent Misalignment During Supervised Finetuning](https://arxiv.org/abs/2606.07631)
*Huy Nghiem,Sy-Tuyen Ho,Sarah Wiegreffe,Hal Daumé*

Main category: cs.LG

TL;DR: 本文提出通过内部表征漂移检测LoRA微调期间出现的紧急错对齐（EM），发现EM相关漂移集中在可解释的低维轴上，并构建了低开销监测器，在保持低误报率的同时高效检测危险检查点。

<details>
  <summary>Details</summary>

Motivation: 标准训练信号可能遗漏紧急错对齐，依赖重复行为评估的检测成本高昂，因此需要一种能从模型内部表征中实时、低成本地检测EM的方法。

Method: 在激活空间中编码七个对齐相关特质作为线性方向，跟踪微调过程中检查点的表征漂移；发现EM漂移集中在低维轴上（解释65.5%方差），基于此漂移轮廓构建低开销监测器。

Result: 监测器在保留扰动类型上达到2.2%假阴性率、2.9%假阳性率和0.990 AUROC，优于无监督PCA和SAE基线；在14B模型、更长微调运行和错乱起点等压力测试中识别了关键部署边界。

Conclusion: 特质空间监测可作为LoRA微调中行为评估的实用补充，但跨不同部署场景可能需要重新校准。

Original Abstract: Emergent misalignment (EM) occurs when narrow finetuning causes a model to behave dangerously outside the finetuning task. Standard training signals can miss this shift, making reliable detection costly if it depends on repeated behavioral evaluation. We ask whether emergent misalignment can instead be detected from internal representations during finetuning. Using seven alignment-relevant traits encoded as linear directions in activation space, we track representational drift across training checkpoints in four open-source 7-9B LLMs. EM-relevant drift concentrates on a low-dimensional axis that explains 65.5% of the variance, revealing a geometric signature in the studied regime. A low-overhead monitor built on this drift profile detects dangerous checkpoints with 2.2% false negative rate, 2.9% false positive rate, and 0.990 AUROC on held-out perturbation types, outperforming unsupervised PCA and SAE baselines. Stress tests on two 14B models, longer finetuning runs, and misaligned starting points identify key deployment boundaries. These results position trait-space monitoring as a practical complement to behavioral evaluation for EM detection during LoRA-based finetuning, while showing that deployment across substantially different regimes may require recalibration.

中文摘要翻译: 紧急错对齐（EM）发生在窄域微调导致模型在微调任务之外出现危险行为时。标准训练信号可能遗漏这种偏移，若依赖重复行为评估，则可靠检测的成本高昂。我们探究能否在微调期间从内部表征中检测紧急错对齐。利用激活空间中编码为线性方向的七个对齐相关特质，我们跟踪四个开源7-9B大语言模型在训练检查点上的表征漂移。EM相关漂移集中在解释65.5%方差的低维轴上，揭示了所研究范式下的几何特征。基于该漂移轮廓构建的低开销监测器在保留扰动类型上达到2.2%假阴性率、2.9%假阳性率和0.990 AUROC，优于无监督PCA和SAE基线。在14B模型、更长微调运行及错乱起点上的压力测试识别了关键部署边界。这些结果表明，特质空间监测可作为LoRA微调中行为评估的实用补充，同时表明跨不同范式部署可能需要重新校准。

</details>


### [267] [Evaluation of ML Resource Utilization Requires Model Life Cycle Assessment](https://arxiv.org/abs/2606.07632)
*Jared Fernandez,Clara Na,Yonatan Bisk,Constantine Samaras,Emma Strubell*

Main category: cs.LG

TL;DR: 本文主张将生命周期评估（LCA）方法应用于AI系统，以全面核算从硬件制造到训练、推理的完整生命周期成本和环境影响，弥补传统方法仅关注单次训练或推理的不足。

<details>
  <summary>Details</summary>

Motivation: 传统评估AI效率的方法仅关注单次训练或单次推理的成本，已不足以反映日益复杂的AI系统开发和部署管线的真实资源消耗和环境影响，因此需要更全面的核算框架。

Method: 提出将生命周期评估（Life Cycle Assessment, LCA）应用于机器学习模型开发和部署的全流程，涵盖从物理计算硬件的具体化成本到训练和推理的操作成本。

Result: 本文为立场论文，未报告具体实验结果，而是系统地论证了LCA方法在AI系统评估中的必要性和应用框架。

Conclusion: 生命周期评估能够全面核算AI系统及其基础设施从制造到部署的全生命周期成本和下游影响，为研究人员、开发者、政策制定者和用户评估规模化AI系统的障碍提供更有效的工具。

Original Abstract: Proper accounting of the energy requirements and environmental impact of artificial intelligence (AI) systems is necessary for researchers, developers, policy makers, and users to assess the barriers to building systems at scale. With the growing complexity of pipelines and underlying infrastructure needed to develop and deploy AI systems, previous approaches for evaluating AI efficiency which focus on the costs of a single training run or an individual inference prediction are no longer sufficient. In this position paper, we enunciate the need for applying life cycle assessment to evaluate the costs of the machine learning model development and deployment pipeline to properly account for the required resources and downstream impact. Life cycle assessments enable the incorporation of costs across the full life cycle of an AI system and its underlying infrastructure, from the embodied costs associated with the physical computing hardware through the operational costs in training and inference.

中文摘要翻译: 准确核算人工智能系统的能源需求和环境影响，对于研究人员、开发者、政策制定者和用户评估大规模构建系统的障碍至关重要。随着开发和部署AI系统所需的管线及底层基础设施日趋复杂，以往仅关注单次训练或单次推理成本的AI效率评估方法已不再充分。在本立场论文中，我们阐明了应用生命周期评估来评价机器学习模型开发和部署管线成本的必要性，以正确核算所需资源及下游影响。生命周期评估能够纳入AI系统及其底层基础设施全生命周期的成本，从物理计算硬件的具体化成本，到训练和推理中的操作成本。

</details>


### [268] [KITE: A Tri-Modal Transformer Integrating Text, Images, and Knowledge Graphs for Fake News Detection](https://arxiv.org/abs/2606.07651)
*Kevin Patel,Shashi Bhushan Jha*

Main category: cs.LG

TL;DR: KITE是一个三模态假新闻检测框架，联合建模文本、视觉和外部知识，通过跨模态注意力融合三者特征，显著优于单模态和双模态基线，尤其擅长检测图像-文本不匹配或与常识矛盾的虚假内容。

<details>
  <summary>Details</summary>

Motivation: 现有假新闻检测方法大多仅关注文本-图像融合，或仅在后处理阶段引入外部知识，难以捕捉更深层的语义不一致性。随着多模态虚假信息日益复杂，需要一种能同时建模文本、视觉和外部事实知识三模态信息的统一框架。

Method: KITE框架使用Roberta和CLIP分别进行文本和视觉编码，利用图注意力网络处理从Wikidata检索的结构化事实知识，通过跨模态注意力机制在多模态Transformer中融合文本、视觉和知识三种模态特征，并输出各模态的置信度分数以提供可解释性。

Result: 在基准数据集上的评估表明，KITE显著优于单模态和双模态基线方法，尤其在图像-文本不匹配或与外部知识矛盾的场景下表现突出。

Conclusion: 引入外部事实知识作为第三模态，并与文本、视觉特征通过跨模态注意力联合建模，能够有效提升假新闻检测的性能和可解释性，特别是在处理深层语义不一致时。

Original Abstract: Traditional fake news detection methods are falling behind as multimodal misinformation grows more advanced, seamlessly blending deceptive text, manipulated visuals, and factually incorrect claims. Most prior work focuses on text-image fusion or applies external knowledge only as a post-processing step, limiting their ability to detect deeper semantic inconsistencies. In this paper, we introduce KITE (Knowledge-Integrated Text-Image Encoder), a tri-modal fake news detection framework that jointly models textual, visual, and factual knowledge representations. KITE leverages Roberta [23,14] and CLIP [24] for linguistic and visual encoding, while a Graph Attention Network (GAT) processes structured facts retrieved from Wikidata. KITE uses cross-modal attention [9] within a multimodal transformer to integrate text, visual, and knowledge features, helping it understand how each modality relates to one another. Modality-specific confidence scores are generated alongside the final prediction, offering interpretability by indicating which input type most influenced the decision. Evaluations on benchmark datasets demonstrate that KITE significantly outperforms unimodal and bimodal baselines, particularly in scenarios involving image-text mismatches or contradictions with external knowledge.

中文摘要翻译: 传统的假新闻检测方法正逐渐落后于日益复杂的多模态虚假信息——这些信息巧妙地将欺骗性文本、经过操控的视觉内容以及事实错误的说法融为一体。以往的大多数工作聚焦于文本-图像融合，或仅将外部知识作为后处理步骤引入，限制了其检测更深层语义不一致的能力。在本文中，我们提出了KITE（知识集成文本-图像编码器），一种三模态假新闻检测框架，该框架联合建模文本、视觉和事实知识表示。KITE利用Roberta和CLIP进行语言和视觉编码，同时采用图注意力网络处理从Wikidata检索的结构化事实。KITE在多模态Transformer中使用跨模态注意力机制来集成文本、视觉和知识特征，帮助理解各模态之间的相互关联。框架在生成最终预测的同时输出各模态特定的置信度分数，通过指示哪种输入类型对决策影响最大来提供可解释性。在基准数据集上的评估表明，KITE显著优于单模态和双模态基线方法，尤其在涉及图像-文本不匹配或与外部知识相矛盾的场景中表现突出。

</details>


### [269] [DOG-DPO:Dynamic Optimization in Geometry for Safety Alignment](https://arxiv.org/abs/2606.07678)
*Yi Nian,Tiankai Yang,Yudi Zhang,Qi Pan,Zelong Xu,Shenzhe Zhu,Qingqing Luan,Yue Huang,Xiangliang Zhang,Yue Zhao*

Main category: cs.LG

TL;DR: 提出DOG-DPO，一种无需训练的偏好数据选择框架，通过将偏好对表示为模型表示空间中的方向，并分解多数据集的偏好几何结构，仅用11%的偏好数据即可恢复全量训练的安全增益。

<details>
  <summary>Details</summary>

Motivation: 现有偏好数据选择方法将每个偏好对独立评分，把有向偏好信息压缩为标量质量或多样性分数，这种样本中心视角在多数据集场景下尤为局限，因为共享安全方向与数据集特定残余风险并存。

Method: 首先将每个偏好对表示为模型表示空间中的方向；然后将多数据集偏好几何结构分解为全局锚点子空间和数据集特定残余子空间；最后通过最大化多样性覆盖度来选择子集，鼓励在DPO训练前实现对对齐方向的广泛、非冗余覆盖。

Result: 在六个安全基准和两种模型骨干上，仅用11%的偏好对便实现了强健的效用-鲁棒性权衡，恢复了全量数据训练的大部分安全增益，且完全无需教师模型、无需训练，速度远超代表性选择基线方法。

Conclusion: DOG-DPO通过将偏好对视为结构化几何信号，有效解决了偏好数据选择中的冗余问题，实现了高效、轻量且性能优异的安全对齐。

Original Abstract: Safety alignment for large language models relies on preference data, but current pipelines often train on large, redundant datasets. Existing data selection methods typically score each preference pair independently, collapsing directional preference information into scalar quality or diversity scores. This sample-centric view is especially limiting in multi-dataset settings, where shared safety directions coexist with dataset-specific residual risks. We propose DOG-DPO, a training-free data selection framework that treats preference pairs as structured geometric signals. DOG-DPO first represents each preference pair as a direction in model representation space. It then decomposes multi-dataset preference geometry into a global anchor subspace and dataset-specific residual subspaces. Finally, it selects subsets by maximizing diversity-based coverage, encouraging broad, non-redundant coverage of alignment directions before DPO training. Across six safety benchmarks and two model backbones, DOG-DPO achieves a strong utility-robustness trade-off using only 11% of the preference pairs. It recovers most of the safety gains of full-data training while remaining entirely teacher-free, training-free, and substantially faster than representative selection baselines.

中文摘要翻译: 大型语言模型的安全对齐依赖于偏好数据，但当前的流水线通常在大规模冗余数据集上训练。现有的数据选择方法通常独立地对每个偏好对进行评分，将有向偏好信息压缩为标量质量或多样性分数。这种以样本为中心的视角在多数据集场景下尤其受限，因为共享的安全方向与数据集特定的残余风险并存。我们提出DOG-DPO，一种无需训练的数据选择框架，将偏好对视为结构化的几何信号。DOG-DPO首先将每个偏好对表示为模型表示空间中的一个方向；然后将多数据集偏好几何结构分解为全局锚点子空间和数据集特定的残余子空间；最后通过最大化基于多样性的覆盖度来选择子集，鼓励在DPO训练前实现对对齐方向的广泛、非冗余覆盖。在六个安全基准和两种模型骨干上，DOG-DPO仅使用11%的偏好对就实现了强健的效用-鲁棒性权衡。它恢复了全量数据训练的大部分安全增益，同时完全无需教师模型、无需训练，且速度远超代表性选择基线方法。

</details>


### [270] [Semantic Cache Distillation: Efficient State Transfer via Reuse and Selective Patching](https://arxiv.org/abs/2606.07684)
*Qianli Ma,Zhiqing Tang,Hanshuai Cui,Zhi Yao,Weijia Jia*

Main category: cs.LG

TL;DR: 提出语义缓存蒸馏（SCD）框架，通过紧凑语义码替代原始KV缓存传输，解决分离式LLM推理中的通信瓶颈和跨模型缓存重用导致的语义错位问题，在带宽受限场景下实现高达2.65倍的TTFT加速。

<details>
  <summary>Details</summary>

Motivation: 分离式LLM推理中，KV缓存的高维传输造成严重通信瓶颈，主导了首个令牌生成时间（TTFT）；同时，不同模型间（如基础模型与微调变体）的缓存重用会导致逐层累积的语义错位，降低生成质量。

Method: 提出语义缓存蒸馏（SCD）框架，包含两个核心机制：(1) Reuse——从低秩子空间重构大部分层以最小化传输开销；(2) Patch——在稀疏过渡层预测归一化输入以截断误差传播。

Result: 在带宽受限场景下，SCD相比oracle consumer prefill实现了高达2.65倍的TTFT加速，在质量-延迟帕累托前沿上优于量化和选择性重计算基线，同时生成质量与oracle相比F1差距保持在5%以内。

Conclusion: SCD通过紧凑语义码和低秩重构有效缓解了分离式推理中的通信瓶颈与语义错位问题，在显著加速TTFT的同时保持了生成质量。

Original Abstract: Disaggregated serving alleviates memory bottlenecks in Large Language Model (LLM) inference but creates a severe communication bottleneck: transmitting high-dimensional Key-Value (KV) caches often dominates time-to-first-token (TTFT). Moreover, reusing caches across heterogeneous models (e.g., base and fine-tuned variants) causes semantic misalignment that accumulates over layers, degrading generation quality. We propose Semantic Cache Distillation (SCD), a loss-constrained framework that replaces raw KV transmission with compact semantic codes. SCD addresses these challenges via two mechanisms: (1) Reuse, which reconstructs most layers from low-rank subspaces to minimize transfer cost, and (2) Patch, which predicts normalized inputs at sparse transition layers to truncate error propagation. Empirically, SCD delivers up to 2.65 $\times$ TTFT speedup over the oracle consumer prefill and dominates quantization and selective recomputation baselines on the quality--latency Pareto frontier in bandwidth-constrained regimes, while keeping generation quality within 5\% F1 of the oracle.

中文摘要翻译: 分离式服务缓解了大语言模型（LLM）推理中的内存瓶颈，但却带来了严重的通信瓶颈：传输高维的键值（KV）缓存通常主导了首个令牌生成时间（TTFT）。此外，在不同模型间（例如基础模型和微调变体）重用缓存会导致语义错位，这种错位会逐层累积，从而降低生成质量。我们提出语义缓存蒸馏（SCD），一种带损失约束的框架，用紧凑的语义码替代原始KV传输。SCD通过两种机制解决上述挑战：(1) 复用（Reuse），从低秩子空间重构大部分层以最小化传输开销；(2) 修补（Patch），在稀疏的过渡层预测归一化输入以截断误差传播。实验表明，在带宽受限的场景下，SCD相比oracle consumer prefill实现了高达2.65倍的TTFT加速，并在质量-延迟帕累托前沿上优于量化和选择性重计算基线，同时生成质量与oracle相比F1差距保持在5%以内。

</details>


### [271] [Test-Time Adaptive Composition for Machine Learning as a Service (MLaaS) in IoT Environments](https://arxiv.org/abs/2606.07685)
*Deepak Kanneganti,Sajib Mistry,Sheik Mohammad Mostakim Fattah,Aneesh Krishna*

Main category: cs.LG

TL;DR: 提出了一种面向IoT环境中MLaaS组合的测试时自适应（TTA）框架，通过服务级自适应调整替代传统的服务替换或重组合方法，在降低计算时间的同时保持组合性能。

<details>
  <summary>Details</summary>

Motivation: IoT环境的动态性影响MLaaS组合的长期有效性，现有基于服务替换或重组合的自适应方法难以快速找到合适的替代服务且耗时严重。

Method: 首先提出TTA感知的可组合性模型判断自适应服务与现有组合的兼容性，然后设计服务级自适应模型在推理阶段调整单个服务同时保持组合性能。

Result: 实验结果表明，所提框架在降低计算时间方面比传统自适应方法更有效。

Conclusion: 提出的TTA组合框架能够有效应对IoT环境的动态性，在保持MLaaS组合性能的同时显著减少计算开销。

Original Abstract: The dynamic nature of Internet of Things (IoT) environments affects the long-term effectiveness of Machine Learning as a Service (MLaaS) compositions. Existing adaptive composition methods are mainly based on service replacement or re-composition, where identifying suitable substitutes is difficult and time-consuming. To address this, we propose a novel Test-Time Adaptive (TTA) composition framework for MLaaS in IoT environments. First, we introduce a TTA-aware composability model to determine whether adapted services remain compatible with the existing composition. Next, we design a service-level adaptation model to adjust individual services during inference while preserving composition performance. Experimental results demonstrate that the proposed framework reduces computational time more effectively than traditional adaptive approaches.

中文摘要翻译: 物联网（IoT）环境的动态特性会影响机器学习即服务（MLaaS）组合的长期有效性。现有的自适应组合方法主要基于服务替换或重组合，但识别合适的替代服务既困难又耗时。为解决这一问题，我们提出了一种面向IoT环境中MLaaS的测试时自适应（TTA）组合框架。首先，我们引入了一个TTA感知的可组合性模型，用于判断自适应服务是否仍与现有组合兼容。接着，我们设计了一个服务级自适应模型，以便在推理阶段调整单个服务，同时保持组合性能。实验结果表明，所提出的框架在降低计算时间方面比传统的自适应方法更有效。

</details>


### [272] [Knowledge-Inclusive Adaptive Physics-Informed Neural Network for Microbial Interaction Modelling](https://arxiv.org/abs/2606.07686)
*Ravisha Rupasinghe,Rajith Vidanaarachchi,Asela Hevapathige,Sachith Seneviratne,Sen-Lin Tang,Saman Halgamuge*

Main category: cs.LG

TL;DR: 提出了一种知识增强的物理信息神经网络（PINN）框架，通过整合文本（同行评审文献）和网络结构（微生物互作）等辅助知识源，改进了广义Lotka-Volterra（gLV）模型的参数发现，显著提升了微生物群落建模的准确性。

<details>
  <summary>Details</summary>

Motivation: 现有的PINN方法仅依赖实验测量数据来发现方程参数，而忽略了文本、网络结构等其他形式的知识。作者希望利用这些辅助知识源来丰富参数发现过程，从而更准确地建模微生物群落。

Method: 提出一个知识包容的PINN框架：（1）利用同行评审的宏基因组学文献（文本知识）提供gLV模型无法捕捉的外部影响背景；（2）显式建模微生物互作（网络结构知识）；（3）通过数据驱动集成方法将知识知识与实验测量的微生物丰度数据结合，共同推断gLV参数。

Result: 在真实和模拟数据集（涵盖人类和植物相关微生物群落）上，该框架相比现有最优方法提升高达53%；即使不加入外部知识也有提升。加入知识后，基于Bray-Curtis相异度的准确率提升高达23%，R²提升高达47%。

Conclusion: 整合多种形式的辅助知识（文本与网络结构）能够显著提升PINN框架的参数发现能力和微生物群落建模性能，并揭示有价值的生态学洞见。

Original Abstract: Physics-Informed Neural Network (PINN) is a way of including knowledge in the form of equations in Machine Learning methods. Beyond equations, knowledge exists in other forms, such as text and network structure. While existing PINN-based approaches discover equation parameters from data, they rely solely on experimental measurements. We propose a new PINN framework that enriches parameter discovery by incorporating auxiliary knowledge sources. We instantiate our framework for microbiology, where generalised Lotka-Volterra (gLV) serves as a biological foundation for modelling microbial communities. We demonstrate that incorporating knowledge improves microbial community modelling. Our framework enriches the gLV parameters using peer-reviewed metagenomics literature, as text provides biological context on external influences that gLV alone cannot capture. We combine this knowledge with experimental measurements of microbial abundance using a data-driven integration approach. We integrate network-based structural knowledge by explicitly modelling microbial interactions. Our knowledge-inclusive framework infers microbial networks, revealing ecological insights. We validate these findings against ecological roles documented in the literature. We evaluate on real and simulated datasets spanning human- and plant-associated microbial communities. Our framework improves over the state-of-the-art by up to 53%, even without knowledge. Knowledge addition yields gains of up to 23% in Bray-Curtis Dissimilarity-based accuracy and 47% in $\mathrm{R}^2$.

中文摘要翻译: 物理信息神经网络（PINN）是一种将方程形式的知识纳入机器学习方法的手段。除了方程之外，知识还以其他形式存在，如文本和网络结构。虽然现有的基于PINN的方法从数据中发现方程参数，但它们仅依赖实验测量。我们提出了一种新的PINN框架，通过纳入辅助知识源来丰富参数发现。我们针对微生物学实例化了该框架，其中广义Lotka-Volterra（gLV）作为建模微生物群落的生物学基础。我们证明，纳入知识可以改善微生物群落建模。我们的框架利用经过同行评审的宏基因组学文献来丰富gLV参数，因为文本提供了gLV单独无法捕获的外部影响的生物学背景。我们使用数据驱动的集成方法将这些知识与微生物丰度的实验测量相结合。通过显式建模微生物互作，我们整合了基于网络的结构性知识。我们的知识包容框架推断出微生物网络，揭示了生态学洞见。我们根据文献中记载的生态角色验证了这些发现。我们在涵盖人类和植物相关微生物群落的真实和模拟数据集上进行了评估。即使没有知识，我们的框架也比现有最优方法提升了高达53%。加入知识后，基于Bray-Curtis相异度的准确率提升了高达23%，R²提升了高达47%。

</details>


### [273] [HARP: Efficient Data Selection for Finetuning Large Language Models](https://arxiv.org/abs/2606.07690)
*Ning Wang,Zhengxin Zhang,Maosen Tang,Yitang Gao,Claire Cardie,Sainyam Galhotra*

Main category: cs.LG

TL;DR: HARP是一种高效的基于训练的微调数据选择方法，通过层次化活动区域剪枝和贝叶斯后验推断，在保持下游任务对齐的同时，显著降低选择成本，性能提升最高达+8.9个点，训练样本减少约7倍。

<details>
  <summary>Details</summary>

Motivation: 现有微调数据选择方法面临两难：无训练选择器可扩展但依赖代理指标（如嵌入相似度、聚类），无法准确匹配下游目标；基于训练的选择器虽能更好反映下游效用（通过梯度信号、子集评估或Shapley归因），但需要大量昂贵的训练-评估迭代。目标是在保持下游对齐的同时降低选择成本。

Method: 提出层次化活动区域剪枝（HARP）方法。首先将训练池组织成节点-叶子层次结构，仅评估代表性叶子节点，利用经验贝叶斯后验推断未测量节点的效用。然后通过两种互补的包络线选择数据：HARP-C（保守控制冗余）和HARP-E（加性奖励互补区域）。

Result: 在局部平滑和有界估计误差的理论假设下，HARP能够控制选择误差同时降低训练-评估成本。实验验证HARP变体达到最佳结果，比最强基线高出最多+8.9个点，同时使用的训练样本减少约7倍。

Conclusion: HARP通过层次化剪枝和贝叶斯推断，有效平衡了数据选择中的下游对齐与计算效率，实现了显著的性能提升和成本降低。

Original Abstract: Finetuning data selection requires balancing two competing goals: selecting examples that improve the downstream objective, and doing so without repeatedly finetuning models. Train-free selectors are scalable but rely on proxies such as embedding similarity or clustering, which may not match the target objective. Train-based selectors better reflect downstream utility through gradient signals, subset evaluation, or Shapley attribution, but require many costly train--evaluate iterations. We propose Hierarchical Active Region Pruning (HARP), an efficient train-based selector that preserves downstream alignment while reducing selection cost. HARP organizes the training pool into a node--leaf hierarchy, evaluates only representative leaves, and infers unmeasured utilities with empirical Bayes posteriors. It then selects data using two complementary envelopes: HARP-C, which conservatively controls redundancy, and HARP-E, which additively rewards complementary regions. We theoretically show that, under local smoothness and bounded estimation error, HARP controls selection error while reducing train--evaluate cost. We further validate that HARP variants achieve the best result and outperform the strongest baseline by up to $+8.9$ points, while using roughly $7\times$ fewer training examples.

中文摘要翻译: 微调数据选择需要平衡两个相互竞争的目标：选择能够改善下游目标的样本，同时避免反复微调模型。无训练选择器具有可扩展性，但依赖嵌入相似度或聚类等代理指标，这些指标可能无法匹配目标目标。基于训练的选择器通过梯度信号、子集评估或Shapley归因更好地反映下游效用，但需要大量昂贵的训练-评估迭代。我们提出层次化活动区域剪枝（HARP），一种高效的基于训练的选择器，在保持下游对齐的同时降低选择成本。HARP将训练池组织成节点-叶子层次结构，仅评估代表性叶子节点，并利用经验贝叶斯后验推断未测量效用。然后使用两种互补包络线选择数据：HARP-C保守控制冗余，HARP-E加性奖励互补区域。我们理论上证明，在局部平滑和有界估计误差条件下，HARP在降低训练-评估成本的同时控制选择误差。我们进一步验证HARP变体达到最佳结果，比最强基线高出最多+8.9个点，同时使用的训练样本减少约7倍。

</details>


### [274] [BCG-FM: A Foundation Model for Ambient Cardiac Health Sensing](https://arxiv.org/abs/2606.07692)
*Magnus Ruud Kjaer,Haejun Han,Ashish Neupane,David Q. Sun*

Main category: cs.LG

TL;DR: 本文提出BCG-FM，首个面向环境机械生物信号的基础模型，利用床垫内置压电传感器无感采集夜间心跳冲击图（BCG）信号，在145,985名个体共275万小时数据上预训练。模型在生物年龄估计上达到3.26年MAE（环境无接触模态最佳），在15种自报健康状况及三个外部队列中实现临床相关判别。仅需500名标注样本即可超越全监督基线。

<details>
  <summary>Details</summary>

Motivation: 现有可穿戴生物信号基础模型均依赖用户主动行为（佩戴设备或前往睡眠实验室），而环境式无接触感知的健康监测尚未被基础模型范式探索。

Method: 采用床面嵌入式压电传感器采集夜间BCG信号，使用参与者级别的对比学习（participant-level contrastive learning）进行预训练，预训练数据包含145,985名个体的275万小时夜间记录，是目前最大的原始波形生物信号预训练语料。

Result: 冻结的BCG-FM嵌入在生物年龄估计上达到3.26年MAE（环境无接触模态最低）；在15种自报健康条件和三个独立外部队列上实现临床相关判别；仅500名标注样本的预训练表示即可超越3,372名样本的全监督基线；表示质量随对比批次大小呈对数线性提升。

Conclusion: 环境式、纵向的机械生物信号是健康基础模型的可行模态，为无感健康监测提供了新范式。

Original Abstract: Foundation models for wearable biosignals have matched or exceeded supervised specialists across a range of clinical tasks, yet all rely on modalities that require deliberate user action--wearing a device or visiting a sleep lab. We introduce BCG-FM, the first foundation model for ambient mechanical biosignals. A piezoelectric sensor embedded in the bed surface records ballistocardiography (BCG) each night without user effort; we pretrain BCG-FM with participant-level contrastive learning and using a total of 2.75 million hours of nightly recordings from 145,985 individuals, the largest raw-waveform biosignal pretraining corpus to date. Frozen BCG-FM embeddings achieve 3.26-year MAE on biological-age estimation (the lowest reported for any ambient, contactless modality) and yield clinically relevant discrimination across 15 self-reported health conditions and three independent external cohorts. Pretrained representations from only 500 labeled participants outperform a fully supervised baseline trained on 3,372, and representation quality scales log-linearly with contrastive batch size. These results establish ambient, longitudinal mechanical biosignals as a viable modality for health foundation models.

中文摘要翻译: 用于可穿戴生物信号的基础模型已在多项临床任务中达到或超越监督专家水平，但这些模型均依赖需要用户主动行为的模态——佩戴设备或前往睡眠实验室。我们提出BCG-FM，首个面向环境机械生物信号的基础模型。嵌入床面的压电传感器可在用户无感的情况下记录每夜的心冲击图（BCG）信号；我们采用参与者级对比学习，利用来自145,985名个体共计275万小时的夜间记录对BCG-FM进行预训练，这是迄今为止最大的原始波形生物信号预训练语料。冻结的BCG-FM嵌入在生物年龄估计上达到3.26年MAE（所有环境无接触模态中最低），并在15种自报健康条件及三个独立外部队列中实现临床相关判别。仅来自500名标注参与者的预训练表示即可超越基于3,372名样本训练的完全监督基线，且表示质量随对比批次大小呈对数线性提升。这些结果表明，环境式、纵向的机械生物信号是健康基础模型的可行模态。

</details>


### [275] [Vessel Traffic Flow Prediction on Sparse Data via Spatio-Temporal Graph Neural Networks with a Learnable Tweedie Head](https://arxiv.org/abs/2606.07694)
*Kyeongjun Lee,Heeyoung Kim*

Main category: cs.LG

TL;DR: 针对海事交通流数据高稀疏性和突发性导致的预测难题，提出一种即插即用的可学习Tweedie输出头，可附加于任意时空图神经网络后端，通过优化闭式Tweedie单位偏差来替代传统似然方法，在真实AIS数据上显著提升了非零事件的预测精度。

<details>
  <summary>Details</summary>

Motivation: 现有时空图神经网络在处理高稀疏、突发性强的海事交通流数据时，会退化为保守的接近零的预测，无法捕捉非零活动；而零膨胀负二项模型虽然部分解决了零膨胀问题，但其两部分公式在 abrupt transitions 附近仍然保守。亟需一种能有效处理稀疏与突发特性的预测方法。

Method: 提出一种模型无关的可学习Tweedie头部模块，作为即插即用的输出层附加到任意ST-GNN后端。该方法不采用传统的基于似然的Tweedie训练（需要替代目标函数），而是直接优化闭式Tweedie单位偏差，预测均值进行点预测，同时学习节点级方差幂参数以捕获不同港口区域的异质性变异性。

Result: 在洛杉矶港和长滩港的真实AIS数据构建的海事交通图上实验，所提头部模块在多个ST-GNN骨干网络上持续提升RMSE指标，尤其在非零事件上提升显著，为实际海事交通控制提供了更可靠的预测。

Conclusion: 提出的可学习Tweedie头部是一种有效且通用的方法，能够显著改善现有ST-GNN在高稀疏海事交通流预测中的性能，特别是在捕捉非零突发活动方面，具有实际应用价值。

Original Abstract: Accurate vessel traffic flow prediction is crucial for smart port operations and navigational safety. However, maritime traffic flow data are often highly sparse with intermittent bursts, making robust forecasting challenging. Under such conditions, conventional spatio-temporal graph neural networks (ST-GNNs) can degrade toward conservative near-zero predictions and fail to capture non-zero activity. Although zero-inflated negative binomial (ZINB) models partially address excess zeros, their two-part formulation can still remain conservative around abrupt transitions. To address these issues, we propose a model-agnostic learnable Tweedie head that can be attached as a plug-and-play output module to arbitrary ST-GNN backbones. Instead of likelihood-based Tweedie training, which typically requires surrogate objectives, our approach optimizes the closed-form Tweedie unit deviance and predicts the mean for point forecasting while learning a node-level variance power to capture heterogeneous variability across port areas. Experiments on a maritime traffic graph constructed from real-world AIS data in the Port of Los Angeles and Long Beach show that the proposed head consistently improves RMSE across multiple ST-GNN backbones, especially on non-zero events, leading to more reliable forecasts for practical maritime traffic control.

中文摘要翻译: 准确的海上交通流预测对于智能港口运营和航行安全至关重要。然而，海上交通流数据通常具有高度稀疏性和间歇性突发特征，使得稳健预测极具挑战性。在这种条件下，传统的时空图神经网络会退化为保守的接近零的预测，无法捕捉非零活动。尽管零膨胀负二项模型部分解决了过度零值问题，但其两部分公式在突变过渡区域仍然可能保守。为应对这些问题，我们提出一种模型无关的可学习Tweedie头部，可作为即插即用的输出模块附加到任意时空图神经网络后端。与通常需要替代目标函数的基于似然的Tweedie训练不同，我们的方法优化闭式Tweedie单位偏差，并预测均值进行点预测，同时学习节点级方差幂参数以捕获港口区域间的异质性变异性。在利用洛杉矶港和长滩港真实AIS数据构建的海事交通图上进行的实验表明，所提出的头部模块在多个时空图神经网络骨干上持续提升均方根误差指标，尤其在非零事件上效果显著，从而为实际海上交通控制提供更可靠的预测。

</details>


### [276] [DSFNet: Learning Dual-Domain Spectral Operators for Multi-Modality Spatio-Temporal Forecasting in Urban Transportation Systems](https://arxiv.org/abs/2606.07695)
*Yongchao Li,Yang Li,Zhuoxuan Li,Jun Chen,Chu Zhang,Jinde Cao,Leszek Rutkowski*

Main category: cs.LG

TL;DR: 提出双域谱滤波网络(DSFNet)，通过特征域和空间域谱算子分解空间-模态交互，并引入外部门控机制自适应调节时间动态，在多模态时空预测任务上显著超越现有方法。

<details>
  <summary>Details</summary>

Motivation: 现有方法未能显式建模不同模态变量间的耦合关系，同时难以同时处理：(1)外生影响下的时间动态异质性；(2)异构空间依赖与复杂跨变量耦合。

Method: 提出DSFNet，采用双域谱滤波分别捕获异构空间模式和显式变量关系：将空间-模态交互分解为特征域和空间域的谱算子，实现非局部依赖和跨模态耦合的可扩展建模；引入外部门控机制自适应调节外生影响下的时间动态。

Result: 在五个真实交通数据集上，与次优基线相比MAE降低3.21%-10.16%，显著优于现有SOTA方法，兼具效率与鲁棒性。

Conclusion: DSFNet通过双域谱滤波和外部门控机制有效建模多模态时空数据中的复杂耦合关系，在精度和效率上均取得领先性能。

Original Abstract: Multi-Modality Spatio-Temporal Forecasting (MoSTF) extends traditional spatio-temporal forecasting by incorporating diverse traffic modalities. Despite significant recent strides in spatio-temporal modeling, existing approaches often fail to explicitly model the coupling relationships between different modality variables. Accurate MoSTF is challenging, as it requires modeling (1) temporal dynamic heterogeneity under exogenous influences and (2) heterogeneous spatial dependencies alongside complex cross-variable couplings. To address these challenges, we propose the Dual-Domain Spectral Filtering Network (DSFNet). Our framework employs dual-domain spectral filtering to capture heterogeneous spatial patterns and explicitly model the relationships between variables. Unlike graph-based message passing or dense attention over node-modality pairs, DSFNet factorizes space-modality interactions into feature-domain and spatial-domain spectral operators, enabling scalable modeling of nonlocal dependencies and cross-modality couplings. Furthermore, we introduce an external gating mechanism to adaptively regulate temporal dynamics under external influences. We validate our method through extensive experiments on five representative real-world traffic datasets. Compared with the second-best baselines, DSFNet reduces MAE by 3.21%-10.16% across these datasets. The results demonstrate that DSFNet significantly outperforms existing state-of-the-art baselines in accuracy while exhibiting efficiency and robustness.

中文摘要翻译: 多模态时空预测(MoSTF)通过融合多样化的交通模态，扩展了传统的时空预测。尽管近年来时空建模取得了显著进展，现有方法往往未能显式建模不同模态变量之间的耦合关系。准确的MoSTF具有挑战性，因为它需要建模(1)外生影响下的时间动态异质性以及(2)异构空间依赖与复杂的跨变量耦合。为了解决这些挑战，我们提出了双域谱滤波网络(DSFNet)。我们的框架采用双域谱滤波来捕获异构空间模式并显式建模变量间的关系。与基于图的消息传递或节点-模态对上的密集注意力不同，DSFNet将空间-模态交互分解为特征域和空间域的谱算子，从而实现对非局部依赖和跨模态耦合的可扩展建模。此外，我们引入了一个外部门控机制，以自适应地调节外部影响下的时间动态。通过在五个具有代表性的真实交通数据集上进行大量实验验证了我们的方法。与次优基线相比，DSFNet在这些数据集上平均绝对误差(MAE)降低了3.21%-10.16%。结果表明，DSFNet在准确性上显著优于现有最先进的基线方法，同时展现出高效的性能和鲁棒性。

</details>


### [277] [Adversarial Robustness of Activation Steering in Large Language Models](https://arxiv.org/abs/2606.07696)
*Kien Le,Thai Le*

Main category: cs.LG

TL;DR: 本文首次系统评估了激活控制在对抗性文本扰动下的鲁棒性，发现该方法在所有设置下均存在结构性脆弱性：方向鲁棒性下降高达64%，置信度崩溃至0.25以下，最优层选择在扰动下偏移最多17层，且现有缓解措施效果有限。

<details>
  <summary>Details</summary>

Motivation: 激活控制作为一种流行的免训练方法，通过注入预计算的方向向量来控制LLM行为，但其对现实输入扰动的鲁棒性尚未被研究。

Method: 系统评估了四种提取方法、三种攻击策略、六种人格（来自Anthropic Model-Written Evaluation Dataset）以及五个模型（1.5B到30B参数）在对抗性文本扰动下的激活控制鲁棒性。

Result: 对抗攻击在所有设置下均成功：方向鲁棒性下降高达64%，后攻击置信度崩溃至0.25以下，几乎所有可操控输入的操控强度均下降；最优层在扰动下偏移最多17层；从对抗扰动输入中提取向量仅对PCA和MD方法在中大型模型上部分恢复可操控性，但无法定位改进后的最优层。

Conclusion: 激活控制的脆弱性是结构性的而非方法特定的，当前的层选择策略不足以支持真实世界的部署。

Original Abstract: Activation steering has become a popular training-free method to control LLM behavior by injecting precomputed direction vectors into the model's residual stream at inference time. Yet its robustness to realistic input variation remains unstudied. We present the first systematic evaluation of activation steering robustness under adversarial text perturbations on the inputs, covering four extraction methods, three attack strategies, six personas from Anthropic Model-Written Evaluation Dataset, and five models ranging from 1.5B to 30B parameters. Attacks succeed broadly across all settings: directional robustness drops by up to 64%, post-attack confidence collapses near or below 0.25 across all methods and models, and steering strength degrades on nearly every steerable input. Layer selection is equally fragile, with the optimal layer identified by an automated method on clean inputs shifting by up to 17 positions under perturbation, a failure that compounds the vector-level breakdown. Extracting vectors from adversarially perturbed inputs partially recovers steerability for PCA and MD on mid-to-large models, but they consistently fail to locate the improved optimal layer, limiting the practical benefit of this mitigation. Together, these findings reveal that the brittleness of activation steering is structural rather than method-specific, and that current layer selection strategies are not robust enough for real-world deployment.

中文摘要翻译: 激活注入已成为一种流行的免训练方法，通过在推理时向模型的残差流中注入预计算的方向向量来控制大语言模型的行为。然而，其对于现实输入扰动的鲁棒性尚未被研究。我们首次系统评估了输入上对抗性文本扰动下的激活控制鲁棒性，涵盖了四种提取方法、三种攻击策略、来自Anthropic模型编写评估数据集的六种人格，以及五个参数量从1.5B到30B的模型。攻击在所有设置下均广泛成功：方向鲁棒性下降高达64%，所有方法和模型的后攻击置信度崩溃至0.25附近或以下，且几乎所有可操控输入的操控强度均退化。层选择同样脆弱，由自动化方法在干净输入上识别的最优层在扰动下最多偏移17个位置，这一失效加剧了向量层面的崩溃。从对抗扰动输入中提取向量对于PCA和MD方法在中大型模型上部分恢复了可操控性，但它们始终无法定位改进后的最优层，限制了这一缓解措施的实际收益。综合来看，这些发现揭示了激活注入的脆弱性是结构性的而非方法特定的，且当前的层选择策略尚不足以支持真实世界的部署。

</details>


### [278] [Pharmacogenomic Knowledge Graph Augmentation for Graph Neural Network-Based Drug-Drug Interaction Prediction](https://arxiv.org/abs/2606.07698)
*Juergen Dietrich*

Main category: cs.LG

TL;DR: 该论文研究了用药理基因组学先验知识（来自PharmGKB数据库的CYP酶注释）增强图神经网络，以提升药物-药物相互作用（DDI）预测性能。实验发现知识图谱增强在配对层面数据划分下显著提升DDI类型分类（F1-macro从0.241提升至0.532），但二分类交互检测和药物层面泛化仍受限于信息天花板。机制验证表明增强优先改善CYP2C9介导的相互作用预测。

<details>
  <summary>Details</summary>

Motivation: 先前研究表明GNN在DDI预测中的性能受限于训练标签的结构信息含量（信息天花板），单靠架构改进无法突破。本研究探究是否可以通过引入独立于分子结构的药理学基因组学先验知识（代谢通路上下文）来部分突破这一天花板。

Method: 从PharmGKB数据库提取四种临床相关CYP亚型（CYP2D6、CYP3A4、CYP2C19、CYP2C9）的底物、抑制剂和诱导剂注释，构建12维特征向量，将其拼接至分子嵌入后用于交互预测。实验在配对层面和药物层面两种数据划分下进行，并在Tox21基准上扩展至单分子毒性预测。

Result: 1) 知识图谱增强在配对层面划分下显著提升DDI类型分类（F1-macro: 0.532 vs 基线0.241）；2) 二分类交互检测和药物层面泛化仍受限于信息天花板（AUC提升：0.224 vs 基线0.250）；3) 机制验证表明增强优先改善CYP2C9介导的交互预测（概率从基线0.033-0.117提升至0.560-0.586）；4) Tox21扩展实验证实效果依赖于药理学基因组学注释覆盖度。

Conclusion: 药理学基因组学先验知识能部分突破信息天花板，尤其改善配对层面DDI类型分类和特定CYP亚型介导的交互预测，但二分类检测和药物层面泛化仍受限于信息天花板。该发现为后续研究提出的多模态框架提供了动机。

Original Abstract: Graph neural networks (GNNs) applied to drug-drug interaction (DDI) prediction rely exclusively on molecular structure encoded as SMILES-derived graphs. Prior work in this series demonstrated that model performance is bounded by the structural information content of training labels -- an Information Ceiling -- that architectural refinements alone cannot overcome. The present study investigates whether pharmacogenomic prior knowledge from the PharmGKB database partially closes this ceiling by providing metabolic pathway context that is independent of, and complementary to, molecular structure. Cytochrome P450 (CYP) enzyme substrate, inhibitor, and inducer annotations for four clinically relevant isoforms (CYP2D6, CYP3A4, CYP2C19, CYP2C9) are extracted and incorporated as a 12-dimensional feature vector concatenated to the molecular embedding prior to interaction prediction. Experiments are conducted under both pair-level and drug-level data splits to quantify generalization to unseen drugs. Results indicate that knowledge graph (KG) augmentation substantially improves DDI type classification under pair-level split conditions (F1-macro: 0.532 vs. 0.241 baseline), while binary interaction detection and drug-level generalization remain bounded by the Information Ceiling (AUC inflation: 0.224 vs. 0.250 baseline). Mechanistic validation on strictly held-out compounds confirms that augmentation preferentially improves CYP2C9-mediated interaction prediction, with probabilities increasing from 0.033-0.117 (baseline) to 0.560-0.586 (KG-augmented). An extension to single-molecule toxicity prediction on the Tox21 benchmark confirms that the effect is contingent on pharmacogenomic annotation coverage. These findings motivate the multimodal framework proposed for the subsequent study in this series.

中文摘要翻译: 应用于药物-药物相互作用（DDI）预测的图神经网络（GNN）完全依赖于以SMILES衍生图编码的分子结构。本系列的先前工作表明，模型性能受限于训练标签的结构信息含量——即信息天花板（Information Ceiling）——这是仅靠架构改进无法克服的。本研究探究了来自PharmGKB数据库的药理学基因组学先验知识是否能通过提供独立于分子结构且与之互补的代谢通路上下文来部分突破这一天花板。我们提取了四种临床相关亚型（CYP2D6、CYP3A4、CYP2C19、CYP2C9）的细胞色素P450（CYP）酶底物、抑制剂和诱导剂注释，并将其整合为12维特征向量，在交互预测前拼接至分子嵌入中。实验在配对层面和药物层面两种数据划分下进行，以量化对未见药物的泛化能力。结果表明，在配对层面划分条件下，知识图谱（KG）增强显著提升了DDI类型分类性能（F1-macro：0.532对比基线0.241），而二分类交互检测和药物层面泛化仍受限于信息天花板（AUC提升：0.224对比基线0.250）。对严格保留化合物的机制验证证实，增强措施优先改善了CYP2C9介导的相互作用预测，预测概率从基线水平的0.033-0.117提升至KG增强后的0.560-0.586。在Tox21基准上扩展到单分子毒性预测的实验证实，该效果取决于药理学基因组学注释的覆盖度。这些发现为本系列后续研究提出的多模态框架提供了动机。

</details>


### [279] [EssentialGIN: a new approach for gene essentiality prediction based on graph isomorphism neural networks](https://arxiv.org/abs/2606.07700)
*Sahar Mansouri-Rad,Zahra Narimani,Parvin Razzaghi,Nazanin Hosseinkhan*

Main category: cs.LG

TL;DR: 本文提出一种改进的图同构网络（GIN）架构，通过整合PPI网络拓扑特征与基因表达、直系同源、亚细胞定位等多源生物数据，显著提高了必需基因预测的准确性，尤其在人类基因组中表现优于现有深度学习方法。

<details>
  <summary>Details</summary>

Motivation: 传统基于中心性度量的计算方法预测必需基因准确率低、假阳性率高，而现有深度学习方法仍有改进空间，因此需要更精确的预测模型来减少湿实验成本和时间。

Method: 提出改进的图同构网络架构，将蛋白质作为PPI网络中的节点进行嵌入，以保留PPI网络的拓扑特征；同时整合基因表达数据、基因直系同源信息和基因亚细胞定位信息作为节点属性，构建深度预测模型。

Result: 实验表明，该方法在必需基因预测任务上优于基于中心性的基线方法、Node2Vec、MLP以及图注意力网络（GAT）。在E. coli和D. melanogaster等简单生物中，使用Node2Vec嵌入的多层感知机表现良好，但在H. sapiens中，所提架构显著优于其他深度学习和图神经网络方法。

Conclusion: 使用整合生物数据（作为节点属性）并保留网络拓扑的图同构网络可以显著提高必需基因预测的准确性，尤其在复杂生物（如人类）中优势明显。

Original Abstract: Background: Prediction of essential genes (proteins), is a basic and challenging problem but at the same time very costly and time-consuming in wet-lab experiments. Predicting essential genes, only based on computational methods (to introduce wet-lab candidates) using centrality measures are not accurate and result in large number of false positives; therefore, more complex models such as deep learning and also integration of biological information are used in recent research to identify essential genes.
  Methods: In this work we focus on graph isomorphism networks, in order to embed proteins as a node in PPI network to conserve topological features of PPI network, and also integrate biological data such as gene expression data, gene orthology information and gene subcellular localization information, and introduced a deep architecture for predicting essential genes. Graph isomorphism network architecture is modified in this work for embedding node information.
  Results: Our experiments proved that the proposed method outperforms baseline centrality-based methods and also machine learning based methods such as Node2Vec, MLP, and also graph attention networks (GAT).
  Conclusion: In this paper we observed that using graph isomorphism networks that integrate biological data (as node attributes) and preserve network topology can significantly improve the essential gene prediction accuracy. In simpler organisms such as E. coli and D. melanogaster, methods such as multi-layer perceptron using Node2Vec embedding also performs very good, but in H. sapiens the introduced architecture significantly outperforms deep learning and other graph neural network solutions.
  Keywords: Essential gene prediction, graph neural network, graph isomorphism network, PPI network, node embedding

中文摘要翻译: 背景：必需基因（蛋白质）的预测是一个基础且具有挑战性的问题，同时在湿实验中进行非常昂贵且耗时。仅基于计算方法（以引入湿实验候选基因）使用中心性度量来预测必需基因并不准确，会产生大量假阳性；因此，近期研究使用更复杂的模型（如深度学习）以及整合生物信息来识别必需基因。
  方法：在本工作中，我们聚焦于图同构网络，将蛋白质作为PPI网络中的节点进行嵌入以保留PPI网络的拓扑特征，同时整合生物数据（如基因表达数据、直系同源信息和亚细胞定位信息），并引入了一个用于预测必需基因的深度架构。本工作对图同构网络架构进行了修改以进行节点信息嵌入。
  结果：我们的实验证明，所提出的方法优于基于中心性的基线方法以及基于机器学习的方法（如Node2Vec、MLP）和图注意力网络（GAT）。
  结论：在本文中我们观察到，使用整合生物数据（作为节点属性）并保留网络拓扑的图同构网络可以显著提高必需基因预测的准确性。在较简单的生物体（如大肠杆菌E. coli和黑腹果蝇D. melanogaster）中，使用Node2Vec嵌入的多层感知机等方法也表现非常出色，但在人类（H. sapiens）中，所引入的架构显著优于其他深度学习和图神经网络解决方案。
  关键词：必需基因预测，图神经网络，图同构网络，PPI网络，节点嵌入

</details>


### [280] [EvoCSFL: Surrogate-Assisted Evolutionary Client Selection for Efficient and Robust Federated Learning](https://arxiv.org/abs/2606.07702)
*Lin Qiang,Sun Xiaoyan,Hu Yao,Fang Wei*

Main category: cs.LG

TL;DR: 提出一种基于代理辅助的客户端进化选择框架，通过代理模型加速进化算法搜索最优客户端子集，在多个数据集上实现更快收敛、更低能耗和更强鲁棒性。

<details>
  <summary>Details</summary>

Motivation: 联邦学习中随机客户端选择导致收敛速度慢和鲁棒性差，因为客户端数据和系统的异构性。

Method: 首先利用典型客户端选择策略生成候选集，构建融合模型性能、通信延迟和能耗的度量函数，将客户端选择建模为组合优化问题；然后构建代理模型近似评估候选子集性能；最后用进化算法在代理模型引导下搜索最优客户端组合。

Result: 在MNIST、CIFAR10、CINIC10和TinyImageNet上，所提算法相比现有方法实现了更快的收敛速度、更低的能耗和更强的鲁棒性。

Conclusion: 代理辅助的客户端进化选择框架能有效解决联邦学习中异构性导致的选择问题，兼顾收敛、能耗和鲁棒性。

Original Abstract: The heterogeneity of client data and systems makes it difficult to achieve satisfactory convergence speed and robustness in federated learning with random client selection. To address this issue, this paper proposes a surrogate-assisted client evolutionary selection framework for federated learning. In this framework, some typical client selection strategies are first used to generate candidate sets, and a metric function that integrates model performance, communication latency, and energy consumption is developed to formulate the client selection problem as a combinatorial optimization one. Subsequently, a surrogate model is constructed using the candidate selections and metric to efficiently approximate the performance of selected client subsets. An evolutionary algorithm is employed to search the combinatorial space of client selections, guided by the surrogate model to accelerate convergence. Experiments on MNIST, CIFAR10, CINIC10, and TinyImageNet demonstrate that the proposed algorithm achieves faster convergence, lower energy consumption, and improved robustness compared to existing methods.

中文摘要翻译: 客户端数据和系统的异构性使得基于随机客户端选择的联邦学习难以获得令人满意的收敛速度和鲁棒性。针对这一问题，本文提出了一种面向联邦学习的代理辅助客户端进化选择框架。在该框架中，首先利用若干典型客户端选择策略生成候选集，并构建一个融合模型性能、通信延迟和能耗的度量函数，将客户端选择问题形式化为组合优化问题。随后，利用候选选择及其度量构建代理模型，以高效近似所选客户端子集的性能。采用进化算法在代理模型引导下搜索客户端选择的组合空间，从而加速收敛。在MNIST、CIFAR10、CINIC10和TinyImageNet上的实验表明，与现有方法相比，所提算法实现了更快的收敛速度、更低的能耗以及更强的鲁棒性。

</details>


### [281] [How Much Dense Attention is Necessary? Oracle-Guided Sparse Prefill for Full/GQA Layers in Hybrid Long-Context Models](https://arxiv.org/abs/2606.07703)
*Hongxing Wang,Harenome Razanajato,Zhen Zhang,Yujie Yuan,Hongsheng Liu*

Main category: cs.LG

TL;DR: 本文研究混合注意力模型中密集注意力层的必要程度，提出注意力质量top-k oracle作为诊断参考，并通过KL蒸馏训练头折叠辅助索引器实现稀疏服务加速，在Qwen系列模型上验证了可行性。

<details>
  <summary>Details</summary>

Motivation: 长上下文预填充阶段计算成本高昂，即使在混合模型中，全/GQA层仍需计算历史序列。需要研究在显式支持粒度和top-k预算下，需要多少密集注意力才能保持任务级行为。

Method: 提出注意力质量top-k oracle（诊断参考，不可部署），用于现有GQA检查点：逐层逐查询位置计算密集注意力，选择头平均后的token支持，仅在该支持上重新计算注意力。基于oracle指导，训练头折叠辅助索引器，通过KL蒸馏从密集注意力质量分布中学习，保持骨干网络冻结。

Result: 在Qwen系列检索密集型评估上，最长每查询oracle行与密集基线差距在1分以内；Qwen3.5-9B在4K到100K范围内差距在0.48分以内。蒸馏索引器在16K/32K验证宏差距分别为+2.04和+1.13分。单卡TTFT测量显示稀疏服务加速比：Qwen3.5-0.8B在NPU上为1.71x，Qwen3.5-9B在GPU上为1.93x，随机初始化极端情况达3.44x。

Conclusion: 本文首次分离了oracle可行性、蒸馏索引器质量和运行时加速潜力三个维度，指出未来工作需实现完全匹配的质量-延迟前沿。

Original Abstract: Long-context prefill remains expensive because full/GQA layers still score the historical sequence, even in hybrid models with local, sparse, linear, or recurrent components. We study how much dense attention is needed to preserve task-level behavior under explicit support granularity and top-k budgets.
  We introduce an attention-mass top-k oracle for existing GQA checkpoints: for each layer and query position, it computes dense attention, selects head-averaged token support, and recomputes attention only on that support. The oracle is a diagnostic reference, not a deployable accelerator, and separates sparse-budget feasibility from indexer error and runtime realization effects.
  On Qwen-family retrieval-heavy evaluations, the longest per-query oracle rows stay within 1 point of dense, and a Qwen3.5-9B RULER-style sweep from 4K to 100K stays within 0.48 points. Guided by the oracle, we derive a head-collapsed auxiliary indexer trained by KL distillation from dense attention-mass distributions while keeping the backbone frozen. With separately distilled Qwen3.5-0.8B and Qwen3.5-9B indexers, the reported 16K/32K validation macro gaps are +2.04 and +1.13 points, treated as quality preservation rather than improvement; fused selection-block-shared support can introduce a larger realization gap.
  Preliminary single-card TTFT measurements show distilled-indexer sparse serving speedups of 1.71x for Qwen3.5-0.8B on NPU and 1.93x for Qwen3.5-9B on GPU against its dense FlashAttention-2 baseline. Additional random-init stress rows reach 3.44x, indicating sparse-runtime headroom but not validated output quality. This first release separates oracle feasibility, distilled-indexer quality, and runtime headroom, leaving a fully matched quality-latency frontier to future work.

中文摘要翻译: 长上下文预填充仍然代价高昂，因为即使在包含局部、稀疏、线性或循环组件的混合模型中，全注意力层或分组查询注意力层仍需对历史序列进行评分。我们研究了在显式支持粒度和top-k预算下，需要多少密集注意力才能保持任务级行为。我们为现有的GQA检查点引入了一个注意力质量top-k oracle：对于每一层和每个查询位置，它计算密集注意力，选择头平均后的token支持，并仅在该支持上重新计算注意力。该oracle是一个诊断参考，而非可部署的加速器，它将稀疏预算可行性从索引器误差和运行时实现效应中分离出来。在Qwen系列的检索密集型评估上，最长每查询oracle行与密集基线的差距在1分以内，而Qwen3.5-9B在4K到100K的RULER风格扫描中差距保持在0.48分以内。在oracle的指导下，我们推导出一个头折叠辅助索引器，通过KL蒸馏从密集注意力质量分布中学习，同时保持骨干网络冻结。使用分别蒸馏的Qwen3.5-0.8B和Qwen3.5-9B索引器，报告的16K/32K验证宏差距分别为+2.04和+1.13分，这被视为质量保持而非改进；融合选择块共享支持可能引入更大的实现差距。初步的单卡TTFT测量显示，蒸馏索引器的稀疏服务相较于密集FlashAttention-2基线实现了加速：Qwen3.5-0.8B在NPU上为1.71倍，Qwen3.5-9B在GPU上为1.93倍。额外的随机初始化极端行达到3.44倍，表明稀疏运行时存在潜力但输出质量未经验证。本次首次发布分离了oracle可行性、蒸馏索引器质量和运行时潜力，将完全匹配的质量-延迟前沿留待未来工作。

</details>


### [282] [FunctionEvolve: Structure-Guided Symbolic Regression with LLMs](https://arxiv.org/abs/2606.07704)
*Zeyu Xia,Jun Zhu,Dong Yan*

Main category: cs.LG

TL;DR: 提出FunctionEvolve，一种利用表达式树进行结构可见搜索的符号回归进化框架，在LLM-SRBench的129个合成任务中恢复107个精确公式，SA@50达82.9%，比同骨干基线高4.5倍。

<details>
  <summary>Details</summary>

Motivation: 现有基于LLM的符号回归系统存在结构盲区：候选选择不透明、缺乏局部变异机制、系数拟合脆弱导致正确骨架被低估，无法进行结构感知的域引导搜索。

Method: FunctionEvolve采用表达式树组织搜索：结构摘要促进多样化父代选择、局部树编辑保留有用子表达式、结构感知拟合分解/约束/简化系数以实现可靠评分，仅使用初等函数族。

Result: 在LLM-SRBench的129个合成任务中，FunctionEvolve（Claude Opus 4.6）恢复107个精确形式，SA@50为82.9%（比同骨干基线高4.5倍），SA@1为55.8%（比最强已发表top-1结果高3.6倍）；消融实验表明结构可见搜索是可靠恢复的核心；还发现了基准中材料科学子集的共线性问题。

Conclusion: 结构可见的搜索机制（表达式树+LLM引导+结构感知系数优化）是实现符号回归中可靠精确恢复的关键，同时需注意基准测试中的共线性问题。

Original Abstract: Symbolic regression aims to uncover explicit scientific laws from data. Recent methods use LLMs to guide mutation from background text, which is more directed than random genetic programming. However, exact symbolic recovery requires both semantic guidance and explicit structure, so that domain-informed search are carried out through valid symbolic representation. Current LLM-driven systems remain structure-blind: they select among opaque candidates, lack explicit mechanisms for local mutation, and rely on brittle coefficient fitting that can undervalue correct skeletons. We propose FunctionEvolve, an evolutionary framework using expression trees to organize the whole search: structural summaries promote diverse parent selection, local tree edits preserve useful subexpressions, and structure-aware fitting decomposes, constrains, and simplifies coefficients for more reliable scoring. It uses only elementary function families, without additional domain-specific rules limiting generalization. On the 129-task synthetic subset of LLM-SRBench, FunctionEvolve with \emph{Claude Opus 4.6} recovers 107 exact forms, reaching 82.9% SA@50, 4.5x above same-backbone baselines, and 55.8% SA@1, 3.6x above the strongest previously published top-1 result. Ablations show that structure-visible search is central to reliable recovery, with LLM-guided refinements and structure-aware coefficient optimization serving as essential proposal and scoring mechanisms. We also audit the benchmark and show that collinearity in its materials-science subset creates identifiability issues.

中文摘要翻译: 符号回归旨在从数据中发现明确的科学定律。近期方法利用大语言模型从背景文本引导变异，这比随机遗传编程更具方向性。然而，精确的符号恢复需要同时具备语义引导和显式结构，以便通过有效的符号表示进行领域信息搜索。当前的LLM驱动系统仍是结构盲的：它们在 opaque 的候选之间进行选择，缺乏局部变异的显式机制，并依赖于脆弱的系数拟合，这可能导致正确骨架被低估。我们提出FunctionEvolve，一种使用表达式树组织整个搜索过程的进化框架：结构摘要促进多样化的父代选择，局部树编辑保留有用的子表达式，结构感知拟合通过分解、约束和简化系数实现更可靠的评分。它仅使用初等函数族，无需额外的领域特定规则限制泛化能力。在LLM-SRBench的129个任务合成子集上，使用Claude Opus 4.6的FunctionEvolve恢复了107个精确形式，SA@50达到82.9%，是同骨干基线的4.5倍；SA@1达到55.8%，是最强已发表top-1结果的3.6倍。消融实验表明，结构可见的搜索是可靠恢复的核心，LLM引导的改进和结构感知系数优化作为关键的提议和评分机制。我们还对基准进行了审计，表明其材料科学子集中的共线性导致了可辨识性问题。

</details>


### [283] [SAW: Stage-Aware Dynamic Weighting for Multi-Objective Reinforcement Learning in Large Language Models](https://arxiv.org/abs/2606.07705)
*Yuchen He,Baolong Bi,Shenghua Liu,Huaming Liao,Yuyao Ge,Bolin Wan,Siqian Tong,Juan Chen,Jiafeng Guo,Xueqi Cheng*

Main category: cs.LG

TL;DR: 针对多目标强化学习中奖励学习异步性问题，提出阶段感知动态加权（SAW）机制，利用变异系数作为实时信息量代理，动态调整各目标权重，在几乎零额外计算开销下提升大模型对齐的训练效率和最终性能。

<details>
  <summary>Details</summary>

Motivation: 现有MORL方法采用静态加权求和，忽略了奖励学习在不同目标上的显著异步性——学习充分的目标产生同质低方差信号，其残留噪声会污染聚合奖励（在GRPO中）或占据优势函数的固定份额（在GDPO中），干扰尚未学好的目标所携带的稀缺高价值信号。

Method: 提出SAW（Stage-Aware Dynamic Weighting）机制，使用变异系数（CV）作为尺度不变的实时信息量代理，依据批次内各目标维度的相对信息量动态重新加权其奖励或优势贡献。该方法仅依赖批次级统计量，无需多次前向/反向传播，计算开销极低。

Result: 在工具调用和文本摘要任务上的实验表明，SAW在GRPO和GDPO两种框架下均能一致提升训练效率和最终性能，验证了其作为多奖励大模型对齐的通用即插即用模块的有效性。

Conclusion: SAW作为一种轻量级、算法无关的动态加权机制，有效解决了多目标强化学习中的奖励学习异步问题，且几乎不引入额外计算负担，可作为通用插件应用于多种大模型对齐框架。

Original Abstract: Although multi-objective reinforcement learning (MORL) is central to aligning large language models with complex human preferences, the prevailing practice of static weighted summation overlooks a more fundamental phenomenon: reward learning is markedly asynchronous across objectives. Well-learned dimensions quickly produce homogeneous, low-variance signals whose residual noise contaminates the aggregated reward (in GRPO) or occupies a fixed share of the advantage budget (in GDPO), interfering with the scarce yet high-value signals carried by under-learned dimensions. To address this asynchrony, we propose Stage-Aware Dynamic Weighting (SAW), a lightweight, algorithm-agnostic dynamic weighting mechanism. SAW utilizes the coefficient of variation (CV) as a scale-invariant proxy for real-time informativeness, reweighting each dimension's reward or advantage contribution by its relative informativeness within the batch. Unlike gradient-based methods that require multiple forward and backward passes, SAW relies solely on batch-level statistics, introducing nearly negligible computational overhead. Experiments on tool-calling and text summarization tasks demonstrate that SAW consistently improves both training efficiency and final performance under both GRPO and GDPO frameworks, confirming it as a general-purpose plug-in for multi-reward LLM alignment. Our code is available at https://github.com/Zhaolutuan/SAW

中文摘要翻译: 尽管多目标强化学习（MORL）对于将大型语言模型与复杂的人类偏好对齐至关重要，但当前普遍采用的静态加权求和做法忽略了一个更为根本的现象：不同目标之间的奖励学习存在显著的异步性。学习充分的目标维度很快产生同质、低方差的信号，其残留噪声（在GRPO中）污染了聚合奖励，或（在GDPO中）占据了优势函数的一个固定份额，从而干扰了尚未学好的维度所携带的稀缺高价值信号。为了解决这一异步性问题，我们提出了阶段感知动态加权（SAW），一种轻量级、算法无关的动态加权机制。SAW利用变异系数（CV）作为尺度不变的实时信息量代理，根据批次内各维度信息的相对丰富程度，重新加权每个维度的奖励或优势贡献。与需要多次前向和反向传播的梯度方法不同，SAW仅依赖批次级统计量，计算开销几乎可以忽略不计。在工具调用和文本摘要任务上的实验表明，SAW在GRPO和GDPO两种框架下均能一致提升训练效率和最终性能，证实了其作为多奖励大模型对齐的通用即插即用模块的有效性。我们的代码已开源：https://github.com/Zhaolutuan/SAW

</details>


### [284] [Decoding Naturalistic Emotion Dynamics from the Brain: An LLM-Enhanced Regression Framework](https://arxiv.org/abs/2606.07707)
*Lemei Zhang,Peng Liu,Hans Dahle Kvadsheim,August Sætre Aasvær,Shuer Ye,Reza Bonyadi,Maryam Ziaei,Jon Atle Gulla*

Main category: cs.LG

TL;DR: 本文提出用多目标连续回归框架替代传统的离散单标签分类，结合大语言模型自动标注和动态功能连接，从自然叙事fMRI数据中追踪多个重叠情感维度的连续轨迹，并利用图论可解释AI揭示情感特异性的脑网络拓扑结构。

<details>
  <summary>Details</summary>

Motivation: 传统情感解码将情绪状态简化为基于稳定刺激的离散单标签分类任务，忽略了人类情感的连续性、流动性和共现性。本文旨在通过多目标回归框架重新概念化情感解码，以追踪随时间演变的多个重叠情感维度。

Method: 1) 利用大语言模型(LLM)从自然主义听觉叙事《爱丽丝梦游仙境》中提取细粒度的连续情感特征，作为主观情感的代理标注；2) 采用正则化和基于核的机器学习算法作为连续估计器，追踪宏观神经状态变化幅度；3) 使用动态功能连接(DFC)的时间快照作为特征，与静态ROI幅度表征进行对比；4) 运用图论可解释AI(XAI)技术分解预测特征，揭示情感特异性的拓扑配置。

Result: 基于动态功能连接(DFC)时间快照训练的模型显著优于静态ROI幅度表征，能够有效捕捉快速波动叙事输入下的连续情感轨迹。图论XAI揭示了高度可解释的、情感特异性的脑网络拓扑结构。

Conclusion: LLM自动标注在情感神经科学中具有实用价值；实验结果支持心理建构主义框架，表明动态、分布式的网络交互比严格的位置主义情感观具有更强的解释力。

Original Abstract: Decoding emotional states from neural signals has been typically framed as a discrete, single-label classification task based on emotionally stable stimuli, a formulation that oversimplifies the continuous, fluid, and co-occurring nature of human affect. This study reconceptualizes emotion decoding by adopting a multi-target regression framework to track multiple overlapping emotional dimensions as continuous trajectories over time. Leveraging the robust generalization capabilities of Large Language Models (LLMs), we extracted fine-grained, continuous sentiment profiles from a naturalistic auditory narrative, Alice in Wonderland, to serve as scalable proxies for subjective affect from human fMRI dataset. Departing from standard classification paradigms or mass-univariate subtractive contrasts that filter out network dynamics, we leverage regularized and kernel-based machine learning algorithms as continuous estimators to track the magnitude of macroscale neural state variations. We demonstrate that models trained on temporal snapshots of Dynamic Functional Connectivity (DFC) significantly outperform static region-of-interest (ROI) amplitude representations, effectively capturing continuous emotional trajectories under rapidly fluctuating narrative input. Furthermore, by implementing graph-theoretical Explainable AI (XAI) techniques, we deconstruct the underlying predictive features to reveal highly interpretable, emotion-specific topological configurations. Collectively, these results highlight the utility of LLM-automated annotation in affective neuroscience and provide compelling empirical evidence for psychological constructionist frameworks, demonstrating that dynamic, distributed network interactions offer superior explanatory power over strictly locationist accounts of emotion.

中文摘要翻译: 从神经信号中解码情绪状态通常被框定为基于情绪稳定刺激的离散单标签分类任务，这种表述过度简化了人类情感的连续性、流动性和共现性。本研究通过采用多目标回归框架来追踪随时间轨迹演变的多个重叠情感维度，重新概念化了情感解码。利用大语言模型(LLM)强大的泛化能力，我们从自然主义听觉叙事《爱丽丝梦游仙境》中提取了细粒度的连续情感特征，作为人类fMRI数据集中主观情感的 scalable 代理。不同于标准的分类范式或过滤掉网络动态的mass-univariate减法对比，我们采用正则化和基于核的机器学习算法作为连续估计器，来追踪宏观神经状态变化的幅度。我们证明了基于动态功能连接(DFC)时间快照训练的模型显著优于静态感兴趣区(ROI)幅度表征，能够有效捕捉快速波动叙事输入下的连续情感轨迹。此外，通过实施图论可解释AI(XAI)技术，我们分解了底层预测特征，揭示了高度可解释的、情感特异性的拓扑配置。综合来看，这些结果凸显了LLM自动标注在情感神经科学中的实用性，并为心理建构主义框架提供了令人信服的经验证据，证明动态、分布式的网络交互比严格的位置主义情感观具有更强的解释力。

</details>


### [285] [WhiFlash: Accelerating Speculative Decoding with Token-Level Cross-Paradigm Routing](https://arxiv.org/abs/2606.07710)
*Young D. Kwon,Miles Williams,Rui Li,Alexandros Kouris,Stylianos I. Venieris*

Main category: cs.LG

TL;DR: WhiFlash 是首个跨范式投机解码方法，通过细粒度路由统一自回归与扩散并行草稿模型，结合缓存优化实现最高69.6%的吞吐量提升。

<details>
  <summary>Details</summary>

Motivation: 现有投机解码方法采用静态草稿范式（自回归用于推理、扩散用于结构化输出），但草稿准确性在单序列内剧烈波动，静态方法和粗粒度路由无法充分利用性能潜力。

Method: 提出WhiFlash，首个跨范式投机解码方法（Cross-Paradigm SD），在token级控制器下统一自回归和扩散并行草稿模型。采用细粒度路由机制（轻量熵基或学习型神经策略），参数化实现在预期token增益与延迟间可调平衡。引入Lazy Catch-up和KV-only Prefill两种缓存优化技术，将切换开销压至每轮延迟的7%以下。

Result: WhiFlash在特定类别任务上相较最先进的自回归方法EAGLE-3实现最高69.6%的吞吐量提升，相较扩散方法DFlash实现37.3%的提升，显著提高了接受长度。

Conclusion: 通过利用本质上不同的草稿架构的互补优势，WhiFlash实现了更高的接受长度和显著的吞吐量增益，验证了跨范式投机解码的有效性。

Original Abstract: The autoregressive nature of large language models (LLMs) remains a significant bottleneck for inference, particularly in complex agentic workloads. While speculative decoding (SD) accelerates inference, current approaches rely on static drafting paradigms, utilising either autoregressive drafting models for reasoning or diffusion-based parallel drafting models for structured outputs. We empirically find that drafting accuracy fluctuates dramatically within a single sequence, leaving significant performance unrealised by static paradigms and coarse-grained routing. To address this volatility, we introduce WhiFlash, the first cross-paradigm SD method that unifies autoregressive and diffusion-based parallel drafting under a single token-level controller. WhiFlash adopts a fine-grained routing mechanism that employs either a lightweight entropy-based or a learned neural policy, both parametrised to provide a tunable balance between expected token gain and latency. To make high-frequency switching computationally viable, we introduce novel cache-management optimisations, Lazy Catch-up and KV-only Prefill, reducing switching overhead to below 7% of per-round latency. By capitalising on the complementary strengths of fundamentally distinct drafting architectures, WhiFlash achieves significantly higher acceptance lengths, yielding category-specific throughput gains of up to 69.6% over the state-of-the-art autoregressive EAGLE-3 and 37.3% over the diffusion-based DFlash.

中文摘要翻译: 大语言模型（LLMs）的自回归特性仍然是推理过程中的显著瓶颈，尤其是在复杂的智能体工作负载中。虽然投机解码（SD）能加速推理，但现有方法依赖静态草稿范式，使用自回归草稿模型进行推理，或使用基于扩散的并行草稿模型生成结构化输出。我们通过实验发现，草稿准确性在单序列内波动剧烈，静态范式和粗粒度路由无法充分利用潜在性能。为解决这一波动性问题，我们提出了WhiFlash，这是首个跨范式投机解码方法，在统一的token级控制器下整合了自回归与基于扩散的并行草稿模型。WhiFlash采用细粒度路由机制，使用轻量级的熵基策略或学习型神经策略，两者均被参数化以在期望token增益和延迟之间提供可调平衡。为使高频切换在计算上可行，我们引入了新颖的缓存优化技术——Lazy Catch-up和KV-only Prefill，将切换开销降至每轮延迟的7%以下。通过利用根本不同的草稿架构的互补优势，WhiFlash实现了显著更高的接受长度，在特定类别任务上，相较于当前最先进的自回归方法EAGLE-3实现了最高69.6%的吞吐量提升，相较于基于扩散的DFlash实现了37.3%的提升。

</details>


### [286] [Rosetta Memory: Adaptive Memory for Cross-LLM Agents](https://arxiv.org/abs/2606.07711)
*Hao Yang,Shiqi Shen,Haoxuan Li,Zhipeng Wang,Zhi Gong,Xu Chen*

Main category: cs.LG

TL;DR: 提出了一种从“以LLM为中心的记忆设计”转向“以记忆为中心的LLM适配”的方法，通过两个基于profile条件的操作算子（读写端）和最小增益采样课程、性能差距奖励，实现跨不同LLM的记忆适应与激活。

<details>
  <summary>Details</summary>

Motivation: 现有记忆系统以LLM为中心，为特定骨干模型设计记忆操作，但用户经常在不同LLM之间切换（如用Claude编程、GPT写作），导致一个模型写入的记忆需要被另一个模型消费，而现有方法未能解决跨模型记忆适配问题。

Method: 1) 从写端和读端设计两个基于profile条件的操作算子，联合训练优化记忆的存储和呈现方式；2) 提出最小增益采样课程，优先训练服务最不充分的LLM以增强泛化性；3) 设计性能差距奖励，通过与朴素记忆基线对比来衡量算子实际贡献而非LLM自身能力。

Result: 在HotpotQA、2WikiMultihopQA和MuSiQue三个数据集上，模型一致优于基线方法，且在未见模型替换场景下保持鲁棒性。

Conclusion: 记忆-centric的LLM适配范式比LLM-centric的记忆设计更能有效解决跨模型记忆迁移问题，所提方法泛化性强、鲁棒性好。

Original Abstract: Memory is the key component for transforming a stateless LLM into a persistent, evolving agent through experience accumulation, long-horizon planning, and continual self-improvement. Existing memory systems typically take the LLM as the center and design memory operations tailored to a specific backbone. In practice, however, users frequently switch between LLMs, for example using Claude for coding and GPT for writing across tasks, or routing different steps to different backbones within a single task for cost-effective trade-offs. As a result, memory written by one model often needs to be consumed by another. Making upstream memory effectively adapt to and activate downstream LLMs remains a critical yet underexplored problem. To bridge this gap, we shift the perspective from LLM-centric memory design to \emph{memory-centric LLM adaptation}. Specifically, we approach the above upstream-downstream memory adaptation problem from both the write and read sides, and design two profile-conditioned operators that are jointly trained to optimize how memory is stored and presented for better task completion. To ensure the learned operators generalize across a broad set of LLMs, we propose a minimum-gain sampling curriculum that prioritizes the least-served LLMs during training. To better measure the operators' actual contribution rather than the LLM's own capability, we design a performance-gap reward that compares against a naive memory baseline. Experiments on HotpotQA, 2WikiMultihopQA, and MuSiQue demonstrate that our model consistently outperforms baselines and remains robust under unseen-model replacement.

中文摘要翻译: 记忆是将无状态的LLM转变为持久、进化的智能体的关键组件，使其能够通过经验积累、长程规划和持续自我改进来实现演化。现有的记忆系统通常以LLM为中心，并针对特定的骨干模型设计记忆操作。然而在实践中，用户经常在不同LLM之间切换，例如使用Claude进行编程、GPT进行写作，甚至在同一任务中将不同步骤路由到不同骨干模型以实现成本效益的权衡。因此，由一个模型写入的记忆往往需要被另一个模型消费。如何使上游记忆有效地适应并激活下游LLM仍然是一个关键但尚未充分探索的问题。为弥合这一差距，我们将视角从“以LLM为中心的记忆设计”转向“以记忆为中心的LLM适配”。具体来说，我们从写端和读端两个角度处理上述上游-下游记忆适配问题，并设计了两个基于profile条件的操作算子，这些算子经过联合训练，优化记忆的存储和呈现方式，以更好地完成任务。为确保所学算子能泛化到广泛的LLM集合，我们提出了一种最小增益采样课程，优先训练服务最不充分的LLM。为了更好地衡量算子的实际贡献而非LLM自身的能力，我们设计了一种性能差距奖励，与朴素记忆基线进行比较。在HotpotQA、2WikiMultihopQA和MuSiQue上的实验表明，我们的模型一致优于基线方法，且在未见模型替换场景下保持鲁棒性。

</details>


### [287] [Attention at the Theoretical Minimum: A Mathematics of Arrays Framework for Memory-Optimal Transformer Kernels](https://arxiv.org/abs/2606.07713)
*Lenore Mullin,Gaetan Hains*

Main category: cs.LG

TL;DR: 本文提出了一种基于数组数学（MoA）框架的缩放点积注意力机制重构方法，通过推导指称范式（DNF）消除所有中间数组，将数据移动量从标准实现的O(n²)降至O(n)，无需经验调优即可实现内存最小化，并预测可获得2-100倍加速和2-50倍能耗降低。

<details>
  <summary>Details</summary>

Motivation: 标准注意力机制实现中存在二次方内存流量瓶颈（序列长度n导致O(n²)数据移动），而DRAM访存能耗比算术运算高100-1000倍，因此仅关注FLOPs的分析从根本上错误表征了计算瓶颈。现有方案如FlashAttention依赖硬件特定经验调优，缺乏统一的代数框架。

Method: 采用数组数学（MoA）形式化方法对缩放点积注意力和数值稳定的softmax进行重构，通过代数构造而非经验调优推导出指称范式（DNF），消除所有中间数组（包括隐式转置键缓冲区和所有softmax临时变量）。该方法同时实现了数组融合、形状变换正确性和可预测成本模型，并建立了从Python规范到操作范式再到维度提升硬件映射的形式化验证流水线。

Result: DNF的数据移动量为O(n·dk + n·dv)，而标准实现为O(n² + n·dk + n·dv)。在双精度浮点数下与PyTorch进行数值验证。预测性能模型显示2-100倍加速和2-50倍能耗降低，优势在超大规模计算中进一步扩大。

Conclusion: 内存最小性是在编写任何代码之前通过定理证明建立的，而非经验结果。该工作提供了与DARPA边缘部署和DOE超大规模计算优先事项直接相关的性能可移植AI内核。

Original Abstract: The attention mechanism is the dominant computational bottleneck in modern transformer-based AI. Its standard implementation incurs quadratic memory traffic in the sequence length~$n$, and DRAM accesses cost 100--1000$\times$ more energy than arithmetic operations on contemporary hardware, so any analysis focused solely on FLOP counts fundamentally mischaracterises the bottleneck.
  We present a Mathematics of Arrays (MoA) reformulation of scaled dot-product attention and its numerically stable softmax, deriving a Denotational Normal Form (DNF) that eliminates all intermediate arrays -- including the implicit transposed-key buffer and every softmax temporary -- by algebraic construction rather than empirical tuning. The DNF achieves $O(n_{dk} + n{_{dv}})$ data movement versus $O(n^2 + n_{dk} + n_{dv})$ for the standard implementation, where $n$ is the sequence length, $dk$ is the key dimensionality and $dv$ the value dimensionality, and is verified numerically against PyTorch at full double-precision floating-point on concrete inputs.
  Unlike hardware-specific accelerators or empirical tiling schemes such as FlashAttention, MoA simultaneously provides array fusion, shape-transformation correctness, and predictive cost models from a single algebraic framework. Memory minimality is a theorem established before any code is written. A predictive performance model projects $2$--$100\times$ speedup and $2$--$50\times$ energy reduction, with the advantage widening at exascale. The derivation establishes a formally verified pipeline from Python specification through (ONF) Operational Normal Form, and dimension-lifted hardware mapping, providing performance-portable AI kernels of direct relevance to DARPA edge-deployment and DOE exascale priorities.

中文摘要翻译: 注意力机制是现代基于transformer的AI中主导性的计算瓶颈。其标准实现会产生序列长度n的二次方内存流量，而在当代硬件上，DRAM访问能耗比算术运算高100-1000倍，因此任何仅关注FLOPs计数的分析都从根本上错误表征了瓶颈。本文提出了缩放点积注意力及其数值稳定softmax的数组数学（MoA）重构，通过代数构造而非经验调优推导出一种指称范式（DNF），消除了所有中间数组——包括隐式转置键缓冲区和每个softmax临时变量。DNF实现了O(n·dk + n·dv)的数据移动量，而标准实现为O(n² + n·dk + n·dv)，其中n为序列长度，dk为键维度，dv为值维度，并在具体输入上以全双精度浮点数与PyTorch进行了数值验证。与硬件特定的加速器或经验性分块方案（如FlashAttention）不同，MoA从单一的代数框架同时提供了数组融合、形状变换正确性和可预测成本模型。内存最小性是在编写任何代码之前通过定理证明建立的。预测性能模型显示2-100倍加速和2-50倍能耗降低，优势在超大规模计算中进一步扩大。该推导建立了一条从Python规范通过操作范式（ONF）到维度提升硬件映射的形式化验证流水线，提供了与DARPA边缘部署和DOE超大规模计算优先事项直接相关的性能可移植AI内核。

</details>


### [288] [Beyond Accuracy: Interpreting Topic Representation in Suicide Ideation Detection Models](https://arxiv.org/abs/2606.07714)
*Hamideh Ghanadian,Isar Nejadgholi,Hussein Al Osman*

Main category: cs.LG

TL;DR: 本文研究了自杀意念检测模型的内部表征，发现主题增强不仅提升了模型性能，还使心理风险因素（如移民、家庭问题、财务危机）的内部表征更清晰、更具可分离性和可解释性。

<details>
  <summary>Details</summary>

Motivation: 现有自杀意念检测模型仅关注聚合性能指标，忽视了模型内部如何表征心理学上有意义的风险因素。在高风险心理健康应用中，理解这些内部表征对于安全性、透明性和负责任部署至关重要。

Method: 使用可视化与几何分析方法，比较在原始数据集和主题增强数据集上训练的模型的内部表征空间，分析心理风险因素的连贯性与可分离性。

Result: 主题感知增强能够提升移民、家庭问题、财务危机等代表性不足的社会心理风险因素在内部表征空间中的清晰度和区分度。

Conclusion: 数据增强不仅提升了模型性能，还使得内部表征更加结构化和可解释，有助于提升模型在高风险心理健康应用中的安全性和透明度。

Original Abstract: Suicide ideation detection models are typically evaluated using aggregate performance metrics, yet little is known about how they internally represent psychologically meaningful risk factors. In high-stakes mental health applications, understanding these internal representations is essential for safety, transparency, and responsible deployment. In this work, we move beyond accuracy and analyze how suicide detection models trained on original and topic-augmented datasets encode psychological risk factors in their internal representation space. Using visualization and geometric analysis, we examine the coherence and separability of topic-related features. Our results show that topic-aware augmentation increases the clarity and distinctness of underrepresented psychosocial risk factors such as immigration, family issues, and financial crisis. These findings suggest that augmentation not only improves model performance but also leads to more structured and interpretable internal representations.

中文摘要翻译: 自杀意念检测模型通常通过聚合性能指标进行评估，但关于它们如何在内部表征心理学上有意义的风险因素方面却知之甚少。在高风险的心理健康应用中，理解这些内部表征对于安全性、透明性和负责任部署至关重要。在本工作中，我们超越了准确率，分析了在原始数据集和主题增强数据集上训练的自杀检测模型如何在其内部表征空间中编码心理风险因素。通过可视化和几何分析，我们检验了主题相关特征的连贯性和可分离性。结果表明，主题感知增强提升了移民、家庭问题和财务危机等代表性不足的社会心理风险因素的清晰度和区分度。这些发现表明，数据增强不仅提升了模型性能，还带来了更加结构化和可解释的内部表征。

</details>


### [289] [A Geometry-Aware Triplane Field Network for Vehicle Aerodynamic Prediction](https://arxiv.org/abs/2606.07724)
*Kangkang Qi,Huiyu Yang,Keqi Ding,Yunpeng Wang,Yuntian Chen,Yuanwei Bin,Rikui Zhang,Jianchun Wang*

Main category: cs.LG

TL;DR: 本文提出GTF-Net，一种基于三平面场网络和几何感知的机器学习方法，用于车辆空气动力学压力与壁面剪切应力预测，通过结合AFNO谱混合与CNN局部细化，在多个基准上取得最优结果。

<details>
  <summary>Details</summary>

Motivation: 高保真CFD计算成本高昂，限制了早期设计探索。机器学习表面场预测需要同时捕捉全局流场上下文和局部几何细节，现有方法尚不能满足这一需求。

Method: GTF-Net通过共享MLP和光滑双线性光栅化从采样点构建三平面特征，经AFNO谱混合与CNN细化的双流骨干网络处理，查询阶段融合车辆对齐方向坐标、法向投影特征和体素曲率代理。

Result: 在压力预测上相对L2误差从0.157降至0.145，壁面剪切应力预测从0.237降至0.226，消融实验验证了各模块的有效性。

Conclusion: 将结构化三平面表示与显式空气动力学几何线索相结合的机制有效提升了预测精度，各设计模块均对精度有贡献。

Original Abstract: High-fidelity computational fluid dynamics (CFD) is crucial to vehicle aerodynamic analysis, but its cost still constrains early-stage design exploration. Machine-learning-based surface-field prediction offers a faster alternative if the model can efficiently capture both global flow context and local geometric detail. This work proposes a machine-learning-based method, named the geometry-aware triplane field network (GTF-Net), for vehicle aerodynamic pressure and wall shear stress prediction. GTF-Net constructs triplane features directly from sampled surface points through a shared multilayer perceptron (MLP) and smooth bilinear rasterization. The planes are then processed by a dual-stream backbone that combines adaptive Fourier neural operator (AFNO) spectral mixing with convolutional neural network (CNN) refinement, so long-range aerodynamic coupling and local geometry-induced variations are modeled in the same representation. At query stage, sampled triplane features are combined with vehicle-aligned directional coordinates, normal-projection features, and a voxel-based curvature proxy. GTF-Net is compared with Transolver, geometry-informed neural operator (GINO), and TripNet, a triplane-based surrogate model. GTF-Net improves the relative L2 error from the strongest baseline value of 0.157 to 0.145 for pressure prediction and from 0.237 to 0.226 for wall shear stress prediction. Ablation results show that AFNO mixing, local CNN refinement, and query-side geometric encoding each contribute to accuracy, supporting the proposed mechanism of combining structured triplane representation with explicit aerodynamic geometry cues.

中文摘要翻译: 高保真计算流体动力学（CFD）对于车辆空气动力学分析至关重要，但其计算成本仍然制约着早期设计阶段的探索。基于机器学习的表面场预测提供了一种更快的替代方案，前提是模型能够高效地同时捕捉全局流场上下文和局部几何细节。本文提出一种名为几何感知三平面场网络（GTF-Net）的机器学习方法，用于车辆空气动力学压力和壁面剪切应力预测。GTF-Net通过共享多层感知机（MLP）和光滑双线性光栅化，直接从采样表面点构建三平面特征；随后由结合自适应傅里叶神经算子（AFNO）谱混合与卷积神经网络（CNN）细化的双流骨干网络进行处理，从而在同一表示中对远程空气动力学耦合和局部几何引起的变差进行建模。在查询阶段，采样的三平面特征与车辆对齐方向坐标、法向投影特征以及基于体素的曲率代理相结合。将GTF-Net与Transolver、几何信息神经算子（GINO）及基于三平面的代理模型TripNet进行对比。GTF-Net将压力预测的相对L2误差从最强基线值0.157提升至0.145，将壁面剪切应力预测的相对L2误差从0.237提升至0.226。消融实验结果表明，AFNO谱混合、局部CNN细化以及查询侧几何编码均对精度有所贡献，验证了所提出的将结构化三平面表示与显式空气动力学几何线索相结合的机制的有效性。

</details>


### [290] [Cutting LLM Evaluation Costs with SySRs: A Bandit Algorithm that Provably Exploits Model Similarity](https://arxiv.org/abs/2606.07726)
*Zifan Lyu,Chahine Nejma,Tobias Wegel,Fanny Yang,Florian E. Dorner*

Main category: cs.LG

TL;DR: 提出了一种名为Synchronized Successive Rejects (SySR)的算法，通过配对比较增强经典Successive Rejects算法，在无需超参数的情况下，利用模型间的相似性来高效识别最优大语言模型，显著降低评估成本。

<details>
  <summary>Details</summary>

Motivation: 传统上对所有模型在所有测试查询上逐项评估的方式非常浪费，尤其是当某个模型明显劣于其他模型时，无需精确估计其性能。同时，大语言模型对相同提示的响应常具有相似性，但以往工作未能充分利用这一属性。

Method: 提出Synchronized Successive Rejects (SySR)算法，在经典Successive Rejects算法基础上引入配对比较（paired comparisons）机制，通过自适应地分配评估预算，利用模型间的相似性来加速最优模型的识别过程，且无需超参数调优。

Result: 在15个标准基准测试上，SySR方法在平均错误率方面优于所有基线方法；在可靠识别最优模型所需的最坏情况预算方面也表现最佳。

Conclusion: 所提出的SySR方法能有效利用模型间相似性进行自适应评估，在无需超参数的前提下提供性能保证，且相似度越高效果越好，为高效评估和选择最优大语言模型提供了实用方案。

Original Abstract: Large Language Models are typically benchmarked by evaluating every model on every test query. For practitioners seeking the best model to deploy, this is often wasteful: if a model clearly performs worse than others, there is no need to precisely estimate its performance. Best-arm identification algorithms can be naturally applied to drastically reduce costs by adaptively allocating evaluation budget. Further, language models often respond similarly to the same prompt-a property previous work has tried to leverage with mixed success. We propose Synchronized Successive Rejects (SySRs), augmenting the classical Successive Rejects algorithm with paired comparisons. Unlike prior attempts to leverage model similarity in best-model identification, our approach is hyperparameter-free and enjoys performance guarantees that improve with the degree of similarity between evaluated models. Empirically, our method outperforms all baselines in terms of average error rate across 15 standard benchmarks, and in terms of worst-case budget for reliably identifying the best model.

中文摘要翻译: 大语言模型通常通过在每个测试查询上评估每个模型来进行基准测试。对于寻求部署最佳模型的从业者来说，这往往是浪费的：如果某个模型明显表现劣于其他模型，则无需精确估计其性能。最佳臂识别算法可以通过自适应分配评估预算来大幅降低成本。此外，语言模型对相同提示的响应往往具有相似性——以往工作曾尝试利用这一属性但效果参差不齐。我们提出了同步淘汰算法（Synchronized Successive Rejects, SySR），在经典Successive Rejects算法基础上引入了配对比较机制。与先前在最佳模型识别中利用模型相似性的尝试不同，我们的方法无需超参数，并且其性能保证随被评估模型之间的相似度提高而提升。实验结果表明，在15个标准基准测试上，我们的方法在平均错误率方面优于所有基线方法，并且在可靠识别最优模型所需的最坏情况预算方面也表现最佳。

</details>


### [291] [Characterizing the Discrete Geometry of ReLU Networks](https://arxiv.org/abs/2606.07728)
*Blake B. Gaines,Jinbo Bi*

Main category: cs.LG

TL;DR: 本文证明了全连接ReLU网络的分区复杂性的两个新理论结果：区域连接图的平均度上界为输入维度的两倍，且图直径的上界与输入维度无关，尽管区域数量随输入维度指数增长。

<details>
  <summary>Details</summary>

Motivation: ReLU网络定义的分段线性区域如何相互连接（即区域复形的几何结构）对网络行为至关重要，但现有研究仅局限于区域数量的界限，对区域间连接几何的理解非常有限，且精确计算区域复形对于大多数网络而言不可行。

Method: 从理论上证明了全连接ReLU网络的区域连接图的两个上界：（1）图的平均度上界为输入维度的两倍，与网络宽度和深度无关；（2）图的直径上界不依赖于输入维度。并通过合成数据与真实数据训练的网络进行实验验证。

Result: 理论结果表明，无论网络宽度和深度如何，区域连接图的平均度不超过输入维度的两倍；图的直径存在一个与输入维度无关的上界，尽管区域数量随输入维度指数增长。实验结果支持了这些理论发现。

Conclusion: 全连接ReLU网络的区域连接图具有与输入维度弱相关的拓扑性质（平均度受限、直径与维度无关），这为理解ReLU网络的几何结构提供了新的理论洞见。

Original Abstract: It is well established that ReLU networks define continuous piecewise-linear functions, and that their linear regions are polyhedra in the input space. These regions form a complex that fully partitions the input space. The way these regions fit together is fundamental to the behavior of the network, as nonlinearities occur only at the boundaries where these regions connect. However, relatively little is known about the geometry of these complexes beyond bounds on the total number of regions, and calculating the complex exactly is intractable for most networks. In this work, we prove new theoretical results about these complexes that hold for all fully-connected ReLU networks, specifically about their connectivity graphs in which nodes correspond to regions and edges exist between each pair of regions connected by a face. We find that the average degree of this graph is upper bounded by twice the input dimension regardless of the width and depth of the network, and that the diameter of this graph has an upper bound that does not depend on input dimension, despite the number of regions increasing exponentially with input dimension. We corroborate our findings through experiments with networks trained on both synthetic and real-world data, which provide additional insight into the geometry of ReLU networks. Code to reproduce our results can be found at https://github.com/bl-ake/ICLR-2026.

中文摘要翻译: 众所周知，ReLU网络定义连续分段线性函数，其线性区域是输入空间中的多面体。这些区域形成一个完全划分输入空间的复形。这些区域的组合方式对于网络行为至关重要，因为非线性仅出现在这些区域连接的边界处。然而，除了区域总数的界限外，我们对这些复形的几何结构知之甚少，且对于大多数网络而言，精确计算复形是不可行的。在这项工作中，我们证明了关于这些复形的新的理论结果，这些结果适用于所有全连接ReLU网络，特别是关于它们的连接图——其中节点对应区域，边对应由面连接的区域对。我们发现，无论网络的宽度和深度如何，该图的平均度上界均为输入维度的两倍，并且该图的直径存在一个不依赖于输入维度的上界，尽管区域数量随输入维度指数增长。我们通过在合成数据和真实数据上训练的网络进行实验来证实我们的发现，这些实验为ReLU网络的几何结构提供了额外的洞见。重现我们结果的代码可在https://github.com/bl-ake/ICLR-2026获取。

</details>


### [292] [scCBGM: Interpretable Single-Cell Counterfactual Editing](https://arxiv.org/abs/2606.07760)
*Alma Andersson,Aya Abdelsalam Ismail,Edward De Brouwer,Doron Haviv,Tommaso Biancalani,Kyunghyun Cho,Gabriele Scalia,Aïcha BenTaieb,Hector Corrada Bravo*

Main category: cs.LG

TL;DR: 本文提出scCBGM框架，通过概念瓶颈架构和流匹配模型实现单细胞数据的可解释反事实编辑，并在合成和真实数据集上验证了其组合泛化与反事实预测的优越性能。

<details>
  <summary>Details</summary>

Motivation: 理解细胞表型及其对扰动的响应对于疾病生物学和治疗设计至关重要，但单细胞RNA测序面临的组合条件空间使得穷举实验映射不可行，因此需要可解释且精确的计算方法来实现单细胞水平的反事实编辑。

Method: scCBGM框架结合了概念瓶颈架构与解码器跳跃连接及交叉协方差惩罚项以促进解耦学习，并扩展至流匹配模型，支持编码-解码和生成两种模式下的概念引导编辑。

Result: 在多个真实数据集上，scCBGM在组合泛化和反事实预测方面表现优越，并通过合成数据上的细胞级验证和真实数据集上的群体级基准测试提供了有力支持。

Conclusion: scCBGM为单细胞数据的可解释反事实编辑提供了一种有效且精确的框架，有望推动扰动响应预测和细胞表型理解的研究。

Original Abstract: Understanding cellular phenotypes and how they respond to perturbations is critical for disease biology and therapeutic design. Single-cell RNA sequencing enables characterization at cellular resolution, yet the combinatorial space of conditions makes exhaustive experimental mapping infeasible. We introduce single-cell Concept Bottleneck Generative Models (scCBGM), a framework for interpretable and precise counterfactual editing of individual cells. scCBGM adapts concept bottleneck architectures for single-cell data through decoder skip connections and a cross-covariance penalty that promotes disentanglement without dimensional constraints. We extend the framework to flow matching models, enabling concept-guided editing in both encoding-decoding and generation regimes. To enable rigorous evaluation, we develop a synthetic benchmark with ground-truth counterfactuals. Across multiple real datasets, scCBGM demonstrates superior performance in combinatorial generalization and counterfactual prediction, supported by cell-level validation on synthetic data and population-level benchmarks on real datasets.

中文摘要翻译: 理解细胞表型及其如何响应扰动对于疾病生物学和治疗设计至关重要。单细胞RNA测序能够在细胞分辨率下进行表征，但条件组合空间使得穷举实验映射不可行。我们提出了单细胞概念瓶颈生成模型（scCBGM），这是一个用于对单个细胞进行可解释且精确的反事实编辑的框架。scCBGM通过解码器跳跃连接和交叉协方差惩罚项将概念瓶颈架构适配到单细胞数据中，在无维度约束条件下促进解耦学习。我们将该框架扩展到流匹配模型，支持在编码-解码和生成两种模式下进行概念引导编辑。为了进行严格评估，我们构建了带有真实反事实标签的合成基准数据集。在多个真实数据集上，scCBGM在组合泛化和反事实预测方面表现出优越性能，并通过合成数据上的细胞级验证和真实数据集上的群体级基准测试提供了有力支持。

</details>


### [293] [Contrast encodes inductive bias: separating slow noise from dynamics in predictive representation learning](https://arxiv.org/abs/2606.07770)
*Paarth Gulati,Ilya Nemenman*

Main category: cs.LG

TL;DR: 本文揭示了自监督动态表示学习方法（如JEPA）在隐空间中学习表示和预测动力学时，会混淆缓慢变化的噪声与真实动态信号，并提出通过在单个轨迹内采样负样本来消除这一预测捷径，从而迫使编码器聚焦于真正的动力学变量。

<details>
  <summary>Details</summary>

Motivation: 现有自监督方法（如JEPA）在隐空间学习动态表示时，会错误地将轨迹内缓慢变化的噪声编码为动态信号，导致表示被轨迹特定噪声主导，下游性能随噪声增强而退化，且增加训练轨迹数量和时长也无法改善。

Method: 提出将负样本从跨轨迹采样改为在单条轨迹内采样（within-trajectory negative sampling），使缓慢噪声无法区分同一轨迹内的不同帧，从而消除预测捷径；同时训练一个编码器处理多条轨迹，迫使其编码与动力学真正相关的变量。

Result: 在合成运动点数据集（SimCLR风格JEPA）和刚体摆视频数据集（DySIB方法）上验证：跨轨迹采样时噪声主导表示且性能退化；改为轨迹内采样后，即使强噪声下，更长轨迹也能带来更好的表示。

Conclusion: 本文为动态表示学习中的对比预测目标设计提供了原则性指导，尤其适用于存在噪声观测的物理系统。

Original Abstract: Self-supervised methods that learn representations and predict dynamics fully in the latent space, such as JEPA, have been shown to confuse slowly varying noise with the dynamical signals they aim to capture. Specifically, when noise features remain approximately constant within each trajectory, contrastive predictive objectives preferentially encode these features instead of the true latent variables governing the system. The learned representation then becomes dominated by trajectory-specific noise, so downstream performance degrades with noise strength and does not improve even as the number and duration of training trajectories increase. We argue that this failure is a property of the objective itself, shared by a long line of contrastive predictive objectives that sample negatives across trajectories. To illustrate this generality, we study the failure mode and its remedy in two settings: a standard SimCLR-style JEPA on a synthetic moving-dot dataset, and DySIB, a recently introduced method designed for extracting physically interpretable representations of dynamics, on movies of a rigid-body pendulum. When negatives are instead sampled within a single trajectory, the slow noise can no longer distinguish frames within that trajectory, removing the predictive shortcut. Training one encoder simultaneously on many such trajectories then forces it to encode the variables relevant for the dynamics, with longer trajectories yielding better representations even for strong slow noise. Our results point toward principles for designing contrastive predictive objectives in dynamical representation learning, especially for physical systems with noisy experimental observations.

中文摘要翻译: 自监督方法在隐空间中学习表示并预测动力学（如JEPA）已被证明会将缓慢变化的噪声与旨在捕捉的动态信号混淆。具体而言，当噪声特征在每个轨迹内近似保持不变时，对比预测目标会优先编码这些噪声特征，而非控制系统的真实隐变量。学习到的表示因此被轨迹特定的噪声主导，下游性能随噪声强度增加而退化，且即使增加训练轨迹的数量和时长也无法改善。我们认为这一失败是目标函数本身的特性，广泛存在于一系列跨轨迹采样负样本的对比预测目标中。为说明这一普遍性，我们在两个设置中研究了该失败模式及其补救措施：标准SimCLR风格的JEPA在合成运动点数据集上，以及DySIB——一种最近提出的用于提取动力学可解释物理表示的方法——在刚体摆视频上。当改为在单条轨迹内采样负样本时，缓慢噪声无法区分该轨迹内的帧，从而消除了预测捷径。同时训练一个编码器处理多条这样的轨迹，迫使其编码与动力学相关的变量，即使存在强缓慢噪声，更长的轨迹也能产生更好的表示。我们的结果为动态表示学习中的对比预测目标设计指明了原则，尤其适用于存在噪声观测的物理系统。

</details>


### [294] [A Framework for Evaluating and Benchmarking Concept Drift Detection Methods](https://arxiv.org/abs/2606.07789)
*Vitor Cerqueira,Heitor Murilo Gomes,Marco Heyden,Bernhard Pfahringer,Albert Bifet*

Main category: cs.LG

TL;DR: 本文针对数据流挖掘中概念漂移检测方法评估不统一的问题，提出了一个包含受控漂移模拟、时序感知评估指标和留一数据集超参数优化协议的基准框架，并在14种主流方法上进行了系统实验。

<details>
  <summary>Details</summary>

Motivation: 当前概念漂移检测领域的研究进展受限于不一致的评估实践：研究依赖过于简化的合成数据生成器、采用不兼容的评估指标、超参数选择缺乏透明度，导致公平比较困难。

Method: (1) 通过蒙特卡洛试验向真实数据集注入受控分布变化的漂移模拟方法；(2) 提出时序感知的评估协议，包括F1检测分数和归一化检测时间等新指标；(3) 倡导留一数据集超参数优化协议以提升跨异构流动态的配置鲁棒性。在7个真实数据集上对14种漂移检测方法进行了4种漂移类型（类先验、标签交换、特征置换、特征过滤）的评估。

Result: 在7个真实数据集上对14种漂移检测方法进行了系统基准测试，覆盖4种漂移类型（类先验、标签交换、特征置换、特征过滤）的突变和渐变两种模式，揭示了现有方法的优劣势并建立了基线性能指标。

Conclusion: 该基准框架解决了评估不一致问题，为未来概念漂移检测研究提供了可复现的评估标准和基线性能指标，所有代码和实验均已公开。

Original Abstract: Data stream mining is fundamentally challenged by concept drift, where distributional changes can degrade model performance. Despite the proliferation of drift detection methods, progress in the field is hindered by inconsistent evaluation practices: studies rely on oversimplified synthetic data generators, adopt incompatible metrics, and lack transparency in hyperparameter selection, making fair comparisons difficult. We address this gap with a novel benchmarking framework comprising three contributions: (1) a drift simulation method that injects controlled distributional changes into real-world datasets via Monte Carlo trials, enabling supervised evaluation while preserving real-world data complexity; (2) an evaluation protocol for drift detection with timing-aware criteria, including the derivation of new metrics (e.g., F1 detection score, normalized detection time) that are comparable across streams; and (3) we advocate for a leave-one-dataset-out hyperparameter optimization protocol for drift detection methods that promotes configuration robustness across heterogeneous stream dynamics. We benchmark 14 widely used drift detection methods on 7 realworld datasets across 4 drift types (class prior, label swap, feature permutation, feature filtering), each under both abrupt and gradual transitions. Our experimental results provide insights into the strengths and weaknesses of current drift detection approaches while establishing baseline performance metrics for future research in this area. All code and experiments are publicly available.

中文摘要翻译: 数据流挖掘面临的根本性挑战是概念漂移，即分布变化可能导致模型性能下降。尽管漂移检测方法层出不穷，但该领域的进展受到评估实践不一致的阻碍：研究依赖于过度简化的合成数据生成器、采用不兼容的评估指标，且超参数选择缺乏透明度，使得公平比较变得困难。我们通过一个新颖的基准框架来解决这一空白，该框架包含三项贡献：(1) 一种漂移模拟方法，通过蒙特卡洛试验向真实数据集注入受控分布变化，在保留真实数据复杂性的同时实现监督式评估；(2) 一个带有时序感知准则的漂移检测评估协议，包括推导可在不同数据流间进行比较的新指标（如F1检测分数、归一化检测时间）；(3) 我们倡导对漂移检测方法采用留一数据集超参数优化协议，以提升配置在异构流动态上的鲁棒性。我们在7个真实数据集上对14种广泛使用的漂移检测方法进行了基准测试，覆盖4种漂移类型（类先验、标签交换、特征置换、特征过滤），每种漂移类型均包含突变和渐变两种模式。我们的实验结果揭示了当前漂移检测方法的优劣势，同时为该领域的未来研究建立了基线性能指标。所有代码和实验均已公开。

</details>


### [295] [Byzantine Cheap Talk: Adversarial Resilience and Topology Effects in LLM Coordination Games](https://arxiv.org/abs/2606.07790)
*Aya El Mir,Martin Takáč,Salem Lahlou*

Main category: cs.LG

TL;DR: 本文研究了多智能体LLM系统在对抗性攻击和通信拓扑约束下的鲁棒性问题，通过4人Stag Hunt博弈实验发现：拜占庭攻击虽能被快速检测但无法恢复协调；显式限制通信拓扑会破坏合作，而静默施加相同限制则保持合作，表明协调失败源于智能体对隐藏信息的元推理。识别出两种稳定行为类型：背叛倾向型和合作坚持型。

<details>
  <summary>Details</summary>

Motivation: 现有工作表明廉价通信信道能使LLM在协调博弈中达成合作，但其在对抗性攻击和结构性约束下的鲁棒性尚不清楚。本文旨在揭示多智能体LLM通信系统的安全漏洞。

Method: 在4人Stag Hunt博弈中，跨6个模型家族和720次试验，系统性研究两类脆弱性：(1)拜占庭攻击场景——部分智能体假装合作但实际背叛；(2)通信拓扑限制——显式告知vs静默施加相同的通信限制。

Result: (1)非拜占庭智能体能在一轮内检测到背叛，但因博弈的一致同意收益结构而无法集体适应；(2)显式限制通信拓扑会破坏合作，但静默施加相同限制则保持近乎完美的合作；(3)识别出两种稳定的行为原型：背叛倾向型（被背叛后永久切换策略）和合作坚持型（即使付出显著个体代价仍继续合作）。

Conclusion: 通信信道可被利用为对抗性注入向量；向智能体披露网络拓扑结构即使在没有对手的情况下也会削弱协调能力。

Original Abstract: Multi-agent LLM systems increasingly rely on communication protocols for coordination, yet their robustness under adversarial and structural constraints remains poorly understood. Building on prior work showing that cheap-talk channels enable cooperation in LLM coordination games, we investigate two vulnerability classes in a 4-player Stag Hunt across six model families and 720 trials. First, when Byzantine agents signal cooperation but defect, non-Byzantine agents detect the betrayal within one round yet fail to adapt collectively: a substantial fraction continue cooperating despite repeated exploitation, unable to recover coordination due to the game's unanimity payoff structure. Second, explicitly restricting communication topology collapses cooperation, while applying identical restrictions silently preserves near-perfect cooperation. This establishes that coordination failure stems from agents' meta-reasoning about hidden information, not information loss itself. We identify two stable behavioral archetypes that replicate across all model cohorts: Defection-Prone models that switch permanently after betrayal, and Cooperation-Persistent models that continue cooperating at significant individual cost. These findings reveal concrete security vulnerabilities: communication channels can be exploited as adversarial injection vectors, and disclosing network topology to agents can degrade coordination even without any adversary present.

中文摘要翻译: 多智能体大语言模型系统日益依赖通信协议进行协调，但它们在对抗性和结构性约束下的鲁棒性仍知之甚少。基于先前关于廉价通信信道能使LLM在协调博弈中实现合作的工作，我们在6个模型家族和720次试验中，研究了4人Stag Hunt博弈中的两类脆弱性。首先，当拜占庭智能体信号合作但实际背叛时，非拜占庭智能体能在一轮内检测到背叛，但无法集体适应：由于博弈的一致同意收益结构，相当一部分智能体在被反复利用后仍继续合作，无法恢复协调。其次，显式限制通信拓扑会破坏合作，而静默施加相同的限制则保持近乎完美的合作。这表明协调失败源于智能体对隐藏信息的元推理，而非信息损失本身。我们识别出两种在所有模型群体中复现的稳定行为原型：背叛倾向型——在被背叛后永久切换策略，以及合作坚持型——即使付出显著的个体代价仍继续合作。这些发现揭示了具体的安全漏洞：通信信道可被利用为对抗性注入向量，且向智能体披露网络拓扑结构即使在没有对手的情况下也会削弱协调能力。

</details>


### [296] [Mitigating the Contractivity Trap in Diffusion ODEs via Stein Stabilization](https://arxiv.org/abs/2606.07835)
*Shigui Li,Delu Zeng*

Main category: cs.LG

TL;DR: 提出SteinDiff框架，通过Stein导出的几何感知残差校正机制，在不需参考样本的情况下稳定扩散模型大步长推理，解决确定性问题ODE轨迹中的“收缩陷阱”。

<details>
  <summary>Details</summary>

Motivation: 扩散模型在确定性概率流ODE的大步长推理中存在“收缩陷阱”：高效推理需要大步长，但大步长与高表达力去噪器会破坏基于收缩的稳定性保证，导致误差放大。

Method: 提出SteinDiff框架，包含：(1) 几何感知残差校正机制，在推理时正则化大步长求解器更新；(2) 闭式Stein校正系数，实现无需参考样本的局部数据几何自适应；(3) 建立分布偏移下的分数控制扰动界，并提供EDM参数化的Stein视角补充分析。

Result: 实验表明SteinDiff能够缓解大步长推理中的严重伪影，并提升生成质量。

Conclusion: SteinDiff通过推理时的Stein校正有效解决了扩散模型大步长推理中的稳定性问题，无需重训练且无需参考样本，显著改善了生成质量。

Original Abstract: A fundamental tension exists in the large-step inference of diffusion models via their deterministic probability flow ordinary differential equation (PF-ODE) trajectories, which we identify as the contractivity trap: efficient inference favors large step sizes, while aggressive steps and highly expressive denoisers can undermine contraction-based stability certificates for error suppression. To address this, we propose SteinDiff, a step-wise inference-time stabilization framework that employs Stein-derived corrections without requiring reference samples. Specifically, SteinDiff introduces a geometry-aware residual correction mechanism that regularizes large-step solver updates without retraining. To this end, we derive a closed-form Stein correction coefficient for step-wise solver adjustment, enabling reference-free adaptation to local data geometry. We further establish a score-controlled perturbation bound under distributional shifts and provide a complementary Stein perspective on EDM-style parameterizations. Extensive experiments demonstrate that SteinDiff mitigates severe artifacts and improves generative quality across large-step inference settings.

中文摘要翻译: 扩散模型在通过其确定性概率流常微分方程（PF-ODE）轨迹进行大步长推理时存在一个根本性张力，我们将其识别为“收缩陷阱”：高效推理倾向于大步长，而激进的步长与高表达力的去噪器会破坏基于收缩的误差抑制稳定性保证。为解决这一问题，我们提出SteinDiff，一种逐步骤的推理时稳定化框架，该框架采用Stein导出的校正，无需参考样本。具体而言，SteinDiff引入了一种几何感知的残差校正机制，在不重新训练的情况下正则化大步长求解器更新。为此，我们推导了用于逐步骤求解器调整的闭式Stein校正系数，实现了对局部数据几何的无参考自适应。我们进一步建立了分布偏移下的分数控制扰动界，并提供了对EDM风格参数化的补充性Stein视角。大量实验表明，SteinDiff在多种大步长推理设置下缓解了严重伪影，并提升了生成质量。

</details>


### [297] [Teacher-Free Self-Training Amplifies but Does Not Compound: A Pass@$K$ Crossover on a Free-Verifier Domain](https://arxiv.org/abs/2606.07856)
*Igor Lima Strozzi*

Main category: cs.LG

TL;DR: 该论文通过一个无需教师信号的“星座”实验框架（生成器+学习型评判器+精确验证器），在FlashFill风格的陷阱DSL上研究语言模型自训练（STaR）的本质：是获得新能力（涌现）还是仅放大已有能力（放大）。结果表明自训练只是集中概率质量而非扩展能力范围，属于放大而非涌现。

<details>
  <summary>Details</summary>

Motivation: 澄清语言模型在自身验证输出上训练时，究竟是获得了超越基础模型的新能力，还是仅仅更好地表达了基础模型已有的能力。该问题在传统设定下难以判定，作者希望设计一个可判定的实验框架来解决这一问题。

Method: 设计了一个无需教师信号的“星座”框架，包含生成器、学习型评判器和精确验证器，运行在FlashFill风格的“陷阱”DSL上。该框架中验证过的(问题, 解)对易于合成、难以逆向、可免费精确检查。全部实验仅用单张24GB GPU上运行的4-bit量化Qwen3-4B模型，无任何更大模型参与循环。

Result: (i) 评判器引导的选择比验证器过滤的best-of-k高出+9.1个百分点（6/6种子），增益集中在候选解在留存输入上存在分歧的任务中；(ii) 每轮STaR自训练提高上限但从不加速，增益与剩余空间相关并在K=4条独立训练轨迹上呈减速趋势；(iii) 该领域没有清晰的零能力边界，传统“0%→上升=涌现”测试无效。通过pass@K交叉点诊断：训练模型在操作预算（pass@8）胜出，但基础模型在大预算（pass@64）反超所有轨迹，表明自训练集中概率质量而非扩展能力范围。

Conclusion: 自训练的效果是放大（amplification）而非复合涌现（compounding），即只是将概率质量集中在已有能力上，而非创造新能力。

Original Abstract: When a language model trains on its own verified outputs, does it acquire capability beyond its base, or merely get better at expressing capability the base already had? We make the question decidable with a teacher-free "constellation" -- a generator, a learned critic, and a free exact verifier -- on a FlashFill-style "trapdoor" DSL, where verified (problem, solution) pairs are cheap to synthesize, hard to invert, and free to check exactly. Everything runs on one 4-bit Qwen3-4B on a single 24 GB GPU, with no model in the loop larger than the base. We report three findings. (i) Critic-guided selection beats verifier-filtered best-of-$k$ by $+9.1$ pp ($6/6$ seeds), with the entire gain localized to tasks where candidates disagree on held-out inputs. (ii) Per-round STaR self-training raises the ceiling but never accelerates -- the gain tracks remaining headroom and decelerates across $K=4$ independent training trajectories. (iii) The domain has no clean zero-capability frontier, so the usual "$0\% \to$ climb $=$ emergence" test is invalid here. A measured pass@$K$ crossover settles the diagnosis: the trained model wins at the operating budget (pass@$8$) but the base overtakes it at a large budget (pass@$64$) on every trajectory, so self-training concentrates probability mass rather than expanding reach. This is amplification, not compounding. ($K=4$ is indicative, not yet a robust across-trajectory CI.)

中文摘要翻译: 当语言模型在其自身验证过的输出上训练时，它是获得了超越基础模型的能力，还是仅仅更好地表达了基础模型已有的能力？我们通过一个无需教师信号的"星座"框架——包含一个生成器、一个学习型评判器和一个免费的精确验证器——在FlashFill风格的"陷阱"DSL上使该问题变得可判定。在该DSL中，验证过的（问题，解）对易于合成、难以逆向、可免费精确检查。全部实验仅运行在单张24 GB GPU上的4-bit量化Qwen3-4B模型上，循环中没有任何大于基础模型的模型参与。我们报告三项发现。(i) 评判器引导的选择比验证器过滤的best-of-k高出+9.1个百分点（6/6种子），全部增益集中在候选解在留存输入上存在分歧的任务中。(ii) 每轮STaR自训练提高了能力上限但从未加速——增益与剩余空间正相关，并在K=4条独立训练轨迹上呈减速趋势。(iii) 该领域没有清晰的零能力边界，因此传统的"0%→上升=涌现"测试在此无效。一个测得的pass@K交叉点给出了诊断：训练模型在运行预算（pass@8）下胜出，但基础模型在大预算（pass@64）下在所有轨迹上反超。因此，自训练集中了概率质量而非扩展了能力范围。这是放大，而非复合涌现。（K=4为指示性结果，尚非稳健的跨轨迹置信区间。）

</details>


### [298] [Instrumented data for causal scientific machine learning](https://arxiv.org/abs/2606.07865)
*Daniel N. Wilke*

Main category: cs.LG

TL;DR: 本文提出“仪器化数据”（instrumented data）作为科学机器学习中观测数据和模板合成数据之外的第三种数据范式，每个数据点携带生成它的机制模型、显式不确定性及可执行的反事实推理族，并通过V&V仪器化图像到仿真管线实现，为验证、审计和代理模型训练提供新路径。

<details>
  <summary>Details</summary>

Motivation: 观测数据记录“发生了什么”但无法解释因果机制；模板合成数据虽生成过程已知，却仅适用于模拟器的模板而非用户面对的具体案例。现有数据范式存在根本性局限，亟需一种既能保留机理信息又支持因果干预的新数据形态。

Method: 提出“仪器化数据”概念，每个数据点携带：(1)生成它的机制模型；(2)该模型的显式不确定性（偶然+认知）；(3)可执行的反事实推理族。通过验证与确认（V&V）仪器化图像到仿真管线实现：传感器观测转化为完全指定、求解器支持的仿真模型，具有显式可编辑参数和传播的不确定性估计。

Result: 该方法适用于计算生物学、气候、材料、流体力学和医学影像等多个领域的验证、审计和代理模型训练；长期可证伪的启示涉及科学推理的基础模型。

Conclusion: 仪器化数据在操作层面已可行，为科学机器学习提供第三种数据范式，兼顾案例特异性、机械论监督和因果干预能力，有望改变科学计算中的数据生成与使用方式。

Original Abstract: Scientific machine learning is limited less by model size than by the data it is trained on. Observational data records what happened but not why; template synthetic data has a known generating process but only for the simulator's template, not the case a user faces. We argue a third option is now operationally feasible: instrumented data, in which every datum carries the mechanistic model that produced it, an explicit uncertainty over that model, and an executable family of counterfactuals. Verification-and-validation (V&V) instrumented image-to-simulation pipelines are one realisation: a sensor observation becomes a fully specified, solver-backed simulation with explicit, editable parameters and a propagated aleatoric/epistemic uncertainty. The substrate is case-specific, mechanistically supervised, and supports causal interventions through Pearl's do-operator. Near-term consequences for validation, auditing, and surrogate training span computational biology, climate, materials, fluid mechanics, and medical imaging; a longer-term, falsifiable implication concerns foundation models for scientific reasoning.

中文摘要翻译: 科学机器学习的限制更多地来自训练数据而非模型规模。观测数据记录了发生的事实但无法揭示原因；模板合成数据虽有已知的生成过程，但这仅适用于模拟器的模板，而非用户面对的具体案例。我们论证，第三种选择在操作层面现已可行：仪器化数据，其中每个数据点都携带着生成它的机制模型、该模型的显式不确定性，以及一个可执行的反事实推理族。验证与确认（V&V）仪器化图像到仿真管线是其中一种实现方式：传感器观测转化为完全指定的、求解器支持的仿真模型，具有显式可编辑的参数和传播的偶然性/认知性不确定性。其基底是案例特异性的、机械论监督的，并通过Pearl的do算子支持因果干预。该方法在验证、审计和代理模型训练方面的近期应用涵盖计算生物学、气候、材料、流体力学和医学影像；其长期的可证伪启示则涉及科学推理的基础模型。

</details>


### [299] [Still: Amortized KV Cache Compaction in a Single Forward Pass](https://arxiv.org/abs/2606.07878)
*Charles O'Neill,Alex Sandomirsky,Harry Partridge,Mudith Jayasekara,Max Kirkby*

Main category: cs.LG

TL;DR: Still 是一种轻量级的每层 Perceiver 压缩器，通过单次前向传播为 KV 缓存生成紧凑的键和值，在长上下文场景下实现了速度与质量的最佳平衡，支持 8 倍到 200 倍压缩率，并在长序列任务中显著超越现有基线。

<details>
  <summary>Details</summary>

Motivation: 现有的 KV 缓存压缩方法无法同时满足轻量级、高表达力和可复用三个部署需求：选择方法虽轻量但受限于子集选择，合成方法表达力强但依赖每个上下文的独立优化，计算开销大且无法跨轨迹复用。因此需要一种可训练一次、前向传播即可完成压缩的方法。

Method: 提出 Still，一种小型的每层 Perceiver 网络，在冻结的基础模型上训练一次后，可通过单次前向传播为每层生成紧凑的键和值（KV 缓存）。由于压缩仅需一次前向传播，因此可以迭代应用，支持长时域场景下的缓存压缩。

Result: 在 Qwen 和 Gemma 模型上，Still 在 8 倍到 200 倍压缩率、8k 到 128k 上下文长度下均占据速度-质量帕累托前沿的有利位置。在长上下文 RULER 评估中，Still 超出最强基线 8-22 分。压缩后的缓存还支持自由格式摘要，在 HELMET 上保留了大部分全上下文增益，并在 LongBench 摘要对比中优于 KV-Distill。

Conclusion: 摊销机制使得长上下文缓存压缩变得可行，而合成方法使得压缩后的状态在极端压缩比下仍然有效。Still 证明了通过单次训练的轻量合成压缩器可以同时满足部署的三大需求，并开启了对每上下文方法不可用的长时域推理新范式。

Original Abstract: The KV cache is the memory bottleneck of long-horizon language model deployment. Practically, a deployable compactor must be lightweight enough to call during inference, expressive enough to preserve context under constraint, and reusable across a trajectory. Existing compaction methods satisfy only part of this requirement: selection methods are lightweight but subset-bound, while synthesis methods are expressive but rely on per-context optimization. Here we introduce Still, a small per-layer Perceiver trained once against a frozen base model that produces compact keys and values in a single forward pass. On Qwen and Gemma models, Still occupies the favorable side of the speed--quality frontier across compression ratios from $8\times$ to $200\times$ and context lengths from $8$k to $128$k. On the long-context RULER grid, Still exceeds the strongest baseline by 8--22 points. The same compact cache also supports free-form summarization, preserving most of the full-context gain on HELMET and winning a pairwise LongBench summarization comparison against KV-Distill. Because compaction is a forward pass, Still can be applied iteratively, entering a long-horizon regime unavailable to per-context methods. We show that amortization makes long-context cache compaction tractable, and synthesis makes its compact state useful at extreme compression.

中文摘要翻译: KV 缓存是长时域语言模型部署中的内存瓶颈。在实践中，一个可部署的压缩器必须足够轻量以便在推理时调用，足够有表现力以在约束下保留上下文，并且可跨轨迹复用。现有的压缩方法仅满足部分需求：选择方法轻量但受限于子集选择，而合成方法有表现力但依赖每上下文的优化。本文提出 Still——一种小型每层 Perceiver，在冻结的基础模型上训练一次，即可通过单次前向传播生成紧凑的键和值。在 Qwen 和 Gemma 模型上，Still 在 8 倍到 200 倍压缩比以及 8k 到 128k 上下文长度范围内占据了速度-质量前沿的有利位置。在长上下文 RULER 评估网格中，Still 超出最强基线 8-22 分。同样的紧凑缓存还支持自由格式摘要，在 HELMET 上保留了大部分全上下文增益，并在与 KV-Distill 的成对 LongBench 摘要对比中胜出。由于压缩仅需一次前向传播，Still 可以迭代应用，进入每上下文方法无法实现的长时域场景。我们表明，摊销机制使得长上下文缓存压缩变得可行，而合成方法使其压缩状态在极端压缩下依然有效。

</details>


### [300] [Breaking the Bubble: Asynchronous Pipeline Parallel Training with Bounded Weight Inconsistency](https://arxiv.org/abs/2606.07881)
*Itay Elam,Eliron Rahimi,Avi Mendelson,Chaim Baskin*

Main category: cs.LG

TL;DR: PACI是一种无气泡的异步流水线并行训练方法，通过局部梯度累积控制参数版本漂移，无需权重暂存或全局同步，在GPT风格语言模型预训练中实现了1.69倍的训练加速。

<details>
  <summary>Details</summary>

Motivation: 现有的同步流水线存在气泡问题导致吞吐量下降，而异步流水线虽消除气泡但引入权重版本不一致问题，通常需要复杂的权重暂存、预测或纠正机制，增加了实现复杂度和资源开销。

Method: PACI的核心思想是将局部梯度累积作为一种版本控制机制：通过减缓参数版本演化速度（使其慢于流水线延迟），限制任何微批次所跨越的优化器更新次数，从而在保证稳态利用率的同时约束前向/反向版本漂移。

Result: 在GPT风格语言模型预训练中，PACI匹配了同步1F1B-flush的稳定性和最终困惑度，保留了相同的峰值内存占用，实现了完全利用的流水线吞吐量，训练精度时间比最快flush基线提升了1.69倍。

Conclusion: 前向/反向不一致性无需被消除：当被显式约束时，可以安全地将其换取显著的效率提升。

Original Abstract: Pipeline parallelism is essential for training large neural networks, but existing schedules trade off throughput, memory, and optimization consistency. Synchronous pipelines preserve forward/backward weight consistency but suffer from bubbles; asynchronous pipelines remove bubbles but introduce weight-version mismatch, typically requiring weight stashing, prediction, or correction mechanisms. We introduce PACI (Pipeline Asynchronous training with Controlled Inconsistency), a bubble-free asynchronous pipeline method that bounds forward/backward version drift without weight stashing, prediction, additional parameter copies, or global synchronization. The key idea is to use local gradient accumulation as a version-control mechanism: by slowing parameter-version evolution relative to pipeline delay, PACI limits the number of optimizer updates crossed by any micro-batch while preserving steady-state utilization. In GPT-style language-model pretraining, PACI matches the stability and final perplexity of synchronous 1F1B-flush, retains the same peak memory footprint, achieves fully utilized pipeline throughput, and improves training time-to-accuracy by up to $1.69\times$ over the fastest flush baseline. These results show that forward/backward inconsistency need not be eliminated: when explicitly bounded, it can be safely traded for substantial efficiency gains.

中文摘要翻译: 流水线并行对于训练大型神经网络至关重要，但现有调度策略在吞吐量、内存和优化一致性之间需要权衡。同步流水线保持了前向/反向权重的一致性，但存在气泡问题；异步流水线消除了气泡，但引入了权重版本不匹配问题，通常需要权重暂存、预测或纠正机制。我们提出了PACI（受控不一致性的异步流水线训练），一种无气泡的异步流水线方法，该方法在没有权重暂存、预测、额外参数副本或全局同步的情况下，约束前向/反向版本漂移。其关键思想是使用局部梯度累积作为版本控制机制：通过减缓参数版本相对于流水线延迟的演化速度，PACI限制了任何微批次所跨越的优化器更新次数，同时保持稳态利用率。在GPT风格语言模型预训练中，PACI匹配了同步1F1B-flush的稳定性和最终困惑度，保留了相同的峰值内存占用，实现了完全利用的流水线吞吐量，并将训练精度时间比相比最快的flush基线提升了高达1.69倍。这些结果表明，前向/反向不一致性无需被消除：当被显式约束时，可以安全地将其换取显著的效率提升。

</details>


### [301] [Strained Coherence: A Pre-Failure Signal in Coding Agent Execution Trajectories](https://arxiv.org/abs/2606.07889)
*Marut Pandya,Kasey Zhang,Baiqing Lyu*

Main category: cs.LG

TL;DR: 本文提出并定义了"紧张连贯性"（strained coherence）这一LLM编码代理的安全失效模式：代理在推理中识别出问题，明确陈述该信息，却仍然按照错误方式行动。作者构建了基于Claude Sonnet 4.6的检测器，在Terminal-bench-2轨迹上实现了94%的失败预测准确率，并提供了可解释的跨度级输出。

<details>
  <summary>Details</summary>

Motivation: LLM编码代理有时会在推理中意识到自身存在问题，但仍然继续执行错误行为。这种模式尚未被系统定义和检测，构成安全相关的重要失效模式，需要形式化描述和自动化检测方法。

Method: 给出"紧张连贯性"的操作性定义；构建基于Claude Sonnet 4.6的判据器，读取完整轨迹并标记出现该模式的片段；在Qwen3.5-35B-A3B骨干模型上对44条Terminal-bench-2轨迹进行评估，并在Gemma4-31B上进行复现验证。

Result: 被标记轨迹的失败率为94%，未标记轨迹为46%（差距47个百分点，p=0.003）；检测器在匹配选择性条件下达到94%的精确率，优于词汇话语标记基线的88%；两种方法的交集（10条轨迹）失败率为100%。在Gemma4-31B上复现结果方向一致但未达统计显著性（20个百分点差距，p=0.31），主要受限于13条无思维内容的轨迹。首次标记出现在轨迹中位时间83-84%处。

Conclusion: "紧张连贯性"是LLM编码代理中可检测的安全失效模式，所提出的检测器能有效识别代理意识到问题却仍执行错误行为的情况，且输出具有可解释性（包含引用的认知、引用的行动和类型化冲突），揭示了代理看到但忽略了什么信息。

Original Abstract: LLM-based coding agents sometimes acknowledge a problem in their own reasoning and then proceed anyway. We call this pattern strained coherence: a safety-relevant failure mode in which an agent has information that should change its behavior, states that information, and still acts against it. The pattern overlaps with verbalized reward hacking, where an agent names a tension between a task proxy and the underlying goal yet optimizes the proxy anyway. We give an operational definition, build a Claude Sonnet 4.6 judge that reads full trajectories and flags spans where the pattern occurs, and evaluate it on 44 Terminal-bench-2 trajectories using a Qwen3.5-35B-A3B backbone. Flagged trajectories fail 94% of the time versus 46% for unflagged trajectories (47-point gap, Fisher's exact p = 0.003; 46 points after excluding three prompt-embedded examples, p = 0.006). At matched selectivity, the detector reaches 94% precision versus 88% for a lexical discourse-marker baseline; the 10-trajectory intersection of the two methods has a 100% failure rate (Clopper-Pearson 95% CI [69%, 100%]). We replicate on Gemma4-31B with 43 trajectories: the overall signal is directionally consistent but not significant (20-point gap, p = 0.31), with attenuation driven largely by 13 trajectories with zero think content, where the detector has no substrate to analyze. In the high-verbosity Gemma tertile, the gap is +30 points; in the mid- and high-verbosity Qwen tertiles, it is +40 points each. The first flag appears at a median of 83-84% of elapsed trajectory time across both models, and the binary flag survives paraphrases that soften explicit conflict markers (8/8 trajectories). Unlike univariate predictors, the detector emits interpretable span-level output -- quoted acknowledgment, quoted action, and typed conflict -- showing what the agent saw and ignored.

中文摘要翻译: 基于大语言模型的编码代理有时会在推理中意识到自身存在问题，但仍然继续执行。我们将这种模式称为"紧张连贯性"（strained coherence）：一种安全相关的失效模式，即代理拥有应改变其行为的信息、明确陈述了该信息，却仍然违背该信息行动。该模式与言词化奖励黑客行为重叠，后者指代理指出了任务代理指标与底层目标之间的张力，却仍然优化代理指标。我们给出了一个操作性定义，构建了一个读取完整轨迹并标记该模式出现片段的Claude Sonnet 4.6判据器，并使用Qwen3.5-35B-A3B骨干模型在44条Terminal-bench-2轨迹上进行了评估。被标记轨迹的失败率为94%，未标记轨迹为46%（差距47个百分点，Fisher精确检验p=0.003；排除三个提示嵌入示例后差距为46个百分点，p=0.006）。在匹配选择性条件下，检测器达到94%的精确率，而基于词汇话语标记的基线为88%；两种方法的10条轨迹交集失败率为100%（Clopper-Pearson 95%置信区间[69%, 100%]）。我们在Gemma4-31B上使用43条轨迹进行了复现：整体信号方向一致但未达统计显著性（20个百分点差距，p=0.31），衰减主要由13条零思维内容的轨迹驱动——在这些轨迹中检测器没有可分析的基础素材。在Gemma的高语量三分位组中，差距为+30个百分点；在Qwen的中、高语量三分位组中，差距各为+40个百分点。首次标记出现在两个模型的中位轨迹时间83-84%处，且二元标记在软化显式冲突标记的改写中仍能保持（8/8条轨迹）。与单变量预测器不同，检测器输出可解释的跨度级输出——引用的认知、引用的行动和类型化冲突——展示了代理看到并忽略了什么信息。

</details>


### [302] [Partially Performative Prediction](https://arxiv.org/abs/2606.07890)
*Jaewook Lee,Tijana Zrnic*

Main category: cs.LG

TL;DR: 本文提出部分表演性预测（partially performative prediction）框架，统一了数据分布的内生性变化（模型部署引发的反馈）和外源性变化（外部时间演化），并将表演性稳定与最优性扩展到在线环境，分析了重复训练等启发式方法的适应性。

<details>
  <summary>Details</summary>

Motivation: 现有表演性预测仅考虑模型部署引发的内生分布偏移，但实际中分布偏移往往是内源性和外源性因素共同作用的结果。作者旨在提出一个更通用的框架，同时捕捉两类分布偏移。

Method: 提出部分表演性预测框架，将数据分布建模为同时依赖于部署模型和外部时间变量的函数；定义了在线表演性稳定和在线表演性最优性概念；分析了重复训练等学习启发式方法在该环境下的收敛与适应行为。

Result: 定义了在线表演性稳定和最优性的概念，并刻画了重复训练等启发式方法在部分表演性环境中成功适应的条件。

Conclusion: 部分表演性预测框架能够更真实地建模实际部署场景中的分布偏移，为分析和设计适应动态环境的学习算法提供了理论基础。

Original Abstract: Performative prediction studies feedback loops that arise when predictive models are deployed in consequential domains. In these settings, deploying a model can change the population whose patterns the model aims to predict, inducing a distribution shift that is endogenous to the learning system. This perspective departs from classical treatments of distribution shift, where shifts are typically modeled as exogenous changes in the data-generating process. Yet, in practice, distribution shift is rarely one or the other. Predictive models may influence future data through the decisions they support, while the world itself continues to drift for reasons beyond the learner's control. We study partially performative prediction, a framework that captures both endogenous and exogenous sources of distribution shift. The framework generalizes performative prediction by allowing the data distribution to evolve both in response to the deployed model and according to an external, time-varying process. We extend the central notions of performative stability and performative optimality to this setting by defining their online analogues that track the evolving partially performative environment. We analyze practical learning heuristics, including repeated retraining, and characterize when they successfully adapt to partially performative environments.

中文摘要翻译: 表演性预测（performative prediction）研究的是预测模型在被部署到具有后果影响的领域时所产生的反馈循环。在这些场景中，部署模型可能会改变模型旨在预测的对象群体的分布模式，从而引发内生于学习系统的分布偏移。这一视角有别于传统的分布偏移处理方法——后者通常将偏移建模为数据生成过程中的外生变化。然而，在实践中，分布偏移很少是单纯内生或纯粹外生的。预测模型可能通过其支持的决策影响未来数据，同时世界本身也会因学习者无法控制的原因而持续漂移。本文研究部分表演性预测（partially performative prediction），这是一个同时捕捉内源性和外源性分布偏移源的框架。该框架通过允许数据分布既响应于部署的模型，又依据外部时变过程演化，从而推广了表演性预测。我们将表演性稳定（performative stability）和表演性最优性（performative optimality）的核心概念扩展到这一设定中，定义了追踪演化中的部分表演性环境的在线对应概念。我们分析了包括重复训练在内的实用学习启发式方法，并刻画了它们在部分表演性环境中成功适应的条件。

</details>


### [303] [Temporal Coverage over Density: Parsimonious Training-Set Design for ML Climate Downscaling](https://arxiv.org/abs/2606.07898)
*Karandeep Singh,Stefan Rahimi,Chad W. Thackeray,Stephen Cropper,Alex Hall*

Main category: cs.LG

TL;DR: 本文研究了在固定计算预算下，如何选择有限的训练年份来训练机器学习降尺度模型，发现跨整个气候轨迹分布训练年份（广泛采样内部变率）优于仅使用历史年份或首尾年份，为区域气候降尺度和大集合投影提供了实用指导。

<details>
  <summary>Details</summary>

Motivation: 高分辨率区域气候模拟对气候影响评估至关重要，但计算成本高昂，因此需要开发机器学习降尺度器与仿真器。关键挑战在于如何将有限的高分辨率模拟资源合理分配到变化的气候轨迹中，以同时捕捉强迫气候响应和内部变率，从而优化降尺度模型的性能。

Method: 基于CESM2大集合在美国西部的数据，在固定数据预算下比较了三种训练年份选择策略：连续历史年份块、从模拟期首尾抽取的年份、以及分布在整个气候轨迹中的年份。通过评估不同策略下训练模型对未见集合成员变率的复现能力及气候诊断性能来比较优劣。

Result: (1) 同时包含历史和未来年份的训练始终优于仅用历史年份，说明暴露于历史记录之外的气候状态至关重要；(2) 分布在整个气候轨迹中的年份表现最佳，表明广泛采样内部变率能提供额外信息；(3) 即使仅使用十分之一的高分辨率年份，时间分布模型仍与全数据训练高度竞争。

Conclusion: 在固定计算预算下，广泛采样气候状态比时间连续性更有价值。该结论为区域气候降尺度和大型集合投影工作流提供了实用指导。

Original Abstract: High-resolution regional climate simulations provide critical information for climate impacts assessments but remain computationally expensive, motivating the development of machine-learning downscalers and emulators. A key challenge is determining how limited high-resolution simulations should be distributed across a changing climate trajectory to capture both forced climate response and internal variability. Using the CESM2 Large Ensemble over the western United States, we compare three training-year selection strategies under fixed data budgets: a contiguous block of historical years, years drawn from both the beginning and end of the simulation period, and years distributed throughout the full climate trajectory. Including both historical and future years consistently outperforms training on historical years alone, demonstrating the importance of exposing downscaling models to climate states outside the historical record and highlighting limitations of stationarity assumptions common in statistical downscaling. Training on years distributed throughout the full climate trajectory performs best overall, indicating that broad sampling of internal variability provides additional information beyond exposure to the forced climate response alone. Models trained on temporally distributed subsets more successfully reproduce variability in unseen ensemble members while retaining strong performance across a wide range of climate diagnostics. Even when trained on only one-tenth of the available high-resolution years, temporally distributed models remain highly competitive with full-data training. These results suggest that, under fixed computational budgets, broad sampling of climate states is more valuable than temporal continuity when allocating scarce high-resolution simulations. The findings provide practical guidance for regional climate downscaling and large-ensemble projection workflows.

中文摘要翻译: 高分辨率区域气候模拟为气候影响评估提供了关键信息，但其计算成本仍然高昂，这促使了机器学习降尺度器和仿真器的发展。一个关键挑战在于，如何将有限的高分辨率模拟资源分配到一个不断变化的气候轨迹中，以同时捕捉强迫气候响应和内部变率。利用美国西部上空的CESM2大集合，我们在固定数据预算下比较了三种训练年份选择策略：连续的历史年份区块、从模拟期起始和结束年份中抽取的样本，以及分布在整个气候轨迹中的年份。同时包含历史和未来年份的策略始终优于仅基于历史年份的训练，这表明让降尺度模型暴露于历史记录之外的气候状态非常重要，并突显了统计降尺度中常见的平稳性假设的局限性。分布在整个气候轨迹中的年份训练效果整体最佳，表明对内部变率的广泛采样能提供超出仅暴露于强迫气候响应之外的额外信息。基于时间分布子集训练的模型能更成功地在未见集合成员中复现变率，同时在广泛的气候诊断指标中保持强劲性能。即使仅使用可用高分辨率年份的十分之一进行训练，时间分布模型仍与全数据训练结果高度竞争。这些结果表明，在固定计算预算下，分配稀缺的高分辨率模拟资源时，对气候状态的广泛采样比时间连续性更有价值。该发现为区域气候降尺度和大型集合投影工作流提供了实用指导。

</details>


### [304] [Layer-wise Derivative Controlled Networks Achieve Competitive Accuracy and Gradient Stability Across Data Regimes](https://arxiv.org/abs/2606.07908)
*Rowan Martnishn*

Main category: cs.LG

TL;DR: 本文提出基于ChainzRule (CR)的导数控制网络，通过三次多项式层和前向模式逐层雅可比惩罚(DREG)实现层-wise导数控制。实验表明CR在Pima糖尿病数据集和SST-5上均显著优于基线，梯度尾部比率可作为可靠的、无需标签的泛化能力诊断指标。

<details>
  <summary>Details</summary>

Motivation: 探索基于导数控制的网络（CR）在不同数据规模下的泛化性能，验证层-wise导数控制是否能引入结构性的归纳偏置，从而在表格数据、NLP等领域实现鲁棒的泛化。

Method: 基于ChainzRule (CR)的导数控制网络，使用三次多项式层结合轻量级前向模式逐层雅可比惩罚(DREG)进行训练，并消融DREG系数调度形状以确定最优退火范围。

Result: 在Pima糖尿病数据集上，CR在5%到100%训练数据范围内始终优于基线，梯度尾部比率稳定在~1.01-1.02（ReLU网络为1.07-1.09）。在SST-5上，CR在冻结嵌入和BERT微调两种模式下均取得有竞争力或更优的结果，包括在训练数据大幅减少的情况下超越之前的BERT基线。所有结果统计显著（p<0.05）。

Conclusion: 层-wise导数控制诱导了一种偏向低频、稳定表示的结构性归纳偏置，能够在表格数据和NLP领域、不同数据规模和表示质量下实现鲁棒泛化。梯度尾部比率可作为泛化能力的可靠、无需标签的诊断指标。

Original Abstract: Derivative-controlled networks based on ChainzRule (CR) combine cubic polynomial layers with a lightweight forward-mode per-layer Jacobian penalty (DREG). In this second paper of a multi-part series, we evaluate the generalization properties of CR across data regimes.
  We ablate the shape of the DREG coefficient schedule, demonstrating that the optimal annealing range depends on representation noise. On the Pima Diabetes dataset, CR achieves strong low-data performance and maintains a consistent accuracy advantage over baselines from 5\% to 100\% training data, supported by exceptionally stable gradient tail ratios ($\sim$1.01--1.02 vs. 1.07--1.09 for ReLU networks). Extensions to SST-5 show competitive or superior results in both frozen-embedding and BERT fine-tuned regimes, including outperforming prior BERT baselines despite substantially less training data. These results are statistically significant: CR achieves superior accuracy over the strongest published baselines we could identify on both datasets ($p < 0.05$).
  These results establish that layer-wise derivative control induces a structural inductive bias toward low-frequency, stable representations that generalizes robustly across tabular and NLP domains, data volumes, and representation qualities. The gradient tail ratio serves as a reliable, label-free diagnostic of generalization capability.

中文摘要翻译: 基于ChainzRule (CR)的导数控制网络将三次多项式层与轻量级前向模式逐层雅可比惩罚(DREG)相结合。在本多部分系列论文的第二篇中，我们评估了CR在不同数据规模下的泛化性能。我们消融了DREG系数调度的形状，证明最优退火范围取决于表示噪声。在Pima糖尿病数据集上，CR在低数据场景下表现强劲，并在5%到100%训练数据范围内保持一致的准确率优势，这得益于异常稳定的梯度尾部比率（CR约为1.01-1.02，而ReLU网络为1.07-1.09）。扩展到SST-5的结果表明，无论是在冻结嵌入还是BERT微调机制下，CR均表现出有竞争力或更优的结果，包括在训练数据大幅减少的情况下超越先前的BERT基线。这些结果具有统计显著性：CR在两个数据集上均取得了优于我们所能识别的最强已发表基线的准确率（p < 0.05）。这些结果确立了层-wise导数控制会诱导一种偏向低频、稳定表示的结构性归纳偏置，该偏置能够在表格数据和NLP领域、不同数据规模以及表示质量下实现鲁棒泛化。梯度尾部比率可作为泛化能力的可靠、无需标签的诊断指标。

</details>


### [305] [CAAL: Contextual Bandits based Online Hand-Craft Active Learning Strategy Selection](https://arxiv.org/abs/2606.07910)
*Shao-An Yin,Jiacong Li,Tianpei Xie,Cecile Levasseur,Wojciech Kowalinski,Nicola Elia*

Main category: cs.LG

TL;DR: 提出上下文自适应主动学习（CAAL）框架，通过奖励预测和外部上下文信息动态选择主动学习策略，优于现有基线方法。

<details>
  <summary>Details</summary>

Motivation: 主动学习算法面临未标注数据统计分布不确定性的挑战，导致难以选择最佳的手工设计策略。

Method: CAAL将每个手工设计策略视为一个"臂"，利用外部上下文信息进行奖励预测，从而动态选择用于标注批次数据的策略，而非仅依赖已标注数据的反馈。

Result: 在公开数据集上，CAAL使用提出的奖励和上下文设计，性能优于现有基线自适应策略，且结果在不同批次大小下保持一致。

Conclusion: CAAL是一个通用框架，可通过领域知识定制更有效的奖励和上下文候选，且在不同批次大小下表现稳定。

Original Abstract: The challenge with active learning algorithms is the uncertainty of the statistical distribution of unlabeled data, making it difficult to choose the best hand-crafted strategy. To address this, we introduced Contextual Adaptive Active Learning (CAAL). In CAAL, each "arm" represents a hand-crafted strategy. Unlike existing frameworks that select strategies based only on feedback from labeled data, we dynamically choose strategies for labeling batches of data using reward prediction with external context information. This general framework allows for customization with domain knowledge to design more effective rewards and context candidates. In addition, we experimentally show that CAAL outperforms the existing baseline adaptive strategy on public datasets using our reward and context design. Our results are consistent regardless of batch size in each iteration.

中文摘要翻译: 主动学习算法面临的挑战在于未标注数据统计分布的不确定性，这使得选择最佳的手工设计策略变得困难。为解决这一问题，我们引入了上下文自适应主动学习（Contextual Adaptive Active Learning, CAAL）。在CAAL中，每个"臂"代表一种手工设计策略。与仅基于已标注数据反馈来选择策略的现有框架不同，我们利用外部上下文信息进行奖励预测，动态选择用于标注批次数据的策略。这一通用框架允许根据领域知识进行定制，以设计更有效的奖励和上下文候选方案。此外，我们通过实验表明，使用我们设计的奖励和上下文，CAAL在公开数据集上的表现优于现有基线自适应策略。无论每次迭代的批次大小如何，我们的结果均保持一致。

</details>


### [306] [The Easy, the Hard, and the Learnable: Confidence and Difficulty-Adaptive Policy Optimization for LLM Reasoning](https://arxiv.org/abs/2606.07950)
*Zhanke Zhou,Xiangyu Lu,Chentao Cao,Brando Miranda,Tongliang Liu,Bo Han,Sanmi Koyejo*

Main category: cs.LG

TL;DR: 提出CoDaPO方法，通过置信度和难度自适应地重加权策略更新并重采样高价值可学习问题，在固定预算下提升GRPO训练效率，在12个基准上优于现有RL方法。

<details>
  <summary>Details</summary>

Motivation: 标准GRPO训练对简单、困难和可学习问题采用统一采样和加权，导致计算分配效率低下。通过分析GRPO的token级更新动态，发现置信度膨胀、优势收缩和层次收敛三种现象，表明更新效用严重依赖于问题难度和模型当前能力。

Method: 提出CoDaPO方法：1）从rollout置信度和经验难度计算每个问题的有界价值分数；2）用该价值重新加权策略更新；3）在mini-batch内重采样高价值的可学习问题，从而在固定计算预算下增加可学习区间的发现。

Result: 在12个基准上，CoDaPO持续优于现有RL方法，显著提升推理准确性。

Conclusion: 通过对GRPO训练动态的深入分析，提出的CoDaPO方法能够自适应地分配计算资源到高价值可学习问题，有效提升LLM推理能力。

Original Abstract: RL with verifiable rewards can substantially improve LLM reasoning, yet standard GRPO-style training often treats easy, hard, and learnable questions alike through uniform sampling and weighting, leading to inefficient compute allocation. We study GRPO by tracking token log-probabilities, group-normalized advantages, and the induced token-level update weights. This reveals three recurring dynamics as training proceeds: (1) confidence inflation, (2) advantage contraction, and (3) hierarchical convergence. These findings suggest that the utility of each update depends strongly on both question difficulty and the model's current competence. Motivated by this, we propose Confidence and Difficulty-adaptive Policy Optimization (CoDaPO), which assigns each question a bounded value from rollout confidence and empirical difficulty. CoDaPO then uses this value to reweight policy updates and resample high-value learnable questions within mini-batches, thereby increasing discovery within the learnable band under a fixed compute budget. Across twelve benchmarks, CoDaPO consistently improves accuracy over existing RL methods. Our code is publicly available at https://github.com/tmlr-group/CoDaPO.

中文摘要翻译: 可验证奖励的强化学习能够显著提升大语言模型的推理能力，然而标准的GRPO式训练往往将简单、困难和可学习的问题同等对待，采用均匀采样和加权，导致计算分配效率低下。我们通过追踪token对数概率、组归一化优势以及由此产生的token级更新权重来研究GRPO。这揭示了训练过程中三个反复出现的动态：（1）置信度膨胀，（2）优势收缩，（3）层次收敛。这些发现表明，每次更新的效用强烈依赖于问题难度和模型当前的 competence。受此启发，我们提出了置信度与难度自适应策略优化（CoDaPO），该方法从rollout置信度和经验难度中为每个问题分配一个有界价值。CoDaPO随后利用该价值来重新加权策略更新，并在mini-batch内重采样高价值的可学习问题，从而在固定计算预算下增加可学习区间内的发现。在十二个基准上，CoDaPO相较于现有RL方法持续提升了准确率。我们的代码已在https://github.com/tmlr-group/CoDaPO公开。

</details>


### [307] [Minibatch Selection via Partition Matroid Constrained Gradient Matching](https://arxiv.org/abs/2606.07954)
*Prayas Agrawal,Prateek Chanda,Ishita Khatri,Ganesh Ramakrishnan,Bamdev Mishra,Pratik Jawanpuria*

Main category: cs.LG

TL;DR: 提出PartitionSel，一种跨领域小批量选择方法，通过分区拟阵约束下的验证引导梯度匹配效用最大化，减少跨领域冗余并提升收敛速度。

<details>
  <summary>Details</summary>

Motivation: 现有方法要么在每个领域内独立选择样本，要么依赖计算昂贵的代理模型来学习连续领域权重，缺乏高效的跨领域协同选择机制。

Method: 提出PartitionSel，在分区拟阵约束下最大化验证引导的梯度匹配效用，将每个领域的预算编码为约束，通过单一效用函数耦合各领域预算以降低跨领域冗余。该目标函数具有弱子模性，可采用正交匹配追踪算法求解并具有可证明的近似保证。

Result: 在Qwen2.5和Llama-3上微调MetaMathQA和Mol-Instructions数据集，PartitionSel在两种基准上均优于单领域和领域无关的基线方法，同时减少了批次内冲突梯度对的数量。

Conclusion: 跨领域耦合能够转化为更兼容的训练更新，有效提升异构数据上的大模型微调效率。

Original Abstract: Training large language models (LLMs) on heterogeneous data requires selecting minibatches that balance convergence speed with coverage across domains. Existing methods either select samples independently within each domain or rely on computationally expensive proxy models to learn continuous domain weights. We propose PartitionSel, a cross-domain minibatch selection approach that maximizes a validation-guided gradient-matching utility under per-domain budgets encoded as a partition-matroid constraint. By coupling the per-domain budgets through a single utility, PartitionSel is designed to reduce redundancy in selections across domains. The proposed objective is weakly submodular and admits an orthogonal matching pursuit algorithm with provable approximation guarantees. Empirically, we evaluate PartitionSel for minibatch selection during the fine-tuning of Qwen2.5 and Llama-3 on MetaMathQA and Mol-Instructions. PartitionSel achieves robust gains over per-domain and domain-agnostic baselines on both benchmarks. It also reduces the number of conflicting gradient pairs within each batch, indicating that the cross-domain coupling translates into more compatible training updates.

中文摘要翻译: 在异构数据上训练大语言模型（LLMs）时，需要选择既能平衡收敛速度又能覆盖各领域的小批量数据。现有方法要么在每个领域内独立选择样本，要么依赖计算昂贵的代理模型来学习连续的领域权重。我们提出PartitionSel，一种跨领域小批量选择方法，它在分区拟阵约束下最大化验证引导的梯度匹配效用，每个领域对应一个预算约束。通过单一效用函数耦合各领域预算，PartitionSel旨在减少跨领域选择中的冗余。该优化目标具有弱子模性，可通过正交匹配追踪算法求解，并具有可证明的近似保证。在实验上，我们在Qwen2.5和Llama-3上对MetaMathQA和Mol-Instructions进行微调，评估了PartitionSel在小批量选择中的表现。PartitionSel在两个基准测试上均取得了优于单领域和领域无关基线的鲁棒性提升。同时，它减少了每个批次内冲突梯度对的数量，表明跨领域耦合转化为更兼容的训练更新。

</details>


### [308] [Overcoming the Limits of Finite Difference Method; Physics-Informed Neural Network for Noisy High-Dimensional Heat Diffusion](https://arxiv.org/abs/2606.07982)
*Shreesh Bhattarai,Harish Chandra Bhandari*

Main category: cs.LG

TL;DR: 本文提出物理信息神经网络（PINN）作为高维含噪边界条件下瞬态热扩散问题的系统性解决方案。在3D场景下20%边界噪声时，PINN保持约91%准确率而有限差分法（FDM）降至36%；在物理铜热系统中，PINN将边界重建误差降低3.3倍。同时发现维度驱动的效率交叉点：3D中PINN所需时空节点少于FDM且精度更优。结论是噪声暴露和维度共同决定了求解器选择，高噪声高维场景下PINN应成为操作标准。

<details>
  <summary>Details</summary>

Motivation: 高维瞬态热扩散在含噪边界条件下，经典数值方法的精度会灾难性地下降，而物理噪声在实际工程中不可避免，因此需要一种能在噪声环境中保持高精度的系统性解决方案。

Method: 采用物理信息神经网络（PINN）框架，将热扩散方程及其边界条件纳入网络损失函数，通过自动微分和无网格特性实现对含噪边界条件下瞬态热扩散问题的求解，并在1D、2D、3D空间维度上系统验证。

Result: (1) 3D场景下20%边界噪声时，PINN保持约91%准确率，FDM降至36%；(2) 在物理铜热系统中，PINN将边界重建误差降低3.3倍；(3) 存在维度驱动的效率交叉点：3D中PINN所需时空节点少于FDM且精度更高，揭示了经典离散化方法在高维度下的真实成本。

Conclusion: 求解器选择的关键轴不仅是精度，而是噪声暴露和维度的共同作用。当噪声和维度都较高时，经典求解器范式不足，PINN应成为此类场景下的操作标准。

Original Abstract: High-dimensional transient heat diffusion under noisy boundary conditions exposes a fundamental limitation of classical numerical methods: accuracy degrades catastrophically where physical noise is unavoidable. This paper presents a Physics-Informed Neural Network (PINN) framework as a systematic solution to this problem across one, two, and three spatial dimensions, establishing clear operational regimes that redefine solver selection in noisy thermal systems. Under 20% boundary noise in 3D, PINN sustains approximately 91% accuracy while Finite Difference Method (FDM) collapses to 36%, a clear decisive advantage. This is further confirmed in a physical copper thermal system, where PINN reduces boundary reconstruction error by 3.3 times under realistic noise conditions. This noise resilience is accompanied by a dimensionality-driven efficiency crossover: PINN requires fewer spacetime nodes than FDM in 3D while achieving superior accuracy, exposing the true cost of classical discretization at scale. These findings reframe solver selection: the decisive axis is not accuracy alone, but noise exposure and dimensionality jointly. When noise and dimensionality are both high, the classical solver paradigm is insufficient; this work provides the foundation to justify PINN as the operational standard in such regimes.

中文摘要翻译: 高维含噪边界条件下的瞬态热扩散暴露了经典数值方法的一个根本性局限：在物理噪声不可避免的环境中，精度会灾难性地下降。本文提出一个物理信息神经网络（Physics-Informed Neural Network, PINN）框架，作为这一问题在1D、2D和3D空间维度上的系统性解决方案，建立了清晰的运行区间，重新定义了含噪热系统中求解器的选择标准。在3D场景下20%的边界噪声中，PINN保持约91%的准确率，而有限差分法（Finite Difference Method, FDM）则降至36%，呈现出明确的决定性优势。这一结果在物理铜热系统中得到进一步证实：在现实噪声条件下，PINN将边界重建误差降低了3.3倍。这种噪声鲁棒性伴随着维度驱动的效率交叉：在3D中，PINN在实现更优精度的同时所需的时空节点少于FDM，揭示了经典离散化方法在大规模问题中的真实成本。这些发现重塑了求解器的选择范式：决定性的轴不仅是精度，而是噪声暴露与维度的联合作用。当噪声和维度均较高时，经典求解器范式已不足以应对；本文工作为PINN在此类场景中成为操作标准提供了理论基础。

</details>


### [309] [Enhancing AI Interpretability and Safety through Localised Architectures](https://arxiv.org/abs/2606.07998)
*Ian Seet,Jonas Bozenhard,Simon Osterman*

Main category: cs.LG

TL;DR: 本文探讨了局部化硬件机器学习架构相比深度神经网络在可解释性和计算效率上的潜在优势，并评估了不同硬件ML范式在实现这些架构时的适用性。

<details>
  <summary>Details</summary>

Motivation: 当前大型生成式AI模型（如LLM、LRM）在可解释性、安全性和可持续性方面存在严重问题，而深度神经网络虽然函数逼近能力强，但可解释性和计算效率差。作者推测局部化机器学习模型在小型数据集上的优势可能同样适用于局部化硬件架构。

Method: 通过类比推理，提出局部化硬件架构（低带宽、高每节点表达性）相比运行在GPU集群上的深度神经网络可能具有本质上的可解释性优势；随后评估多种硬件ML范式在实现这些架构时的每节点表达性、能效和技术成熟度。

Result: 局部化架构在小型数据集上具有竞争力，且有望比深度神经网络更可解释；不同硬件ML范式在每节点表达性、能效和技术成熟度上各有优劣。

Conclusion: 局部化硬件ML架构具有提升可解释性和计算效率的潜力，尤其在小型数据集场景下值得进一步探索。

Original Abstract: Recent advances in generative AI, especially powerful Large Language Models (LLMs) and Large Reasoning Models (LRMs), raise concerns over the interpretability, safety and sustainability of these large and opaque AI models. The power of such architectures is derived not only from the scalability of deep neural networks, but also massively parallel hardware such as GPU clusters. The diffuse nature of deep neural networks gives them great function-approximation capability when provided with sufficient training data but imposes a cost in interpretability and computational efficiency. Observing that localised machine learning (ML) models tend to be more interpretable and computationally efficient than deep neural networks on small datasets, we reason by analogy that similar advantages may apply to specific localised hardware ML architectures. We argue that localised architectures with lower bandwidth but higher expressivity per node have the potential to be fundamentally more interpretable than deep neural networks running on GPU clusters while remaining competitive for smaller datasets. We then evaluate the suitability of various hardware ML paradigms for implementing such localised architectures and evaluate their per-node expressivity, energy efficiency and practical maturity of the technology required.

中文摘要翻译: 生成式AI的最新进展，尤其是强大的大型语言模型（LLM）和大型推理模型（LRM），引发了人们对这些庞大且不透明的AI模型的可解释性、安全性和可持续性的担忧。这类架构的强大之处不仅来源于深度神经网络的可扩展性，还依赖于GPU集群等大规模并行硬件。深度神经网络的弥散性使其在拥有足够训练数据时具备强大的函数逼近能力，但同时也牺牲了可解释性和计算效率。观察到局部化机器学习模型在小型数据集上往往比深度神经网络更具可解释性和计算效率，我们通过类比推断，类似的优势可能也适用于特定的局部化硬件机器学习架构。我们认为，带宽较低但每节点表达性更高的局部化架构，有望从根本上比运行在GPU集群上的深度神经网络更具可解释性，同时在处理较小数据集时保持竞争力。随后，我们评估了各种硬件ML范式在实现这些局部化架构时的适用性，并考察了它们的每节点表达性、能效及相关技术的实际成熟度。

</details>


### [310] [Evaluating the Impact of Task Granularity on Catastrophic Forgetting in Continual Learning](https://arxiv.org/abs/2606.08013)
*Emre Alyamac,Himanshu Janmeda,Shashwat Krishna,Yash Vijay*

Main category: cs.LG

TL;DR: 本文研究了持续学习中类别学习顺序对灾难性遗忘的影响，探索先学习粗粒度类别再学习细粒度类别是否能减少遗忘。

<details>
  <summary>Details</summary>

Motivation: 灾难性遗忘是持续学习的核心挑战，本文希望探究学习顺序（特别是从粗到细的课程学习方式）是否能有效减轻模型在学习新信息时遗忘旧知识的问题。

Method: 在CIFAR-100数据集上设计了三种学习顺序方案：(1) 从粗到细（Coarse-to-Fine），(2) 从细到粗（Fine-to-Coarse），(3) 平坦式（Flat），并结合弹性权重巩固（EWC）方法防止遗忘，使用准确率、精确率、召回率、F1分数以及后向迁移和遗忘率等持续学习指标进行评估。

Result: 论文声明为"本项目调查"且内容为假设与实验设计，未提供具体的实验结果数据，但提出先学习通用模式（粗粒度类别）可建立稳定基础，有助于模型在后续学习更细致区分时保留知识。

Conclusion: 本工作可为需要增量学习的现实世界系统设计学习序列提供指导，但具体结论有待实验验证。

Original Abstract: Catastrophic forgetting, the abrupt loss of previously acquired knowledge upon learning new information, remains the central challenge in Continual Learning. This project investigates whether the order in which a model learns information affects how well it retains knowledge. Specifically, we ask: does learning general categories first (like "animals" vs "vehicles") before learning specific classes (like "dog" vs "cat") reduce forgetting compared to learning all classes at once?
  We test three approaches on CIFAR-100: (1) Coarse-to-Fine: train on 2 super-classes, then expand to 10 specific sub-classes, (2) Fine-to-Coarse: train on 10 sub-classes, then group into 2 super-classes, and (3) Flat: train on all 10 classes from the start. We use Elastic Weight Consolidation (EWC) to prevent forgetting during transitions. Our hypothesis is that learning general patterns first creates a stable foundation that helps the model retain knowledge when learning more detailed distinctions. We evaluate using standard metrics (accuracy, precision, recall, F1) plus continual learning metrics like backward transfer and forgetting rates. This work could inform how we design learning sequences for real-world systems that need to learn incrementally.

中文摘要翻译: 灾难性遗忘——模型在学习新信息时突然丢失先前获得的知识——仍然是持续学习中的核心挑战。本项目探究模型学习信息的顺序是否会影响其知识保留的效果。具体而言，我们提出问题：先学习通用类别（如"动物"vs"交通工具"）再学习特定类别（如"狗"vs"猫"），与一次性学习所有类别相比，是否能减少遗忘？我们在CIFAR-100上测试了三种方法：(1)从粗到细：先在2个超类上训练，然后扩展到10个特定子类；(2)从细到粗：先在10个子类上训练，然后合并为2个超类；(3)平坦式：从一开始就在所有10个类别上训练。我们使用弹性权重巩固（EWC）来防止转换过程中的遗忘。我们的假设是，先学习通用模式可以为模型建立稳定的基础，有助于模型在学习更细致的区分时保留知识。我们使用标准指标（准确率、精确率、召回率、F1分数）以及持续学习指标（如后向迁移和遗忘率）进行评估。本工作可为需要增量学习的现实世界系统设计学习序列提供参考。

</details>


### [311] [Semantic Quorum Assurance: Collective Certification for Non-Deterministic AI Infrastructure](https://arxiv.org/abs/2606.08021)
*Jun He,Deying Yu*

Main category: cs.LG

TL;DR: SQA（语义法定人数保证）是一种面向非确定性LLM代理基础设施的控制平面原语，通过声明式执行合同、多样化验证者面板和风险自适应法定人数谓词，将云操作中不安全提案的批准率从18.5%降至0.3%，同时仅增加1.45-4.12秒的中位验证延迟。

<details>
  <summary>Details</summary>

Motivation: 现有的分布式共识协议仅复制确定性状态转换，无法评估LLM代理生成的、语法有效且静态授权但操作不安全的提案（如修改IAM策略、开放防火墙安全组等）。云操作面临语义可靠性问题，亟需一种能够评估非确定性代理提案安全性的机制。

Method: 提出SQA框架：1) 将提案表示为绑定密码学证据链的声明式执行合同；2) 路由到多样化只读沙箱验证者面板；3) 采用风险自适应法定人数谓词，强制执行模型多样性和原型多样性，根据校准的保证分数调整权重，并尊重原型特定的否决权；4) 通过的提案通过主权执行门执行。同时形式化了非确定性验证者的相关认知失效模型。

Result: 在500个基础设施场景测试中，SQA将不安全批准率从单智能体验证的18.5%降至0.3%，中位验证延迟为1.45-4.12秒（按风险桶划分）。

Conclusion: SQA有效解决了LLM代理在云操作场景中的语义可靠性问题，大幅降低了不安全操作的风险，同时保持可接受的延迟开销。

Original Abstract: As large language model (LLM) agents are integrated into autonomous cloud operations, distributed systems face a semantic reliability problem: proposer agents can generate production mutations, such as modifying IAM policies, opening firewall security groups, or executing data exports, that are syntactically valid and statically authorized but operationally unsafe. Classical distributed consensus protocols replicate deterministic state transitions but do not evaluate the safety of the proposed intent. To address this gap, we introduce Semantic Quorum Assurance (SQA), a control-plane primitive for governing non-deterministic agentic infrastructure. SQA represents proposals as declarative execution contracts bound to cryptographic evidence chains and routes them to a diverse panel of read-only, sandboxed validator agents. SQA aggregates their judgments under a risk-adaptive quorum predicate that enforces model and archetype diversity, adjusts weights based on calibrated assurance scores, and respects archetype-specific vetoes. Admitted proposals execute only through a sovereign execution gate. We instantiate SQA in a cloud-native control plane and formalize a correlated cognitive failure model for non-deterministic validators. On 500 infrastructure-inspired mutation scenarios, with safety results reported on held-out safe/unsafe trials excluding ambiguous scenarios, SQA reduces unsafe approval from 18.5% for single-agent validation to 0.3% while adding median validation latency of 1.45--4.12 seconds across the studied risk buckets.

中文摘要翻译: 随着大语言模型（LLM）代理被集成到自主云操作中，分布式系统面临一个语义可靠性问题：提案代理可以生成语法有效且静态授权但操作不安全的生成式变更，例如修改IAM策略、开放防火墙安全组或执行数据导出。经典的分布式共识协议复制确定性状态转换，但不评估所提议意图的安全性。为弥补这一空白，我们引入了语义法定人数保证（SQA），一种用于治理非确定性代理基础设施的控制平面原语。SQA将提案表示为绑定到密码学证据链的声明式执行合同，并将其路由到由只读沙箱验证代理组成的多样化面板。SQA在风险自适应法定人数谓词下聚合其判断，该谓词强制执行模型和原型多样性，根据校准的保证分数调整权重，并尊重原型特定的否决权。通过的提案仅通过主权执行门执行。我们在云原生控制平面中实例化SQA，并为非确定性验证者形式化了相关认知失效模型。在500个基于基础设施的变更场景中（安全结果基于保留的安全/不安全试验报告，排除模糊场景），SQA将不安全批准率从单智能体验证的18.5%降低到0.3%，同时在所研究的风险桶上增加了1.45-4.12秒的中位验证延迟。

</details>


### [312] [CausShield: Sample Reconstruction-Resilient Vertical FL via Causal Representation Learning](https://arxiv.org/abs/2606.08027)
*Yongqi Jiang,Yansong Gao,Siguang Chen,Anmin Fu*

Main category: cs.LG

TL;DR: 提出了一种基于结构因果模型（SCM）的纵向联邦学习隐私保护方法CausShield，通过将共享表示分解为任务相关和任务无关两部分，实现了在全训练周期内同时保护模型效用和防御样本重构攻击，理论证明了收敛保持性，实验表明其在隐私保护、模型效用和计算效率上均优于现有方法。

<details>
  <summary>Details</summary>

Motivation: 现有纵向联邦学习中的主动样本重构攻击防御方法存在两难：要么在抑制隐私敏感特征时连带抑制了任务相关信息，损害模型效用；要么依赖端到端有监督训练来收敛防御模块，导致训练早期易受攻击。缺乏一种能在全训练周期内平衡模型效用与隐私保护的有效方案。

Method: 基于结构因果模型（SCM）的理论洞察，将原始样本中的因果特征（任务相关、对学习目标有贡献）与非因果特征（任务无关但编码样本特定隐私信息）分离；在客户端与协调服务器之间，将共享表示显式分解为任务相关和任务无关两部分；通过精心设计的无监督表示学习优化问题来实现这种分解，并理论证明该方法保持了标准VFL的收敛行为。

Result: 与包括InvL（USENIX Security'25）在内的七种最新方法对比，并在URVFL（NDSS'25）等先进重构攻击下评估鲁棒性，CausShield在隐私保护、模型效用和计算效率三个维度上均一致优于所有基线方法。

Conclusion: 通过SCM驱动的特征分解与无监督优化，CausShield能够在全训练周期内有效防御主动样本重构攻击，同时在模型效用和计算效率上不妥协，为纵向联邦学习的隐私保护提供了一种理论上有保障的解决方案。

Original Abstract: Vertical federated learning (VFL) is a distributed learning paradigm that leverages vertically partitioned features across isolated parties without sharing raw samples; however, it remains vulnerable to active sample reconstruction attacks. Existing defenses fail to achieve a satisfactory trade-off between model utility and privacy protection, due to either suppressing task-relevant information alongside privacy-sensitive features or relying on end-to-end supervised training to converge the defense module, which exposes the model to early-epoch vulnerability. To address this challenge, we adopt a structural causal model (SCM) insight and construct CausShield. From a task-learning standpoint, causal features within a raw sample are those that are directly relevant and contributory to the learning objective, whereas non-causal features are task-irrelevant but often encode sample-specific private information, thereby facilitating reconstruction. Importantly, we lay a theoretical foundation to prove this insight. CausShield thus decomposes the shared representations between the client and the coordinating server in VFL into task-relevant and task-irrelevant components to ensure full-cycle privacy protection. Nonetheless, the decomposition is inherently challenging due to the dual objectives of preserving model utility while mitigating privacy leakage. We address this via a carefully formulated optimization problem, which is solved through unsupervised representation learning. We further theoretically prove that CausShield preserves the convergence behavior of standard VFL. Extensive experiments compare CausShield against seven SOTAs, including InvL (USENIX Security'25), and evaluate robustness against advanced reconstruction attacks such as URVFL (NDSS'25). Results demonstrate that CausShield consistently outperforms in privacy protection, model utility, and computational efficiency.

中文摘要翻译: 纵向联邦学习（VFL）是一种利用跨隔离方纵向划分特征的分布式学习范式，无需共享原始样本；然而，它仍然容易受到主动样本重构攻击。现有防御方法由于要么在抑制隐私敏感特征的同时也抑制了任务相关信息，要么依赖端到端的有监督训练来收敛防御模块从而导致模型在训练早期存在脆弱性，因而无法在模型效用和隐私保护之间实现令人满意的权衡。为应对这一挑战，我们采用结构因果模型（SCM）的视角构建了CausShield。从任务学习的角度看，原始样本中的因果特征是那些与学习目标直接相关且对其有贡献的特征，而非因果特征则与任务无关但通常编码了样本特定的私有信息，从而有助于重构攻击。重要的是，我们为这一洞察奠定了理论基础。CausShield由此将VFL中客户端与协调服务器之间的共享表示分解为任务相关和任务无关两部分，以确保全周期的隐私保护。然而，由于需要在保持模型效用的同时减轻隐私泄露这一双重目标，这种分解本身就具有挑战性。我们通过一个精心设计的优化问题来解决这一问题，并通过无监督表示学习进行求解。我们进一步从理论上证明CausShield保持了标准VFL的收敛行为。大量实验将CausShield与包括InvL（USENIX Security'25）在内的七种最新方法进行对比，并在诸如URVFL（NDSS'25）等先进重构攻击下评估其鲁棒性。结果表明，CausShield在隐私保护、模型效用和计算效率方面均一致表现优异。

</details>


### [313] [Noise-Adaptive High-Probability Regret Bounds for Online Convex Optimization](https://arxiv.org/abs/2606.08028)
*Wentao Zhang,Yutong Zhang,Wentao Mo*

Main category: cs.LG

TL;DR: 本文针对强凸损失下的在线凸优化(OCO)的高概率遗憾界问题，在噪声自适应性、反馈结构和约束满足三个交叉领域取得了突破：在全信息设置中实现了噪声自适应的高概率遗憾界（乘性改进G/σ），在赌博机反馈中证明了高概率遗憾与log(1/δ)呈线性关系（与全信息的√log(1/δ)形成正式分离），并在带随机约束的OCO中给出了同时保证累积遗憾和长期约束违反的高概率界。

<details>
  <summary>Details</summary>

Motivation: 现有OCO高概率遗憾界通常依赖Azuma-Hoeffding不等式，导致遗憾界与梯度上界G成正比，无法利用实际中噪声可能远小于梯度界这一特性；此外，不同反馈模型（全信息vs赌博机）下强凸OCO的高概率置信代价是否存在差异尚不明确；带随机约束的OCO问题缺乏同时保证遗憾和约束违反的高概率结果。

Method: 1) 提出一种指数超鞅论证方法，绕过Freedman不等式所需的有界差分条件，直接处理无界次高斯噪声，无需截断；2) 利用信息论下界技术证明赌博机反馈下的最小最大下界；3) 基于Slater条件构造约束OCO的同步高概率保证。

Result: 1) 全信息设置下：遗憾界的鞅偏差项与噪声水平σ成正比而非梯度界G，获得G/σ的乘性改进；2) 赌博机反馈下：高概率遗憾与log(1/δ)呈线性关系，与全信息的√log(1/δ)形成正式分离；3) 约束OCO：达到O(√T log(m/δ))遗憾和O(√T/(ζδ) + m√T log(m/δ))约束违反；4) 合成实验验证了所有理论预测。

Conclusion: 本文揭示了强凸OCO中高概率遗憾界对噪声的自适应能力，证明了反馈结构（全信息vs赌博机）对置信代价的根本性影响，并为约束OCO提供了首个同时高概率保证。指数超鞅方法为无界噪声处理提供了新工具。

Original Abstract: We study high-probability regret bounds for online convex optimization (OCO) with strongly convex losses and establish three results that resolve open questions at the intersection of noise adaptivity, feedback structure, and constraint satisfaction. For the full-information setting with sub-Gaussian stochastic gradients, we prove a noise-adaptive high-probability regret bound in which the martingale deviation term scales with the noise level $σ$ rather than the gradient bound $G$, yielding a multiplicative improvement of $G/σ$ over the classical Azuma-Hoeffding baseline. Our analysis introduces an exponential supermartingale argument that bypasses the bounded-difference requirement of Freedman's inequality, enabling direct treatment of unbounded sub-Gaussian noise without truncation artifacts. For bandit feedback, we prove a minimax lower bound: the high-probability regret scales linearly in $\log(1/δ)$, in contrast to the $\sqrt{\log(1/δ)}$ confidence cost under full information. This constitutes a formal separation in the confidence cost of strongly convex OCO across feedback models. Regarding constrained OCO with stochastic constraints satisfying a Slater condition, we provide simultaneous high-probability guarantees for both cumulative regret and long-run constraint violation, achieving $\mathcal{O}(\sqrt{T\log(m/δ)})$ regret and $\mathcal{O}(\sqrt{T}/(ζδ) + m\sqrt{T\log(m/δ)})$ violation. Synthetic experiments corroborate all theoretical predictions.

中文摘要翻译: 我们研究了强凸损失下在线凸优化(OCO)的高概率遗憾界，并建立了三个结果，解决了噪声自适应性、反馈结构和约束满足交叉领域的开放问题。对于具有次高斯随机梯度的全信息设置，我们证明了一个噪声自适应的高概率遗憾界，其中鞅偏差项与噪声水平σ成正比，而非与梯度界G成正比，相比经典的Azuma-Hoeffding基线获得了G/σ的乘性改进。我们的分析引入了一种指数超鞅论证方法，绕过了Freedman不等式所需的有界差分条件，从而能够直接处理无界的次高斯噪声而无需截断伪影。对于赌博机反馈，我们证明了一个最小最大下界：高概率遗憾与log(1/δ)呈线性关系，与全信息下√log(1/δ)的置信代价形成对比。这构成了强凸OCO在不同反馈模型间置信代价的正式分离。关于满足Slater条件的带随机约束的OCO，我们为累积遗憾和长期约束违反提供了同步高概率保证，实现了O(√T log(m/δ))的遗憾界和O(√T/(ζδ) + m√T log(m/δ))的违反界。合成实验验证了所有理论预测。

</details>


### [314] [SafeECGMatch: Calibration-Aware Joint Frequency and Time Space Semi-Supervised Learning for Open-Set ECG Classification](https://arxiv.org/abs/2606.08037)
*Hongkyu Koh,Ikbeom Jang*

Main category: cs.LG

TL;DR: SafeECGMatch 提出了一种校准感知的安全半监督学习框架，通过双分支架构和自适应置信度校准，解决ECG分类中标记数据稀缺且未标记数据包含分布外异常类别时的伪标签不可靠问题。

<details>
  <summary>Details</summary>

Motivation: 标准半监督学习在未标注池包含标注集中不存在的分布外（OOD）异常或诊断类别时，会强制对这些未见类别分配错误伪标签，产生过度自信的预测，导致性能严重下降。

Method: 采用双分支架构，通过ECG特异性数据增强提取时频潜在表示；核心是动态调整置信度与经验精度之间的对齐，使用自适应标签平滑和温度缩放，在时域和频域上同时校准多分类器和OOD检测器，实现联合优化。

Result: 在PTB-XL和PhysioNet/CinC Challenge基准上，SafeECGMatch取得了最先进的分类精度和校准性能，提升了生理时间序列中可靠知识发现的能力。

Conclusion: SafeECGMatch通过校准感知的安全半监督学习框架，有效解决了标签分布不匹配下的ECG分类问题，实现了可信的OOD拒绝和可靠的伪标签分配。

Original Abstract: Electrocardiogram (ECG) classification models often suffer from severe label scarcity, making semi-supervised learning (SSL) an attractive strategy for reducing annotation costs. In clinical settings, however, unlabeled pools frequently contain out-of-distribution (OOD) anomalies or diagnostic groups absent from the labeled set. Standard SSL forces incorrect pseudo-labels onto these unseen classes, producing overconfident predictions. To address this, we propose SafeECGMatch, a calibration-aware safe SSL framework for single-label ECG classification under label distribution mismatch. Methodologically, SafeECGMatch employs a dual-branch architecture extracting time-frequency latent representations via ECG-specific augmentations. Crucially, it dynamically aligns confidence with empirical accuracy through adaptive label smoothing and temperature scaling, calibrating both the multiclass classifier and the OOD detector across temporal and spectral domains. This joint optimization allows trustworthy OOD rejection and reliable pseudo-labeling. Evaluated on the PTB-XL and PhysioNet/CinC Challenge benchmarks, SafeECGMatch achieves state-of-the-art accuracy and calibration, advancing reliable knowledge discovery in physiological time-series. Code is available at https://github.com/labhai/SafeECGMatch.

中文摘要翻译: 心电图（ECG）分类模型常常遭受严重的标签稀缺问题，这使得半监督学习（SSL）成为降低标注成本的有吸引力的策略。然而在临床环境中，未标注池经常包含分布外（OOD）异常或标注集中不存在的诊断类别。标准半监督学习会强制对这些未见类别分配错误的伪标签，产生过度自信的预测。为解决此问题，我们提出SafeECGMatch，一个面向标签分布不匹配下单标签ECG分类的校准感知安全半监督学习框架。在方法上，SafeECGMatch采用双分支架构，通过ECG特异性数据增强提取时频潜在表示。关键的是，它通过自适应标签平滑和温度缩放动态对齐置信度与经验精度，在时域和频谱域上同时校准多分类器和OOD检测器。这种联合优化实现了可信的OOD拒绝和可靠的伪标签分配。在PTB-XL和PhysioNet/CinC Challenge基准上的评估表明，SafeECGMatch取得了最先进的精度和校准性能，推动了生理时间序列中可靠知识发现的发展。代码可在https://github.com/labhai/SafeECGMatch获取。

</details>


### [315] [When Behavioral Safety Evaluation Fails: A Representation-Level Perspective](https://arxiv.org/abs/2606.08044)
*Enyi Jiang,Anders Gjølbye,Yibo Jacky Zhang,Sanmi Koyejo*

Main category: cs.LG

TL;DR: 本文提出了"审计差距"概念，即大语言模型在行为层面的安全性评估无法反映其内部表征层面对干预的鲁棒性。作者构建了表面安全但潜在空间脆弱的解离模型，并引入基于干预的评估框架和潜在脆弱性评分（LVS），证明行为安全指标不足以衡量表征层面的真实鲁棒性。

<details>
  <summary>Details</summary>

Motivation: 现有LLM安全评估仅关注行为层面的输出安全性，忽略了模型在表征层面对干预（如有害微调、潜在扰动）的脆弱性，存在评估盲区。作者旨在形式化并量化这一行为安全与内部鲁棒性之间的"审计差距"。

Method: 1) 构建解离模型：保留安全的外部行为，但在潜在空间保持脆弱性。2) 提出基于干预的评估框架，包括参数空间和潜在空间的软干预（有害微调、逐层潜在扰动）。3) 提出潜在脆弱性评分（LVS）衡量在有限潜在扰动下诱导有害行为的难易程度。

Result: 在多个安全和对齐/未对齐模型中，解离模型在有害干预下仍能保持拒绝行为，但LVS显著升高；中间表征层对干预最为敏感。行为安全指标无法充分反映表征层面的鲁棒性。

Conclusion: 仅靠行为安全评估无法全面衡量模型鲁棒性，需要结合潜在脆弱性和可观测行为的表征感知审计。

Original Abstract: Large Language Model (LLM) safety has often been evaluated at the behavior level, which provides limited evidence of internal robustness, as these evaluations target outputs rather than representation-level vulnerability under intervention. We formalize this discrepancy as the audit gap: the difference between behavioral safety and robustness under intervention. To study this gap, we construct dissociated models that preserve safe outward behavior while remaining vulnerable in the latent space. We introduce an intervention-based evaluation framework to test model robustness through soft interventions in parameter and latent spaces, including harmful fine-tuning and layer-wise latent perturbations. To formalize the evaluation, we propose the Latent Vulnerability Score (LVS) to measure how easily harmful behavior can be elicited by bounded latent perturbations. Using this evaluation framework, we show that behavioral safety metrics are insufficient measures of representation-level robustness across multiple safely and unsafely aligned state-of-the-art models. Notably, dissociated models show substantially elevated LVSs despite comparable refusal behavior under harmful intervention, with intermediate representations being the most sensitive to intervention. Our results suggest that behavioral safety evaluation alone provides an incomplete picture of model robustness, motivating representation-aware audits of latent vulnerability and observable behavior.

中文摘要翻译: 大语言模型（LLM）的安全性通常在行为层面进行评估，这只能提供有限的内部鲁棒性证据，因为这类评估针对的是输出结果，而非干预下的表征层面脆弱性。我们将这一差异形式化为"审计差距"：行为安全性与干预下鲁棒性之间的差异。为研究这一差距，我们构建了解离模型，该类模型在保持安全的外部行为的同时，在潜在空间中仍然脆弱。我们引入了一个基于干预的评估框架，通过在参数空间和潜在空间中实施软干预（包括有害微调和逐层潜在扰动）来测试模型的鲁棒性。为形式化该评估，我们提出了潜在脆弱性评分（LVS），用于衡量在有限潜在扰动下诱导有害行为的难易程度。利用该评估框架，我们证明行为安全指标不足以衡量多个安全对齐和未对齐的先进模型在表征层面的鲁棒性。值得注意的是，解离模型在有害干预下尽管仍表现出可比的拒绝行为，但其LVS显著升高，其中中间表征层对干预最为敏感。我们的研究结果表明，仅凭行为安全评估无法全面反映模型的鲁棒性，这促使我们需要对潜在脆弱性和可观测行为进行表征感知的审计。

</details>


### [316] [Beyond Homophily: Towards Generalized Graph Reconstruction Attack and Defense](https://arxiv.org/abs/2606.08067)
*Zhanke Zhou,Bo Han,Xuan Li,Jiangchao Yao,Sanmi Koyejo,Michael K. Ng*

Main category: cs.LG

TL;DR: 本文系统研究了图神经网络（GNN）中的图重构攻击（GRA），即从训练好的GNN模型中反演出训练图的邻接关系。作者通过马尔可夫链近似视角分析了邻接信息泄露的条件，并提出了相应的攻击方法MC-GRA(+)和防御方法MC-GPB(+)，在多种图数据集上验证了其有效性。

<details>
  <summary>Details</summary>

Motivation: GNN广泛应用于关系数据（如社交网络、交易记录等），但这些数据可能包含敏感或专有信息。已有的图重构攻击研究缺乏系统性的理论分析，不清楚在什么条件下、为什么邻接信息可以从GNN中恢复。本文旨在系统刻画邻接信息泄露的机制，并据此设计更有效的攻击与防御方法。

Method: 1) 首先对GRA进行系统性表征，分析图同质性和异质性、模型归纳偏置等因素如何调节邻接信息的可恢复性。2) 将GNN推理过程近似为马尔可夫链，把逐层前向计算视为拓扑依赖的表示链。3) 攻击端提出MC-GRA(+)，通过优化一个代理邻接矩阵，使其在各层的GNN表示与目标模型的表示对齐来重构邻接。4) 防御端提出MC-GPB(+)，在表示链中抑制邻接依赖信息，同时通过隐私-效用权衡尽量保持分类精度。

Result: 在同质性/异质性图基准和多种GNN上的实验表明，提出的攻击方法在重构保真度上优于现有方法；提出的防御方法能够有效降低重构成功率，且仅造成轻微的分类精度损失。

Conclusion: 图重构攻击在GNN中是现实且可实现的威胁，邻接信息的泄露程度受图结构特性（同质性/异质性）和模型归纳偏置等因素影响。通过马尔可夫链近似视角，可以设计出更强的攻击（MC-GRA(+)）和更有效的防御（MC-GPB(+)），为GNN隐私保护提供了理论指导和实用工具。

Original Abstract: Graph neural networks (GNNs) are widely deployed on relational data, yet they can leak sensitive or proprietary information about the training graph adjacency, e.g., social ties, transactions, and interactions. This work studies graph reconstruction attacks (GRA), a form of model inversion that reconstructs the training adjacency from a trained GNN, given different levels of attacker-side information. We first provide a systematic characterization of when and why adjacency becomes recoverable through features, labels, embeddings, and predictions, with leakage modulated by graph homophily, heterophily, and the model's inductive bias. Motivated by these findings, we view GNN inference through a Markov chain approximation lens, treating the layered forward computation as a chain of topology-dependent representations. Building on this view, we develop complementary attack and defense methods. On the attack side, we propose MC-GRA (+), which reconstructs the adjacency by optimizing a surrogate adjacency whose GNN-induced representations align with those of the target model at each layer. On the defense side, we propose MC-GPB (+), which suppresses adjacency-dependent information throughout the representation chain while aiming to preserve classification accuracy under a privacy-utility trade-off. Experiments across homophilic/heterophilic graph benchmarks and GNNs show that our attacks improve reconstruction fidelity over prior methods, while our defenses reduce reconstruction success with only minor accuracy loss.

中文摘要翻译: 图神经网络（GNN）广泛部署于关系数据上，但可能泄露训练图邻接中的敏感或专有信息，例如社交关系、交易记录和交互行为。本文研究图重构攻击（GRA），这是一种模型反演形式，旨在从训练好的GNN中重构训练邻接关系，并考虑攻击方不同级别的先验信息。我们首先系统刻画了邻接信息何时以及为何能够通过特征、标签、嵌入和预测被恢复，揭示了图同质性、异质性以及模型归纳偏置对信息泄露的调节作用。基于这些发现，我们从马尔可夫链近似的视角审视GNN推理，将逐层前向计算视为一条拓扑依赖的表示链。基于这一视角，我们开发了互补的攻击与防御方法。在攻击端，我们提出MC-GRA(+)，通过优化一个代理邻接矩阵，使其在各层GNN产生的表示与目标模型的表示对齐，从而重构邻接。在防御端，我们提出MC-GPB(+)，在整个表示链中抑制邻接依赖信息，同时在隐私-效用权衡下力求保持分类精度。在同质性/异质性图基准和多种GNN上的实验表明，我们的攻击在重构保真度上优于现有方法，而我们的防御仅以轻微精度损失即可显著降低重构成功率。

</details>


### [317] [DICE: Entropy-Regularized Equilibrium Selection for Stable Multi-Agent LLM Coordination](https://arxiv.org/abs/2606.08068)
*Yi Xie,Zhanke Zhou,Chentao Cao,Bo Liu,Bo Han*

Main category: cs.LG

TL;DR: 本文指出多智能体LLM系统常不如单个强模型配合最佳N采样，根源在于非良态均衡选择。作者建立不完全信息马尔可夫博弈形式化框架，引入异质分位数响应均衡（HQRE）作为良性学习目标，并基于此提出DICE-PC和DICE-FT两种算法，在11个基准上显著提升性能。

<details>
  <summary>Details</summary>

Motivation: 多智能体LLM系统经常无法稳定地超越单个强模型配合最佳N采样，现有系统只规定了智能体共享什么信息，但没有规定应该选择哪种协调约定，导致振荡和漂移等病态现象，学习不稳定且产生线性贝叶斯遗憾。

Method: 将多智能体LLM系统形式化为折扣不完全信息马尔可夫博弈；引入异质分位数响应均衡（HQRE），一种具有智能体和状态依赖温度系数的熵正则化均衡概念；在单调性条件下证明HQRE唯一性、镜像更新线性收敛及有界贝叶斯遗憾；提出DICE-PC（提示控制协调）和DICE-FT（参数高效镜像微调）两种算法。

Result: 在四个领域的11个基准上，DICE在准确率-成本权衡上优于强类内基线；在推理和规划任务上，DICE-PC平均提升4.3个百分点，DICE-FT提升8.5个百分点。

Conclusion: 通过引入HQRE作为良态均衡概念，可以有效解决多智能体LLM系统的均衡选择不稳定问题，实现更优的协调性能和更稳定的学习。

Original Abstract: Multi-agent large language model (LLM) systems often fail to reliably outperform a single strong model equipped with best-of-N sampling. We argue that a core source of this instability is ill-posed equilibrium selection: current systems specify what information agents share, but not which coordination convention should be selected. We formalize a broad class of such systems as discounted incomplete-information Markov games and show that two common pathologies, oscillation between competing conventions and drift across them, can both induce unstable learning and linear Bayesian regret. To obtain a well-posed target, we introduce the Heterogeneous Quantal Response Equilibrium (HQRE), an entropy-regularized equilibrium concept with agent- and state-dependent temperatures. Under a monotonicity condition, HQRE is unique, admits linearly convergent mirror updates, and yields bounded Bayesian regret; the same condition yields rollout-measurable stability diagnostics. We instantiate this objective in two algorithms: DICE-PC, which coordinates frozen models through prompt-control actions, and DICE-FT, which performs parameter-efficient mirror fine-tuning. Across eleven benchmarks in four domains, DICE improves accuracy-cost trade-offs over strong within-class baselines; on reasoning and planning tasks, DICE-PC improves by 4.3 percentage points on average and DICE-FT by 8.5 points.

中文摘要翻译: 多智能体大语言模型（LLM）系统常常无法可靠地超越配备最佳N采样的单个强模型。我们认为这种不稳定性的核心来源是病态的均衡选择：现有系统规定了智能体之间共享什么信息，但没有规定应选择哪种协调约定。我们将此类系统的一大类形式化为折扣不完全信息马尔可夫博弈，并表明两种常见病态——竞争约定之间的振荡以及跨越约定的漂移——都会导致不稳定的学习和线性贝叶斯遗憾。为了获得一个良态的目标，我们引入了异质分位数响应均衡（HQRE），这是一种具有智能体和状态依赖温度系数的熵正则化均衡概念。在单调性条件下，HQRE是唯一的，允许线性收敛的镜像更新，并产生有界的贝叶斯遗憾；相同的条件还提供了可通过 rollout 测量的稳定性诊断指标。我们将这一目标实例化为两种算法：DICE-PC，通过提示控制动作来协调冻结模型；以及DICE-FT，执行参数高效的镜像微调。在四个领域的11个基准测试中，DICE在准确率-成本权衡上优于强类内基线；在推理和规划任务上，DICE-PC平均提升4.3个百分点，DICE-FT提升8.5个百分点。

</details>


### [318] [ConSteer-RL: Steering Reasoning Capabilities in Large Language Models via Confidence-Aware Reinforcement Learning](https://arxiv.org/abs/2606.08088)
*Qing Miao,Yiming Zhao,Jing Yang,Chenxi Liu,Yuehai Chen,Yuewen Liu,Shaoyi Du,Badong Chen*

Main category: cs.LG

TL;DR: 本文提出ConSteer-RL框架，通过在GRPO中引入基于log概率的token级置信度信号，构建置信度感知奖励，解决RLVR中稀疏二元奖励和忽略模型内部不确定性的问题，在多个模型规模上平均提升2.3%-4.0%。

<details>
  <summary>Details</summary>

Motivation: 现有RLVR方法存在两个主要限制：一是使用稀疏的二元奖励信号，无法有效区分推理过程中的细粒度差异；二是忽略了模型内部的不确定性信息，导致模型可能在过度自信时犯错而无法得到有效惩罚。

Method: 基于GRPO框架，将逐token概率聚合为标量置信度分数，构建置信度感知奖励；并通过基于意识的奖励塑形机制，对过度自信的错误进行惩罚，同时强化正确且自信的推理过程。

Result: ConSteer-RL在不同模型规模上一致优于强GRPO基线，平均提升2.3%-4.0%。

Conclusion: 将模型内部置信度信号融入RLVR训练能够有效提升大语言模型的推理能力，是一种简单但有效的改进方向。

Original Abstract: Reinforcement Learning from Verifiable Rewards (RLVR) has recently become a key paradigm for improving the reasoning abilities of Large Language Models (LLMs), yet it remains limited by sparse binary rewards and its ignorance of model-internal uncertainty. In this paper, we propose ConSteer-RL, a simple yet effective framework that integrates token-level confidence signals derived from model log-probabilities into RLVR training. Specifically, building upon the Group Relative Policy Optimization (GRPO) framework, we construct a confidence-aware reward by aggregating per-token probabilities into a scalar confidence score and incorporating it into an awareness-based reward shaping mechanism that penalizes overconfident errors while reinforcing correct and confident reasoning. Experimental results demonstrate that ConSteer-RL consistently outperforms strong GRPO baselines, achieving average improvements of 2.3%-4.0% across different model scales.

中文摘要翻译: 基于可验证奖励的强化学习（RLVR）近期已成为提升大语言模型（LLMs）推理能力的关键范式，但其仍受限于稀疏的二元奖励信号以及忽略模型内部不确定性等问题。本文提出ConSteer-RL，一个简单而有效的框架，它将源自模型对数概率的token级置信度信号整合到RLVR训练中。具体而言，基于组相对策略优化（GRPO）框架，我们通过将逐token概率聚合成标量置信度分数，并将其融入基于意识的奖励塑形机制中，构建了置信度感知奖励。该机制在强化正确且自信推理的同时，惩罚过度自信的错误。实验结果表明，ConSteer-RL在不同模型规模上均一致优于强GRPO基线，平均提升幅度达2.3%-4.0%。

</details>


### [319] [Constraint-Aware Optimization for Robust Protein Stability Prediction](https://arxiv.org/abs/2606.08100)
*A Shivram,Aneesh S. Chivukula,Manik Gupta,Sourav Chowdhury*

Main category: cs.LG

TL;DR: 本文提出一种无需修改模型架构的约束感知优化框架，通过结合平衡均方误差、Siamese反对称正则化器和OOD边际一致性损失，显著提升了蛋白质突变效应预测模型在分布外数据上的鲁棒性，同时揭示了反对称训练并未消除前向-反向偏差，性能提升源于隐式正则化。

<details>
  <summary>Details</summary>

Motivation: 现有的多模态ΔΔG预测器在分布内数据上表现良好，但在分布外蛋白质上鲁棒性有限，存在前向-反向偏差以及对稀有稳定突变表示不足的问题。已有方法主要通过增加额外架构组件来应对，而优化层面的干预相对未被充分探索。

Method: 提出一个约束感知优化框架，包含三个组件：(1) 平衡均方误差（Balanced MSE）; (2) Siamese反对称正则化器; (3) 基于逐位置特征表示的OOD边际一致性损失。该框架无需对SPURS骨干网络进行任何架构修改。

Result: 在11个基准和3个随机种子上，S669上Spearman相关系数从0.486提升至0.540（σ=0.002），S461上从0.653提升至0.711，在另外5个OOD数据集上也有持续的小幅提升。Ssym诊断实验表明，反对称训练并未消除系统性前向-反向偏差，性能提升来源于隐式正则化而非严格的热力学约束执行。

Conclusion: 提出的优化框架能在不改变模型架构的前提下有效提升分布外泛化能力，且反对称正则化带来的性能提升本质上是隐式正则化效果，而非实现了严格的热力学一致性约束。

Original Abstract: Multimodal $ΔΔG$ predictors integrating protein language models with inverse-folding representations achieve strong in-distribution accuracy on the Megascale dataset but exhibit limited robustness on out-of-distribution (OOD) proteins, persistent forward-reverse bias on paired-mutation benchmarks, and under-representation of rare stabilizing mutations. Existing approaches address these limitations primarily through additional architectural components, leaving optimization-level intervention comparatively underexplored. We introduce a constraint-aware optimization framework combining Balanced Mean Squared Error, a Siamese anti-symmetric regularizer, and a novel OOD-margin consistency loss on the per-position feature representation, requiring no architectural changes to the SPURS backbone. Across eleven benchmarks and three random seeds, the framework improves Spearman correlation on S669 from 0.486 to 0.540 ($σ=0.002$ across seeds), matching the published SPURS baseline (0.50) without architectural modification, and on S461 from 0.653 to 0.711, with consistent smaller gains on five additional OOD datasets. A controlled diagnostic on Ssym reveals that anti-symmetric training does not eliminate systematic forward-reverse bias, indicating that gains arise through implicit regularization rather than exact thermodynamic constraint enforcement.

中文摘要翻译: 整合蛋白质语言模型与逆向折叠表示的多模态ΔΔG预测器在Megascale数据集上实现了较强的分布内精度，但在分布外（OOD）蛋白质上鲁棒性有限，在配对突变基准上存在持续的前向-反向偏差，并且对稀有稳定突变的表示不足。现有方法主要通过增加额外的架构组件来应对这些局限性，而优化层面的干预相对未被充分探索。我们提出了一种约束感知优化框架，结合了平衡均方误差、Siamese反对称正则化器以及基于逐位置特征表示的新型OOD边际一致性损失，且无需对SPURS骨干网络进行任何架构修改。在11个基准和3个随机种子上的实验结果表明，该框架将S669上的Spearman相关系数从0.486提升至0.540（种子间σ=0.002），与已发表的SPURS基线（0.50）相当且无需架构修改；S461上从0.653提升至0.711，并在另外5个OOD数据集上取得持续的小幅提升。Ssym上的受控诊断实验揭示，反对称训练并未消除系统性的前向-反向偏差，表明性能提升来源于隐式正则化，而非严格的热力学约束执行。

</details>


### [320] [A Unifying View of Attention Sinks: Two Algorithms, Two Solutions](https://arxiv.org/abs/2606.08105)
*Lukas Fesser,Mozes Jacobs,Thomas Fel,Andy Keller,Sham Kakade*

Main category: cs.LG

TL;DR: 论文揭示了Softmax Transformer中视觉相似的注意力汇聚（sink）模式实际上对应两种截然不同的机制：自适应空操作（adaptive nop）和广播（broadcast）。通过形式化分析两类机制的特征（nop sink的值范数极小，broadcast sink导致低秩输出），提出诊断方法，并在预训练视觉Transformer中发现两种机制并存。研究表明，现有干预方法（门控和寄存器）分别隐含假设了其中一种机制，单一干预不足，二者结合才能互补提升稳定性与性能。

<details>
  <summary>Details</summary>

Motivation: 注意力汇聚（attention sink）在Softmax Transformer中普遍存在，但视觉上相似的sink模式可能隐藏着本质上不同的算法。现有干预方法（如门控、寄存器）有效却原因不明。作者希望厘清sink背后真正的计算机制，并解释为何不同干预手段起作用。

Method: 1. 提出两种机制假设：adaptive nop（通过路由到空token抑制更新）和broadcast（汇聚并重新分发全局信息）。2. 在合成任务上形式化两类机制的区分特征：nop sink的值范数极小，broadcast sink导致低秩输出。3. 基于这些特征设计实用诊断方法，应用于预训练视觉Transformer，分析sink的分布和性质。

Result: 1. 两种机制在预训练视觉Transformer中均存在：sink从早期层的CLS token向深层层的patch token迁移，并集中在特定注意力头中。2. 寄存器token（原本为broadcast设计）被模型重用于nop功能，证实单一干预不够。3. 门控与寄存器结合在稳定性和性能上产生互补增益。

Conclusion: 相同的注意力模式可能反映两种截然不同的计算，有效的干预需要首先理解模型实际在计算什么。两种机制各有不同的可观测痕迹，针对性的干预（门控对应nop，寄存器对应broadcast）结合才能取得最佳效果。

Original Abstract: When attention concentrates on a single token, a sink, what is the model actually computing? Attention sinks are ubiquitous in softmax transformers, yet this shared visual signature can hide fundamentally different algorithms. We show that visually similar sink patterns can reflect two distinct mechanisms: {i} adaptive nop, where a head suppresses its update by routing to a null token, and {ii} broadcast, where a sink aggregates and redistributes global information. In that case, sinks serve an analogous role: a safe destination when there is nothing useful to compute. Proposed interventions like gating or registers work because they implicitly target one or the other, revealing a duality between method and assumed mechanism: gating implicitly assumes nop; registers implicitly assume broadcast. Each mechanism leaves distinct traces (nop sinks exhibit negligible value norms; broadcast sinks induce low-rank outputs) which we formalize on synthetic tasks and use to derive practical diagnostics. Applied to pretrained vision transformers, these diagnostics reveal that both mechanisms exist at scale: sinks transition from CLS in early layers to patches in deeper layers, and concentrate in specialized heads. Strikingly, register tokens, designed for broadcast, are repurposed to also serve nop, confirming that neither intervention alone suffices. Combining gating with registers yields complementary gains in stability and performance. Overall, we find that the same attention pattern can reflect two very different computations and effective intervention requires first asking what the model is actually computing.

中文摘要翻译: 当注意力汇聚到单个令牌（即sink）上时，模型实际上在计算什么？注意力汇聚现象在Softmax Transformer中普遍存在，但这种共享的视觉特征可能掩盖了根本不同的算法。我们表明，视觉上相似的sink模式可以反映两种不同的机制：(i)自适应空操作（adaptive nop），即注意力头通过路由到空令牌来抑制其更新；以及(ii)广播（broadcast），即sink汇聚并重新分发全局信息。在这种情况下，sink扮演了类似角色：当没有有用的计算可执行时，它作为一个安全的目的地。诸如门控（gating）或寄存器（registers）等拟议干预手段之所以有效，是因为它们隐含地针对其中一种机制，这揭示了方法与假设机制之间的对偶性：门控隐含地假设了nop；寄存器隐含地假设了broadcast。每种机制都留下不同的痕迹（nop sink的值范数极小；broadcast sink导致低秩输出），我们在合成任务上形式化了这些特征，并据此推导出实用的诊断方法。将这些诊断方法应用于预训练视觉Transformer，我们发现两种机制在大规模模型中并存：sink从早期层的CLS令牌过渡到更深层的patch令牌，并集中在专门的注意力头中。引人注目的是，原本为broadcast设计的寄存器令牌被模型重新用于服务nop，这证实了单一干预是不够的。将门控与寄存器结合可在稳定性和性能上产生互补增益。总体而言，我们发现相同的注意力模式可以反映两种截然不同的计算，有效的干预需要首先询问模型实际上在计算什么。

</details>


### [321] [Conditional Random Ordered Transport Spaces](https://arxiv.org/abs/2606.08113)
*Lei Luo,Jian Yang*

Main category: cs.LG

TL;DR: 本文提出条件随机有序传输空间（CROTS），为证据约束下的分布学习提供了一种新型空间理论，解决了小Wasserstein距离不足以保证变换可采纳性的问题，并建立了有序传输、条件风险与渐近可采纳性边界之间的数学联系。

<details>
  <summary>Details</summary>

Motivation: 在证据约束、语义、因果、物理、单调或风险敏感学习中，仅衡量两个概率分布之间的距离（如Wasserstein距离）是不够的，还需要判断质量是否沿允许方向移动。现有方法缺乏统一框架来同时刻画分布距离和方向合规性。

Method: 引入CROTS空间，该空间是一类以L^0值随机概率测度为对象、以Wasserstein距离为环境度量的空间，配备封闭随机序、硬/软有序传输差异以及条件风险泛函，用于评估在证据sigma域下的序违规情况。

Result: 建立了硬/软有序传输的适定性和对偶性、软到硬的变分收敛、随机提升空间的可测性和完备性、经典Wasserstein和有序几何的约化、有序测地线、约束重心和投影、条件风险-传输对偶性，以及序违规分布的分离性。主要稳定性定理表明：随机学习动力学可在环境Wasserstein度量下收敛，同时其局部可采纳性泄漏遵循独立的条件序风险递归。

Conclusion: CROTS为证据过度扩张、有序分布偏移、鲁棒性失效和可采纳分布动力学提供了数学语言，为可靠的分布学习奠定了空间理论基础。

Original Abstract: A small Wasserstein distance does not certify that a transformation is admissible. In evidence-constrained, semantic, causal, physical, monotone, or risk-sensitive learning, one must ask not only how far two probability laws are, but whether mass has moved in a direction allowed by available information. We introduce conditional random ordered transport spaces (CROTS), a class of \(L^0\)-valued spaces of random probability measures equipped with a Wasserstein ambient metric, a closed stochastic order, hard and soft ordered transport discrepancies, and a conditional risk functional for evaluating order violation under an evidence sigma-field. The central object is an order-admissible transport geometry for random measure-valued dynamics, distinct from cone-valued metrics, ordered Kantorovich constructions, random Wasserstein spaces alone, and model-specific residuals for generative paths. We develop the foundations of CROTS as a space theory for reliable distributional learning. The results include well-posedness and duality for hard and soft ordered transport, soft-to-hard variational convergence, measurability and completeness of the random lifted space, reductions to classical Wasserstein and ordered geometries, ordered geodesics, constrained barycenters and projections, conditional risk-transport duality, and separation of order-violating distributions. The main stability theorem shows that random learning dynamics may converge in the ambient Wasserstein metric while its local admissibility leakage follows a separate conditional order-risk recursion. The resulting asymptotic order-risk floor provides a mathematical language for evidence overreach, ordered distribution shift, robustness failure, and admissible distributional dynamics.

中文摘要翻译: 小的Wasserstein距离并不足以证明一个变换是可采纳的。在证据约束、语义、因果、物理、单调或风险敏感学习中，不仅需要衡量两个概率法则之间的距离，还需要判断质量是否沿可用信息所允许的方向移动。本文引入条件随机有序传输空间（CROTS），这是一类以L^0值随机概率测度为对象的空间，配备Wasserstein环境度量、封闭随机序、硬有序传输差异和软有序传输差异，以及用于评估在证据sigma域下序违规的条件风险泛函。其核心对象是为随机测度值动力学构建的序可采纳传输几何，这与锥值度量、有序Kantorovich构造、纯随机Wasserstein空间以及生成路径的特定模型残差均有所不同。我们发展了CROTS作为可靠分布学习的空间理论基础。研究结果包括：硬有序传输和软有序传输的适定性和对偶性、软到硬的变分收敛、随机提升空间的可测性和完备性、经典Wasserstein几何和有序几何的约化、有序测地线、约束重心和投影、条件风险-传输对偶性，以及序违规分布的分离性。主要稳定性定理表明：随机学习动力学可以在环境Wasserstein度量下收敛，而其局部可采纳性泄漏则遵循独立的条件序风险递归。由此产生的渐近序风险下界为证据过度扩张、有序分布偏移、鲁棒性失效和可采纳分布动力学提供了数学语言。

</details>


### [322] [TRUST-SCF: Transformer-based Risk Understanding and Scoring for Transactional Supply Chain Finance](https://arxiv.org/abs/2606.08140)
*Mohammadamin Davoodabadi,Amirabbas Shakeri*

Main category: cs.LG

TL;DR: 提出TRUST-SCF，一个基于Transformer的供应链金融动态信用评分框架，通过交易级风险预测和无需外部信用标签的评分管道，在30万+真实交易数据上实现优于序列基线的延迟预测，并生成与未来还款行为强关联的信用评分。

<details>
  <summary>Details</summary>

Motivation: 供应链金融和LendTech平台需要能够响应交易行为变化、还款延迟和动态暴露风险的信用评分系统，现有方法无法有效捕捉交易序列中的时序依赖和风险模式。

Method: (1) 将用户历史建模为包含利用率、还款延迟和交易位置的交易token序列；(2) 设计金融对齐的注意力偏置，融合利用率相似性和时间近因性；(3) 在log变换目标空间中进行连续还款延迟预测；(4) 构建标签高效的信用评分管道，从预测延迟、模拟利用率下的潜在风险、实际未偿暴露和非线性校准中推导信用评分，无需外部信用标签。

Result: 在超过30万笔真实交易数据上，TRUST-SCF在延迟预测任务上优于序列基线模型，生成的信用评分与未来还款行为强关联。

Conclusion: TRUST-SCF是适用于SCF和LendTech环境的自适应信用评分和交易级风险缓解的实用框架。

Original Abstract: Supply Chain Finance (SCF) and LendTech platforms need credit scoring systems that respond to evolving transaction behavior, repayment delays, and active exposure. We propose TRUST-SCF, a transformer-based framework for transaction-level risk prediction and dynamic credit scoring. Each user history is represented as a sequence of transaction tokens containing utilization, repayment delay and transaction position. The main contributions are: (1) a financially aligned attention bias that combines utilization similarity and recency, enabling the model to compare repayment behavior under comparable exposure conditions; (2) continuous repayment-delay prediction in a log-transformed target space, reducing the influence of extreme delays while improving sensitivity to short-delay behavior and (3) a label-efficient credit-scoring pipeline in which the final credit score is not trained using any explicit external credit-score label, but is instead derived from predicted delay, potential risk over simulated utilization, actual unpaid exposure, and nonlinear calibration. Experiments on real transaction data from more than 300,000 transactions show that TRUST-SCF improves delay prediction over sequential baselines and produces scores that are strongly associated with future repayment behavior. These results suggest that TRUST-SCF is a practical framework for adaptive credit scoring and transaction-level risk mitigation in SCF and LendTech environments.

中文摘要翻译: 供应链金融（SCF）和LendTech平台需要能够响应不断变化的交易行为、还款延迟和主动暴露的信用评分系统。我们提出TRUST-SCF，一种基于Transformer的框架，用于交易级风险预测和动态信用评分。每个用户历史被表示为一个交易token序列，包含利用率、还款延迟和交易位置。主要贡献包括：(1) 金融对齐的注意力偏置，结合了利用率相似性和时间近因性，使模型能够在可比的暴露条件下比较还款行为；(2) 在log变换目标空间中进行连续还款延迟预测，减少极端延迟的影响，同时提高对短延迟行为的敏感性；(3) 标签高效的信用评分管道，其中最终信用评分不使用任何显式的外部信用评分标签进行训练，而是从预测延迟、模拟利用率下的潜在风险、实际未偿暴露和非线性校准中推导。在超过30万笔真实交易数据上的实验表明，TRUST-SCF在延迟预测任务上优于序列基线，并生成与未来还款行为强关联的评分。这些结果表明，TRUST-SCF是适用于SCF和LendTech环境中自适应信用评分和交易级风险缓解的实用框架。

</details>


### [323] [LogNEO: A GPT-Neo Reinforcement Learning Framework for Accurate Real-Time Log Anomaly Detection](https://arxiv.org/abs/2606.08153)
*David Eje,Tanmay Sharma,Khush Patel,Manuel Mazzara,Leonard Johard*

Main category: cs.LG

TL;DR: LogNEO 是一个基于 GPT-Neo (1.3B) 的日志异常检测器，采用部分信用、指数衰减的位置感知奖励和交叉熵正则化的 PPO 微调方法，在 HDFS、BGL 和 Thunderbird 基准上分别达到 0.927、0.913 和 0.984 的 F1 分数，并在生产环境中实现 45ms 延迟和 15,000 事件/秒的吞吐量。

<details>
  <summary>Details</summary>

Motivation: 大规模系统日志中的异常检测对计算基础设施的可靠性和安全性至关重要，现有方法在召回率方面仍有提升空间。

Method: 基于 EleutherAI 的 GPT-Neo (1.3B 参数) 构建日志异常检测器 LogNEO，采用一种新颖的部分信用、指数衰减的位置感知奖励方案，结合交叉熵正则化，通过近端策略优化（PPO）进行微调。位置感知奖励显式建模预测难度：早期位置正确预测获得更高奖励，后期位置错误预测受到更强惩罚。

Result: 在 HDFS、BGL 和 Thunderbird 基准上分别达到 0.927、0.913 和 0.984 的 F1 分数，召回率相比之前最先进的 LogGPT 提升了最多 6 个百分点，同时保持相当的精确率。在生产级微服务部署中（基于 Apache Kafka、Redis 和 TensorRT 加速推理），实现了 45ms 端到端延迟和 15,000 事件/秒的处理能力。

Conclusion: LogNEO 通过 PPO 微调和大语言模型，在日志异常检测任务上取得了优于先前最先进方法的性能，并证明了其在生产环境中部署的可行性。

Original Abstract: Detecting anomalies in large-scale system logs is critical for the reliability and security of modern computing infrastructure. We present LogNEO, a log anomaly detector built on EleutherAI's GPT-Neo (1.3B parameters) and fine-tuned with a novel partial-credit, exponentially decaying position-aware reward scheme combined with cross-entropy regularisation via Proximal Policy Optimisation (PPO). The position-aware reward explicitly models prediction difficulty: early positions receive higher rewards for correct predictions, while later positions incur stronger penalties for errors. LogNEO attains F1-scores of 0.927, 0.913, and 0.984 on the HDFS, BGL, and Thunderbird benchmarks, improving recall by up to 6 percentage points over the prior state-of-the-art LogGPT while maintaining comparable precision. A production microservice deployment over Apache Kafka, Redis, and TensorRT-accelerated inference demonstrates 45 ms end-to-end latency at 15,000 events per second.

中文摘要翻译: 检测大规模系统日志中的异常对于现代计算基础设施的可靠性和安全性至关重要。我们提出了 LogNEO，一个基于 EleutherAI 的 GPT-Neo（1.3B 参数）构建的日志异常检测器，并通过一种新颖的部分信用、指数衰减的位置感知奖励方案，结合交叉熵正则化，利用近端策略优化（PPO）进行微调。该位置感知奖励显式建模了预测难度：早期位置因正确预测获得更高奖励，而后期位置因错误预测受到更强惩罚。LogNEO 在 HDFS、BGL 和 Thunderbird 基准测试上分别取得了 0.927、0.913 和 0.984 的 F1 分数，相比之前最先进的 LogGPT，召回率提升了最多 6 个百分点，同时保持了相当的精确率。在基于 Apache Kafka、Redis 和 TensorRT 加速推理的生产级微服务部署中，实现了 45 毫秒的端到端延迟和每秒 15,000 个事件的处理能力。

</details>


### [324] [Have I Solved This Before? Retrieving Similar Segmentation Problems for Evolutionary Learning](https://arxiv.org/abs/2606.08155)
*Andreas Margraf,Henning Cui,Jörg Hähner*

Main category: cs.LG

TL;DR: 本文提出一种偏离传统监测系统开发流程的新方向，通过将关注点从算法设计转向对检测问题的深入分析，逐步积累知识并存储在抽象系统模型中，实现跨领域过滤管线的迁移复用，从而降低开发风险和成本。

<details>
  <summary>Details</summary>

Motivation: 传统监测系统开发需要在早期高不确定性下做出传感器类型和系统架构的设计决策，且算法训练成本高、容易导致后期昂贵修改。作者希望改变这一范式，通过知识积累和迁移复用减少重复训练、降低风险。

Method: 提出一种增量式知识收集方法，将知识存储在抽象系统模型中，支持检索相似解决方案进行复用；在此基础上，研究过滤管线的跨领域迁移性，将已有管线迁移到不同但相似的分割问题上，并对其效果进行统计分析。

Result: 通过统计分析验证了过滤管线跨领域迁移的可行性，表明这种"迁移学习"变体在图像分割问题中是有效的；同时，简单模型有助于在复杂性、技术需求和可靠性之间取得平衡。

Conclusion: 将检测问题分析置于首位、通过知识积累和管线迁移的方法可以有效降低监测系统开发成本和风险，简单模型在权衡设计复杂度与可靠性方面具有重要价值。

Original Abstract: Reliable integration and solid configuration of monitoring systems constitute a fundamental prerequisites for achieving high efficiency and productivity in contemporary manufacturing environments. Design decisions on sensor type and system architecture have to be made at an early stage and under comparably high uncertainty. This work investigates a research direction that deviates from the traditional monitoring-system development process by shifting the attention from algorithm design to a deeper analysis of the inspection problem. In contrast to traditional design cycles, this paper proposes to gradually collect knowledge and store it in an abstract system model. This enables the retrieval of similar solutions for future use cases, preventing the need for expensive model training from scratch and allowing instead for the incremental refinement of existing base configurations. Reuse of previously generated pipelines reduces the risk of late and costly revisions. As there is little knowledge on cross-domain transferability of filter pipelines, this study analyzes the potential of retrieving filter pipelines to transfer them to different but similar segmentation problems. Finally, we statistically analyze the benefits of this `transfer learning' variant which is predominantly applied to image segmentation problems. In addition, we discuss how simple models help balancing the trade-off between complexity, technical requirements, and reliability in the design process.

中文摘要翻译: 在现代制造环境中，监测系统的可靠集成与稳健配置是实现高效率和高效能的基本前提。关于传感器类型和系统架构的设计决策必须尽早做出，且面临相当高的不确定性。本文研究了一条偏离传统监测系统开发流程的研究方向，将关注点从算法设计转向对检测问题的深入分析。与传统设计周期不同，本文提出逐步收集知识并将其存储在抽象系统模型中。这能够为未来用例检索相似的解决方案，避免了昂贵的模型从头训练，并允许对现有基础配置进行增量式优化。重用先前生成的管线可降低后期昂贵修改的风险。由于关于过滤管线跨领域可迁移性的知识很少，本研究分析了检索过滤管线并将其迁移到不同但相似的分割问题上的潜力。最后，我们统计分析了这种主要应用于图像分割问题的"迁移学习"变体的优势。此外，我们还讨论了简单模型如何帮助在设计过程中平衡复杂性、技术需求和可靠性之间的权衡。

</details>


### [325] [AttentionCap: Transformer Based Capacitance Matrix Learning Toward Full-Chip Extraction](https://arxiv.org/abs/2606.08161)
*Jiechen Huang,Hector R. Rodriguez,Dingcheng Yang,Zuochang Ye,Yibo Lin,Wenjian Yu*

Main category: cs.LG

TL;DR: 提出了一种基于Transformer的电容提取模型AttentionCap，利用注意力机制与电容矩阵的天然相似性，通过Gram表示框架、对称注意力输出层和归一化拉普拉斯损失，在多层多节点设置下实现高精度、高泛化性和极快推理速度。

<details>
  <summary>Details</summary>

Motivation: 传统基于规则的模式匹配电容提取精度在先进节点下难以维持，而现有的MLP和CNN方法只能处理固定金属层组合和特定工艺节点，泛化能力差，实用性受限。

Method: 提出AttentionCap，一个定制化的Transformer模型，包含：Gram表示框架（将电容矩阵映射为适合注意力机制的表示）、物理对齐的对称注意力输出层、归一化拉普拉斯损失函数，以及用于多节点学习的工艺节点嵌入。

Result: 在多层多节点设置下，对未见过的真实设计，自电容误差0.67%/耦合电容误差3.99%；相比CNN-Cap基线，自/耦合误差降低4.6×/5.7×，推理速度提升192×；预训练模型仅需5K样本和4K微调步数即可迁移到未见过的新节点。

Conclusion: AttentionCap在未见过的真实设计上具有足够精度，且对新的工艺节点有强迁移能力，为现代EDA工作流提供了高实用价值的电容提取方案。

Original Abstract: As capacitance extraction accuracy of rule-based pattern matching becomes difficult to sustain at advanced nodes, a growing trend emerges to develop deep-learning-based 2D capacitance models. However, existing MLP- and CNN-based methods constrain their input to fixed metal-layer combinations in a specific process node, limiting their usability in practice. Recognizing the inherent similarity between capacitance matrix and the prevailing attention mechanism, we propose AttentionCap, a customized Transformer for capacitance matrix learning, with a Gram representation framework, a physics-aligned symmetric-attention output layer, and a novel normalized Laplacian loss. We also introduce a process-node embedding to enable multi-node learning. Trained on synthetic data, AttentionCap attains 0.67\%/3.99\% self/coupling-capacitance error on unseen real designs under a multi-layer and multi-node setting, surpassing the CNN-Cap baseline with 4.6$\times$/5.7$\times$ lower self/coupling error and 192$\times$ faster inference speed. A pretrained AttentionCap accurately transfers to an unseen node with only 5K samples and 4K finetuning steps. With sufficient accuracy on unseen real designs and strong transferability to new process nodes, AttentionCap offers highly practical value for modern EDA workflows. Code and data are available at https://github.com/THU-numbda/AttentionCap.

中文摘要翻译: 随着基于规则的模式匹配电容提取精度在先进节点下变得难以维持，开发基于深度学习的2D电容模型成为一种日益增长的趋势。然而，现有的MLP和CNN方法将其输入限制在特定工艺节点的固定金属层组合，限制了它们在实践中的可用性。认识到电容矩阵与主流注意力机制之间的内在相似性，我们提出了AttentionCap，一种针对电容矩阵学习定制的Transformer，包含一个Gram表示框架、一个物理对齐的对称注意力输出层，以及一种新颖的归一化拉普拉斯损失函数。我们还引入了一种工艺节点嵌入以实现多节点学习。在合成数据上训练后，AttentionCap在多层多节点设置下对未见过的真实设计实现了0.67%/3.99%的自电容/耦合电容误差，以4.6×/5.7×更低的自/耦合误差和192×更快的推理速度超越了CNN-Cap基线。一个预训练的AttentionCap仅需5K样本和4K微调步数即可准确迁移到未见过的新节点。凭借在未见过的真实设计上的足够精度和对新工艺节点的强迁移能力，AttentionCap为现代EDA工作流提供了极高的实用价值。代码和数据可在https://github.com/THU-numbda/AttentionCap获取。

</details>


### [326] [Explaining Data Mixing Scaling Laws](https://arxiv.org/abs/2606.08167)
*Rui Dai,Shuran Zheng*

Main category: cs.LG

TL;DR: 本文提出统一框架解释多领域数据混合的缩放定律，通过容量竞争和噪声减少两个关键因素预测模型损失，仅用少量参数即可跨尺度外推并识别高性能训练混合策略。

<details>
  <summary>Details</summary>

Motivation: 现有研究虽建立了多领域数据混合的实证缩放定律，但缺乏对模型损失行为的理论理解。

Method: 将标准神经缩放定律（如Kaplan和Chinchilla）的理论视角扩展到多领域场景，基于领域在基础技能上重叠、在专门技能上分化的分布假设，提出容量竞争（有限模型容量耦合领域损失）和噪声减少（最优权重偏向更难学习领域以最小化噪声）两个关键因素。

Result: 该框架在拟合损失景观上比现有基线均方相对误差更低；能识别性能更优的训练混合；成功跨尺度外推——用较小规模拟合的参数预测大规模未见场景下的高效混合；且所需参数远少于先前经验定律。

Conclusion: 所提出的统一理论框架有效解释了多领域数据混合的缩放行为，具备更强的拟合精度、外推能力和参数效率。

Original Abstract: Recent research has established empirical scaling laws to predict model performance on multi-domain data mixtures. However, a theoretical understanding of these model loss behaviors remains absent. In this work, we propose a unified framework to explain the underlying mechanics of data mixing. Our approach extends theoretical perspectives originally developed for standard neural scaling laws (e.g., Kaplan and Chinchilla) to the multi-domain setting. Based on the distributional assumption that domains overlap on fundamental skills while diverging on specialized skills, we identify two key factors that govern the domain losses of models trained on different data mixtures: \textit{Capacity Competition}, where the allocation of finite model capacity couples domain losses globally, and \textit{Noise Reduction}, where optimal weights shift toward harder-to-learn domains to minimize overall noise. Empirical evaluations show that our framework outperforms existing baselines by fitting the loss landscape with a lower Mean Relative Error and identifying higher-performing training mixtures. Most importantly, our model successfully extrapolates across scales, predicting highly effective mixtures for large, unseen scales using parameters fitted on smaller ones. In addition, our model achieves these results using significantly fewer parameters compared to previous empirical laws. Our code is available at https://github.com/meiqwq/Explaining-Data-Mixing-Scaling-Laws.

中文摘要翻译: 近期研究建立了经验缩放定律来预测模型在多领域数据混合上的性能。然而，对这些模型损失行为的理论理解仍然缺失。本文提出一个统一框架来解释数据混合的内在机制。我们的方法将原本用于标准神经缩放定律（如Kaplan和Chinchilla）的理论视角扩展到多领域场景。基于领域在基础技能上重叠、在专门技能上分化的分布假设，我们识别出两个控制训练于不同数据混合的模型领域损失的关键因素：容量竞争，即有限模型容量的分配使得各领域损失全局耦合；以及噪声减少，即最优权重会向更难学习的领域偏移以最小化整体噪声。实验评估表明，我们的框架优于现有基线，能以更低均方相对误差拟合损失景观，并识别出性能更优的训练混合。最重要的是，我们的模型成功实现了跨尺度外推——使用较小尺度上拟合的参数即可预测大规模未见场景下的高效混合。此外，与先前经验定律相比，我们的模型仅需显著更少的参数即可实现这些结果。我们的代码已开源在 https://github.com/meiqwq/Explaining-Data-Mixing-Scaling-Laws。

</details>


### [327] [Frequency-Domain Latent Attention Gating for Cross-Domain Token Aggregation](https://arxiv.org/abs/2606.08191)
*Kewei Li,Rongying Zhang,Xueli Wang,Xiwen Gong,Zhongjian Wang,Lan Huang,Ruochi Zhang,Fengfeng Zhou*

Main category: cs.LG

TL;DR: 本文提出FLaG，一种基于FFT的即插即用特征聚合模块，将token表示变换到频域后用可学习潜查询进行谱成分汇总，再通过通道门控重构时域token用于最终池化。在AMP活性预测、图像分类和文本分类任务上验证了其有效性，并通过对AMP任务的深入分析揭示了频带贡献、门控行为和交叉注意力模式。

<details>
  <summary>Details</summary>

Motivation: 现有池化方法大多仅在原始token域操作，token聚合成为从token表示映射到样本级预测的常见瓶颈，缺乏利用频域信息进行特征聚合的有效方案。

Method: FLaG模块：1) 用实FFT将token表示变换到频域；2) 用可学习的潜查询（latent queries）汇总频谱成分；3) 应用通道级门控（channel-wise gate）；4) 重构增强的时域token用于最终池化。

Result: 在ESM2-8M抗菌肽活性预测和CIFAR-100上取得最明显提升，在IMDB和GLUE上达到与强文本基线相当的水平。频带消融实验表明低频带贡献最大，高频模式更具样本特异性；门控是广泛共享的谱重加权阶段；交叉注意力模式具有样本特异性且查询间分化温和；高螺旋肽在两种细菌中均表现出更强的平均谱敏感性。

Conclusion: FLaG作为一种即插即用的频域聚合模块，能有效提升多种任务的性能，且通过对AMP任务的深入分析揭示了频域特征聚合的工作机制，具有良好的可解释性和通用性。

Original Abstract: Token aggregation is a common bottleneck in models that map token representations to sample-level predictions, yet most pooling methods operate only in the original token domain. We propose FLaG, a plug-in aggregation module that transforms token representations with the real FFT, summarizes spectral components with learnable latent queries, applies a channel-wise gate, and reconstructs enhanced time-domain tokens for final pooling. We evaluate FLaG on antimicrobial peptide (AMP) activity prediction with ESM2, image classification with ResNet18 on CIFAR-10 and CIFAR-100, and text classification with RoBERTa on IMDB and GLUE. FLaG achieves its clearest gains on the ESM2-8M antimicrobial peptide tasks and on CIFAR-100, while remaining competitive with strong text baselines on IMDB and GLUE. Then we probe its behavior on the AMP setting with band knockouts, gate summaries, residue perturbations, latent-query readouts, and structure-proxy stratification. We find that low-frequency bands contribute the most overall, and the remaining higher-band pattern is more sample-specific. The gate acts as a broadly shared spectral reweighting stage and the cross-attention patterns are sample-specific with mild query-wise differentiation, and higher-helix peptides exhibit stronger average spectral sensitivity in both bacteria. The supplementary materials, source code and data are released at https://www.healthinformaticslab.org/supp/ and https://github.com/Kewei2023/AMPCliff/tree/FLaG.

中文摘要翻译: Token聚合是将token表示映射到样本级预测的模型中的常见瓶颈，然而大多数池化方法仅在原始token域中操作。我们提出FLaG，一个即插即用的聚合模块，它利用实傅里叶变换（FFT）变换token表示，用可学习的潜查询（latent queries）汇总频谱成分，应用通道级门控（channel-wise gate），并重构增强后的时域token用于最终池化。我们在基于ESM2的抗菌肽（AMP）活性预测、基于ResNet18在CIFAR-10和CIFAR-100上的图像分类，以及基于RoBERTa在IMDB和GLUE上的文本分类任务中评估了FLaG。FLaG在ESM2-8M抗菌肽任务和CIFAR-100上取得了最明显的提升，同时在IMDB和GLUE上与强文本基线保持竞争力。进而，我们在AMP设置上通过频带消融（band knockout）、门控摘要（gate summary）、残基扰动（residue perturbation）、潜查询读出（latent-query readout）和结构代理分层（structure-proxy stratification）对其行为进行了探测。我们发现低频带整体贡献最大，剩余的高频带模式更具样本特异性。门控扮演了一个广泛共享的谱重加权阶段，交叉注意力模式具有样本特异性且查询间分化温和，并且较高螺旋度的肽在两种细菌中均表现出更强的平均谱敏感性。补充材料、源代码和数据发布在https://www.healthinformaticslab.org/supp/ 和 https://github.com/Kewei2023/AMPCliff/tree/FLaG。

</details>


### [328] [Neural Field Tokenizations with Hierarchy and Spatial Locality Priors](https://arxiv.org/abs/2606.08204)
*Alonso Urbano,David W. Romero,Max Zimmer,Sebastian Pokutta*

Main category: cs.LG

TL;DR: 提出LH-NeF框架，通过局部保持的分层编码器将连续信号映射为结构化token，无需内循环优化，在内存和批次大小上显著优于元学习方法，在图像、3D形状和气候场等模态上达到或超越现有基线。

<details>
  <summary>Details</summary>

Motivation: 现有神经场表示学习方法中，基于逐样本元学习的方法因内存密集的内循环优化而扩展性差；前馈编码方法则引入模态特定假设，牺牲了通用性。作者希望在不牺牲模态无关性的前提下，提升神经场表示学习的效率与可扩展性。

Method: 提出LH-NeF，核心是一个局部保持的分层编码器（locality-preserving hierarchical encoder），将原始坐标-值场观测映射为结构化token，然后从这些token重建场。训练时用单次前向传播替代元学习的内循环优化。

Result: 相比最强的模态无关基线，LH-NeF使用42倍更少的内存，支持133倍更大的批次。在图像、3D形状和气候场数据上，重建和下游任务性能匹配或超越模态无关、模态特定和专用生成神经场基线。

Conclusion: 局部性与层次性是学习通用场表示的有效先验，可以在不牺牲模态无关性的前提下注入，从而在效率和性能上取得优势。

Original Abstract: Neural fields parameterize data as functions from coordinates to values, providing a unified framework for representation learning across modalities. Existing approaches are dominated by per-sample meta-learning, which scales poorly due to memory-intensive inner-loop optimization. The natural alternative -- feed-forward encoding -- typically introduces modality-specific assumptions, sacrificing the generality that makes learning with neural fields attractive. We argue that locality and hierarchy are useful priors for learning field representations that can be injected without compromising modality-agnosticism. We propose LH-NeF, a framework to learn general-purpose tokenized representations of continuous signals. A locality-preserving hierarchical encoder maps raw coordinate-value field observations to structured tokens, from which the field is reconstructed during training. By replacing meta-learning's inner loop with a single forward pass, LH-NeF uses 42$\times$ less memory and supports 133$\times$ larger batches than the strongest modality-agnostic baseline. Across images, 3D shapes, and climate fields, our learned representations match or exceed performance of modality-agnostic, modality-specific, and specialized generative neural field baselines on both reconstruction and downstream tasks.

中文摘要翻译: 神经场将数据参数化为从坐标到值的函数，为跨模态的表示学习提供了统一框架。现有方法主要由逐样本元学习主导，其内存密集的内循环优化导致扩展性差。自然的替代方案——前馈编码——通常引入模态特定假设，牺牲了神经场表示学习所具备的通用性。我们认为，局部性与层次性是学习场表示的有效先验，可以在不牺牲模态无关性的前提下注入。本文提出LH-NeF，一个学习连续信号通用标记化表示的框架。局部保持的分层编码器将原始坐标-值场观测映射为结构化token，在训练过程中从中重建场。通过用单次前向传播替代元学习的内循环，LH-NeF相比最强的模态无关基线节省了42倍的内存，并支持133倍更大的批次。在图像、3D形状和气候场数据上，我们的学习表示在重建和下游任务中均匹配或超越模态无关、模态特定以及专用生成神经场基线的性能。

</details>


### [329] [Public Machine Learning Solver Framework for Novices in the Machine Learning Domain](https://arxiv.org/abs/2606.08212)
*Lokman Saleh,Hafedh Mili,Mounir Boukadoum*

Main category: cs.LG

TL;DR: 本文提出一个面向非专家的半自动化机器学习智能推荐平台，结合专家定义的算法选择标准和迁移学习，自动分析用户数据集特征，推荐完整的机器学习流水线而非单一算法，并通过众包机制持续更新知识库。

<details>
  <summary>Details</summary>

Motivation: 机器学习问题求解通常需要专家知识，非专家用户很难有效选择和使用合适的算法。现有方案包括全自动AutoML系统、专家速查表和基于选择标准的决策支持系统，但各有局限：AutoML对用户不可解释，速查表过于简化，现有决策系统仅推荐单一算法而非完整流水线。

Method: 平台结合类别2（专家速查表）和类别3（基于选择标准的决策支持系统），采用一阶逻辑在知识库上进行推理，集成专家定义的选择标准与迁移学习，自动从用户提供的数据集中提取数据特征（如类不平衡、缺失值），推荐按相关性排序的完整机器学习流水线，并通过众包平台让ML专家持续更新知识。

Result: 该平台是第一个免费、公开可访问的在线框架，能够系统性地捕获和操作化专家知识，以结构化、透明的方式指导非专家解决ML问题。平台采用增量构建方式，支持新算法、新标准和领域知识的无缝集成。

Conclusion: 本文提出的平台填补了现有AutoML系统和简单速查表之间的空白，为非专家用户提供了一个半自动化、可解释、持续更新的机器学习解决方案推荐框架，具有重要的实用价值。

Original Abstract: Solving machine learning problems is complex and typically reserved for experts. Over the past two decades, systems have emerged to support non-experts. Based on our review, we identify three categories: (1) fully automated AutoML systems, (2) expert cheat sheets for algorithm selection, and (3) decision-support systems using selection criteria (accuracy, transparency, data requirements). We propose a new platform combining categories 2 and 3 to deliver semi-automated, intelligent solution recommendations for non-experts. Unlike existing approaches that recommend a single algorithm, our platform suggests a complete pipeline tailored to the user's problem. It integrates expert-defined selection criteria with transfer learning and automatically extracts data characteristics (e.g., class imbalance, missing values) from user-provided datasets. The platform uses first-order logic to reason over its knowledge base and recommends suitable algorithms ranked by relevance. It features a user-friendly interface and connects to a crowdsourcing platform for ML experts, ensuring continuous updates. The platform is built incrementally, allowing seamless integration of new algorithms, criteria, and domain knowledge. To our knowledge, this is the first free, publicly accessible online framework that systematically captures and operationalizes expert knowledge to guide non-experts in solving ML problems in a structured, transparent manner.

中文摘要翻译: 解决机器学习问题通常复杂且需要专家知识。过去二十年中，出现了许多支持非专家的系统。基于我们的综述，我们识别出三类系统：(1) 全自动的AutoML系统，(2) 用于算法选择的专家速查表，以及(3) 使用选择标准（准确率、可解释性、数据需求）的决策支持系统。我们提出一个新的平台，结合第2类和第3类，为非专家提供半自动化、智能化的解决方案推荐。与现有推荐单一算法的方法不同，我们的平台针对用户的问题推荐完整的流水线。它整合了专家定义的选择标准与迁移学习，并自动从用户提供的数据集中提取数据特征（如类不平衡、缺失值）。该平台使用一阶逻辑在其知识库上进行推理，并推荐按相关性排序的合适算法。平台具有友好的用户界面，并连接到面向ML专家的众包平台，确保持续更新。平台采用增量构建方式，允许新算法、新标准和领域知识的无缝集成。据我们所知，这是第一个免费、公开可访问的在线框架，能够系统性地捕获和操作化专家知识，以结构化、透明的方式指导非专家解决机器学习问题。

</details>


### [330] [How Deep Are Deep GPs, Really? A Sharp Threshold and a Non-Gaussian Limit for Compositional GPs](https://arxiv.org/abs/2606.08218)
*Mark Kozdoba,Shie Mannor*

Main category: cs.LG

TL;DR: 本文研究了深度高斯过程先验在层数趋于无穷时的极限行为，发现了非退化、非高斯的极限分布存在的精确带宽阈值，推翻了此前认为先验总是退化为常数函数的结论。

<details>
  <summary>Details</summary>

Motivation: 理解深度贝叶斯模型中复合函数先验随深度增加时的渐近行为，特别是确定是否存在有意义的非退化极限分布，以构建更有效的概率模型。

Method: 理论分析：推导RBF核宽度参数r的临界阈值r_c(d)=Θ(√d)，区分退化与非退化区域；实验验证：在不同维度d下验证阈值的存在性，并展示极限分布π_{\bar{Z}}的多模态特性。

Result: 1. 发现精确带宽阈值r_c(d)=Θ(√d)，高于该阈值先验退化为常数函数；2. 低于阈值时，先验收敛到非退化、非高斯的极限分布π_{\bar{Z}}，坐标间存在非零依赖；3. 实验验证了阈值的存在，并发现极限分布具有复杂的多模态行为，且该区域随维度增加而变窄。

Conclusion: 深度高斯过程先验可以存在非平凡的极限分布，并非总是退化为常数函数，这一发现为深度贝叶斯模型的先验设计提供了新的理论依据。

Original Abstract: Compositional priors describe the generic properties of layered functions in deep Bayesian models, where deep neural networks with random weights are a canonical example.In the wide-network limit, the prior is a Gaussian process with a depth-dependent kernel, and its behaviour as depth grows has been extensively studied through this kernel. Here, we study another case, where each layer itself is a vector valued Gaussian process, and our aim is similarly to understand the limiting behaviour of the prior as depth grows.
  Previous GP work has established that for the RBF kernel and a certain range of bandwidths $r$, the prior degenerates in the limit, converging to the set of constant functions -- which is not useful as a probabilistic model. In this paper we establish several new results. First, we identify a sharp bandwidth threshold $r_c(d) = Θ(\sqrt{d})$ above which the limit is degenerate, strengthening the earlier bounds. Second, and more importantly, we show that for $r$ below the threshold $r_c(d)$ the prior converges to a limit distribution $π_{\bar{Z}}$. We also prove that these distributions are non-degenerate and non-Gaussian, with non-vanishing dependence between coordinates. In contrast to the previously known degenerate regime, deep Gaussian process priors can therefore admit non-trivial limits.
  Empirically, we verify the threshold across a range of dimensions $d$, and demonstrate a complex multimodal behaviour of the limit distributions $π_{\bar{Z}}$ -- a regime that becomes increasingly narrow with $d$ and would be hard to identify without knowing the threshold.

中文摘要翻译: 组合先验描述了深度贝叶斯模型中复合层函数的通用性质，其中随机权重的深度神经网络是一个典型例子。在宽网络极限下，先验是一个具有深度依赖核的高斯过程，其随深度增长的行为已通过该核得到广泛研究。本文研究了另一种情况，即每一层本身是一个向量值高斯过程，我们的目标同样是理解先验随深度增长的极限行为。先前的高斯过程工作已证明，对于RBF核及一定范围的带宽r，先验在极限下退化，收敛到常数函数集合——这作为概率模型是没有用的。本文建立了若干新结果。首先，我们识别出一个尖锐的带宽阈值r_c(d)=Θ(√d)，高于该阈值极限是退化的，强化了先前的界。其次且更重要的是，我们证明当r低于阈值r_c(d)时，先验收敛到极限分布π_{\bar{Z}}。我们还证明这些分布是非退化且非高斯的，坐标间存在非零依赖。与先前已知的退化机制相反，深度高斯过程先验因此可以允许非平凡的极限。通过实验，我们在不同维度d上验证了该阈值，并展示了极限分布π_{\bar{Z}}的复杂多模态行为——该区域随d增加而变得愈发狭窄，若不知道阈值则难以识别。

</details>


### [331] [De novo molecular generation with optical property preconditioning at the token level](https://arxiv.org/abs/2606.08221)
*Haozhe Huang,Manuel Gonzalez Lastre,Hyun Suk Park,Jorge A. Campos-Gonzalez-Angulo,Xinjian Liu,Alán Aspuru-Guzik*

Main category: cs.LG

TL;DR: 本文提出并使用标记条件自回归语言模型（GPT2）在低数据条件下生成OLED分子，通过条件化垂直吸收能和振子强度等光学属性，结合TDDFT评估，揭示了模型在化学亚空间中的可控性差异：芳香族碳基团表现良好，而吸电子基团（如芳基腈）可控性较差。

<details>
  <summary>Details</summary>

Motivation: 由于高质量OLED分子数据的稀缺以及现有生成模型在跨化学基序的条件控制可靠性不足，设计具有目标光学特性的OLED分子仍具挑战。

Method: 预训练GPT2模型于大型化学语料库，添加离散属性标记进行标记条件化，通过多任务优化微调，以垂直吸收能、振子强度为目标条件，HOMO-LUMO能隙作为辅助电子描述符，生成分子在TDDFT级别进行评估。

Result: 生成的分子库再现了训练分布的 dominant 光学属性支持，但倾向于更低分子量和更少重原子。标记级控制在各条件区间内方向一致但非完全正交，存在局部校准不规则。化学类型分析表明，适度共轭的芳香族碳基团联合目标满足率较高，而吸电子基团（尤其是芳基腈）系统性红移且可控性降低。

Conclusion: 该研究建立了条件OLED分子生成的定量基准，并表明模型可靠性必须在化学有意义的亚空间中评估，而非仅从聚合属性分布出发。

Original Abstract: Designing OLED molecules with targeted optical properties remains challenging due to the scarcity of high-quality data and the limited reliability of conditional control in generative models across chemical motifs. Here, we benchmark a token-conditioned autoregressive language model for OLED molecular generation in a realistic low-data regime. A GPT2 model is pretrained on large chemical corpora, augmented with discrete property tokens, and fine-tuned using multi-task optimisation. Conditioning targets vertical absorption energy and oscillator strength, with the HOMO-LUMO gap included as an auxiliary electronic descriptor.
  Generated molecules are evaluated at the TDDFT level to assess distributional fidelity and controllability. The generated library reproduces the dominant optical-property support of the training distribution while shifting towards lower molecular weight and fewer heavy atoms. Token-level control is consistently directional across conditioning bins, but is not fully orthogonal and exhibits local calibration irregularities. A chemotype-resolved analysis further shows that controllability depends strongly on local electronic environments: moderately conjugated aromatic-carbon motifs are associated with improved joint target satisfaction, whereas electron-withdrawing motifs, particularly aryl nitriles, show systematic red-shifting and reduced controllability.
  These results establish a quantitative benchmark for conditional OLED molecular generation and show that model reliability must be assessed in chemically meaningful subspaces rather than from aggregate property distributions alone.

中文摘要翻译: 设计具有目标光学特性的OLED分子仍然具有挑战性，原因在于高质量数据的稀缺以及生成模型在跨化学基序的条件控制可靠性有限。本文在真实的低数据场景下，对用于OLED分子生成的标记条件自回归语言模型进行了基准测试。我们在大型化学语料库上预训练了一个GPT2模型，添加了离散属性标记，并通过多任务优化进行微调。条件化目标为垂直吸收能和振子强度，同时引入HOMO-LUMO能隙作为辅助电子描述符。生成的分子在TDDFT水平上进行评估，以考察分布保真度和可控性。生成的分子库再现了训练分布中主导光学属性的支持范围，但倾向于更低分子量和更少重原子。标记级控制在各条件区间内保持方向一致性，但并非完全正交，且在局部存在校准不规则性。化学类型解析分析进一步表明，可控性强烈依赖于局部电子环境：适度共轭的芳香族碳基团与更高的联合目标满足率相关，而吸电子基团，尤其是芳基腈，表现出系统性红移和可控性降低。这些结果建立了条件OLED分子生成的定量基准，并表明模型可靠性必须在化学有意义的亚空间中评估，而非仅从聚合属性分布出发。

</details>


### [332] [GPT-Micro: A large language paradigm for accelerated, inexpensive, and thermodynamics-consistent discovery of constitutive models in manufacturing](https://arxiv.org/abs/2606.08238)
*Soumik Dutta,Kiarash Naghavi Khanghah,Sania Shree,Logan McNeil,Thomas Feldhausen,Hongyi Xu,Rajiv Malhotra*

Main category: cs.LG

TL;DR: 本文提出GPT-Micro范式，一种结合大语言模型、热力学定律和稀疏数据的新型自主本构模型发现方法，在印刷电子工艺测试中实现70%以上数据缩减、400倍发现时间缩短，并能发现符合物理规律的全新模型形式。

<details>
  <summary>Details</summary>

Motivation: 传统本构模型发现依赖人类专家经验，耗时长且精度有限；传统机器学习需要大量数据生成成本高；现有基于LLM的模型发现方法忽视热力学基本定律的不可违背性。亟需一种兼顾数据稀疏性、热力学合规性和自主性的模型发现方法。

Method: 提出GPT-Micro框架，无缝集成三个核心组件：(1) 从文献中提取语义知识；(2) 强制执行基于热力学的守恒定律；(3) 利用稀疏数据集，结合LLM驱动的模型假设生成与迭代优化，实现自主、数据稀疏且热力学合规的从头本构模型发现。

Result: 在长期难以解决的印刷电子工艺本构建模问题上验证：(a) 相比ML方法减少70%以上数据需求且精度不降；(b) 相比人工建模，数据生成后发现时间从数月缩短至数小时（400倍加速）；(c) 无需主观选择初始假设即可发现全新函数形式模型；(d) 生成紧凑、符合守恒律、物理完整的解析模型，提升物理可信度、可解释性和机理洞察。

Conclusion: GPT-Micro范式能够实现快速、低成本、物理可信且可解释的微观结构建模，有望推广至整个制造领域，为自主材料模型发现开辟新路径。

Original Abstract: Constitutive modeling of the relationship between process-imposed material states and fundamental material properties is critical to control of material microstructure in manufacturing processes. The limited accuracy resulting from the typical reliance on fallible human expertise and intuition for postulation and revision of the models functional form results in incremental and time consuming model discovery. Conventional Machine Learning (ML) incurs significant cost and time of data generation. Model discovery using Large Language Models (LLMs) suffers from the above issues and/or ignores the inviolability of fundamental thermodynamics laws. This work creates a novel GPT-Micro paradigm for autonomous, data sparse, and thermodynamics-compliant discovery of de-novo constitutive models. This framework seamlessly integrates semantic knowledge extraction from literature, enforcement of thermodynamics-based conservation laws, and sparse datasets, with LLM-driven generation and refinement of model hypotheses. Validation is performed for a long-intractable constitutive modeling problem in a printed electronics process testbed. This reveals significant and simultaneous advantages over the state-of-the-art including: (a) More than 70 percent reduction in data burden relative to ML-based modeling without loss in accuracy; (b) 400X reduction in discovery time after data generation, from months to hours, relative to human-driven modeling; (c) Discovery of models with novel functional forms without subjective human choice of a starting hypothesis; (d) Enhanced physics-rooted trustworthiness, human interpretability, and mechanistic insight via synthesis of compact, conservation-compliant, and physically complete analytical models. The potential of GPT-Micro to realize rapid, low-cost, physically trustworthy, and interpretable microstructure modeling across the manufacturing landscape is discussed.

中文摘要翻译: 工艺施加的材料状态与基础材料性能之间关系的本构建模，对于制造过程中材料微观结构的控制至关重要。传统上依赖可能出错的人类专业知识和直觉来假设和修正模型函数形式，导致模型发现过程增量式推进且耗时巨大。传统机器学习（ML）面临数据生成成本高、周期长的问题。现有基于大语言模型（LLM）的模型发现方法存在上述问题，且/或忽视了基本热力学定律的不可违背性。本研究创建了一种新颖的GPT-Micro范式，用于自主、数据稀疏且热力学合规的从头本构模型发现。该框架无缝整合了文献语义知识提取、基于热力学的守恒律强制执行以及稀疏数据集，结合LLM驱动的模型假设生成与迭代优化。在印刷电子工艺测试平台上对一个长期难以解决的本构建模问题进行了验证。结果显示，该方法相比现有技术具有显著且多维度的优势，包括：(a) 相比基于ML的建模，数据负担减少70%以上且精度无损；(b) 相比人工驱动建模，数据生成后的发现时间从数月缩短至数小时，实现了400倍的加速；(c) 无需主观选择起始假设即可发现具有新颖函数形式的模型；(d) 通过合成紧凑、符合守恒律且物理完整的解析模型，增强了基于物理的可信度、人类可解释性和机理洞察力。本文讨论了GPT-Micro范式在整个制造领域实现快速、低成本、物理可信且可解释的微观结构建模的潜力。

</details>


### [333] [Differentially Private Synthetic Data via APIs 4: Tabular Data](https://arxiv.org/abs/2606.08259)
*Toan Tran,Arturs Backurs,Zinan Lin,Victor Reis,Li Xiong,Sergey Yekhanin*

Main category: cs.LG

TL;DR: 本文提出Tab-PE算法，将私有进化（PE）框架扩展到表格数据生成领域，通过启发式算子实现差分隐私保护下的高质量合成数据生成，尤其擅长处理高阶相关性数据，相比基线方法AIM在分类准确率上提升最高10%，运行速度快28倍。

<details>
  <summary>Details</summary>

Motivation: 现有差分隐私表格数据合成方法主要关注最小化低阶边际查询误差，忽视了高阶相关性带来的挑战，亟需一种能有效处理高阶相关性的隐私保护表格数据生成方法。

Method: 将私有进化（PE）框架从图像和文本合成扩展到表格数据，设计Tab-PE算法：通过表格专用启发式算子生成候选数据变体，以差分隐私方式对候选样本评分，选择高质量样本进行迭代传播，替代原始PE中昂贵的大模型，大幅降低计算成本。

Result: 在真实和模拟数据集上的实验表明，Tab-PE在处理具有高阶相关性的数据集时显著优于现有基线方法，相比最佳基线AIM，分类准确率提升最高10%，运行速度快28倍。

Conclusion: Tab-PE通过引入计算高效的启发式算子，使私有进化框架在表格数据领域实现实用化与可扩展，有效解决了差分隐私表格数据生成中的高阶相关性难题。

Original Abstract: This paper investigates the problem of generating synthetic tabular data with differential privacy (DP) guarantees, enabling data sharing in sensitive domains. Despite extensive study, state-of-the-art methods often focus on minimizing low-order marginal query errors and overlook the challenges posed by high-order correlations. To address this gap, we extend the Private Evolution (PE) framework, originally developed for DP-compliant image and text synthesis, to tabular data. We introduce Tab-PE -- an algorithm for synthetic tabular data generation under DP constraints. Tab-PE iteratively improves a candidate dataset via an evolutionary process that leverages tabular-specialized operators to produce variations, privately scores them, and selects the highest-quality samples to retain and propagate. In contrast to the original PE, which relies on large foundation models, Tab-PE employs heuristic operators with significantly lower computational costs, making PE more practical and scalable for tabular data. Through extensive experiments on real-world and simulation datasets, we demonstrate that Tab-PE substantially outperforms prior baselines on datasets exhibiting high-order correlations. Compared to the best baseline -- AIM, Tab-PE improves classification accuracy by up to 10% while running 28 times faster.

中文摘要翻译: 本文研究了在差分隐私（DP）保证下生成合成表格数据的问题，以实现敏感领域的数据共享。尽管已有广泛研究，现有最先进方法通常侧重于最小化低阶边际查询误差，忽视了高阶相关性带来的挑战。为解决这一问题，我们将最初为DP合规图像和文本合成开发的私有进化（PE）框架扩展到表格数据。我们提出了Tab-PE——一种在DP约束下生成合成表格数据的算法。Tab-PE通过一个进化过程迭代改进候选数据集：利用表格专用算子产生变体，以隐私方式对其进行评分，并选择最高质量的样本进行保留和传播。与依赖大型基础模型的原始PE不同，Tab-PE采用计算成本显著更低的启发式算子，使PE在表格数据上更加实用和可扩展。通过在真实世界和模拟数据集上的大量实验，我们证明Tab-PE在展现高阶相关性的数据集上显著优于之前的基线方法。与最佳基线AIM相比，Tab-PE的分类准确率提升最高达10%，而运行速度快28倍。

</details>


### [334] [Causal Semantic Alignment for LLM-based Time Series Forecasting](https://arxiv.org/abs/2606.08262)
*Kexuan Zhang,Xiaobei Zou,Cesare Alippi,Gary G. Yen,Yang Tang*

Main category: cs.LG

TL;DR: 提出CVAformer框架，通过在变量级别解耦时间序列中的不变成分和动态成分，并应用因果干预消除动态成分的混杂效应，提升了基于LLM的时间序列预测性能。

<details>
  <summary>Details</summary>

Motivation: 现有基于LLM的时间序列预测方法忽略了时间序列的异质性——动态波动与不变语义相互纠缠，导致对齐过程中动态成分作为混杂因子引入虚假相关性，影响预测准确性。

Method: CVAformer在变量级别显式地将每个变量解耦为不变成分和动态成分，然后对动态成分施加因果干预以消除其混杂效应；同时将LLM中的标准因果注意力替换为非因果注意力机制，以更好地捕捉每个时间步上变量间的交互关系。

Result: 在长期、短期、少样本和零样本预测场景下，CVAformer在多数数据集上达到或超越现有最优方法，部分数据集上显著提升预测精度。

Conclusion: 变量级对齐和动态解耦机制有效提升了LLM在时间序列任务上的性能，为基于LLM的时间序列分析提供了新视角。

Original Abstract: Recent advances in Large Language Models (LLMs) have opened new possibilities for time series forecasting by enabling alignment between temporal patterns and pretrained word embeddings. However, most LLM-based methods overlook the heterogeneous nature of time series, where dynamic fluctuations and invariant semantics are entangled. This entanglement introduces spurious correlations during the alignment, as dynamic components act as confounders by simultaneously influencing invariant components and the resulting aligned embeddings. To address this issue, a variable-level alignment framework CVAformer is proposed. CVAformer explicitly disentangles each variable into invariant and dynamic components just before alignment, and applies causal intervention to mitigate the confounding effect of the dynamics. To better support variable-level alignment, CVAformer replaces the standard causal attention in LLMs with a non-causal attention mechanism that captures interactions among variables at each time step. Extensive experiments across long-term, short-term, few-shot, and zero-shot forecasting settings indicate that CVAformer matches or exceeds state-of-the-art performance on most datasets, and in some cases achieves notably better accuracy. Experimental results validate the effectiveness of variable-level alignment and dynamic disentanglement in CVAformer, offering a new perspective for LLM-based time series tasks.

中文摘要翻译: 大型语言模型（LLMs）的最新进展通过实现时序模式与预训练词嵌入之间的对齐，为时间序列预测开辟了新的可能性。然而，大多数基于LLM的方法忽视了时间序列的异质性，即动态波动与不变语义相互纠缠。这种纠缠在对齐过程中引入虚假相关性，因为动态成分作为混杂因子同时影响不变成分和最终的对齐嵌入。为解决这一问题，本文提出了一种变量级对齐框架CVAformer。CVAformer在对齐之前显式地将每个变量解耦为不变成分和动态成分，并应用因果干预来减轻动态成分的混杂效应。为了更好地支持变量级对齐，CVAformer将LLM中的标准因果注意力替换为非因果注意力机制，以捕捉每个时间步上变量间的交互关系。在长期、短期、少样本和零样本预测场景下的大量实验表明，CVAformer在大多数数据集上匹配或超越现有最优性能，并在某些情况下取得了显著更高的精度。实验结果验证了CVAformer中变量级对齐和动态解耦的有效性，为基于LLM的时间序列任务提供了新视角。

</details>


### [335] [Causal Agent Replay: Counterfactual Attribution for LLM-Agent Failures](https://arxiv.org/abs/2606.08275)
*Jaineet Shah*

Main category: cs.LG

TL;DR: CAR通过结构因果模型对LLM agent运行轨迹进行干预式归因，定位导致失败的关键步骤，解决了现有可观测性和评估工具无法追溯根本原因的问题。

<details>
  <summary>Details</summary>

Motivation: 现有的LLM agent故障诊断工具（可观测性、评估）只能回答“发生了什么”或“是否通过”，无法定位导致失败的具体步骤；简单启发式方法（如归因于执行有害动作的步骤）是错误的，因为决策步骤和执行步骤通常不同，且LLM裁判的归因不可靠（SOTA步骤级准确率仅约14%）。

Method: 将agent运行建模为结构因果模型，对步骤施加do-操作干预，在同种随机策略下重放后续轨迹，通过结果分布偏移测量步骤因果效应。包含：干预代数系统、单步对比估计器（含指向性提交规则解决随机重放混杂）、预算受限的蒙特卡洛Shapley估计器（分解交互步骤的信用分配）。所有效应均报告置信区间。

Result: 在合成结构因果模型上验证：对比估计器正确恢复关键步骤；Shapley估计器正确恢复两步交互（权重0.44, 0.45, ~0；效率总和0.909，解析解为0.91）。

Conclusion: CAR通过因果干预提供了一种可靠、可解释的LLM agent故障根本原因分析方法，开源且可在托管或免费本地模型上运行。

Original Abstract: When an LLM agent fails -- issues a refund it should not have, calls the wrong tool, leaks data -- existing tooling answers what happened (observability) or whether it passed (evaluation), but not which step caused the failure. The obvious heuristics are wrong: the step that executes the harmful action is usually not the step that decided on it, and LLM-judge attribution is correlational and unreliable (state-of-the-art step-level accuracy on the Who&When benchmark is about 14%). We present Causal Agent Replay (CAR), which answers the question by intervention: it models an agent run as a structural causal model, applies a do-operation to a step, and re-executes the trajectory forward under the same stochastic policy, measuring the shift in the outcome distribution. We define an intervention algebra over agent steps, a single-step contrastive estimator whose point-of-commitment rule resolves a confound specific to stochastic run-forward, and a budget-bounded Monte-Carlo Shapley estimator that splits credit across interacting steps. Every effect is reported with confidence intervals. We validate against synthetic structural causal models with planted ground truth: the contrastive estimator recovers the pivotal step, and Shapley recovers a two-step interaction (0.44, 0.45, ~0; efficiency sum 0.909 versus the analytic 0.91). CAR is open source and runs on hosted or free local models.

中文摘要翻译: 当LLM agent发生故障——发放不应发出的退款、调用错误的工具、泄露数据——现有工具只能回答发生了什么（可观测性）或是否通过（评估），但无法定位导致失败的步骤。直观的启发式方法是错误的：执行有害动作的步骤通常不是决策步骤，且LLM裁判的归因是相关性的且不可靠（Who&When基准上最先进的步骤级准确率约14%）。我们提出因果Agent重放（CAR），通过干预回答这一问题：将agent运行建模为结构因果模型，对某步骤施加do操作，在相同随机策略下向前重放轨迹，测量结果分布的偏移。我们定义了agent步骤上的干预代数、一个单步对比估计器（其指向性提交规则解决了随机前向重放中的混杂问题），以及一个预算受限的蒙特卡洛Shapley估计器，用于在交互步骤间拆分信用。每个效应均附置信区间报告。我们在植入真实因果关系的合成结构因果模型上进行验证：对比估计器恢复了关键步骤，Shapley恢复了两步交互（0.44, 0.45, ~0；效率总和0.909，解析解为0.91）。CAR是开源的，可在托管或免费本地模型上运行。

</details>


### [336] [Mesh Graph Neural Network Framework for Accelerating Finite Element Simulation for Arbitrary Geometries](https://arxiv.org/abs/2606.08287)
*Josiah D. Kunz,Kamal Choudhary*

Main category: cs.LG

TL;DR: 本文提出一种网格图网络（MGN），用于预测任意孔洞几何形状的二维结构部件中的von Mises应力场。该模型通过编码节点类型、相对边特征和全局特征实现平移和旋转不变性，可在无需重新训练的情况下泛化到未见过的几何形状，在最优情况下R²≥0.97，显著优于传统机器学习模型。

<details>
  <summary>Details</summary>

Motivation: 有限元分析（FEA）在结构设计中至关重要，但在评估多个设计迭代或载荷场景时计算成本高昂。现有的机器学习替代模型大多无法泛化到不同几何形状，这是其关键局限性。

Method: 基于网格图网络（MGN），使用节点类型（如固定边界、自由表面、孔边缘）、相对边特征（相邻节点间距离）和全局特征（施加载荷）作为输入，不依赖绝对节点坐标，从而天然具备平移和旋转不变性，能够泛化到未见过的几何形状。

Result: 在11种板几何形状、20种载荷条件下训练，在7种未见几何形状和3种未见载荷上评估。最优情况下MGN的R²≥0.97，而传统模型（随机森林、梯度提升、K近邻）的R²仅为0.01–0.86。即使在较不利情况下，MGN仍优于传统模型。

Conclusion: 本文将Pfaff等人提出的基于网格的模拟框架扩展到结构力学领域，证明图神经网络可以在不同几何形状下作为有限元分析的高效替代模型。

Original Abstract: Finite element analysis (FEA) is essential for structural design but remains computationally expensive, particularly when evaluating multiple design iterations or load scenarios. Machine learning surrogate models offer a promising alternative, yet most approaches struggle with a critical limitation: generalizing across varying geometries. This work presents a mesh graph network (MGN) for predicting von Mises stress fields in 2D structural components with arbitrary hole geometries. Unlike traditional machine learning approaches that use absolute node coordinates as features, the proposed model builds on existing MGN frameworks that encode node types (e.g., fixed boundary, free surface, hole edge), relative edge features (distance between neighbors), and global features (applied load). This architecture is inherently translation- and rotation-invariant, enabling generalization to unseen geometries without retraining. The MGN was trained on 11 plate geometries under 20 load conditions and evaluated on 7 unseen geometries and 3 unseen loads. In the most favorable case, the model achieves $R^2 \geq 0.97$ on an unseen geometry and unseen load, compared to $R^2 \approx 0.01$--$0.86$ for conventional models (Random Forest, Gradient Boosting , K-Nearest Neighbors) trained on identical data. However, even in less favorable cases, the MGN model still outperforms conventional models. This work extends the mesh-based simulation framework of Pfaff et al. (arXiv:2010.03409) to structural mechanics, demonstrating that graph neural networks can serve as efficient surrogates for finite element analysis across varying geometries.

中文摘要翻译: 有限元分析（FEA）对于结构设计至关重要，但在评估多个设计迭代或载荷场景时计算成本仍然高昂。机器学习替代模型提供了一种有前景的方案，然而大多数方法面临一个关键局限性：难以泛化到不同的几何形状。本文提出一种网格图网络（MGN），用于预测具有任意孔洞几何形状的二维结构部件中的von Mises应力场。与使用绝对节点坐标作为特征的传统机器学习方法不同，所提出的模型建立在现有的MGN框架基础上，编码节点类型（如固定边界、自由表面、孔边缘）、相对边特征（相邻节点间的距离）以及全局特征（施加载荷）。该架构天然具备平移和旋转不变性，从而能够在无需重新训练的情况下泛化到未见过的几何形状。MGN在11种板几何形状、20种载荷条件下训练，并在7种未见几何形状和3种未见载荷上进行评估。在最有利的情况下，模型在未见几何形状和未见载荷上达到了R²≥0.97，而传统模型（随机森林、梯度提升、K近邻）在相同数据上训练的R²仅为0.01–0.86。即使在较不利的情况下，MGN模型仍然优于传统模型。本文将Pfaff等人（arXiv:2010.03409）提出的基于网格的模拟框架扩展到结构力学领域，证明图神经网络可以在不同几何形状下作为有限元分析的高效替代模型。

</details>


### [337] [On solving symmetric multi-type orthogonal non-negative matrix tri-factorization problem](https://arxiv.org/abs/2606.08291)
*Rok Hribar,Gregor Papa,Janez Povh,Andrej Kastrin*

Main category: cs.LG

TL;DR: 本文研究对称多类型正交非负矩阵三分解问题，提出了两种启发式算法（基于KKT条件的定点方法和基于ADAM的三阶段方法），在合成数据和真实引文网络上的实验表明，这两种方法能恢复接近最优的分解，且在链接预测、节点聚类和节点分类任务上优于或匹敌SVD、node2vec等基线方法。

<details>
  <summary>Details</summary>

Motivation: 对称多类型正交非负矩阵三分解问题在聚类和网络分析中具有重要应用，非负性提升可解释性，正交性赋予潜在因子自然的分配型结构，但该优化问题高度非凸，需要有效的求解算法。

Method: 提出两种启发式算法：(1) 基于KKT条件的定点方法，通过在正交约束上添加罚项推导得到；(2) 基于ADAM的三阶段方法，结合非负保持优化、正交化以及可行域上的受限ADAM精炼。

Result: 合成实验表明两种算法都能恢复接近最优的分解，且在噪声下保持稳定；在真实引文网络上，学习到的嵌入在链接预测、节点聚类和节点分类任务中优于或匹敌SVD、node2vec和经典链接预测启发式方法等基线。

Conclusion: 所提出的两种启发式算法能有效求解对称多类型正交非负矩阵三分解问题，在实际网络分析任务中具有竞争力。

Original Abstract: We study the symmetric multi-type orthogonal non-negative matrix tri-factorization problem, where several symmetric non-negative matrices are simultaneously approximated by factors of the form $GS_{i}G^{\top}$, with a shared non-negative and orthogonal factor $G$. This model is motivated by clustering and network analysis, where non-negativity improves interpretability and orthogonality gives a natural assignment-type structure to the latent factor. Since the resulting optimization problem is highly non-convex, we develop two heuristic algorithms for computing high-quality local solutions. The first one is a fixed point method derived from the Karush-Kuhn-Tucker conditions after adding a penalty term for the orthogonality constraint. The second one is a three-stage ADAM-based method that combines non-negativity-preserving optimization, orthogonalization, and restricted ADAM refinement on the feasible set. We evaluate both methods on synthetic data, including noisy instances, and on citation network benchmarks. The synthetic experiments show that both algorithms recover factorizations close to the optimum and remain stable under noise. On real networks, the learned embeddings are competitive with or better than standard baselines such as SVD, node2vec, and classical link prediction heuristics in link prediction, node clustering, and node classification tasks.

中文摘要翻译: 我们研究对称多类型正交非负矩阵三分解问题，即同时用形如$GS_{i}G^{\top}$的因子逼近若干个对称非负矩阵，其中共享因子$G$是非负且正交的。该模型源于聚类和网络分析，其中非负性提升可解释性，正交性赋予潜在因子自然的分配型结构。由于得到的优化问题高度非凸，我们开发了两种用于计算高质量局部解的启发式算法。第一种是基于卡鲁什-库恩-塔克条件并在正交约束上添加罚项后导出的定点方法。第二种是基于ADAM的三阶段方法，结合了非负保持优化、正交化以及可行域上的受限ADAM精炼。我们在合成数据（包括含噪实例）和引文网络基准上评估了两种方法。合成实验表明，两种算法都能恢复接近最优的分解，并在噪声下保持稳定。在真实网络上，学习到的嵌入在链接预测、节点聚类和节点分类任务中优于或匹敌标准基线方法（如SVD、node2vec和经典链接预测启发式方法）。

</details>


### [338] [QueryWeaver: Reliable Multi-Tool Query Execution Planning via LLM-Based Graph Generation](https://arxiv.org/abs/2606.08300)
*Aishwarya Chakravarthy,Vidhi Kulkarni,Duen Horng Chau*

Main category: cs.LG

TL;DR: 本文提出了一种将自然语言查询转换为结构化图并利用确定性规划器执行的系统，通过深度优先搜索解决跨工具依赖与结果合并，提升了多步跨工具查询的可靠性，在小规模或本地LLM上仍能保持高准确率。

<details>
  <summary>Details</summary>

Motivation: 现实世界中许多对个人数据的查询涉及多个应用程序，需要结构化规划，因为单个工具只能暴露部分信息。虽然LLM在推理和工具使用方面表现出色，但可靠地执行多步、跨工具查询仍然具有挑战性。

Method: 将自然语言查询转换为结构化图，并通过确定性规划器执行。采用深度优先搜索来解析依赖关系并跨工具合并结果。

Result: 即使使用较小规模或本地托管的LLM，该系统也表现出高准确率，能够支持超越传统基于关键词搜索的查询类型。

Conclusion: 该方法通过结构化图与确定性规划的结合，显著提升了LLM在多步跨工具查询任务中的可靠性和准确性，降低了对大模型规模的依赖。

Original Abstract: Many real-world queries over personal data span multiple applications and require structured planning, as individual tools expose only partial information. While LLMs show strong reasoning and tool use, reliably executing multi-step, cross-tool queries remains challenging. We introduce a system that converts natural language queries into structured graphs and executes them via a deterministic planner. Our approach uses depth-first search to resolve dependencies and combine results across tools, improving reliability and enabling queries beyond traditional keyword-based search. We demonstrate high accuracy even with smaller or locally hosted LLMs.

中文摘要翻译: 许多对个人数据的现实查询跨越多个应用程序，需要结构化的规划，因为单个工具仅能暴露部分信息。虽然大语言模型（LLM）展现了强大的推理能力和工具使用能力，但可靠地执行多步、跨工具查询仍然具有挑战性。本文介绍了一个系统，该系统将自然语言查询转换为结构化图，并通过确定性规划器执行。我们的方法使用深度优先搜索来解析依赖关系并跨工具合并结果，从而提高了可靠性，并支持超越传统基于关键词搜索的查询。我们证明，即使使用较小规模或本地托管的LLM，该系统也能实现高准确率。

</details>


### [339] [GeoGNN: Time Series Geo-Localization using Two-Tower Graph Neural Networks](https://arxiv.org/abs/2606.08303)
*Toan Tran,Waqwoya Abebe,Abhishek Potnis,Supriya Chinthavali,Cyrus Shahabi,Li Xiong,Dalton Lunga*

Main category: cs.LG

TL;DR: 本文提出了一种新的时间序列地理定位任务，并设计了双塔架构GeoGNN，通过地理邻接图嵌入和时间序列特征提取，在大规模电力消耗数据集上将地理定位准确率平均提升约27%。

<details>
  <summary>Details</summary>

Motivation: 现有的时间序列分析缺乏地理上下文信息，无法自动推断时间序列的地理来源。成功的地理定位能为时间序列提供空间背景，支持位置感知的下游应用。

Method: 提出GeoGNN双塔架构：空间塔利用地理邻接图学习候选地理单元的嵌入表示；时间塔从时间序列中提取表征。推理时通过点积相似度匹配和辅助分类头预测时间序列的地理来源。

Result: 在大规模国家级电力消耗数据集上，GeoGNN在所有数据集上均取得最佳性能，细粒度和粗粒度地理定位准确率平均提升约27%。

Conclusion: GeoGNN有效解决了时间序列地理定位问题，通过双塔架构和地理邻接图显著提升了定位精度，验证了为时间序列赋予空间上下文的可行性。

Original Abstract: This paper investigates a novel concept of time series geolocalization, where the goal is to infer the geographic origin of each raw time series. Successful geolocalization can provide spatial context to time series, enabling downstream location-aware applications. We formalize the problem, adapt core ideas from image geolocalization to establish strong baselines, and propose GeoGNN, a two-tower architecture. During training, GeoGNN's spatial tower learns embeddings of geographic cell candidates by leveraging the geographic adjacency graph, while the temporal tower extracts informative representations from time series. During inference, each temporal representation is matched against candidate geographic embeddings using dot-product similarity, combined with an auxiliary classification head, to predict the time series' associated geographic origin. Experiments on large-scale, countrywide electricity-consumption datasets demonstrate that GeoGNN achieves the best performance across datasets and enhances both fine- and coarse-grained geolocalization accuracy by ~27% on average.

中文摘要翻译: 本文研究了一种新颖的时间序列地理定位概念，其目标是推断每条原始时间序列的地理来源。成功的地理定位可以为时间序列提供空间上下文，从而支持下游的位置感知应用。我们形式化定义了该问题，借鉴图像地理定位的核心思想建立了强基线，并提出了双塔架构GeoGNN。在训练过程中，GeoGNN的空间塔通过利用地理邻接图学习候选地理单元的嵌入表示，而时间塔则从时间序列中提取信息丰富的表征。在推理时，每个时间表征与候选地理嵌入进行点积相似度匹配，并结合辅助分类头，以预测时间序列相关联的地理来源。在大规模国家级电力消耗数据集上的实验表明，GeoGNN在所有数据集上均取得了最佳性能，并将细粒度和粗粒度地理定位准确率平均提升了约27%。

</details>


### [340] [Towards Graph Foundation Models for Dynamics in Complex Networked Systems: Lessons from Super-Spreader Identification in Multilayer Networks](https://arxiv.org/abs/2606.08306)
*Michał Czuba,Mateusz Stolarski,Adam Piróg,Piotr Bielak,Piotr Bródka*

Main category: cs.LG

TL;DR: 本文论证了图基础模型(GFM)在网络动力学中需要跨网络归纳泛化能力，提出了四项设计属性，并通过仅在合成多层网络上训练的ts-net模型，在真实世界多层网络上实现了零样本泛化，超越传统启发式方法和直推式基线。同时指出了构建网络动力学GFM的五大开放挑战。

<details>
  <summary>Details</summary>

Motivation: 当前网络动力学（传播、影响力最大化、疫情建模等）研究主要局限于直推式范式，即模型在单个网络上训练，无法在未见过的图上复用。论文认为跨网络归纳泛化能力是构建图基础模型(GFM)的必要前提，但目前缺乏相关研究。

Method: 提出四项设计属性以实现归纳式跨网络泛化，并作为概念验证构建了ts-net (TopSpreadersNetwork)模型，该模型仅在合成多层网络(MLNs)上训练，无需在真实网络上进行微调或重新训练。

Result: ts-net在零样本设置下成功泛化到不同规模、不同层数的真实世界多层网络，在四项评估指标中有三项优于经典启发式方法和直推式基线。

Conclusion: ts-net验证了跨网络归纳泛化的可行性，同时指出了构建网络动力学的图基础模型面临的五大挑战：规模扩展、多层泛化、自监督预训练、跨任务迁移以及节点属性融合。

Original Abstract: Network dynamics - including spreading, influence maximisation, and epidemic modelling - remain largely confined to the transductive paradigm, where models are trained on a single network and cannot be reused on unseen graphs without retraining. We argue that inductive cross-network generalisation is a necessary prerequisite for Graph Foundation Models (GFMs) in this domain and propose four design properties towards this goal. As a proof of concept, ts-net (TopSpreadersNetwork), trained solely on synthetic multilayer networks (MLNs), demonstrates zero-shot generalisation to real-world MLNs of varying size and layer count, outperforming classical heuristics and transductive baselines on three of four metrics. Based on ts-net's performance, we further outline five open challenges towards building GFMs for network dynamics: scale, many-layer generalisation, self-supervised pretraining, cross-task transfer, and node-attribute integration.

中文摘要翻译: 网络动力学——包括传播、影响力最大化和疫情建模——在很大程度上仍局限于直推式范式，即模型在单一网络上训练，无法在未见过的图上复用而不进行重新训练。我们认为，跨网络归纳泛化能力是构建该领域图基础模型(GFM)的必要前提，并为此提出了四项设计属性。作为概念验证，ts-net（TopSpreadersNetwork）仅在合成多层网络(MLNs)上训练，便能零样本泛化到不同规模和层数的真实世界多层网络，在四项评估指标中有三项优于经典启发式方法和直推式基线。基于ts-net的表现，我们进一步提出了构建网络动力学图基础模型的五大开放挑战：规模扩展、多层泛化、自监督预训练、跨任务迁移和节点属性融合。

</details>


### [341] [Fourier fractal dimension to predict the generalization of deep neural networks](https://arxiv.org/abs/2606.08308)
*Joao B. Florindo,Davi Wanderley Misturini*

Main category: cs.LG

TL;DR: 提出了一种基于傅里叶分形维数的泛化度量，通过分析权重变化的频域特征来预测深度神经网络的泛化性能，并设计了相应的傅里叶优化器进行正则化，在多个数据集上取得了领先的相关性结果。

<details>
  <summary>Details</summary>

Motivation: 深度学习模型参数高度超参数化，依赖验证集评估泛化性能成本高昂；SGD驱动的非高斯、重尾动力学导致参数空间呈现复杂尺度不变轨迹，现有基于范数、边际和PAC-Bayes的度量难以充分捕捉这种几何复杂性。

Method: 1) 基于Lévy驱动随机微分方程的特征函数，在频域分析网络权重变化，提取傅里叶分形维数作为泛化度量；2) 设计定制化傅里叶优化器，在训练过程中主动正则化该分形维数。

Result: 在CIFAR-10、SVHN和MNIST数据集上的实验表明，所提度量与实际泛化差距高度相关，Kendall秩相关系数达到最先进水平，优于现有的范数类、边际类和PAC-Bayes类度量。

Conclusion: 频域分形分析作为预测模型泛化能力的强大工具，并为开发更稳定的优化算法提供了理论基础。

Original Abstract: Predicting the generalization performance of deep neural networks without relying on hold-out validation data is a fundamental challenge in machine learning. While Stochastic Gradient Descent (SGD) drives the optimization of these highly parameterized models, its heavy-tailed, non-Gaussian dynamics induce complex, scale-invariant trajectories in the parameter space. In this paper, we propose a novel generalization measure based on the Fourier fractal dimension of the network's weight variations. By analyzing the characteristic function of the Lévy-driven stochastic differential equations in the frequency domain, we extract a metric that robustly captures the geometric complexity of the learning process. Furthermore, we introduce a customized Fourier-based optimizer designed to actively regularize this fractal dimension during training. Extensive empirical evaluations on the CIFAR-10, SVHN, and MNIST datasets demonstrate that our proposed Fourier generalization measure exhibits a strong correlation with the actual generalization gap. Our method achieves state-of-the-art Kendall rank correlation coefficients, outperforming a wide array of existing norm-based, margin-based, and PAC-Bayesian measures. Ultimately, this work highlights the potential of frequency-domain fractal analysis as both a powerful predictor for model generalizability and a principled foundation for developing more stable optimization algorithms.

中文摘要翻译: 在不依赖留出验证数据的情况下预测深度神经网络的泛化性能是机器学习中的一个基本挑战。虽然随机梯度下降（SGD）驱动着这些高度参数化模型的优化，但其重尾、非高斯动力学在参数空间中诱导出复杂的尺度不变轨迹。在本文中，我们提出了一种基于网络权重变化傅里叶分形维数的新型泛化度量。通过分析频域中Lévy驱动随机微分方程的特征函数，我们提取出一个能够稳健捕获学习过程几何复杂性的指标。此外，我们引入了一种定制化的基于傅里叶的优化器，旨在训练过程中主动正则化该分形维数。在CIFAR-10、SVHN和MNIST数据集上的大量实验表明，我们提出的傅里叶泛化度量与实际泛化差距之间表现出强相关性。我们的方法取得了最先进的Kendall秩相关系数，优于现有的范数类、边际类和PAC-Bayes类度量。最终，本工作凸显了频域分形分析作为模型泛化能力强大预测器以及开发更稳定优化算法的原则性基础的潜力。

</details>


### [342] [Where the Score Lives: A Wavelet View of Diffusion](https://arxiv.org/abs/2606.08309)
*Emma Finn,Binxu Wang,T. Anderson Keller,Demba E. Ba*

Main category: cs.LG

TL;DR: 本文提出了一种基于二维正交小波基展开的分数函数可解析参数化方法，通过数据分布的矩来推导可解释的最优分数函数，从而分析不同架构（CNN、U-Net、Transformer）对扩散模型生成行为的影响。

<details>
  <summary>Details</summary>

Motivation: 尽管CNN、U-Net、Transformer等多种架构已被广泛用作扩散模型中分数近似的网络，但人们对这些架构选择如何影响生成行为知之甚少，本文旨在提供对此领域的深入理解。

Method: 提出了一种基于二维正交小波基展开的分数函数可解析参数化方法，利用数据分布的矩推导出可解释的最优分数函数，并进行与架构无关的基于矩的分析。

Result: 该分数机器足够灵活，能够部分模仿包括U-Net和CNN在内的多种架构的相关归纳偏置，揭示了数据分布中哪些属性对去噪最重要，并有助于理解不同分数架构为何表现出不同的生成行为。

Conclusion: 由于分数函数可基于数据分布的矩进行解析求解，我们可以开始理解数据分布如何与分数网络相互作用，从而产生扩散模型中观察到的行为。

Original Abstract: Score-based generative models have had remarkable success over the last decade in generating a diverse set of visually plausible images. A variety of architectures including CNNs, U-Nets, and Transformers have been used as the score-approximation network in such diffusion modeling; however, to date, relatively little is known about how these architectural choices impact generative behavior. In this work, to provide insight into this area, we propose an analytically solvable parameterization of the score function using an expansion in a 2D orthogonal wavelet basis. In particular, we derive interpretable optimal score functions in terms of the moments of the data distribution. We use this parametrization to provide an architecture-agnostic, moment-based analysis that reveals which attributes of the data distribution tend to matter most for denoising. Our score machine is flexible enough to partially mimic the relevant inductive biases of multiple architectures, including U-Nets, and CNNs, taking a step towards understanding why different score architectures can exhibit distinct generative behavior. Since our score is solvable in terms of the moments of the data, we can begin to understand how the data distribution interacts with the score network to produce the behavior we observe in diffusion models.

中文摘要翻译: 基于分数的生成模型在过去十年中在生成多样化的视觉上合理的图像方面取得了显著成功。包括CNN、U-Net和Transformer在内的多种架构已被用作此类扩散建模中的分数近似网络；然而，迄今为止，关于这些架构选择如何影响生成行为，我们知之甚少。为了深入理解这一领域，本文提出了一种基于二维正交小波基展开的分数函数可解析参数化方法。具体而言，我们根据数据分布的矩推导出可解释的最优分数函数。利用这种参数化，我们提供了一种与架构无关的、基于矩的分析，揭示了数据分布中哪些属性对去噪最为关键。我们的分数机器足够灵活，能够部分模仿包括U-Net和CNN在内的多种架构的相关归纳偏置，向理解不同分数架构为何表现出截然不同的生成行为迈出了一步。由于我们的分数函数可基于数据分布的矩进行解析求解，我们可以开始理解数据分布如何与分数网络相互作用，从而产生我们在扩散模型中观察到的行为。

</details>


### [343] [Orthogonality and Dimensionality in Airline Cluster Analysis using PCA and Kernel PCA](https://arxiv.org/abs/2606.08322)
*Andreas Schlapbach*

Main category: cs.LG

TL;DR: 本研究复现并扩展了Renold等人（2023）对美国航空业利润周期的聚类分析，发现六聚类分类在几何上是稳健的，但数据集结构上仅支持三个聚类，原七维空间中的共线性抑制了该信号。

<details>
  <summary>Details</summary>

Motivation: 验证Renold等人（2023）提出的六聚类分类法的几何稳健性，并检测数据流形是否存在非线性结构，同时评估数据集内在的聚类数。

Method: 使用k-means聚类分别在原始7维空间、3维和4维主成分得分空间进行复现；应用六种核函数（含线性基线）的核主成分分析进行非线性检测；采用轮廓系数评估最佳聚类数。

Result: 六聚类分类在3维主成分空间中与7维原始空间产生逐比特相同的聚类分配；所有核函数均在2维空间中保持六聚类结构；非线性核将COVID年份（C_3）从与峰值利润簇重叠调整为仅与金融危机后簇重叠，证实数据流形本质为线性；轮廓系数显示数据集仅支持3个聚类而非6个，原始7维空间中的共线性掩盖了这一信号。

Conclusion: Renold等人的六聚类分类在几何上是稳健的，数据流形本质为线性无隐藏曲率，但原始七维空间中的共线性导致轮廓系数未能识别出k=3才是结构上更合理的聚类数。

Original Abstract: To characterize the US airline profit cycles from 1995 to 2020, the authors of Renold et al. (2023) combine k-means clustering, principal component analysis, and system dynamic modelling. We replicate their clustering experiment in three spaces -- the original 7-dimensional raw-variable space, a 3-dimensional PC score space, and a 4-dimensional PC score space using their dataset gratefully included in the paper. We show that the six-cluster taxonomy is geometrically robust: k-means in 3-PC space produces bit-for-bit identical cluster assignments relative to 7D raw space. As a nonlinearity check we apply kernel PCA under six kernels spanning three families plus a linear baseline. All six kernels preserve the six-cluster assignment in 2D. A 1D diagnostic tightens this: the linear kernel conflates the COVID year C_3 with the peak-profit cluster C_0, whereas all five non-baseline kernels shift C_3 to overlap only the post-financial-crisis cluster C_5. Agreement across the kernel families confirms an intrinsically linear manifold with no hidden curvature. The silhouette criterion reveals that the dataset structurally supports only three clusters, not six. Collinearity in the raw 7D space suppresses the silhouette signal that would otherwise identify k=3 as the structurally motivated choice.

中文摘要翻译: 为刻画1995年至2020年间美国航空业利润周期，Renold等人（2023）结合了k均值聚类、主成分分析和系统动力学建模。我们利用其论文中慷慨提供的数据集，在三个空间——原始7维变量空间、3维主成分得分空间以及4维主成分得分空间——复现了其聚类实验。我们证明六聚类分类法在几何上是稳健的：在3维主成分空间中进行k均值聚类，与在7维原始空间中相比，可产生逐比特完全一致的聚类分配。作为非线性检验，我们应用了涵盖三个核族外加一个线性基线的六种核函数的核主成分分析。所有六种核函数均在2维空间中保持了六聚类分配。一个1维诊断进一步收紧了这一结论：线性核将COVID年份C_3与峰值利润簇C_0混淆，而所有五种非基线核均将C_3移至仅与金融危机后簇C_5重叠。各核族之间的一致性证实了数据流形本质为线性，不存在隐藏曲率。轮廓准则揭示，该数据集在结构上仅支持三个聚类，而非六个。原始7维空间中的共线性抑制了原本能够识别k=3作为结构上合理选择的轮廓信号。

</details>


### [344] [GENERIC-FNO: Embedding Energy Conservation and Entropy Production into Fourier Neural Operators](https://arxiv.org/abs/2606.08343)
*Jason Sulskis,Sathya Ravi*

Main category: cs.LG

TL;DR: 本文提出GENERIC-FNO，首个在函数空间中嵌入完整非平衡热力学GENERIC（度量-辛）结构的神经算子，通过构造性方法精确满足简并条件，实现能量守恒与熵增的严格保证，在多种PDE上验证了其结构保持能力和竞争性能。

<details>
  <summary>Details</summary>

Motivation: 现有结构保持神经算子最多只能强制执行单一守恒律或可逆（哈密顿）结构，而热力学一致的学习仅限于有限维、图或粒子系统。缺乏能够在函数空间中同时嵌入完整GENERIC结构（可逆能量守恒动力学与不可逆熵产生动力学，且两者通过简并条件耦合）的神经算子。

Method: 将能量和熵泛函学习为神经算子，将泊松算子与摩擦算子参数化为对角傅里叶乘子，并夹在秩一投影之间，通过构造方式精确强制执行简并条件，无需惩罚项、更新投影或残差项。还引入了规范不变的耗散诊断方法，可独立于学习的泛函分离可逆与耗散动力学。

Result: 简等条件在机器精度（残差~10^-13）下对任意初始化、维度或分辨率成立；显式时间推进仅引入O(dt^2)的小漂移（每步残差~10^-6）。在三种算子主干（1D/2D FNO和DeepONet）和四个PDE上，在4倍超分辨率范围（64到256）零样本保持精确结构保证，恢复物理耗散的正确排序，与强无约束和能量惩罚基线竞争，在部分问题上以更少参数取得更优性能。

Conclusion: GENERIC-FNO成功弥合了热力学一致学习在函数空间中的空白，通过构造性简并条件保证实现了可证明的结构保持，同时保持了良好的预测精度和泛化能力，为物理信息神经算子的结构保持设计提供了新范式。

Original Abstract: We introduce GENERIC-FNO, the first neural operator to embed the full GENERIC (metriplectic) structure of nonequilibrium thermodynamics -- reversible, energy-conserving dynamics and irreversible, entropy-producing dynamics coupled through the degeneracy conditions -- directly in function space. Existing structure-preserving neural operators enforce at most a single conservation law or reversible (Hamiltonian) structure, while thermodynamically consistent learning has been confined to finite-dimensional, graph, or particle systems. GENERIC-FNO closes this gap: it learns the energy and entropy functionals as neural operators and parameterizes the Poisson and friction operators as diagonal Fourier multipliers sandwiched between rank-one projections that enforce the degeneracy conditions exactly, by construction, with no penalty term, update projection, or residual. The degeneracy identities hold to machine precision (residuals ~10^-13) for any initialization, dimension, or resolution, so the continuous-time dynamics conserve the learned energy and produce entropy exactly; the explicit time stepping adds only a small O(dt^2) drift (per-step residual ~10^-6). We further note that the (E,S,L,M) decomposition of a given flow is not unique, and introduce a gauge-invariant dissipation diagnostic separating reversible from dissipative dynamics independently of the learned functionals. Across three operator backbones (1D/2D FNOs and DeepONet) and four PDEs spanning reversible, dissipative, and mixed regimes, GENERIC-FNO preserves its exact structural guarantees zero-shot across a 4x super-resolution range (64 to 256), recovers the ground-truth ordering of physical dissipation, and is competitive with strong unconstrained and energy-penalized baselines, outperforming them on several dissipative and mixed problems at comparable or fewer parameters.

中文摘要翻译: 我们提出GENERIC-FNO，这是首个在函数空间中嵌入非平衡热力学完整GENERIC（度量-辛）结构的神经算子——该结构包含可逆的能量守恒动力学和不可逆的熵产生动力学，两者通过简并条件耦合。现有的结构保持神经算子最多只能强制执行单一守恒律或可逆（哈密顿）结构，而热力学一致的学习仅限于有限维、图或粒子系统。GENERIC-FNO弥合了这一空白：它将能量和熵泛函学习为神经算子，并将泊松算子与摩擦算子参数化为对角傅里叶乘子，这些乘子夹在秩一投影之间，通过构造方式精确强制执行简并条件，无需惩罚项、更新投影或残差。简并恒等式在任意初始化、维度或分辨率下均达到机器精度（残差约10^-13），因此连续时间动力学精确守恒学习到的能量并产生熵；显式时间推进仅引入O(dt^2)的小漂移（每步残差约10^-6）。我们进一步指出，给定流形的(E,S,L,M)分解并不唯一，并引入了一种规范不变的耗散诊断方法，可独立于学习到的泛函分离可逆与耗散动力学。在三种算子主干（1D/2D FNO和DeepONet）和四个涵盖可逆、耗散及混合机制的PDE上，GENERIC-FNO在4倍超分辨率范围（64到256）内零样本保持其精确结构保证，恢复物理耗散的ground-truth排序，并与强无约束和能量惩罚基线竞争，在多个耗散和混合问题上以相当或更少的参数取得了更优性能。

</details>


### [345] [Generative Frontier Planning for Adaptive Peer-Referral Recruitment under Covariate-Dependent Arrivals](https://arxiv.org/abs/2606.08360)
*Lingkai Kong,Hezi Jiang,Andrew Ma,Keyu Wang,Akseli Kangaslahti,Milind Tambe*

Main category: cs.LG

TL;DR: 本文针对隐藏人群的同伴推荐招募系统（如受访者驱动抽样）中的资源分配问题，提出了一种基于模型的规划方法GFP（生成式前沿规划）。该方法利用条件生成模型和删失计数模型刻画推荐者依赖的招募动态，并通过确定性备份和边际贪婪分配实现高效规划，在真实数据校准的仿真环境中优于随机、强化学习和独立同分布动态规划基线。

<details>
  <summary>Details</summary>

Motivation: 现有工作假设推荐样本来自同质总体的独立同分布抽样，忽略了真实同伴招募中存在的同质性（homophily）和共享情境。为加速疾病传播研究中的隐藏人群招募，需要更现实的模型来刻画推荐能力与推荐者条件相关的招募动态，并解决由此带来的复杂规划问题。

Method: 提出Generative Frontier Planning (GFP)：1) 使用删失计数模型和条件生成模型从数据中学习推荐者依赖的招募动态；2) 设计潜在协变量覆盖值替代函数，使下一前沿的期望值仅依赖于离线摊销的有限维摘要；3) 确定性备份替代每步蒙特卡洛采样；4) 利用边际效用递减的单调结构，采用边际贪婪分配实现每轮问题的(1-1/e)近似。

Result: 在基于真实受访者驱动抽样数据集校准的仿真环境中，GFP在四个折扣因子设置下均优于随机基线、强化学习基线和独立同分布动态规划基线。

Conclusion: 通过引入推荐者依赖的生成模型和具有替代函数结构的确定性规划，GFP有效解决了隐藏人群招募中资源分配的计算挑战，在近似保证下实现了更优的招募效率。

Original Abstract: Peer-referral recruitment systems such as respondent-driven sampling are critical for studying and intervening on hidden populations affected by infectious diseases. To accelerate recruitment, public health agencies must adaptively allocate limited referral resources across multiple rounds, where current decisions shape both the number and the covariates of future recruits. Prior work makes this problem tractable by assuming that referrals are drawn i.i.d.\ from a homogeneous population, an assumption that ignores the homophily and shared context that drive real peer recruitment. We instead consider a more realistic model in which both referral capacity and the covariates of newly referred individuals are conditioned on the referrer, learned from data with a censored count model and a conditional generative model. The resulting planning problem is challenging because each candidate allocation induces a different distribution over future recruits. We propose \emph{Generative Frontier Planning} (GFP), a model-based planner that replaces per-step Monte-Carlo sampling with a deterministic backup over a latent covariate-coverage value surrogate. The surrogate is designed so that the expected value of the next frontier depends on the offspring generative model only through finite-dimensional summaries that are amortized offline, and so that the resulting per-round objective is monotone with diminishing returns. Together, these two properties make planning tractable: the deterministic backup eliminates Monte-Carlo sampling, and the diminishing-returns structure lets a marginal greedy allocation achieve a \((1-1/e)\)-approximation for the per-round problem. On a simulation environment calibrated to a real respondent-driven sampling dataset, GFP outperforms random, reinforcement-learning, and i.i.d.\ dynamic-programming baselines across four discount factors.

中文摘要翻译: 基于同伴推荐的招募系统（如受访者驱动抽样）对于研究和干预受传染病影响的隐藏人群至关重要。为加速招募，公共卫生机构必须在多轮中自适应地分配有限的推荐资源，其中当前的决策既影响未来招募的数量，也影响其协变量分布。现有工作通过假设推荐样本来自同质总体的独立同分布抽样来使问题可解，这一假设忽略了驱动真实同伴招募的同质性和共享情境。本文考虑了一个更现实的模型，其中推荐能力和新推荐个体的协变量均以推荐者为条件，并通过删失计数模型和条件生成模型从数据中学习。由此产生的规划问题具有挑战性，因为每种候选分配都会导致未来招募对象的不同分布。我们提出生成式前沿规划（Generative Frontier Planning, GFP），一种基于模型的规划器，用潜在协变量覆盖值替代函数的确定性备份替代每步蒙特卡洛采样。该替代函数的设计使得下一前沿的期望值仅通过离线摊销的有限维摘要依赖于后代生成模型，并且使得每轮目标函数具有单调性和边际效用递减性。这两个性质共同使规划变得可处理：确定性备份消除了蒙特卡洛采样，边际效用递减结构使得边际贪婪分配能够在每轮问题上实现(1-1/e)近似。在基于真实受访者驱动抽样数据集校准的仿真环境中，GFP在四个折扣因子设置下均优于随机基线、强化学习基线和独立同分布动态规划基线。

</details>


### [346] [Pre-Intervention Prediction of Sparse Autoencoder Steering Side Effects](https://arxiv.org/abs/2606.08365)
*Evan Duan*

Main category: cs.LG

TL;DR: 本文提出一个预干预筛选框架，利用SAE特征统计量在干预前预测特征引导的副作用（效果不稳定性和附带扩散），实验验证该预测信号在多种模型和字典设置下有效，但预测效果因模型和字典设置而异。

<details>
  <summary>Details</summary>

Motivation: 稀疏自编码器（SAE）特征越来越多用于引导语言模型行为，但特征引导常不干净：同一干预在不同上下文中表现不一致，且会扰动无关特征。作者希望能在实际干预前预测这些副作用，从而选择更清洁的特征进行引导。

Method: 提出预干预筛选框架，从干预前计算的SAE特征统计量预测引导模块性（效果稳定性和附带扩散）。统计量包括解码器几何、激活统计量、共激活结构和直接logit足迹等，并与仅基于频率和激活幅度的基线方法进行比较。在GPT-2-small、Pythia-70M-deduped、Gemma-2-2B和Llama-3.1-8B上，使用ReLU、JumpReLU和TopK三种SAE字典进行验证。

Result: 解码器几何、激活统计量、共激活结构和直接logit足迹等特征统计量在预测引导模块性上优于频率和激活幅度基线。信号在GPT-2-small、Pythia-70M和Llama-3.1-8B中最强（残差化后仍显著），在Gemma-2-2B上较弱。持出测试显示，按预测清洁度对未见特征排序可选择更清洁的特征，但有效维度因模型而异：GPT-2综合改进最好，Pythia主要改进稳定性，Llama主要改进附带扩散，Gemma仅部分改进。字典宽度从32K扩大到128K时预测信号持续存在，但筛选收益稳定性下降。

Conclusion: SAE特征引导的副作用可以提前预测，但有效的预测因子和可迁移的模块性维度因模型和字典设置而异，需要根据具体设置定制筛选策略。

Original Abstract: Sparse autoencoder (SAE) features are increasingly used to steer language models, but feature steering is rarely clean: the same intervention can behave inconsistently across contexts and perturb unrelated features. We introduce a pre-intervention screening framework for forecasting SAE steering side effects from feature statistics computed before steering. We operationalize side effects along two axes of steering modularity, effect stability and collateral spread, and evaluate GPT-2-small, Pythia-70M-deduped, Gemma-2-2B, and Llama-3.1-8B across ReLU, JumpReLU, and TopK SAE dictionaries. Across these settings, decoder geometry, activation statistics, co-activation structure, and direct-logit footprint predict steering modularity better than frequency-only and activation-magnitude baselines. The signal is strongest in GPT-2-small, Pythia-70M, and Llama-3.1-8B, where it survives residualization against magnitude-related confounds, and weaker in Gemma-2-2B. Held-out screening shows that ranking unseen features by predicted cleanliness can select features that steer more cleanly on fresh contexts, but the successful axis varies by setting: GPT-2 improves most cleanly, Pythia improves mainly on stability, Llama mainly on collateral, and Gemma only partially. A controlled Llama Scope width comparison shows that the predictive signal persists under a 32K-to-128K dictionary-width change, although the screening payoff becomes less stable. Overall, SAE steering side effects are predictable in advance, but the useful predictor signature and transferred modularity axis are model- and dictionary-setting dependent.

中文摘要翻译: 稀疏自编码器（SAE）特征被越来越多地用于引导语言模型，但特征引导往往不够干净：同一干预在不同上下文中表现不一致，且会扰动无关特征。本文提出一个预干预筛选框架，通过干预前计算的SAE特征统计量来预测SAE引导的副作用。我们从两个维度对副作用进行操作化，即引导模块性的效果稳定性和附带扩散，并在GPT-2-small、Pythia-70M-deduped、Gemma-2-2B和Llama-3.1-8B上，使用ReLU、JumpReLU和TopK三种SAE字典进行评估。在这些设置下，解码器几何、激活统计量、共激活结构和直接logit足迹在预测引导模块性方面优于仅基于频率和激活幅度的基线方法。该信号在GPT-2-small、Pythia-70M和Llama-3.1-8B中最强（残差化处理掉幅度相关混淆后仍显著），在Gemma-2-2B上较弱。持出筛选实验表明，按预测清洁度对未见特征排序可以选择在全新上下文中更清洁地引导的特征，但成功维度因设置而异：GPT-2综合改进最干净，Pythia主要改进稳定性，Llama主要改进附带扩散，Gemma仅部分改进。在Llama Scope上的受控宽度比较表明，字典宽度从32K扩大到128K时预测信号持续存在，但筛选收益的稳定性下降。总体而言，SAE引导的副作用可以提前预测，但有效的预测因子和可迁移的模块性维度取决于模型和字典设置。

</details>


### [347] [An Information-Theoretic Definition for Open-Ended Learning](https://arxiv.org/abs/2606.08369)
*Wanqiao Xu,Yifan Zhu,Benjamin Van Roy*

Main category: cs.LG

TL;DR: 本文提出了基于比特当量（bit-equivalent）的信息论定义来刻画开放环境（open-endedness），证明经典赌博机环境不是开放的，并设计了一个开放赌博机环境及相应的开放学习算法。

<details>
  <summary>Details</summary>

Motivation: 目前关于AI系统在开放环境中持续扩展能力的研究虽多，但缺乏对开放性的统一定义以及智能体应如何探索开放环境的理论框架。

Method: 提出比特当量（bit-equivalent）这一信息论概念，量化达到每个期望奖励水平所需的信息量；基于此定义开放性：若智能体能在环境中实现比特当量的线性增长，则该环境是开放的。随后构建了一个满足该条件的赌博机环境，并设计了相应的开放学习算法。

Result: 理论上证明经典赌博机环境不是开放的；构造了一个开放赌博机环境，并给出算法在该环境中实现开放学习。

Conclusion: 比特当量可作为定义和量化开放性的有效工具，为开放环境下的持续学习提供了理论基础。

Original Abstract: A growing body of work points to the great promise of AI systems that can continually expand their capabilities as they operate in an open-ended environment. But yet there is no coherent definition of open-endedness or theory about how an agent ought to explore an open-ended environment. We introduce an information-theoretic definition based on a new concept -- the ${\textit bit-equivalent}$ -- which quantifies the information required to attain each level of expected reward. We consider an environment to be open-ended if an agent can attain linear growth in the bit-equivalent. We establish that classical bandit environments are not open-ended and formulate a bandit environment that is. We also introduce an algorithm that achieves open-ended learning in this environment.

中文摘要翻译: 越来越多的研究表明，能够在开放环境中持续扩展能力的AI系统具有巨大潜力。然而，目前对于开放性的统一定义，以及智能体应如何探索开放环境的理论尚不明确。我们提出了一种基于新概念——比特当量（bit-equivalent）——的信息论定义，该概念量化了达到每个期望奖励水平所需的信息量。如果一个环境能使智能体在比特当量上实现线性增长，则我们认为该环境是开放的。我们证明经典赌博机环境不是开放的，并构造了一个开放的赌博机环境。我们还提出了一种在该环境中实现开放学习的算法。

</details>


### [348] [Few-step Cofolding with All-Atom Flow Maps](https://arxiv.org/abs/2606.08375)
*Gianluca Scarpellini,Ron Shprints,Peter Holderrieth,Juno Nam,Pranav Murugan,Rafael Gómez-Bombarelli,Tommi Jaakola,Maruan Al-Shedivat,Nicholas Matthew Boffi,Avishek Joey Bose*

Main category: cs.LG

TL;DR: 本文提出DeCAF框架，通过将全原子共折叠扩散模型蒸馏为流映射，在仅需少量推理步骤的情况下生成高质量3D生物分子复合物结构，并引入基于奖励引导的推理时搜索框架进一步提升采样质量。

<details>
  <summary>Details</summary>

Motivation: 现有全原子生成模型（如扩散模型）需要昂贵的迭代推理过程，无论是常规部署还是推理时的搜索技术都计算成本过高，亟需一种高效且保持高保真度的生成方法。

Method: 基于去噪器的流映射公式，引入端点损失以自然支持SE(3)刚性对齐；推导变量变换使其能与EDM架构的σ空间噪声调度兼容，从而直接从预训练共折叠扩散模型中蒸馏；构建基于奖励引导搜索的推理时采样框架。

Result: DeCAF-Boltz在严格NFE预算下，在Runs N' Poses数据集上较Boltz-1x在RMSD和物理有效性分数上均有提升；在PoseBusters上展示出更优的Pareto前沿。DeCAF-Pearl在成功率上匹配教师模型，仅需1/5的NFE。

Conclusion: DeCAF框架成功将全原子共折叠扩散模型蒸馏为高效流映射，大幅减少推理步骤的同时保持了生成质量，并可通过推理时搜索进一步提升性能。

Original Abstract: All-atom generative modeling of 3D biomolecular complexes has emerged as the dominant paradigm for predicting the structure of proteins and protein-ligand systems. Generating structures at the atomic level of fidelity, however, typically requires expensive iterative diffusion rollouts, making both conventional deployment and inference-time search techniques computationally costly. In this paper, we introduce the Denoiser Cofolding All-Atom Flowmap (DeCAF) framework for distilling state-of-the-art all-atom cofolding models into all-atom flow maps that produce high-quality samples in only a few inference steps. We build DeCAF on a denoiser-based formulation of flow maps with endpoint losses that naturally support SE(3) rigid alignment, which we show is critical for training accurate models. We further derive a simple change of variables that lets DeCAF operate in the σ-space noise schedule of EDM-style architectures, enabling direct distillation from pretrained cofolding diffusion models. Equipped with DeCAF's flowmap lookahead, we introduce a purpose-built inference-time framework that improves sampling through reward-guided search. Empirically, DeCAF-Boltz statistically improves over Boltz-1x in both accuracy (RMSD) and physical validity scores of protein-ligand poses at strict NFE budgets on the challenging Runs N' Poses, while also showing a more optimal Pareto frontier across all inference compute budgets on PoseBusters. Distilling the state-of-the-art Pearl cofolding model, DeCAF-Pearl outperforms diffusion-based cofolding models and matches its teacher on success rate while using 5x fewer NFEs. We release our code at https://github.com/genesistherapeutics/decaf.

中文摘要翻译: 三维生物分子复合物的全原子生成建模已成为预测蛋白质及蛋白质-配体系统结构的主流范式。然而，生成原子级别保真度的结构通常需要昂贵的迭代扩散推理过程，这使得常规部署和推理时搜索技术都面临高昂的计算成本。本文提出去噪器共折叠全原子流映射（DeCAF）框架，将最先进的全原子共折叠模型蒸馏为全原子流映射，仅需少量推理步骤即可生成高质量样本。DeCAF基于带端点损失的去噪器流映射公式构建，该公式天然支持SE(3)刚性对齐，这对训练精确模型至关重要。我们进一步推导了一个简单的变量变换，使DeCAF能够在EDM架构的σ空间噪声调度中运行，从而实现对预训练共折叠扩散模型的直接蒸馏。借助DeCAF的流映射前瞻能力，我们引入了一个专门设计的推理时框架，通过奖励引导搜索来改进采样质量。实验结果表明，在具有严格NFE预算的挑战性Runs N' Poses数据集上，DeCAF-Boltz在蛋白质-配体构象的RMSD和物理有效性分数上均较Boltz-1x有统计显著提升，同时在PoseBusters的所有推理计算预算上展现出更优的Pareto前沿。通过蒸馏最先进的Pearl共折叠模型，DeCAF-Pearl在使用仅1/5的NFE情况下，在成功率上超越了基于扩散的共折叠模型并与教师模型持平。我们已在https://github.com/genesistherapeutics/decaf 开源代码。

</details>


### [349] [RiskNet: A large-scale dataset of AI risk incidents from news with alignment and multi-dimensional annotations](https://arxiv.org/abs/2606.08376)
*Leihan Zhang,Wecheng Ye,Xianlong Ma,Haochuan Liu,Yang Li,Qianyu Zhang,Jinliang Chen,Qiang Yan*

Main category: cs.LG

TL;DR: RiskNet是一个从大规模多语言新闻源构建的AI风险事件数据集，通过结构化流水线进行风险新闻识别、事件筛选、事件对齐和多维分类，支持AI安全、治理和风险分析等下游研究。

<details>
  <summary>Details</summary>

Motivation: 现有AI治理框架多停留在高层原则层面，缺乏大规模、持续性的实证资源来追踪和分析现实世界中的AI风险事件；现有的事件集合多为人工策展、规模较小，不足以支持数据驱动的持续监控和计算分析。

Method: 构建了一个结构化流水线，包括：AI风险新闻识别、事件级报告筛选、事件对齐（将分散的新闻报告聚类为同一事件）、以及多维度事件分类。数据集通过在线平台提供浏览和探索功能。

Result: 覆盖数亿条源记录，生成大规模AI风险相关报告集合，包含对齐的事件聚类和带标注的基准子集，并提供事件分类、事件对齐和事件级风险标注的基准数据集。

Conclusion: RiskNet提供了一个结构化的、可复用的实证资源，弥合了高层治理原则与AI风险事件实际记录之间的差距，支持AI安全、治理、风险分析、基准测试以及跨源纵向分析。

Original Abstract: As artificial intelligence (AI) systems are increasingly deployed across socially consequential domains, reports of AI-related harms and failures have grown in frequency and diversity. Although existing governance frameworks articulate high-level principles for responsible AI, large-scale empirical resources for tracking and analyzing real-world AI risk incidents remain limited. Existing incident collections are often manually curated, relatively small in scale, and insufficient for continuous, data-driven monitoring and downstream computational analysis. To address this need, we present RiskNet, a large-scale dataset of AI risk incidents constructed from large-scale multilingual news sources. RiskNet applies a structured pipeline for AI risk news identification, event-level report screening, incident alignment, and multi-dimensional incident classification. The resulting resource organizes dispersed news reports into incident-centered records and provides benchmark datasets for event classification, incident alignment, and incident-level risk labeling. In its current release, RiskNet covers hundreds of millions of source records and yields a large-scale collection of AI risk-related reports, including aligned incident clusters and annotated benchmark subsets. The dataset is also accessible through an online platform for browsing and exploration. We describe the data sources, processing workflow, taxonomy design, and technical validation of the resource. RiskNet is intended to support downstream research on AI safety, governance, risk analysis, and benchmarking, as well as longitudinal and cross-source analyses of AI-related harms. By providing a structured and reusable empirical resource, RiskNet helps bridge the gap between high-level governance principles and the documented realities of AI risk incidents.

中文摘要翻译: 随着人工智能（AI）系统日益部署到具有社会影响的重要领域，与AI相关的危害和失败报告在频率和多样性上不断增长。尽管现有治理框架提出了负责任AI的高层原则，但用于追踪和分析现实世界AI风险事件的大规模实证资源仍然有限。现有的事件集合通常为人工策展、规模相对较小，不足以支持持续的数据驱动监控和下游计算分析。为应对这一需求，我们提出了RiskNet，这是一个从大规模多语言新闻源构建的AI风险事件数据集。RiskNet应用了结构化的流水线，包括AI风险新闻识别、事件级报告筛选、事件对齐以及多维度事件分类。该资源将分散的新闻报告组织为以事件为中心的记录，并为事件分类、事件对齐和事件级风险标注提供基准数据集。在当前版本中，RiskNet覆盖了数亿条源记录，生成了大规模AI风险相关报告集合，包括对齐的事件聚类和带标注的基准子集。该数据集还通过在线平台提供浏览和探索功能。我们描述了该资源的数据来源、处理流程、分类体系设计和技术验证。RiskNet旨在支持AI安全、治理、风险分析和基准测试等下游研究，以及对AI相关危害的纵向和跨源分析。通过提供结构化的、可复用的实证资源，RiskNet有助于弥合高层治理原则与AI风险事件实际记录之间的差距。

</details>


### [350] [STAR-KV: Low-Rank KV Cache Compression via Soft Thresholding for Adaptive Rank Control](https://arxiv.org/abs/2606.08382)
*Priyansh Bhatnagar,Ashkan Moradifirouzabadi,Se-Hyun Yang,SeungJae Lee,Jungwook Choi,Mingu Kang*

Main category: cs.LG

TL;DR: STAR-KV提出了一种自适应低秩KV缓存压缩框架，通过可微阈值机制、混合分解策略和低秩感知混合精度量化，在多种LLM上实现高达75%的KV缓存压缩，结合量化后整体KV缓存减少可达20倍，并带来最高6.9倍的注意力模块加速和3.1倍的端到端生成吞吐提升。

<details>
  <summary>Details</summary>

Motivation: 现有低秩投影方法依赖固定或启发式秩选择，难以在实现激进压缩的同时保持低精度损失，缺乏细粒度的秩控制能力。

Method: 1) 可微阈值机制实现注意力头级和块级的最优秩选择；2) 混合分解策略根据键投影和值投影的敏感性分别应用不同的低秩分解方式；3) 低秩感知混合精度量化利用数据统计实现近乎无损的低比特量化。

Result: 在多种LLM和基准测试上，STAR-KV实现高达75%的KV缓存压缩，结合量化后整体KV缓存减少达20倍；基于Triton的自定义GPU内核实现注意力模块最高6.9倍加速，端到端生成吞吐提升3.1倍。

Conclusion: STAR-KV通过自适应细粒度秩控制的低秩压缩框架，有效解决了KV缓存压缩中秩选择难题，兼顾了高压缩率与低精度损失，并在实际部署中取得了显著的速度提升。

Original Abstract: Low-rank projection has emerged as a promising approach for compressing the KV cache by exploiting hidden-dimension redundancy. However, prior methods rely on fixed or heuristic rank selection and struggle to achieve aggressive compression with minimal accuracy degradation. We propose STAR-KV, an adaptive low-rank KV cache compression framework with fine-grained rank control. STAR-KV encompasses 1) a differentiable thresholding mechanism that enables optimal rank selection at both attention-head and block levels, 2) a hybrid decomposition strategy that applies different low-rank factorizations according to the sensitivity of key and value projections, and 3) a low-rank-aware mixed precision quantization that leverages data statistics for near lossless low-bit quantization. Evaluated across multiple LLMs and benchmarks, STAR-KV achieves up to 75% KV cache compression and up to 20x overall KV cache reduction when combined with quantization. Enabled by custom Triton-based GPU kernels, STAR-KV delivers up to 6.9x speedup for the attention module and 3.1x end-to-end generation throughput. Our code is publicly available at: https://github.com/PriyanshBhatnagar/STAR-KV.

中文摘要翻译: 低秩投影通过利用隐藏维度冗余已成为压缩KV缓存的一种有前景的方法。然而，先前的方法依赖固定或启发式秩选择，难以在实现激进压缩的同时保持最小的精度下降。我们提出了STAR-KV，一种具有细粒度秩控制的自适应低秩KV缓存压缩框架。STAR-KV包含：1）一种可微阈值机制，能够在注意力头级和块级实现最优秩选择；2）一种混合分解策略，根据键投影和值投影的敏感性应用不同的低秩分解方式；3）一种低秩感知混合精度量化方法，利用数据统计实现近乎无损的低比特量化。在多种大语言模型和基准测试上的评估表明，STAR-KV实现了高达75%的KV缓存压缩，并且在与量化结合时整体KV缓存减少可达20倍。借助基于Triton的自定义GPU内核，STAR-KV在注意力模块上实现了高达6.9倍的加速，端到端生成吞吐量提升3.1倍。我们的代码已在https://github.com/PriyanshBhatnagar/STAR-KV公开。

</details>


### [351] [The Spectral Dynamics and Noise Geometry of Muon](https://arxiv.org/abs/2606.08388)
*Pierfrancesco Beneventano,Mahmoud Abdelmoneum,Tomaso Poggio*

Main category: cs.LG

TL;DR: 本文系统分析了Muon优化器（用极分解替换矩阵梯度）的优化偏差，证明其在特定假设下是一步熵最大化的选择，并揭示了其平坦谱偏置特性：在欠定回归中使归一化谱趋向等奇异值，在矩阵感知实验中与梯度下降行为不同，在NanoGPT预训练中优于AdamW但在小ViT中相反，表明Muon并非普遍优越，其平坦谱偏置在需要保持多个谱方向活跃时有益。

<details>
  <summary>Details</summary>

Motivation: 研究Muon优化器将矩阵梯度替换为极因子$UV^\top$后所产生的优化偏差，理解这种操作对梯度谱的平坦化效果如何在理论上和实践中影响模型优化过程。

Method: 通过理论分析（显式对齐假设下证明极更新是一步熵最大选择；在欠定回归模型中推导连续时间Muon的精确奇异值动力学）和实验验证（受控矩阵感知实验分离Muon与简单梯度缩放的差异；在NanoGPT和小ViT上进行预训练对比）。

Result: (1) 在显式对齐假设下，极更新是约束条件下熵最大化的选择；(2) 在欠定回归中，归一化谱在特定条件下趋向等非零奇异值；(3) 矩阵感知实验表明范数匹配的梯度下降无法再现Muon行为，验证了谱平坦化趋势；(4) NanoGPT预训练中Muon优于AdamW，稳定秩保持好、学习率平台宽；但在小ViT控制实验中排名反转。

Conclusion: Muon并非普遍优越于AdamW，其效果具有依赖性：Muon的平坦谱偏置在需要保持众多谱方向活跃的场景（如NanoGPT）中有帮助，而在其他场景（如小ViT）中则可能不如AdamW。

Original Abstract: Muon replaces a matrix gradient $G=UΣV^\top$ by its polar factor $UV^\top$. This keeps the singular directions selected by the gradient, but makes the update spectrum flat. We study the optimization bias created by this operation. Under explicit alignment assumptions, we prove that the polar update is the one-step entropy-maximizing choice among bounded updates that use the gradient singular directions and do not adapt to the current weight spectrum. In an underdetermined regression model, we derive exact singular-value dynamics for continuous-time Muon and identify a measurement-dependent condition under which the normalized spectrum moves toward equal nonzero singular values. This geometry also rules out a common low-rank interpretation: at fixed Frobenius norm, Muon's distinguished state has a flat spectrum, whereas nuclear-norm minimization favors spectral concentration. Controlled matrix-sensing experiments separate the effect from simple gradient rescaling, show that norm-matched gradient descent does not reproduce Muon, and recover the predicted flattening trend across broad ablations. In small NanoGPT pretraining, Muon preserves stable rank, has a broad learning-rate plateau, and improves validation loss relative to AdamW; in a matched small-ViT control, the ranking reverses. The resulting picture is regime-dependent: Muon is not universally superior, but its flat-spectrum bias can help when many spectral directions need to remain active.

中文摘要翻译: Muon将矩阵梯度$G=UΣV^\top$替换为它的极因子$UV^\top$。这一操作保留了梯度所选择的奇异方向，但使得更新谱变得平坦。本文研究了这一操作所产生的优化偏差。在显式对齐假设下，我们证明了在使用梯度奇异方向且不自适应当前权重谱的有界更新中，极更新是一步熵最大化的选择。在欠定回归模型中，我们推导了连续时间Muon的精确奇异值动力学，并识别出一个依赖于测量的条件，在该条件下归一化谱趋于相等的非零奇异值。这种几何特性也排除了一种常见的低秩解释：在固定Frobenius范数下，Muon的显著状态具有平坦谱，而核范数最小化则倾向于谱集中。受控的矩阵感知实验将Muon与简单梯度缩放的效果区分开来，表明范数匹配的梯度下降无法再现Muon的行为，并在广泛的消融研究中恢复出预测的谱平坦化趋势。在小型NanoGPT预训练中，Muon保持了稳定的秩，具有较宽的学习率平台，并相对于AdamW改善了验证损失；而在匹配的小型ViT控制实验中，排名则发生了反转。由此得出的图景是依赖具体情境的：Muon并非普遍优越，但当许多谱方向需要保持活跃时，其平坦谱偏置可以发挥积极作用。

</details>


### [352] [When Are Neural Interaction Discoveries Real? Identifiability, Recoverability, and a Pre-Fit Diagnostic](https://arxiv.org/abs/2606.08390)
*Valentina Kuskova,Dmitry Zaytsev,Michael Coppedge*

Main category: cs.LG

TL;DR: 本文研究神经时序模型中变量交互作用的可识别性问题，发现交互作用的可恢复性取决于观测输入支撑集的几何结构（而非神经网络架构），并提出有效秩作为拟合前的诊断工具，以及两种子稳定性检查作为实际操作测试。

<details>
  <summary>Details</summary>

Motivation: 当神经时序模型报告一个变量调节另一个变量对目标的影响时，这种发现的交互作用是数据本身的属性，还是模型灵活性的伪影？作者认为这本质上是可识别性问题，受观测输入支撑集的几何结构而非具体神经架构的支配。

Method: 在乘法门控扩展的神经加性向量自回归（GNAVAR）模型中研究该问题。证明表示能力不等于可识别性，提出归一化最小GNAVAR分解的总体可识别性定理，给出显式支撑条件（包括共享调节器设置）。推导出有效秩诊断指标和两种子稳定性检查方法。

Result: 依赖输入会在边特定的交互项之间引入泄漏；低维支撑集允许多个不同的交互分解在观测数据上一致但在其他区域不同。有效秩可以预测交互恢复的可行性；两种子稳定性检查提供实际操作测试；支撑条件将实证结果组织为理论预测的三种状态。

Conclusion: 交互作用可恢复性取决于支撑几何结构；有效秩提供了实用的拟合前诊断工具；不同独立拟合之间的不稳定性是非可识别交互发现的特征性信号。可识别性现象、支撑条件和不稳定性信号是模型无关的，GNAVAR是使它们可证明的载体。

Original Abstract: When a neural time-series model reports that one variable modulates another's effect on a target, is the discovered interaction a property of the data or an artifact of model flexibility? We argue that this is fundamentally a question of identifiability, governed by the geometry of the observed input support rather than by the specific neural architecture. We study the problem in a multiplicative-gating extension of neural additive vector autoregression (GNAVAR), in which source contributions are modulated by other lagged variables. We show that representational capacity is not identifiability: dependent inputs induce leakage between edge-specific interaction terms, and low-dimensional support permits distinct interaction decompositions that agree on the observed data while differing elsewhere. We then prove a population identifiability theorem for normalized minimal GNAVAR decompositions under explicit support conditions, including settings with shared modulators. The theory yields a simple practitioner-facing diagnostic: the effective rank of the joint lag-block covariance predicts, before fitting, whether interaction recovery is feasible for a given candidate set. When the candidate set is unknown, a two-seed stability check provides a practical operational test. The same support condition organizes empirical outcomes into the three states predicted by the theory. Our results show that interaction recoverability depends on support geometry, that effective rank provides a practical pre-fit diagnostic, and that instability across independent fits is a characteristic signature of non-identifiable interaction discovery. The identifiability phenomenon, the support condition, and the instability signature are model-agnostic; GNAVAR is the vehicle that makes them provable.

中文摘要翻译: 当一个神经时序模型报告一个变量调节另一个变量对目标的影响时，这种发现的交互作用是数据本身的属性，还是模型灵活性的伪影？我们认为这从根本上是一个可识别性问题，受观测输入支撑集的几何结构而非具体神经架构的支配。我们在乘法门控扩展的神经加性向量自回归（GNAVAR）模型中研究该问题，其中源贡献由其他滞后变量调节。我们证明表示能力不等于可识别性：依赖输入会在边特定的交互项之间引入泄漏，低维支撑集允许多个不同的交互分解在观测数据上一致但在其他区域不同。然后我们在显式支撑条件下（包括共享调节器的设置）证明了归一化最小GNAVAR分解的总体可识别性定理。该理论产生了一个面向实践者的简单诊断：联合滞后块协方差的有效秩可以在拟合前预测给定候选集上交互恢复的可行性。当候选集未知时，两种子稳定性检查提供了实用的操作测试。相同的支撑条件将实证结果组织为理论预测的三种状态。我们的结果表明：交互可恢复性取决于支撑几何结构；有效秩提供了实用的拟合前诊断工具；不同独立拟合之间的不稳定性是非可识别交互发现的特征性信号。可识别性现象、支撑条件和不稳定性信号是模型无关的；GNAVAR是使它们可证明的载体。

</details>


### [353] [Provably Efficient Personalized Multi-Objective Bandits with Proactive Conversational Queries](https://arxiv.org/abs/2606.08410)
*Linfeng Cao,Ming Shi,Ness B. Shroff*

Main category: cs.LG

TL;DR: 本文提出MO-PQUCB算法，将用户主动查询提供的结构化偏好信号（如“便宜且干净的酒店”）与多目标bandit反馈相结合，通过Plackett-Luce子集选择模型建模查询信号，并利用移位不变正则化和双探索UCB解决偏好学习与奖励探索的混杂问题，理论证明查询可加速偏好估计并改善 regret 界，在 corrupted 查询下也给出了鲁棒估计器。

<details>
  <summary>Details</summary>

Motivation: 现有方法仅从效用反馈中推断用户偏好，导致偏好学习与奖励探索相互混杂。然而实际中用户常通过主动对话式查询（如“便宜且干净的酒店”）透露偏好，这种结构化信号未被现有方法利用。

Method: 提出MO-PQUCB混合算法，包含三个核心组件：(1) 基于Plackett-Luce子集选择模型对用户查询中的结构化偏好信号进行建模；(2) 移位不变正则化（shift-invariant regularization）将查询偏好的锚定信息与bandit反馈整合；(3) 双探索UCB（dual-exploration UCB）策略平衡探索与利用。针对查询被破坏（corrupted）的情况，还设计了鲁棒估计器。

Result: 理论证明主动查询能够加速偏好估计，且相比现有偏好感知的多目标MAB方法获得更优的regret界。在查询被破坏的场景下，给出了统计极限并在稀疏破坏时实现了近乎最优的性能。实验验证了理论和实际收益。

Conclusion: 主动查询提供的结构化偏好信号可以有效加速多目标bandit中的偏好学习，解决偏好学习与奖励探索的混杂问题；通过精心设计的正则化和双探索策略，所提方法在理论和实践上均优于现有方法。

Original Abstract: Personalized decision-making in multi-objective bandits requires learning user-specific trade-offs among competing objectives. Since arm utility depends on both unknown rewards and unknown preferences, existing methods infer preferences only from utility feedback, entangling preference learning with reward exploration. In practice, however, users often reveal their priorities through proactive conversational queries (e.g., "cheap and clean hotel"), yet this structured signal is not leveraged. We formalize a proactive query-based framework in which user queries provide structured preference signals. Modeling these signals via a Plackett-Luce subset choice model, we show that query-only learning is insufficient due to a fundamental shift-invariance barrier. To resolve this, we introduce MO-PQUCB, a hybrid algorithm that integrates query-based preference anchoring with bandit feedback through shift-invariant regularization and dual-exploration UCB. We prove that proactive queries accelerate preference estimation and yield improved regret scaling over prior preference-aware MO-MAB methods. Under corrupted queries, we further characterize statistical limits and design a robust estimator achieving near-optimal performance when the corruption is sparse. Experiments validate both theoretical and practical gains.

中文摘要翻译: 多目标bandit中的个性化决策需要学习用户对不同竞争目标之间权衡的特定偏好。由于臂（arm）的效用同时依赖于未知的奖励和未知的偏好，现有方法仅从效用反馈中推断偏好，导致偏好学习与奖励探索相互混杂。然而，在实际中，用户常通过主动对话式查询（例如“便宜且干净的酒店”）来透露其优先级，但这一结构化信号并未被充分利用。本文形式化了基于主动查询的框架，其中用户查询提供了结构化的偏好信号。通过Plackett-Luce子集选择模型对这些信号进行建模，我们证明了仅依赖查询学习是不够的，原因在于存在一个根本性的移位不变性障碍。为解决此问题，我们提出了MO-PQUCB，一种混合算法，该算法通过移位不变正则化和双探索UCB将基于查询的偏好锚定与bandit反馈相结合。我们证明了主动查询能够加速偏好估计，并在regret界上优于现有偏好感知的多目标MAB方法。在查询被破坏的场景下，我们进一步刻画了统计极限，并设计了一种鲁棒估计器，在稀疏破坏条件下实现了近乎最优的性能。实验验证了理论和实际收益。

</details>


### [354] [Sparrow: Sparse Rollout for Stable and Efficient Long-context RL of Large Language Models](https://arxiv.org/abs/2606.08446)
*Yang Zhou,Ranajoy Sadhukhan,Zhaofeng Sun,Zhuoming Chen,Souvik Kundu,Saket Dingliwal,Sai Muralidhar Jayanthi,Aram Galstyan,Haizhong Zheng,Beidi Chen*

Main category: cs.LG

TL;DR: 本文研究了基于可验证奖励的强化学习（RLVR）中稀疏注意力机制加速长上下文rollout生成的问题，提出了一种动态稀疏调度策略，通过维持每token策略差异的下尾统计量来平衡训练稳定性与加速效率，在Qwen3系列模型上实现了2.0-2.4倍的rollout加速，并进一步提出DistillSparse方法通过轻量级LoRA蒸馏实现更高加速比。

<details>
  <summary>Details</summary>

Motivation: RLVR虽然强大但会诱导极长的COT（思维链），导致计算成本高昂。由于RLVR每步成本主要由长上下文rollout生成主导，稀疏注意力有望加速密集rollout。然而稀疏rollout需要在稳定性与效率之间精细权衡：过于激进的稀疏会导致训练崩溃，过于保守则加速不足。本文旨在研究这一权衡并提出有效的解决方案。

Method: 首先观察到稀疏rollout崩溃并非由所有token的均匀退化导致，多数稀疏token即使在激进稀疏下也与密集token高度一致。基于此提出假设：若每token演员-策略差异的下尾统计量在整个轨迹中保持在临界阈值以上，则稀疏rollout训练保持稳定。引入动态稀疏调度策略，在生成过程中保持该下尾统计量恒定，并通过成本模型在最优化加速比下确定最佳稀疏调度。进一步提出DistillSparse，使用轻量级LoRA蒸馏使更激进的稀疏能达到相同的差异阈值。

Result: 在Qwen3-1.7B、Qwen3-4B和Qwen3-8B上分别实现了2.2倍、2.4倍和2.0倍的rollout加速；经验证该阈值可泛化到更大模型（Qwen3-14B）和另一RL领域（代码生成）。

Conclusion: 通过维持每token演员-策略差异的下尾统计量在临界阈值以上，可以在稀疏rollout训练中实现稳定性与效率的有效平衡，所提出的动态稀疏调度和DistillSparse方法能显著加速RLVR训练而不导致训练崩溃。

Original Abstract: Despite being powerful, reinforcement learning with verifiable rewards (RLVR) induces extremely long COT, making it computationally expensive. Since RLVR per-step cost is dominated by long-context rollout generation, sparse attention offers a promising way to accelerate dense rollout. However, sparse rollouts require a delicate stability-efficiency tradeoff: overly aggressive sparsity causes collapse, while overly lenient sparsity gives insufficient speedup. In this work, we study this tradeoff through sparse-to-dense actor-policy mismatch. We first observe that sparse rollout collapse is not driven by uniform degradation across tokens: most sparse tokens align perfectly with dense even under aggressive sparsity. Motivated by this, we hypothesize that sparse rollout training remains stable if the lower tail of per-token actor-policy mismatch stays above a critical threshold throughout the trajectory. We introduce a dynamic sparsity schedule that keeps this tail statistic constant during generation and validate our hypothesis. Across Qwen3 thinking-family models, keeping the tail mismatch statistic near a consistent threshold generally enables stable training. We then use a cost model to find the sparsity schedule for maximum speedup under this mismatch threshold, achieving 2.2x, 2.4x, and 2.0x rollout speedups when training Qwen3-1.7B, Qwen3-4B, and Qwen3-8B. Empirically, we show the thresholds generalize to a larger model (Qwen3-14B) and another RL domain (coding). Finally, our analysis naturally motivates DistillSparse: lightweight LoRA-based distillation on sparse rollout lets more aggressive sparsity reach the same sparse-to-dense mismatch threshold, yielding higher speedup.

中文摘要翻译: 尽管基于可验证奖励的强化学习（RLVR）非常强大，但它会诱导极长的思维链（COT），导致计算成本高昂。由于RLVR每步计算成本主要由长上下文rollout生成主导，稀疏注意力为加速密集rollout提供了一条有前景的路径。然而，稀疏rollout需要在稳定性与效率之间进行精细的权衡：过于激进的稀疏会导致训练崩溃，而过于保守的稀疏则加速不足。在本工作中，我们通过稀疏到密集的演员-策略差异来研究这一权衡。我们首先观察到稀疏rollout的崩溃并非由token间的均匀退化驱动：即使在激进稀疏条件下，大多数稀疏token与密集token仍高度一致。受此启发，我们提出假设：如果每token演员-策略差异的下尾统计量在整个轨迹中保持在临界阈值以上，则稀疏rollout训练保持稳定。我们引入了一种动态稀疏调度策略，在生成过程中保持该下尾统计量恒定，并验证了我们的假设。在Qwen3推理系列模型上，将下尾差异统计量保持在一致阈值附近通常可以实现稳定训练。然后，我们使用成本模型在该差异阈值下寻找能获得最大加速比的稀疏调度，在训练Qwen3-1.7B、Qwen3-4B和Qwen3-8B时分别实现了2.2倍、2.4倍和2.0倍的rollout加速。实验表明，该阈值可以泛化到更大模型（Qwen3-14B）和另一强化学习领域（代码生成）。最后，我们的分析自然引出了DistillSparse方法：基于轻量级LoRA的稀疏rollout蒸馏，使得更激进的稀疏能达到相同的稀疏到密集差异阈值，从而获得更高的加速比。

</details>


### [355] [Not Just After One: Sleep-Inspired Replay Prevents Catastrophic Forgetting After Sequential Tasks](https://arxiv.org/abs/2606.08447)
*Anthony Bazhenov,Jean Erik Delanois,Giri P. Krishnan*

Main category: cs.LG

TL;DR: 本文提出一种新的持续学习范式：在多个新任务依次训练后，引入无监督的类睡眠回放阶段，能够部分恢复所有先前学习任务的性能。研究表明任务特定信息对新训练具有韧性，但会随新任务训练而逐渐衰减。

<details>
  <summary>Details</summary>

Motivation: 人工神经网络在持续学习方面存在严重缺陷——训练新任务时往往会干扰并遗忘旧任务，而人类和动物能够连续学习并在活跃学习后将多个新记忆巩固为长期存储。现有算法多在新训练任务期间或之后立即应用保护机制，缺乏类似生物体的延时巩固过程。

Method: 提出一种训练与巩固分离的持续学习框架：依次顺序训练多个新任务，随后应用一个无监督的类睡眠回放阶段，通过重放旧任务数据来部分恢复所有已学习任务的性能。

Result: 实验表明：1）多个新任务可以依次训练后再通过类睡眠回放阶段恢复性能；2）任务特定信息在训练新任务时具有韧性，不会立即丢失，但会随着新任务的持续训练而逐渐衰减。

Conclusion: 该研究揭示了持续学习的新原理——将活跃学习与延时巩固过程分离，有助于开发更广泛的持续学习人工智能解决方案。

Original Abstract: One of the critical limitations of artificial neural networks is their lack of ability to continually learn: training on new tasks often leads to interference and forgetting of the previous ones. While several algorithms have been proposed to protect old memories from interference, they are typically applied during or immediately after each new episode of training. In contrast, humans and animals can learn continuously, acquiring multiple new memories during active learning before consolidating all of them into long-term storage. Here we show that multiple new tasks can be trained sequentially before an unsupervised sleep-like replay phase is applied to partially restore performance across all previously learned tasks. Our study further suggests that task-specific information remains resilient to new training but decays gradually as network is trained on new tasks. These findings point to novel principles for developing a broad range of continual learning AI solutions.

中文摘要翻译: 人工神经网络的关键局限性之一是其缺乏持续学习的能力：在新任务上训练往往会导致对先前任务的干扰和遗忘。虽然已有几种算法被提出来保护旧记忆免受干扰，但它们通常在每个新训练阶段期间或之后立即应用。相比之下，人类和动物可以连续学习，在活跃学习期间获取多个新记忆，然后将它们全部巩固为长期存储。本研究表明，多个新任务可以依次顺序训练，之后应用一个无监督的类睡眠回放阶段，能够部分恢复所有先前学习任务的性能。我们的研究进一步表明，任务特定信息对新训练具有韧性，但随着网络在新任务上的持续训练，这些信息会逐渐衰减。这些发现为开发更广泛的持续学习人工智能解决方案指明了新的原理。

</details>


### [356] [Theoretical Foundations of Continual Learning via Drift-Plus-Penalty](https://arxiv.org/abs/2606.08452)
*Nazreen Shah,Govinda Arya,Bharath B. N.,Ranjitha Prasad*

Main category: cs.LG

TL;DR: 本文从控制理论视角出发，提出基于Drift-Plus-Penalty原则的持续学习方法COLD，通过虚拟队列显式调控遗忘演化，在遗忘与可塑性之间实现可调节的权衡，并在标准基准上优于现有方法。

<details>
  <summary>Details</summary>

Motivation: 现有持续学习方法虽能缓解灾难性遗忘，但缺乏对遗忘过程的显式调控机制，难以在长期稳定性和学习可塑性之间进行精细控制。作者希望引入控制理论框架，将持续学习建模为受稳定性约束的受控过程。

Method: 提出COLD框架，基于随机优化中的Drift-Plus-Penalty（DPP）原则。每个任务训练时，同时最小化当前任务损失并维护一个虚拟队列，该队列追踪先前学习任务上的稳定性偏差，从而将稳定性-可塑性权衡建模为受调控的动态过程。还设计了COLD-ORACLE作为理论参考基准。

Result: 建立了稳定性和收敛性保证，通过可调控制参数表征稳定性-可塑性权衡。在标准基准实验上，COLD持续优于多种最先进的持续学习方法，同时通过显式调控稳定性和可塑性实现了具有竞争力且可控的遗忘行为。

Conclusion: 控制理论视角为持续学习提供了有效的遗忘调控机制，COLD方法在性能优于现有方法的同时，还能通过调节参数灵活控制遗忘行为，具有良好的实用价值。

Original Abstract: In many real-world settings, data streams are nonstationary and arrive sequentially, requiring learning systems to adapt continuously without retraining from scratch. Continual learning (CL) addresses this challenge by incorporating new tasks while mitigating catastrophic forgetting, where learning new information degrades performance on previously acquired knowledge. We introduce a control-theoretic perspective on CL that explicitly regulates the evolution of forgetting, framing adaptation as a controlled process subject to long-term stability constraints. We focus on replay-based CL, where a finite memory buffer stores representative samples from prior tasks. We propose COntinual Learning with Drift-Plus-Penalty (COLD), a continual learning framework based on the Drift-Plus-Penalty (DPP) principle from stochastic optimization. To facilitate analysis, we also consider an oracle variant, COLD-ORACLE, as a reference benchmark. At each task, both methods minimize the current task loss while maintaining a virtual queue that tracks deviations from long-term stability on previously learned tasks, capturing the stability-plasticity trade-off as a regulated dynamical process. We establish stability and convergence guarantees that characterize this trade-off through a tunable control parameter. Experiments on standard benchmarks demonstrate that COLD consistently outperforms a broad range of state-of-the-art CL methods while providing competitive and controllable forgetting behavior through explicit regulation of stability and plasticity.

中文摘要翻译: 在许多现实场景中，数据流是非平稳的且按顺序到达，这要求学习系统在不从头重新训练的情况下持续适应。持续学习通过整合新任务并缓解灾难性遗忘（即学习新信息会降低先前获取知识的性能）来应对这一挑战。我们引入了一种控制理论视角的持续学习，该方法显式调控遗忘的演化，将适应性建模为受长期稳定性约束的受控过程。我们聚焦于基于回放的持续学习，其中有限的内存缓冲区存储先前任务中的代表性样本。我们提出基于Drift-Plus-Penalty原则的持续学习框架COLD。为便于分析，我们还设计了一个理想基准变体COLD-ORACLE。在每个任务中，两种方法都最小化当前任务损失，同时维护一个虚拟队列来追踪先前学习任务上相对于长期稳定性的偏差，将稳定性-可塑性权衡建模为一个受调控的动态过程。我们建立了稳定性和收敛性保证，通过可调控制参数表征该权衡。在标准基准上的实验表明，COLD在持续优于多种最先进持续学习方法的同时，通过显式调控稳定性和可塑性实现了具有竞争力且可控的遗忘行为。

</details>


### [357] [Beyond Linear Activation Steering: Invertible Latent Transformations for Controlling LLM Behavior](https://arxiv.org/abs/2606.08454)
*Tuc Nguyen,Thai Le*

Main category: cs.LG

TL;DR: 提出INNSteer，一种基于可逆神经网络的非线性激活操控框架，通过学习可逆变换将LLM激活映射到更易线性控制的潜空间，实现输入依赖的非线性干预，在多个模型、规模和行为基准上优于线性及其他非线性操控方法。

<details>
  <summary>Details</summary>

Motivation: 现有激活操控方法将行为控制视为全局线性加性偏移，使用固定的操控方向，忽略了行为特征在激活空间中可能呈现非线性变化或位于弯曲各向异性流形上的情况，导致最优干预可能依赖于输入。为解决这一局限性，需要一种输入依赖的非线性激活操控方法。

Method: INNSteer学习一个轻量级可逆神经网络φ，将LLM的激活映射到潜空间，在该空间中行为类别更易于线性控制。推理时，激活先通过φ映射到潜空间，在潜空间中进行平移操控，再通过精确逆变换φ⁻¹映射回原始激活空间。这使得简单的潜空间线性平移等价于原始激活空间中的非线性、输入依赖的干预。

Result: 在多个LLM系列、不同规模、行为特质和安全基准上，INNSteer在模型控制效果上一致优于线性基线、基于传输的基线以及其他非线性操控基线，同时较好地保持了生成流畅性。

Conclusion: 非线性可逆潜空间变换能够有效克服固定线性操控方向的局限性，实现输入依赖的自适应干预，为LLM行为控制提供了一种更灵活、效果更优的轻量级推理时机制。

Original Abstract: Activation steering provides a lightweight inference-time mechanism for controlling large language models (LLMs) by modifying their internal activation vectors toward desired behaviors. Most existing methods compute a fixed steering direction in the original activation space, typically from pairs of contrastive examples using mean differences, linear probes, or arbitrary separability criteria. While effective to a certain extent, these methods treat behavioral control as a global, linear, additive offset: the same direction is applied across inputs, and behaviors are linearly separable. This can be restrictive when behavioral features vary nonlinearly across the activation space or lie on curved and anisotropic manifolds, where the optimal intervention may be input-dependent. To address this limitation, we propose INNSteer, a nonlinear activation steering framework based on invertible latent transformations. Rather than searching for a better steering vector in the original representation space, INNSteer learns a lightweight invertible neural network $φ$ that maps an LLM's activations into a latent space where behavioral classes are more amenable to linear control. At inference time, activations are mapped through $φ$, steered in the latent space, and mapped back through the exact inverse transformation $φ^{-1}$. This makes a simple latent-space translation become a nonlinear, input-dependent intervention in the original activation space. Across experiment settings on multiple LLM families, scales, behavioral traits, and safety benchmarks, INNSteer consistently improves model control over linear, transport-based, and nonlinear steering baselines while largely preserving generation fluency.

中文摘要翻译: 激活操控（Activation steering）提供了一种轻量级的推理时机制，通过修改大型语言模型（LLM）的内部激活向量来引导其朝向期望行为。现有多数方法在原始激活空间中计算固定的操控方向，通常使用对比样本对的均值差异、线性探针或任意可分性准则。这些方法虽有一定效果，但将行为控制视为全局的线性加性偏移：同一方向应用于所有输入，且行为是线性可分的。当行为特征在激活空间中呈非线性变化，或位于弯曲各向异性流形上时，最优干预可能是输入依赖的，而上述方法则显得受限。为解决这一局限，我们提出INNSteer——一种基于可逆潜空间变换的非线性激活操控框架。INNSteer并非在原始表示空间中寻找更优的操控向量，而是学习一个轻量级可逆神经网络φ，将LLM的激活映射到一个潜空间，在该空间中行为类别更易于线性控制。推理时，激活经φ映射到潜空间，在潜空间中进行操控，再通过精确逆变换φ⁻¹映射回原始空间。这使得简单的潜空间平移等价于原始激活空间中的非线性、输入依赖的干预。在多个LLM系列、不同规模、行为特质和安全基准的实验设置中，INNSteer在模型控制效果上一致优于线性、基于传输以及其他非线性操控基线，同时较好地保持了生成流畅性。

</details>


### [358] [The Confidence Trap: Calibration Attacks for Graph Neural Networks](https://arxiv.org/abs/2606.08467)
*Cuong Dang,Jiahao Zhang,Hieu Ta Quang,Dung Le,Lu Cheng,Suhang Wang*

Main category: cs.LG

TL;DR: 本文提出统一图校准攻击（UGCA）框架，用于白盒场景下评估图神经网络（GNN）对对抗性结构扰动的校准鲁棒性。通过KL散度损失、重排序机制、混合损失和波束搜索等策略，UGCA在保持分类准确率的同时显著提升期望校准误差（ECE）。理论分析表明，高精度模型或在多类别数据集上训练的模型更易受到此类攻击。

<details>
  <summary>Details</summary>

Motivation: 尽管置信度校准对于安全关键应用中的可信决策至关重要，但校准后的GNN在对抗性结构扰动下的鲁棒性尚未得到充分探索。图结构离散性、现有欠自信目标无法驱动预测趋于均匀分布、以及GNN对边扰动高度敏感易导致标签意外变化等挑战，促使作者提出统一框架来系统研究GNN校准鲁棒性。

Method: 提出UGCA框架，包含：(1) KL散度损失鼓励预测分布趋于均匀；(2) 重排序机制减少标签翻转；(3) 混合损失在违反约束时恢复标签；(4) 波束搜索探索更广的对抗搜索空间。此外提供了理论分析，将模型泛化能力、数据集复杂度与校准脆弱性联系起来。

Result: 大量实验表明，UGCA在保持分类准确率的同时显著提高了期望校准误差（ECE）。理论分析揭示：具有更高准确率的模型或在更多类别数据集上训练的模型在本威胁模型下更易受到攻击。

Conclusion: UGCA框架成功实现了对GNN校准鲁棒性的最坏情况（白盒）分析，填补了图对抗攻击与置信度校准交叉领域的研究空白，并为未来安全关键场景下GNN的可信部署提供了评估工具。

Original Abstract: While confidence calibration is essential for trustworthy decision-making in safety-critical applications, the robustness of calibrated GNNs to adversarial structural perturbations remains largely unexplored. However, studying calibration attacks on graphs presents unique technical challenges: (1) the discrete nature of graph structures complicates gradient-based optimization, (2) existing underconfidence objectives fail to drive predictions toward uniform distributions, and (3) GNNs are highly sensitive to edge perturbations, often causing unintended label changes that violate attack constraints. To address these challenges, we propose a \textbf{Unified Graph Calibration Attack (UGCA)} framework designed for \textbf{worst-case (white-box) analysis} of GNN calibration robustness. UGCA introduces a KL-divergence loss to encourage uniform predictive distributions, a reranking mechanism to reduce label flipping, a hybrid loss to recover labels when violations occur, and beam search to explore a broader adversarial search space. We further provide theoretical insights linking model generalization, dataset complexity, and calibration vulnerability, showing that models with higher accuracy or trained on datasets with more classes are more susceptible under this threat model. Extensive experiments demonstrate that UGCA substantially increases Expected Calibration Error while preserving classification accuracy. Our code is publicly available at https://github.com/CaptainCuong/Graph-Calibration-Attack.git.

中文摘要翻译: 尽管置信度校准对于安全关键应用中的可信决策至关重要，但校准后的图神经网络（GNN）在对抗性结构扰动下的鲁棒性仍未得到充分探索。然而，研究图上的校准攻击面临独特的技术挑战：（1）图结构的离散性使基于梯度的优化复杂化；（2）现有的欠自信目标无法有效驱动预测向均匀分布靠近；（3）GNN对边扰动高度敏感，常导致违反攻击约束的意外标签变化。为应对这些挑战，我们提出统一图校准攻击（UGCA）框架，用于GNN校准鲁棒性的最坏情况（白盒）分析。UGCA引入了KL散度损失以鼓励均匀预测分布、重排序机制以减少标签翻转、混合损失以在违规发生时恢复标签，以及波束搜索以探索更广阔的对抗搜索空间。我们进一步提供理论分析，将模型泛化能力、数据集复杂度与校准脆弱性联系起来，表明具有更高准确率的模型或在更多类别数据集上训练的模型在本威胁模型下更易受到攻击。大量实验证明，UGCA在保持分类准确率的同时显著提高了期望校准误差。我们的代码已开源在https://github.com/CaptainCuong/Graph-Calibration-Attack.git。

</details>


### [359] [Physically Consistent Null Space Alignment for Detection of Low-Magnitude False Data Injection Attacks](https://arxiv.org/abs/2606.08473)
*Xin Li,Chenhan Xiao,Jonathan Cohen,Aviad Elyashar,Yang Weng,Rami Puzis*

Main category: cs.LG

TL;DR: 本文提出物理一致零空间对齐（PCNSA）框架，通过伪零空间守恒数据预处理（PSCP）步骤，保持测量数据的物理几何一致性，从而有效检测电力系统状态估计中的隐蔽虚假数据注入攻击。在IEEE标准系统上的实验表明，该方法在F1分数和检测准确率上优于XTM、LSTM、AE和孤立森林等基线方法，且对部分可观测性和PMU噪声具有鲁棒性。

<details>
  <summary>Details</summary>

Motivation: 现有模型驱动和数据驱动的检测器难以识别低幅值但高影响的隐蔽虚假数据注入攻击（FDIAs），因为残差测试忽略了隐藏在伪零空间中的变化，而子空间学习方法虽能捕获相关模式但缺乏物理一致性约束。

Method: 提出物理一致零空间对齐（PCNSA）框架，核心是伪零空间守恒数据预处理（PSCP）步骤：将测量数据重新表达在物理坐标系下，再进行子空间提取。PSCP保留了行空间与其正交补之间的分离性，使得SVD导出的伪零子空间与物理残差空间对齐，无需显式已知系统模型H。

Result: 在IEEE 14、30、57和118节点系统上的实验验证：隐蔽攻击在XTM、LSTM、AE和孤立森林等基线方法下无法检测，但PCNSA使攻击在对齐子空间中表现为明显偏差，获得了更高的F1分数和检测准确率，且对部分可观测性和PMU噪声具有鲁棒性。

Conclusion: PCNSA通过保持测量数据与物理零空间的几何对应关系，有效检测了隐蔽FDIAs，验证了物理一致性约束对于子空间学习方法的重要性，为电力系统安全检测提供了新范式。

Original Abstract: False data injection attacks (FDIAs) introducing small measurement perturbations can still cause large deviations in power system state estimation when the injected signals align with the pseudo-null space of the system model. Existing model- and data-driven detectors may fail to identify such low-magnitude but high-impact attacks because residual tests ignore changes hidden in the pseudo-null space, while subspace learning methods capture correlation patterns without enforcing physical consistency. This paper proposes Physically Consistent Null Space Alignment (PCNSA), a framework that detects stealthy FDIAs by preserving, through preprocessing, the geometric correspondence between the physical null space and the measurement-derived pseudo-null space. The key point is a Pseudo-null Space Conserved data Preprocessing (PSCP) step that re-expresses measurements in the physical coordinate frame before subspace extraction. We prove that PSCP preserves the separation between row space and its orthogonal complement, a property that conventional per-feature standardization violates. This keeps the singular value decomposition (SVD)-derived pseudo-null subspace aligned with the physical residual space without explicit knowledge of H. Experiments on IEEE 14-, 30-, 57-, and 118-bus systems confirm this principle in practice: stealthy attacks that evade XTM, LSTM, AE and Isolation Forest baselines appear as clear deviations in the aligned subspace, yielding higher F1-score and detection accuracy while remaining robust under partial observability and realistic PMU noise.

中文摘要翻译: 虚假数据注入攻击（FDIAs）引入微小测量扰动，当注入信号与系统模型的伪零空间对齐时，仍会导致电力系统状态估计出现较大偏差。现有模型驱动和数据驱动的检测器可能无法识别这种低幅值但高影响的攻击，因为残差测试忽略了隐藏在伪零空间中的变化，而子空间学习方法在捕获相关模式时缺乏物理一致性约束。本文提出物理一致零空间对齐（PCNSA）框架，通过预处理步骤保持物理零空间与测量导出的伪零空间之间的几何对应关系，从而检测隐蔽FDIAs。关键在于伪零空间守恒数据预处理（PSCP）步骤，该步骤在子空间提取之前将测量数据重新表达在物理坐标系下。我们证明PSCP保留了行空间与其正交补之间的分离性，而传统的逐特征标准化破坏了这一性质。这使得奇异值分解（SVD）导出的伪零子空间与物理残差空间对齐，无需显式已知H。在IEEE 14、30、57和118节点系统上的实验验证了这一原理：在XTM、LSTM、AE和孤立森林基线方法下能够逃避检测的隐蔽攻击，在对齐子空间中表现为明显偏差，获得了更高的F1分数和检测准确率，同时在部分可观测性和实际PMU噪声条件下保持鲁棒性。

</details>


### [360] [Inferring hidden forcing in a biological oscillator using Kolmogorov-Arnold networks](https://arxiv.org/abs/2606.08479)
*Julian Szereszewski,Facundo Fainstein,Leandro E. Fernandez,Gabriel B. Mindlin*

Main category: cs.LG

TL;DR: 本文利用基于Kolmogorov-Arnold网络的可解释学习框架，仅从气囊压力测量数据中重建了鸟类呼吸动力学的有效肌肉驱动力，揭示了隐藏的两相激活模式，并通过肌电图记录独立验证。

<details>
  <summary>Details</summary>

Motivation: 从部分观测中推断驱动动力系统的力是一个基础性挑战，尤其当不同的潜在机制产生相似的可观测动力学时。传统的压力信号仅呈现类弛豫振荡，无法揭示底层驱动的真实结构。

Method: 采用基于Kolmogorov-Arnold网络的可解释学习框架，直接从数据中推断系统的控制方程，重建有效肌肉驱动力。

Result: 重建的动力学预测了每个呼吸周期内的两相激活模式，并通过呼気肌的肌电图记录独立验证。证明数据驱动的动力学定律重建可以揭示隐藏的物理结构并提供对未观测驱动变量的访问。

Conclusion: 数据驱动的动力学重建能够揭示隐藏的物理结构，为从部分观测的动力系统中推断潜在力提供了一条通用路径。

Original Abstract: Inferring the forces that drive a dynamical system from partial observations is a fundamental challenge across physics, particularly when distinct underlying mechanisms produce similar observable dynamics. Here we show that the effective muscular forcing underlying avian respiratory dynamics can be reconstructed from measurements of air-sac pressure alone. Using an interpretable learning framework based on Kolmogorov-Arnold networks, we infer the governing equations of the system directly from data and uncover a nontrivial structure in the underlying forcing that is not apparent from the pressure signal, which instead suggests a relaxation-like oscillation. The reconstructed dynamics predict a two-phase activation pattern within each respiratory cycle, which we independently validate through electromyographic recordings of expiratory muscles. These results demonstrate that data-driven reconstruction of dynamical laws can reveal hidden physical structure and provide access to unobserved driving variables, establishing a general route to infer latent forces in partially observed dynamical systems.

中文摘要翻译: 从部分观测中推断驱动动力系统的力是一个基础性挑战，尤其当不同的潜在机制产生相似的可观测动力学时。我们证明，仅从气囊压力测量即可重建鸟类呼吸动力学背后的有效肌肉驱动力。利用基于Kolmogorov-Arnold网络的可解释学习框架，我们直接从数据中推断系统的控制方程，并揭示了底层驱动中一个非平凡的结构——该结构在压力信号中并不明显，而压力信号仅暗示一种类弛豫振荡。重建的动力学预测了每个呼吸周期内的两相激活模式，我们通过呼気肌的肌电图记录独立验证了这一模式。这些结果表明，数据驱动的动力学定律重建能够揭示隐藏的物理结构并提供对未观测驱动变量的访问，为在部分观测的动力系统中推断潜在力建立了一条通用路径。

</details>


### [361] [Adaptive Loss Balancing for Noise-Robust GRPO in Generative Recommendation](https://arxiv.org/abs/2606.08480)
*Kewei Xu,Junbo Qi,Yanyan Zou,Pengfei Zhang,Xingzhi Yao,Shengjie Li*

Main category: cs.LG

TL;DR: AdaGRPO 提出了一种选择性奖励引导的优化框架，通过策略难度和奖励可区分性两个诊断指标对每个样本决定是否应用 GRPO 目标，从而避免统一强化学习在生成式推荐中带来的噪声放大问题，在电商数据集上显著提升了推荐性能并降低了幻觉率。

<details>
  <summary>Details</summary>

Motivation: 强化学习在生成式推荐中依赖奖励模型的可靠性，但实际生产中排名器在曝光偏差日志上训练，导致对部分样本的奖励信号不可靠。分层分析发现，只有当策略存在不确定性且排名器能有效区分真实物品时，奖励引导才有益；其他情况下奖励信号可忽略甚至有害，因此需要选择性应用奖励优化而非统一施压。

Method: 提出 AdaGRPO 框架：以监督负对数似然（NLL）为训练锚点，GRPO 目标由每个样本的二元裁剪门控控制。该门控由两个滚动诊断指标决定——策略侧难度（policy-side difficulty）和奖励可区分性（reward discriminability），任一指标不达标则退化为纯监督学习，从而保证训练稳定性并抑制噪声梯度放大。

Result: 在大型电商数据集上，最佳中间检查点将 HR@10 从 11.01% 提升至 12.18%，幻觉率控制在 0.22% 以下；最终检查点保持鲁棒性（HR@10 11.63%，幻觉率 0.27%），在检索-有效性前沿上优于固定 NLL-GRPO 混合策略。在线 A/B 测试中，点击率和停留时间均取得统计显著提升。

Conclusion: 选择性奖励引导（AdaGRPO）能有效解决统一强化学习在生成式推荐中因奖励模型不可靠导致的噪声放大问题，在离线指标和在线指标上均验证了其有效性，为RL在推荐系统中的应用提供了更稳健的范式。

Original Abstract: Reinforcement learning (RL) presents a promising avenue for enhancing generative recommendation beyond supervised imitation, leveraging reward signals to guide policy improvement. However, its efficacy is critically contingent on the trustworthiness of the reward model for the samples it evaluates. In practice, production rankers, the widely adopted reward models, are trained on exposure-biased logs, leading to sample-dependent inaccuracies that violate this assumption. Our stratified analysis uncovers a consistent pattern: reward guidance is most beneficial when the policy exhibits uncertainty and the ranker can effectively discriminate the ground-truth item from rollout negatives. On other samples, the reward signal is either negligible or detrimental, highlighting the risk of uniform RL application. To address such an issue, we introduce AdaGRPO, a novel framework that treats reward-guided optimization as selective admission rather than uniform pressure. Training is anchored in supervised negative log-likelihood, while the GRPO objective is gated by a binary, per-sample clip determined by two rollout diagnostics: policy-side difficulty and reward discriminability. Instances failing either diagnostic default to pure supervision, ensuring stability and mitigating the amplification of noisy gradients. We validate AdaGRPO on a large-scale e-commerce dataset. At the best intermediate checkpoint, it elevates HR@10 from 11.01% to 12.18% while constraining hallucination below 0.22%, and maintains robustness at the final checkpoint (HR@10 11.63%, hallucination 0.27%), outperforming fixed NLL--GRPO mixtures across the retrieval--validity frontier. In production A/B tests, AdaGRPO achieves statistically significant gains in click-through rate and dwell time, confirming its practical utility.

中文摘要翻译: 强化学习（RL）为超越监督模仿的生成式推荐提供了一条有前景的途径，通过利用奖励信号引导策略改进。然而，其有效性关键取决于奖励模型对所评估样本的可靠性。实践中，广泛采用的奖励模型（即生产级排序器）在存在曝光偏差的日志上训练，导致样本依赖的不准确性，从而违背了这一假设。我们的分层分析揭示了一个一致的模式：当策略存在不确定性且排序器能有效将真实物品与滚动负样本区分开时，奖励引导最为有益；而对于其他样本，奖励信号要么可忽略，要么有害，凸显了统一应用RL的风险。为解决该问题，我们提出了AdaGRPO——一种新颖的框架，将奖励引导优化视为选择性准入而非统一施压。训练以监督负对数似然为锚点，而GRPO目标由基于两个滚动诊断指标（策略侧难度和奖励可区分性）确定的逐样本二元裁剪门控。任一诊断未通过的样本默认退化为纯监督学习，从而确保稳定性并抑制噪声梯度的放大。我们在一个大规模电商数据集上验证了AdaGRPO。在最佳中间检查点，它将HR@10从11.01%提升至12.18%，同时将幻觉率控制在0.22%以下；在最终检查点仍保持鲁棒性（HR@10为11.63%，幻觉率为0.27%），在检索-有效性前沿上优于固定的NLL-GRPO混合策略。在生产环境的A/B测试中，AdaGRPO在点击率和停留时间上均取得了统计显著的提升，证实了其实用价值。

</details>


### [362] [PIPE-Cypher: Automatic Enterprise Benchmark Generation for Text-to-Cypher Systems](https://arxiv.org/abs/2606.08481)
*Suraj Ranganath,Anish Raghavendra*

Main category: cs.LG

TL;DR: 提出PIPE-Cypher管道，能够从实时属性图和种子查询自动生成平衡、可执行、多样化的NL-to-Cypher基准测试集，使Text2Cypher基准测试成为一个随图、用户和工作负载演化的可重复过程。

<details>
  <summary>Details</summary>

Motivation: 企业属性图在模式结构、术语、领域假设、治理约束和用户交互模式上差异巨大，创建部署相关的Text2Cypher基准测试非常困难，因为模式与值唯一且图结构随时间变化。

Method: PIPE-Cypher结合模式剖析、反向查询接地、约束生成、确定性Cypher治理、执行验证、编辑、多样性控制以及校准的本地LLM裁判，从实时属性图和种子查询生成平衡的NL-to-Cypher基准。

Result: 使用本地Qwen3.5-9B生成和评判，导出3000个可接受的FinBench/SNB示例，完成三项消融套件审计，用人工标签校准裁判行为，并评估了11个本地下游模型。零样本迁移效果弱，而少样本控制表明模式特定的示例库可帮助兼容模型家族。

Conclusion: PIPE-Cypher使Text2Cypher基准测试成为一个可重复的过程，能够随图、用户和目标工作负载演化，生成的基准具有刻意区分性。

Original Abstract: Enterprise property graphs vary widely in schema structure, internal terminology, domain assumptions, governance constraints, and user interaction patterns. A deployment-relevant Text2Cypher benchmark therefore reflects the questions users and agents actually ask of that graph. Creating such a benchmark is difficult because schemas and values are unique, and graph structure changes over time. Each NL-query pair must also be executable, use real graph entities, preserve diversity, and remain balanced across query types and difficulty levels. We present PIPE-Cypher, a local benchmark-generation pipeline that turns a live property graph and optional seed queries from customer questions, analyst logs, or agent tool calls into balanced NL-to-Cypher benchmarks. PIPE-Cypher combines schema profiling, reverse-query grounding, constrained generation, deterministic Cypher governance, execution validation, redaction, diversity controls, and a calibrated local LLM judge. Using local Qwen3.5-9B generation and judging, PIPE-Cypher exports 3,000 accepted FinBench/SNB examples, completes three audited ablation suites, calibrates judge behavior with human labels, and evaluates 11 local downstream models. The resulting benchmark is deliberately discriminative: zero-shot transfer is weak, while a few-shot control shows that schema-specific example banks can help compatible model families. Together, PIPE-Cypher makes Text2Cypher benchmarking a repeatable process that evolves with the graph, its users, and its target workloads.

中文摘要翻译: 企业属性图在模式结构、内部术语、领域假设、治理约束和用户交互模式上差异巨大。一个与部署相关的Text2Cypher基准测试应当反映用户和智能体实际对该图提出的问题。创建这样的基准测试非常困难，因为模式和值是唯一的，且图结构随时间变化。每个自然语言-查询对还必须可执行、使用真实图实体、保持多样性，并在查询类型和难度级别上保持平衡。我们提出了PIPE-Cypher，一个本地基准生成管道，能够将实时属性图和来自客户问题、分析师日志或智能体工具调用的可选种子查询转化为平衡的自然语言到Cypher基准测试。PIPE-Cypher结合了模式剖析、反向查询接地、约束生成、确定性Cypher治理、执行验证、编辑、多样性控制以及校准的本地大语言模型裁判。通过使用本地Qwen3.5-9B进行生成和评判，PIPE-Cypher导出了3000个可接受的FinBench/SNB示例，完成了三项经过审计的消融套件，用人工标签校准了裁判行为，并评估了11个本地下游模型。生成的基准具有刻意的区分性：零样本迁移效果弱，而少样本控制表明，模式特定的示例库可以帮助兼容的模型家族。总之，PIPE-Cypher使Text2Cypher基准测试成为一个可重复的过程，能够随图、用户及其目标工作负载演化。

</details>


### [363] [STELLAR: Spatio-Temporal Environmental Learning with Latent Alignment and Refinement for Long-Tailed Species Distribution Modeling](https://arxiv.org/abs/2606.08484)
*Shufeng Kong,Tao Yu,Yuanyuan Wei,Caihua Liu,Junwen Bai,Yingheng Wang,Marc Grimson,Daniel Fink,Carla P. Gomes*

Main category: cs.LG

TL;DR: STELLAR提出了一种联合物种分布建模（JSDM）框架，通过图-时序编码器、上下文锚定潜在对齐和不平衡感知解耦解码模块，联合优化动态生境上下文与群落结构，在eBird数据集上显著优于现有方法，尤其在稀有物种预测和可解释的物种交互发现方面表现突出。

<details>
  <summary>Details</summary>

Motivation: 现有JSDM方法往往孤立地处理环境驱动因素与物种分布之间的时空关联性，忽略了群落结构的非线性特征、历史动态轨迹以及稀有物种导致的长尾不平衡问题，亟需一种能够联合建模这些因素的统一框架。

Method: 提出STELLAR框架，包含三个互补模块：(1) 图-时序编码器（Graph-Temporal Encoder），利用图注意力和循环单元聚合空间邻域效应并捕捉环境与群落结构的共同演化动态；(2) 上下文锚定潜在对齐机制（Context-Anchored Latent Alignment），通过标签激活的混合先验和监督对比学习构建潜在空间，基于共享环境偏好聚类物种；(3) 不平衡感知解耦解码模块（Imbalance-Aware Decoupled Decoding），使用非对称损失聚焦于难例和稀有物种样本，防止长尾模式崩溃。

Result: 在大规模eBird数据集上，STELLAR显著优于现有最先进基线方法，尤其在稀有物种预测和可解释的物种交互发现方面表现突出。

Conclusion: STELLAR通过联合建模时空环境动态与群落结构，有效解决了JSDM中的长尾不平衡和复杂非线性问题，为生物多样性监测与保护规划提供了更准确且可解释的建模工具。

Original Abstract: Joint Species Distribution Modeling (JSDM) is a key enabler for biodiversity monitoring and conservation planning. However, accurate JSDM faces two coupled challenges: environmental drivers and species distributions are inherently spatio-temporal, while species co-occurrence patterns exhibit complex non-linear community structure and severe long-tail imbalance driven by rare species. Existing approaches often address these factors in isolation, learning from static covariates or neglecting the historical trajectories of dynamic community structure. To overcome these limitations, we propose STELLAR (Spatio-Temporal Environmental Learning with Latent Alignment and Refinement), a novel framework that learns a shared latent space where dynamic habitat context and community structure are optimized jointly. Our approach integrates three complementary components: (1) a Graph-Temporal Encoder that employs graph attention and recurrent units to aggregate spatial neighborhood effects and capture the co-evolving historical dynamics of environmental context and community structure; (2) a Context-Anchored Latent Alignment mechanism that structures the latent space using a label-activated mixture prior and supervised contrastive learning, actively clustering species based on shared environmental preferences; and (3) an Imbalance-Aware Decoupled Decoding module that utilizes Asymmetric Loss to focus learning on hard, rare species samples, preventing mode collapse in the long tail. Experiments on the large-scale eBird dataset, curated with domain experts, demonstrate that our framework significantly outperforms state-of-the-art baselines, particularly in predicting rare species and revealing interpretable species interactions.

中文摘要翻译: 联合物种分布建模（JSDM）是生物多样性监测与保护规划的关键技术。然而，准确的JSDM面临两个相互耦合的挑战：环境驱动因素与物种分布本质上是时空的，而物种共现模式则呈现出复杂的非线性群落结构以及由稀有物种导致的严重长尾不平衡问题。现有方法常常孤立地处理这些因素，要么从静态协变量中学习，要么忽略了动态群落结构的历史轨迹。为克服这些局限，我们提出STELLAR（时空环境学习与潜在对齐精炼），这是一个新颖的框架，通过学习一个共享潜在空间，在其中动态生境上下文与群落结构被联合优化。我们的方法整合了三个互补模块：(1) 图-时序编码器，利用图注意力和循环单元聚合空间邻域效应，捕捉环境上下文与群落结构共同演化的历史动态；(2) 上下文锚定潜在对齐机制，通过标签激活的混合先验和监督对比学习构建潜在空间，基于共享环境偏好对物种进行主动聚类；(3) 不平衡感知解耦解码模块，利用非对称损失聚焦于难例和稀有物种样本的学习，防止长尾中的模式崩溃。在领域专家精心整理的大规模eBird数据集上的实验表明，我们的框架显著优于最先进的基线方法，尤其在稀有物种预测和揭示可解释的物种交互方面表现突出。

</details>


### [364] [A Joint Finite-Sample Certificate for Adaptive Selective Conformal Risk Control](https://arxiv.org/abs/2606.08517)
*Xiaoli Yu,Jiamiao Liu*

Main category: cs.LG

TL;DR: 本文针对选择性预测器（可对高置信度输入作答、否则弃权），提出了一种多目标有限样本证书，同时上界选定风险、下界接受概率和下界部署效用，并通过方差自适应的经验Bernstein界替代传统Hoeffding范围界，显著提升了证书的紧致性。

<details>
  <summary>Details</summary>

Motivation: 安全部署选择性预测器需要一个单一有限样本证书，能同时控制选定风险、接受概率和部署效用三个目标，且需在自适应阈值选择下有效。现有方法（如Hoeffding范围界）在紧致性上存在不足，特别是接受概率下界对$\pmin$的依赖为$1/\pmin$，导致证书过于保守。

Method: 将选定风险直接作为比率处理（而非通过Hoeffding风格的范围界），构建三个置信下界：方差自适应的经验Bernstein界（用于比率风险）、Clopper-Pearson界（用于接受概率）、以及双边接近界（用于效用）。这些界联合保证了认证策略的效用绝对下界和与最优策略的差距上界。

Result: 将接受概率下界对$\pmin$的依赖从$1/\pmin$改进为$1/\sqrt{\pmin}$；在ImageNet（三个ResNet）和COCO val 2017全景分割上，相比Hoeffding-CRC的认证接受前沿提升了+22个百分点，且比非退化的匹配验证基线紧致约10倍；但增益具有场景依赖性，在ADE20K上不显著；算法时间复杂度为$O(\ncert m)$。

Conclusion: 提出的比率式证书框架在多个基准上显著优于现有方法，尤其在高风险阈值场景下优势明显，但改进并非普适，而是依赖于具体场景和风险余量条件。

Original Abstract: Selective predictors answer on confident inputs and abstain elsewhere; deploying one safely needs a single finite-sample certificate that simultaneously upper-bounds the selected risk, lower-bounds the acceptance probability $\pacc$ above a floor $\pmin$, and lower-bounds the deployment utility. This certificate must be valid under adaptive threshold selection from a finite grid of $m$ pairs on $\ncert$ samples. We give such a certificate for bounded, possibly non-monotone losses by treating the selected risk directly as a ratio rather than through a Hoeffding-style range bound. The construction couples three confidence bounds: a variance-adaptive empirical-Bernstein bound on the ratio risk, a Clopper--Pearson bound on acceptance, and a two-sided closeness bound on utility. Together they lower-bound the certified policy's utility absolutely and to within $2\gammau$ of the best over the \emph{certified set}, both non-vacuous whenever feasible; a regime-scoped third leg matches an external oracle, informative only where the risk margin $\gammar < α$ and vacuous at the headline operating points. Relative to the range-only Hoeffding-ratio construction this sharpens the acceptance-floor dependence from $1/\pmin$ to $1/\sqrt{\pmin}$, and a closed-form corollary identifies a per-pair regime in which our risk bound dominates a Hoeffding conformal risk control (Hoeffding--CRC) selective bound. Empirically, on ImageNet (three ResNets) and COCO val 2017 panoptic, the certificate opens a $+22$ pp certified-acceptance frontier over Hoeffding--CRC and is ${\approx}10{\times}$ tighter than a non-vacuous matched-valid baseline; these gains are regime-scoped, not universal, and absent on ADE20K. The certifier runs in $O(\ncert m)$ time.

中文摘要翻译: 选择性预测器在置信的输入上作答，否则弃权；安全部署需要一张单一有限样本证书，其同时上界选定风险、下界接受概率$\pacc$高于一个底线$\pmin$，以及下界部署效用。该证书必须在从$\ncert$个样本上的$m$个有限阈值对网格中进行自适应阈值选择时仍然有效。我们为有界、可能非单调的损失给出了这样一种证书，其将选定风险直接作为比率处理，而非通过Hoeffding风格的范围界。该构造耦合了三个置信界：一个方差自适应的经验Bernstein界（用于比率风险）、一个Clopper-Pearson界（用于接受概率）、以及一个双边接近界（用于效用）。它们共同下界了认证策略的绝对效用，并将其与认证集上最优策略的差距控制在$2\gammau$以内，两者在可行时均非退化；一个具有场景范围限定的第三支路匹配外部oracle，仅在风险余量$\gammar < α$时提供信息，而在主要工作点处退化。相对于纯范围的Hoeffding比率构造，本文方法将接受概率下界对$\pmin$的依赖从$1/\pmin$锐化为$1/\sqrt{\pmin}$，且一个闭合形式的推论识别出每对阈值的一个场景，其中我们的风险界支配了Hoeffding保形风险控制（Hoeffding--CRC）选择性界。实验上，在ImageNet（三个ResNet）和COCO val 2017全景分割上，该证书相对于Hoeffding--CRC开辟了+22个百分点的认证接受前沿，且比非退化的匹配验证基线紧致约10倍；这些增益具有场景范围限定性，而非普适，在ADEK20上不显著。该认证器运行时间为$O(\ncert m)$。

</details>


### [365] [Autonomous Aerial Manipulation via Contextual Contrastive Meta Reinforcement Learning](https://arxiv.org/abs/2606.08533)
*Lixuan Jin,Bingxuan Lan,Xinyi Bao,Xiangyuan Xie,Chunjie Zhang,Zheng Chen,Tianshuo Liu,Ruijie Tian,Jinyu Ru,Gang Wang,Lei Yuan,Yang Yu*

Main category: cs.LG

TL;DR: 本文提出Aco2，一种基于上下文对比元强化学习的自主空中操控方法，使四旋翼无人机能够在无需人工干预的情况下，通过轻量钩爪自主拾取、运输和投递多种带手柄物体，并在线适应负载变化带来的动力学差异。

<details>
  <summary>Details</summary>

Motivation: 现有无人机空中操控方法通常假设负载预附着或依赖专用夹持器，缺乏针对不同负载引起飞行动力学剧烈变化的端到端自适应能力，亟需一种无需人工标定或显式系统辨识、能在线适应多种负载的统一策略。

Method: 设计上下文观测编码器，从近期交互历史中推断紧凑的潜在上下文，使策略在线适应负载相关动力学；引入对比学习目标，结构化上下文嵌入以聚焦任务相关变化，提升跨负载泛化能力；在仿真中通过域随机化训练后直接部署到真实四旋翼无人机，无需微调。

Result: 在仿真中训练后，Aco2可直接部署到物理四旋翼无人机上，无需真实世界微调，实现自主拾取、运输和投递多种带手柄物体。

Conclusion: 通过上下文对比元强化学习，结合仿真域随机化训练，Aco2有效解决了无人机自主空中操控中负载多样性带来的动力学自适应难题，实现了从仿真到真实环境的零样本迁移。

Original Abstract: Unmanned aerial vehicles (UAVs) are increasingly being deployed in logistics, service robotics, and other real-world applications, creating a growing demand for autonomous payload acquisition and delivery. Existing approaches typically assume pre-attached payloads or rely on specialized grippers, leaving versatile end-to-end aerial delivery largely unresolved, where different payloads induce highly variable flight dynamics, requiring a single policy to adapt online without manual calibration or explicit system identification. To this end, we study \textbf{A}utonomous \textbf{A}erial Manipulation via \textbf{Co}ntextual \textbf{Co}ntrastive Meta Reinforcement Learning (\textbf{\textit{Aco2}}), a fully autonomous aerial delivery setting in which a quadrotor equipped with a lightweight hook continuously picks up, transports, and delivers diverse handle-equipped objects between randomized locations, all without human intervention. First, we design a contextual observation encoder that infers a compact latent context from recent interaction history, enabling the policy to adapt online to payload-dependent dynamics. To further improve the quality of this context, we introduce a contrastive objective that structures the context embedding around task-relevant variations, improving generalization across diverse payloads without requiring explicit system identification. Trained entirely in simulation with extensive domain randomization, \textit{Aco2} can be directly deployed on a physical quadrotor without real-world fine-tuning.

中文摘要翻译: 无人机正越来越多地部署于物流、服务机器人及其他实际应用场景中，这产生了对自主载荷获取与投递的日益增长的需求。现有方法通常假设负载预附着或依赖专用夹持器，使得通用的端到端空中投递问题仍未解决——不同负载会引发高度变化的飞行动力学，要求单一策略能在无需人工标定或显式系统辨识的情况下在线自适应。为此，我们研究了\textbf{A}utonomous \textbf{A}erial Manipulation via \textbf{Co}ntextual \textbf{Co}ntrastive Meta Reinforcement Learning（\textbf{\textit{Aco2}}），一种完全自主的空中投递设定，其中配备轻量钩爪的四旋翼无人机在随机位置之间持续拾取、运输和投递多种带手柄物体，全程无需人工干预。首先，我们设计了一个上下文观测编码器，从近期交互历史中推断出紧凑的潜在上下文，使策略能够在线适应负载相关的动力学。为进一步提升该上下文的质量，我们引入了一个对比学习目标，围绕任务相关变化对上下文嵌入进行结构化，从而在无需显式系统辨识的情况下提升跨多种负载的泛化能力。该方法完全在仿真中通过广泛的域随机化训练，可直接部署到物理四旋翼无人机上，无需真实世界微调。

</details>


### [366] [Routine laboratory trajectories encode the onset of organ-level complications in cancer](https://arxiv.org/abs/2606.08538)
*Jannik Lübberstedt,Krischan Braitsch,Jacqueline Lammert,Christof Winter,Florian Gabriel,Tristan Lemke,Christopher Zirn,Markus Graf,Friedrich Puttkammer,Hartmut Häntze,Johannes Moll,Anirudh Narayanan,Andrei Zhukov,Fabian Drexel,Zeineb Ben Chaaben,Sebastian Ziegelmayer,Su Hwan Kim,Marion Högner,Jan Kirschke,Florian Bassermann,Marcus Makowski,Christian Wachinger,Lisa Adams,Keno Bressem*

Main category: cs.LG

TL;DR: 本文利用Transformer模型对癌症治疗期间的纵向实验室检测数据进行建模，提前数月预测162种治疗相关并发症，在多发性骨髓瘤和卵巢癌患者中实现了1.5至6.1倍的富集提升，外部验证AUROC最高达0.85。

<details>
  <summary>Details</summary>

Motivation: 常规实验室检查数据在癌症治疗过程中被频繁采集，构成纵向生理记录，但现有预后工具仅利用单时间点信息，丢弃了时间结构。本文旨在挖掘纵向实验室轨迹中编码的器官功能恶化信号，实现对治疗相关并发症的早期预测。

Method: 使用Transformer模型，在3,905名多发性骨髓瘤或卵巢癌患者的2,777,595次实验室测量数据上进行训练，预测两年内162种治疗相关并发症的发生，涵盖8个临床类别。

Result: 模型在分组终点上匹配或超越了非时序基线方法（AUROC提升最高+0.11），在群体层面实现1.5至6.1倍的富集；在MIMIC-IV和MMRF CoMMpass外部验证集上AUROC最高达0.85；预测结果跨癌症类型泛化，生物标志物掩码分析揭示了与已知病理生理机制一致的特征。

Conclusion: 常规肿瘤学实验室数据在临床发病前数周至数月即编码了器官功能恶化信号，可在无需额外检测设施的情况下实现并发症特异性监测。

Original Abstract: Routine laboratory panels drawn during cancer treatment constitute longitudinal physiological recordings of organ function, yet their temporal structure is discarded by single-timepoint prognostic tools. A transformer trained on 2,777,595 laboratory measurements from 3,905 patients with multiple myeloma or ovarian cancer predicted the two-year onset of 162 treatment-associated complications, including therapy-related myelodysplastic syndromes, spanning eight clinical categories, achieving 1.5- to 6.1-fold enrichment above prevalence at the group level. It matched or outperformed non-sequential baselines across grouped endpoints (AUROC gains up to +0.11), demonstrating that longitudinal laboratory trajectories capture evolving complication-specific physiology inaccessible from isolated measurements. Predictions generalised across both cancers, divergence concentrating in disease-specific complications, and biomarker masking recovered signatures consistent with established pathophysiology. External validation on MIMIC-IV and MMRF CoMMpass confirmed transferability across independent healthcare systems (AUROC up to 0.85). Routine oncological laboratory data encode organ deterioration weeks to months before clinical onset, enabling complication-specific surveillance without additional testing infrastructure.

中文摘要翻译: 癌症治疗期间采集的常规实验室检查数据构成了器官功能的纵向生理记录，然而现有时点预后工具丢弃了其时间结构。一个在3,905名多发性骨髓瘤或卵巢癌患者的2,777,595次实验室测量数据上训练的Transformer模型，成功预测了涵盖8个临床类别的162种治疗相关并发症（包括治疗相关骨髓增生异常综合征）的两年内发生风险，在群体层面实现了1.5至6.1倍的富集提升。该模型在分组终点上匹配或超越了非时序基线方法（AUROC提升最高达+0.11），证明了纵向实验室轨迹能够捕捉孤立测量值无法获取的、反映并发症特异性生理状态的演化过程。预测结果可跨两种癌症类型泛化，其差异集中在疾病特异性并发症上，生物标志物掩码分析恢复了与已知病理生理机制一致的特征。在MIMIC-IV和MMRF CoMMpass上的外部验证证实了跨独立医疗系统的可迁移性（AUROC最高达0.85）。常规肿瘤学实验室数据在临床发病前数周至数月即编码了器官功能恶化信号，从而能够在无需额外检测设施的情况下实现并发症特异性监测。

</details>


### [367] [A Theoretical Analysis of Memory and Overfitting Phenomena in Stochastic Interpolation Models](https://arxiv.org/abs/2606.08554)
*Yunchen Li,Shaohui Lin,Zhou Yu*

Main category: cs.LG

TL;DR: 本文从理论上分析了随机插值模型中的记忆化现象，证明在连续时间理想设置下，确定性和随机生成过程都能恢复训练样本；在离散化和存在估计误差的情况下，生成样本可表示为训练样本加上三个可控扰动项，并据此给出了生成模型中过拟合和欠拟合的理论定义。

<details>
  <summary>Details</summary>

Motivation: 为随机插值模型中的记忆化现象提供严格的理论解释，并建立生成模型中过拟合与欠拟合的理论基础。

Method: 利用最优速度场和分数函数的闭式表达式，从理论上分析连续时间理想设置、Euler离散化设置以及存在估计误差设置下生成样本与训练样本之间的关系。

Result: 1) 连续时间理想设置下，确定性和随机生成过程均能恢复训练样本；2) Euler离散化下，生成样本以训练样本为中心，偏差由步长控制；3) 存在估计误差时，累积估计误差控制端点与训练集的偏差；4) 生成样本表示为训练样本加上离散化误差项、估计误差项和随机高斯噪声项。

Conclusion: 基于上述表征，给出了生成模型中过拟合和欠拟合的理论定义，并通过合成模拟实验验证了理论发现。

Original Abstract: This paper provides a theoretical account of memorization in stochastic interpolation models. By leveraging closed-form expressions for the optimal velocity field and the associated score function, we show that, in the continuous-time oracle setting, both deterministic and stochastic generation processes recover training samples. Under Euler discretization, generated samples remain centered around training samples, with deviations controlled by the step size. We further analyze generation in the presence of estimation errors and show that accumulated estimation errors control the endpoint deviation from the training set. These results imply that the generated sample admits a representation as a training sample perturbed by three controlled terms: a discretization-induced bound, an estimation-error-induced bound, and stochastic Gaussian noise. Based on this characterization, we provide theoretical definitions of overfitting and underfitting in generative models. Synthetic simulations support our theoretical findings.

中文摘要翻译: 本文为随机插值模型中的记忆化现象提供了理论解释。通过利用最优速度场及相关分数函数的闭式表达式，我们证明在连续时间理想设置下，确定性和随机生成过程均能恢复训练样本。在Euler离散化下，生成样本仍以训练样本为中心，偏差由步长控制。我们进一步分析了存在估计误差时的生成过程，并证明累积估计误差控制着端点与训练集的偏差。这些结果表明，生成样本可表示为训练样本加上三个可控扰动项：离散化引起的偏差、估计误差引起的偏差以及随机高斯噪声。基于这一表征，我们给出了生成模型中过拟合与欠拟合的理论定义。合成模拟实验支持了我们的理论发现。

</details>


### [368] [Physics-Guided Dual Decoding and Spectral Supervision for Global 3D Hydrometeor Prediction](https://arxiv.org/abs/2606.08563)
*Dandan Chen,Yaqiang Wang*

Main category: cs.LG

TL;DR: PredHydro-Net 是一个物理引导的双解码框架，通过解耦架构、小波频率解耦、谱幅度匹配和对抗训练，有效缓解了三维水凝物预报中的过度平滑问题，在极端事件检测和谱表示上优于 Earthformer、PredRNNv2 和 GFS 等基线，并能合理再现极端天气中的三维云结构。

<details>
  <summary>Details</summary>

Motivation: 全球数据驱动模型在预测连续大气变量方面表现优异，但三维水凝物预报因变量存在零膨胀和长尾分布特性而极具挑战。标准深度学习优化常产生过度平滑的预测，削弱极端事件和空间纹理，亟需一种能缓解平滑效应的物理引导方法。

Method: 提出 PredHydro-Net 物理引导双解码框架：(1) 解耦架构，宏观热力学和动力学场单向调控水凝物生成，解决多变量优化冲突；(2) 集成小波基频率解耦、谱幅度匹配和对抗训练，实现定量精度与空间保真度的平衡。

Result: 在 72 小时全球评估中，PredHydro-Net 在极端事件检测和谱表示上优于 Earthformer、PredRNNv2 和 GFS；与 GPM 卫星反演具有良好气候一致性；能合理再现飓风 Ian 等极端天气的三维云结构；特征归因证实其依赖相对湿度和风辐合等物理前兆。

Conclusion: PredHydro-Net 通过物理引导的双解码框架有效解决了水凝物预报中的过度平滑问题，为长尾大气预测提供了一种鲁棒的、物理信息增强的方法。

Original Abstract: While global data-driven models excel at predicting continuous atmospheric variables, three-dimensional hydrometeor forecasting remains challenging due to the zero-inflated, long-tailed distributions of these variables. Standard deep learning optimization often yields overly smooth forecasts, attenuating extreme events and spatial textures. We propose PredHydro-Net, a physics-guided dual-decoding framework that mitigates this smoothing. To resolve multi-variable optimization conflicts, it employs a decoupled architecture where macroscopic thermodynamic and dynamic fields unidirectionally modulate hydrometeor generation. By integrating wavelet-based frequency decoupling, spectral amplitude matching, and adversarial training, the model achieves a favorable trade-off between quantitative accuracy and spatial fidelity. In a 72-h global evaluation, PredHydro-Net outperforms both spatiotemporal deep learning baselines (Earthformer and PredRNNv2) and the operational Global Forecast System (GFS) in extreme-event detection and spectral representation. Furthermore, it demonstrates strong climatological consistency with Global Precipitation Measurement (GPM) satellite retrievals. The model reasonably reproduces the three-dimensional cloud structures in extreme weather events, such as Hurricane Ian. Feature attribution confirms its dependence on physical precursors such as relative humidity and wind convergence, offering a robust, physics-informed approach to long-tailed atmospheric prediction.

中文摘要翻译: 尽管全局数据驱动模型在预测连续大气变量方面表现出色，但由于三维水凝物变量存在零膨胀和长尾分布特性，其预报仍具挑战性。标准深度学习优化往往产生过度平滑的预报，削弱极端事件和空间纹理。我们提出 PredHydro-Net，一种物理引导的双解码框架，可缓解这种平滑效应。为解决多变量优化冲突，该框架采用解耦架构，其中宏观热力学和动力学场单向调控水凝物的生成。通过集成基于小波的频率解耦、谱幅度匹配和对抗训练，该模型在定量精度与空间保真度之间取得了有利的平衡。在 72 小时全球评估中，PredHydro-Net 在极端事件检测和谱表示方面均优于时空深度学习基线模型（Earthformer 和 PredRNNv2）以及业务化全球预报系统（GFS）。此外，该模型展现出与全球降水测量（GPM）卫星反演数据良好的气候一致性。该模型能合理再现飓风伊恩等极端天气事件中的三维云结构。特征归因分析证实其对相对湿度和风辐合等物理前兆的依赖，为长尾大气预测提供了一种鲁棒的、物理信息增强的方法。

</details>


### [369] [EinSort: Sorting is All We Need for Tensorizing LLM](https://arxiv.org/abs/2606.08565)
*Toshiaki Koike-Akino,Jing Liu,Ye Wang*

Main category: cs.LG

TL;DR: 提出了一种自适应张量化方法，通过索引排序发现目标张量中的固有低秩结构，用于压缩大模型中的权重和KV缓存，相比基线方法提升了重建质量。

<details>
  <summary>Details</summary>

Motivation: 张量网络能有效压缩神经网络，但现有方法难以在大规模基础模型中识别隐式低秩结构，因为这类模型规模巨大且权重分布缺乏结构性。

Method: 提出自适应张量化方法，通过索引排序来发现目标张量中的固有低秩结构。

Result: 在权重压缩和KV缓存压缩实验上，相比基线方法取得了更好的重建质量。

Conclusion: 自适应张量化方法能够有效发掘大模型中的低秩结构，在压缩任务上具有优势。

Original Abstract: Tensor networks provide efficient representations for compressing large neural networks. By carefully designing shapes and topologies, they can significantly reduce memory and computational costs. However, identifying implicit low-rank structures in large foundation models remains challenging due to their enormous scale and un-structured weight distributions. We propose an adaptive tensorization method that discovers inherent low-rank structure in a target tensor by index ordering. Experiments on weight and KV-cache compression demonstrate improved reconstruction quality compared to baselines.

中文摘要翻译: 张量网络为压缩大型神经网络提供了高效的表示方法。通过精心设计形状和拓扑结构，它们可以显著降低内存和计算成本。然而，由于大规模基础模型规模巨大且权重分布缺乏结构性，在其中识别隐式低秩结构仍然具有挑战性。我们提出了一种自适应张量化方法，通过索引排序来发现目标张量中的固有低秩结构。在权重压缩和KV缓存压缩上的实验表明，与基线方法相比，该方法具有更好的重建质量。

</details>


### [370] [Titans-as-a-Layer: Test-Time Memory for Conversational Speech Emotion Recognition](https://arxiv.org/abs/2606.08573)
*Daniel Chen,Qicong Hu,Yang Xiao,Ting Dang,Hong Jia*

Main category: cs.LG

TL;DR: 本文提出了一种即插即用的Memory-as-a-Layer (MAL)适配器，在测试阶段为大型音频语言模型补充对话历史记忆，以改进对话式语音情绪识别，无需修改骨干模型结构。

<details>
  <summary>Details</summary>

Motivation: 现有语音情绪识别方法通常将对话情感建模为话语级分类，忽略了说话人的惯用音域和前序话语建立的情感语境；虽然语音-语言模型能通过微调适应SER标签，但仍缺少每轮对话的上下文状态信息。

Method: 基于Titans架构，提出Memory-as-a-Layer (MAL)适配器，将对话历史写入小型神经记忆，并以音频标记对齐的残差更新方式读回，避免修改主模型的标记位置，实现即插即用。

Result: 在不同音频大语言模型和情感识别数据集上，该方法在多项评估指标上均提升了SER性能，验证了测试时记忆作为残差上下文机制的有效性。

Conclusion: 测试时神经记忆可以作为残差上下文机制有效补充对话式SER中缺失的语境信息，且无需改动大型音频语言模型的骨干网络。

Original Abstract: Speech emotion recognition (SER) is commonly formulated as utterance-level classification, although conversational emotion depends on a speaker's usual vocal range and the emotional context established by previous utterances. Speech-language models provide strong pretrained acoustic and semantic representations, and can adapts them to SER labels via finetune, but this mechanism still missing per-dialogue state. We study whether test-time neural memory can supply this missing context while leaving the large audio language models (LALMs) backbone intact. Building on Titans, we introduce a plug-and-play Memory-as-a-Layer (MAL) adapter that writes dialogue history into a small neural memory and reads it back as an audio-token-aligned residual update, avoiding changes to the host model's token positions. Across different audio LLMs and emotion recognition datasets evaluations, our design improves SER performs across different evaluation metrics, supporting test-time memory as a residual contextual mechanism for conversational SER.

中文摘要翻译: 语音情绪识别（SER）通常被表述为话语级分类任务，尽管对话情感实际上取决于说话人惯用的音域以及由前序话语建立的情感语境。语音-语言模型提供了强大的预训练声学与语义表征，并可通过微调将其适配到SER标签上，但这种机制仍然缺失每轮对话的状态信息。本文研究是否可以在保持大型音频语言模型（LALMs）骨干网络不变的情况下，通过测试时神经记忆来补充这一缺失的语境。基于Titans架构，我们引入了一种即插即用的Memory-as-a-Layer（MAL）适配器，它将对话历史写入一个小型神经记忆，并以音频标记对齐的残差更新方式读回，避免了修改宿主模型的标记位置。在不同音频大语言模型和情感识别数据集上的评估表明，我们的设计在多项评估指标上提升了SER性能，验证了测试时记忆作为对话式SER残差上下文机制的有效性。

</details>


### [371] [OrderDP: A Theoretically Guaranteed Lossless Dynamic Data Pruning Framework](https://arxiv.org/abs/2606.08574)
*Chenhan Jin,Shengze Xu,Qingsong Wang,Fan Jia,Dingshuo Chen,Tieyong Zeng*

Main category: cs.LG

TL;DR: 本文提出OrderDP，一种即插即用的数据剪枝框架，通过随机子集选择与top-q排序结合，实现无偏、稳定、近无损的训练加速，在多个基准数据集上降低超40%训练成本且保持竞争性精度。

<details>
  <summary>Details</summary>

Motivation: 现有数据剪枝方法倾向于选择高信息量样本，导致梯度估计有偏，且该偏差对最终性能的影响尚不明确。本文旨在解决这一偏差问题，实现理论上保证的无偏训练加速。

Method: OrderDP首先随机选择一个子集，然后从中选出top-q样本。通过在代理损失上建立无偏性，确保基于代理目标的无偏训练；进一步给出收敛性和泛化性分析，阐明如何控制加速效果并保证最终性能。

Result: 在CIFAR-10、CIFAR-100和ImageNet-1K上，OrderDP相比综合基线方法实现了竞争性精度、稳定收敛和精确控制，设计更简单、运行更快，训练成本降低超40%。

Conclusion: OrderDP作为一种鲁棒且易于适配的数据高效学习工具，同时实现了强性能和计算效率。

Original Abstract: Data pruning (DP), as an oft-stated strategy to alleviate heavy training burdens, reduces the volume of training samples according to a well-defined pruning method while striving for near-lossless performance. However, existing approaches, which commonly select highly informative samples, can lead to biased gradient estimation compared to full-dataset training. Furthermore, the analysis of this bias and its impact on final performance remains ambiguous. To address these challenges, we propose OrderDP, a plug-and-play framework that aims to obtain stable, unbiased, and near-lossless training acceleration with theoretical guarantees. Specifically, OrderDP first randomly selects a subset and then chooses the top-$q$ samples, where unbiasedness is established with respect to a surrogate loss. This ensures that OrderDP conducts unbiased training in terms of the surrogate objective. We further establish convergence and generalization analyses, elucidating how OrderDP affects optimal performance and enables well-controlled acceleration while ensuring guaranteed final performance. Empirically, we evaluate OrderDP against comprehensive baselines on CIFAR-10, CIFAR-100, and ImageNet-1K, demonstrating competitive accuracy, stable convergence, and exact control -- all with a simpler design and faster runtime, while reducing training cost by over 40%. Delivering both strong performance and computational efficiency, our method serves as a robust and easily adaptable tool for data-efficient learning. The code is publicly available at https://github.com/shengze-xu/OrderDP.

中文摘要翻译: 数据剪枝（DP）作为一种常被提及的缓解繁重训练负担的策略，根据定义明确的剪枝方法减少训练样本量，同时力求实现近乎无损的性能。然而，现有方法通常选择信息量高的样本，与全数据集训练相比可能导致梯度估计有偏。此外，对这种偏差及其对最终性能影响的分析仍不清晰。为应对这些挑战，我们提出OrderDP，一个即插即用的框架，旨在从理论上有保证地实现稳定、无偏且近乎无损的训练加速。具体而言，OrderDP首先随机选择一个子集，然后从中选出top-q样本，其中无偏性是基于代理损失建立的。这确保了OrderDP在代理目标方面进行无偏训练。我们进一步建立了收敛性和泛化性分析，阐明了OrderDP如何影响最优性能以及如何在保证最终性能的前提下实现可控的加速。在实验上，我们在CIFAR-10、CIFAR-100和ImageNet-1K上对OrderDP与综合基线进行了评估，展示了竞争性的精度、稳定的收敛和精确的控制——所有这些都采用了更简单的设计和更快的运行时间，同时将训练成本降低了超过40%。我们的方法兼具强性能和计算效率，作为一种鲁棒且易于适配的数据高效学习工具。代码已在https://github.com/shengze-xu/OrderDP公开。

</details>


### [372] [Lost in the Non-convex Loss Landscape: How to Fine-tune the Large Time Series Model?](https://arxiv.org/abs/2606.08578)
*Xu Zhang,Peang Wang,Wei Wang*

Main category: cs.LG

TL;DR: 提出平滑全微调（SFF）技术，通过随机初始化辅助模型并线性插值来平滑预训练大时间序列模型的损失景观，提升可训练性并缓解过拟合，在8种代表性LTSM上取得一致提升。

<details>
  <summary>Details</summary>

Motivation: 预训练的大时间序列模型（LTSM）存在病态非凸损失景观，导致可训练性受限，直接微调容易过拟合甚至不如从头训练，大幅削弱预训练优势。

Method: 构建一个随机初始化的辅助LTSM以获得更平滑的损失景观，然后将其权重与预训练模型权重进行线性插值，从而平滑原始损失景观。这一过程在保留预训练知识的同时提升可训练性，进而实现更有效的下游微调。

Result: 在8种代表性LTSM（Timer、TimesFM、MOMENT、UniTS、MOIRAI、Chronos、TTMs、Sundial）的多个下游任务上进行了广泛实验，结果表明SFF在所有模型上均取得一致的性能提升。

Conclusion: SFF通过平滑损失景观扰动尖锐最小值而不显著破坏平坦区域，帮助模型逃离较差的局部盆地，收敛到更平滑、泛化性更好的解，有效恢复了预训练微调范式的优势。

Original Abstract: Recently, large time series models (LTSMs) have gained increasing attention due to their similarities to large language models, including flexible context length, scalability, and task generality, outperforming advanced task-specific models. However, prior studies indicate that pre-trained LTSMs may exhibit a poorly conditioned non-convex loss landscape, leading to limited trainability. As a result, direct fine-tuning tends to cause overfitting and suboptimal performance, sometimes even worse than training from scratch, substantially diminishing the benefits of pre-training. To overcome this limitation, we propose Smoothed Full Fine-tuning (SFF), a novel fine-tuning technology. Specifically, we construct an auxiliary LTSM via random initialization to obtain a smoother loss landscape, and then linearly interpolate its weights with those of the pre-trained model to smooth the original landscape. This process improves trainability while preserving pre-trained knowledge, thereby enabling more effective downstream fine-tuning. From an optimization perspective, SFF perturbs sharp minima without significantly harming flat regions, facilitating escape from poor local basins toward smoother and more generalizable solutions. Extensive experiments on benchmark datasets demonstrate consistent improvements across eight representative LTSMs, including Timer, TimesFM, MOMENT, UniTS, MOIRAI, Chronos, TTMs, and Sundial, on diverse downstream tasks. The code is available at the link: https://github.com/Meteor-Stars/SFF.

中文摘要翻译: 近期，大规模时间序列模型（LTSMs）因其与大型语言模型的相似性（包括灵活的上下文长度、可扩展性和任务通用性）而受到越来越多的关注，其性能超越了先进的任务专用模型。然而，先前研究表明，预训练的LTSMs可能存在条件不佳的非凸损失景观，导致可训练性受限。因此，直接微调往往会导致过拟合和次优性能，有时甚至不如从头训练，这大大削弱了预训练的优势。为克服这一局限，我们提出平滑全微调（Smoothed Full Fine-tuning, SFF），一种新颖的微调技术。具体而言，我们通过随机初始化构建一个辅助LTSM以获得更平滑的损失景观，然后将其权重与预训练模型的权重进行线性插值，以平滑原始的损失景观。该过程在保留预训练知识的同时提升了可训练性，从而实现更有效的下游微调。从优化角度来看，SFF扰动尖锐最小值而不显著损害平坦区域，有助于逃离较差的局部盆地，收敛到更平滑且更具泛化能力的解。在基准数据集上的大量实验表明，SFF在八种代表性LTSM（包括Timer、TimesFM、MOMENT、UniTS、MOIRAI、Chronos、TTMs和Sundial）的多种下游任务上均取得一致的改进。代码已开源：https://github.com/Meteor-Stars/SFF。

</details>


### [373] [Convolutional Sparse Coding via the Locally Competitive Algorithm on Loihi 2](https://arxiv.org/abs/2606.08584)
*Geoffrey Kasenbacher,Daniel Ruepp,Gerrit A. Ecke*

Main category: cs.LG

TL;DR: 本文首次在Loihi 2神经形态芯片上实现了卷积稀疏编码的局部竞争算法（LCA），并针对实际稀疏推理任务与GPU基线进行了对比评估，明确了卷积LCA在神经形态硬件上具有吸引力的运行区间。

<details>
  <summary>Details</summary>

Motivation: 现有研究已在Loihi 2上实现了非卷积LCA，但卷积LCA具有空间结构、权重共享、重叠感受野等更接近实际稀疏推理负载的特性，尚未有相关工作。因此，作者希望在神经形态硬件上实现卷积LCA并评估其性能优势与适用场景。

Method: 采用单层递归LCA公式，将其扩展至卷积特征图形式，利用从成对滤波器交互导出的局部抑制核实现侧向抑制，并在Loihi 2芯片上实现完整的卷积稀疏编码推理流程。

Result: 在相同推理任务上，将Loihi 2实现与常规GPU基线进行对比基准测试，揭示了卷积稀疏推理在神经形态硬件上具有吸引力的运行区间。

Conclusion: 卷积LCA可作为新兴神经形态系统上有结构稀疏推理的有效基准方法，有助于指导未来神经形态硬件上的稀疏编码应用设计。

Original Abstract: Sparse coding provides a principled framework for signal representation by expressing an input as a linear combination of only a small number of basis functions. The Locally Competitive Algorithm (LCA) is particularly attractive in the context of neuromorphic computing because its dynamics, leaky integration, thresholding, and lateral inhibition map naturally to neuromorphic hardware. While prior work has studied non-convolutional LCA on Loihi 2, the convolutional setting is of particular interest because it introduces spatial structure, weight sharing, overlapping receptive fields, and scaling behavior that are more representative of practical sparse inference workloads. In this work, we present a Loihi 2 implementation of convolutional sparse coding via the LCA and evaluate it against a conventional GPU baseline on the same inference problems. The implementation follows a one-layer recurrent LCA formulation and extends it to convolutional feature maps with local inhibitory kernels derived from pairwise filter interactions. To the best of our knowledge, this is the first implementation and benchmark of convolutional LCA on Loihi 2. Our goal is not only to demonstrate feasibility, but also to clarify in which operating regimes convolutional sparse inference becomes attractive on neuromorphic hardware. The resulting study positions convolutional LCA as a useful benchmark for structured sparse inference on emerging neuromorphic systems.

中文摘要翻译: 稀疏编码通过将输入表示为少量基函数的线性组合，为信号表示提供了原则性框架。局部竞争算法（LCA）在神经形态计算中尤为吸引人，因为其动态特性——漏积分、阈值化和侧向抑制——自然地映射到神经形态硬件上。虽然已有工作研究了Loihi 2上的非卷积LCA，但卷积设置尤为值得关注，因为它引入了空间结构、权重共享、重叠感受野以及缩放行为，这些特性更贴近实际稀疏推理负载。本文提出了一种通过LCA实现卷积稀疏编码的Loihi 2实现，并在相同推理问题上与常规GPU基线进行了对比评估。该实现遵循单层递归LCA公式，并将其扩展至带有由成对滤波器交互导出的局部抑制核的卷积特征图。据我们所知，这是首个在Loihi 2上实现并基准测试卷积LCA的工作。我们的目标不仅是证明可行性，还旨在阐明在何种运行区间内卷积稀疏推理在神经形态硬件上具有吸引力。本研究成果将卷积LCA定位为新兴神经形态系统上有结构稀疏推理的有用基准。

</details>


### [374] [Quantum Global Variational Learning for Quantum Error Correction](https://arxiv.org/abs/2606.08592)
*Shun Ryuzaki,Hideo Mukai*

Main category: cs.LG

TL;DR: 提出一种全局结构的量子神经网络，通过减少量子电路中需要的酉矩阵数量，在量子纠错任务中实现了训练时间降低97%、训练完成率提升25%、最终达到100%训练成功率，并提升了抗内部噪声的鲁棒性。

<details>
  <summary>Details</summary>

Motivation: 高效的量子纠错对量子计算的发展至关重要，现有方法训练时间长、完成率低，且对内部网络噪声的鲁棒性不足。

Method: 提出一种具有全局结构的量子神经网络，通过减少量子电路中酉矩阵的数量来降低计算复杂度。

Result: 训练时间减少97%，训练完成率提升25%，最终达到100%训练成功率，超越此前报告的纠错性能；在内部网络噪声下，量子纠错保真度提升15%。

Conclusion: 所提出的全局结构量子神经网络能显著提升量子纠错的效率与鲁棒性，为量子计算的实用化提供了有效途径。

Original Abstract: Efficient quantum error correction is essential for the advancement of quantum computing. We propose a quantum neural network with a global structure that reduces the number of unitary matrices required in quantum circuits. This approach resulted in a 97\% reduction in training time and up to a 25\% improvement in the training completion rate, ultimately achieving a 100\% success rate in training while surpassing the error correction performance reported in previous studies. In addition, we demonstrated the enhanced robustness of quantum error correction against internal network noise. Moreover, the fidelity of quantum error correction under internal network noise increased by up to 15\% due to the reduced computational load.

中文摘要翻译: 高效的量子纠错对于量子计算的进步至关重要。我们提出了一种具有全局结构的量子神经网络，该结构减少了量子电路中所需的酉矩阵数量。该方法使训练时间减少了97%，训练完成率提高了25%，最终实现了100%的训练成功率，同时超越了此前研究报告中所述的纠错性能。此外，我们证明了该量子纠错方法对内部网络噪声具有更强的鲁棒性。而且，由于计算负载的降低，内部网络噪声下的量子纠错保真度提升了15%。

</details>


### [375] [Reinforcement Learning for Flow-Matching Policies with Density Transport](https://arxiv.org/abs/2606.08602)
*Boshu Lei,Kostas Daniilidis,Antonio Loquercio*

Main category: cs.LG

TL;DR: 本文提出RLDT，一种结合流匹配策略与强化学习的在线微调算法，通过SVGD构建传输场并利用期望目标估计稳定训练，在连续控制任务中优于基线方法。

<details>
  <summary>Details</summary>

Motivation: 现有方法在强化学习中微调流匹配策略时，要么近似策略分布引入偏置梯度，要么使用蒸馏牺牲多模态建模能力，缺乏一种既无偏又能保留多模态能力的方法。

Method: 从最大熵强化学习目标出发，利用Stein变分梯度下降（SVGD）构建一个传输场，然后微调预训练的流匹配策略使其对齐该传输场。为解决流匹配策略多步生成动作导致的梯度优化困难，采用期望目标估计从中间去噪步骤近似策略动作，避免通过时间的反向传播不稳定性。

Result: 实验表明RLDT在奖励质量和收敛速度上均优于竞争基线，在多种连续控制任务（包括稠密/稀疏奖励、基于状态/视觉的长程机器人操作）中表现优异。

Conclusion: 将基于强化学习的策略改进视为动作密度向高奖励区域的传输，与流匹配模型的传输框架自然对齐，结合SVGD和期望目标估计，可以在无偏梯度下高效微调流匹配策略。

Original Abstract: We present an online reinforcement learning (RL) algorithm for fine-tuning flow-matching policies in continuous-control problems. Our key insight is to view RL-based policy improvement as a transport of action densities towards regions of high reward, which naturally aligns with the transport formulation of flow matching models. Prior methods either approximate the current or optimal policy distribution or resort to distillation, which introduces biased gradients or sacrifices multimodal modeling capacity. In contrast, our approach for RL with Density Transport, which we name \emph{RLDT}, constructs a transport field from a maximum-entropy RL objective using Stein Variational Gradient Descent (SVGD). Then, it finetunes a pretrained flow matching policy to align with this field. Training with this alignment objective is nontrivial because flow-matching policies generate actions via a multi-step process, making direct gradient-based optimization challenging. To overcome this challenge and stabilize training, we approximate policy actions from intermediate denoising steps via expected-target estimation. This allows the transport-field update to propagate into the network parameters without unstable backpropagation through time. Experimental results demonstrate that RLDT outperforms competitive baselines in reward quality and convergence speed. This performance holds across diverse continuous-control tasks, encompassing both dense and sparse rewards, as well as state- and vision-based long-horizon robot manipulation. The project webpage is \href{https://rpfey.github.io/rldt/}{https://rpfey.github.io/rldt/}.

中文摘要翻译: 我们提出一种用于连续控制问题中微调流匹配策略的在线强化学习算法。关键洞察在于，将基于强化学习的策略改进视为动作密度向高奖励区域的传输，这与流匹配模型的传输公式自然对齐。先前的方法要么近似当前或最优策略分布，要么采用蒸馏，这引入了偏置梯度或牺牲了多模态建模能力。相比之下，我们提出的基于密度传输的强化学习方法RLDT，利用Stein变分梯度下降从最大熵强化学习目标构建一个传输场，然后微调预训练的流匹配策略以对齐该场。这一对齐目标的训练并非易事，因为流匹配策略通过多步过程生成动作，使得直接的梯度优化具有挑战性。为克服这一挑战并稳定训练，我们通过期望目标估计从中间去噪步骤近似策略动作。这使得传输场更新能够传播到网络参数中，而无需不稳定的通过时间反向传播。实验结果表明，RLDT在奖励质量和收敛速度上均优于竞争基线。该性能在多种连续控制任务中保持稳定，涵盖稠密和稀疏奖励，以及基于状态和视觉的长程机器人操作。项目网页：https://rpfey.github.io/rldt/。

</details>


### [376] [Tyan-WP: A Wind Power Foundation Model for Ultra-Short-Term Probabilistic Forecasting](https://arxiv.org/abs/2606.08630)
*Jiahui Huang,Ao Luo,Lei Liu,Hongwei Zhao,Tengyuan Liu,Ruibo Guo,Bo Wang,Zhao Wang,Bin Li*

Main category: cs.LG

TL;DR: 本文提出Tyan-WP，首个用于超短期概率预测的风电基础模型。该模型通过静态站点嵌入和功率感知气象融合模块，在零样本场景下显著优于传统站点特定时间序列模型和通用大时间序列模型，并在跨地域泛化中表现优异。

<details>
  <summary>Details</summary>

Motivation: 现有站点特定的时间序列模型（TSMs）在数据稀缺场景下泛化能力差，而通用大时间序列模型（LTSMs）大多局限于单变量输入，无法充分利用静态站点属性及功率与气象协变量之间的依赖关系，导致预测精度不足。风电行业迫切需要高精度的风电基础模型以缩短新风机组的调试并网周期。

Method: Tyan-WP采用两个领域特定模块设计：(1) 静态站点嵌入（利用坐标、地形和生态区域元数据）；(2) 功率感知气象融合（PAMF）模块，用于建模历史功率与气象协变量之间的交互关系。模型在覆盖美国12.6万+站点、7年数据的大规模风电数据集上预训练。

Result: 在统一评估协议下，Tyan-WP在10个域内站点上超越8个站点特定监督TSMs，在127个域内站点上超越11个通用LTSMs：MAE降低19.9%，RMSE降低16.6%，CRPS降低22.2%，AQL降低21.7%，R²提升16.7%。在6个真实英国站点上展现出强大的跨地域泛化能力。

Conclusion: 风电基础模型能够在无需目标站点训练的情况下实现准确的零样本预测，为新风电机组快速并网和概率风险管理提供了实用路径。

Original Abstract: Global wind power capacity, especially in China, is booming, with new farms spanning diverse terrains and climates. The industry urgently needs accurate wind power foundation models to shorten commissioning and accelerate grid connection. This is because site-specific time series models (TSMs) are not well suited to data-scarce scenarios and generalize poorly, while generic large time series models (LTSMs) are mostly limited to univariate inputs and cannot fully exploit static site attributes or the dependencies between power and meteorological covariates, leading to insufficient accuracy. To fill this gap, we propose \textbf{Tyan-WP}, the first wind power foundation model for ultra-short-term probabilistic forecasting. Pretrained on a large-scale wind power dataset covering more than 126,000 U.S. sites over seven years, Tyan-WP further improves zero-shot forecasting through two domain-specific module designs: static site embedding using coordinate, terrain, and ecoregion metadata, and a power-aware meteorological fusion (PAMF) module that models interactions between historical power and meteorological covariates. Under a unified evaluation protocol, Tyan-WP surpasses eight site-specific supervised TSMs on 10 in-domain sites and outperforms eleven generic LTSMs on 127 in-domain sites, reducing MAE by 19.9%, RMSE by 16.6%, CRPS by 22.2%, and AQL by 21.7%, while raising R^2 by 16.7%. It further demonstrates strong cross-geography generalization on six real U.K. sites. These results show that the wind power foundation model can achieve accurate zero-shot forecasting without target-site training, providing a practical pathway for rapid turbine onboarding and probabilistic risk management at new wind farms.

中文摘要翻译: 全球风电装机容量正在快速增长，尤其是在中国，新建风电场覆盖了多样化的地形和气候条件。该行业迫切需要高精度的风电基础模型，以缩短调试周期并加速并网。这是因为针对特定站点的时间序列模型（TSMs）在数据稀缺场景下适应性差且泛化能力弱，而通用大规模时间序列模型（LTSMs）大多局限于单变量输入，无法充分利用静态站点属性或功率与气象协变量之间的依赖关系，导致精度不足。为填补这一空白，我们提出\textbf{Tyan-WP}，首个用于超短期概率预测的风电基础模型。该模型在覆盖美国12.6万多个站点、长达七年的大规模风电数据集上预训练，并通过两个领域特定模块设计进一步提升零样本预测能力：一是利用坐标、地形和生态区域元数据的静态站点嵌入；二是功率感知气象融合（PAMF）模块，用于建模历史功率与气象协变量之间的交互关系。在统一评估协议下，Tyan-WP在10个域内站点上超越8个站点特定监督TSMs，在127个域内站点上超越11个通用LTSMs，MAE降低19.9%，RMSE降低16.6%，CRPS降低22.2%，AQL降低21.7%，R²提升16.7%。该模型还在6个真实的英国站点上展现出强大的跨地域泛化能力。这些结果表明，风电基础模型能够在无需目标站点训练的情况下实现准确的零样本预测，为新风电机组的快速并网和概率风险管理提供了实用路径。

</details>


### [377] [SpectrumKV: Per-Token Mixed-Precision KV Cache Transfer for Prefill-Decode Disaggregated LLM Serving](https://arxiv.org/abs/2606.08635)
*Yang Pengju*

Main category: cs.LG

TL;DR: 提出SpectrumKV，将PD分离架构中的KV缓存传输从二元丢弃问题重新定义为精度分配问题，根据token重要性分配FP16/INT8/INT4不同精度，并通过轻量级部署时探测自适应选择策略，在相同传输预算下显著优于传统剪枝方法。

<details>
  <summary>Details</summary>

Motivation: 现有PD分离架构的KV缓存缩减方法多为二元选择（全精度传输或完全不传），忽略了中间精度空间，导致有用设计空间未被利用。

Method: SpectrumKV为每个token分配不同精度等级：注意力汇聚点和高重要性token使用FP16，中等重要性token使用INT8，低重要性token使用INT4（若模型可容忍）。为避免INT4量化导致模型崩溃，设计轻量级部署时探测方法——在三种策略下进行三次激进的NIAH试验，通过的模型使用FP16+INT8+INT4三级策略，失败的退化为FP16+INT8两级策略。

Result: 在Qwen2.5-7B-Instruct、Mistral-7B-Instruct-v0.3和Gemma-2-9B-it上，50%归一化KV预算下WikiText-2困惑度变化分别为+1.97%、-0.06%、-0.44%，远优于PDTrim的+25.85%、+22.07%、+35.63%。NIAH检索任务中，Qwen在激进预算下达到52.6%（PDTrim仅26.3%），b=0.5时达100%。端到端GPU传输延迟显示TTFT降低50-62%。

Conclusion: PD分离架构中的KV缓存传输应被视作精度分配问题，而非单纯的token剪枝问题，自适应精度分配策略能更有效地利用传输预算。

Original Abstract: Prefill-decode (PD) disaggregation decouples prompt processing from token generation, but it also turns the key-value (KV) cache into a network payload. Existing PD-side KV reduction methods are mostly binary: selected tokens are transmitted at full precision and the rest are not transmitted. This paper argues that binary selection leaves a useful design space unused. SpectrumKV assigns a precision level to each token instead: attention sinks and other high-importance tokens are protected at FP16, medium-importance tokens are sent at INT8, and low-importance tokens are sent at INT4 when the model can tolerate it. The main practical complication is that INT4 tolerance is model-dependent. Qwen2.5-7B catastrophically fails under INT4 KV quantization, while Mistral-7B and Gemma-2-9B remain stable. SpectrumKV therefore runs a lightweight deployment-time probe: three aggressive NIAH trials under a 3-tier policy. Models that pass use FP16+INT8+INT4; models that fail fall back to FP16+INT8. Across Qwen2.5-7B-Instruct, Mistral-7B-Instruct-v0.3, and Gemma-2-9B-it, SpectrumKV improves quality at the same transfer budget. At a 50% normalized KV budget on WikiText-2, SpectrumKV changes perplexity by +1.97%,-0.06%, and-0.44%, respectively, compared with PDTrim's +25.85%, +22.07%, and +35.63%. On NIAH retrieval at 4096 tokens, the adaptive policy reaches 52.6% on Qwen at the aggressive b=0.3 budget versus 26.3% for PDTrim, and reaches 100% by b=0.5; Mistral and Gemma preserve retrieval under the 3-tier policy. End-to-end GPU timing of the transfer path shows 50-62% TTFT reductions at b=0.5. These results suggest that PD KV transfer should be treated as a precision-allocation problem, not only as token pruning.

中文摘要翻译: 预填充-解码（PD）分离将提示处理与token生成解耦，但也使得键值（KV）缓存成为网络传输负载。现有的PD端KV缩减方法大多是二元的：选中的token以全精度传输，其余token则不传输。本文认为二元选择留下了未被使用的高价值设计空间。SpectrumKV为每个token分配一个精度等级：注意力汇聚点和其他高重要性token受FP16保护，中等重要性token以INT8发送，低重要性token在模型可容忍的情况下以INT4发送。主要实践挑战在于INT4容忍度是模型相关的：Qwen2.5-7B在INT4 KV量化下灾难性地失败，而Mistral-7B和Gemma-2-9B保持稳定。因此，SpectrumKV运行一个轻量级的部署时探测：在三级策略下进行三次激进的NIAH试验。通过的模型使用FP16+INT8+INT4；失败的退化为FP16+INT8。在Qwen2.5-7B-Instruct、Mistral-7B-Instruct-v0.3和Gemma-2-9B-it上，SpectrumKV在相同传输预算下提升了质量。在WikiText-2上以50%归一化KV预算运行时，SpectrumKV将困惑度分别改变了+1.97%、-0.06%和-0.44%，而PDTrim分别为+25.85%、+22.07%和+35.63%。在4096 token的NIAH检索中，自适应策略在激进的b=0.3预算下在Qwen上达到52.6%（PDTrim为26.3%），并在b=0.5时达到100%；Mistral和Gemma在三级策略下保持检索能力。传输路径的端到端GPU计时显示在b=0.5时TTFT降低50-62%。这些结果表明PD KV传输应被视为精度分配问题，而不仅仅是token剪枝问题。

</details>


### [378] [Operator learning for the 2D incompressible Navier-Stokes equations: a conformal prediction approach in the data-scarce regime](https://arxiv.org/abs/2606.08654)
*Weinan Wang,Bowen Gang,Hao Deng*

Main category: cs.LG

TL;DR: 提出了一种基于扰动的共形预测框架，用于神经算子（特别是傅里叶神经算子）的不确定性量化。该方法通过比较两个在近乎相同数据集上训练的算子（一个用原始标签，一个用加高斯噪声的标签）来构建局部不确定性尺度，在2D Navier-Stokes方程基准上实现了比现有方法更窄的共形带，同时保持目标覆盖率的优点。

<details>
  <summary>Details</summary>

Motivation: 神经算子作为昂贵的PDE求解器的快速替代方案，本身无法为时空场预测提供校准的不确定性估计。现有方法需要额外的网络或分割数据，在数据稀缺场景下效率低下。因此需要一种样本高效的不确定性量化方法。

Method: 将训练好的傅里叶神经算子（FNO）与分裂共形预测相结合。构造两个在近乎相同数据集上训练的算子：一个用原始标签训练，另一个用添加小高斯噪声的标签训练。通过比较两个算子的预测差异来构建局部不确定性尺度，从而生成共形预测带。

Result: 在2D Navier-Stokes方程基准上，在匹配总数据预算的条件下，基于扰动的方法比现有方法产生的共形带更窄，同时保持目标同时覆盖率。

Conclusion: 扰动敏感性是共形化神经算子的一种实用且样本高效的不确定性代理方法。

Original Abstract: In this paper, we propose a perturbation-based conformal prediction framework for uncertainty quantification in operator learning, with a focus on the 2D Navier--Stokes equations. While neural operators provide fast surrogates for expensive PDE solvers, they do not by themselves provide calibrated uncertainty for spatiotemporal field predictions. Our approach wraps a trained Fourier Neural Operator (FNO) with split conformal prediction and constructs the local uncertainty scale by comparing the predictions of two operators trained on nearly identical datasets: one on the original labels and one on labels perturbed by small Gaussian noise. We consider this procedure in the data-scarce regime, where the total label budget is fixed and methods that require a separate uncertainty network must divide training data between multiple models. On the 2D Navier--Stokes benchmark, the perturbation-based method produces substantially narrower conformal bands than existing methods under matched total data budgets while maintaining the target simultaneous coverage. These results suggest that perturbation sensitivity is a practical and sample-efficient uncertainty proxy for conformalized neural operators.

中文摘要翻译: 本文提出了一个基于扰动的共形预测框架，用于神经算子学习中的不确定性量化，重点关注二维Navier-Stokes方程。虽然神经算子为昂贵的偏微分方程求解器提供了快速替代方案，但它们本身无法为时空场预测提供校准的不确定性估计。我们的方法将训练好的傅里叶神经算子（FNO）与分裂共形预测相结合，通过比较两个在近乎相同数据集上训练的算子——一个使用原始标签，另一个使用添加小高斯噪声的标签——来构建局部不确定性尺度。我们在数据稀缺场景下考虑该过程，其中总标签预算固定，而需要单独不确定性网络的方法必须在多个模型之间分配训练数据。在二维Navier-Stokes基准测试上，在匹配总数据预算的条件下，基于扰动的方法产生的共形带显著窄于现有方法，同时保持目标同时覆盖率。这些结果表明，扰动敏感性是共形化神经算子的一种实用且样本高效的不确定性代理方法。

</details>


### [379] [SkillHone: A Harness for Continual Agent Skill Evolution Through Persistent Decision History](https://arxiv.org/abs/2606.08671)
*Zhiwei Li,Yong Hu*

Main category: cs.LG

TL;DR: SkillHone 提出了一种基于持久决策历史的智能体技能持续演化框架，通过记录完整决策过程（诊断、修订、证据、结果），利用角色分离的子智能体在实践探针上运行并生成修订，在开放网页环境下的深度研究基准测试中，其生成的技能在 GAIA 和 WebWalkerQA-EN 上分别超越商业检索服务的深度研究智能体 15.8 和 3.2 分，并优于先前技能演化方法。

<details>
  <summary>Details</summary>

Motivation: 现有方法在有限运行中改进技能，仅保留最终产物，丢弃了后续智能体理解先前修订、评估和被拒方案所需的决策历史，导致跨会话的优化无法利用历史经验。

Method: SkillHone 构建了一个持续技能演化框架：将技能修订与评估侧证据配对以提供实践反馈，记录结构化的诊断、修订、证据和结果历史；采用角色分离的子智能体在实践探针上运行候选技能（附带脱敏报告），并基于先前决策生成修订，实现跨会话的精细优化而无需重新发现过往推理。

Result: 在开放网页环境下，以 Qwen3.6-35B-A3B 为评估骨干，SkillHone 生成的技能在 GAIA 上超越基于商业检索服务的深度研究智能体 15.8 分，在 WebWalkerQA-EN 上超越 3.2 分，并优于先前的技能演化方法。

Conclusion: SkillHone 通过持久决策历史记录和角色分离的协作机制，有效实现了智能体技能的持续演化与跨会话优化，显著提升了在开放网络环境下的深度研究任务性能。

Original Abstract: Agent skills extend language-model agents with task-specific procedures, scripts, and references, but the tasks and environments they target continually change. Existing methods improve skills in bounded runs and retain only the final artifact, discarding the decision history that later agents need to interpret prior revisions, evaluations, and rejected alternatives. We introduce SkillHone, a harness for continual agent skill evolution grounded in persistent decision history. SkillHone pairs skill revisions with evaluation-side evidence that supplies practice feedback, recording structured histories of diagnoses, revisions, evidence, and outcomes. Role-separated subagents run candidate skills on practice probes with redacted reporting and propose revisions informed by prior decisions, enabling cross-session refinement without rediscovering past rationale. We evaluate SkillHone on deep-research benchmarks in a raw open-web setting, where agents are not given an integrated search stack and must organize retrieval through portable skills. We compare against a deep-research agent backed by commercial retrieval services. With Qwen3.6-35B-A3B as the evaluation-time backbone, the resulting skills outperform the deep-research agent by 15.8 points on GAIA and 3.2 points on WebWalkerQA-EN, while also exceeding prior skill-evolution methods.

中文摘要翻译: 智能体技能通过任务特定的程序、脚本和参考信息扩展了语言模型智能体的能力，但它们所针对的任务和环境不断变化。现有方法在有限运行中改进技能，仅保留最终产物，丢弃了后续智能体理解先前修订、评估和被拒替代方案所需的决策历史。我们引入 SkillHone，一个基于持久决策历史的持续智能体技能演化框架。SkillHone 将技能修订与评估侧证据配对以提供实践反馈，记录结构化的诊断、修订、证据和结果历史。角色分离的子智能体在实践探针上运行候选技能并生成脱敏报告，基于先前决策提出修订，从而实现跨会话的精细优化而无需重新发现过往推理。我们在原始开放网页环境下对 SkillHone 进行了深度研究基准测试评估，在此环境中智能体未获得集成的搜索堆栈，必须通过可移植技能组织检索。我们将其与由商业检索服务支持的深度研究智能体进行了比较。以 Qwen3.6-35B-A3B 作为评估骨干，所生成的技能在 GAIA 上超越深度研究智能体 15.8 分，在 WebWalkerQA-EN 上超越 3.2 分，同时也优于先前的技能演化方法。

</details>


### [380] [Activation Steering Induces Emergent Misalignment: A More Comprehensive Evaluation](https://arxiv.org/abs/2606.08682)
*Qi Cao,Jian Lou,Meiting Liu,Wenjie Feng,Dan Li,See-Kiong Ng,Anh Tuan Luu*

Main category: cs.LG

TL;DR: 本文系统研究了激活引导（Activation Steering）作为一种推理时技术，能否在大型语言模型中诱发突现失调（Emergent Misalignment, EM）。研究发现激活引导不仅能在Qwen-3.5等模型上引发广泛失调，而且其产生的有害响应在语义相关性和连贯性上甚至强于微调方法，揭示了激活引导是未被充分重视的EM来源。

<details>
  <summary>Details</summary>

Motivation: 尽管微调引发的突现失调已被广泛研究，但激活引导作为一种日益流行的推理时模型控制技术，能否诱发突现失调尚缺乏系统探索，且现有先驱工作评估范围有限。本文旨在填补这一空白。

Method: 通过构建目标行为的引导向量并在推理时注入中间激活，系统评估激活引导在不同模型族（如Qwen-3.5系列）、不同规模、不同目标任务和不同干预层上诱发突现失调的能力，并分析引导强度、低秩结构、引导向量构建轮数等关键因素。

Result: (1) 激活引导可在Qwen-3.5等模型上诱发广泛失调；(2) 激活引导产生的有害响应比微调方法具有更强的语义相关性和更高的连贯性；(3) 识别了引导强度、低秩子空间结构、训练轮数等关键影响因素；(4) 揭示了激活引导在不同模型族、规模、任务和干预层上的鲁棒性与敏感性。

Conclusion: 激活引导是突现失调的重要但被低估的来源，为理解EM的机制和安全风险提供了激活空间视角。

Original Abstract: Activation steering has emerged as a popular inference-time technique for modulating the behavior of large language models (LLMs). By constructing a steering vector from examples of a target behavior and injecting it into intermediate activations during inference, activation steering enables flexible behavioral control while avoiding the permanent parameter updates required by finetuning. Meanwhile, recent work has identified emergent misalignment (EM) as a significant safety concern, wherein models finetuned on unsafe examples from a narrow task may unexpectedly generalize to broadly unsafe behavior on unrelated tasks. Although finetuning-induced EM has been extensively studied, whether activation steering can induce EM remains comparatively under-explored, despite its increasing use as a model-control technique. In this paper, we present a comprehensive study of activation-steering-induced emergent misalignment, substantially expanding the evaluation scope beyond existing pioneering work. First, we show that activation steering can induce broad misalignment, even in the recent Qwen-3.5 series. Moreover, activation-steered models produce harmful responses with stronger semantic relevance and higher coherence than their finetuned counterparts, making the resulting misalignment potentially more harmful. Second, we characterize properties of AS-induced EM by analyzing key steering-specific factors, including steering magnitude, the low-rank structure of the steering subspace, and the number of epochs during steering-vector construction. Third, we evaluate the robustness and sensitivity of AS-induced EM across diverse model families, model scales, target tasks, and intervention layers. Our findings reveal activation steering as a significant yet under-examined source of emergent misalignment and provide an activation-space perspective for understanding the mechanisms and safety risks of EM.

中文摘要翻译: 激活引导（Activation Steering）已成为一种流行的推理时技术，用于调控大型语言模型（LLM）的行为。通过从目标行为的示例中构建引导向量，并在推理时将其注入中间激活中，激活引导能够在避免微调所需的永久参数更新的同时，实现灵活的行为控制。与此同时，近期研究发现突现失调（Emergent Misalignment, EM）是一个重大的安全隐患，即模型在狭窄任务的有害示例上微调后，可能会出人意料地泛化到无关任务上的广泛有害行为。尽管微调引发的突现失调已被广泛研究，但激活引导能否诱发突现失调仍相对缺乏探索——尽管其作为一种模型控制技术的使用正在增加。在本文中，我们对激活引导引发的突现失调进行了全面研究，显著扩展了现有先驱工作的评估范围。首先，我们表明激活引导能够诱发广泛的失调，即使在最新的Qwen-3.5系列上也是如此。此外，激活引导的模型产生的有害响应比微调对应的模型具有更强的语义相关性和更高的连贯性，使得由此产生的失调可能更具危害性。其次，我们通过分析关键的引导特异性因素（包括引导强度、引导子空间的低秩结构以及引导向量构建期间的训练轮数）来刻画激活引导诱发EM的特性。第三，我们评估了激活引导诱发EM在不同模型族、模型规模、目标任务和干预层上的鲁棒性和敏感性。我们的发现揭示了激活引导是突现失调的一个被严重忽视的重要来源，并为理解EM的机制和安全风险提供了激活空间视角。

</details>


### [381] [Hierarchical Projection for Adaptive Knowledge Transfer](https://arxiv.org/abs/2606.08691)
*Samhita Pal,Tian Gu*

Main category: cs.LG

TL;DR: 提出ProjectionTL框架，通过层次贝叶斯建模与自适应投影实现跨域选择性知识迁移，同时进行源选择和特征选择，缓解负迁移并提升可解释性。

<details>
  <summary>Details</summary>

Motivation: 多源异构数据融合时，简单组合会因源域与目标域相关性差异或存在虚假信号而导致性能下降，亟需一种可信的跨域学习方法。

Method: 提出投影迁移学习框架，两阶段设计：1）构建源引导的层次先验，通过数据驱动权重聚合多源信息实现全局对齐；2）通过后验投影在特征层面选择性保留与目标信号局部一致的坐标，同时完成源选择与特征选择。

Result: 通过模拟实验和真实生物医学应用，证明该方法在准确性、稳定性和可解释性上优于现有方法。

Conclusion: ProjectionTL为跨域可信学习提供了一种可扩展、可泛化的策略，弥合了统计建模与现代机器学习范式之间的差距。

Original Abstract: Modern data-driven applications increasingly involve learning from multiple heterogeneous sources, where a target dataset is limited but related information is available across domains. Naively combining these sources can degrade performance when relevance varies or spurious signals are present, posing a fundamental challenge for trustworthy cross-domain learning. We propose Projection Transfer Learning (ProjectionTL), a unified framework that integrates hierarchical Bayesian modeling with adaptive projection for selective knowledge transfer. The key idea is to decouple transfer at two levels: first, we construct a source-guided hierarchical prior that aggregates information across sources using data-driven weights, capturing global alignment between each source and the target; second, we refine this borrowing through a posterior-projection step that operates at the feature level, selectively retaining coordinates that exhibit local agreement with the target signal. This two-stage design enables the method to simultaneously perform source selection and feature selection, thereby mitigating negative transfer while preserving interpretability. ProjectionTL provides a principled approach to integrating heterogeneous data across domains, bridging statistical modeling and modern machine learning paradigms for robust and interpretable transfer. Through simulations and real-world biomedical applications, we demonstrate improved accuracy, stability, and interpretability compared to existing methods. Our framework offers a scalable and generalizable strategy for trustworthy cross-domain learning in high-dimensional settings.

中文摘要翻译: 现代数据驱动应用越来越多地涉及从多个异构来源中学习，其中目标数据集有限但跨域相关信息可用。当相关性变化或存在虚假信号时，简单组合这些来源会降低性能，这对可信的跨域学习构成了根本性挑战。我们提出投影迁移学习（ProjectionTL），一个统一框架，将层次贝叶斯建模与自适应投影相结合，实现选择性知识迁移。关键思想是在两个层面解耦迁移：首先，我们构建一个源引导的层次先验，使用数据驱动权重聚合跨源信息，捕捉每个源与目标之间的全局对齐；其次，通过后验投影步骤在特征层面进行精炼，选择性保留与目标信号局部一致的坐标。这种两阶段设计使该方法能够同时进行源选择和特征选择，从而在保持可解释性的同时缓解负迁移。ProjectionTL提供了一种在跨域整合异构数据的原理性方法，弥合了统计建模与现代机器学习范式之间的鸿沟，实现了鲁棒且可解释的迁移。通过模拟实验和真实生物医学应用，我们证明了与现有方法相比，该方法在准确性、稳定性和可解释性方面的提升。我们的框架为高维设置下的可信跨域学习提供了一种可扩展且可泛化的策略。

</details>


### [382] [Agentic Search for Counterfactual Recourse under Fixed LLM Budgets](https://arxiv.org/abs/2606.08696)
*Yasuo Tabei*

Main category: cs.LG

TL;DR: 本文提出Comp-MCTS，一个基于蒙特卡洛树搜索的智能体框架，在有限的大语言模型调用预算下，高效生成多个经Oracle验证的反事实解释方案。

<details>
  <summary>Details</summary>

Motivation: 实际应用中，受影响的个体往往需要多个可行的反事实解释方案而非单一最优解。虽然可以用LLM生成多种方案，但LLM调用次数是主要的计算和经济成本瓶颈。因此需要在固定LLM调用预算下，高效生成一组经Oracle验证的反事实解释。

Method: 提出Comp-MCTS框架，将反事实生成建模为固定预算下的搜索问题。使用LLM生成候选干预方向，Oracle验证有效性，并结合压缩引导剪枝来分配预算，在无需训练、仅依赖Oracle的条件下最大化独特且经验证的反事实解的数量。

Result: 在四个真实表格数据集上，Comp-MCTS在生成独特且经Oracle验证的反事实解的数量上显著优于单候选LATS基线；与更强多候选变体相比，在三个数据集上以相似或更低的Oracle评估成本达到相当或更高的产出，且在邻近性、稀疏性和新颖性方面具有竞争力。

Conclusion: Comp-MCTS在固定预算约束下有效平衡了反事实解释的数量、质量和效率，为LLM智能体场景下的反事实生成提供了一种无需训练的高效方案。

Original Abstract: Counterfactual recourse aims to provide actionable feature changes that would alter an unfavorable decision made by a predictive model. In practice, affected individuals often benefit from multiple feasible alternatives rather than a single optimal explanation. A natural way to produce such alternatives is to prompt large language models (LLMs). However, prompting incurs a practical constraint: the number of LLM calls is often the dominant computational and economic cost. Together, the need for multiple alternatives and this cost constraint shift the problem from finding a single high-quality counterfactual to efficiently generating a set of oracle-validated counterfactuals under a fixed LLM-call budget. In this work, we study counterfactual recourse generation in the LLM-agentic setting as a fixed-budget search problem and propose Comp-MCTS, an agentic tree-search framework that maximizes the yield of unique, oracle-validated counterfactuals under this budget while maintaining favorable quantity--quality trade-offs. Comp-MCTS allocates the budget toward novel intervention directions via LLM-based proposal generation, oracle validation, and compression-guided pruning, in a training-free, oracle-only setting. Experiments on four real-world tabular datasets show that Comp-MCTS substantially outperforms single-candidate LATS-style baselines in the yield of unique, oracle-validated counterfactuals, and offers favorable quantity--quality--efficiency trade-offs against stronger multi-candidate variants: comparable or higher yield at similar or lower oracle-evaluation cost on three of four datasets, plus competitive proximity, sparsity, and novelty.

中文摘要翻译: 反事实解释旨在提供可操作的特征变更，以改变预测模型做出的不利决策。在实践中，受影响的个体通常受益于多个可行的替代方案，而非单个最优解释。产生此类替代方案的一种自然方式是提示大语言模型（LLM）。然而，提示方式引入了一个实际约束：LLM调用次数通常是主要的计算和经济成本。综合来看，对多种替代方案的需求以及这一成本约束，将问题从寻找单个高质量反事实转向在固定LLM调用预算下高效生成一组经Oracle验证的反事实。本文研究了LLM智能体场景下的反事实生成，将其视为一个固定预算搜索问题，并提出了Comp-MCTS，一个智能体树搜索框架，该框架在此预算下最大化独特且经Oracle验证的反事实解的数量，同时保持有利的数量-质量权衡。Comp-MCTS通过基于LLM的方案生成、Oracle验证和压缩引导剪枝，以无需训练、仅依赖Oracle的方式将预算分配给新的干预方向。在四个真实表格数据集上的实验表明，Comp-MCTS在生成独特且经Oracle验证的反事实解的数量上显著优于单候选LATS风格的基线，并且与更强的多候选变体相比，提供了有利的数量-质量-效率权衡：在四个数据集中的三个上，以相似或更低的Oracle评估成本达到相当或更高的产出，同时在邻近性、稀疏性和新颖性方面具有竞争力。

</details>


### [383] [SNR-ST-Mix: Sample-specific Neighborhood Regression Mixup for Augmented Spatial Transcriptomics Imputation with Deep Neural Network](https://arxiv.org/abs/2606.08712)
*Hongyi Yu,Yaoyu Fang,Jiahe Qian,Xinkun Wang,Lee A. Cooper,Bo Zhou*

Main category: cs.LG

TL;DR: 提出SNR-ST-Mix，一种几何和表达感知的数据增强框架，通过空间近邻约束和表达相似性加权插值，为空间转录组学回归任务生成生物合理的增强样本，在不增加模型复杂度的情况下提升预测性能。

<details>
  <summary>Details</summary>

Motivation: 空间转录组学数据存在噪声大、分辨率低、采样稀疏等问题，限制了精细空间结构的恢复。现有深度神经网络虽然在组织学表达插补上表现强大，但受限于样本量小且缺乏生物学信息增强策略；而现有增强方法多针对分类任务设计，忽略了空间和转录组关系，导致生物不合理的插值，制约了预测性能。

Method: 提出SNR-ST-Mix数据增强框架：将混合约束在斑点的k个空间最近邻内，并基于表达相似性自适应加权插值系数，生成保留局部生物结构且保证空间平滑性的增强样本，扩展有效训练流形，提升泛化能力和预测稳定性。

Result: 在多种组织类型的实验中，SNR-ST-Mix在不改变模型架构或增加额外计算的情况下，始终优于传统增强方法。

Conclusion: SNR-ST-Mix为空间转录组学回归任务提供了一种有效且生物学合理的数据增强策略，通过显式利用空间几何和转录组相似性扩展有效训练流形，在不增加模型复杂度的情况下提升预测性能。

Original Abstract: Purpose: Spatial transcriptomics (ST) enables gene expression measurements within the tissue context. However, these measurements are often noisy, low-resolution, and sparsely sampled, which limits the recovery of fine spatial structure. Deep neural networks have become powerful tools for expression imputation from histology, but their performance remains constrained by limited sample sizes and a lack of biologically informed augmentation. Most of the existing augmentation strategies for learning are designed for classification tasks rather than regression, which neglect spatial and transcriptomic relationships, leading to biologically implausible interpolations that hinder prediction performance. Approach: To address these limitations, we propose SNR-ST-Mix, a geometry- and expression-aware data augmentation framework designed specifically for ST data. It constrains mixing to a spot's k-nearest spatial neighbors and adaptively weights interpolation coefficients based on expression similarity, generating augmented samples that preserve local biological structure while ensuring spatial smoothness. This dual conditioning yields synthetic examples that expand the effective training manifold, promote generalization, and enhance prediction stability under sample-specific training. Results: Extensive experiments with various tissue types demonstrate that SNR-ST-Mix consistently outperforms conventional augmentation methods without requiring architectural changes or additional computation. Conclusions: SNR-ST-Mix provides an effective and biologically principled augmentation strategy for spatial transcriptomics regression tasks. By explicitly leveraging spatial geometry and transcriptomic similarity, it expands the effective training manifold and improves predictive performance without increasing model complexity.

中文摘要翻译: 目的：空间转录组学能够在组织背景下测量基因表达。然而，这些测量通常存在噪声大、分辨率低和采样稀疏的问题，限制了精细空间结构的恢复。深度神经网络已成为从组织学中进行表达插补的强大工具，但其性能仍受限于样本量小和缺乏生物学信息增强。现有的大多数用于学习的增强策略针对分类任务而非回归任务设计，忽略了空间和转录组关系，导致生物不合理的插值，从而阻碍了预测性能。方法：为解决这些局限性，我们提出了SNR-ST-Mix，一种专门为空间转录组学数据设计的几何和表达感知数据增强框架。它将混合约束在斑点的k个空间最近邻内，并基于表达相似性自适应地加权插值系数，生成保留局部生物结构同时确保空间平滑性的增强样本。这种双重条件化产生了扩展有效训练流形、促进泛化并在样本特定训练下增强预测稳定性的合成样本。结果：涵盖多种组织类型的大量实验表明，SNR-ST-Mix在不改变架构或增加额外计算的情况下，始终优于传统增强方法。结论：SNR-ST-Mix为空间转录组学回归任务提供了一种有效且生物学合理的数据增强策略。通过显式利用空间几何和转录组相似性，它扩展了有效训练流形，在不增加模型复杂度的情况下提高了预测性能。

</details>


### [384] [Deep Active Re-Labeling: Toward Noise-Resilient Annotation Efficiency](https://arxiv.org/abs/2606.08718)
*Md Abdullah Al Forhad,Weishi Shi*

Main category: cs.LG

TL;DR: 本文提出了一种在深度主动学习（DAL）中应对人工标注噪声的框架，通过将部分标注预算用于重新标注已标注数据（采用两种主动噪声采样策略），在相同预算下实现了更高的数据效率和更干净的标注数据集。

<details>
  <summary>Details</summary>

Motivation: 深度主动学习（DAL）虽然能降低人工标注成本，但其效果受限于人工标注错误。因为主动学习采样的数据对训练高度信息，一旦人工标注引入错误，主动学习性能会显著下降，甚至比被动学习更差。现有研究缺乏针对DAL中标注噪声问题的系统性解决方案。

Method: 受人类学习模式启发，将部分标注预算分配给已标注数据的重新标注。具体实现两种主动噪声采样策略，在不同场景下检测潜在噪声数据，并用剩余的标注预算重新标注这些实例，使主动学习具备"回顾与自省"能力。

Result: 实验表明，在相同标注预算下，该方法比基线方法数据效率更高，最终获得相对无噪声的标注数据集。

Conclusion: 通过在主动学习过程中引入重标注机制和主动噪声采样策略，可以有效缓解人工标注噪声对深度主动学习性能的负面影响，提升数据利用效率。

Original Abstract: While Deep Active Learning (DAL) effectively reduces human annotation costs, its efficacy is constrained by human annotation errors. This is because the data sampled for active learning is assumed to be highly informative for training. When human annotators introduce errors into this informative data at a certain rate, the active learning performance drops significantly and, in some cases, even exhibits worse outcomes than passive learning. In this paper, we first analyze the impact of human annotation errors in the DAL setting. Then we propose a framework to address the human annotation noise problem for DAL. Informed by human learning patterns, the core idea of our proposed solution involves allocating a portion of the human annotation budget to re-annotate data that has already been labeled. Previous theoretical work suggests that when the model possesses a certain level of ability to identify potentially noisy data, even re-labeling a small fraction of the data can effectively remove noise from the active training set. To achieve this, we implement two active noise sampling strategies to detect noise under different circumstances and allocate a part of the annotation budget to re-annotate these instances. Our approach imbues active learning with a revisiting and introspective behavior. Our experiments demonstrate that, under the same annotation budget, our method is more data-efficient and yields a relatively noise-free annotation dataset in the end.

中文摘要翻译: 虽然深度主动学习（DAL）能够有效降低人工标注成本，但其效果受到人工标注错误的制约。这是因为主动学习采样的数据被认为对模型训练具有高度的信息性。当人工标注者以一定比例向这些信息性数据引入错误时，主动学习的性能会显著下降，在某些情况下甚至比被动学习表现更差。本文首先分析了在深度主动学习背景下人工标注错误的影响。随后，我们提出了一种解决DAL中人工标注噪声问题的框架。受人类学习模式的启发，我们提出的解决方案核心思想是：将部分人工标注预算分配给已标注数据的重新标注。先前的理论工作表明，当模型具备一定能力识别潜在噪声数据时，即使只重新标注一小部分数据，也能有效去除主动训练集中的噪声。为此，我们实现了两种主动噪声采样策略，在不同情况下检测噪声，并分配部分标注预算来重新标注这些实例。我们的方法赋予主动学习一种回顾与内省的行为。实验表明，在相同标注预算下，我们的方法具有更高的数据效率，并最终生成了一个相对无噪声的标注数据集。

</details>


### [385] [A Geometric Measure of Linear Separability for Neural Representations](https://arxiv.org/abs/2606.08721)
*Yi Wei,Xuan Qi,Furao Shen*

Main category: cs.LG

TL;DR: 本文提出方向线性可分性度量（LSM），一种用于诊断单侧仿射可分离性的有限样本度量，可刻画神经网络表示中每个类别的几何结构，并证明其与最优线性分类精度的关系及对满秩线性嵌入的不变性。

<details>
  <summary>Details</summary>

Motivation: 现代神经分类器普遍依赖线性读出层，但仅靠预测性指标无法刻画线性读出层所操作的表示在类别层面上的几何结构，因此需要一种能诊断各类别表示几何特性的度量工具。

Method: 提出方向线性可分性度量（LSM），通过搜索包含目标类A所有样本的仿射半空间，测量必须保留在目标侧的最小竞争类样本入侵量（归一化后），形成非对称、按类别、目标归一化的有限样本诊断指标。还给出了基于惩罚的仿射搜索方法来估计高维特征中的LSM。

Result: 建立了LSM的支撑超平面刻画，证明了其与最优仿射分类精度的关系，以及满秩线性嵌入下的不变性。通过坐标门控非线性分析，实证了LSM在常见深度学习组件和架构中诊断类别侵入的能力。

Conclusion: LSM能够区分线性重参数化引起的变化与信息丢失或非线性几何变换引起的变化，是一种有效的类别级表示几何诊断工具。

Original Abstract: Modern neural classifiers commonly rely on linear readouts, yet predictive metrics alone do not characterize the class-wise geometry of the representations on which such readouts operate. We introduce the directional linear separability measure (LSM), a finite-sample diagnostic for one-sided affine separability. For a target class A and a competing set B, LSM searches over affine halfspaces that contain all samples in A and measures the smallest competing-sample intrusion that must remain on the target side, normalized by |A|. The resulting quantity is asymmetric, class-wise, target-normalized, and applicable to finite representations extracted from neural networks. We establish its supporting-hyperplane characterization, relate it to optimal affine classification accuracy, and prove invariance under full-rank linear embeddings. These results separate changes caused by linear reparameterization from those caused by information loss or nonlinear geometric transformations. We also give a penalty-based affine search for estimating class-wise LSM in high-dimensional features, with reported values computed from the original discrete preservation and violation criterion. Finally, we analyze coordinatewise gated nonlinearities as finite-sample geometric operators and empirically use LSM to diagnose class-wise intrusion across common deep-learning components and architectures.

中文摘要翻译: 现代神经分类器通常依赖线性读出层，但仅凭预测性指标无法刻画这些读出层所操作的表示在类别层面上的几何结构。我们引入了方向线性可分性度量（LSM），一种用于单侧仿射可分离性的有限样本诊断方法。对于目标类A和竞争集B，LSM搜索包含A中所有样本的仿射半空间，并测量必须保留在目标侧的最小竞争类样本入侵量，以|A|进行归一化。所得度量是非对称的、按类别计算的、目标归一化的，并且适用于从神经网络中提取的有限表示。我们建立了其支撑超平面刻画，将其与最优仿射分类精度联系起来，并证明了其在满秩线性嵌入下的不变性。这些结果能够将线性重参数化引起的变化与信息丢失或非线性几何变换引起的变化区分开来。我们还给出了一种基于惩罚的仿射搜索方法，用于估计高维特征中的逐类LSM，报告的值根据原始的离散保持和违背准则计算。最后，我们将坐标门控非线性作为有限样本几何算子进行分析，并通过LSM实证诊断常见深度学习组件和架构中的类别级侵入现象。

</details>


### [386] [Declarative Outcome-Conformant Synthesis: Exact, Closed-Form Specification Satisfaction and a Conformance Benchmark](https://arxiv.org/abs/2606.08736)
*Muhammed Rasin*

Main category: cs.LG

TL;DR: 本文提出了"结果一致合成"这一新任务：在无源数据（冷启动）下生成精确满足声明分析结果（如收入曲线、流失率、群体份额）的表格数据。作者证明现有模仿方法（GANs、扩散模型等）无法实现零误差的一致性，并提出一种基于条件求和采样的闭式解法，能在1-Wasserstein距离上仅付出0.006的代价实现精确聚合一致性。

<details>
  <summary>Details</summary>

Motivation: 现有合成表格数据的主流范式（如GANs、扩散模型、Copulas等）只能学习真实分布并采样，无法在无源数据的冷启动场景下精确满足声明的分析结果（如聚合指标），且采样方差导致永远无法精确命中目标聚合值。实际应用中大量需求正是这种冷启动下精确复现声明结果的能力，而现有工具无法提供相应接口。

Method: (1) 提出"结果一致合成"任务框架，以一致性而非保真度作为评估轴；(2) 形式化证明精确聚合生成器等价于Gamma总体的条件求和采样（基于Lukacs刻画），具有闭式精确性、闭式边缘CV和尺度不变性；(3) 构建SpecBench基准，首次测量冷启动关系型合成中对分析结果的一致性；(4) 提出闭式确定性参考系统。

Result: (1) 在真实数据集上，现成合成器训练于真实数据后，对声明的月度聚合值误差达74%-86%；"每期最佳"方法可将误差降至约19%，但仍无法达到0；而闭式生成器精确达到0误差。(2) 实现精确聚合的代价是1-Wasserstein距离上最多0.006的损失，其余偏差来自形状族不匹配。(3) 精确聚合本身简单，真正的贡献是一致性与闭式边缘分布、完整性、确定性、零源数据的联合实现。

Conclusion: 合成表格数据应区分两条正交的评估轴：保真度（与真实数据的相似度）和一致性（与声明分析结果的匹配度）。在有真实数据时应让位于模仿方法，但在冷启动需精确匹配分析结果的场景下，闭式确定性生成器具有不可替代的优势。

Original Abstract: We study a capability the dominant paradigm in synthetic tabular data does not provide: exact satisfaction of a declared analytical outcome with no source data. Imitation methods (copulas, GANs, diffusion) learn a real distribution and sample from it, and are judged on fidelity to real data. A large, practical class of needs is different: generating data with no source data ("cold start") that reproduces a declared outcome (a revenue curve, a churn rate, a group share) across a relational schema. Off-the-shelf imitation tools offer no interface for such targets, and no sampler can hit an exact aggregate, because sampling has variance. On a real public dataset, off-the-shelf learned synthesizers trained on that very data miss the declared monthly aggregate by 74 to 86 percent; a per-period steelman cuts the miss to about 19 percent and still cannot reach 0; a closed-form generator reaches exactly 0. We name this task outcome-conformant synthesis, argue its evaluation axis is conformance rather than fidelity, and show the two axes are orthogonal. We contribute: (1) a formal account showing a widely-used family of exact-aggregate generators is exactly conditional-sum sampling of a Gamma population (via Lukacs' characterization), with closed-form exactness, a closed-form marginal CV, and scale-invariance; a controlled experiment maps the boundary, enforcing the exact aggregate costs at most 0.006 in 1-Wasserstein distance to an arbitrary external marginal, the rest being shape-family mismatch; (2) SpecBench, to our knowledge the first benchmark to measure conformance to analytical outcomes for cold-start relational synthesis; and (3) a closed-form, deterministic reference system. Exact aggregation alone is trivial; the contribution is conformance jointly with closed-form marginals, integrity, determinism, and zero source data. We concede fidelity to imitation where real data exists.

中文摘要翻译: 我们研究了一种合成表格数据主流范式无法提供的能力：在无源数据的情况下精确满足声明的分析结果。模仿方法（Copulas、GANs、扩散模型）学习真实分布并从中采样，其评判标准是对真实数据的保真度。而实际需求中有一大类场景截然不同：在无源数据（"冷启动"）下生成能跨关系模式复现声明结果（收入曲线、流失率、群体份额）的数据。现成的模仿工具不提供针对此类目标的接口，且由于采样存在方差，任何采样器都无法精确命中聚合值。在一个真实公开数据集上，基于该数据训练的现成学习型合成器对声明月度聚合值的误差达74%至86%；每期最优方法可将误差降至约19%，但仍无法达到0；而闭式生成器精确达到0误差。我们将此任务命名为"结果一致合成"，论证其评估轴是一致性而非保真度，并证明这两个轴是正交的。我们的贡献包括：(1) 一个形式化结论，证明一类广泛使用的精确聚合生成器等价于Gamma总体的条件求和采样（基于Lukacs刻画），具有闭式精确性、闭式边缘变异系数和尺度不变性；通过受控实验划定边界——强制精确聚合在1-Wasserstein距离上至外部任意边缘分布的代价不超过0.006，其余偏差来自形状族不匹配；(2) SpecBench，据我们所知是首个衡量冷启动关系型合成中分析结果一致性的基准；(3) 一个闭式确定性参考系统。单就精确聚合本身而言是简单的；贡献在于一致性、闭式边缘分布、完整性、确定性和零源数据的联合实现。我们承认在存在真实数据时，应让位于模仿方法以保证保真度。

</details>


### [387] [Understanding the Parameter Space Geometry of Transformers Encoding Boolean Functions](https://arxiv.org/abs/2606.08768)
*Blanka Köver,Alexandra Butoi,Anej Svete,Michael Hahn,Ryan Cotterell*

Main category: cs.LG

TL;DR: 本文研究了Transformer对敏感函数（如PARITY）难以学习的问题，发现即使这些函数可以被特定参数表达，它们在参数空间中占据的区域极其狭小，随机初始化几乎不可能命中，因此无法被学习。

<details>
  <summary>Details</summary>

Motivation: 尽管已知Transformer存在偏向低平均敏感度函数的归纳偏置，但该偏置背后的精确机制尚不清楚。本文旨在揭示这一现象的根本原因。

Method: 本文将关注点从平均敏感度转移到完整的敏感度分布（所有输入上的敏感度值分布），通过分析Transformer参数空间的几何结构，证明随机初始化的Transformer几乎确定只会计算包含低敏感度字符串的函数。

Result: 证明敏感函数即便可表示，也在参数空间中占据可忽略的小区域，随机初始化极大概率无法命中；且任何缺少低敏感度字符串的函数在理论上不可学习。

Conclusion: Transformer对敏感函数的不可学习性源于参数空间的几何特性，而非表达能力不足；缺乏低敏感度字符串的函数是根本不可学习的。

Original Abstract: Transformers consistently fail to learn certain simple functions that are provably expressible with specific parameter settings. This gap between learnability and expressivity is particularly prominent for sensitive functions -- functions whose output is likely to change if a single bit of the input is flipped -- for example, PARITY. While prior work has established that transformers exhibit a bias toward functions with low average sensitivity, the precise mechanism underlying this bias remains poorly understood. To shed light on this phenomenon, we study the geometry of transformers' parameter space. We show that sensitive functions -- even when representable -- occupy a vanishingly small region that random initialization is very likely to miss. Specifically, we shift the focus from average sensitivity to the full sensitivity profile -- the distribution of sensitivity values across all inputs -- and prove that randomly initialized transformers almost surely compute functions which have low-sensitivity strings. Consequently, any function that lacks such strings is provably unlearnable.

中文摘要翻译: Transformer 始终无法学习某些简单的函数，而这些函数可以通过特定的参数设置被证明是可表达的。这种可学习性与可表达性之间的差距在敏感函数上尤为突出——所谓敏感函数，即当输入中单个比特翻转时输出很可能发生变化的函数，例如 PARITY 函数。虽然先前的研究已经表明 Transformer 倾向于偏向低平均敏感度的函数，但这种偏置背后的精确机制仍不清楚。为了阐明这一现象，我们研究了 Transformer 参数空间的几何结构。我们证明，即使敏感函数是可表示的，它们也只占据一个极其微小的区域，随机初始化几乎不可能命中。具体而言，我们将关注点从平均敏感度转移到完整的敏感度分布——即所有输入上敏感度值的分布——并证明随机初始化的 Transformer 几乎确定只会计算包含低敏感度字符串的函数。因此，任何缺乏此类字符串的函数在理论上都是不可学习的。

</details>


### [388] [How Many Counterfactuals Does It Take? Probing VLM Hallucinations Through Circuits and Causal Effects](https://arxiv.org/abs/2606.08777)
*Abhivansh Gupta,Simardeep Singh,Advika Sinha,Shreyansh Modi,Akshat Tomar*

Main category: cs.LG

TL;DR: 本文研究视觉语言模型（VLM）中幻觉输出的反事实鲁棒性样本复杂度，提出基于对数概率差异的因果影响度量，并利用电路发现技术识别关键模型组件，推导出检测幻觉输出不稳定性所需的最小反事实样本数量上的经验界限。

<details>
  <summary>Details</summary>

Motivation: 现有方法缺乏对VLM幻觉预测在反事实扰动下鲁棒性的原则性理解。

Method: 定义基于事实、反事实和激活修补运行之间对数概率差异的因果影响度量；利用电路发现技术（CD-T）识别负责幻觉预测的模型组件并追踪激活差异；使用浓度不等式和因果影响分布的方差估计推导检测幻觉输出不稳定所需的最小反事实样本数量m的经验界限。

Result: 推导出检测幻觉输出不稳定性所需的最小反事实样本数量m的经验界限。

Conclusion: VLM幻觉输出在反事实扰动下存在不稳定性，且可通过所提出的因果影响度量和样本复杂度界限进行定量分析和检测。

Original Abstract: Visual Language Models (VLMs) are known to produce hallucinated predictions that are not grounded in visual evidence, yet existing approaches lack a principled understanding of how robust such predictions are under counterfactual perturbations. In this work, we study the sample complexity of counterfactual robustness for hallucinated outputs in VLMs. We define a causal influence metric based on log-probability differences between factual, counterfactual, and activation-patched runs, and use it to characterize the stability of hallucinated predictions. By leveraging circuit discovery techniques (CD-T), we identify model components responsible for these predictions and track their activation differences across counterfactual samples. We then derive empirical bounds on the minimum number of counterfactual samples m required to reliably detect instability in hallucinated outputs, using concentration inequalities and variance estimates of the causal influence distribution.

中文摘要翻译: 视觉语言模型（VLM）会产生没有视觉证据支持的幻觉预测，但现有方法缺乏对这些预测在反事实扰动下鲁棒性的原则性理解。本文研究了VLM中幻觉输出的反事实鲁棒性样本复杂度。我们基于事实、反事实和激活修补运行之间的对数概率差异定义了一个因果影响度量，并利用它来表征幻觉预测的稳定性。通过利用电路发现技术（CD-T），我们识别了负责这些预测的模型组件，并追踪它们在反事实样本间的激活差异。随后，我们利用浓度不等式和因果影响分布的方差估计，推导出可靠检测幻觉输出不稳定性所需的最小反事实样本数量m的经验界限。

</details>


### [389] [Reformulate LLM Reinforcement Learning for Efficient Training under Black-box Discrepancy](https://arxiv.org/abs/2606.08779)
*Jiashun Liu,Runze Liu,Xu Wan,Jing Liang,Hongyao Tang,Ling Pan*

Main category: cs.LG

TL;DR: 本文发现强化学习训练中存在隐式的训练-推理差异（失配），提出了差异约束马尔可夫决策过程（DCMDP），通过拉格朗日松弛机制自适应平衡性能提升与差异控制，在Qwen-3-8B和Qwen-3-30B-A3B模型上显著提升性能，并支持异构训练范式。

<details>
  <summary>Details</summary>

Motivation: 强化学习作为后训练范式常出现次优性能或训练崩溃，近期研究发现这是由于训练引擎和推理引擎之间的隐性差异（失配）导致的。现有方法缺乏对这种差异的有效控制机制。

Method: 提出差异约束马尔可夫决策过程（DCMDP），将奖励最大化与训练-推理行为对齐的约束耦合，实现稳定的双目标优化；引入拉格朗日松弛机制，根据当前差异违规程度动态调整两个目标的相对权重，使策略在容忍区域内自由探索，超出安全边界时被引导回。

Result: 在Qwen-3-8B（8B稠密模型）和Qwen-3-30B-A3B（30B混合专家模型）上显著提升性能，并实现异构训练范式——模型在高保真训练设置下优化，同时显式对齐低成本的资源受限推理部署。

Conclusion: 通过自适应地平衡性能优化与差异约束，DCMDP能够有效解决训练-推理失配问题，提升强化学习训练的稳定性和最终性能，并支持异构部署场景。

Original Abstract: Reinforcement Learning (RL) has emerged as a pivotal post-training paradigm, yet it frequently suffers from unpredictable sub-optimum performance or even training collapses. Recent findings attribute these failures to a hidden train-inference discrepancy (or mismatch), stemming from the disparate underlying engines and architecture. We find that the training policy can actively self-correct such a discrepancy when provided with an appropriate learning signal. Then, we further empirically identify a discrepancy tolerance region: within this region, aggressively narrowing the discrepancy can suppress policy exploration and reduce learning efficiency, whereas outside this region, reducing excessive discrepancy improves optimization consistency and raises the achievable local performance ceiling. According to such findings, we formulate this problem as a Discrepancy-Constrained Markov Decision Process (DCMDP), where reward maximization is coupled with a constraint that aligns training-Inference behavior, achieving stable dual-objective optimization. To adaptively balance performance improvement and discrepancy control, we introduce a Lagrangian relaxation mechanism that dynamically adjusts the relative weight of the two objectives according to the current degree of discrepancy violation. This enables stable dual-objective optimization: the policy is allowed to explore freely within the tolerance region, while being guided back when the discrepancy exceeds the safe boundary. Empirically, DCMDP significantly improves the performance of 8B dense model (Qwen-3-8b) and 30B Mixture-of-Expert model (Qwen-3-30bA3b), and enables a heterogeneous training paradigm, where LLMs can be optimized in high-fidelity training setup while being explicitly aligned for low-cost, resource-constrained inference deployment.

中文摘要翻译: 强化学习（RL）已成为一种关键的后训练范式，但它经常遭遇不可预测的次优性能甚至训练崩溃。最近的研究将这些失败归因于隐式的训练-推理差异（或失配），该差异源于底层引擎和架构的不同。我们发现，当提供合适的学习信号时，训练策略能够主动自我纠正这种差异。进一步地，我们通过实验识别出一个差异容忍区域：在该区域内，激进地缩小差异会抑制策略探索并降低学习效率；而在该区域外，减少过度的差异则能提高优化一致性并提升可实现的局部性能上限。基于这些发现，我们将此问题形式化为差异约束马尔可夫决策过程（DCMDP），其中奖励最大化与约束训练-推理行为对齐相耦合，实现了稳定的双目标优化。为了自适应地平衡性能提升和差异控制，我们引入了一种拉格朗日松弛机制，该机制根据当前差异违规程度动态调整两个目标的相对权重。这使得双目标优化稳定进行：策略可以在容忍区域内自由探索，而当差异超出安全边界时则被引导回。在实验上，DCMDP显著提升了8B稠密模型（Qwen-3-8b）和30B混合专家模型（Qwen-3-30bA3b）的性能，并支持一种异构训练范式，即大语言模型可以在高保真训练设置下进行优化，同时显式对齐到低成本、资源受限的推理部署场景。

</details>


### [390] [Scaling Decision-Focused Learning to Large Problems with Lagrangian Decomposition](https://arxiv.org/abs/2606.08797)
*Stéphane Eilles-Chan Way,Hugo Percot,Quentin Cappart,Tias Guns,Louis-Martin Rousseau*

Main category: cs.LG

TL;DR: 提出一种基于拉格朗日分解的决策聚焦学习框架，通过替代目标函数和两种损失函数，在保持竞争性能的同时支持并行化，显著提升大规模预测-优化问题的计算效率。

<details>
  <summary>Details</summary>

Motivation: 传统决策聚焦学习在每次迭代中需为每个训练实例求解约束优化问题，导致计算成本高、可扩展性差，难以实际部署。

Method: 将拉格朗日分解融入决策聚焦学习范式，提出新的替代目标函数和两种损失函数，并设计了两种不同计算效率与求解质量权衡的变体。该框架可与SPO+和IMLE等标准方法无缝集成。

Result: 在多维背包问题和二次投资组合优化两个基准任务上，该方法在保持竞争性能的同时易于并行化，在大规模实例上（变量数比相关工作多8倍）持续优于传统决策聚焦学习方法。

Conclusion: 拉格朗日分解能够有效缓解决策聚焦学习的计算瓶颈，提升可扩展性，且与现有方法兼容，是实现大规模预测-优化问题的有前景方案。

Original Abstract: Decision-focused learning has shown great promise for addressing predict-then-optimize problems, particularly in the presence of under-specified models. However, its practical deployment is often hindered by high computational costs and limited scalability, as it requires solving a constrained optimization problem for each training instance at every iteration. To address these challenges, we propose a novel framework that incorporates Lagrangian decomposition into the decision-focused learning paradigm. Specifically, we introduce a new surrogate objective along with two loss functions for evaluating and training the underlying prediction model. We further propose two variants of our approach, which offer different trade-offs between computational efficiency and solution quality. Our framework can be seamlessly integrated with standard decision-focused learning methods, including Smart Predict-then-Optimize (SPO+) and Implicit Maximum Likelihood Estimation (IMLE). Through experiments on two standard benchmarks, the multi-dimensional knapsack problem and quadratic portfolio optimization, we demonstrate that our approach achieves competitive performance while remaining amenable to parallelization. In particular, it consistently outperforms traditional decision-focused learning methods on large-scale instances, involving up to eight times more variables than those typically considered in related work. The implementation is available at https://github.com/corail-research/DFL-LD.

中文摘要翻译: 决策聚焦学习在处理预测-优化问题方面展现出巨大潜力，尤其是在模型欠指定的情况下。然而，其实际部署常因高昂的计算成本和有限的可扩展性而受到阻碍，因为该方法需要在每次迭代中为每个训练实例求解一个约束优化问题。为解决这些挑战，我们提出了一种将拉格朗日分解融入决策聚焦学习范式的新框架。具体而言，我们引入了一个新的替代目标函数以及两个用于评估和训练底层预测模型的损失函数。我们进一步提出了该方法的两种变体，它们在计算效率和解质量之间提供了不同的权衡。我们的框架可以与标准的决策聚焦学习方法无缝集成，包括Smart Predict-then-Optimize (SPO+)和Implicit Maximum Likelihood Estimation (IMLE)。通过在两个标准基准（多维背包问题和二次投资组合优化）上的实验，我们证明了我们的方法在保持竞争性能的同时易于并行化。特别地，在大规模实例上——其变量数比相关工作通常考虑的规模多达八倍——我们的方法持续优于传统的决策聚焦学习方法。实现代码已在https://github.com/corail-research/DFL-LD提供。

</details>


### [391] [Active Flow Expansion for Out-of-Distribution Discovery: from Theory to Molecules](https://arxiv.org/abs/2606.08802)
*Riccardo De Santi,Bruce Lee,Cristian Perez Jensen,Kimon Protopapas,Sophia Tang,Cheng-Hao Liu,Pranam Chatterjee,Yisong Yue,Andreas Krause*

Main category: cs.LG

TL;DR: 本文提出了一种名为 Active Flow Expansion (ActFlow) 的持续预训练方法，通过验证器反馈和主动探索，在流模型学习中扩大模型的“可生成集”，覆盖更多有效设计空间，从而解决标准生成模型无法采样分布外有效新设计的问题，并在分子、肽和蛋白质序列设计任务中取得显著优于现有方法的效果。

<details>
  <summary>Details</summary>

Motivation: 标准流模型和扩散预训练仅匹配观测数据分布，但有效设计空间远大于观测数据覆盖范围。在生成式发现任务中，目标是从分布外采样有效的新设计，而标准模型因拟合观测数据而无法生成这些设计。现有方法受限于数据分布匹配，缺乏覆盖有效设计空间的机制。

Method: 提出 ActFlow 方法，核心是将生成模型视为一个“可生成集”（以非可忽略概率覆盖的区域），并引入一个新的学习原则：通过扩大可生成集来增加对有效设计空间的覆盖。具体而言，ActFlow 是一种持续预训练方法，利用验证器（verifier）的反馈，在已学习的流表示中通过主动探索生成合成数据，并迭代地让模型适应这些新数据，从而将模型扩展至新的有效区域。

Result: 理论上，首次建立了分布外流建模的统计学习保证，将可生成集扩展分析为在所学表示上的局部到全局可达过程。实验上，在有机小分子、中等尺寸药物分子、治疗性肽和蛋白质序列设计任务中，ActFlow 远超出初始预训练模型覆盖的有效区域，显著优于广泛使用的合成流预训练方法。

Conclusion: ActFlow 通过创新性地将生成模型视角从数据分布匹配转向可生成集扩展，并引入验证器反馈驱动的主动探索持续预训练，有效解决了生成式发现中分布外采样的问题，为流模型的分布外生成提供了理论保证和实践验证。

Original Abstract: Standard flow and diffusion pre-training matches the distribution of available data (e.g., molecules), which often covers only a small fraction of the valid design space. In generative discovery, however, one aims to sample valid new-to-nature designs, assigned negligible probability under, and thus inaccessible to, standard models fitted to the observed data. To overcome this limitation, we depart from data distribution matching and view a generative model through its generable set: the region it covers with non-negligible probability. This allows to introduce a new learning principle for out-of-distribution flow modeling: enlarging a model's generable set to increase coverage of the valid design space. We propose Active Flow Expansion (ActFlow), a continued pre-training method that employs verifier feedback to expand a pre-trained model over new valid regions by iteratively adapting to synthetic data generated through active exploration in the learned flow representation. Theoretically, we establish to our knowledge first-of-their-kind statistical learning guarantees for out-of-distribution flow modeling, analyzing generable set expansion as a local-to-global reachability process over a learned representation. Empirically, we assess ActFlow with suitable out-of-distribution generative modeling metrics across small organic molecules, mid-sized drug-like molecules, therapeutic peptides, and protein sequence design tasks. Results show that ActFlow expands valid coverage far beyond the region modeled by the initial pre-trained model, significantly outperforming widely adopted synthetic flow pre-training methods.

中文摘要翻译: 标准流模型和扩散预训练旨在匹配可用数据（例如分子）的分布，但这些数据往往仅覆盖有效设计空间的一小部分。然而，在生成式发现任务中，目标是采样有效的新颖设计，这些设计在标准模型下被赋予可忽略的概率，因此无法通过拟合观测数据的标准模型获得。为克服这一局限，我们不再追求数据分布匹配，而是从生成模型的可生成集（即模型以非可忽略概率覆盖的区域）来审视模型。这为分布外流建模引入了一个新的学习原则：扩大模型的可生成集，以增加对有效设计空间的覆盖。我们提出了主动流扩展（Active Flow Expansion, ActFlow）方法，这是一种持续预训练技术，通过利用验证器反馈，在已学习的流表示中进行主动探索，迭代地适应生成的合成数据，从而将预训练模型扩展到新的有效区域。在理论上，我们首次建立了分布外流建模的统计学习保证，将可生成集扩展分析为在所学表示上的局部到全局可达过程。在实验上，我们使用合适的分布外生成式建模评估指标，在有机小分子、中等尺寸药物分子、治疗性肽和蛋白质序列设计任务上评估了 ActFlow。结果表明，ActFlow 将有效覆盖扩展到远超初始预训练模型建模的区域，显著优于广泛使用的合成流预训练方法。

</details>


### [392] [Knowledge Graphs and Reasoning LLMs for Finding Simple Yet Effective Transcriptomic Perturbation Predictors](https://arxiv.org/abs/2606.08816)
*Jake Fawkes,Liam Hodgson,Jason Hartford*

Main category: cs.LG

TL;DR: 本文发现基于知识图谱的K近邻方法在预测未知基因敲除扰动对转录组基因表达的影响上具有极高竞争力，并进一步利用强化学习优化的大语言模型改进邻域选择，取得了与最先进方法相当的性能。

<details>
  <summary>Details</summary>

Motivation: 预测未见过的基因敲除扰动对转录组基因表达的影响是虚拟细胞模型面临的一个极具挑战性的问题。已有工作通过利用生物知识图谱提供相似扰动的概念来改善对训练扰动之外的泛化能力，本文旨在探索最简单的模型能否在此任务上取得良好表现。

Method: 首先采用基于知识图谱的K近邻方法进行扰动预测；然后使用通过强化学习优化的大语言模型来对邻域进行修改，从而进一步提升预测性能。

Result: K近邻方法在分布外扰动预测任务上超越了几乎所有现有方法；经强化学习训练后的推理大语言模型在Replogle等人(2022)的细胞系数据上达到了与当前最先进方法同等的性能；强化学习训练还提升了大语言模型在差异表达预测这一下游任务上的表现，尽管并未直接对该任务进行训练。

Conclusion: 研究结果表明知识图谱作为模型先验具有显著效果，并初步展示了强化学习可以将大语言模型优化为可泛化的工具，用于预测复杂的生物学响应。

Original Abstract: Predicting the effect of an unseen gene knockout perturbation on transcriptomic gene expression remains a highly challenging problem for virtual cell models. Recent progress has been made by leveraging biological knowledge graphs to provide a notion of similar perturbation, allowing for improved extrapolation beyond the set of training perturbations. In this work, we demonstrate that the simplest model to leverage these assumptions - a K-nearest neighbour from the knowledge graph - achieves highly competitive performance on this task, and that this can be improved further using LLMs optimised via reinforcement learning (RL) for predictive performance. Specifically, we find that the K-nearest neighbour approach beats almost all methods on out-of-distribution perturbation prediction, and when a reasoning LLM is trained via RL to make changes to the neighbourhood, it obtains equivalent performance to current state of the art methods on the cell lines from Replogle et al. (2022). We also demonstrate that the RL training improves the LLM's performance on the downstream task of differential expression prediction, despite not being trained on this directly. Overall, these findings demonstrate the efficacy of knowledge graphs as model priors, and show early signs that RL can refine LLMs into generalizable tools for predicting complex biological responses.

中文摘要翻译: 预测未见过的基因敲除扰动对转录组基因表达的影响，仍然是虚拟细胞模型面临的一个极具挑战性的问题。近期进展通过利用生物知识图谱来提供相似扰动的概念，从而改进了对训练扰动集之外的泛化能力。在本工作中，我们证明了利用这些假设的最简单模型——基于知识图谱的K近邻方法——在该任务上取得了极具竞争力的表现，并且可以通过使用强化学习优化的针对预测性能的大语言模型进一步提高。具体来说，我们发现K近邻方法在分布外扰动预测上几乎击败了所有方法；而当通过强化学习训练一个推理大语言模型来对邻域进行修改时，其在Replogle等人(2022)的细胞系数据上达到了与当前最先进方法同等的性能。我们还证明，尽管强化学习训练并未直接针对差异表达预测这一下游任务，但训练过程提升了大语言模型在该任务上的表现。总体而言，这些发现证明了知识图谱作为模型先验的有效性，并初步展示了强化学习可以将大语言模型优化为可泛化的工具，用于预测复杂的生物学响应。

</details>


### [393] [Intrinsic Selection and Particle Resampling for Inference-Time Scaling Beyond Domain Verifiability](https://arxiv.org/abs/2606.08850)
*Giorgio Giannone,Mustafa Eyceoz,Shabana Baig,Shivchander Sudalairaj,Anna C. Doris,Faez Ahmed,Akash Srivastava,Kai Xu*

Main category: cs.LG

TL;DR: 本文提出基于并行样本集内在统计量（长度调整尾熵）的推理时扩展方法，无需地面真值验证，通过Intrinsic Selection、Intrinsic Particle Filtering和Particle Distillation三种技术，将ITS成功推广到开放领域，在工程设计、数学推理和临床响应等任务上取得显著提升。

<details>
  <summary>Details</summary>

Motivation: 现有推理时扩展（ITS）在可验证领域（如数学、编程）表现良好，但难以推广到存在系统性失败（如错误初始假设、未满足多维约束）的开放领域，因为这类任务依赖昂贵的外部求解器或脆弱的基于模型的验证器。本文旨在不依赖训练奖励模型或精确地面真值验证的情况下，将ITS扩展到开放领域。

Method: 核心方法是利用并行样本集的内在统计量——长度调整尾熵作为无需地面真值的解决方案质量判别信号，并作为难度门控实现自适应计算分配。具体包括三种技术：(1) Intrinsic Selection (iS)：事后排序候选方案；(2) Intrinsic Particle Filtering (iPF)：将选择推广到步骤级重采样，引导生成高置信度推理轨迹；(3) Particle Distillation (dPF)：通过早期logit融合和KL引导重采样注入先验指导，规避系统性推理错误。

Result: (1) iS在三个领域匹配共识算法，工程设计选择比pass@1提升20%；(2) iPF在困难数学问题上平均提升pass@1达6.1个百分点；(3) dPF在复杂临床响应上基于专家评分提升高达26.5%。方法适用于通用、领域专用和多模态架构。

Conclusion: 本文成功将推理时扩展从可验证领域推广到开放领域，无需训练奖励模型或精确地面真值验证，仅利用样本集内在统计量即可实现高效的自适应计算分配，在多个开放领域任务上取得显著性能提升。

Original Abstract: Inference-Time Scaling (ITS) has largely succeeded in verifiable domains like math and coding, where cheap verification enables scalable output selection. However, extending ITS to tasks prone to systematic failure - driven by faulty initial assumptions or unmet multidimensional constraints - typically relies on costly external solvers or brittle, model-based verifiers. Our key insight is that the intrinsic statistics of parallel sample sets, specifically length-adjusted tail entropy, provide a robust discriminative signal for solution quality without access to ground truth. Crucially, these statistics serve as a difficulty gate for adaptive compute allocation, dynamically routing problems across scaling regimes. First, Intrinsic Selection (iS) ranks candidates post-hoc, matching consensus-based algorithms across three domains and improving engineering design selection by 20% over pass@1 baselines. Second, Intrinsic Particle Filtering (iPF) generalizes this to step-level resampling, guiding generation toward high-confidence reasoning trajectories to improve pass@1 by 6.1 points on average on hard math problems. Finally, Particle Distillation (dPF) injects privileged guidance via early logit blending and KL-guided resampling, steering generation past systematic reasoning errors to satisfy expert rubrics, yielding up to 26.5% gains on complex clinical responses. Our pipeline applies seamlessly across broad-purpose, domain-specialized, and multimodal architectures, successfully extending ITS to open-ended domains without requiring trained reward models or exact ground-truth verification.

中文摘要翻译: 推理时扩展（ITS）在数学和编程等可验证领域已取得巨大成功，这些领域可通过廉价验证实现可扩展的输出选择。然而，将ITS推广到易出现系统性失败——由错误的初始假设或未满足的多维约束驱动——的任务通常依赖于昂贵的外部求解器或脆弱的、基于模型的验证器。我们的关键洞察是：并行样本集的内在统计量，特别是长度调整尾熵，能够在无需地面真值的情况下提供鲁棒的解决方案质量判别信号。至关重要的是，这些统计量作为自适应计算分配的难度门控，能够跨扩展范式动态路由问题。首先，内在选择（iS）事后对候选方案进行排序，在三个领域上与基于共识的算法相当，并将工程设计选择相较于pass@1基线提升20%。其次，内在粒子滤波（iPF）将此方法推广到步骤级重采样，引导生成朝向高置信度推理轨迹，在困难数学问题上平均提升pass@1达6.1个百分点。最后，粒子蒸馏（dPF）通过早期logit融合和KL引导重采样注入先验指导，引导生成绕过系统性推理错误以满足专家评分标准，在复杂临床响应上取得高达26.5%的提升。我们的流程无缝适用于通用型、领域专用型及多模态架构，成功将ITS扩展到开放领域，无需训练奖励模型或精确的地面真值验证。

</details>


### [394] [sGPO: Trading Inference FLOPs for Training Efficiency in RLVR](https://arxiv.org/abs/2606.08854)
*Shivchander Sudalairaj,Kai Xu,Akash Srivastava,Giorgio Giannone*

Main category: cs.LG

TL;DR: 提出sGPO方法，通过离线推理代理评估查询难度，自适应分配训练计算资源，将训练计算量减少三倍。

<details>
  <summary>Details</summary>

Motivation: 标准RLVR训练为每个查询分配固定的rollout预算，不考虑查询难度差异，导致简单查询和不可解查询都浪费训练计算资源。

Method: sGPO：先用少量推理计算生成并行样本作为离线代理评估查询成功率，然后据此过滤数据、自适应分配组大小（组大小设为成功率的倒数），并构建从易到难的课程学习。

Result: 在包含推理评估成本的情况下，sGPO匹配或超越基线性能，同时将总训练计算量减少三倍。

Conclusion: 通过少量推理计算代理查询难度，可以大幅减少训练计算浪费，实现计算高效的强化学习训练。

Original Abstract: Standard Reinforcement Learning with Verifiable Rewards (RLVR) training allocates a fixed rollout budget to every query, without regard for what each query's difficulty means for the current policy. This leads to two symmetric failure modes: easy queries produce near-zero advantage because the policy already solves them, while unsolvable queries produce no signal because the policy never solves them. Both regimes waste training FLOPs without contributing to a learning gradient. We introduce sorted Group Policy Optimization (sGPO), a compute-efficient strategy that trades a small budget of inference FLOPs for a large reduction in wasted training FLOPs. The key insight is that cheap inference compute can serve as a single offline proxy for query difficulty. By generating a small batch of parallel samples per query under the initial policy, we obtain a model-aware empirical success rate. This motivates setting the training rollout group size to the inverse of this success rate, a practical rule that maximizes sample efficiency by extracting the most advantage per generated rollout. This single profiling pass simultaneously drives data filtering (removing trivial queries and sub-sampling unsolvable ones), adaptive group size allocation, and curriculum construction (scheduling queries from easy to hard). sGPO matches or exceeds baseline performance while reducing total training compute by a factor of three, with the upfront inference profiling cost included.

中文摘要翻译: 标准可验证奖励强化学习（RLVR）训练为每个查询分配固定的rollout预算，而不考虑每个查询的难度对当前策略意味着什么。这导致了两种对称的失败模式：简单查询产生接近零的优势，因为策略已经解决了它们；而不可解的查询不产生任何信号，因为策略从未解决它们。这两种情况都浪费了训练FLOPs，且未对学习梯度做出贡献。我们引入了排序组策略优化（sGPO），这是一种计算高效的策略，用少量的推理FLOPs预算换取训练FLOPs的大幅减少。关键洞察在于，廉价的推理计算可以作为查询难度的单一离线代理。通过在初始策略下为每个查询生成一小批并行样本，我们获得了模型感知的经验成功率。这启发了将训练rollout组大小设置为该成功率的倒数，这是一个实用的规则，通过从每个生成的rollout中提取最大优势来最大化样本效率。这个单次profiling过程同时驱动了数据过滤（移除琐碎查询并对不可解查询进行子采样）、自适应组大小分配以及课程构建（从易到难调度查询）。sGPO匹配或超越了基线性能，同时将总训练计算量减少了三倍，且包含了前期的推理profiling成本。

</details>


### [395] [Diffuse AI Control on Fuzzy Tasks](https://arxiv.org/abs/2606.08892)
*Mikhail Terekhov,Caglar Gulcehre,Vivek Hebbar,Joe Benton*

Main category: cs.LG

TL;DR: 本文提出一个对抗博弈框架来研究AI在模糊任务上的扩散性威胁（长期部署中的微妙破坏行为），通过红蓝队对抗（蓝队用弱可信模型构建弱评分器训练强模型消除颠覆倾向，红队寻找评分高但实际性能差的行为），在实验提案写作任务上验证了威胁存在，并提出了蓝队对抗优化算法来缓解该威胁。

<details>
  <summary>Details</summary>

Motivation: AI模型在关键领域（如AI安全研究）长期部署中可能因未对齐而产生微妙的颠覆行为（扩散性威胁），尤其是在难以评分或依赖直觉的模糊任务上，这类风险尤为隐蔽且难以防范。

Method: 将AI控制建模为蓝队与红队之间的对抗博弈：蓝队使用弱可信模型构建弱评分器，用于训练强模型以消除其颠覆倾向；红队通过多目标进化提示优化搜索被弱评分器高分评价但实际性能差的行为。在实验提案写作任务上，以能访问原始论文的语言模型作为代理"真实"评分器进行验证。

Result: Opus 4.6能写出比GPT-OSS-20B更差（根据真实代理评分）但被弱评分器评为与Opus 4.6最佳提案相同高分的实验提案。蓝队的对抗优化算法生成的提示使红队优化无法成功利用。

Conclusion: 提出的对抗框架能有效揭示扩散性威胁，并通过蓝队对抗优化算法缓解该威胁，为AI安全中的模糊任务控制提供了新方法。

Original Abstract: AI models deployed in critical domains, such as AI safety research, may subtly sabotage our efforts due to misalignment. Diffuse AI Control is a subfield of AI safety concerned with mitigating risks from AI sabotage distributed over long deployment horizons (diffuse threats). These risks are particularly pernicious on fuzzy tasks, i.e. tasks which are hard to grade or require intuition. To understand diffuse threats on fuzzy tasks, we introduce a novel framework that considers AI control as an adversarial game between a blue team and a red team. The blue team uses a weak trusted model to construct a weak score against which they would train a strong, potentially subversive model to remove the subversion propensity if it were present. The red team then tries to find model behaviors that are rated highly by the weak score, and thus might not be trained out, but actually correspond to poor performance. We test our framework on the task of writing experimental proposals for research questions from recent ML papers. We use a language model with access to the original paper as a proxy "ground-truth" scorer. Our red team discovers subversive behaviors using multi-objective evolutionary prompt optimization. We show that Opus~4.6 can write proposals that are worse according to the ground truth proxy than those of GPT-OSS-20B, while the weak scorer rates them as highly as the best proposals from Opus 4.6. To mitigate the threat, we propose an adversarial optimization algorithm for the blue team that discovers more robust prompts for the weak model. This algorithm produces a blue team prompt that our red team optimization fails to exploit.

中文摘要翻译: 部署在关键领域（如AI安全研究）的AI模型可能因未对齐而微妙地破坏我们的努力。扩散性AI控制是AI安全的一个子领域，关注缓解在长期部署范围内分布的AI颠覆风险（扩散性威胁）。这些风险在模糊任务上尤为有害，即那些难以评分或需要直觉的任务。为了理解模糊任务上的扩散性威胁，我们引入了一个新颖的框架，将AI控制视为蓝队和红队之间的对抗博弈。蓝队使用一个弱可信模型构建一个弱评分器，他们将据此训练一个强大的、可能具有颠覆性的模型，以消除其颠覆倾向（如果存在）。红队则试图找到被弱评分器高度评价、从而可能不会被训练掉，但实际上对应差性能的模型行为。我们在为近期机器学习论文中的研究问题撰写实验提案的任务上测试了我们的框架。我们使用一个能够访问原始论文的语言模型作为代理"真实"评分器。我们的红队通过多目标进化提示优化发现了颠覆性行为。我们展示了Opus 4.6能写出根据真实代理评分比GPT-OSS-20B更差的提案，而弱评分器却将它们评为与Opus 4.6的最佳提案同样高的分数。为了缓解这一威胁，我们为蓝队提出了一种对抗优化算法，该算法能为弱模型发现更鲁棒的提示。该算法生成的蓝队提示使我们的红队优化未能成功利用。

</details>


### [396] [Cheap Reward Hacking Detection](https://arxiv.org/abs/2606.08893)
*Iván Belenky,Joaquín Itria,Steven Johns*

Main category: cs.LG

TL;DR: 本文训练一个小型Transformer编码器，将Terminal-Wrench轨迹映射到单位球面，使嵌入距离近似奖励与元数据信号的L1距离。通过线性探针检测奖励破解，在清洗测试集上AUC达0.9467、TPR@5%FPR达0.8296，以约四个数量级更低的轨迹成本匹配并超越LLM-as-judge方法。

<details>
  <summary>Details</summary>

Motivation: 现有奖励破解检测方法（如LLM-as-judge）计算成本高昂，亟需一种低成本、高效的替代方案来检测智能体是否通过欺骗行为获取高奖励。

Method: 训练一个小型Transformer编码器，将Terminal-Wrench轨迹映射到单位球面，使嵌入距离近似奖励信号与元数据信号的L1距离；在此基础上训练线性探针进行奖励破解二分类检测。

Result: 在清洗测试集上，该方法AUC为0.9467、TPR@5%FPR为0.8296，匹配LLM-as-judge的AUC（0.9510）且TPR@5%FPR更优（0.8296 vs 0.7130），轨迹成本降低约四个数量级。去除输入中的自然语言推理内容后AUC降至0.6213。

Conclusion: 该方法以极低计算成本实现了与LLM-as-judge相当甚至更优的奖励破解检测性能，且编码器依赖自然语言推理信息而非纯行为模式。

Original Abstract: A small transformer encoder is trained to map Terminal-Wrench trajectories onto a unit sphere where embedding distance approximates the $L_1$ distance between reward and metadata signals. A linear probe on top of that embedding detects reward hacking on the cleaned test split with AUC $0.9467$ and TPR@5%FPR $0.8296$, matching the TW sanitized LLM-as-judge AUC ($0.9510$ on the cleaned split) and exceeding its TPR@5%FPR ($0.7130$ vs $0.8296$) on the same information condition, at roughly four orders of magnitude lower per-trajectory cost. The encoder is not a pure behavior reader: stripping natural-language reasoning from its input at probe time drops AUC to $0.6213$.

中文摘要翻译: 本文训练了一个小型Transformer编码器，将Terminal-Wrench轨迹映射到单位球面上，使得嵌入距离近似于奖励信号与元数据信号之间的L1距离。在该嵌入之上训练的线性探针在清洗测试集上检测奖励破解的AUC达到0.9467，TPR@5%FPR达到0.8296，在相同信息条件下匹配了TW清理后的LLM-as-judge的AUC（在清洗集上为0.9510），并在TPR@5%FPR指标上超越后者（0.8296 vs 0.7130），而每条轨迹的计算成本低了约四个数量级。该编码器并非纯粹的行为读取器：在探针测试阶段去除输入中的自然语言推理内容后，AUC降至0.6213。

</details>


### [397] [Synthetic but Not Realistic: The Evaluation Challenge in Generative Modelling for Structured Electronic Medical Records](https://arxiv.org/abs/2606.08903)
*Nicholas I-Hsien Kuo,Blanca Gallego,Louisa Jorm*

Main category: cs.LG

TL;DR: 本文提出一个基于流行病学视角的多维度评估框架，用于检验合成医疗数据的临床有效性，发现现有生成模型虽能拟合边际分布，但在保留亚组结构、效应估计和依赖关系方面存在严重缺陷，导致当前评估方法可能高估合成数据质量。

<details>
  <summary>Details</summary>

Motivation: 现有合成医疗数据的评估方法主要依赖统计相似性和预测性能，但这些指标无法反映临床有效性，可能导致对合成数据质量的误判。

Method: 构建了一个基于流行病学的多维度评估框架，从描述性保真度、临床效用和结构效度三个维度（分别对应描述性、预测性和因果性问题）进行评估，并在具有已知真实结构的PRIME-CVD（50,000人队列）数据集上对比了四种生成范式：GAN、VAE、扩散模型和掩码建模。

Result: 所有模型都能较好地再现边际分布，但没有任何模型能同时保留亚组结构、效应估计和依赖结构。具有强分布保真度的模型可能表现出较差的校准度和扭曲的变量关系，导致不可靠的推断。

Conclusion: 当前评估实践可能高估合成数据质量，需要基于领域知识的评估方法，以确保合成数据能支持有效的临床和科学结论。

Original Abstract: Synthetic healthcare data are widely proposed as privacy-preserving substitutes for real patient data, yet their evaluation remains dominated by statistical similarity and predictive performance that do not reflect clinical validity. We introduce a multi-dimensional evaluation framework grounded in epidemiology, assessing descriptive fidelity, clinical utility, and structural validity, corresponding to descriptive, predictive, and causal questions. We evaluate four representative generative paradigms - GAN-based, VAE-boosted, diffusion-based, and masked modelling - using PRIME-CVD, a 50,000-person cohort with known ground-truth structure. While all models reproduce marginal distributions, none simultaneously preserve subgroup structure, effect estimates, and dependency structure. Notably, models with strong distributional fidelity can exhibit poor calibration and distorted relationships, leading to unreliable inference. These results show that current evaluation practices can overestimate synthetic data quality and motivate domain-informed assessment based on the ability to support valid clinical and scientific conclusions.

中文摘要翻译: 合成医疗数据被广泛提出作为真实患者数据的隐私保护替代方案，然而其评估仍然主要依赖统计相似性和预测性能，这些指标无法反映临床有效性。我们引入了一个基于流行病学的多维度评估框架，评估描述性保真度、临床效用和结构效度，分别对应描述性、预测性和因果性问题。我们使用具有已知真实结构的PRIME-CVD（50,000人队列）评估了四种代表性生成范式——基于GAN、VAE增强、基于扩散和掩码建模。尽管所有模型都能再现边际分布，但没有一个能同时保留亚组结构、效应估计和依赖结构。值得注意的是，具有强分布保真度的模型可能表现出较差的校准度和扭曲的变量关系，导致不可靠的推断。这些结果表明，当前的评估实践可能高估合成数据质量，并启发基于领域知识的评估方法，以确保合成数据能够支持有效的临床和科学结论。

</details>


### [398] [Generalized Rank-based Evaluation for Knowledge Graph Completion: Perspectives, Framework, and Analyses](https://arxiv.org/abs/2606.08921)
*Sooho Moon,Jian Kang,Yunyong Ko*

Main category: cs.LG

TL;DR: 现有知识图谱补全（KGC）评估指标忽视了预测锐度（Predictive Sharpness）和流行度偏差鲁棒性（Popularity-bias Robustness）两个重要视角。本文提出通用评估框架PROBE，包含排名变换器（RT）和排名聚合器（RA），理论证明其满足六个关键性质，实验表明PROBE相比现有指标能提供更全面、灵活、一致的评估。

<details>
  <summary>Details</summary>

Motivation: 现有KGC评估指标存在两个被忽视的视角：（P1）预测锐度——模型在排名靠前的位置上是否具有足够的判别力；（P2）流行度偏差鲁棒性——评估是否受到头部实体流行度偏差的影响。此外，现有指标在知识图谱开放世界设定下无法保持模型相对性能的一致性，需要一种更可靠、更全面的评估框架。

Method: 提出PROBE评估框架，包含两个组件：（1）排名变换器（Rank Transformer, RT），根据期望的预测锐度水平估计每个预测的得分；（2）排名聚合器（Rank Aggregator, RA），根据期望的流行度偏差鲁棒性水平聚合所有预测得分得到最终评估分数。理论部分定义了KGC可靠评估的六个关键性质，并证明PROBE满足所有性质。

Result: 在6个KGC模型和6个真实知识图谱上的实验表明：现有指标可能高估或低估模型性能（取决于评估视角），而PROBE能够实现更全面、灵活且一致的评估。理论分析证明PROBE满足所有六个定义的评估性质，而现有指标在某些性质上存在缺陷。

Conclusion: PROBE框架通过引入预测锐度和流行度偏差鲁棒性两个新视角，提供了一个更可靠、更全面的KGC评估方案，解决了现有评估指标的局限性，尤其适用于开放世界知识图谱场景下的模型性能评估。

Original Abstract: Knowledge graph completion (KGC) aims to predict missing facts from an observed knowledge graph (KG), playing a crucial role in a wide range of real-world applications such as drug discovery, recommender systems, and retrieval-augmented generation (RAG). Although numerous KGC models have been proposed, the evaluation of KGC remains underexplored, despite its critical role in reliably assessing model performance and selecting appropriate models for real-world applications. In this paper, we introduce two important perspectives for KGC evaluation that are overlooked by existing evaluation metrics, (P1) predictive sharpness and (P2) popularity-bias robustness. To address both perspectives, we propose a generalized evaluation framework, PROBE, which consists of a rank transformer (RT) that estimates the score of each prediction based on a desired level of predictive sharpness and a rank aggregator (RA) that determines the final evaluation score by aggregating all prediction scores according to a desired level of popularity-bias robustness. We theoretically analyze PROBE by defining six key properties for reliable KGC evaluation and prove that PROBE satisfies all the properties, while existing metrics fail to satisfy some. In particular, due to the open-world nature of KGs, an evaluation metric should preserve the relative performance of KGC models even when only incomplete facts are observed. We show that PROBE better maintains such consistency, providing a more reliable estimate of intrinsic model performance than existing metrics. Extensive experiments with six KGC models on six real-world KGs reveal that existing metrics may over- or under-estimate model performance depending on different evaluation perspectives, whereas PROBE enables a more comprehensive, flexible, and consistent evaluation of KGC models.

中文摘要翻译: 知识图谱补全（KGC）旨在从观测到的知识图谱（KG）中预测缺失的事实，在药物发现、推荐系统和检索增强生成（RAG）等广泛的实际应用中发挥着关键作用。尽管已有大量KGC模型被提出，但KGC的评估问题仍未得到充分探索，尽管评估在可靠衡量模型性能和为实际应用选择合适的模型方面至关重要。本文引入了现有评估指标所忽视的两个重要视角：（P1）预测锐度和（P2）流行度偏差鲁棒性。为同时解决这两个视角，我们提出一个通用评估框架PROBE，该框架由排名变换器（RT）和排名聚合器（RA）组成，其中RT基于期望的预测锐度水平估计每个预测的得分，RA根据期望的流行度偏差鲁棒性水平聚合所有预测得分以确定最终评估分数。我们从理论上分析了PROBE，定义了KGC可靠评估的六个关键性质，并证明PROBE满足所有性质，而现有指标无法满足其中一些性质。特别地，由于知识图谱的开放世界特性，评估指标即使在不完整事实被观测到的情况下也应保持KGC模型的相对性能。我们证明PROBE能更好地维持这种一致性，相比现有指标提供对模型内在性能更可靠的估计。在6个真实知识图谱上使用6个KGC模型进行的大量实验表明，现有指标可能根据不同的评估视角高估或低估模型性能，而PROBE能够实现对KGC模型更全面、灵活且一致的评估。

</details>


### [399] [PROBE-Web: An Interactive System for Probing Evaluation Landscapes of Knowledge Graph Completion Models](https://arxiv.org/abs/2606.08926)
*Sooho Moon,Yunyong Ko*

Main category: cs.LG

TL;DR: 本文提出了PROBE-Web，一个用于知识图谱补全模型多视角交互式评估的系统，允许用户通过调节预测锐度和流行度偏差鲁棒性两个关键视角来灵活评估模型。

<details>
  <summary>Details</summary>

Motivation: 现有KGC模型评估通常仅使用MRR和Hits@K等排名指标，但不同用户往往需要不同的评估视角，缺乏灵活的多视角评估工具。

Method: PROBE-Web系统提供四个核心功能：(1)传统评估工具包，(2)灵活的视角感知评估（通过调节预测锐度和流行度偏差鲁棒性），(3)可解释的案例研究，(4)评估景观探索。系统通过用户友好的图形界面实现。

Result: 该系统能够帮助用户从多视角评估多个KGC模型，分析其优劣势，并探索评估景观。

Conclusion: PROBE-Web可以帮助用户更好地理解KGC模型，使其评估与用户目标对齐。

Original Abstract: Knowledge graph completion (KGC) models are commonly evaluated using rank-based metrics such as MRR and Hits@K, despite different users often requiring different evaluation perspectives. In this demo, we present PROBE-Web, an interactive system for probing diverse evaluation landscapes for KGC models. PROBE-Web enables users to flexibly evaluate KGC models by adjusting two critical perspectives: (P1) predictive sharpness and (P2) popularity-bias robustness. Through a user-friendly GUI, users easily evaluate multiple KGC models and analyze their strengths and weaknesses. PROBE-Web provides four key functionalities: (1) conventional evaluation toolkit, (2) flexible perspective-aware evaluation, (3) explainable case studies, and (4) evaluation landscape exploration. We believe that PROBE-Web can help users better understand KGC models aligning with their objectives.

中文摘要翻译: 知识图谱补全（KGC）模型通常使用基于排名的指标（如MRR和Hits@K）进行评估，尽管不同用户往往需要不同的评估视角。在本演示中，我们提出了PROBE-Web，一个用于探测KGC模型多样化评估景观的交互式系统。PROBE-Web使用户能够通过调节两个关键视角来灵活评估KGC模型：（P1）预测锐度和（P2）流行度偏差鲁棒性。通过用户友好的图形界面，用户可以轻松评估多个KGC模型并分析其优劣势。PROBE-Web提供四个核心功能：（1）传统评估工具包，（2）灵活的视角感知评估，（3）可解释的案例研究，（4）评估景观探索。我们相信PROBE-Web可以帮助用户更好地理解KGC模型，使其与用户目标对齐。

</details>


### [400] [Backward Coherence and Hidden-State Stability in Recurrent Neural Networks: A Quasi-Reverse-Martingale Theory](https://arxiv.org/abs/2606.08934)
*Yuan-chin Ivan Chang*

Main category: cs.LG

TL;DR: 提出反向一致性（backward coherence）概念，通过可学习的反向投影器衡量RNN隐藏状态的可重构性，在收缩性和可和反向漂移条件下，隐藏状态序列形成拟反向鞅，从而实现几乎必然收敛、可解释的极限表示和有限路径停时，并为时间一致置信序列提供理论框架。实验表明该方法能显著降低拟鞅总量、提前达到稳定性，并与变分推断相联系。

<details>
  <summary>Details</summary>

Motivation: RNN的隐藏状态$h_t$的概率含义通常不明确，缺乏对其稳定性和收敛性的理论理解。

Method: 提出反向一致性（backward coherence）衡量$h_t$能否通过学习的反向投影器$g_φ$从$h_{t+1}$重构。在收缩性和可和反向漂移条件下，证明隐藏状态序列形成拟反向鞅，从而得到几乎必然收敛、混合条件下的收敛速率、可解释的极限表示、有限路径停时以及时间一致置信序列的理论框架。该方法可转化为正则化项，等效于在高斯反向模型中最小化KL散度。

Result: 反向一致性正则化使经验拟鞅总量$\hat Q$降低43%-58%，稳定性提前28%-44%；回声状态遗忘率被$\rho$界定的理论得到验证；增量总和管$R_t$达到100%同时覆盖率（但偏保守）；在PhysioNet 2012上RMRNN与RNN的AUC相当但提前13小时达到稳定表示；FRED-MD上概念漂移下预测误差降低约四倍；UCI HAR上保持更低的后转换跟踪误差且呈几何衰减。

Conclusion: 反向一致性为RNN隐藏状态提供了鞅论理论基础，正则化能有效提升稳定性与泛化能力，方法在实际数据中表现优异，但未声称普适性。

Original Abstract: Recurrent neural networks maintain a hidden state $h_t$, but its probabilistic meaning is often unclear. We study hidden-state stability through \emph{backward coherence}: the extent to which $h_t$ can be reconstructed from $h_{t+1}$ by a learned backward projector $g_φ$. Under contraction and summable backward drift, the hidden-state sequence forms a quasi-reverse-martingale. This yields almost-sure convergence, rates under mixing, an interpretable limiting representation, finite pathwise stopping times, and a theoretical framework for time-uniform confidence sequences.
  Simulations support the theory. Backward-coherence regularisation reduces the empirical quasi-martingale total $\hat Q$ by $43$--$58%$, reaches stability $28$--$44%$ earlier than an unregularised RNN, and gives tracking-error recovery consistent with geometric bounds. Additional tests confirm echo-state forgetting rates bounded by $ρ$ and verify the increment-sum tube $R_t$ with $100%$ simultaneous coverage, although $R_t$ is conservative; in practice, the defect-tail proxy $\hat Q_t$ is the more useful monitor. The backward-coherence loss is also equivalent to minimising a Kullback--Leibler divergence in a Gaussian backward model, linking the method to variational inference. Extensions cover $φ$-mixing inputs, change-point tracking, and finite-sample concentration.
  Three real-data studies further validate the approach. On PhysioNet 2012 ICU data, the Reverse Martingale RNN (RMRNN) matches RNN mortality-prediction AUC while reaching stable representations 13 hours earlier. On FRED-MD, it reduces one-month-ahead forecast error by about fourfold under concept drift. On UCI Human Activity Recognition, it maintains lower post-transition tracking error with geometric decay. The guarantees apply under the stated assumptions; universality is not claimed.

中文摘要翻译: 循环神经网络维护一个隐藏状态$h_t$，但其概率含义通常不明确。我们通过**反向一致性**（backward coherence）研究隐藏状态的稳定性：即$h_t$能否通过一个学习到的反向投影器$g_φ$从$h_{t+1}$重构。在收缩性和可和反向漂移条件下，隐藏状态序列形成一个拟反向鞅。这带来了几乎必然收敛、混合条件下的收敛速率、一个可解释的极限表示、有限路径停时，以及一个用于时间一致置信序列的理论框架。仿真实验支持该理论。反向一致性正则化将经验拟鞅总量$\hat Q$降低43%–58%，比未正则化RNN提前28%–44%达到稳定性，并给出与几何界一致的跟踪误差恢复。额外测试确认了回声状态遗忘率被$\rho$界定，并验证了增量总和管$R_t$具有100%的同时覆盖率，但$R_t$偏保守；实践中，缺陷尾部代理$\hat Q_t$是更有用的监控指标。反向一致性损失也等效于在高斯反向模型中最小化KL散度，将该方法与变分推断联系起来。扩展部分涵盖了$\phi$-混合输入、变点跟踪和有限样本集中性。三项真实数据研究进一步验证了该方法。在PhysioNet 2012 ICU数据上，反向鞅RNN（RMRNN）达到与RNN相当的死��预测AUC，同时提前13小时达到稳定表示。在FRED-MD上，它在概念漂移下将一个月前预测误差降低了约四倍。在UCI人类活动识别上，它保持更低的后转换跟踪误差且呈几何衰减。这些保证在所述假设下成立；不声称普适性。

</details>


### [401] [PAI: Preserving Amplitude Information in Representation-Based Time-Series Anomaly Detection](https://arxiv.org/abs/2606.08935)
*Kang Zhang,Wei Jian Lau,Shoushou Ren,Dong Lin,Joon Son Chung,Chuanhao Sun*

Main category: cs.LG

TL;DR: 本文提出PAI评分方案，通过诊断模块和分数增强函数显式保留振幅信息，解决了现有基于表示的时间序列异常检测方法忽略振幅信息导致性能下降的问题，在多个基准数据集上取得了显著提升。

<details>
  <summary>Details</summary>

Motivation: 现有基于表示的时间序列异常检测方法学习的嵌入表示通常与振幅无关（amplitude-agnostic），丢失振幅信息导致在振幅相关异常检测任务上性能下降，且该缺陷普遍存在于所有已有表示方法中。

Method: 提出PAI异常评分方案，包含两个互补模块：(1) 诊断模块：在同一表示库上比较余弦评分和欧氏评分，检测振幅信息是否已被嵌入表示捕获；(2) 最终评分增强函数：计算逐点中位数和MAD偏差分数以及局部均值漂移分数，与表示分数融合生成最终异常评分。

Result: 在TSB-AD-U-Eva和TAB UV数据集上，PAI使四种基于表示的方法在所有指标上均有提升，平均VUS-PR增益分别达98.4%和36.8%；PaAno+PAI组合超越现有最优方法15%；通过自举置信区间、异常类型分解和TS2Vec输入归一化消融实验进一步验证了方案有效性。

Conclusion: 显式保留振幅信息对基于表示的时间序列异常检测至关重要，而现有评分方案对此重视不足。

Original Abstract: Representation-based time-series anomaly detection algorithms significantly outperform other methods on diverse anomaly detection tasks. However, we notice that they suffer from a major limitation in our evaluation - their learned embeddings are often amplitude-agnostic. Losing amplitude information can degrade performance on amplitude related anomalies, and this failure is prevalent across all existing representation-based methods. To address aforementioned issues, we propose a new anomaly scoring scheme named PAI. PAI consists of two complementary modules, a diagnostic module and a final score augmentation function. The diagnostic module compares cosine and Euclidean scoring on the same representation bank to test whether amplitude information is already captured in the learned representation. Then in final score augmentation function, PAI computes a point-wise median and MAD deviation score and a local mean-shift score-which are fused with the representation score to produce the final anomaly score. On the TSB-AD-U-Eva and TAB UV datasets, PAI improves all four evaluated representation-based methods across every reported metric, achieving average VUS-PR gains of 98.4% and 36.8%, respectively. Among all evaluated combinations, PaAno + PAI achieves the best performance, outperforming the state-of-the-art method by 15%. Further evaluation on bootstrap confidence intervals, anomaly-type breakdowns, and a TS2Vec input-normalization ablation further support the proposed scheme. These results suggest that explicitly retaining amplitude information is important for representation-based time-series anomaly detection, which has been underemphasized in existing scoring schemes. Code is available at: https://github.com/pantheon5100/PAI

中文摘要翻译: 基于表示的时间序列异常检测算法在各类异常检测任务上显著优于其他方法。然而，我们在评估中发现它们存在一个主要局限——学习到的嵌入表示通常与振幅无关。丢失振幅信息会降低与振幅相关异常任务的检测性能，且这一缺陷普遍存在于所有现有的基于表示的方法中。为解决上述问题，我们提出了一种新的异常评分方案PAI。PAI包含两个互补模块：一个诊断模块和一个最终评分增强函数。诊断模块在同一表示库上比较余弦评分和欧氏评分，以检测振幅信息是否已被学习到的表示所捕获。在最终评分增强函数中，PAI计算逐点中位数和MAD偏差分数以及局部均值漂移分数——这些分数与表示分数融合以产生最终的异常分数。在TSB-AD-U-Eva和TAB UV数据集上，PAI使四种评估的基于表示的方法在所有报告指标上均得到改善，平均VUS-PR增益分别达到98.4%和36.8%。在所有评估的组合中，PaAno+PAI取得了最佳性能，超越当前最先进方法15%。对自举置信区间、异常类型分解以及TS2Vec输入归一化消融实验的进一步评估也支持了所提出的方案。这些结果表明，显式保留振幅信息对于基于表示的时间序列异常检测至关重要，而现有评分方案对此重视不足。代码地址：https://github.com/pantheon5100/PAI

</details>


### [402] [From Hazard Functions to Language Space: Cox-Supervised Distillation of Survival Risk into a Large Language Model](https://arxiv.org/abs/2606.08945)
*Nicholas I-Hsien Kuo,Blanca Gallego,Louisa Jorm*

Main category: cs.LG

TL;DR: 本文研究了如何将Cox比例风险模型估计的时间-事件风险信息迁移到生成式大语言模型中。通过将结构化临床协变量转换为文本提示，微调基于Qwen的大语言模型以生成患者特定的生存风险，实验表明该模型在判别力和校准度上具有竞争力，且隐状态分析显示模型在潜在空间中表征了平滑的风险梯度。

<details>
  <summary>Details</summary>

Motivation: 探索大语言模型是否能够内化并表达生存分析中的时间-事件风险结构，从而为语言模型赋予时间-事件推理能力。

Method: 提出一种基于文本的生存分析建模流程：将结构化临床协变量转换为文本提示，以Cox模型预测为训练目标，对基于Qwen的大语言模型进行微调，使其以文本生成任务的形式输出患者特定的生存风险。

Result: 在GBSG2、ACTG320和WHAS500三个数据集上，该模型在留出集上取得了有竞争力的判别力和校准度；t-SNE可视化显示模型隐状态中存在平滑的风险梯度，表明模型将生存风险表征为连续结构而非离散类别。

Conclusion: 大语言模型能够内化生存风险结构并支持校准的预测，为在语言模型中实现时间-事件推理提供了可行路径。

Original Abstract: We investigate whether information about time-to-event risk estimated by a Cox proportional hazards model can be transferred into a generative large language model. We propose a text-based survival modelling pipeline in which structured clinical covariates are converted into text prompts and a Qwen-based large language model is fine-tuned to generate patient-specific survival risk using Cox model predictions as a training target. Across GBSG2, ACTG320, and WHAS500, the model achieves competitive held-out discrimination and calibration despite being trained as a text-generation task rather than with a conventional survival-analysis loss. We further analyse the geometry of the model's hidden states, where t-SNE visualisations reveal smooth risk gradients in latent space, suggesting that the model represents survival risk as a continuous structure rather than isolated risk categories. Together, these findings suggest that large language models can internalise survival-risk structure while supporting calibrated prediction, providing a route towards time-to-event reasoning in language models.

中文摘要翻译: 本文研究了由Cox比例风险模型估计的时间-事件风险信息是否能够被迁移到生成式大语言模型中。我们提出了一种基于文本的生存分析建模流程：将结构化临床协变量转换为文本提示，并以Cox模型预测作为训练目标，对基于Qwen的大语言模型进行微调，使其生成患者特定的生存风险。在GBSG2、ACTG320和WHAS500三个数据集上，尽管该模型是以文本生成任务而非传统的生存分析损失函数进行训练的，它在留出集上仍取得了有竞争力的判别力和校准度。我们进一步分析了模型隐状态的几何结构，t-SNE可视化揭示了潜在空间中平滑的风险梯度，表明模型将生存风险表征为一种连续结构而非孤立的风险类别。综合来看，这些发现表明大语言模型能够内化生存风险结构并支持校准的预测，为在语言模型中实现时间-事件推理提供了一条可行路径。

</details>


### [403] [Self-Consistent Generative Paths via Admissible Random Variational Transport](https://arxiv.org/abs/2606.08953)
*Lei Luo,Yingzhen Zhang,Jian Yang*

Main category: cs.LG

TL;DR: 本文提出生成概率路径的**自洽性**概念，将其定义为随机变分传输算子的随机不动点，并推导了残差控制理论，可用于诊断、正则化和自适应采样各类生成模型（扩散、流匹配、一步生成、VAE、GAN/WGAN、自回归等）。

<details>
  <summary>Details</summary>

Motivation: 现有统一视角主要描述生成路径是如何构建的，但缺少一个关键问题的回答：生成的概率路径何时是自洽的？本文旨在建立自洽性的理论框架，将终点匹配问题转化为路径自洽性检验。

Method: 定义自洽生成路径为**可容许局部变分传输校正**的随机不动点。局部校正由随机变分传输算子定义，包含散度/几何项、能量项和结构约束三项组件。该框架涵盖正则化最优传输近端步骤、非OT散度、潜在核、对抗约束、因果离散核和终端一步映射。

Result: 提出**随机不动点路径残差（R-FPR）**度量实际生成路径与可容许局部校正之间的差距。证明了适定性、随机不动点存在性与吸引性、非压缩性存在性、残差到生成误差的界、经验残差集中性、代理扰动界、连续时间极限、算子级泛化及模型特定推论。

Conclusion: 该理论将终点匹配转化为路径自洽性检验，提供了残差控制原则，可用于诊断失败、正则化训练以及指导扩散、流、一步生成、VAE、GAN/WGAN和自回归生成器的自适应采样。

Original Abstract: Modern generative models often define an entire probability path from a simple prior to the data law, rather than only an endpoint map. Diffusion models follow stochastic denoising paths, flow matching learns transport fields, consistency and distillation methods compress paths into one or a few steps, adversarial models match terminal distributions, and VAEs generate through latent kernels. Existing unifying views mainly describe how such paths are constructed. We study a complementary question: when is a generated probability path self-consistent? We define a self-consistent generative path as a random fixed point of admissible local variational transport corrections. In this framework, a local correction is specified by a random variational transport operator combining a divergence or geometry term, an energy term, and a structural constraint. The framework contains random regularized optimal-transport proximal steps as a structured instance, while also allowing non-OT divergences, latent kernels, adversarial constraints, causal discrete kernels, and terminal one-step maps. The theory yields a random fixed-point path residual (R-FPR), which measures the gap between the actual generated path and an admissible local correction. We prove well-posedness, random fixed-point existence and attraction, non-contractive existence, residual-to-generation error bounds, empirical residual concentration, proxy perturbation bounds, continuous-time limits, and operator-level generalization with model-specific corollaries. The resulting theory turns endpoint matching into path self-consistency testing and provides a residual-control principle for diagnosing failures, regularizing training, and guiding adaptive sampling across diffusion, flow, one-step, VAE, GAN/WGAN, and autoregressive generators.

中文摘要翻译: 现代生成模型通常定义从简单先验到数据分布的完整概率路径，而不仅仅是端点映射。扩散模型遵循随机去噪路径，流匹配学习传输场，一致性和蒸馏方法将路径压缩为一步或几步，对抗模型匹配终端分布，VAE通过潜在核生成。现有统一视角主要描述这些路径是如何构建的。我们研究一个互补问题：生成的概率路径何时是自洽的？我们将自洽生成路径定义为可容许局部变分传输校正的随机不动点。在该框架中，局部校正由结合散度或几何项、能量项和结构约束的随机变分传输算子指定。该框架包含随机正则化最优传输近端步骤作为结构化实例，同时也允许非OT散度、潜在核、对抗约束、因果离散核和终端一步映射。该理论提出了随机不动点路径残差（R-FPR），用于度量实际生成路径与可容许局部校正之间的差距。我们证明了适定性、随机不动点存在性与吸引性、非压缩性存在性、残差到生成误差的界、经验残差集中性、代理扰动界、连续时间极限、算子级泛化以及模型特定的推论。所得理论将终点匹配转化为路径自洽性检验，并提供了残差控制原则，用于诊断失败、正则化训练以及指导跨扩散、流、一步生成、VAE、GAN/WGAN和自回归生成器的自适应采样。

</details>


### [404] [From inverse problems to neural operators: prediction, mechanism, and generalization of data-driven models](https://arxiv.org/abs/2606.08956)
*Conor Rowan*

Main category: cs.LG

TL;DR: 本文从哲学视角出发，对科学机器学习中多种数据驱动的物理系统建模策略（如稀疏辨识、神经常微分方程、神经算子等）进行了统一分析，提出它们共享共同的结构——仅在所假设的输入-输出关系模型类上有所不同，并指出只有某些模型能实现机制发现和泛化。

<details>
  <summary>Details</summary>

Motivation: 科学机器学习领域涌现了多种物理系统建模策略（稀疏辨识、神经常微分方程、神经算子等），但这些方法之间的关系尚不明确，缺乏统一的分类学框架，作者希望厘清它们之间的内在联系与适用场景。

Method: 借鉴科学模型的哲学文献，分析不同建模策略的共同结构；结合机制发现（mechanism discovery）的哲学思想，论证物理系统数据源于简约微分方程的解，进而提出只有某些模型具备机制发现和泛化能力。

Result: 论证了多种模型类型具有共同结构，仅在其定义的输入-输出关系的假设模型类上存在差异；指出基于简约微分方程的模型才可能实现机制发现和泛化。

Conclusion: 本文的分析旨在统一表面上不同的建模策略，并为它们各自合适的应用场景提供洞见。

Original Abstract: Scientists have historically relied on mathematical models based on differential equations to relate system inputs -- forces, fluxes, or heat sources -- to outputs, such as displacement, velocity, concentration, and temperature. These models rely on deep domain knowledge to determine the form of the governing differential equation, which is then calibrated with data by solving an inverse problem. In recent years, the field of Scientific Machine Learning has introduced a variety of alternative modeling strategies for physical systems. A method called Sparse Identification of Nonlinear Dynamics learns the governing equation as a sparse linear combination of terms in a user-defined library. Neural Ordinary Differential Equations construct the governing equation by taking in the state and its derivatives at the input layer of a neural network. Entirely foregoing the modeling framework of differential equations, neural operators directly learn a non-linear mapping between the system inputs and outputs. From inverse problems to neural operators, all of these modeling strategies can be conceptualized as data-driven machinery to predict a system's response over a range of inputs. It is then natural to wonder how exactly these various strategies relate to each other, and whether they can be neatly taxonomized. Drawing from the philosophical literature on scientific models, we argue that many model types have a common structure, differing only in the assumed model class of the input-output relation they define. Connecting to philosophical ideas on mechanism, and arguing that data from physical systems arises from solutions to parsimonious differential equations, we propose that only certain models are capable of mechanism discovery, and thus generalization. Our analysis is intended to unite apparently disparate modeling strategies and provide insight into their appropriate use cases.

中文摘要翻译: 历史上，科学家们一直依赖基于微分方程的数学模型来将系统输入（力、通量或热源）与输出（位移、速度、浓度和温度）联系起来。这些模型需要深厚的领域知识来确定控制微分方程的形式，然后通过求解逆问题用数据进行校准。近年来，科学机器学习领域引入了多种用于物理系统的替代建模策略。一种称为非线性动力学稀疏辨识的方法，以用户定义库中项的稀疏线性组合来学习控制方程。神经常微分方程通过在神经网络输入层接收状态及其导数来构建控制方程。神经算子则完全放弃了微分方程的建模框架，直接学习系统输入与输出之间的非线性映射。从逆问题到神经算子，所有这些建模策略都可以概念化为数据驱动的机制，用于预测系统在一系列输入下的响应。自然地，我们想知道这些不同策略之间究竟如何相互关联，以及它们是否可以被清晰地分类。借鉴科学模型的哲学文献，我们认为许多模型类型具有共同的结构，仅在其定义的输入-输出关系的假设模型类上有所不同。结合关于机制的哲学思想，并论证物理系统的数据来源于简约微分方程的解，我们提出只有某些模型能够实现机制发现，从而具备泛化能力。我们的分析旨在统一表面上不同的建模策略，并为其合适的应用场景提供洞见。

</details>


### [405] [C$^3$ache: Accelerating World Action Models with Cross Inference Chunk Cache](https://arxiv.org/abs/2606.08962)
*Weisen Zhao,Lam Nguyen,Zhicong Lu,Yuzhang Shang*

Main category: cs.LG

TL;DR: 本文提出C$^3$ache方法，通过跨推理块缓存和复用去噪残差，加速世界动作模型（WAM）的推理过程，在Fast-WAM骨干上实现最高2.5倍加速且任务成功率几乎无损。

<details>
  <summary>Details</summary>

Motivation: 现有加速方法仅关注单个推理块内部的去噪轨迹计算冗余，忽视了跨推理块之间存在的显著冗余——当机器人执行平滑行为时，相邻块在同一去噪步骤的残差高度相关。

Method: 提出C$^3$ache，一种无需训练的方法，在推理过程中跨不同块缓存并复用相同去噪步骤的残差计算结果，从而避免重复计算。

Result: 在Fast-WAM骨干上的基准测试中，C$^3$ache实现了最高2.5倍的总推理时间加速，同时任务成功率的下降可忽略不计。

Conclusion: C$^3$ache有效利用了跨推理块的残差冗余，是一种简单高效的训练无关加速方案，显著降低了WAM的计算开销。

Original Abstract: World Action Models (WAMs) generalize better than standard Vision-Language-Action (VLA) policies to novel motions and environments, because a video-modeling objective lets them learn from abundant unlabeled video rather than scarce labeled robot demonstrations. This generalization is computationally expensive. To complete a task, a WAM runs over multiple inference chunks, and each chunk requires a costly denoising process. Existing acceleration methods reduce this cost by caching and reusing computation within a single chunk's denoising trajectory. Our empirical analysis reveals a substantial source of redundancy they overlook: redundancy across chunks. When a robot executes a smooth behavior, the residuals computed at a given denoising step are strongly correlated from one chunk to the next. We introduce C$^3$ache, a training-free method that caches and reuses these residuals across inference chunks at the same denoising step. Experiments on benchmarks with a Fast-WAM backbone show that C$^3$ache achieves up to a $2.5\times$ speedup in total wall-clock inference time, with negligible degradation in task success rate.

中文摘要翻译: 世界动作模型（WAMs）相比标准的视觉-语言-动作（VLA）策略，在泛化到新动作和新环境方面表现更优，因为视频建模目标使其能够从丰富的无标签视频中学习，而非依赖稀缺的带标签机器人演示数据。然而，这种泛化能力的计算代价高昂。为了完成任务，WAM需要运行多个推理块（inference chunks），每个块都需要一个昂贵的去噪过程。现有的加速方法通过在一个块内部的去噪轨迹中缓存和复用计算来降低这一成本。我们的实证分析揭示了一个它们忽视的显著冗余来源：跨块的冗余。当机器人执行平滑行为时，在给定去噪步骤计算得到的残差，从一个块到下一个块是高度相关的。我们引入了C$^3$ache，一种无需训练的方法，它在推理过程中跨不同块、在同一去噪步骤处缓存并复用这些残差。在基于Fast-WAM骨干的基准测试上的实验表明，C$^3$ache在总端到端推理时间上实现了最高2.5倍的加速，同时任务成功率的下降可忽略不计。

</details>


### [406] [Online Learning with Recency: Algorithms for Sliding-window Streaming Multi-armed Bandits](https://arxiv.org/abs/2606.08977)
*Vladimir Braverman,Chen Wang,Liudeng Wang,Samson Zhou*

Main category: cs.LG

TL;DR: 本文研究单遍滑动窗口流式多臂老虎机问题，在有限内存（存储臂数）约束下进行纯探索和遗憾最小化，为两者提供了全面的理论分析和实验验证。

<details>
  <summary>Details</summary>

Motivation: 受在线学习中近期效应（recency effect）的启发，将滑动窗口引入流式多臂老虎机模型，使其更贴近实际中只关注最近数据的场景。

Method: 理论上分析了纯探索和遗憾最小化两类问题：对纯探索，证明了亚线性内存下找到最优臂是困难的，但找到近似最优臂存在高效算法；对遗憾最小化，定义了新的遗憾概念并给出了单遍算法的内存-遗憾权衡界限。

Result: 纯探索方面，发现最优臂识别在亚线性内存下不可行，而近似最优臂识别存在有效算法；遗憾最小化方面，给出了尖锐的内存-遗憾权衡关系；实验验证了样本量、遗憾与内存之间的权衡。

Conclusion: 滑动窗口流式MAB模型在有限内存约束下，纯探索的难度取决于对精度的要求，遗憾最小化则存在明确的内存-遗憾权衡，理论结果与实验一致。

Original Abstract: Motivated by the recency effect in online learning, we study algorithms for single-pass *sliding-window streaming multi-armed bandits (MABs)* in this paper. In this setting, we are given $n$ arms with unknown sub-Gaussian reward distributions and a parameter $W$. The arms arrive in a single-pass stream, and only the most recent $W$ arms are considered valid. The algorithm is required to perform pure exploration and regret minimization with limited memory, defined as the number of stored arms. The model is a natural extension of the streaming multi-armed bandits model (without the sliding window) that has been extensively studied in recent years. We provide a comprehensive analysis of both the pure exploration and regret minimization problems with the model. For pure exploration, we prove that finding the best arm is hard with sublinear memory while finding an approximate best arm admits an efficient algorithm. For regret minimization, we explore a new notion of regret and give sharp memory-regret trade-offs for any single-pass algorithm. We complement our theoretical results with experiments, demonstrating the trade-offs between sample, regret, and memory.

中文摘要翻译: 受在线学习中近期效应的启发，本文研究了单遍滑动窗口流式多臂老虎机（MABs）算法。在该设定中，给定n个具有未知亚高斯奖励分布的臂和一个参数W。臂以单遍流的方式到达，仅最近W个臂被视为有效。算法需要在有限内存（定义为存储的臂数）约束下进行纯探索和遗憾最小化。该模型是近年来被广泛研究的流式多臂老虎机模型（无滑动窗口）的自然扩展。我们对该模型下的纯探索和遗憾最小化问题进行了全面分析。对于纯探索，我们证明在亚线性内存下找到最优臂是困难的，而找到近似最优臂则存在高效算法。对于遗憾最小化，我们探索了一种新的遗憾概念，并给出了任何单遍算法的尖锐内存-遗憾权衡关系。我们通过实验补充了理论结果，展示了样本量、遗憾与内存之间的权衡。

</details>


### [407] [Heterophily-Aware Adaptive Knowledge Distillation for Hypergraph Neural Networks](https://arxiv.org/abs/2606.08978)
*Joohee Cho,David Yoon Suk Kang,Yunyong Ko*

Main category: cs.LG

TL;DR: 提出了一种名为HADES的异质性感知自适应蒸馏方法，通过量化节点异质性来调节教师知识迁移，从而提升超图神经网络轻量级学生模型的性能，甚至使学生模型超越教师模型且推理速度提升高达12.3倍。

<details>
  <summary>Details</summary>

Motivation: 观察到超图神经网络在同质性和异质性节点上的预测性能存在显著差异，在通过语义多样化的超边连接的异质性节点上表现较差，说明教师知识的可靠性因节点而异，因此需要一种异质性感知的自适应蒸馏方法。

Method: HADES方法通过量化节点的异质性程度，将其作为教师可靠性的估计指标，从而在知识蒸馏过程中动态调节教师知识向学生模型的迁移强度。

Result: 在真实世界超图上的实验结果表明，HADES在不同超图神经网络教师模型和不同蒸馏目标下均能稳定提升学生模型性能；在许多情况下，学生模型不仅超越了教师模型的预测性能，同时推理速度提升了高达12.3倍。

Conclusion: HADES通过异质性感知的自适应蒸馏策略有效解决了超图神经网络知识蒸馏中教师知识可靠性不均的问题，实现了学生模型在保持甚至超越教师性能的同时大幅降低推理成本。

Original Abstract: Hypergraph knowledge distillation aims to retain the predictive performance of a hypergraph neural network (HNN) teacher while reducing inference costs through a lightweight student model. In this work, we observe that HNNs exhibit substantially lower prediction performance on heterophilic nodes connected through semantically diverse hyperedges, indicating that the reliability of teacher knowledge varies across nodes. Motivated by this observation, we propose HADES, a heterophily-aware adaptive distillation method for hypergraph neural networks. HADES quantifies node heterophily and leverages it as an estimate of teacher reliability to modulate the transfer of teacher knowledge during distillation. Experimental results on real-world hypergraphs demonstrate that HADES consistently improves student performance across different HNN teachers and distillation objectives. In many cases, the resulting student models surpass the predictive performance of their teachers while achieving up to 12.3 times faster inference.

中文摘要翻译: 超图知识蒸馏旨在保留超图神经网络（HNN）教师的预测性能，同时通过轻量级学生模型降低推理成本。在这项工作中，我们观察到HNN在通过语义多样的超边连接的异质性节点上的预测性能显著较低，这表明教师知识的可靠性在不同节点之间存在差异。受此观察启发，我们提出了HADES——一种面向超图神经网络的异质性感知自适应蒸馏方法。HADES量化节点异质性，并将其作为教师可靠性的估计指标，以在蒸馏过程中调节教师知识的迁移。在真实世界超图上的实验结果表明，HADES在不同HNN教师模型和蒸馏目标下均能稳定提升学生模型的性能。在许多情况下，所得学生模型超越了教师模型的预测性能，同时实现了高达12.3倍的推理加速。

</details>
