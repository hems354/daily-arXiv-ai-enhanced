<div id=toc></div>

# Table of Contents

- [cs.LG](#cs.LG) [Total: 8]


<div id='cs.LG'></div>

# cs.LG [[Back]](#toc)

### [1] [On Adversarial Vulnerability of Vision-Language Models through the Lens of Intermediate Spectral Subspaces](https://arxiv.org/abs/2607.07375)
*Chethan Krishnamurthy Ramanaik,Tobias Callies,Michael Hecht,Eirini Ntoutsi*

Main category: cs.LG

TL;DR: 本文提出了一种基于谱子空间引导的自盒攻击方法（SSGRA），通过对齐中间表示与底部右奇异向量子空间，揭示了Transformer视觉-语言模型中线性层谱结构与对抗脆弱性之间的关联，并在攻击效果上超越了现有基线方法。

<details>
  <summary>Details</summary>

Motivation: 现有对抗脆弱性研究多从决策边界几何、特征鲁棒性、输入-输出雅可比矩阵及逆问题不稳定性等角度展开，但中间线性变换的谱结构作为对抗脆弱性的潜在机制尚未被探索。本文旨在填补这一空白。

Method: 提出白盒谱子空间引导攻击（SSGRA），通过将中间表示对齐到底部右奇异向量张成的子空间，利用线性层的可解释谱分解来生成对抗样本。

Result: 实验表明SSGRA在攻击效果上优于现有基线方法，同时提供了对视觉-语言模型中对抗脆弱性的谱解释。

Conclusion: SSGRA不仅提升了攻击有效性，还从谱角度揭示了VLM的对抗脆弱性成因，为提升模型鲁棒性提供了新的见解。

Original Abstract: Adversarial vulnerability in deep neural networks (DNNs) has been studied from the perspectives of decision-boundary geometry, feature robustness, input-output Jacobians, and the instability of inverse problems. Here, we focus on the spectral structure of intermediate linear transformations that propagate information through modern DNNs, an unexplored mechanism of adversarial vulnerability. Specifically, we investigate transformer-based vision-language models, whose linear layers admit interpretable spectral decompositions and whose widespread adoption makes understanding their robustness increasingly important. We propose a white-box spectral-subspace-guided attack (SSGRA) that aligns intermediate representations with the subspace spanned by the bottom right singular vectors. Our experiments show improved attack effectiveness over existing baselines. In addition, SSGRA offers a spectral interpretation of adversarial vulnerability in VLMs, providing insights for improving their robustness.

中文摘要翻译: 深度神经网络（DNN）的对抗脆弱性已从决策边界几何、特征鲁棒性、输入-输出雅可比矩阵以及逆问题不稳定性等角度得到了广泛研究。本文聚焦于信息在现代DNN中传播所依赖的中间线性变换的谱结构，这是一个尚未被探索的对抗脆弱性机制。具体而言，我们研究基于Transformer的视觉-语言模型（VLM），其线性层具有可解释的谱分解，且其广泛采用使得理解其鲁棒性日益重要。我们提出了一种白盒谱子空间引导攻击（SSGRA），该方法将中间表示与由底部右奇异向量张成的子空间对齐。实验表明，我们的方法在攻击效果上优于现有基线方法。此外，SSGRA为VLM中的对抗脆弱性提供了谱角度的解释，为改进其鲁棒性提供了见解。

</details>


### [2] [TimEE: End-to-end Time Series Classification via In-Context Learning](https://arxiv.org/abs/2607.07500)
*Jaris Küken,Shi Bin Hoo,Martin Mráz,Frank Hutter,Lennart Purucker*

Main category: cs.LG

TL;DR: 本文提出TimEE，一个仅4.5M参数的基础模型，通过元学习在合成时序分类任务上预训练，实现端到端的上下文学习时序分类。无需在真实数据上训练，即可在UCR基准上达到SOTA性能（ROC AUC第一，准确率第三）。

<details>
  <summary>Details</summary>

Motivation: 现有时序分类采用两阶段范式（特征编码器+分类器），存在三个问题：表示学习与分类目标解耦导致优化不协同、需要逐数据集训练、推理时无法利用标签信息。作者期望通过上下文学习实现端到端、无需逐数据集训练的时序分类模型。

Method: 基于Prior-Data Fitted Networks (PFN)框架，在纯合成的时序分类任务上进行元训练。每个合成任务包含由结构化分布偏移生成的不同类别的时间序列。模型TimEE仅4.5M参数，输入标注的支持集和查询时序后，单次前向传播直接输出类别分布，无需逐数据集训练。

Result: 在UCR基准上，TimEE在ROC AUC指标上排名第一，在准确率上排名第三，超越了包括基础模型和监督深度学习基线在内的所有对比方法。这是首个仅通过合成数据预训练就在UCR基准上达到SOTA性能的模型。

Conclusion: 端到端上下文学习结合合成先验是时序分类中一个极具潜力且未被充分探索的方向。未来可通过扩展模型规模、改进先验设计和更丰富的生成机制进一步提升性能。

Original Abstract: Time series classification (TSC) is dominated by a two-stage paradigm: train a feature encoder -- either from scratch on the target dataset or via pretraining on large corpora -- and then fit a task-specific classifier on top. While effective, this decoupling optimizes representation learning independently of the classification objective, requires per-dataset training, and prevents the model from exploiting label information during inference. We introduce TimEE, a 4.5M-parameter foundation model for end-to-end TSC via in-context learning. Given a labeled support set and a query time series, TimEE directly outputs a predicted class distribution in a single forward pass with no per-dataset training required. Following the prior-data fitted network (PFN) framework, TimEE is meta-trained exclusively on synthetic TSC tasks, where each task contains time series with distinct class identities arising from structured distributional shifts in the generative process. Despite seeing no real time series during pre-training, TimEE ranks first in ROC AUC (and third on accuracy) on the UCR benchmark among all compared methods, which include both foundation models and supervised deep learning baselines. To our knowledge, TimEE is the first purely synthetic-pretrained model to reach state-of-the-art performance on the UCR benchmark. These results establish end-to-end ICL with synthetic priors as a compelling, largely unexplored direction for TSC, with scaling, prior design, and richer generation mechanisms as natural avenues for improvement. Code is publicly available at http://github.com/automl/timee.

中文摘要翻译: 时序分类（TSC）目前主要采用两阶段范式：先训练一个特征编码器（无论是在目标数据集上从头训练还是通过大规模语料库预训练），再在其上拟合一个任务特定的分类器。尽管有效，但这种解耦方式使得表示学习独立于分类目标进行优化，需要逐数据集训练，并且阻止了模型在推理时利用标签信息。我们提出了TimEE，一个仅4.5M参数的基础模型，通过上下文学习实现端到端的时序分类。给定一个标注的支持集和一个查询时序，TimEE在单次前向传播中直接输出预测的类别分布，无需逐数据集训练。遵循先验数据拟合网络（PFN）框架，TimEE仅在合成的时序分类任务上进行元训练，其中每个任务包含由生成过程中结构化分布偏移产生的具有不同类别标识的时间序列。尽管在预训练过程中从未见过真实时序数据，TimEE在UCR基准上的ROC AUC指标中排名第一（准确率中排名第三），超越了包括基础模型和监督深度学习基线在内的所有对比方法。据我们所知，TimEE是首个仅通过合成数据预训练就在UCR基准上达到最先进性能的模型。这些结果表明，结合合成先验的端到端上下文学习是时序分类中一个极具吸引力且尚未被充分探索的方向，而模型扩展、先验设计和更丰富的生成机制是其自然的改进途径。代码已在http://github.com/automl/timee公开。

</details>


### [3] [Single-Rollout Asynchronous Optimization for Agentic Reinforcement Learning](https://arxiv.org/abs/2607.07508)
*Zhenyu Hou,Yujiang Li,Jie Tang,Yuxiao Dong*

Main category: cs.LG

TL;DR: 提出SAO（单 rollout 异步优化）方法，通过单 rollout 采样和严格双面 token 级裁剪策略，解决异步 RL 训练大语言模型时的稳定性和 off-policy 问题，在 agentic 编程和推理基准上持续优于 GRPO 及其变体。

<details>
  <summary>Details</summary>

Motivation: 现有异步 RL 系统虽然提高了吞吐量，但训练稳定性和任务有效性未被充分探索。特别是 GRPO 框架中的组级采样不适用于异步 agentic 训练，存在 off-policy 和优化不稳定等挑战。

Method: (1) 用单 rollout 采样替代组级采样，每个 prompt 仅使用一个 rollout 来减少 off-policy 效应并提升泛化能力，并辅以实际的价值模型训练设计；(2) 引入严格的双面 token 级裁剪策略来提升优化稳定性。

Result: SAO 能稳定训练一千步，在 SWE-Bench Verified、BeyondAIME、IMOAnswerBench 等 agentic 编程和推理基准上持续优于 GRPO 及其变体；在模拟在线学习场景中尤其有效；已成功部署到 GLM-5.2 (750B-A40B) 模型的 agentic RL 训练流程中。

Conclusion: 单 rollout RL 在异步 agentic 训练中比组级采样更有效，SAO 提供了一种稳定且高效的异步 RL 训练方案，适用于大规模语言模型的后训练。

Original Abstract: Reinforcement learning (RL) is becoming increasingly important for post-training large language models (LLMs). Previous RL pipelines for LLMs were mostly synchronous and batch-interleaved, which is inefficient for long-horizon agentic tasks. Recently, asynchronous RL has emerged as a more efficient alternative by updating the model as rollouts arrive. However, existing asynchronous RL systems often emphasize throughput, while leaving training stability and task effectiveness largely underexplored. For example, a key challenge is that group-wise sampling in the widely-used GRPO framework does not naturally fit asynchronous agentic training. In this paper, we present Single-rollout Asynchronous Optimization (SAO) to address the stability and off-policy challenges in asynchronous RL. To reduce off-policy effects and improve generalization, we replace group-wise sampling with single-rollout sampling, that is, using one rollout per prompt. We further improve this single-rollout strategy with practical value-model training designs. To improve optimization stability, we introduce a strict double-side token-level clipping strategy. SAO is able to train stably for one thousand steps and consistently outperform GRPO and its variants on agentic coding and reasoning benchmarks, such as SWE-Bench Verified, BeyondAIME, and IMOAnswerBench. We also demonstrate that single-rollout RL is particularly effective in a simulated online learning setting, where the model must adapt to changing evolving environments. To this end, SAO is successfully deployed in the agentic RL pipeline for training the open GLM-5.2 model (750B-A40B).

中文摘要翻译: 强化学习（RL）在后训练大型语言模型（LLM）中变得日益重要。以往针对LLM的RL训练流程大多是同步和批交错的，这在处理长程智能体任务时效率低下。最近，异步RL作为一种更高效的替代方案出现，它允许在rollout到达时即时更新模型。然而，现有的异步RL系统往往强调吞吐量，而对训练稳定性和任务有效性的探索不足。例如，一个关键挑战是广泛使用的GRPO框架中的组级采样并不自然地适配异步智能体训练。在本文中，我们提出了单 rollout 异步优化（SAO）方法，以解决异步RL中的稳定性和离策略挑战。为减少离策略效应并提升泛化能力，我们以单 rollout 采样替代组级采样，即每个提示仅使用一个rollout。我们通过实用的价值模型训练设计进一步改进了这一单 rollout 策略。为提升优化稳定性，我们引入了一种严格的双面 token 级裁剪策略。SAO能够稳定训练一千步，并在SWE-Bench Verified、BeyondAIME和IMOAnswerBench等智能体编程与推理基准上持续优于GRPO及其变体。我们还证明了单 rollout RL在模拟在线学习场景中尤其有效，在该场景中模型必须适应不断变化的环境。基于此，SAO已成功部署到训练开源GLM-5.2模型（750B-A40B）的智能体RL流水线中。

</details>


### [4] [Fast Rates for Semi-Supervised Learning via Data-Augmentation Graph Regularization](https://arxiv.org/abs/2607.07513)
*Adam M. Oberman*

Main category: cs.LG

TL;DR: 本文为自监督学习的标签效率提供了理论解释，证明了在数据增强诱导的相似图上进行图拉普拉斯正则化学习可实现 $O(1/n_L)$ 的快速传递率，优于监督学习的 $O(1/\sqrt{n_L})$，并将误差界与增强质量直接关联。

<details>
  <summary>Details</summary>

Motivation: 自监督学习在少量标注样本下能达到与全监督相当的精度，但其标注效率缺乏理论解释。

Method: 将数据增强诱导的未标注数据相似图上的下游学习建模为图拉普拉斯正则化学习；将Johnson和Zhang的留一法稳定性工具扩展到增强图上，无需极限分析的不切实际假设；使用简化的损失函数，去掉投影头、负样本和正交性开销。

Result: 证明了传递率为 $O(1/n_L)$，优于监督学习的 $O(1/\sqrt{n_L})$；期望误差不超过 $C/n_L + R_{\mathrm{DA}}(y)$，其中 $R_{\mathrm{DA}}(y)$ 是跨越标签边界的增强的图割质量；简化损失在无限数据极限下仍能恢复 top-$K$ 理想特征（增强核特征空间）。

Conclusion: 自监督学习的标签效率源于数据增强图的拉普拉斯正则化结构，好的增强使得少量标签即可达到高精度，理论曲线与实际精度-标签数曲线吻合，而不仅仅是界定泛化差距。

Original Abstract: Self-supervised learning matches supervised accuracy from a fraction of the labels, but the labeled-sample efficiency behind this has lacked a theoretical explanation. We provide one. Data augmentation induces a similarity graph on the unlabeled data, so downstream learning on that graph is graph-Laplacian-regularized learning. We prove a fast transductive rate, $O(1/n_L)$ in the number of labels, in place of the supervised $O(1/\sqrt{n_L})$, by carrying the leave-one-out stability apparatus of Johnson and Zhang (JMLR 2007) over to the augmentation graph, and without the unrealistic assumptions of limit-based analyses (exact kernel, generalizing features). The bound makes augmentation quality explicit: the expected error is at most $C/n_L + R_{\mathrm{DA}}(y)$, where the data-augmentation alignment error $R_{\mathrm{DA}}(y)$ is the graph-cut mass of augmentations that cross a label boundary, so good augmentations let few labels suffice. The analysis uses a streamlined loss that drops the projector, negative-sample, and orthogonality overhead of standard objectives yet still recovers the top-$K$ ideal features in the infinite-data limit, the augmentation-kernel eigenspace studied by Zhai et al. The result explains the observed accuracy-versus-label-count curve rather than only bounding a generalization gap.

中文摘要翻译: 自监督学习在仅使用少量标注样本的情况下就能达到与全监督相当的精度，但这一现象背后的标注样本效率一直缺乏理论解释。本文提供了一个理论解释。数据增强在未标注数据上诱导出一个相似图，因此该图上的下游学习本质上是图拉普拉斯正则化学习。通过将Johnson和Zhang（JMLR 2007）的留一法稳定性工具推广到增强图上，且无需极限分析的不切实际假设（精确核、泛化特征），我们证明了快速的传递率 $O(1/n_L)$（$n_L$ 为标签数），优于监督学习的 $O(1/\sqrt{n_L})$。该界使增强质量显式化：期望误差不超过 $C/n_L + R_{\mathrm{DA}}(y)$，其中数据增强对齐误差 $R_{\mathrm{DA}}(y)$ 是跨越标签边界的增强操作的图割质量，因此好的增强只需少量标签即可。分析采用了一种精简的损失函数，去掉了标准目标中的投影头、负样本和正交性开销，但在无限数据极限下仍能恢复 top-$K$ 理想特征——即Zhai等人研究的增强核特征空间。该结果解释了观察到的精度-标签数曲线，而不仅仅是界定泛化差距。

</details>


### [5] [Gradient-free Riemannian Langevin Sampler](https://arxiv.org/abs/2607.07519)
*Ricardo Baptista,Olivier Zahm*

Main category: cs.LG

TL;DR: 提出了一种无需梯度计算的黎曼朗之万采样器（GRiLS），通过黎曼度量重塑局部几何结构，促进多模态分布间的混合，避免模式被困。

<details>
  <summary>Details</summary>

Motivation: 标准MCMC方法在多模态概率分布采样中易出现混合不良和模式被困问题，且许多复杂目标密度无法获取梯度信息。

Method: 提出GRiLS算法：引入黎曼度量重塑局部几何以促进模态间转移；利用交互粒子集合估计目标密度的均值和协方差；无需目标密度的梯度计算。

Result: 在多模态基准测试上，GRiLS相比现有基于梯度和无梯度的MCMC方法实现了更优的混合性能。

Conclusion: GRiLS作为一种无梯度MCMC算法，在无需梯度评估的情况下有效改善了多模态分布的采样混合效率，适用于梯度不可用或不可行的复杂计算场景。

Original Abstract: We address the problem of efficiently sampling multimodal probability distributions, where standard Markov Chain Monte Carlo methods often suffer from poor mixing and mode trapping. To mitigate these issues, we propose Gradient-free Riemannian Langevin Sampler (GRiLS), a novel proposal that improves exploration without requiring gradient evaluations of the target density. Our approach introduces a Riemannian metric which reshapes the local geometry in order to facilitate transitions across modes. The resulting gradient-free MCMC algorithm is particularly suitable for complex, computationally expensive targets where derivatives are unavailable or impractical. The GRiLS proposal requires knowing the mean and covariance of the target density, which we estimate using an ensemble of interacting particles. Empirical results on multimodal benchmarks demonstrate that GRiLS achieves improved mixing compared to existing gradient-based and gradient-free MCMC approaches.

中文摘要翻译: 我们研究了高效采样多模态概率分布的问题，其中标准马尔可夫链蒙特卡洛方法常因混合不良和模式被困而表现不佳。为缓解这些问题，我们提出了无梯度黎曼朗之万采样器（GRiLS），一种无需目标密度梯度评估即可改善探索能力的新型提议机制。我们的方法引入黎曼度量来重塑局部几何结构，从而促进模态间的转移。由此产生的无梯度MCMC算法特别适用于梯度不可用或不可行的复杂、计算昂贵的目标分布。GRiLS提议需要知道目标密度的均值和协方差，我们通过一组交互粒子进行估计。在多模态基准测试上的实验结果表明，与现有基于梯度和无梯度的MCMC方法相比，GRiLS实现了更优的混合性能。

</details>


### [6] [Avoiding unsafe sets when training with Langevin Dynamics](https://arxiv.org/abs/2607.07538)
*Adam M. Oberman*

Main category: cs.LG

TL;DR: 该论文研究了带噪声梯度下降训练轨迹进入失败区域的概率，针对强凸损失函数给出了三种上界，并指出轨迹瞬态膨胀由失败区域的几何形状而非仅由全局谱隙决定。

<details>
  <summary>Details</summary>

Motivation: 在噪声梯度下降训练中，轨迹可能意外进入失败区域，现有理论主要关注稳态平衡概率，缺乏对训练过程中瞬态行为的严格刻画，尤其是失败区域形状对轨迹概率的影响尚未被充分理解。

Method: 将带噪声梯度下降建模为过阻尼朗之万动力学，针对d维光滑强凸损失函数，使用全局谱隙m和局部松弛率（通过中心化指示函数的谱测度定义）来推导轨迹概率的上界，并结合最大原理进行封顶。

Result: (1) 平衡态下失败区域的质量π(A_H)在d上指数小；(2) 无形状依赖的界ν_t(A_H) ≤ π(A_H)(1+√(χ₀²/π(A_H)) e^{-mt})，表明经过O(d)的烧入时间后松弛到静态值的两倍；(3) Ornstein-Uhlenbeck例子显示平衡壳的角切片可瞬态膨胀指数d倍；(4) 局部松弛率对几何隔离区域高于全局速率，成比例缩短烧入时间，且最大原理可统一封顶轨迹概率。

Conclusion: 强凸性决定了训练松弛的速度，但失败区域的几何形状决定了轨迹是否会在此区域凸起；仅靠全局谱隙无法刻画瞬态行为，需引入局部松弛率来更精确地控制轨迹概率。

Original Abstract: Training a model with noisy gradient descent can be idealized as overdamped Langevin dynamics on the loss landscape, and a natural safety question is to bound the probability $ν_t(\mathcal{A}_H) = \mathbb{P}(Q_t \in \mathcal{A}_H)$ that the trajectory lies in a designated failure region $\mathcal{A}_H$. We study this for a smooth, strongly convex loss in $d$ dimensions and a failure region separated from the minimizer by an energy gap. Three bounds emerge. At the end of training, the equilibrium mass $π(\mathcal{A}_H)$ is exponentially small in $d$, with a complementary energy-barrier rate when the noise is small. Along the trajectory, a shape-free bound $ν_t(\mathcal{A}_H) \le π(\mathcal{A}_H)(1 + \sqrt{χ_0^2/π(\mathcal{A}_H)}\,e^{-mt})$ shows that the in-set probability relaxes to (twice) the static value after a burn-in time of order $d$, using only the global spectral gap $m$ of the loss. A worked Ornstein-Uhlenbeck example shows this burn-in is necessary: an angular slice of the equilibrium shell can transiently swell by a factor exponential in $d$, even though its equilibrium mass is tiny. To rule such swelling out we introduce a local relaxation rate attached to the failure region, defined through the spectral measure of its centered indicator rather than a Dirichlet-form Rayleigh quotient. For geometrically isolated regions this rate exceeds the global one, shrinking the burn-in proportionally, and combined with a maximum-principle ceiling it caps the trajectory probability uniformly in time. The picture is that strong convexity sets how fast training relaxes, but the shape of the unsafe set decides whether the trajectory bulges through it on the way home.

中文摘要翻译: 带噪声梯度下降训练模型可以理想化为损失景观上的过阻尼朗之万动力学，一个自然的安全问题是限制轨迹位于指定失败区域A_H内的概率ν_t(A_H)=ℙ(Q_t∈A_H)。本文针对d维光滑强凸损失函数以及一个与极小值点被能隙分隔的失败区域进行研究，得到了三个界。在训练结束时，平衡质量π(A_H)在d上指数小，且当噪声较小时具有互补的能量势垒速率。沿轨迹方向，一个无形状依赖的界ν_t(A_H) ≤ π(A_H)(1+√(χ₀²/π(A_H)) e^{-mt})表明，仅利用损失的全局谱隙m，在O(d)量级的烧入时间后，集合内概率松弛到静态值的两倍。一个经过充分研究的Ornstein-Uhlenbeck例子表明这种烧入是必要的：平衡壳的角切片可以瞬态膨胀指数d倍，尽管其平衡质量极小。为了排除这种膨胀，我们引入了与失败区域相关的局部松弛率，该速率通过其中心化指示函数的谱测度而非Dirichlet形式Rayleigh商来定义。对于几何隔离的区域，该速率超过全局速率，成比例地缩短烧入时间，并且与最大原理上界相结合，可统一地随时间限制轨迹概率。总体图景是：强凸性决定了训练松弛的速度，但不安全集合的形状决定了轨迹在返回途中是否会从该区域凸起。

</details>


### [7] [Collaborative Synthetic Data Generation for Knowledge Transfer in Federated Learning](https://arxiv.org/abs/2607.07565)
*Maximilian Andreas Hoefler,Karsten Mueller,Wojciech Samek*

Main category: cs.LG

TL;DR: FedKT-CSD 提出了一种基于神经图像压缩启发的一轮联邦学习框架，通过公开预训练自编码器构建共享潜空间，在保证严格差分隐私的同时实现轻量级通信，并在异构数据下达到甚至超越非隐私基线的性能。

<details>
  <summary>Details</summary>

Motivation: 现有的一轮联邦学习方法大多缺乏形式化的隐私保证，无法同时实现低通信开销、对数据异构的鲁棒性和严格的隐私保护。

Method: 利用公开预训练的自编码器作为共享潜空间；每个客户端仅需一次前向传播对私有数据进行编码，计算类别条件的潜变量统计量并发送给服务器；服务器通过安全聚合聚合统计量，添加校准的差分隐私噪声，然后解码生成合成数据集，用于训练全局模型及下游任务。

Result: 在多种数据集和异构性设置下，FedKT-CSD 在隐私约束下与非隐私基线竞争甚至超越其性能，并能扩展到大量客户端。

Conclusion: FedKT-CSD 通过精心设计的潜空间聚合与差分隐私机制，在单轮通信场景下成功兼顾了隐私、通信效率和模型质量，填补了 OSFL 中缺乏形式化隐私保证的空白。

Original Abstract: One-shot federated learning (OSFL) addresses the communication overhead of federated learning by limiting training to a single round, but doing so without sacrificing model quality is non-trivial, particularly when client data distributions diverge. Recent work has addressed this challenge by aggregating client knowledge on the server through the construction of transferable synthetic datasets or distillates. However, most of these methods lack formal privacy guarantees, leaving a gap in jointly achieving low communication, robustness to heterogeneity, and rigorous privacy. We propose FedKT-CSD (Federated Knowledge Transfer via Collaborative Synthetic Data), a framework inspired by neural image compression that closes this gap by leveraging publicly pretrained autoencoders as a shared latent space. Each client encodes its private data in a single forward pass, computes class-conditional latent statistics, and transmits these to the server. The server aggregates these statistics via secure aggregation, adds calibrated differential privacy noise, and decodes a synthetic dataset for training a global model and further downstream tasks. This design provides formal $(\varepsilon,δ)$-differential privacy by construction, while keeping client-side computation and communication lightweight. Despite operating under privacy constraints, FedKT-CSD is competitive with and even outperforms non-private baselines across diverse datasets and heterogeneity settings, and scales to a large number of clients. Our code is available at: https://github.com/an7123/FedKT-CSD

中文摘要翻译: 单轮联邦学习通过将训练限制在单个通信轮次中来降低通信开销，但在不牺牲模型质量的前提下做到这一点并非易事，尤其是当客户端数据分布存在分歧时。最近的工作通过构建可迁移的合成数据集或蒸馏物，在服务器端聚合客户端知识来应对这一挑战。然而，这些方法大多缺乏形式化的隐私保证，在同时实现低通信开销、对异构性的鲁棒性和严格隐私保护方面存在空白。我们提出 FedKT-CSD（基于协作合成数据的联邦知识迁移），这是一个受神经图像压缩启发的框架，通过利用公开预训练的自编码器作为共享潜空间来填补这一空白。每个客户端通过单次前向传播对其私有数据进行编码，计算类别条件的潜变量统计量，并将这些统计量传输给服务器。服务器通过安全聚合聚合这些统计量，添加经过校准的差分隐私噪声，然后解码生成一个合成数据集，用于训练全局模型和进一步的下游任务。该设计从构造上提供了形式化的 $(\varepsilon,\delta)$-差分隐私保障，同时保持客户端计算和通信的轻量化。尽管在隐私约束下运行，FedKT-CSD 在多种数据集和异构性设置下能够与非隐私基线竞争甚至超越其性能，并且能够扩展到大量客户端。我们的代码可在 https://github.com/an7123/FedKT-CSD 获取。

</details>


### [8] [ALER-TI: Aligned Latent Embedding Retrieval for Time Series Imputation](https://arxiv.org/abs/2607.07640)
*Xuan-Thong Truong,Trung-Kien Le,Tung Kieu,Thi-Thu Nguyen,Nhat-Hai Nguyen*

Main category: cs.LG

TL;DR: 提出ALER-TI，一种检索增强的时间序列插补框架，通过对齐潜在嵌入中的历史模式来补充局部上下文信息，提升缺失值重建效果。

<details>
  <summary>Details</summary>

Motivation: 现有时间序列插补方法主要依赖 corrupted 输入序列中的局部时间上下文，但在非平稳动态、弱时间相关性和稀疏模式的实际场景中，仅靠局部相邻观测难以可靠重建缺失值。

Method: 核心是潜在嵌入对齐(LEA)，通过在潜在空间中进行后验掩码，将历史候选嵌入与查询的缺失模式对齐，同时允许历史嵌入预计算和缓存以实现高效检索。ALER-TI是模型无关的，可通过轻量适配模块集成到各种插补骨干网络中。

Result: 在6个真实世界数据集、不同缺失率下的广泛实验表明，ALER-TI能够持续改进强基线模型，并在多种插补设置下增强鲁棒性。

Conclusion: ALER-TI通过检索历史模式有效弥补了局部上下文不足的问题，为时间序列插补提供了一种通用且鲁棒的检索增强框架。

Original Abstract: Deep learning has significantly advanced time series imputation, yet most existing architectures primarily rely on localized temporal context within the corrupted input sequence. This reliance can be limiting in real-world scenarios, where time series often exhibit non-stationary dynamics, weak temporal correlations, and infrequent patterns that are difficult to reconstruct from nearby observations alone. In this paper, we propose ALER-TI, Aligned Latent Embedding Retrieval for Time Series Imputation, a retrieval-augmented framework that explicitly leverages historical patterns to supplement degraded local context for more reliable missing-value reconstruction. The core of ALER-TI is Latent Embedding Alignment (LEA), which mitigates the representation mismatch between corrupted queries and complete historical candidates. By applying post-hoc masking in the latent space, LEA aligns candidates with the query's missingness pattern while allowing historical embeddings to be pre-computed and cached for efficient retrieval. ALER-TI is model-agnostic and can be integrated with various imputation backbones through a lightweight adaptation module. Extensive experiments on six real-world datasets under different missing rates demonstrate that ALER-TI consistently improves strong baseline models and enhances robustness across diverse imputation settings.

中文摘要翻译: 深度学习极大地推动了时间序列插补的发展，然而现有大多数架构主要依赖于受损输入序列内的局部时间上下文。这种依赖在实际场景中可能存在局限，因为时间序列常常表现出非平稳动态、弱时间相关性以及难以仅凭邻近观测重建的稀疏模式。本文提出ALER-TI（对齐潜在嵌入检索用于时间序列插补），这是一种检索增强框架，通过显式利用历史模式来补充退化的局部上下文，实现更可靠的缺失值重建。ALER-TI的核心是潜在嵌入对齐（LEA），它缓解了受损查询与完整历史候选之间表征不匹配的问题。通过在潜在空间中应用后验掩码，LEA在将历史嵌入与查询的缺失模式对齐的同时，允许历史嵌入被预计算和缓存以实现高效检索。ALER-TI是模型无关的，可通过轻量适配模块集成到各种插补骨干网络中。在六个真实世界数据集上、不同缺失率下的广泛实验表明，ALER-TI能够持续改进强基线模型，并在多种插补设置下增强鲁棒性。

</details>
